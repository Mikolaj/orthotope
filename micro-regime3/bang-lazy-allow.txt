# Candidates horde-ad's tools/bang-lazy-check.py prints over Main.hs that a
# reader has read, one a line as `Main.hs:NAME ARG  # reason`, read by the
# `bang shapes` step of checks.py. The reason is the point of an entry: one
# without a reason hides the very shape the checker exists to show. The step
# reports stale only an entry whose name and argument match no candidate, so
# a definition that moves or changes shape under an unchanged name is yours
# to notice: delete or re-read its entry then, and a new candidate fails the
# step until it is read and listed here or fixed.
Main.hs:mergeInto 1  # canonView's merge step, read 2026-09-14: the guard forces both fields where they are compared, and the pair passes to the result unopened otherwise
