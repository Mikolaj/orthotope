TEMPLATE for $R-pair.txt, which `--draft` fills as the next paragraph says.
KEEP EVERY [SAME] AND [PAIR'S] MARKER in the note you
write. A marked note shows at a glance which of its blocks would be a
COPYING ERROR to carry forward, which is the one thing a session cannot
tell by reading. Every pair here is hand-built and its note is filled
from the registration that declares its recipes (the one declaration
site, ruled 2026-09-19), so the note is written BEFORE either binary exists
-- which is also why this is a template and not a generator: what goes in
it is a person's, and only the slots are known in advance.

DO NOT WRITE THE NOTE FROM THIS FILE BY HAND. `./read-run.py --note
$PREV-pair.txt --draft $R --halves <basis>,<other>` prints the whole note,
and the blocks below are marked [SAME] or [PAIR'S]:
  [SAME]    the same for every pair, so the draft takes each from THIS
            FILE, with the run, the halves and the previous note's LAUNCH
            and RIDERS values put in -- the naming rule, the read-back
            commands, the launch and rider lines, the counts, the machine
            check, the gate declarations. What a pair adds to one of them
            is a [PAIR'S] block's to say.
  [PAIR'S]  written for this pair: the entry point, what the pair measures
            and prices, why the basis is this recipe, the two recipes, the
            registration and the roster. The draft carries the previous
            note's, the entry point excepted as a handover, under a
            `<yours>` line as a model; rewrite each and
            delete its line, run-status.sh's 2c counting the ones left.
            A model left standing is how a note comes to describe the run
            before it.

AND THE MARKERS SAY WHO WRITES, NOT WHO ACTS. The EXECUTING session reads
this note at run-list step 13 and the step says to read all of it, a
preparation's whole record, for the handful of blocks it acts on: the
HALVES, LAUNCH and RIDERS lines every script reads, the gate arms, and the fill-in rows the write-up transcribes. Mark those [EXEC] as
well as [SAME] or [PAIR'S] -- the two are orthogonal, one saying who wrote
the block and the other who runs on it -- so that step 13 can say read the
[EXEC] blocks and skim the rest. Added 2026-09-15, after Run 32's executing
session read the whole note at the step that told it to.

AND KEEP THE FIGURES OUT OF IT. A note is gitignored and goes with the
pair, so a session other than the one that wrote it cannot correct it:
Run 22's arms' session amended that run's registration in README.md at
ce8f5f7 and could not touch the note, which went on asserting a
superseded count until the preparing session came back to it. So state a
figure here only where this file is the ONLY place it lives -- the md5s,
the .text bytes, the fills, the counts a leg came out at -- and point at
README.md or the run's own file for everything else.

AND NO RUN RANGES IN A [PAIR'S] BLOCK: `as Runs 36 to 39` goes false the
moment the next run carries the block, and Run 40's preparation bumped them
by hand, block after block. Say `as the previous runs of this pair`, or name
the one run a claim is about.

The pair $R-<basis> and $R-<other>, Run NN's, written by hand YYYY-MM-DD
BEFORE either binary exists, which is the order the procedure asks for.

Nothing appends to this file but run-gate.sh.

ENTRY POINT FOR THE SESSION THAT RUNS THIS [PAIR'S]. It is a HANDOVER and
not a courtesy: a run is always two sessions, this file is the whole of
what crosses between them, and the session that reads it wrote none of it
and cannot ask you. So it goes here at the head, and it says, in this
order: which of the pre-run steps are SPENT and on what date; whether the
gate has run; what is
INHERITED and must not be repaid, naming each -- the gate, the smoke
sweep, the roster pass, the counts, anything else that cost machine time
-- and what is still OWED; and, last, WHAT THE PREPARATION LEARNED, which
is post-run step 9's four things met a day early -- a check that would
have caught an error, a computation improvised, a step skipped, a
capability found. Step 9 is the executing session's and that session was
not here, so this block is the only route those findings have OUT OF THE
PREPARATION. It is not the only place they end up, and this note going
with the pair loses none of them: step 9 says outright to read this block
before writing its own and to carry BOTH halves, and it feeds step 10,
the open list, which is committed. So the note being gitignored is not a
leak to fix -- do not propose committing it, mirroring this block into
the run file, or sending it to MARGINALIA. Ruled 2026-09-08, on the
question being asked once; it is written here so it is not asked again
every run. Those
lists are the interface; anything true of this run and in none of them is
lost. Then say outright whether anything here needs
a rebuild, since the offsets any registration is written against die
with one. INHERITED never reaches the binaries: both are built at step 2 of
this run's own preparation, never taken from a previous run, a probe, a
copy of the other half, or one binary run twice under two sets of flags.

AND SAY THAT NOTHING IN THE NOTE IS A REASON TO STOP [PAIR'S]. A preparation
that leaves a decision open states it plainly and states that it is not
a stop: Run 19's put a claims-retirement decision at this note's head in
the procedure's own stop-vocabulary, with *before the gate is paid*
beside it, and its first operator read it as a stop and lost the night
(2026-08-24). The test the procedure applies is whether the answer
changes what the machine does next; if it changes only the write-up, the
note says so here.

WHAT THIS PAIR MEASURES [PAIR'S]: one variable, named here, and nothing else. Say
what the halves differ in and what they do not. The recipe itself -- its
switches, flags and project file -- is spelled out in HOW EACH HALF IS BUILT
alone; the input blocks below each say whether their input MOVED, and none
restates the recipe.

THE BASIS IS $R-<basis> [PAIR'S] -- it publishes the big table, the bench counts are
read from it, every --in-place table comes from it, and it runs second. The
line below is the ONE place the two names are set: every script that takes
a run reads it through pair-halves.sh, and an environment that names them
differently is refused. Nothing is set in any script. The COMPARE line
under it names the earlier run every cross-run reading of this pair goes
against -- run-gate.sh's machine check, `--movement`, `--bridge` and
`--half-movers` all default to it -- and `--draft` writes the run it drafts
from; a ruling that picks another run changes this line and nothing else.
HALVES: basis=<basis> other=<other>
COMPARE: <previous run, as run<N>>

NAMING THE HALVES [SAME]: neither name may begin with the other's followed by a
hyphen, and a tag is one token of `[A-Za-z0-9_]`: install-tables.sh's
`$R-<basis>-*.json` glob and pair-halves.sh both require it. Why, and the
roll of every tag this chapter has used, are README's *Which two halves a
pair has*; the roll is not copied here.

WHY THE BASIS IS THIS RECIPE [PAIR'S], decided YYYY-MM-DD: a decision and not a
measurement, so the reason belongs here where the next session meets it.

WHAT THE PAIR PRICES [PAIR'S], said plainly: what a reader of the run should think
the figures are about.

REGISTERED BEFORE IT RAN [PAIR'S], in ONE place since the ruling of
2026-09-19: the PREDICTION, the pair, its half names and its recipes all
live in the open list, as `What Run NN is built to answer, registered
before it runs` at README.md#what-is-open, while the PREVIOUS run's own
file, under `What the next run compares against`, carries the reference
the run reads against and points at that entry as `[registered
<date>][open]`, restating nothing. A note that names only the section
sends the next session to a place its pair and its prediction are not in.
  A registration moves house once, at post-run step 5, from the open
  list into `runs/$R.md`'s last section, the open list keeping an
  `ANSWERED` pointer; so `--para 'What Run'` reads the pointer for a
  finished run and the registration for the one in hand.
  - each prediction, with what would kill it, in the open list. NAME the
    entry here; do NOT copy it. Two copies of a registration is one copy
    that goes stale, and this note is the one nobody re-reads;
  - what the pair varies, in the registration; against what, in the
    compares-against section;
  - anything the pair answers that no earlier pair could.

THE ROSTER [PAIR'S]: N benches, M timed arms over K shapes. Say what landed since
the last run and what that owes -- an -L1 roster pass, a membership
invariance read, and whether either is weak form. THE BREAKDOWN IS NOT
YOURS TO RESTATE: which classes hold how many views is under Provenance
in README, as a delta per run, and the run's own file carries the rest.
What this block owes is the counts a driver checks against and what MOVED
since the last run, which is what decides whether the roster pass is due.

THE COMPILER [PAIR'S], and the first of the blocks that each answer for ONE
input -- the compiler, the shim, the source, the roster, the project file:
EACH OF THOSE BLOCKS IS THE ONE PLACE THAT SAYS WHETHER ITS INPUT MOVED. A
summary block above them and the machine block below point here and restate
nothing, two copies of a verdict being one that goes stale; Run 38's note
restated that its compiler, shim, project file and launch were the previous
run's in block after block.
What `ghc --version` said at build time. The recipe below
carries --ghc-options and not the compiler, so nothing else here records
it, post-run step 9 asks this note for it, and a repetition read against
an earlier run needs both notes to name one.

THE SHIM [PAIR'S]: the commit align-as.py stood at, `git log -1 --format=%h --
:/micro-regime3/align-as.py`. It is on the recipe's -pgma and is as much
an input to the binary as the source; preflight's fill-in reads this back.
A repetition read against an earlier run wants both notes to name the
same one, a shim that moved between them being a difference in the
binaries that no figure attributes to the shim.

HOW EACH HALF IS BUILT [PAIR'S], there being no builder here. Both from one source:

  $R-<basis>    cd here, then the full command, with --builddir, the
                regime, -fforce-recomp and every --ghc-options spelled out
  $R-<other>    the same with the one option that differs

  Spell the copy out too, or write it as `cp $(cabal list-bin micro
  --builddir=DIR) $R-<half>`. The path under a --builddir carries the
  compiler version, so it moves the day the compiler does, and it is the
  one component of a recipe a later reader cannot reconstruct: notes
  before 2026-08-16 elide it as `db-r14a/.../micro`, which is not a
  command, in the file the README calls the only copy of both recipes.

  Say which traps this recipe hits -- -fforce-recomp and a fresh
  --builddir for a -pgma or environment change -- and write no commands
  that verify a half: `preflight.sh --fill-in` derives every row they
  would read, and the pair's own variable is the VARIABLE-CHECK line's,
  below. The RTS line is micro.cabal's since 2026-08-21 and no recipe
  passes one.

THE PAIR'S OWN VARIABLE, READ BACK [PAIR'S]: what preflight's 9b runs to
confirm the halves differ in what this pair says they do, in one of three
forms. `regime basis` or `regime other` holds that half's `diag` to its
own recipe block, as step 9 holds the basis. `run CMD => ERE` runs CMD
here and wants its output to match ERE. `none REASON` names a variable
that leaves no trace in either binary, and preflight echoes the reason as
yours to read. --note-check refuses a note without the line:
VARIABLE-CHECK: regime other

THE SECOND BUILD IS A CHOICE OR A NECESSITY [PAIR'S]: say which, and if a one-binary
form would do, record it here as the alternative so the decision stays one.

WHAT TO READ BACK BEFORE THE EVENING [SAME]: the fill-in block below, which
`./preflight.sh $R --fill-in` derives; by hand, the same readings are
    ./loop-offsets.py $R-<other> $R-<basis>
    ./loop-offsets.py --library $R-<basis> $R-<other>
    ./loop-offsets.py --survey $R-<basis>    # and $R-<other>
    md5sum $R-<basis> $R-<other>
    cmp <(./$R-<basis> check 2>&1) <(./$R-<other> check 2>&1)
  Record this pair's --library figure and not the series; what a reading
  means is banded in README at *the halves differed by more than Main's
  alignment*.

LAUNCH [SAME]: every environment switch this pair wants, on the launch line
of both the gate and the run; a switch the line does not set is off, and
nothing downstream notices. The machine line is what run-evening.sh puts in
front of every stage, `LAUNCH: none` for a pair that wants nothing, and both
drivers record `launch env: ...`, which the executor reads back. The two
launch lines, and under them the machine line:
    <env> ./run-gate.sh $R
    <env> ./run-major.sh $R
LAUNCH: <NAME=value ...>

THE ALONE-LEG RIDERS [SAME]: each half clean and, where the pair carries the
saturating preamble, with `SAT=1` too, control first; run-evening.sh strips
SATURATE from the clean legs. The legs, and under them the machine line,
`clean`, `clean sat` or `none`:
    <env>       ./run-alonelegs.sh $R <other>
    <env> SAT=1 ./run-alonelegs.sh $R <other>
    <env>       ./run-alonelegs.sh $R <basis>
    <env> SAT=1 ./run-alonelegs.sh $R <basis>
RIDERS: clean sat

A RERUN, SAID BEFORE THE HOURS [PAIR'S]: whether post-run step 3 may take
the quiet box again for the populations `--wild` names intruded, without
asking. The owner says it at the preparation, when they are there; the
executing session reads it when the evening lands, when they may not be.
Run 39's evening met an intrusion with nobody to ask, and the answer came
hours later. `allowed` reruns at once, `ask` is the chapter's default, and
`no` records the sensitivity reading in the rerun's place; an `ask` with
nobody there waits for the owner's return:
RERUN: ask

THE BOX AGAIN, SAID BEFORE THE HOURS [PAIR'S]: whether the session may take
the quiet box back after run list step 19a for a timed reading the run's
own results call for -- post-run step 4a's copy test above all, and a
probe the sequence did not carry -- without asking. The same reason as
the line above: Run 40's copy test waited for someone to ask. `allowed`
takes it at once, as the reading's own step says; `ask` is the default:
QUIET-AFTER: ask

THE COUNTS [SAME], owed with the pair over every population on both halves,
wanting no quiet machine: `./run-counts-all.sh $R`, which takes the classes
off the basis binary, and `./run-counts.sh $R <half> <class>` to re-take
one. The previous run's totals are the fill-in block's `prev counts` row,
derived and never carried. Where the registration reads a claim off counts
rather than time, it says so.

THE MACHINE [SAME]: run-gate.sh's machine check reads the gate's basis-half
`a` JSON against the newest run file's fingerprint, and the line it prints
is this run's answer; the previous run's is the fill-in block's `prev
machine` row. The box's eras, and which absolutes may cross them, are
README's Provenance. A net outside the bars, 5% a shape and 3% the geomean,
is read first against what moved since the previous run, which the
[PAIR'S] blocks above name.

GATE: NOT RUN. run-gate.sh takes five benches over the shape set from each
half, twice each, in a palindrome, and both passes are read. Until a line
below states an outcome, this pair has no gate.

AND A GATE THE PAIR'S OWN VARIABLE WILL FIRE IS DECLARED HERE [SAME], by hand
and after the run, as the gate's verdict above is: read-all.sh refuses a run
whose processes do not assert one saturation state, and a pair whose
variable moves what the preamble's spray leaves resident fires that on every
process. Only where the run has shown it, and with its reason, one of:
    EXPECT: state           the processes will not assert one state
    EXPECT: band            the victim's spread will pass the band
    EXPECT: state band      both
A declared firing is still printed in full; it reads as a finding rather
than as a refusal.

ANY OTHER GATE THIS NOTE SETS [SAME] names what discharges it and where the
discharge is written -- a condition, and a slot beneath it for the outcome
and its date, the shape the GATE line has -- so the session that discharges
it has somewhere to say so.

THE FILL-IN BLOCK BELOW [SAME in shape, PAIR'S in content] IS OBSERVATIONS AND NOT INTERPRETATIONS. Record
what was measured -- the commits, the md5s, the byte counts, what agreed
with what -- and leave WHY to the write-up, where a checker can reach it and
an independent reader will re-derive it. Run 15's note wrote that the shim
was why its md5 did not reproduce Run 14's; that was an attribution made
before any evidence, it was inherited into the write-up as fact, and it was
wrong -- the shim is emission-neutral and a dependency relink was the cause.
An md5 that fails to reproduce is an OBSERVATION; what moved is a FINDING
and belongs where findings are checked.

AND DATE EACH LINE WHEN YOU WRITE IT, NOT THE BLOCK WHEN YOU START IT. A
preparation runs some hours and normally crosses midnight -- the build in
the evening, the roster pass and the handover check after it -- so a date
written once at the head is wrong for the lines added later, and nobody
re-derives a date. Run 24's note carried three of them wrong until they
were caught by hand; the header date below is the BUILD's, and a line
taken on another day says so on the line.

Verified when built, YYYY-MM-DD:
  Main.hs at        the commit, and whether the tree carried any other
                    change to it. Preflight's fill-in derives it and the
                    write-up transcribes it; a note without it leaves the
                    write-up with no source
  shim at           align-as.py's commit, and whether it MOVED since the
                    last pair. It is on the recipe's -pgma, so a shim that
                    moved is a difference in the binaries no figure
                    attributes to the shim; where it moved, say what the
                    diff between the two commits actually touches
  compilers         what `ghc --version` said for each half, and what each
                    BINARY carries -- a project file naming a compiler is
                    not a reading of one
  plan              what the build plan resolves through the note's project
                    file: criterion, vector with its two check flags, base,
                    ghc-prim, hashable and aeson
  baked RTS        the `-with-rtsopts` line, by `+RTS --info` and never by
                    `strings`, on both halves
  instruments       the @@ stamps this pair's switches need, counted in
                    each binary
  .text             bytes, and whether the halves agree
  md5 <basis>       ...
  md5 <other>       ...
  launch            where each half launches from, by half-bin.sh, and
                    whether hugebin/ is mounted; `./` is the expected
                    reading while the mount is suspended
  repetition        available or not, and why. Not owed where the source
                    moved: no md5 can reproduce an earlier binary then, so
                    an unequal one names nothing
  fills             the tracked loops' offsets, whether they are the ones
                    the last pair held, and what --library reads.
                    `./loop-offsets.py --delta PREV NOW` is the reading
                    against the previous build of this recipe, and
                    `--library` the one within the pair -- THIS PAIR'S
                    FIGURE AND NOT THE SERIES
  straddle          --survey on each half: how many self-loops, how many at
                    offset 0, how many still straddling. WHERE A ROW'S
                    GUIDANCE IS CONDITIONAL IT SAYS WHAT MAKES IT APPLY,
                    and this one applies to a MAX-SKIP half: of such a half
                    the offsets at 0 are not asked. A pair of two dead-spot
                    halves has no such half, and Run 27's preparation
                    copied the clause into its note before deleting it,
                    which is what a condition left unstated costs
  regime            what `diag` read on the basis, as the run chapter's
                    step 9 reads it -- the one thing no later step can
                    confirm
  check             whether every shape agreed on both halves and whether
                    the two logs are byte-identical
  --list            N benches both, and whether the two listings are
                    identical; `./roster-delta.py PREV NOW` is what says
                    how the roster moved, and its per-class counts are what
                    the roster block above owes
  prev counts       the previous run's counts totals, by --counts-totals
  prev machine      the previous run's gate machine line, off its note
  smoke sweep       ./smoke-sweep.sh $R, clean or not
  L1 ROSTER PASS:   taken or not owed, and on which roster -- a pass
                    recorded for a roster that has since moved is worse
                    than none -- with WHAT IT FOUND, which the script now
                    prints deduped rather than leaving in a temp directory
  document checks   --lint and --check-doc, with the date each passed
  script checks     8b, and 8c to 8e, which are taken separately and after
                    the sweeps
  scripts set       pre-run step 2b: normally NOTHING TO SET, the halves
                    being read from the HALVES line above. An older note's
                    `scripts set` row records a five-site edit that is gone
                    and is not copied forward
  gate arms         run-gate.sh's globs and the count it derives, from
                    `./run-gate.sh $R --show` and never by eye: they move
                    with the roster, and a note that transcribes the
                    PREVIOUS run's is what Run 25's preparation wrote
