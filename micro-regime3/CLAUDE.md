# CLAUDE.md

This directory is worked on from a session rooted in `~/r/horde-ad`, and that repository's `CLAUDE.md` binds here as applicable: its portable-notes section by that section's own terms, and the rest --- the standing checks, the working style, the session facts --- wherever this directory has no rule of its own. Read it in full before touching anything here. What this directory has of its own is `README.md`, the run chapter above all, and the README wins where the two differ.

Each rule below is here because a session met its moment in this directory and did not act on it:

- **The wrong-repo rule in horde-ad's file reads from here in mirror image.** Its build, test and lint commands are the ones that do not transfer; the commands that do are this README's run chapter and `checks.py`, and the shared tools in `~/.claude/bin` take this directory as their argument. *Why:* a session of 2026-09-03 took horde-ad's standing-checks section for the wrong repo's and the run chapter's step 8 subset for the tree's whole check, and the subset is what let a data change in `Main.hs` break 25 audits unseen, found by the next `check-all`.

- **A probe that names one arm runs several: Criterion's positional selector is a prefix.** The roster's names nest, so `classes flip-last-rows/mut-odo-vecdims` runs ten benches under a name that reads as one and `.../mut-odo-vecdims-add-in-leaf-u2` runs four. Read off the legs of 2026-09-09 rather than argued: those two selectors wrote JSONs of ten and four reports, `lib-stage2-lean` two --- itself and its `-u1` --- and `lib-stage1` and `-u2-last` one apiece, their names being nobody's prefix. It fails silently both ways a probe reads a leg: a JSON holding several reports hands whichever ran first to a reader that takes one, and a differenced counter hands back their sum, which is how it was caught --- 56.28 million for `lib-stage2-lean`, being 27.27 plus 29.01, the two arms' own recorded counts. **So check a selector by counting the reports in the JSON it wrote, and never with `--list`**, which prints nothing at all once a positional selector is given, for a prefix these same legs prove selects ten. `-m glob` with a wildcard-free pattern is the exact form horde-ad's portable notes name, and that same count settles it: the legs re-run under it the same evening wrote one report apiece and the right one, `mut-odo-vecdims` included, where the bare form had taken ten. *Why:* those notes carry the prefix rule already but spell their examples as `--benchmark-options=`, a cabal invocation this directory never makes, so the rule reads as being about that harness rather than about the selector; a probe of 2026-09-09 met it, did not recognise it, and quoted three arms' figures that were several arms summed.

- **Search SOURCE the way that file says to search prose: a phrase a break falls inside matches nothing, and adjacent string literals break one as surely as a wrap does.** horde-ad's portable notes give the rule for documents, where `wrap80 --unwrap` is the answer; nothing unwraps Python, and the scripts here hold their prose in literals a line at a time. `command grep 'THAT REASON COVERS STEPS AND NOT REPORTERS' checks.py` finds nothing, and `checks.py` carries that sentence, split across two adjacent literals. So key a search on the shortest span that cannot straddle a break --- one distinctive word --- and read the hit's neighbours. *Why:* on 2026-09-12 a pass over Run 29's own commits ran exactly that grep to check a quotation, got silence, and was one step from recording that `checks.py` lacked text it carries; the silence is indistinguishable from absence, which is what the prose rule exists to say.

- **A *never* claim about this chapter is a claim about its HISTORY, and the working tree is a third of it.** horde-ad's portable notes hold a never-claim to a repo-wide grep, which reads as a claim about everywhere; here it is also a claim about every when, `README.md` alone carrying hundreds of commits and the run files being back-filled stubs that record none of what their runs decided. So a search for something the chapter is said never to have had ends at `git log -S` or `git log --reverse -- <path>` and not at HEAD, and a claim it was never written down is worth only what that search cost. *Why:* on 2026-09-13 a preparation reported that no -O1 claim set existed, having grepped the tree, the claims section and every `runs/run*.md`; the set is one `git show` away at `dc2b119`, nine claims under *The claims Run 8 should test*, and the owner found it by asking. What made the tree's silence convincing is that `runs/run7.md` is a back-filled table with no claims in it at all --- a file that exists and answers nothing reads exactly like a subject that was never recorded.

- **A binder every path forces gets its bang as the code is written, and what asks afterwards is the `bang shapes` step of `checks.py`, which is horde-ad's `tools/bang-lazy-check.py` over `Main.hs` against `bang-lazy-allow.txt`.** The user-scope rule names arguments forced anyway and loop invariants; the walker of 2026-09-09 left its vector and its carry's offset lazy against it, and nothing here asked until a flag's worth on Run 30 was read back to two bangs four days later. The step reaches only the shape with a bang on one equation and none on another --- the carry, which it printed STRONG --- and a candidate it prints is read, not fixed: one read as fine goes into the allow file with its reason, as `canonView`'s merge did, and a definition that moves drops out of the file and is read again. A never-banged argument stays the reader's: count the loop's instructions a run and put the figure beside the body as read, sixty-four against a body of thirty being the reading nobody took. *Why:* the rule was resident in the session that wrote the walker and was not applied, twice in one function, which is this file's own criterion for restating one.

- **Before taking a reading, not only before writing a probe, read the lead of every tracked script and use the one that fits by its knobs: `for f in $(git ls-files '*.sh' '*.py'); do printf '%s: ' "$f"; sed -n 2p "$f"; done`, never a listing cut by `head`. What it reads is built on the last run's recipe, copied from that run's pair note, never on a probe script's.** A counter reading of chosen arms and events is `probe-stalls.sh` with its knobs set, and a lead that names what a script was written for rather than what it does is fixed where it is found. The last run is what the owner measures, so it is what a change is priced in: `runNN-pair.txt`, under HOW EACH HALF IS BUILT, declares it, the basis half the one to start from, while a `probe-*-build.sh` keeps the recipe of the day it was written. *Why:* on 2026-09-23 a session wrote a new script for a cycles and fetches reading `probe-stalls.sh` takes by environment alone, having listed the scripts through `head -50`, and that file's lead named the reading that asked for it rather than what it reads; later that day another listed them whole and still reached for `probe-interleave.sh`, its question being a reading rather than a probe, and built its halves with `probe-tail-build.sh`, on 9.12.4 with the `-fspec-constr` the ruling of 2026-09-13 had dropped, until the owner pointed it at `probe-stalls.sh` and asked which GHC, flags and shims it was using.
