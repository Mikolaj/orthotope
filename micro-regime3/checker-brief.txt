The brief for a run's independent checker, and for the comprehension probe after it. Both are agents, both start where the session started and so arrive knowing none of this, and both were retyped from memory on Run 16 -- once because a rate limit killed the first agent mid-pass and the whole brief had to be written again. THREE THINGS CHANGE FROM RUN TO RUN: the first block, and the two items marked THIS RUN ONLY below. All three are EDITED HERE at the write-up rather than substituted in the prompt -- the file is the copy both agents are sent, so a value that lives only in a prompt is a value the next run retypes. It carries the run whose write-up is happening. PREVBASIS and PREVSAME are one half whenever the run changed no allocation area, which is the ordinary case: two names to hunt for and one to find is not an error.

Substitute per run: RUN=run32 BASIS=nospec OTHER=ghead PREV=run31
                    PREVBASIS=run31-nospec  PREVSAME=run31-nospec
                    PRETIP=3d4f8ac   (the COMMITTED COPY of the previous
                                      run's file, which is the base the
                                      write-up is diffed against -- NOT the
                                      commit before it, which would show the
                                      run file as wholly new and leave the
                                      pass no `before` to find a stale
                                      paragraph in)
                    RUNTIP=<given in the launch message>   (the commit pass 2
                                      READS -- the commit that carries this
                                      line's edit cannot name itself -- and
                                      advanced here after, as the record and
                                      not the input)


=== THE INDEPENDENT CHECKER, PASS 1 (checklist 6b: after 6a's prose,
    on that one commit's diff, with 6c running beside it) ===

You are an independent checker for a benchmark write-up. Report DISCREPANCIES, not opinions or style preferences.

WHERE YOU ARE. Work in /home/mikolaj/r/orthotope/micro-regime3. That is an orthotope checkout, NOT the horde-ad repo your session started in. Your evidence is THIS RUN'S OWN ARTIFACTS in that directory, with the reader ./read-run.py there. They are MORE THAN THE JSONS, and a claim you cannot settle from the JSONs is usually settled by one of the rest:
  RUN-HALF-POP.json  the twenty-two timed processes; every published table
  RUN-HALF-POP.log   the same processes' per-sample stamps -- allocation,
                     mutator, collector, foreign CPU, and the @@saturate
                     plateau line -- read with ./read-run.py FILE --wild
  RUN-counts-HALF[-CLASS].txt  instructions an iteration, PER SHAPE and
                     arm. No reader mode prints a per-shape count, so a
                     per-shape count claim is checked by reading this
                     file directly
  RUN-al-*           the alone-leg riders, behind --deflation
  RUN-gate-*         the gate's four processes
  RUN-pair.txt       the pair note: md5s, .text sizes, the compilers and
                     their commits, the build recipes, the baked RTS
                     line, the fills. EVERY claim about the BINARIES is
                     checkable here and nowhere else
  PREV-*             the previous run's artifacts, for any cross-run
                     figure the write-up quotes
  Main.hs            the shape, class and roster definitions
What is excluded is OTHER REPOSITORIES' tooling, not this run's own files: no other repository's checkers, linters or conventions come near these two documents -- they carry a per-repo configuration and will report correct names as missing. Say `could not check` only after looking in the list above; Run 22's checker returned seven such items of which THREE were checkable -- the per-shape counts, the binary identity and the plateau -- because this paragraph used to read `JSON artifacts and NOTHING ELSE` and all three live in .txt and .log files. Do not edit any file. Read and compute only.

YOUR OBJECT IS A COMMIT AND NOT THE WORKING TREE, which is why the session keeps writing while you run: it may edit the run file during pass 1 to clear a gate, and 6c edits README beside you throughout. Take the diff commands below literally and read the working copy for nothing you are checking.

TWO DOCUMENTS, and the run's numbers are in the second: README.md is what stands between runs, and runs/run<N>.md is this run's write-up entire -- its head, Results, what the next run compares against, the properties it should test, the TEN class blocks and its own Provenance. A run replaces that file and edits README.md around it -- a bullet on the delta chain, and whatever the replace list turns up.

THE TWO INVOCATIONS SEE DIFFERENT DOCUMENTS, and this is not a simplification to ignore. PASS 1 reads THE RUN FILE ALONE, because README's half is being written IN PARALLEL with it and may not exist yet; that parallelism is why pass 1 is launched where it is. PASS 2 reads BOTH, and is the only invocation that can -- it is also the only place a figure quoted in both documents is checked on both sides.

THE DIFF, TWO commands -- and which you run depends on which pass you are: PASS 1 runs the FIRST alone, PASS 2 runs BOTH. They are separate because the two files have different histories. The run file is touched by the write-up ALONE, so it needs no upper bound; README also carries edits to the run CHAPTER, which are procedure and not this run's figures, so it is bounded at RUNTIP:
  git -C /home/mikolaj/r/orthotope diff PRETIP..HEAD   -- micro-regime3/runs
  git -C /home/mikolaj/r/orthotope diff PRETIP..RUNTIP -- micro-regime3/README.md
On Run 22 the unbounded form would have handed you 576 lines of README against the write-up's 224, most of it chapter restructuring under review by nobody. If either document was normalised or rewrapped in that range the diff is mostly character-level; ignore that and judge the substance.

SIX THINGS YOU CANNOT DERIVE, so they are given -- and items 5 and 6 are THIS RUN'S and are rewritten every run:
 1. The basis is RUN-BASIS and publishes every table (RUN-BASIS-main.json, and RUN-BASIS-<class>.json for rev bcast bcastmid window scaled runs flip block small compose -- TEN, none of them new on this run, and NO class view landed since PREV (item 6), reshape1, revsome and slice having retired from timing on 2026-09-04 and being kept in `check`). The control is RUN-OTHER.
 2. PREV is on disk too. Absolutes and anchors are read against PREVSAME, the half at this run's own area; the published lineage and that run's own second column against PREVBASIS.
 3. Alone-leg riders are RUN-al-<half>-<shape>-r1.json, one bench each -- and HALF OF THEM CARRY A `-sat-` INFIX, RUN-al-<half>-sat-<shape> -r1.json, which is a DIFFERENT measurement and not a repetition: the clean legs and the saturated ones are the two columns the deflation splits into, the state the preamble puts on a process and the rest the roster adds on top of it. Count them apart. This run has 22 legs a half in each state, 88 in all -- the 19 main-set shapes and the three anchors on a second reps pass. Probes are probe-*.json/.log, and the two -g3 twins beside the binaries are probe-g3-<half>-RUN.
 4a. TWO STATISTICS, and they can part in SIGN. A margin between two
    arms is --pair's PAIRED figure, the geomean of the per-shape ratio; the Results table's `time` column is winsorized per row, so a ratio of two of its entries equals the paired figure only where neither row had a cell capped. On Run 26 they parted on four pairs, the write-up quoted the paired figure alone, and README's own rule says the published column is the yardstick for comparing two rows -- so a reader following it reached the opposite sign. CHECK BOTH wherever the diff quotes one: --pair prints them side by side and --predictions has since 2026-09-06.
 4. read-run.py is the only reader; start with --help. --cells gives TSV (slope_s raw, slope_net_s corrected); --pair (with --per-shape), --compare (with --chapter, --alloc, --movers, --ci, --counts), --aa, --block, --fingerprint, --machine, --cross-classes and --section are the modes for the JSONs and the documents. TWO MORE READ THE OTHER ARTIFACTS and are the ones a claim outside the JSONs needs: --wild reads a process LOG for its per-sample stamps, which is the only route to a plateau or foreign-CPU claim, and --deflation reads the riders. The counted-work .txt files have no per-shape mode at all -- read them directly for a per-shape count claim.
 5. THIS RUN ONLY -- THE BOX DID NOT CHANGE between PREV and RUN, and RUN'S GATE MACHINE CHECK DID NOT FIRE, as PREV's did not. It read **+0.86%** on `list`'s net over 19 of 19 shapes against the fingerprint PREV installed, worst `cnn-L1-24x24-c1` +3.03%, NONE past 5%, the geomean inside the 3% bar. That is a LIKE-FOR-LIKE reading: PREV published on this very recipe, so the fingerprint and the half reading against it are one recipe. Against PREVBASIS, the previous build of that recipe, the SIXTEEN arms both rosters time and both give a corrected time span **0.9839 to 1.0149**, FOURTEEN of the sixteen inside 1% and those two the only ones outside, with `list` at **1.0031** and the widest, `lib-stage2-lean`, at 1.0149; read as a ratio to `list` within each run the other fifteen give a `--bridge` geomean of **0.9976** with none outside the 3.3% drift band. So absolutes cross between PREV and RUN and RUN publishes inside the third machine era rather than opening a fourth. **BUT A SOURCE TERM DOES RIDE in every cross-run figure, and that is this run's own disclosure**: `Main.hs` moved from `13cbd0d` to `f95795a` in SIX commits -- `c2021a8` comments alone, `396f01c` PREV's own step-6d fixes, `4488631` landing `liblist-stage4-list-sum` AND making the shared loop `sumLazyRuns` sum each run through `sumNoSpec` without vector's `SPEC` argument, and `08ee255`, `e12b000` and `f95795a` taking an unzip immediately undone by a zip out of the zero-stride orders -- so no md5 here reproduces anything, neither half's is two-sided, and an unequal one names nothing. `--delta` against PREVBASIS reads the TWO-copy group's mod-64 offsets PRESERVED, `[0, 0]`, and THE SIX-COPY GROUP'S NOT: `[0, 24, 8, 0, 4, 0]` becomes `[0, 0, 8, 0, 4, 0]`, its second head moving by 24, with NO address surviving to the byte on either and a third displacement on the six, `0x9928`, which is 40 mod 64. That REOPENS the pinning claim's strong form and the write-up says so; it is a disclosure and not an error. THE ROSTER MOVED BY ONE: 31 timed arms to 32 over 19 shapes to 19, ONE in and NONE out, the 31 survivors in the same order, all 58 class views unmoved and no class's count changed. THE PAIR NOTE'S COMMIT IS THE ONE TO BELIEVE: both halves were built from `f95795a`. **THE TWO HALVES DIFFER IN ONE COMPILER AND IN NOTHING ELSE**: same source, same shim commit `b3a1aca`, same shim environment (both LOOP_DEADSPOT=1), same roster, shape set and bench order -- and TWO compilers, ghc-9.12.4 through `cabal.project` against the in-tree stage1 `10.1.20260803` through `cabal.project.ghead`, which is two stores, two plans and two sets of boot libraries, read back out of the binaries as `ghc-internal-9.1204.0` and `ghc-internal-10.100.0`. NEITHER half adds a regime flag: `diag` reads 24066407 against 2408530 on the basis and 24066455 against 2408530 on the control, **9.992 times apart on EACH**, so both are plain -O1. **THE PUBLISHED BASIS IS THE ghc-9.12.4 HALF.** **AND THE TWO COLUMNS MAY BE DIFFERENCED on the main set and on six of the ten classes**, which is where this run differs from PREV: `list` moved **0.43 points** on the main set and 0.06 (`runs`) to 2.27 (`compose`) per class, so only `rev` 0.9855, `scaled` 1.0119, `small` 1.0157 and `compose` 1.0227 are past the 0.7% bar and only those four cross-half readings are orderings. Treat every disclosure here as correct rather than as a hedge.

 6. THIS RUN ONLY -- RESTATE IT EVERY RUN, and Run 20 is why: this block stood with Run 18's window and Run 18's four classes through Run 20's probe, which went looking for the wrong classes because of it. On Run 32: ONE window, the sequence's TWENTY-TWO processes from 2026-09-15T00:35:17 to 2026-09-15T07:42:54, no population rerun and no hole between one process finishing and the next starting, every one exiting 0 at the count its population asks for, no complaint and no `!!` line anywhere, the `ghead` half first throughout. **ONE PROCESS OF THIS RUN WAS INTRUDED ON AND IT IS A GATE'S, NOT THE SEQUENCE'S**: the `foreign` column is a RATIO OF ONE CORE and not a percentage, and `--wild` clears all twenty-two sequence logs, all eighty-eight alone-leg logs and THREE of the gate's four, while `run32-gate-nospec-a` carries 2 of its 95 benches at or above 0.25 foreign, peak 0.82, both on `cnn-L1-6x6-c1` -- the intruder being the write-up session reading documents during the evening's first stage. So exactly ONE log of the hundred and fourteen reaches 0.25, post-run step 3's rerun is NOT owed, and NO figure this run publishes comes from a gate process. The gate's verdict was re-read with that shape dropped from both passes and no spread moved by more than 0.17 of a point. **WHAT THE RUN CARRIES BESIDES IS FIVE WILD CELLS**, read-all.sh's worst-cell column being ONE figure a process: `ghead-main` **16.12%** on `cnn-slice-c32`, `nospec-window` 9.51%, `ghead-flip` 8.11%, `nospec-main` **6.62%** on `gather48-src-50` and `ghead-window` 5.60%. They are wild and not foreign. The gate was launched ONCE, 00:01:51 to 00:35:14, the alarm between it and the sequence read 0.8% non-idle, the whole evening ran as ONE command and the counted work as a second, 07:56:21 to 08:38:33, 22 sweeps with no cell refused by perf. **THE PLATEAU GATE DID NOT FIRE**, where PREV's did: the preamble's victim spans 21.0965 to 21.5398 ms/iter, **2.10%** against a 5% band, and the two halves sit inside one another, `nospec` 1.84% and `ghead` 2.03%. **AND THE SATURATION STATE DOES NOT PART BY HALF**, where PREV's did: all twenty-two processes assert ONE `keep` and ONE `inuse`. There are TEN classes: `runs` has FOURTEEN shapes, `window` EIGHT, `bcast` and `flip` SIX, `block` and `small` FIVE, `bcastmid` and `compose` FOUR, and `rev` and `scaled` THREE; NO class view moved since PREV, ONE timed arm landed and NONE was parked, so `liblist-stage4-list-sum` is a first reading and every other row has a PREV twin. THE FLOOR IS OVER EIGHT A/A PAIRS: **0.66%** and **0.68%** are maxima over eight, the basis's carried by `bq-expand-aa-distant` as PREV's was and the control's by `mut-odo-vecdims-aa-distant`, the family ROOT's pair, which no half has carried before; the restricted four-pair figure reads the SAME two figures, so the two thresholds are closed on both halves. **NO CELL SINKS BELOW THE SHARED FORCING PASS ON EITHER HALF**, so every row that carries a corrected time at all is a geomean over all 19 shapes. **THE COUNTED WORK IS NOT A PARTITION THIS RUN AND THAT IS THE FINDING**: the whole range over the sixteen arms is **0.9996 to 1.0101** at a geomean of **1.0053**, the ten fill-family arms running 0.9996 to 1.0081, `list` with its two A/A copies at 1.0100 to 1.0101 and `bq-expand` with its two at 1.0030 -- where PREV read three separate groups from 1.0380 to 1.4423. THE RUN'S LARGEST FINDING IS THAT THE COMPILER IS WORTH NOTHING THIS ROSTER CAN MEASURE: no main-set arm moves past 3%, the arm geomean is 0.9985, every allocation tier is identical between the halves cell for cell, and the preamble's state cost agrees to six ten-thousandths. Of SIXTEEN registrations FIFTEEN held and ONE was killed -- (3), the fused list, 3.09 points off on `runs` against a 3% bar. All of it is disclosed, not hidden; check that the disclosure matches the artifacts rather than treating any of it as an error.

THE FIGURE SWEEP -- and it is a SWEEP, not a pass: `pass` in this file means one of the two agent invocations above, and both sweeps below are done in EACH of them. Recompute every numeric claim the diff adds, from the reader or --cells, never by dividing figures printed in a table (they are rounded to three significant figures). BE EXHAUSTIVE ON THE TWO HAND-EDITED TABLES AND SAMPLE THE INSTALLED ONES. A FIGURE'S POPULATION IS PART OF THE FIGURE: recompute it on the file it came from, the main set's on the main JSON and a class's on that class's, since every mode reads the file it is handed against that file's own floor -- README's `Which population answers a question, and how to ask all of them` is the rule and the loop. The hand-edited pair is the run's own two-column geomeans and the PROVENANCE ANCHORS: those are typed, and both table errors on record are theirs -- Run 20 omitted the two-column table entirely and Run 22 left the anchors carrying the previous run's figures in seven of nine cells, through every gate and a whole checker pass. The installed tables -- Results, the fingerprint, the class tables, the cross-class summary -- are matched by whole line with the count asserted. So take a sample of the installed rows -- TWENTY, spread across the main table, two class tables and one fingerprint, which is the number and not an encouragement: FIVE passes have now recomputed 487, 491, 352, 401 and 418 rows and found ZERO wrong between them, and Run 26's pass spent 401 of them to find none while nine stale PROSE paragraphs sat in the same diff. **RECOMPUTING MORE THAN TWENTY IS ITSELF A FINDING AND IS REPORTED AS ONE**, naming how many you took and why this line did not hold you: it has not held yet, Run 31's pass 1 recomputing 418 rows with this sentence in front of it, and a brief that is read and not obeyed is a defect of the brief. The pull is real and worth naming rather than resisting -- a table row is mechanical and certain and a paragraph is neither, so an agent measuring its own thoroughness counts rows. Count paragraphs instead. Spend what the sample saves on the prose, which is where every real finding of those five runs was. Say how many rows you verified, by which route, and how many disagreed. Pass 2's scope is narrower and is stated in its own block.

THE SUPERLATIVE SWEEP -- every superlative and count. Every "only", "largest", "widest", "highest", "lowest", "smallest", "most", "first", "never" and every "N of M" is a claim about a whole population: re-derive it BY SORTING that population, not from the arms the sentence names. Flag any sentence contradicting a table or another sentence.

Report a numbered list of discrepancies only -- the claim as written, what you computed, the command you used, and the size of the disagreement -- plus per-sweep counts of what you verified. Say explicitly what you could not compute rather than guessing.


=== THE SAME AGENT, PASS 2 (checklist 6d: after 6b AND 6c, over BOTH
    documents, on the single commit that carries them) ===

IT READS BOTH FILES and this is the only pass that can: the run file with pass one's fixes in it, and README with 6c's requotes. That is the first moment all of a run's prose exists. A figure quoted in BOTH -- the floor pair, the six-pair one -- is checked here on both sides for the first and only time.

YOUR SCOPE IS THIS COMMIT'S DIFF AND NOTHING PASS 1 ALREADY VERIFIED. Do not recompute an installed table row: `--in-place` matches by whole line and asserts the count, and pass 1 has read every row once. Run 16's first pass recomputed 487 rows and found not one wrong; Run 18's recomputed 491, also none -- and both runs' real findings were in the prose. What you owe on a table is the rows a FIX touched, plus the two figures that live in BOTH documents, which is the check only this pass can make.

Send it the fixes, not the prose again. On Runs 15 and 16 most of the second pass's findings existed ONLY because the first round of fixes had been written -- nine of thirteen on Run 16 -- so that is what it is hunting. List each fix you made, so it checks rather than rediscovers, and ask specifically for: any NEW superlative or count the fixes introduced, any place a fix now contradicts a neighbouring sentence left untouched, any figure a fix corrected whose attribution the correction falsified, and any cross-document figure whose two copies now disagree.


=== THE COMPREHENSION PROBE (checklist 7a: a DIFFERENT agent, once
    7's fixes are committed and the documents have settled) ===

It must be uncontaminated: a fresh agent, never the checker, which has seen everything. It reads the two documents and nothing else -- no reader, no JSONs, no other repo -- and answers their own questions with citations, quoting the sentence and naming its section and its file.

Ask nine-ish questions that CROSS section boundaries, since that is the class of defect nothing else can see. The ones that earned their place on Run 16: this run's floor per half with its carrying pair and the threshold two table rows must clear; which area the published tables use and which half publishes; whether a nursery pair's columns may be subtracted and how many such pairs exist; whether the regime 3 fix is ever slower than what it replaced; which half of the previous run a figure is compared against and whether that depends on the kind of figure; what each probe concluded and whether it confirmed or refuted its registration; what a large single A/A cell should tell a reader; the placement-sensitive arms and what is known about why; and whether the machine was undisturbed.

THE CLOSING INSTRUCTION IS THE POINT: list every place the document contradicted itself, gave two answers to one question, or left the reader unable to answer despite clearly intending to -- quoting both sides with their headings -- and everything it had to read three times or first got wrong, saying what misled it.

AND COUNT ITS FINDINGS BEFORE ACTING ON THEM. A probe's summary is a reader's experience and not a census: Run 16's reported five senses of one word where the text had one borrowing, and acting on the summary would have renamed two things that were already correct.
