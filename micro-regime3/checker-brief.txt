The brief for a run's independent checker, and for the comprehension
probe after it. Both are agents, both start where the session started
and so arrive knowing none of this, and both were retyped from memory
on Run 16 -- once because a rate limit killed the first agent mid-pass
and the whole brief had to be written again. THREE THINGS CHANGE FROM
RUN TO RUN: the first block, and the two items marked THIS RUN ONLY
below. All three are EDITED HERE at the write-up rather than
substituted in the prompt -- the file is the copy both agents are
sent, so a value that lives only in a prompt is a value the next run
retypes. It carries the run whose write-up is happening.
PREVBASIS and PREVSAME are one half whenever the run changed no
allocation area, which is the ordinary case: two names to hunt for and
one to find is not an error.

Substitute per run: RUN=run24  BASIS=g912  OTHER=ghead  PREV=run23
                    PREVBASIS=run23-g912  PREVSAME=run23-spot
                    PRETIP=812d859   (the COMMITTED COPY of the previous
                                      run's file, which is the base the
                                      write-up is diffed against -- NOT the
                                      commit before it, which would show the
                                      run file as wholly new and leave the
                                      pass no `before` to find a stale
                                      paragraph in)
                    RUNTIP=d86db3e   (the commit pass 2 READS, given in
                                      the launch message -- the commit that
                                      carries this line's edit cannot name
                                      itself -- and advanced here after, as
                                      the record and not the input)


=== THE INDEPENDENT CHECKER, PASS 1 (checklist 6b: after 6a's prose,
    on that one commit's diff, with 6c running beside it) ===

You are an independent checker for a benchmark write-up. Report
DISCREPANCIES, not opinions or style preferences.

WHERE YOU ARE. Work in /home/mikolaj/r/orthotope/micro-regime3. That is
an orthotope checkout, NOT the horde-ad repo your session started in.
Your evidence is THIS RUN'S OWN ARTIFACTS in that directory, with the
reader ./read-run.py there. They are MORE THAN THE JSONS, and a claim
you cannot settle from the JSONs is usually settled by one of the rest:
  RUN-HALF-POP.json  the twenty timed processes; every published table
  RUN-HALF-POP.log   the same processes' per-sample stamps -- allocation,
                     mutator, collector, foreign CPU, and the @@saturate
                     plateau line -- read with ./read-run.py FILE --wild
  RUN-counts-HALF[-CLASS].txt  instructions an iteration, PER SHAPE and
                     arm. No reader mode prints a per-shape count, so a
                     per-shape count claim is checked by reading this
                     file directly
  RUN-al-*           the alone-leg riders, behind --deflation
  RUN-gate-*         the gate's four processes
  RUN-pair.txt       the pair note: md5s, .text sizes, the compilers and
                     their commits, the build recipes, the baked RTS
                     line, the fills. EVERY claim about the BINARIES is
                     checkable here and nowhere else
  PREV-*             the previous run's artifacts, for any cross-run
                     figure the write-up quotes
  Main.hs            the shape, class and roster definitions
What is excluded is OTHER REPOSITORIES' tooling, not this run's own
files: no other repository's checkers, linters or conventions come near
these two documents -- they carry a per-repo configuration and will
report correct names as missing. Say `could not check` only after
looking in the list above; Run 22's checker returned seven such items
of which THREE were checkable -- the per-shape counts, the binary
identity and the plateau -- because this paragraph used to read `JSON
artifacts and NOTHING ELSE` and all three live in .txt and .log files.
Do not edit any file. Read and compute only.

TWO DOCUMENTS, and the run's numbers are in the second: README.md is what
stands between runs, and runs/run<N>.md is this run's write-up entire --
its head, Results, what the next run compares against, the claims it
should test, the NINE class blocks and its own Provenance. A run
replaces that file and edits README.md around it -- a bullet on the delta
chain, and whatever the replace list turns up.

THE TWO INVOCATIONS SEE DIFFERENT DOCUMENTS, and this is not a
simplification to ignore. PASS 1 reads THE RUN FILE ALONE, because
README's half is being written IN PARALLEL with it and may not exist
yet; that parallelism is why pass 1 is launched where it is. PASS 2
reads BOTH, and is the only invocation that can -- it is also the only
place a figure quoted in both documents is checked on both sides.

THE DIFF, TWO commands -- and which you run depends on which pass you
are: PASS 1 runs the FIRST alone, PASS 2 runs BOTH. They are separate
because the two files have different histories. The run file is touched by the write-up ALONE, so it needs no
upper bound; README also carries edits to the run CHAPTER, which are
procedure and not this run's figures, so it is bounded at RUNTIP:
  git -C /home/mikolaj/r/orthotope diff PRETIP..HEAD   -- micro-regime3/runs
  git -C /home/mikolaj/r/orthotope diff PRETIP..RUNTIP -- micro-regime3/README.md
On Run 22 the unbounded form would have handed you 576 lines of README
against the write-up's 224, most of it chapter restructuring under review
by nobody. If either document was normalised or rewrapped in that range
the diff is mostly character-level; ignore that and judge the substance.

SIX THINGS YOU CANNOT DERIVE, so they are given -- and items 5 and 6
are THIS RUN'S and are rewritten every run:
 1. The basis is RUN-BASIS and publishes every table (RUN-BASIS-main.json,
    and RUN-BASIS-<class>.json for rev revsome bcast bcastmid reshape1
    slice window scaled runs). The control is RUN-OTHER.
 2. PREV is on disk too. Absolutes and anchors are read against
    PREVSAME, the half at this run's own area; the published lineage
    and that run's own second column against PREVBASIS.
 3. Alone-leg riders are RUN-al-<half>-<shape>-r1.json, one bench each
    -- and HALF OF THEM CARRY A `-sat-` INFIX, RUN-al-<half>-sat-<shape>
    -r1.json, which is a DIFFERENT measurement and not a repetition: the
    clean legs and the saturated ones are the two columns the deflation
    splits into, the state the preamble puts on a process and the rest
    the roster adds on top of it. Count them apart. A run has 27 legs a
    half in each state. Probes are probe-*.json/.log.
 4. read-run.py is the only reader; start with --help. --cells gives
    TSV (slope_s raw, slope_net_s corrected); --pair (with --per-shape),
    --compare (with --chapter, --alloc, --movers, --ci, --counts), --aa,
    --claims, --block, --fingerprint, --machine, --cross-classes and
    --section are the modes for the JSONs and the documents. TWO MORE
    READ THE OTHER ARTIFACTS and are the ones a claim outside the JSONs
    needs: --wild reads a process LOG for its per-sample stamps, which
    is the only route to a plateau or foreign-CPU claim, and --deflation
    reads the riders. The counted-work .txt files have no per-shape mode
    at all -- read them directly for a per-shape count claim.
 5. THIS RUN ONLY -- THE BOX DID NOT CHANGE between PREV and RUN. The
    gate's machine check read +0.56% on `list`'s net over 24 of 26
    shapes against the fingerprint PREV kept, worst
    `stretch-pow2stride` +2.67%, none past 5%; the run's own two
    main-set processes read the same comparison at -0.20% (basis,
    worst conv1d-24 -1.78%) and -0.39% (other, worst
    alexnet-L2-27-c48-k5 -2.80%). All inside 3%, so absolutes DO
    cross that boundary freely and a cross-run absolute comparison is
    fair. (The last box event was before Run 18; nothing since.) It
    is 24 OF 26 and not 26 of 26 because the fingerprint PREV kept
    has no row for the two main-set shapes added 2026-09-02; a
    --machine run against THIS run's own file reads 26 of 26 and
    +0.00%, which is the freshly installed fingerprint compared with
    itself and is not a machine check. A LAYOUT TERM DOES ride in
    every cross-run figure this run: Main.hs moved by two commits of
    2026-09-02, so neither half reproduces an earlier binary, no md5
    here matches one on record, and the exact repetition Runs 19 and
    23 could take is NOT available -- the write-up says so and reads
    its cross-run column against run23-spot, the previous build of
    this recipe, at 33 of 45 shared arms within 1%. The two HALVES
    differ in the COMPILER only: same source, same shim commit, same
    shim environment (both LOOP_DEADSPOT=1), the other half built
    through cabal.project.ghead against the in-tree GHC HEAD stage1.
    Treat that disclosure as correct rather than as a hedge.
 6. THIS RUN ONLY -- RESTATE IT EVERY RUN, and Run 20 is why: this
    block stood with Run 18's window and Run 18's four classes through
    Run 20's probe, which went looking for the wrong classes because
    of it. On Run 24: ONE window, all twenty processes,
    2026-09-03T02:54:41 to 2026-09-03T12:58:25, every one exiting 0 at
    its roster's count, no complaint and no `!!` line, the ghead half
    first throughout. NO INTRUSION: `--wild` on both main logs puts
    NOT ONE bench of 1352 at or above 0.25 foreign CPU, so post-run
    step 3 did not fire and no population was rerun. What did happen
    was BEFORE the window: the gate was launched at 02:06, stopped by
    hand two minutes in when the machine was wanted, its four partial
    artifacts moved out of the tree, and the recorded gate is the
    relaunch of 02:09:17 -- the write-up discloses this and it is not
    an error. The plateau gate reads all twenty preamble victims
    inside a 3.08% spread against a 5% band. FIVE classes (rev
    1.0078, revsome 1.0149, bcast 1.0124, reshape1 1.0327, runs
    1.0104) have cross-half baselines past the 0.7% differencing
    threshold and their cross-half lines are explicitly not read for
    the compiler; the MAIN SET is inside it at 1.0025, so its two
    columns MAY be differenced. There are NINE classes; `runs` has
    FOURTEEN shapes and `window` FOUR, three and one of them added
    2026-09-02 -- Main.hs still declares those four "added ...  after
    the run" in README's Run 23 delta bullet, which the write-up
    removes, so a shape count of 14 and 4 is right and a checker
    reading 11 and 3 off that declaration is reading a stale one.
    Cells below the shared forcing pass: FIVE on the basis main set
    (both libunord arms on stretch-inner1 and stretch-wide-2xM,
    libunord-stage2 on stretch-square-1341) so two rows are over 24
    and 23 of 26; SIX on the ghead main set across three rows (the
    two libunord arms on stretch-pow2stride and vgg-14-c512-k3,
    libunord-stage2 on stretch-square-1341, and lib-stage2-lean alone
    on stretch-inner1) so one row is over 25 and two over 24 and 23;
    on reshape1 NINE arms are degenerate on each half, and the shape
    they sink on DIFFERS BETWEEN THE HALVES -- reshape1-500k on the
    basis, reshape1-r3 on ghead -- which is why a reshape1 family
    reading is taken over the two shapes neither half sinks on. Every
    main-set registration figure is taken over the 25 shapes
    excluding stretch-inner1 for that reason, and the write-up says
    so. All of it is disclosed, not hidden; check that the disclosure
    matches the artifacts rather than treating any of it as an error.

THE FIGURE SWEEP -- and it is a SWEEP, not a pass: `pass` in this file
means one of the two agent invocations above, and both sweeps below are
done in EACH of them. Recompute every numeric claim the diff adds,
from the reader or --cells, never by dividing figures printed in a
table (they are rounded to three significant figures). Be exhaustive
on the tables IN PASS 1 -- Results, the fingerprint, the nine class
tables, the cross-class summary, the run's own two geomean columns --
and say how many rows you verified and how many disagreed. Pass 2's
scope is narrower and is stated in its own block.

THE SUPERLATIVE SWEEP -- every superlative and count. Every "only",
"largest",
"widest", "highest", "lowest", "smallest", "most", "first", "never"
and every "N of M" is a claim about a whole population: re-derive it
BY SORTING that population, not from the arms the sentence names.
Flag any sentence contradicting a table or another sentence.

Report a numbered list of discrepancies only -- the claim as written,
what you computed, the command you used, and the size of the
disagreement -- plus per-sweep counts of what you verified. Say
explicitly what you could not compute rather than guessing.


=== THE SAME AGENT, PASS 2 (checklist 6d: after 6b AND 6c, over BOTH
    documents, on the single commit that carries them) ===

IT READS BOTH FILES and this is the only pass that can: the run file
with pass one's fixes in it, and README with 6c's requotes. That is
the first moment all of a run's prose exists. A figure quoted in BOTH
-- the floor pair, the six-pair one -- is checked here on both sides
for the first and only time.

YOUR SCOPE IS THIS COMMIT'S DIFF AND NOTHING PASS 1 ALREADY VERIFIED.
Do not recompute an installed table row: `--in-place` matches by whole
line and asserts the count, and pass 1 has read every row once. Run
16's first pass recomputed 487 rows and found not one wrong; Run 18's
recomputed 491, also none -- and both runs' real findings were in the
prose. What you owe on a table is the rows a FIX touched, plus the two
figures that live in BOTH documents, which is the check only this pass
can make.

Send it the fixes, not the prose again. On Runs 15 and 16 most of the
second pass's findings existed ONLY because the first round of fixes
had been written -- nine of thirteen on Run 16 -- so that is what it
is hunting. List each fix you made, so it checks rather than
rediscovers, and ask specifically for: any NEW superlative or count
the fixes introduced, any place a fix now contradicts a neighbouring
sentence left untouched, any figure a fix corrected whose attribution
the correction falsified, and any cross-document figure whose two
copies now disagree.


=== THE COMPREHENSION PROBE (checklist 7a: a DIFFERENT agent, once
    7's fixes are committed and the documents have settled) ===

It must be uncontaminated: a fresh agent, never the checker, which has
seen everything. It reads the two documents and nothing else -- no
reader, no JSONs, no other repo -- and answers their own questions with
citations, quoting the sentence and naming its section and its file.

Ask nine-ish questions that CROSS section boundaries, since that is
the class of defect nothing else can see. The ones that earned their
place on Run 16: this run's floor per half with its carrying pair and
the threshold two table rows must clear; which area the published
tables use and which half publishes; whether a nursery pair's columns
may be subtracted and how many such pairs exist; whether the regime 3
fix is ever slower than what it replaced; which half of the previous
run a figure is compared against and whether that depends on the kind
of figure; what each probe concluded and whether it confirmed or
refuted its registration; what a large single A/A cell should tell a
reader; the placement-sensitive arms and what is known about why; and
whether the machine was undisturbed.

THE CLOSING INSTRUCTION IS THE POINT: list every place the document
contradicted itself, gave two answers to one question, or left the
reader unable to answer despite clearly intending to -- quoting both
sides with their headings -- and everything it had to read three times
or first got wrong, saying what misled it.

AND COUNT ITS FINDINGS BEFORE ACTING ON THEM. A probe's summary is a
reader's experience and not a census: Run 16's reported five senses of
one word where the text had one borrowing, and acting on the summary
would have renamed two things that were already correct.
