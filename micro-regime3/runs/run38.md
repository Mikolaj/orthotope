# Run 38 (GHC HEAD against itself, plain -O1 against -O1 with -fspec-constr -fliberate-case, under the exit span, launched from disk)

One run's write-up: its head, its Results, what the next run compares against, the properties that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 38 (GHC HEAD `10.1.20260918` against itself, plain -O1 against -O1 with `-fspec-constr -fliberate-case`, under the exit span, launched from disk): the two passes are worth 28.89 points on `list` and 30.32 on `bq-expand`, ALL TWENTY registered spans hold, and the third reading of this pair names Run 36's the outlier.** The pair is Runs 36's and 37's IN ITS VARIABLE --- one source, `Main.hs` at `bb6f0fc`, one shim at `f31bd1c` under the four switches with the exit span, ONE compiler, `10.1.20260918`, unmoved from both, one roster, one shape set, every process launched from disk --- with two `-O2` passes added to the control half's command line and nothing else differing. So `the basis` below is the UNFLAGGED half and every `cross` figure reads basis over control, ABOVE 1 meaning the FLAGGED half is the faster. Over the seventeen main-set arms that carry a cross-half figure, SIX move with the families and ELEVEN do not: the six are the `list` and `bq-expand` families entire, at **1.2868** to **1.3069**, and the eleven others span **0.9974** to **1.0205**, every one of them a fill. **The bar an arm has to clear to be the passes' rather than the run's is 0.45 points** --- the widest an arm and its own A/A duplicate part in this same cross-half reading, which `--compare` prints under its table and which is NOT this population's floor, that being 0.57% on the basis and 0.28% on the control and measured WITHIN one half --- and seven of the nine non-control arms clear it.

**What this pair was built to settle is which of Runs 36's and 37's readings of it is the outlier, and the answer is Run 36's.** Over the eighteen shapes without Run 36's wild cell the three draws of one pair read **1.3129**, **1.2950** and **1.2907** on `list`: Runs 37 and 38 agree to 0.43 of a point where Run 36 sits 2.22 above both, so the registration's narrow span --- built to discriminate exactly this, 1% around 1.2950 with Run 36's figure outside it --- holds, and the pair's `list` figure is Run 37's. **What that costs is Run 36's conclusion.** Run 31 read the whole `-O2` level at 1.2974 on `list`; the two passes now read 0.24 and 0.67 of a point UNDER the level on the two later draws where Run 36 read 1.55 above it, so the level's other passes do not measurably hand `list` back, and the composition of Runs 29's and 30's single-pass readings, 1.3325, overshoots on all three draws. **On `bq-expand` the pair repeats a third time**, 1.3032 against 1.2980 and 1.3101, five accounts now inside 1.58 points with the level's 1.2943 and the composition's 1.2967 among them. So what this run measures is the pair's PRECISION over three draws: 1.21 points on `bq-expand` and, once Run 36's cell is set aside, 2.22 on `list` with 0.43 of it between the two later readings.

**The counted work parts on exactly the two families, and on `bq-expand` it parts far wider than the clock.** `list` retires **1.2932** and `bq-expand` **1.5063** times as many instructions on the unflagged half, against 1.0383 on `mut-odo-vecdims` and 1.0531 on `lib-stage2-lean` --- so the basis retires 50.6 points more instructions than the flagged half on `bq-expand` and takes only 30.3 points more time over it, a `time/counts` of **0.8651**, where `list` cashes almost exactly what it saves at **0.9967**. **And the counted work is again where this run repeats its predecessor most exactly**: those four figures were registered as spans off Run 37's own sweeps and all four hold, two of them at **0.00 points** --- 1.5063 against 1.5063 and 1.0383 against 1.0383 --- with `list` 0.02 and `lib-stage2-lean` 0.09 of a point out, across a source that moved fourteen commits. **They move allocation too, as every earlier flag pair did, and by exactly what Run 37 read**: the flagged half allocates **0.8119** of the basis on `bq-expand` and every one of its three twins and **0.9342** on `list` and both of its, on the main set and in the same shape on all ten classes. That breaks property 3's LEVEL clause in every one of the eleven populations, as it did on Runs 36 and 37 and as Run 31's whole `-O2` level did --- to the published decimal, 2.11x and 23.45x on the main set on each --- while its ORDER clause holds everywhere. **What is NEW is seven arms outside the families that now move with them**: the unordered consumers read 3.80% to 13.08% where Run 37 read 0.9957 to 1.0007, on allocations of hundreds of bytes a call, which is the source and not the passes.

**ALL SIX registration items hold their sentences, over 20 spans of which 20 held and none was killed.** (1) holds on both `list` spans and names Run 36's draw the outlier; (2) holds on `bq-expand`; (3) holds on all nine of its null spans, no arm outside the two families joining them; (4) holds at 1.0205 on `mut-odo-vecdims-add-in-leaf-u1`, which is the item this run was built to answer; (5) holds on all four counts spans; and (6) holds on the three new arms, which are worth nothing outside the floor. **The run's own finding is that ARM**: it read 0.9630 across Run 37's halves and 0.9598 on a probe of that run's own binaries, and here it reads 1.0205, with step 4a putting the whole of the earlier movement on Run 37's CONTROL half --- level on the basis against that run and up to 28.6 points faster on the control, counts at 1.0000 on both halves in all eleven populations. So the term went with that build's file on disk, which is what Run 37's entry concluded and what a rebuilt pair independently confirms. **Two things are worth saying about the run rather than the pair**: 22 of 22 processes gated clean and not one cell of the 2720 sits under R2 0.99 or ten samples, where Run 37 carried one and Run 36 five; and TWO benches of the 114 sampled logs reach 0.25 foreign, both on the control half, for which a sensitivity reading stands in place of post-run step 3's rerun, as it did on Run 33 --- the exposed main-set bench is an A/A copy no span names, and the floor it reaches reads 0.28% with and without its shape.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. **What this run leaves the next one is a basis with a like-for-like predecessor, a pair whose `list` figure is now known to two draws, and one new question**: `run38-gheadnospec` is a plain -O1 GHC HEAD build on the recipe Runs 36 and 37 also built, so those published columns are one subtraction rather than a chain, and this run against Run 37's own basis reads every shared arm within 2.09 points at a `--bridge` geomean of 0.9993. The new question is the seven unordered consumers whose allocation the two passes now change and did not a run ago, which is a source term and wants the commit that introduced it named. The SPLIT --- which of the two passes carries the regime's points --- is still read on no compiler this series builds with, and is what a fourth reading of this pair would be worth less than.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a PLAIN -O1 table under the exit span**, as Runs 32 to 37's were, plain -O1 being the regime `Data/Array/Internal.hs` actually compiles under. **On this run that sentence describes the BASIS half and not the pair**: the control half is that same -O1 with `-fspec-constr -fliberate-case` on its command line, two of `-O2`'s passes and nothing else, so the table below is the unflagged half's. **What is new in it is the SOURCE and the BOOT, and nothing else is**: the compiler is Runs 36's and 37's in-tree stage1 `10.1.20260918` unmoved, and the shim at `f31bd1c`, the project file `cabal.project.ghead`, the four shim variables, the regime and the launch from disk are Run 37's to the commit. What moved is `Main.hs`, from `05cfe93` to `bb6f0fc` in fourteen of the owner's commits, which took the roster from 608 benches and 32 timed arms to 646 and 34; and the box, rebooted once since Run 37's evening and up since 2026-09-21T11:09:53+02:00. **Read against the half Run 37 built by this basis's own recipe, the distance is at most 2.09 points on every arm**: over the 16 arms that carry a corrected time and ran in both, this run over that one runs from **0.9873** on `lib-stage2-lean` to **1.0209** on `lib-stage2-lean-u1` --- below 1 meaning this run is the faster --- and read as a ratio to `list` within each run, which cancels a box term exactly, the fifteen others give a `--bridge` geomean of **0.9993** with NONE outside the 3.3% drift band Run 11 measured. That span carries a moved source and a reboot, and no compiler, shim, project-file or launch term at all. **The `alloc` column is a median over this run's own nineteen shapes**, `bq-expand` at 2.78x and `list` at 25.20x, so it is a statistic of a strategy and a shape set together and does not cross to a run that timed a different set.

**And it is the basis half's**, `run38-gheadnospec`, as every published table here is from Run 11 on: the control half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-four rows. Which half publishes is not the re-declaration of 2026-09-15 this run inherits --- that one names ghc-9.12.4, and this pair has ONE compiler on both halves, so it cannot decide anything here. What decides it is the pair's own variable: the UNFLAGGED half is what `Data/Array/Internal.hs` compiles under, the flagged one is the candidate reading, and `--compare` takes the basis first, so every `cross` figure below reads unflagged over flagged and ABOVE 1 means the FLAGGED half is the faster. **THREE of the thirty-four rows ARE first readings**, where Run 37's table had none: the roster is that run's with three arms in and one out --- `lib-stage3-lean`, `liblist-stage5-sum` and `libunord-stage14-sum` in, `liblist-stage4-list-sum` parked, thirty-one survivors in the same order over the same nineteen shapes, which `roster-delta.py` read off the two runs' binaries --- so every other row has a twin in Run 37's file, and the three new ones are the inward twins registration item (6) prices.

**Comparing runs?** The table below is Run 38's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the properties to test are [the ones after it](#the-properties-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.57%** from 1 on the basis half or **0.28%** on the control, read over the eight pairs this roster carries --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **The whole-set figure and the carry-back one COINCIDE on both halves this run**, as they did on Run 37: 0.57% and 0.28% are the same over the four pairs that carry back to Run 10 as over the eight the roster carries, `bq-expand-aa-distant` carrying both on each half. So the restriction costs nothing here. Beside those, the worst SINGLE A/A cells of the two MAIN-SET processes --- **2.71%** on `stretch-bigstride` on the basis and **4.02%** on `stretch-pow2stride` on the control --- are not floors at all and are not to be quoted as any. **And its two columns may be differenced on NONE of the eleven populations**, `list` having moved **28.89 points** on this run's main set and between 24.54 and 35.32 on the ten classes, all of it past the bar, so every cross-half figure in this file is an ORDERING and not a subtraction.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns, and why `time` is a winsorized geomean of slopes rather than criterion's mean, is [README's *Reading a run file*](../README.md#reading-a-run-file).

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.59* | *55* | *2.78x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.57 | 70 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.60 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.61 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-sum | -- | -- | 0.66 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| liblist-stage5-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over stage four's route with the fill numbered innermost first |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage12-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage twelve's list --- stage eleven with the run chosen among tied unit-stride axes by its length |
| libunord-stage13-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage thirteen's list --- stage twelve's route found with fewer passes over the axes |
| libunord-stage14-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage thirteen's route with the fill numbered innermost first |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage9-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.40* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.03* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.02* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.025 | 0.113 | 0.53 | 70 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage3-lean | 0.025 | 0.113 | 0.56 | 70 | 1.00x | new mutating `Vector` method -- the lean dispatch over the fill numbered innermost first, against `lib-stage2-lean`, which keeps the outermost-first numbering |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.112* | *0.71* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.113 | 0.44 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.113* | *0.49* | *69* | *1.00x* | *A/A control* |
| lib-stage2-lean-u1 | 0.028 | 0.110 | 0.58 | 68 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| lib-stage1 | 0.029 | 0.113 | 0.58 | 70 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u1 | 0.029 | 0.110 | 0.50 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| *mut-odo-vecdims-aa* | *0.045* | *0.112* | *0.40* | *66* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.045** | 0.112 | 0.34 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| *mut-odo-vecdims-aa-distant* | *0.045* | *0.112* | *0.39* | *66* | *1.00x* | *A/A control* |
| bq-expand | 0.128 | 0.259 | 0.66 | 50 | 2.78x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.128* | *0.259* | *0.69* | *50* | *2.78x* | *A/A control* |
| *bq-expand-aa-distant* | *0.129* | *0.260* | *0.36* | *50* | *2.78x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.71 | 21 | 25.20x | -- |
| *list-aa-distant* | *1.001* | *1.010* | *0.66* | *21* | *25.20x* | *A/A control* |
| *list-aa-adjacent* | *1.002* | *1.008* | *0.51* | *21* | *25.20x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and on this run ONE of the 120 pairs among the sixteen timed arms other than `list` parts in SIGN between the two statistics on the basis, where Run 37's basis parted on five of its 105, Run 36's on two, Run 35's on three and Run 34's on six. That one is `lib-stage1` over `lib-stage2-lean-u1`, column **1.0233** against a paired **0.9960**: the column puts the unrolling twin ahead and the pair puts stage one ahead, by about two points on the column and four tenths on the pair. **The widest disagreement of any kind sits on the row the cap touched most**: `bq-expand-aa-distant` over `lib-stage3-lean` divides to **5.1107** on the column where the paired figure is **4.5824**, the column 11.5% above it, and `lib-stage3-lean` is the widest-capped row of the table, four of its nineteen cells capped and its published 0.02521 sitting 10.3 points under its plain per-shape geomean of 0.02812. Those column ratios are `--pair`'s own `published-column ratio` and not the printed table divided. **And a SINGLE row's movement between runs is not the arm's either**: `--movement` reads fourteen of the sixteen rows moved against Run 37's table and two at the same three decimals, where `--compare` against the JSON of the half Run 37 built by this basis's own recipe puts every one of the sixteen within 2.09 points of level and `list` at 1.0036.

**This run's two columns may be differenced on NONE of the eleven populations, as Runs 36's and 37's could not, and the reason is the pair itself.** The 0.7% bar asks whether `list` --- the denominator every other row is divided by --- sits still between the halves, and here the two passes move `list` by **28.89 points** on the main set and by 24.54 on `bcastmid` to 35.32 on `bcast` over the ten classes, every one of the eleven figures past the bar by a factor of thirty-five or more. So on every population in this file an arm-by-arm figure across the halves is an ORDERING and not a subtraction, and each says so in its own cross-half line. Runs 36 and 37, this same pair, marked all eleven past the bar as this one does, and so did Run 31, whose variable was the whole level; Run 35 could difference seven of the eleven and Run 34 nine. **That is the bar working rather than failing**: it exists to stop a margin being read off two columns with different denominators, and a pair built to move the denominator is the case it was written to refuse.

`concat-runs` has no row, and neither do the other 96 arms the roster holds and checks without timing --- **97 of its 131** in all, against Run 37's 94 of 126: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). **THREE untimed twins joined the timed roster and ONE timed arm left it**, in the owner's own commits and not this run's preparation: the pairing of 2026-09-21 timed `lib-stage3-lean`, `liblist-stage5-sum` and `libunord-stage14-sum`, each its counterpart's route under the fill numbered innermost first, and parked `liblist-stage4-list-sum` as `Only`. So `roster-delta.py`, read off the two binaries, reads 32 arms to 34 over 19 shapes to 19, THREE in and ONE out, the thirty-one survivors in the same order and the sixty-one class views unmoved over the same ten classes. A movement against Run 37's own basis column is therefore a movement on the **16 shared arms that carry a corrected time**, with a source term and a boot between the two runs and no compiler, shim, project-file or launch term at all --- and a movement across THIS run's two halves is the two passes, with no term of any other kind.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is a TIE**: `lib-stage2-lean` and `lib-stage3-lean` both read 0.025 and are separated only on the unrounded values, 0.02519 against 0.02521, with the shipped leaf and its two A/A copies at 0.027, `lib-stage2-lean-u1` at 0.028 and `lib-stage1` and `mut-odo-vecdims-add-in-leaf-u1` at 0.029 --- **six timed non-control arms below `mut-odo-vecdims`'s 0.045**, every one of them a fill that writes the result, where Runs 31 to 37 had five and the sixth is this run's new arm. **The column and the pair now DISAGREE about the order on ONE of the 120 pairs**, where Run 37's basis parted on five of its 105, Run 36's on two, Run 35's on three and Run 34's on six --- the cap touching less this run, four of nineteen cells on the widest-capped row where Run 37's widest took five. Paired on the basis, `lib-stage2-lean` over `lib-stage1` is **0.9304** at 15 of 19 and p 0.019, over its own unrolling twin **0.9266** at 15 of 19 and p 0.019, and over the shipped leaf **0.9762** at 14 of 19 and p 0.064 --- so the pairs put the lean fill first throughout and separate it from the leaf by 2.4 points at a p that settles little, which is the column's 0.025-against-0.027 read as a margin rather than as an order. **The third is that the leaf fusion is untouched by the two passes**: `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6369** on the basis and **0.6388** on the control, 0.19 of a point apart on a pair that moves `list` by twenty-nine points, and both inside the 0.6358 to 0.6525 that Run 34's file records ten readings across Runs 29 to 33 spanning --- a span carried from that file and not re-derived here.

**The straddlers stand at TWELVE on each half, and this pair moves tracked fill copies as a whole compiler does.** Within the pair `./loop-offsets.py --delta run38-gheadnospec run38-gheadtwopass` puts the tracked six-copy group at **[18, 15, 0, 0, 9, 2]** on the basis and **[18, 0, 7, 7, 9, 26]** on the control, with six displacements and no address surviving to the byte; the eight-instruction two-copy group reads [0, 0] against [4, 4]; and a seven-instruction three-copy group at [0, 0, 0] is on the control half alone. So two of `-O2`'s passes displace fill copies as a compiler change and an optimisation level do, which is what `--library` says over the whole library too: 808 self-loops in both, **4.3%** at the same offset in line and 46.0% in the same straddle state --- the 4.3% being Runs 36's and 37's to the tenth, and a tenth from Run 31's 4.4% for the whole `-O2` level. **Against the previous build of the basis's own recipe, Run 37's basis half, `--delta` reads the six-copy group MOVED** --- [18, 0, 0, 0, 9, 2] to [18, 15, 0, 0, 9, 2] --- with no address surviving to the byte and four displacements, while the two-copy group keeps every mod-64 offset at [0, 0]; a third group, two copies at [0, 0], is in Run 37's binary only, which is code that went with the parked arm rather than a placement finding. **And the exit spans astride are ZERO on both halves**, as `LOOP_EXITSPAN=1` owes; post-run step 0's naming, taken off the binaries that were timed, puts the same twelve bodies at the same twelve mod-64 offsets on each half --- 39, 41, 18, 41, 18, 41, 18, 49, 45, 42, 19 and 29 in address order --- so the two passes moved no straddling loop across a cache line either. **Five of the twelve are NAMED and seven refused, and both halves name and refuse the same ones**: the named are `fillStage2Short` and four leaf bodies, `...U2Down`, `...U2Last` and two of `...U2` itself, each matched byte for byte in a `-g3` twin built to that half's own recipe; of the seven refused, six carry a `--loose` family of three or four members and name nothing, and one has a family of ONE, `fbMutOdoVecdimsAddInLeafU2Ptr`. The straddle count moved from Run 37's ten, on both halves alike, which is the moved source and not the regime.

**Run 36's one anomaly is two runs gone, and this run reads that cell where its copies say it should be.** Run 36's basis read `list` on `stretch-coprime-r7` at a net 1.5371e-03 s, a criterion CI of 10.07% and an R2 of **0.9395**, while its own two A/A copies in that same process read 1.1012e-03 and 1.1036e-03 --- the original 39% above duplicates that share its code, which is the 28.36% that file refuses to call a floor. **This run's basis reads the same cell at 1.1067e-03, CI 0.2999% and R2 0.999920**, its copies at 1.1059e-03 and 1.1181e-03: the original sits BETWEEN them, 0.07 of a point from the adjacent one and 1.03 from the distant one, and within half a point of where Run 36's copies sat. So that cell was wild and not the shape --- Run 36 could argue it from its copies, Run 37 showed it by repetition, and this run shows it a second time. **And it no longer reaches the machine check at all**: Run 37 installed a fingerprint carrying the cell at its clean value, so this run's check reads over all nineteen shapes with nothing set aside, where Run 37's had one shape past 5% and it was this one. **What the anomaly cost is now fully paid**: the wide `list` span Run 36's whole-set figure centred died on Run 37, and this run's two spans, both centred on Run 37's own figures, hold at 1.2889 over the nineteen and 1.2907 over the eighteen.

**What this run has instead of an anomaly is an ARM RESTORED, and it confirms the instance that arm's own entry named.** `mut-odo-vecdims-add-in-leaf-u1` read **0.9630** across Run 37's halves where that run's nine other arms outside the two families read 1.0026 to 1.0160, and a probe on its own two binaries read 0.9598 --- so the movement reproduced on that DRAW, and what no reading had asked was whether it survives a REBUILD. **This run reads it at 1.0205**, inside the registration's 2.5% band and back among its siblings, which is item (4) holding on the side its sentence named. **Step 4a says which half moved, and it is the CONTROL**: read each half against Run 37's same half, this arm is level on the BASIS --- 0.9592 to 1.0168 across the eleven populations --- and moves on the control in eight of them, at 0.9511 on `scaled`, 0.9594 on the main set, 0.9644 on `bcastmid`, 0.8925 on `block`, 0.8700 on `flip`, 0.7852 on `runs`, 0.7313 on `compose` and **0.7144** on `bcast`, with the counted work at **1.0000** on both halves in every one of the eleven. Below 1 there means THIS run's control is the faster, so Run 37's control binary ran that arm slow by up to 29 points and this rebuild does not --- and the instruments that named the movement then, counts level and the family's copies agreeing, are the ones that name its absence now. **So the term went with that BUILD's file on disk**, which is what Run 37's own entry concluded from three fresh copies of each control: a slow 4 KiB draw costing one cell 14%. This run is that conclusion's first independent test --- a new build is a new file and so a new draw --- and it agrees, which a copy test on the old instance could not do, that instance having died with the binary. **What it still does not say is WHY a draw lands slow**, and the three movers of [the entry on Run 34's four](../README.md#what-is-open) that its copy test could not resolve are untouched by this run.


## What the next run compares against

**Run 38's pair is Run 37's own, [registered 2026-09-21][open]**, on the owner's request of that day for the same binaries --- which the pre-run list's BOTH HALVES ARE BUILT ANEW ruling makes the same two RECIPES built again, so the pair is repeated and neither binary is. **That entry is the ONE declaration site by the ruling of 2026-09-19 and it spells both recipes out, so they are not restated here; `Recommended tasks after Run 38` is NOT that site and holds post-mortems, which is [an open entry](../README.md#what-is-open) of its own. What this run leaves as the reference is `run38-gheadnospec`**, the unflagged half whose column stands below: GHC HEAD `10.1.20260918` through `cabal.project.ghead`, `Main.hs` at `bb6f0fc`, the shim at `f31bd1c` under the four switches with the exit span, every process launched FROM DISK, `hugebin/` being suspended, at plain `-O1`, which is the regime `Data/Array/Internal.hs` compiles under. **It is the third published basis on that compiler and the SECOND with a like-for-like predecessor**: against `run37-gheadnospec`, the same recipe a day earlier, every one of the sixteen timed arms that ran in both reads within 2.09 points of 1, 0.9873 to 1.0209, at a `--bridge` geomean of 0.9993 with NONE outside the 3.3% drift band. **That step is a null on both instruments**, and what it carries is a moved source --- fourteen commits, three timed arms in and one parked --- and one reboot, the compiler, the shim, the project file and the launch all being Run 37's to the commit. **The pair itself is Runs 36's and 37's, repeated a third time**: the two `-O2` passes together against neither, on one compiler, one shim, one shape set and one bench order. **And the straddle Run 37 left open is broken, against Run 36.** Over the eighteen shapes without Run 36's wild cell the three draws read **1.3129**, **1.2950** and **1.2907** on `list`: Runs 37 and 38 agree to 0.43 of a point where Run 36 sits 2.22 above both, so Run 36's is the outlier its own successor suspected, and the registration's narrow span was written to discriminate exactly that. Against Run 31's whole-level **1.2974** the two passes read 0.24 and 0.67 of a point UNDER the level on the two later draws, so **the level's other passes do not measurably hand `list` back** --- Run 36's conclusion refuted rather than repeated --- while the composition of Runs 29's and 30's single-pass readings, **1.3325**, overshoots on all three. **On `bq-expand` the three readings sit inside 1.21 points**, 1.2980, 1.3101 and 1.3032, against 1.2943 from the level and 1.2967 from the composition, so that arm confirms both accounts on every draw. **What it leaves unasked is the split**: this pair prices `-fspec-constr` and `-fliberate-case` TOGETHER, the single-pass readings that would say which carries the regime are Runs 29's and 30's, taken on ghc-9.12.4 and on a roster four sources back, and no reading of either pass alone exists on this compiler. Either pass alone against the unflagged half would supply it at one variable and one term, and it is [an open question][open].

**The COMPILER variable was not this run's to vary --- both halves are one in-tree stage1, `10.1.20260918`, as both of Runs 36's and 37's were --- and the step this run reads is not a compiler step at all.** The tally of pairs BUILT to ask the compiler stands where Run 35 left it, at TEN. **What this run adds instead is the series' second whole-pair REPETITION and its first read three times**: Runs 36, 37 and 38 are the same two recipes on the same compiler, built from sources a day and fourteen commits apart, and their cross-half readings agree to 1.21 points on `bq-expand` and, once Run 36's wild cell is set aside, to 2.22 on `list`, 0.43 of it between the two later draws. That is a repetition of the READING and not of a binary --- no md5 here reproduces one of Run 37's --- so what it bounds is the harness and the box together, not the compiler's determinism, which `-fobject-determinism` and the md5s settle separately. **The REGIME variable has now been asked SIX times.** Put in one orientation, the unflagged half over the flagged, Runs 29, 30 and 31 read `list` at **1.1379**, **1.1710** and **1.2974** and `bq-expand` at **1.2804**, **1.0127** and **1.2943**, all three on ghc-9.12.4; on GHC HEAD the two passes together read **1.3360**, **1.2960** and **1.2889** on `list` over the nineteen shapes and **1.2980**, **1.3101** and **1.3032** on `bq-expand`. On `bq-expand` the single-pass pair multiplies to 1.2967 against Run 31's measured 1.2943, 0.19% apart, and the three HEAD draws sit 0.13, 1.34 and 0.65 of a point above the higher of them --- five accounts inside a point and six tenths, across two compilers. On `list` they multiply to 1.3325 against Run 31's 1.2974, 3.51 points apart, and the two later HEAD draws read 1.2950 and 1.2907 over the eighteen shapes, both on the LEVEL's side of that gap and under the level itself, where Run 36 read 1.3129 and landed above it. The cross-compiler term the reading carries is bounded rather than assumed: the -O1 baseline moved 0.77 of a point from ghc-9.12.4 to the HEAD of 2026-08-03, by `run35-exit` against `run35-gheadexit`, and under a point from that HEAD to this one.

**What Run 38 leaves the next run to read against, and the first item is a check that did NOT fire. The box was REBOOTED ONCE between Run 37 and this run**, and the gate says it still measures as it did: read against the fingerprint Run 37 installed, the machine check that `run-gate.sh` runs on the gate's basis-half `a` JSON puts `list`'s net at **+0.01%**, worst `stretch-primes` at **-1.28%**, 0 of 19 shapes past 5% and the geomean inside the 3% bar --- the main-set JSON reads the same check at +0.27% with a worst of +1.95% on `gather48-src-50`, which is the instrument and not a disagreement, and the gate's is the figure the note carries. **And this reading is like-for-like on every term but two**: Run 37's basis is this basis's own recipe to the compiler, the shim, the project file, the four switches, the level and the launch, so the check carries no compiler, shim, project-file or launch term at all. What it does carry is a source term, fourteen commits with three timed arms in and one parked, and the boot. **No shape is past the bar in either direction, where Run 37's check had one**: that one was the fingerprint's own anomaly, Run 36's wild `stretch-coprime-r7/list` cell, and Run 37 installed a fingerprint carrying that cell at its clean value instead --- so this check reads over all nineteen shapes with nothing to set aside, and the half-point the box moved on Run 37's instrument does not repeat. So the box has not moved measurably since Run 37's evening, and this run publishes INSIDE the third machine era rather than opening a fourth. The fingerprint below is this run's own. **What a next run may take from it is that same like-for-like check**: this basis is the THIRD published one on this compiler and the SECOND with a predecessor built to its own recipe, so a run keeping the recipe compares against this basis with the box and the source as the only terms.

**Registered with the pair.** Run 38's six registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. ALL SIX items hold their sentences, over 20 spans of which 20 held and none was killed. **What a next registration should take from this one is that a band centred on a MEASURED figure of the same pair holds and one centred on a figure an anomaly inflated does not**: every prior here is Run 37's own reading of these same two recipes, re-derived from its JSONs rather than quoted from its prose, and the widest miss over twenty spans is 2.05 points, on the one arm the registration wrote an item for because it expected it to move. Run 37's wide span on `list` died because it was centred on Run 36's whole-set 1.3360, which a wild cell had inflated by 2.31 points; this run centred both of its `list` spans on Run 37's own figures and both held. **And item (5) is the one to copy, for the second run running**: four bands drawn from the previous run's COUNTS sweeps, all at one percent, came in 0.02, 0.00, 0.00 and 0.09 of a point out, where the same run's time bands, drawn from the same run, were out by 0.26 to 2.05. Counted work carries across runs of this harness an order of magnitude better than the clock does --- Run 36 argued it from two spans, Run 37 confirmed it on four, and this run confirms it on four more across a source that moved fourteen commits. **And the amendment Run 37's write-up asked for was made and paid for itself**: that run ended its own list with *a null span is two-sided whether or not its sentence is; say which side the sentence means*, and item (4) here says outright that its sentence means the low side --- which is what lets its HELD be read as *the movement belonged to Run 37's build* rather than merely as *inside the band*.

What this section stands on --- the rulings on the position term, the allocation area, a change of basis and a pair's two halves, which of its tables are edited by hand, and why the fingerprint is kept --- is [README's *Reading a run file*](../README.md#reading-a-run-file).

**The next run compares against Run 38**, whose halves were launched FROM DISK and whose basis carries `LOOP_EXITSPAN=1` at plain -O1 on the in-tree stage1 `10.1.20260918`; a run keeping that recipe reads against this basis within the switch, as this run read `run37-gheadnospec` within it. Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. **The step this run records IS basis to basis, as Run 37's was**: Run 37 published a HEAD half on this basis's own recipe, so the two published columns carry no compiler term and the step is one subtraction rather than a chain. Over the **16 arms both rosters time and both give a corrected time** it runs from **0.9873** on `lib-stage2-lean` to **1.0209** on `lib-stage2-lean-u1`, below 1 meaning this run is the faster, ALL SIXTEEN within 2.09 points of 1 --- and what those points carry is a moved source of fourteen commits and one reboot, with no compiler, shim, project-file or launch term at all. **The table below is this run's own two halves and no earlier run's**, eight strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in TWO GHC FLAGS and in nothing else. Its two columns may NOT be differenced, `list` having moved 28.89 points between them, so the table is two orderings read side by side.
| strategy | Run 38 (plain -O1, dead-spot, exit span, -A32m, HEAD 10.1.20260918) | Run 38 (that recipe plus `-fspec-constr -fliberate-case`) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.045** | 0.058 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.029** | 0.034 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.027** | 0.032 |
| `lib-stage1` | **0.029** | 0.037 |
| `lib-stage2-lean` | **0.025** | 0.035 |
| `lib-stage2-lean-u1` | **0.028** | 0.033 |
| `lib-stage3-lean` | **0.025** | 0.034 |
| `bq-expand` | **0.128** | 0.127 |

**READ THE SECOND COLUMN AS A RATIO AND NOT AS A SPEED, and this run it is not even an ordering of speeds.** Every entry is that arm's net over `list`'s net in ITS OWN half, and `list` moved **28.89 points** between the halves --- forty-one times the 0.7% bar --- so the control column reading HIGHER on seven of the eight rows is the denominator having shrunk under it and not one arm of it having slowed. In absolute terms the flagged half is the faster on sixteen of the seventeen timed arms, `lib-stage2-lean` at 0.9974 the one exception; the column cannot say so, and is not asked to. **And it is not an identity either**: each entry is winsorized per row within its own half, so dividing an arm's two entries does not reproduce its `--compare` figure over `list`'s. On `lib-stage2-lean` the two unrounded published entries, **0.02519** and **0.03489**, divide to 0.7220, where the two PLAIN per-shape geomeans, 0.02795 and 0.03611, divide to **0.7740** --- which is the arm's cross-half figure over `list`'s to within 2e-4, 0.9974 over 1.2889 giving 0.7738. The capping is the whole of the difference: four of that row's nineteen cells capped on each half, with the published figure sitting 9.9 points under the plain one on the basis and 3.4 under it on the control. The arm-by-arm reading of what the two passes are worth is in the head, off `--compare`, where the reference is not divided out.

**A published geomean is over the same 19 shapes, and two halves of one run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered. **THE TABLE ABOVE IS NOT SUCH A PAIR**: `list` moved **28.89 points** between these halves, forty-one times the bar, so the two columns are read side by side as orderings and never differenced. **They print far apart on seven of the eight rows, the control higher on each of those seven** --- `mut-odo-vecdims` 0.045 against 0.058, `mut-odo-vecdims-add-in-leaf-u1` 0.029 against 0.034, the shipped leaf 0.027 against 0.032, `lib-stage1` 0.029 against 0.037, `lib-stage2-lean` 0.025 against 0.035, `lib-stage2-lean-u1` 0.028 against 0.033 and `lib-stage3-lean` 0.025 against 0.034 --- while `bq-expand` prints the other way, 0.128 against 0.127, which is the one arm whose own move outruns the denominator's. **Read DOWN a column and the two HEADS differ, which is the reordering the passes make**: the basis's head is `lib-stage2-lean` at 0.025, resolved against `lib-stage3-lean`'s printed 0.025 by the unrounded 0.02519 against 0.02521, while the control's is the shipped leaf `mut-odo-vecdims-add-in-leaf-u2` at 0.032, with `lib-stage2-lean` fifth of the eight there. `bq-expand` is at the foot of each. **Those gaps are the moved denominator and the winsorizing together, in that order**: `lib-stage2-lean`'s plain per-shape geomeans are 0.02795 and 0.03611, 22.6 points apart where the published 0.02519 and 0.03489 part by 27.8, the cap touching four of its cells on each half --- so on this row the cap WIDENS the printed gap, as it did on Run 37's.

**The control half's own standings on the arms this run's roster carries, which no FULL table here holds, the two-column table above carrying eight of its rows and every other published table being the basis half's.** Read off the control half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9586** (0.9435) and against `mut-odo-vecdims` **0.6388** (0.6369); `lib-stage1` against `-u2` **1.0495** (1.0493); `lib-stage2-lean` against `-u2` **0.9832** (0.9762) and against `lib-stage1` **0.9368** (0.9304); and the headline pair README's opening leads with, `bq-expand` against `mut-odo-vecdims`, **2.2036** (2.8506), 0 of 19 shapes either way. **All six hold their direction across the halves**, and five of them move by under two points; the sixth is the headline pair, which the passes move by 64.7 points and which is the pair's own variable rather than an ordering that shifted. The lean fill is ahead of the shipped leaf on both halves, by 2.4 points on the basis and 1.7 on the control, and ahead of `lib-stage1` by 7.0 and 6.3. **The ordering the open list carries changes sides here too, and opposite to Run 37's**: `mut-odo-vecdims-add-in-leaf-u1` against `lib-stage1` reads **1.0101** on the basis and **0.9940** on the control, where Run 37 read 0.9940 and 1.0486 --- so the arm that ran ahead on that run's basis and behind on its control does the reverse here, which is that run's control-half movement gone rather than a change in either arm. **What the two passes do move is which arm a class bolds**: three of the ten differ across the halves --- `rev` and `block`, `lib-stage3-lean` on the basis against `lib-stage2-lean` on the control, both inside the outside-the-family column; and `small`, which changes COLUMN, the arm outside the family leading on the basis and the family's ceiling on the control.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what the next run reads against. **A class figure compared across the Run 11/Run 12 boundary is not compared on one build**: Run 11's class tables are its *aligned* half's and Run 12's its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06, so a point or two of movement across that boundary is the shim rather than the class. From Run 13 on, every run's class tables are its own basis half's, Run 34's included.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 6.35 us | 0.079 | `lib-stage3-lean` 0.075 | `mut-odo-vecdims-add-in-leaf-u2` 0.055 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.71 us | 0.090 | `lib-stage3-lean` 0.068 | `mut-odo-vecdims-add-in-leaf-u2` 0.069 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 120 us | 0.063 | `lib-stage3-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2` 0.041 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 387 us | 0.044 | `lib-stage3-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `gather48-src-50` | 3 | 22500 | 462 us | 0.048 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-coprime-r7` | 13 | 60060 | 1.11 ms | 0.030 | `lib-stage3-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.77 ms | 0.051 | `lib-stage3-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `stretch-primes` | 89 | 250357 | 4.36 ms | 0.025 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 17.1 ms | 0.040 | `lib-stage3-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `vgg-14-c512-k3` | 3 | 903168 | 19.9 ms | 0.052 | `lib-stage3-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 20 ms | 0.031 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-inner256` | 256 | 1750784 | 44.4 ms | 0.023 | `lib-stage2-lean-u1` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `stretch-pow2stride` | 64 | 1769472 | 31.3 ms | 0.112 | `lib-stage2-lean-u1` 0.110 | `mut-odo-vecdims-add-in-leaf-u1` 0.110 |
| `stretch-r5-8x432` | 8 | 1769472 | 48.6 ms | 0.021 | `lib-stage1` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `stretch-square-1341` | 1341 | 1798281 | 31.1 ms | 0.088 | `lib-stage1` 0.073 | `mut-odo-vecdims-add-in-leaf-u2` 0.077 |
| `stretch-bigstride` | 3 | 1800000 | 51.1 ms | 0.032 | `lib-stage1` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 39.6 ms | 0.059 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1` 0.022 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 41 ms | 0.021 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims` 0.021 |
| `stretch-wide-2xM` | 2 | 1800000 | 39.4 ms | 0.057 | `lib-stage2-lean-u1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 933 us | 0.029 | `lib-stage3-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 29.4 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 29.5 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 29.4 ms | 0.019 | `lib-stage3-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 35.6 ms | 0.015 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 39.5 ms | 0.057 | `lib-stage3-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.66 ms | 0.053 | `lib-stage3-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 4.29 ms | 0.019 | `lib-stage1` 0.012 | `mut-odo-vecdims` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 47.4 ms | 0.034 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 41.9 ms | 0.022 | `lib-stage1` 0.017 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.22 ms | 0.019 | `lib-stage3-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.25 ms | 0.024 | `lib-stage3-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.25 ms | 0.025 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `block-run64-page` | `block` | 64 | 131072 | 2.34 ms | 0.029 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.48 ms | 0.020 | `lib-stage3-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 940 us | 0.029 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 937 us | 0.029 | `lib-stage3-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 29.3 ms | 0.019 | `lib-stage3-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 29.8 ms | 0.019 | `lib-stage2-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.33 ms | 0.026 | `lib-stage3-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.3 ms | 0.026 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.68 ms | 0.053 | `lib-stage3-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 29.3 ms | 0.024 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims` 0.024 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 30.2 ms | 0.024 | `lib-stage3-lean` 0.022 | `mut-odo-vecdims` 0.024 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 32.4 ms | 0.048 | `lib-stage2-lean` 0.041 | `mut-odo-vecdims-add-in-leaf-u2` 0.044 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 118 us | 0.064 | `lib-stage3-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2` 0.041 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 456 us | 0.048 | `lib-stage3-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `rev-primes` | `rev` | 89 | 250357 | 4.41 ms | 0.025 | `lib-stage3-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `runs-65536` | `runs` | 65536 | 1769472 | 27.9 ms | 0.024 | `lib-stage1` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-16384` | `runs` | 16384 | 1785856 | 28.3 ms | 0.024 | `lib-stage1` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-4096` | `runs` | 4096 | 1798144 | 28.5 ms | 0.024 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-1024` | `runs` | 1024 | 1799168 | 28.8 ms | 0.024 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-512` | `runs` | 512 | 1799680 | 28.7 ms | 0.024 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-256` | `runs` | 256 | 1799936 | 28.9 ms | 0.024 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-7` | `runs` | 7 | 1799994 | 32.3 ms | 0.033 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-2` | `runs` | 2 | 1800000 | 39.5 ms | 0.058 | `lib-stage2-lean-u1` 0.025 | `mut-odo-vecdims-add-in-leaf-u1` 0.024 |
| `runs-3` | `runs` | 3 | 1800000 | 35.8 ms | 0.047 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-32` | `runs` | 32 | 1800000 | 29.8 ms | 0.025 | `lib-stage3-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 34.1 ms | 0.041 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u1` 0.023 |
| `runs-48` | `runs` | 48 | 1800000 | 29.5 ms | 0.025 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `runs-5` | `runs` | 5 | 1800000 | 33.2 ms | 0.038 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-64` | `runs` | 64 | 1800000 | 29.2 ms | 0.025 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims` 0.025 |
| `runs-9` | `runs` | 9 | 1800000 | 31.8 ms | 0.030 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-96` | `runs` | 96 | 1800000 | 29.1 ms | 0.025 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims` 0.025 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 29.4 ms | 0.025 | `lib-stage3-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `scaled-r5` | `scaled` | 13 | 15015 | 268 us | 0.030 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 1.04 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 5.13 ms | 0.028 | `lib-stage2-lean-u1` 0.030 | `mut-odo-vecdims` 0.028 |
| `small-patch-k5` | `small` | 5 | 150 | 2.92 us | 0.079 | `lib-stage2-lean-u1` 0.096 | `mut-odo-vecdims-add-in-leaf-u2` 0.058 |
| `small-bcast32` | `small` | 32 | 256 | 4.42 us | 0.051 | `lib-stage2-lean-u1` 0.064 | `mut-odo-vecdims-add-in-leaf-u2` 0.044 |
| `small-flat64` | `small` | 64 | 256 | 4.39 us | 0.058 | `lib-stage3-lean` 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.058 |
| `small-patch-r5` | `small` | 4 | 256 | 5.28 us | 0.088 | `lib-stage2-lean-u1` 0.093 | `mut-odo-vecdims-add-in-leaf-u1` 0.068 |
| `small-row96` | `small` | 96 | 384 | 6.43 us | 0.042 | `lib-stage3-lean` 0.055 | `mut-odo-vecdims-add-in-leaf-u2` 0.042 |
| `window-28x28-k5` | `window` | 5 | 14400 | 277 us | 0.040 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 952 us | 0.084 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.43 ms | 0.053 | `lib-stage3-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 9.59 ms | 0.052 | `lib-stage3-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.028 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.75 ms | 0.051 | `lib-stage3-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.028 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 11.7 ms | 0.052 | `lib-stage3-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 12.4 ms | 0.053 | `lib-stage3-lean` 0.027 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `window-128x128-k7` | `window` | 7 | 729316 | 13.8 ms | 0.031 | `lib-stage1` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |

**No row of the table is read over fewer shapes than the rest, which is a property of the shape set and not of any arm**: NONE of the thirty-four rows is a geomean over fewer shapes than the rest, as on Runs 32 to 37 and where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the THIRTEEN `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers, and with the two `-nosum` controls and the two `sum-only` halves beside them SEVENTEEN of the thirty-four rows carry no corrected time --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows, this one names four different `lib-` arms --- `lib-stage3-lean` on FOUR rows, `lib-stage2-lean` on three, `lib-stage1` on two and `lib-stage2-lean-u1` on one --- where Run 37 named the lean fill on seven and `lib-stage1` on two, with the unrolling twin on `bcastmid`. The cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The properties the next run should test

**Each stride class carries the same three properties, now with Run 38's verdicts** over ten classes, the details beside each class's table. **Two held everywhere and the third broke its LEVEL clause in the same eleven populations Runs 36 and 37 broke it in, at the same multiples**, the pair's variable being the same one: the two `-O2` passes change what `list` and `bq-expand` allocate.

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** **Both clauses held in every one of the eleven populations on both halves**, for a third run running: here `mut-odo-vecdims` is ahead of `bq-expand` over 19 of 19 main-set shapes on each, closest at `stretch-pow2stride` at **0.9997** on the basis and **0.9756** on the control, which is the shape and the arm pair Run 37 read at 0.9945 and 0.9810, Run 36 at 0.9970 and 0.9826, and Run 35 broke at 1.0030. **The basis cell is three ten-thousandths under the line, the closest of the three readings this pair has given it**, so the [open question][open] about that cell is nearer a break than it was --- and it is the CONTROL half that keeps the margin, at 0.9756, as on both earlier runs. The `worst` clause holds in every regime, roster, compiler and layout the README has run, this pair's flagged half included, so `mut-odo-vecdims` --- and this is a statement about THAT arm and not about the route the library ships, which the paragraph below reads separately --- was never slower than the `list` it replaced, on any shape of any population. The `bq-expand` clause folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and is read that way here for the eleventh time.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm still breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `lib-stage1` is slower than `list` on `runs-2` on both halves, at **1.1053** on the basis and **1.3589** on the control, and on `runs-3` on the CONTROL half alone at **1.1295**; `--over-list` reads every other one of the 1280 timed non-control cells this run carries, over all eleven populations on both halves, at or under 1. **They are the same three cells Runs 36 and 37 read, and the three runs sit inside 2.3 points on each** --- 1.0978, 1.3495 and 1.1348 there, 1.1166, 1.3363 and 1.1314 on Run 37, against 1.1053, 1.3589 and 1.1295 here --- **and it is still `list` moving and not `lib-stage1`**: on `runs-2` the fill's own net moves 1.0169 between the halves while `list` moves 1.2503, so the ratio parts by 23 points; and on `runs-3` the fill moves 1.0166 against `list`'s 1.2632. So the one standing break of the wider statement is not merely still there, it is wider under the passes than without them, it sits on the two shortest runs of the class that exists to price short runs, and it has now reproduced twice over.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves, the eighth run running that this property is the one left entirely alone.** The `list` clause is closest at `small-flat64` on the control, **0.06524**, and every closest shape outside `small` sits under 0.053. The `bq-expand` clause is closest at `small-row96` on the CONTROL half, **1.00441**, then `scaled-rank1-m1` at 1.00003 on both halves and `stretch-tall-Mx2` at 1.00000 on both halves of the main set. **Those three figures are Runs 36's and 37's to the digit printed, on the same shape and the same half** --- 0.06524, 1.00441 and 1.00003 --- now across two moved sources and two reboots, which is what allocation being deterministic per call predicts and what no timed column here can show. **The two passes are still what put the closest one where it is**: `small-row96` reads 0.98216 on the basis and 1.00441 on the control, so the clause survives on the flagged half by less than half the margin, and it is the allocation change property 3 records that put it there.

3. **The allocation tiers survive and their ORDER is unbroken in all ten classes and on the main set, on both halves --- and their LEVEL clause BREAKS in every one of the eleven populations, as it did on Runs 36 and 37, and on Run 31 before them, where registration (10) died on it.** The order clause is untouched: the mutable fills sit at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 20.99x to 27.66x, on both halves and in every population, `small` outside the LEVEL clause by the ruling of 2026-09-07 as before. What breaks is the level: **the two passes change what `list` and `bq-expand` ALLOCATE, and this run reads that change at Run 31's own figures.** Run 31's flagged half read `bq-expand` at 2.11x and `list` at 23.45x on the main set and 1.00x to 2.75x and 19.00x to 26.55x across the classes; Runs 36's and 37's flagged halves read the same five, and so does this one --- four runs and two compilers at one set of multiples, with two reboots and two moved sources between the last three. On the main set the fills read 1.00x on both halves while `bq-expand` reads **2.78x** on the basis and **2.11x** on the control and `list` **25.20x** and **23.45x** --- medians over the nineteen shapes, so they are not to be divided. **Read per cell, which is the reading that may be**: over those nineteen shapes the flagged half allocates **0.9342** of the basis on `list` and identically on both its A/A twins, and **0.8119** on `bq-expand` and identically on all three of its, both figures Run 37's to the fourth decimal. Over all eleven populations `list` runs from 0.9073 on `block` to 0.9342 on the main set and `bq-expand` from **0.7263** on `window` to 0.9705 on `block`, and both ranges are that run's to the digit.

**AND SEVEN ARMS OUTSIDE THE TWO FAMILIES NOW MOVE, WHERE ON RUN 37 NONE DID --- this run's one new fact about the level clause.** The unordered consumers `libunord-stage6-sum`, `-stage6-loop-sum`, `-stage7-sum`, `-stage9-sum`, `-stage12-sum`, `-stage13-sum` and the new `-stage14-sum` read geomean deviations of **3.80%** to **13.08%** over the eleven populations, the widest being `libunord-stage12-sum`, which moves further than either family --- `list` at 7.89% and `bq-expand` at 12.52%, both unchanged from Run 37. On Run 37's own JSONs the same arms read **0.9957 to 1.0007** over the same eleven populations, so what changed is the SOURCE: fourteen commits are the only term between the two runs that could reach what those consumers allocate, the compiler, the shim, the project file and the launch being unmoved. **In absolute terms it is hundreds of bytes a call**: `libunord-stage13-sum` allocates 695 B on the basis against 511 on the control on `stretch-inner256`, and the whole family sits at the 0.01x tier, so no tier moves and no property verdict changes with it. It is recorded because the level clause is read as *the passes change what an arm allocates*, and this run adds seven arms to the set of arms they change. `liblist-stage2-sum`, the one arm outside the families that moved on Run 37, moves as it did: **2.19%** against that run's 2.18%. `--alloc` puts 328 of the main set's 608 cells above 100 bytes a call inside 1e-4 between the halves, worst **3.33e-01** on `stretch-wide-2xM/bq-expand-nosum`, where Run 37 put 429 of 570 inside it --- the fall is those seven arms and not a change of instrument --- with the 38 cells under that size set aside as a property of fitting a near-zero allocation. Allocation is deterministic per call, so a level that moves is a code change and never a slot.

`--pair` within a class JSON, the `needs` column's two class-method tiers and the equal weighting of shapes are [README's *Reading a run file*](../README.md#reading-a-run-file).


## The stride classes, run by run

**Run 38 (GHC HEAD `10.1.20260918` against itself, plain -O1 against -O1 with `-fspec-constr -fliberate-case`, dead-spot, exit span, -A32m, launched from disk) records every class twice**, one process per class per half, so each block below has a control-half twin and the cross-half line under it is derived from both. `list` moved between the halves by 24.54 points on `bcastmid` at narrowest and 35.32 on `bcast` at widest, so NONE of the ten classes sits inside the 0.7% that lets two columns be differenced and every cross-half reading below is an ordering of the pair's variable rather than a measurement of it --- as on Runs 36 and 37, which read this same pair, and on Run 31, whose variable was the whole level. Over the ten classes the reader counts **170 arm-comparisons, 62 putting the basis faster and 108 slower**, with no degenerate arm excluded, at geomeans from **1.0547** on `block` to **1.1236** on `window` and extremes of `lib-stage3-lean` at **0.9633** on `block` and `bq-expand-aa-distant` at **1.5155** on `window`. **The low extreme is no longer one arm's, and that is this run's answer to the entry that named it.** `mut-odo-vecdims-add-in-leaf-u1` was the arm the basis ran fastest against the control in nine of Run 37's ten classes, down to **0.7053** on `bcast`; here it takes three --- `compose`, `scaled` and `window` at 0.9931, 0.9880 and 0.9897 --- and the other seven fall to four different `lib-` arms, none of them under 0.96. On the main set that arm reads **1.0205** where Run 37 read 0.9630, inside the registration's 2.5% band, which is item (4) holding and the movement belonging to Run 37's build rather than to this pair. Every `Across the halves` line below reads the basis over the control, ABOVE 1 meaning the control --- the FLAGGED half --- is the faster, as every cross figure in this file does. What each class still decides, and decides on both halves separately, is the three properties, its own floor, and whichever registrations name it. **No registration of this run names a class**: all twenty of its spans are `on main basis`, so each block below carries its properties, its floor and its own cross-half reading, and the registration verdicts are the main set's alone.

First, one table over all of them, transcribed from each class's own table below, in the columns [README's *Reading a run file*](../README.md#reading-a-run-file) fixes, which also says what the blocks under it carry and what installs them.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- and both are read over the POPULATION, so the arm named here may lead on no single shape and the per-shape fingerprint below may name another, which is [the README's per-shape section][pershape]'s own point and not a disagreement --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the properties](#the-properties-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on NINE of the ten --- `bcastmid` and `scaled`, where `lib-stage1` leads, `rev`, `bcast`, `window` and `block`, where this run's new `lib-stage3-lean` does, `runs` and `flip`, where `lib-stage2-lean` does, and `small`, where `lib-stage2-lean-u1` does --- and the family's own ceiling on `compose` alone, where the shipped leaf is faster. **The two pointer leaves held five of Run 30's six bolded ceilings and are parked**, so every bolded ceiling here is the shipped leaf `mut-odo-vecdims-add-in-leaf-u2` itself, which is the form the family would ship. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.043 | 0.064 | **`lib-stage3-lean`** 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 | 0.27% |
| `bcast` | 6 | 0.021 | 0.057 | **`lib-stage3-lean`** 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 | 0.75% |
| `bcastmid` | 4 | 0.029 | 0.053 | **`lib-stage1`** 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 | 0.79% |
| `window` | 8 | 0.051 | 0.084 | **`lib-stage3-lean`** 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 | 0.53% |
| `scaled` | 3 | 0.027 | 0.030 | **`lib-stage1`** 0.022 | `mut-odo-vecdims-add-in-leaf-u1` 0.022 | 0.35% |
| `runs` | 17 | 0.026 | 0.058 | **`lib-stage2-lean`** 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 | 3.40% |
| `flip` | 6 | 0.029 | 0.053 | **`lib-stage2-lean`** 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.026 | 0.82% |
| `block` | 5 | 0.023 | 0.029 | **`lib-stage3-lean`** 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 | 0.53% |
| `small` | 5 | 0.061 | 0.088 | **`lib-stage2-lean-u1`** 0.052 | `mut-odo-vecdims-add-in-leaf-u2` 0.053 | 0.66% |
| `compose` | 4 | 0.023 | 0.029 | `lib-stage2-lean` 0.015 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.015 | 0.54% |

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On ONE row --- `compose` --- the bold sits in the CEILING column instead**, and it names the shipped leaf `mut-odo-vecdims-add-in-leaf-u2`; against Run 37 the rows that changed sides are `block` and `small`, both leaving that set for the arm outside the family, so where that run had three rows in it this one has `compose` alone. SIX rows change the arm they name rather than the side, and FOUR of the six name the arm this run's roster added: `rev`, `bcast`, `window` and `block` name `lib-stage3-lean`, where Run 37 named `lib-stage1` on the first and `lib-stage2-lean` on the other three; `bcastmid` names `lib-stage1` where that run named `lib-stage2-lean`; and `small` names `lib-stage2-lean-u1`. No ceiling arm changed, `scaled`'s being `mut-odo-vecdims-add-in-leaf-u1` and every other row's the shipped leaf, as on Run 37. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship --- an override no row here exercises, both of them having been parked on 2026-09-13. **THREE rows tie at three decimals** --- `rev` and `scaled` at 0.022 and `compose` at 0.015 --- and the bold on each is `--block`'s own, computed on the unrounded values where the printed ones cannot separate: outside the family against the ceiling, 0.021696 against 0.021983, 0.021785 against 0.022300 and 0.014741 against 0.014572, so `rev` and `scaled` fall to the arm outside the family and `compose` to the ceiling. **The three partings are 2.9e-4, 5.2e-4 and 1.7e-4**, all inside the three decimals the table prints, so on each row the bold marks a tie-break and not a lead a reader of the table could see.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.10* | *127* | *3.22x* |
| liblist-stage1-sum | -- | -- | 0.12 | 148 | 1.02x |
| liblist-stage2-sum | -- | -- | 0.09 | 148 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.08 | 148 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.13 | 148 | 1.01x |
| liblist-stage5-sum | -- | -- | 0.09 | 148 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.12 | 147 | 1.03x |
| libunord-stage12-sum | -- | -- | 0.02 | 157 | 0.01x |
| libunord-stage13-sum | -- | -- | 0.02 | 157 | 0.01x |
| libunord-stage14-sum | -- | -- | 0.01 | 157 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.03 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.02 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.02 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.02 | 157 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *148* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *158* | *0.00x* |
| lib-stage3-lean | 0.022 | 0.028 | 0.12 | 148 | 1.01x |
| lib-stage2-lean | 0.022 | 0.028 | 0.12 | 148 | 1.01x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.041* | *0.11* | *147* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.041* | *0.09* | *147* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.041 | 0.10 | 147 | 1.00x |
| lib-stage1 | 0.023 | 0.040 | 0.11 | 147 | 1.02x |
| lib-stage2-lean-u1 | 0.026 | 0.033 | 0.15 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.043 | 0.13 | 147 | 1.00x |
| *mut-odo-vecdims-aa* | *0.043* | *0.064* | *0.10* | *138* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.043* | *0.064* | *0.13* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.043** | 0.064 | 0.10 | 138 | 1.00x |
| *bq-expand-aa-distant* | *0.139* | *0.236* | *0.13* | *122* | *3.22x* |
| bq-expand | 0.139 | 0.237 | 0.10 | 122 | 3.22x |
| *bq-expand-aa-adjacent* | *0.139* | *0.237* | *0.10* | *122* | *3.22x* |
| list (baseline) | 1.000 | 1.000 | 0.20 | 85 | 26.11x |
| *list-aa-distant* | *1.003* | *1.005* | *0.21* | *85* | *26.11x* |
| *list-aa-adjacent* | *1.003* | *1.003* | *0.23* | *85* | *26.11x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0027, worst cell 0.34% on `rev-gather48-src-50`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.02% on `rev-cnn-L1-24x24-c1`, its interval missing 1. The in-situ term reads 1.0013, 1.0147 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0027, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m54s, peak 96 MiB in use, 24 MiB max residency; the reader reads 34 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.56 ms per call raw, 4.41 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.064/0.048/0.025

**Across the halves:** 3 of the 17 arms are faster on this half and 14 slower, at a geomean of 1.0977, from `lib-stage2-lean-u1` at 0.9890 to `bq-expand-aa-adjacent` at 1.3060, with `list` itself at 1.2779. **The baseline moved 27.79% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.064, tiers at 1.00x, 3.22x, 26.11x --- and `lib-stage3-lean` leads outside the family at 0.022, priced against `mut-odo-vecdims` at 0.5095 over 3 of 3 shapes at sign p 0.25, a margin of 49.05% against this class's 0.27% floor (`list-aa-adjacent`). **`rev` is the new arm's outright**: `lib-stage3-lean` leads outside the family here and on three other classes, and this is the class with the run's TIGHTEST floor, 0.27%, so a 49.05% margin is read against the narrowest bar of the eleven populations. Its two columns may NOT be differenced, `list` having moved 27.79 of a point, at a class geomean of 1.0977 over the 17 arms, with 5 of 9 strategies past an A/A bar of 0.53 points. The counted work reads a counts geomean of 1.1579 over the same arms, 17 of them counted. Its counted work parts by 15.79 points where its clock parts by 9.77, so about 0.62 of the instruction saving reaches the clock.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09, for Run 28 --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and Run 28's registration (7) read no crossover on it; this run reads the class for stage twelve's tie with stage eleven, at registration (2).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.58* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.49 | 62 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.48 | 62 | 1.00x |
| liblist-stage5-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage14-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.53 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 74 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.33* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *69* | *0.00x* |
| lib-stage3-lean | 0.015 | 0.020 | 0.49 | 62 | 1.00x |
| lib-stage2-lean | 0.016 | 0.020 | 0.49 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.016* | *0.020* | *0.51* | *62* | *1.00x* |
| lib-stage1 | 0.016 | 0.020 | 0.40 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.016* | *0.020* | *0.43* | *62* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.016 | 0.020 | 0.39 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.024 | 0.49 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.018 | 0.020 | 0.41 | 61 | 1.00x |
| *mut-odo-vecdims-aa* | *0.021* | *0.057* | *0.31* | *61* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.021* | *0.057* | *0.30* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.021** | 0.057 | 0.07 | 61 | 1.00x |
| bq-expand | 0.092 | 0.142 | 0.66 | 46 | 1.00x |
| *bq-expand-aa-adjacent* | *0.092* | *0.141* | *0.68* | *46* | *1.00x* |
| *bq-expand-aa-distant* | *0.092* | *0.143* | *0.06* | *46* | *1.00x* |
| list (baseline) | 1.000 | 1.000 | 1.07 | 17 | 20.99x |
| *list-aa-distant* | *1.006* | *1.014* | *0.80* | *17* | *20.99x* |
| *list-aa-adjacent* | *1.008* | *1.012* | *0.79* | *17* | *20.99x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0075, worst cell 1.18% on `bcast-src8`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0003 on a worst cell of 0.38% on `bcast-src8`, its interval covering 1. The in-situ term reads 1.0185, 1.0110 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0073, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h17m45s, peak 194 MiB in use, 42 MiB max residency; the reader reads 34 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 30.6 ms per call raw, 29.5 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.029/0.019/0.057/0.015/0.019/0.019

**Across the halves:** 8 of the 17 arms are faster on this half and 9 slower, at a geomean of 1.0813, from `lib-stage1` at 0.9970 to `list-aa-distant` at 1.3646, with `list` itself at 1.3532. **The baseline moved 35.32% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.057, tiers at 1.00x, 1.00x, 20.99x --- and `lib-stage3-lean` leads outside the family at 0.015, priced against `mut-odo-vecdims` at 0.6587 over 6 of 6 shapes at sign p 0.031, a margin of 34.13% against this class's 0.75% floor (`list-aa-adjacent`). **`bcast` moves `list` the furthest of the ten**, 35.32 points against `bcastmid`'s 24.54, and it is the class where `bq-expand` allocates at the fills' own 1.00x tier --- so the arm the table calls the last candidate costs nothing in allocation here and everything in time. Its two columns may NOT be differenced, `list` having moved 35.32 of a point, at a class geomean of 1.0813 over the 17 arms, with 2 of 9 strategies past an A/A bar of 0.84 points. The counted work reads a counts geomean of 1.1628 over the same arms, 17 of them counted. Its counted work parts by 16.28 points where its clock parts by 8.13, so about 0.50 of the instruction saving reaches the clock.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.39* | *66* | *1.92x* |
| liblist-stage1-sum | -- | -- | 0.39 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.36 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.36 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.35 | 82 | 1.00x |
| liblist-stage5-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.51 | 82 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage14-sum | -- | -- | 0.02 | 96 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.30 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 96 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.44* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *88* | *0.00x* |
| lib-stage1 | 0.012 | 0.017 | 0.43 | 82 | 1.00x |
| lib-stage2-lean | 0.012 | 0.018 | 0.38 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.012 | 0.018 | 0.38 | 82 | 1.00x |
| lib-stage3-lean | 0.012 | 0.018 | 0.37 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.020* | *0.030* | *0.33* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.020* | *0.030* | *0.25* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.030 | 0.30 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.023 | 0.031 | 0.49 | 78 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.053* | *0.29* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.029* | *0.053* | *0.21* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.053 | 0.21 | 76 | 1.00x |
| *bq-expand-aa-adjacent* | *0.100* | *0.184* | *0.39* | *60* | *1.92x* |
| bq-expand | 0.100 | 0.184 | 0.40 | 60 | 1.92x |
| *bq-expand-aa-distant* | *0.100* | *0.184* | *0.37* | *60* | *1.92x* |
| *list-aa-distant* | *0.999* | *1.002* | *0.77* | *28* | *23.56x* |
| list (baseline) | 1.000 | 1.000 | 0.82 | 28 | 23.56x |
| *list-aa-adjacent* | *1.002* | *1.003* | *0.89* | *28* | *23.56x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9921, worst cell 1.75% on `bcastmid-primes`, and 1 of 8 intervals cover 1. The `sum-only` halves agree at 0.9987 on a worst cell of 0.38% on `bcastmid-primes`, its interval missing 1. The in-situ term reads 1.0170, 1.0903 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9968, which the correction amplifies by 2.38x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h11m52s, peak 129 MiB in use, 35 MiB max residency; the reader reads 34 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 48.5 ms per call raw, 47.4 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.053/0.019/0.034/0.022

**Across the halves:** 8 of the 17 arms are faster on this half and 9 slower, at a geomean of 1.0782, from `lib-stage2-lean` at 0.9766 to `list-aa-adjacent` at 1.2532, with `list` itself at 1.2454. **The baseline moved 24.54% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.053, tiers at 1.00x, 1.92x, 23.56x --- and `lib-stage1` leads outside the family at 0.012, priced against `mut-odo-vecdims` at 0.4139 over 4 of 4 shapes at sign p 0.12, a margin of 58.61% against this class's 0.79% floor (`mut-odo-vecdims-add-in-leaf-u2-aa-distant`). **`bcastmid` carries the widest margin of the ten**, 58.61%: `lib-stage1` leads outside the family at 0.012 against `mut-odo-vecdims`'s 0.029, which is the largest lead any class gives an arm outside the family, and it moves `list` the least of the ten. Its two columns may NOT be differenced, `list` having moved 24.54 of a point, at a class geomean of 1.0782 over the 17 arms, with 4 of 9 strategies past an A/A bar of 0.64 points. The counted work reads a counts geomean of 1.1639 over the same arms, 17 of them counted. Its counted work parts by 16.39 points where its clock parts by 7.82, so about 0.48 of the instruction saving reaches the clock.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09, for Run 28, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.34* | *61* | *3.86x* |
| liblist-stage1-sum | -- | -- | 0.16 | 84 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.18 | 84 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.15 | 84 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.16 | 84 | 1.00x |
| liblist-stage5-sum | -- | -- | 0.17 | 84 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.20 | 84 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.08 | 102 | 0.03x |
| libunord-stage13-sum | -- | -- | 0.08 | 102 | 0.03x |
| libunord-stage14-sum | -- | -- | 0.10 | 102 | 0.03x |
| libunord-stage6-loop-sum | -- | -- | 0.64 | 94 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.08 | 100 | 0.03x |
| libunord-stage7-sum | -- | -- | 0.05 | 102 | 0.03x |
| libunord-stage9-sum | -- | -- | 0.11 | 100 | 0.03x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.26* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *97* | *0.00x* |
| lib-stage3-lean | 0.024 | 0.027 | 0.17 | 84 | 1.00x |
| lib-stage2-lean | 0.024 | 0.028 | 0.16 | 84 | 1.00x |
| lib-stage1 | 0.025 | 0.027 | 0.18 | 84 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.030* | *0.17* | *82* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.030 | 0.16 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.030* | *0.23* | *82* | *1.00x* |
| lib-stage2-lean-u1 | 0.029 | 0.032 | 0.30 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.029 | 0.031 | 0.22 | 82 | 1.00x |
| *mut-odo-vecdims-aa* | *0.051* | *0.084* | *0.15* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.085* | *0.16* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.051** | 0.084 | 0.17 | 76 | 1.00x |
| bq-expand | 0.177 | 0.215 | 0.34 | 57 | 3.86x |
| *bq-expand-aa-adjacent* | *0.177* | *0.215* | *0.32* | *57* | *3.86x* |
| *bq-expand-aa-distant* | *0.178* | *0.215* | *0.27* | *57* | *3.86x* |
| list (baseline) | 1.000 | 1.000 | 0.47 | 30 | 27.66x |
| *list-aa-distant* | *1.001* | *1.005* | *0.58* | *30* | *27.66x* |
| *list-aa-adjacent* | *1.002* | *1.005* | *0.39* | *30* | *27.66x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 0.9947, worst cell 2.66% on `window-224x224-k3-s2`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `window-64x64-c16-k3`, its interval covering 1. The in-situ term reads 1.0087, 1.1582 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9966, which the correction amplifies by 1.55x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h23m32s, peak 124 MiB in use, 49 MiB max residency; the reader reads 34 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 14.2 ms per call raw, 13.8 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.040/0.051/0.084/0.031/0.053/0.052/0.053/0.052

**Across the halves:** 8 of the 17 arms are faster on this half and 9 slower, at a geomean of 1.1236, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9897 to `bq-expand-aa-distant` at 1.5155, with `list` itself at 1.2876. **The baseline moved 28.76% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.084, tiers at 1.00x, 3.86x, 27.66x --- and `lib-stage3-lean` leads outside the family at 0.024, priced against `mut-odo-vecdims` at 0.4232 over 8 of 8 shapes at sign p 0.0078, a margin of 57.68% against this class's 0.53% floor (`mut-odo-vecdims-aa-distant`). **`window` has the highest class geomean of the ten**, 1.1236, and its `bq-expand` allocates 0.7263 of the basis's on the flagged half --- the largest allocation move of the eleven populations, against `block`'s 0.9705. Its two columns may NOT be differenced, `list` having moved 28.76 of a point, at a class geomean of 1.1236 over the 17 arms, with 4 of 9 strategies past an A/A bar of 0.50 points. The counted work reads a counts geomean of 1.1686 over the same arms, 17 of them counted. Its counted work parts by 16.86 points where its clock parts by 12.36, so about 0.73 of the instruction saving reaches the clock, the highest rate of the ten.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.07* | *118* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.20 | 128 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.12 | 128 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.13 | 128 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.14 | 128 | 1.00x |
| liblist-stage5-sum | -- | -- | 0.27 | 128 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.12 | 128 | 1.01x |
| libunord-stage12-sum | -- | -- | 0.18 | 128 | 1.00x |
| libunord-stage13-sum | -- | -- | 0.14 | 128 | 1.00x |
| libunord-stage14-sum | -- | -- | 0.12 | 128 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.15 | 128 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.12 | 128 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.30 | 128 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.16 | 128 | 1.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *146* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *138* | *0.00x* |
| lib-stage1 | 0.022 | 0.030 | 0.20 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.030 | 0.16 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.023 | 0.031 | 0.16 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.023* | *0.030* | *0.13* | *127* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.023* | *0.031* | *0.17* | *127* | *1.00x* |
| lib-stage2-lean | 0.023 | 0.030 | 0.12 | 128 | 1.00x |
| lib-stage3-lean | 0.023 | 0.030 | 0.13 | 128 | 1.00x |
| lib-stage2-lean-u1 | 0.026 | 0.030 | 0.24 | 126 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.029* | *0.08* | *127* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.027* | *0.029* | *0.08* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.030 | 0.12 | 127 | 1.00x |
| *bq-expand-aa-distant* | *0.091* | *0.102* | *0.08* | *111* | *1.21x* |
| *bq-expand-aa-adjacent* | *0.091* | *0.102* | *0.08* | *111* | *1.21x* |
| bq-expand | 0.091 | 0.102 | 0.06 | 111 | 1.21x |
| *list-aa-adjacent* | *0.999* | *1.004* | *0.34* | *69* | *21.49x* |
| list (baseline) | 1.000 | 1.000 | 0.29 | 69 | 21.49x |
| *list-aa-distant* | *1.003* | *1.006* | *0.23* | *69* | *21.49x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa` at 0.9965, worst cell 0.81% on `scaled-r5`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0011 on a worst cell of 0.46% on `scaled-rank1-m1`, its interval covering 1. The in-situ term reads 1.0191, 1.0115 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9986, which the correction amplifies by 2.42x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m54s, peak 114 MiB in use, 38 MiB max residency; the reader reads 34 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 5.31 ms per call raw, 5.13 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.023/0.028/0.030

**Across the halves:** 4 of the 17 arms are faster on this half and 13 slower, at a geomean of 1.0631, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9880 to `list-aa-adjacent` at 1.3004, with `list` itself at 1.2911. **The baseline moved 29.11% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.030, tiers at 1.00x, 1.21x, 21.49x --- and `lib-stage1` leads outside the family at 0.022, priced against `mut-odo-vecdims` at 0.9110 over 2 of 3 shapes at sign p 1, a margin of 8.90% against this class's 0.35% floor (`mut-odo-vecdims-add-in-leaf-u2-aa`). **`scaled` gives the narrowest margin of the ten**, 8.90%: `lib-stage1` at 0.022 barely leads `mut-odo-vecdims` at 0.027, and it is the one class whose ceiling is `mut-odo-vecdims-add-in-leaf-u1` rather than the shipped leaf. Its two columns may NOT be differenced, `list` having moved 29.11 of a point, at a class geomean of 1.0631 over the 17 arms, with 6 of 9 strategies past an A/A bar of 0.72 points. The counted work reads a counts geomean of 1.1465 over the same arms, 17 of them counted. Its counted work parts by 14.65 points where its clock parts by 6.31, so about 0.43 of the instruction saving reaches the clock.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven, on Run 24 to fourteen and on Run 34 to seventeen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-32` (`l` 1800000, `sInner` 32), `runs-48` (`l` 1800000, `sInner` 48) and `runs-64` (`l` 1800000, `sInner` 64) --- the three landed on Run 34, inside the gap from 9 to 96 where a fit to Run 33's stage-eleven curve had put a minimum --- `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.36* | *52* | *1.08x* |
| liblist-stage1-sum | -- | -- | 0.10 | 61 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.17 | 69 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.02 | 72 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 72 | 0.00x |
| liblist-stage5-sum | -- | -- | 0.03 | 72 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.11 | 61 | 0.42x |
| libunord-stage12-sum | -- | -- | 0.03 | 72 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.03 | 72 | 0.00x |
| libunord-stage14-sum | -- | -- | 0.03 | 72 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.04 | 70 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.04 | 72 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.17* | *77* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| lib-stage2-lean | 0.023 | 0.033 | 0.12 | 60 | 1.00x |
| lib-stage3-lean | 0.023 | 0.035 | 0.13 | 60 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.025* | *0.43* | *59* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.025* | *0.11* | *59* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.025 | 0.11 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.026 | 0.11 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.026* | *0.058* | *0.09* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.026** | 0.058 | 0.11 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.058* | *0.09* | *59* | *1.00x* |
| lib-stage2-lean-u1 | 0.034 | 0.037 | 0.13 | 56 | 1.00x |
| bq-expand | 0.093 | 0.143 | 0.45 | 46 | 1.08x |
| *bq-expand-aa-adjacent* | *0.093* | *0.143* | *0.52* | *46* | *1.08x* |
| *bq-expand-aa-distant* | *0.094* | *0.145* | *0.04* | *46* | *1.08x* |
| lib-stage1 | 0.099 | 1.105 | 0.19 | 51 | 1.42x |
| list (baseline) | 1.000 | 1.000 | 2.62 | 17 | 21.30x |
| *list-aa-distant* | *1.033* | *1.048* | *0.34* | *17* | *21.30x* |
| *list-aa-adjacent* | *1.034* | *1.047* | *0.33* | *17* | *21.30x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0340, worst cell 4.75% on `runs-65536`, and 1 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.33% on `runs-512`, its interval covering 1. The in-situ term reads 1.0281, 1.0256 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0328, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h50m8s, peak 644 MiB in use, 289 MiB max residency; the reader reads 34 benchmarks over 17 shapes of the runs class. Anchor: `runs-2`, `list` at 40.6 ms per call raw, 39.5 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-32, runs-48, runs-64, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.058/0.047/0.041/0.038/0.033/0.030/0.025/0.025/0.025/0.025/0.024/0.024/0.024/0.024/0.024/0.024/0.025

**Across the halves:** 4 of the 17 arms are faster on this half and 13 slower, at a geomean of 1.0897, from `lib-stage2-lean` at 0.9974 to `lib-stage2-lean-u1` at 1.3092, with `list` itself at 1.2819. **The baseline moved 28.19% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.058, tiers at 1.00x, 1.08x, 21.30x --- and `lib-stage2-lean` leads outside the family at 0.023, priced against `mut-odo-vecdims` at 0.8003 over 17 of 17 shapes at sign p 1.5e-05, a margin of 19.97% against this class's 3.40% floor (`list-aa-adjacent`). **`runs` carries the run's widest floor**, 3.40%, an order of magnitude above the next class, so its 19.97% margin is the one read against the loosest bar --- and it is the class the standing break of the wider statement sits in, `lib-stage1` slower than `list` on `runs-2` and, on the control half, on `runs-3`. Its two columns may NOT be differenced, `list` having moved 28.19 of a point, at a class geomean of 1.0897 over the 17 arms, with 4 of 9 strategies past an A/A bar of 0.36 points. The counted work reads a counts geomean of 1.1555 over the same arms, 17 of them counted. Its counted work parts by 15.55 points where its clock parts by 8.97, so about 0.58 of the instruction saving reaches the clock.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes: in the order they run, `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which landed 2026-09-09 and is `runs-96`'s construction under a `flip` name --- the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two; `flip-whole-square` (`l` 1798281, `sInner` 1341); `flip-last-c32` (`l` 165888, `sInner` 3); `flip-last-rows` (`l` 1800000, `sInner` 96); and the two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed, `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. The control sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.43* | *66* | *1.05x* |
| liblist-stage1-sum | -- | -- | 0.12 | 83 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.12 | 89 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.09 | 93 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.15 | 93 | 1.00x |
| liblist-stage5-sum | -- | -- | 0.09 | 93 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.12 | 83 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage14-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.01 | 98 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 98 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.11* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *93* | *0.00x* |
| lib-stage2-lean | 0.022 | 0.041 | 0.29 | 84 | 1.00x |
| lib-stage3-lean | 0.022 | 0.042 | 0.29 | 84 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.044* | *0.35* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.044 | 0.12 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.044* | *0.13* | *80* | *1.00x* |
| lib-stage2-lean-u1 | 0.027 | 0.047 | 0.28 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.047 | 0.35 | 80 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.028* | *0.052* | *0.10* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.053 | 0.11 | 77 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.052* | *0.09* | *77* | *1.00x* |
| lib-stage1 | 0.033 | 0.048 | 0.24 | 82 | 1.00x |
| *bq-expand-aa-adjacent* | *0.089* | *0.183* | *0.42* | *60* | *1.05x* |
| bq-expand | 0.089 | 0.183 | 0.46 | 60 | 1.05x |
| *bq-expand-aa-distant* | *0.091* | *0.183* | *0.08* | *60* | *1.05x* |
| list (baseline) | 1.000 | 1.000 | 0.72 | 32 | 21.18x |
| *list-aa-distant* | *1.007* | *1.020* | *0.50* | *32* | *21.18x* |
| *list-aa-adjacent* | *1.008* | *1.024* | *0.26* | *32* | *21.18x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0082, worst cell 2.54% on `flip-last-rows`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.32% on `flip-fwd-rows96`, its interval covering 1. The in-situ term reads 1.0210, 1.0160 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0060, which the correction amplifies by 1.34x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h17m45s, peak 209 MiB in use, 76 MiB max residency; the reader reads 34 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 31.3 ms per call raw, 30.2 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.024/0.024/0.053/0.048/0.026/0.026

**Across the halves:** 7 of the 17 arms are faster on this half and 10 slower, at a geomean of 1.0655, from `lib-stage3-lean` at 0.9822 to `list-aa-adjacent` at 1.2981, with `list` itself at 1.2977. **The baseline moved 29.77% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.053, tiers at 1.00x, 1.05x, 21.18x --- and `lib-stage2-lean` leads outside the family at 0.022, priced against `mut-odo-vecdims` at 0.7238 over 6 of 6 shapes at sign p 0.031, a margin of 27.62% against this class's 0.82% floor (`bq-expand-aa-distant`). **`flip` holds the run's largest single cell movement**: `flip-whole-square/lib-stage2-lean-u1` reads 0.6082 in time on 1.0625 in counts, the top row of `--cell-movers` over all 2720 cells, and the counts say the codegen is not why. Its two columns may NOT be differenced, `list` having moved 29.77 of a point, at a class geomean of 1.0655 over the 17 arms, with 5 of 9 strategies past an A/A bar of 1.24 points. The counted work reads a counts geomean of 1.1463 over the same arms, 17 of them counted. Its counted work parts by 14.63 points where its clock parts by 6.55, so about 0.45 of the instruction saving reaches the clock.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.11* | *103* | *1.06x* |
| liblist-stage1-sum | -- | -- | 0.11 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.14 | 122 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.03 | 129 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.04 | 129 | 0.00x |
| liblist-stage5-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.10 | 113 | 0.42x |
| libunord-stage12-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.04 | 129 | 0.00x |
| libunord-stage14-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.03 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.04 | 129 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 129 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.21* | *129* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *122* | *0.00x* |
| lib-stage3-lean | 0.020 | 0.023 | 0.13 | 111 | 1.00x |
| lib-stage2-lean | 0.020 | 0.023 | 0.08 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.025* | *0.15* | *111* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.021* | *0.025* | *0.12* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.025 | 0.11 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.026 | 0.13 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.023* | *0.029* | *0.09* | *111* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.029* | *0.11* | *111* | *1.00x* |
| **mut-odo-vecdims** | **0.023** | 0.029 | 0.12 | 110 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.027 | 0.20 | 111 | 1.00x |
| lib-stage1 | 0.050 | 0.058 | 0.11 | 103 | 1.42x |
| *bq-expand-aa-adjacent* | *0.086* | *0.086* | *0.11* | *96* | *1.06x* |
| bq-expand | 0.086 | 0.087 | 0.11 | 96 | 1.06x |
| *bq-expand-aa-distant* | *0.086* | *0.087* | *0.10* | *96* | *1.06x* |
| *list-aa-adjacent* | *0.999* | *1.002* | *0.21* | *54* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 0.25 | 54 | 21.22x |
| *list-aa-distant* | *1.001* | *1.011* | *0.19* | *54* | *21.22x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9947, worst cell 2.00% on `block-r3-vol64`, and 8 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.05% on `block-run64-off7`, its interval covering 1. The in-situ term reads 1.0255, 1.0113 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9980, which the correction amplifies by 2.49x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h14m45s, peak 135 MiB in use, 51 MiB max residency; the reader reads 34 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.64 ms per call raw, 4.48 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.019/0.024/0.029/0.025/0.020

**Across the halves:** 9 of the 17 arms are faster on this half and 8 slower, at a geomean of 1.0547, from `lib-stage3-lean` at 0.9633 to `list-aa-adjacent` at 1.3291, with `list` itself at 1.3281. **The baseline moved 32.81% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.029, tiers at 1.00x, 1.06x, 21.22x --- and `lib-stage3-lean` leads outside the family at 0.020, priced against `mut-odo-vecdims` at 0.8598 over 5 of 5 shapes at sign p 0.062, a margin of 14.02% against this class's 0.53% floor (`mut-odo-vecdims-aa`). **`block` has the lowest class geomean of the ten**, 1.0547, and it holds the low extreme of all 170 arm-comparisons, `lib-stage3-lean` at 0.9633 --- the one arm anywhere in the classes that the basis half runs better than three points faster than the control does. Its two columns may NOT be differenced, `list` having moved 32.81 of a point, at a class geomean of 1.0547 over the 17 arms, with 4 of 9 strategies past an A/A bar of 2.54 points. The counted work reads a counts geomean of 1.1437 over the same arms, 17 of them counted. Its counted work parts by 14.37 points where its clock parts by 5.47, so about 0.38 of the instruction saving reaches the clock, the lowest rate of the ten.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.16* | *222* | *1.44x* |
| liblist-stage1-sum | -- | -- | 0.19 | 228 | 1.82x |
| liblist-stage2-sum | -- | -- | 0.19 | 230 | 1.66x |
| liblist-stage3-sum | -- | -- | 0.24 | 225 | 1.85x |
| liblist-stage4-sum | -- | -- | 0.13 | 235 | 1.42x |
| liblist-stage5-sum | -- | -- | 0.24 | 235 | 1.40x |
| libunord-stage1-sum | -- | -- | 0.22 | 222 | 2.08x |
| libunord-stage12-sum | -- | -- | 0.21 | 240 | 0.37x |
| libunord-stage13-sum | -- | -- | 0.12 | 243 | 0.27x |
| libunord-stage14-sum | -- | -- | 0.14 | 243 | 0.27x |
| libunord-stage6-loop-sum | -- | -- | 0.18 | 239 | 0.69x |
| libunord-stage6-sum | -- | -- | 0.13 | 239 | 0.71x |
| libunord-stage7-sum | -- | -- | 0.14 | 239 | 0.71x |
| libunord-stage9-sum | -- | -- | 0.26 | 237 | 0.48x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.29* | *238* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.02* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.04* | *250* | *0.01x* |
| lib-stage2-lean-u1 | 0.052 | 0.096 | 0.19 | 228 | 1.45x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.053 | 0.068 | 0.33 | 229 | 1.28x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.053* | *0.067* | *0.24* | *229* | *1.28x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.053* | *0.068* | *0.31* | *229* | *1.28x* |
| lib-stage2-lean | 0.054 | 0.101 | 0.18 | 228 | 1.60x |
| lib-stage3-lean | 0.054 | 0.103 | 0.17 | 227 | 1.58x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.056 | 0.068 | 0.29 | 229 | 1.28x |
| *mut-odo-vecdims-aa* | *0.061* | *0.088* | *0.29* | *229* | *1.27x* |
| *mut-odo-vecdims-aa-distant* | *0.061* | *0.088* | *0.24* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.061** | 0.088 | 0.29 | 229 | 1.27x |
| lib-stage1 | 0.095 | 0.119 | 0.23 | 221 | 2.36x |
| *bq-expand-aa-adjacent* | *0.137* | *0.198* | *0.17* | *218* | *1.44x* |
| bq-expand | 0.137 | 0.199 | 0.16 | 218 | 1.44x |
| *bq-expand-aa-distant* | *0.137* | *0.199* | *0.16* | *217* | *1.44x* |
| *list-aa-distant* | *0.999* | *1.000* | *0.22* | *180* | *21.57x* |
| *list-aa-adjacent* | *1.000* | *1.003* | *0.21* | *180* | *21.57x* |
| list (baseline) | 1.000 | 1.000 | 0.21 | 180 | 21.57x |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9934, worst cell 2.78% on `small-bcast32`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 0.9998 on a worst cell of 0.07% on `small-flat64`, its interval covering 1. The in-situ term reads 0.9872, 0.9924 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9958, which the correction amplifies by 1.53x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h14m50s, peak 130 MiB in use, 52 MiB max residency; the reader reads 34 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.66 us per call raw, 6.43 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.042/0.079/0.051/0.058/0.088

**Across the halves:** 3 of the 17 arms are faster on this half and 14 slower, at a geomean of 1.0689, from `lib-stage2-lean` at 0.9714 to `list-aa-adjacent` at 1.2858, with `list` itself at 1.2744. **The baseline moved 27.44% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.088, tiers at 1.27x, 1.44x, 21.57x --- and `lib-stage2-lean-u1` leads outside the family at 0.052, priced against `mut-odo-vecdims` at 0.8515 over 1 of 5 shapes at sign p 0.38, a margin of 14.85% against this class's 0.66% floor (`mut-odo-vecdims-aa`). **`small` is the one class whose two halves bold a different COLUMN**: the arm outside the family leads on the basis and the family's own ceiling on the control, where the other nine agree on the column and only `rev` and `block` change the arm. It is also the class the level clause of property 3 has been ruled outside of since 2026-09-07, its fills sitting at 1.27x rather than 1.00x. Its two columns may NOT be differenced, `list` having moved 27.44 of a point, at a class geomean of 1.0689 over the 17 arms, with 7 of 9 strategies past an A/A bar of 0.90 points. The counted work reads a counts geomean of 1.1355 over the same arms, 17 of them counted. Its counted work parts by 13.55 points where its clock parts by 6.89, so about 0.51 of the instruction saving reaches the clock.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.35* | *86* | *1.35x* |
| liblist-stage1-sum | -- | -- | 0.36 | 98 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.33 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.31 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.32 | 98 | 1.00x |
| liblist-stage5-sum | -- | -- | 0.36 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.04 | 110 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.01 | 110 | 0.00x |
| libunord-stage14-sum | -- | -- | 0.04 | 110 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.33 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.33 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.33 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.04 | 110 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.29* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *105* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.015 | 0.016 | 0.27 | 98 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.015* | *0.016* | *0.31* | *98* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.015* | *0.016* | *0.29* | *98* | *1.00x* |
| lib-stage2-lean | 0.015 | 0.017 | 0.30 | 98 | 1.00x |
| lib-stage3-lean | 0.015 | 0.017 | 0.30 | 98 | 1.00x |
| lib-stage1 | 0.015 | 0.017 | 0.37 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.017 | 0.30 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.017 | 0.019 | 0.30 | 97 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.029* | *0.24* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.023** | 0.029 | 0.17 | 94 | 1.00x |
| *mut-odo-vecdims-aa* | *0.023* | *0.029* | *0.23* | *94* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.094* | *0.102* | *0.37* | *79* | *1.35x* |
| bq-expand | 0.094 | 0.102 | 0.38 | 79 | 1.35x |
| *bq-expand-aa-distant* | *0.094* | *0.102* | *0.24* | *79* | *1.35x* |
| list (baseline) | 1.000 | 1.000 | 0.74 | 44 | 22.01x |
| *list-aa-adjacent* | *1.002* | *1.009* | *0.64* | *44* | *22.01x* |
| *list-aa-distant* | *1.002* | *1.007* | *0.69* | *44* | *22.01x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa` at 1.0054, worst cell 1.53% on `compose-scalar`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0002 on a worst cell of 0.14% on `compose-scalar`, its interval covering 1. The in-situ term reads 1.0080, 1.0183 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0016, which the correction amplifies by 3.36x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h11m54s, peak 126 MiB in use, 33 MiB max residency; the reader reads 34 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 30.9 ms per call raw, 29.8 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.029/0.029/0.019/0.019

**Across the halves:** 8 of the 17 arms are faster on this half and 9 slower, at a geomean of 1.0683, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9931 to `list-aa-adjacent` at 1.3130, with `list` itself at 1.2955. **The baseline moved 29.55% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.029, tiers at 1.00x, 1.35x, 22.01x --- and `lib-stage2-lean` leads outside the family at 0.015, priced against `mut-odo-vecdims` at 0.6323 over 4 of 4 shapes at sign p 0.12, a margin of 36.77% against this class's 0.54% floor (`mut-odo-vecdims-add-in-leaf-u2-aa`). **`compose` is the one class of the ten whose bold sits in the CEILING column**, the shipped leaf `mut-odo-vecdims-add-in-leaf-u2` at 0.014741 against `lib-stage2-lean`'s 0.014572 --- a tie at the three decimals the summary prints, resolved on the unrounded values and by 1.7e-4. Its two columns may NOT be differenced, `list` having moved 29.55 of a point, at a class geomean of 1.0683 over the 17 arms, with 2 of 9 strategies past an A/A bar of 1.35 points. The counted work reads a counts geomean of 1.1576 over the same arms, 17 of them counted. Its counted work parts by 15.76 points where its clock parts by 6.83, so about 0.43 of the instruction saving reaches the clock.


## Provenance

**Run 38's halves differ in TWO GHC FLAGS and in nothing else.** One source, `Main.hs` at `bb6f0fc`; one shim, `align-as.py` at `f31bd1c`, unmoved since Run 33's build; one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1 LOOP_EXITSPAN=1` in front of the assembler shim; ONE compiler, the in-tree stage1 `10.1.20260918` reached through `cabal.project.ghead`, which is Runs 36's and 37's compiler unmoved; one roster, one shape set, one class list and one bench order; both halves built with `-fobject-determinism`, both launched FROM DISK, `hugebin/` having been suspended on 2026-09-19, and both run under `WILDLOG=1 SATURATE=1`. The two command lines differ in `-fspec-constr -fliberate-case` on one of them, which micro.cabal's own `-O1` makes two of `-O2`'s passes on top of plain -O1 rather than a level. The basis is `run38-gheadnospec` and is what every table here publishes; `run38-gheadtwopass` is the candidate. **What is new against Run 37 is not the pair but two of its inputs**: the source moved fourteen of the owner's commits and the box was rebooted once. The project file, the shim, the compiler and the launch are that run's to the commit, so this is Runs 36's and 37's pair in its variable, read a third time under two changed terms where Run 37's step carried three and two reboots.

**The roster is 34 timed arms over 19 main-set shapes and 646 benches, with 61 class views over ten classes for 2074 more, and it is Run 37's with THREE ARMS IN and ONE OUT.** `./roster-delta.py run37-gheadnospec run38-gheadnospec`, read off the two binaries, puts `lib-stage3-lean`, `liblist-stage5-sum` and `libunord-stage14-sum` in --- the inward twins of 2026-09-21, each its counterpart's route under the fill numbered innermost first --- and `liblist-stage4-list-sum` out, parked `Only`, with 31 survivors in the same order, the nineteen main-set shapes unmoved and the sixty-one class views unmoved over the same ten classes. So pre-run step 12's condition fires twice over on this run: the membership changed AND the timed roster GAINED arms, which is what took its -L1 pass over every class rather than over `scaled` alone. Every cross-run figure in this file is read over the 31 survivors, and the three new arms have no prior in any run --- which is why registration item (6) prices them within this run and not against one.

**The sequence ran in ONE window and nothing else on the machine was this session's.** The wall-clock log puts the twenty-two processes between 2026-09-22T02:14:55 and 2026-09-22T10:07:04, the largest hole between one process finishing and the next starting 0m, every process reporting rc=0 and the bench count asked of it --- 20 class processes, one per class per half, and two main-set ones. The gate's four processes ran 01:41 to 02:15 before it, the four alone-leg riders 10:07 to 10:20 after it, and the counted work, which wants no quiet machine, from 10:22 to 11:04. **The intrusion verdict is NOT clean, and both exposed benches are the control half's**: `--wild` over the 114 logs that carry samples finds ONE bench at or above 0.25 foreign in `run38-gheadtwopass-main` --- `stretch-wide-2xM/bq-expand-aa-distant` at exactly 0.25, worst sample 292.6 ms foreign --- and ONE in the gate's `run38-gate-gheadtwopass-b`, `stretch-bigstride/bq-expand` at 0.28. The other 112 logs are clean. **Post-run step 3's rerun was NOT taken and a sensitivity reading stands in its place**, as it did on Run 33 when the owner stopped that rerun over a larger exposure, 3 of 627 benches at a peak of 0.35: the exposed main-set bench is an A/A copy no registered span names; the one published figure it reaches is the control half's floor, and `--aa` reads that floor at **0.28% with the shape and 0.28% with `--exclude-shape stretch-wide-2xM`**; and the two registered arms whose cells on that shape are extreme, `lib-stage1` and `lib-stage2-lean`, hold item (3)'s 2.5% band either way, at 1.0043 and 0.9974 with the shape and 0.9965 and 1.0063 without it. **And the bench sits 313 of 646 into its process**, its neighbours at 0.02 to 0.09 and the process's own first four benches decaying 0.14, 0.08, 0.05, 0.02 under the bar --- so it is a foreign burst of some 292 ms in the middle of the evening and not a session command landing on a process start, which is the shape Runs 32, 33 and 35 met.

**The gate read SOUND and the machine check did not fire.** The two palindrome passes agree: `mut-odo-vecdims` reads 1.0023 and 0.9984, `bq-expand` 1.3071 and 1.3040, and `list` 1.2827 and 1.2927 --- 0.39, 0.31 and 1.00 points apart --- with the two `sum-only` controls, on raw `slope`, at 1.0000 and 1.0019 on `-early` and 1.0001 and 1.0018 on `-late`. **What the passes part by is each half's own leg-to-leg movement and not a disagreement about the pair**: the control's `a` leg over its `b` reads `list` at 1.0026 where the basis's two legs read 0.9949, which predicts the second pass at 1.0077 times the first against an observed 1.0078, and the same arithmetic lands three and two thousandths of a point out on `bq-expand` and `mut-odo-vecdims`. The machine check, read against the fingerprint Run 37 installed --- this run's own basis recipe, its compiler, its shim, its project file, its four switches, its level and its launch, all to the commit --- puts `list`'s net at **+0.01%**, worst `stretch-primes` at **-1.28%**, 0 of 19 shapes past 5% and the geomean inside the 3% bar; the main-set JSON reads the same check at +0.27% with a worst of +1.95% on `gather48-src-50`, which is the instrument and not a disagreement. **Nothing is set aside this time**: Run 37 installed a fingerprint carrying `stretch-coprime-r7/list` at its clean value rather than Run 36's wild one, so this check reads over all nineteen shapes with no anomaly inherited, and the half-point the box moved on Run 37's instrument does not repeat.

**Every one of the twenty-two processes gated clean, the plateau refused by declaration, and ONE A/A worst cell sits past 5%.** `read-all.sh` gates each process on its own correction and passes all twenty-two. The plateau band refuses, as the pair note declared before the run that it would: the victim runs 16.7632 to 21.4637 ms/iter across the run, a 28.04% spread against a 5% band, and it splits exactly by half. **But the control half is NOT flat within a few points, which the declaration says is the executing session's to read.** The basis's eleven processes are flat within 1.92%, 21.0602 to 21.4637; the control's spread 9.41%, and the whole of that is ONE process --- ten of its eleven sit within 2.42% at 16.7632 to 17.1686 and the eleventh, `run38-gheadtwopass-main`, reads 18.3407. **That process measured in line with its own half whatever its preamble read**: over the gate's five arms and nineteen shapes it reads 0.9951, 0.9968 and 1.0004 against its own gate leg an hour earlier, where the basis half's same check reads 0.9961, 0.9975 and 1.0041 --- two halves agreeing with their own gate legs to within half a point. So the 18.3407 is the preamble's twenty iterations at that process's very start and not a state its 646 benches were timed in, and the plateau's refusal is the variable working with one slow first reading on top of it. The one A/A worst cell past 5% is `gheadtwopass-runs` at **6.88%**, on the population whose floor is 3.78% in any case, and it is nowhere near the 28.36% outlier Run 36 carried on its own main set.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run38-gheadnospec`, md5 `030929d866585233fab7df151739975f`, and `run38-gheadtwopass`, md5 `52d5569e04609f540f7f6c071ec9522e`, built back to back in one call on 2026-09-21 against a clean tree. Their `.text` sections are **20203327** and **20268863** bytes, the first column of `size -A`, and both load at 4214784, which is Runs 36's and 37's load address unchanged; against Run 37's two the basis is smaller by 4096 bytes, one page exactly, and the flagged half larger by 53248, thirteen --- one arm parked and three landed, recorded and not apportioned. **NEITHER md5 reproduces anything**: the compiler, the shim and the project file are Run 37's to the commit and the source moved fourteen commits, which is enough on its own. What the two md5s do instead is DIFFER, which is the two passes having reached the emission. **AND THE SERIES ON THE FLAGGED HALF PARTS HERE.** It is again the LARGER binary, by 65536 bytes --- sixteen pages exactly, a sixth exact page multiple in that series --- but it carries MORE self-loops this time, 335 against 325, where Run 37's note counts five pairs, itself included, of the larger half holding fewer, a count carried from that note. So [the open list's entry on it](../README.md#what-is-open) keeps its page-multiple half and loses its loop-count half on this pair, which is recorded rather than resolved.

**The compiler stood still and the source moved, as on Run 37, so the pinning claim's strong form is untested again.** The compiler is `10.1.20260918` on both builds and on both of Run 37's, so no figure here carries a compiler term; what is new under the recipe is `Main.hs` from `05cfe93` to `bb6f0fc`, fourteen commits, with the project file, the shim and the launch unmoved. `./loop-offsets.py --delta run37-gheadnospec run38-gheadnospec` reads the six-copy group's offsets MOVED, [18, 0, 0, 0, 9, 2] to [18, 15, 0, 0, 9, 2], with no address surviving to the byte and four displacements, none of them a whole line; the two-copy group keeps every mod-64 offset at [0, 0]; and a THIRD group is in `run37-gheadnospec` only, two copies at [0, 0] --- code that went with the parked arm and not a placement finding. **Step 2d's strict condition fires this run**, three timed arms bringing new functions with them, so neither *every offset preserved* nor *an address surviving* is owed and this is the pinning claim's only reading here. Within the pair the two passes move them further: the six-copy group reads [18, 15, 0, 0, 9, 2] on the basis against [18, 0, 7, 7, 9, 26] on the control, the two-copy group [0, 0] against [4, 4], and a group of three copies exists on the control half alone. `--library` puts the two halves at **4.3%** the same offset in line over 808 common library self-loops, which is Runs 36's and 37's 4.3% to the tenth and a tenth from Run 31's 4.4% for the whole -O2 level.

**The straddling loops stand at TWELVE on each half, where Run 37 read ten, and no exit span sits astride on either.** `loop-offsets.py --survey` reads 325 self-loops of at most 64 B in `_Main_`-compiled code on the basis and 335 on the control, 12 straddling on each and 0 exit spans astride, which is what `LOOP_EXITSPAN=1` owes. The count moved on BOTH halves alike, so it is the moved source and not the regime. **Post-run step 0's naming, taken off the binaries that were timed, puts the SAME twelve bodies at the SAME twelve mod-64 offsets on each half** --- 39, 41, 18, 41, 18, 41, 18, 49, 45, 42, 19 and 29 in address order --- five named by byte identity against this run's own `-g3` twins, `fillStage2Short` and four leaf bodies of `fbMutOdoVecdimsAddInLeafU2`, and seven refused by both halves alike, of which six carry a `--loose` family of three or four members and one a family of ONE, `fbMutOdoVecdimsAddInLeafU2Ptr`. So the two passes moved no straddling loop across a cache line, and the straddle map is one map for the pair. **The basis half's own twin holds FEWER loops than the binary it names for**, 318 against 325, so `--match` refuses the population comparison there and each name rests on its own byte match --- the refusal that makes a name evidence rather than a proximity.

**The regime was confirmed in this run's own binaries before the hours were spent, and the two halves read DIFFERENTLY, which is the point of the pair.** `diag` on `vgg-14-c512` puts `baseOffsetsScan` against `baseOffsetsMut` at 24066455 against 2408538 on `run38-gheadnospec`, 9.992 times apart, which is plain -O1 and is `run37-gheadnospec`'s scan figure to the byte with its mut figure eight higher, on a source fourteen commits on; on `run38-gheadtwopass` the same two read 2408978 against 2408538, EQUAL TO THREE FIGURES, which is SpecConstr having fired. So pre-run steps 9 and 9b are one reading on this pair, and the variable is legible in the binary before any bench runs.

**The three main-set anchors** read **6.35 us** on `cnn-slice-c32`, **3.77 ms** on `cnn-L2-24x24-c32` and **39.4 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, with the control half's beside them --- the absolutes every ratio in this file divides away, kept so a later run can tell a moved box from a moved arm. The control column is the flagged half and sits between 19 and 24 points below the basis on the three, which is the pair's own variable and not the box:
| shape | `l` | `list`, per call | net | `gheadtwopass`, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.52 us | 6.35 us | 4.97 us |
| `cnn-L2-24x24-c32` | 165888 | 3.87 ms | 3.77 ms | 2.89 ms |
| `stretch-wide-2xM` | 1800000 | 40.4 ms | 39.4 ms | 31.7 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's own shapes, raw and net, off the basis half.** `rev-primes` 4.56 ms raw and 4.41 ms net; `bcast-inner900` 30.6 ms and 29.5 ms; `bcastmid-b200k` 48.5 ms and 47.4 ms; `window-128x128-k7` 14.2 ms and 13.8 ms; `scaled-rank1-m1` 5.31 ms and 5.13 ms; `runs-2` 40.6 ms and 39.5 ms; `flip-fwd-rows96` 31.3 ms and 30.2 ms; `block-r3-vol64` 4.64 ms and 4.48 ms; `small-row96` 6.66 us and 6.43 us; `compose-zero-mid` 30.9 ms and 29.8 ms. Each is one process's reading of one shape and crosses to no other population.

**The correction sits on the same footing in both halves, and NOT ONE cell of the whole run is one the reader flags.** The two `sum-only` arms agree to within **0.13%** on every population and on both halves of the pair --- 0.9989 on `scaled`'s basis to 1.0013 on `bcastmid`'s across the twenty-two --- so the term subtracted from one half is the term subtracted from the other. `CI%` reads a geomean of **0.98** over the 34 arms, 13 wider on the basis and 21 narrower. **NO cell of any population on either half sits below R2 0.99, and none is under ten samples** --- 0 of the run's 2720 cells on each count, where Run 37 carried one such cell and Run 36 five, its main set's among them. So nothing in this run's time columns is set aside for how it resolved.

**The counted work covers every population, no cell was refused anywhere, and the two halves emit very different work.** `run-counts-all.sh` wrote 22 sweep files over eleven populations on each half, 0 cells refused, at a cost of 1386s on the basis and 1149s on the control --- both a little above Run 37's 1352s and 1106s, which is what a roster of three arms more predicts, and the two halves parting by 237s where that run's parted by 246s and Run 36's by 239s. The counts geomean over the seventeen timed arms runs **1.1355** on `small` to **1.1686** on `window`, the main set at **1.1596** --- the basis retiring some sixteen percent more instructions than the flagged half across every population. **On the main set `time/counts` separates the families cleanly**: the `bq-expand` trio sits at 0.8651 to 0.8676, retiring 50.6% more instructions on the basis for 30.3 to 30.7% more time; the `list` trio at 0.9954 to 0.9993, cashing almost exactly what it saves; and the eleven others between 0.9471 and 0.9720, retiring 3.8 to 5.3 percent more on the basis while their clocks run from 0.26 points BELOW level to 2.05 above. So the two passes buy `bq-expand` far more in instructions than they cash on the clock, and buy `list` rather less. **Read per class the same way, the rate runs 0.38 to 0.73**: the instruction saving reaching the clock is lowest on `block`, where the counted work parts by 14.37 points and the clock by 5.47, and highest on `window`, 16.86 against 12.36 --- which is the [open question][open] on that rate answered over ten populations at once, and its recorded three quarters is this run's ceiling rather than its middle.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0001** on the basis and **1.0000** on the control on the main set, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0316** and **1.1026** on the basis and **1.0280** and **1.0710** on the control for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs about three percent over the term it stands for on `mut-odo-vecdims` on both halves, and about ten and seven on `bq-expand`. So the two passes do not move the correction, and no ratio in this file is an artefact of a forcing pass that parted between the halves.

**The decomposition reproduces on both halves and its two columns part by the pair's own variable.** The riders time each shape's `list` alone, one bench to a process, clean and then saturated, and the state the preamble puts on a process comes back at a geomean of **1.1217** on the basis and **1.1644** on the control, **4.3** points apart, where Run 37's two parted by 5.2 and Run 36's by 4.9 --- so the two passes change what the spray costs a process as well as what the roster costs it, and by within a point of what they changed it by on each of the two runs before. What the roster adds on top of that state is **1.0223** on the basis, 10 of 19 shapes above 1, and **1.0118** on the control, 12 of 19; the basis's rest runs 0.9749 on `stretch-bigstride` to 1.2319 on `stretch-r5-8x432`, the control's 0.9730 on the same shape to 1.0963 on `stretch-tall-Mx2`. The whole in-process deflation is **1.1467** on the basis, 18 of 19 shapes above 1, and **1.1781** on the control, 19 of 19.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[ramp]: ../README.md#r2-is-the-ramp-detector-not-the-noise-detector
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

The pair is Run 37's, declared by the owner on 2026-09-21 in the words *the same binaries as Run 37* --- which under the pre-run list's BOTH HALVES ARE BUILT ANEW ruling is Run 37's two RECIPES built again and not its two files reused --- and THIS ENTRY IS ITS ONE DECLARATION SITE by the ruling of 2026-09-19, recorded in the entry led *What the next run's two binaries are is declared in TWO places*: both halves GHC HEAD `10.1.20260918` through `cabal.project.ghead` at plain `-O1` in the dead-spot form under the exit span, one source (`Main.hs` at `bb6f0fc`), one shim (`align-as.py` at `f31bd1c`) under the four switches, one roster, one shape set and one launch FROM DISK, and the control half's command line carrying `-fspec-constr -fliberate-case` besides, nothing else differing, so `--compare` reads the unflagged basis over the flagged control as Runs 36's and 37's did; [Run 37's *What the next run compares against*](run37.md#what-the-next-run-compares-against) is what left this pair to be declared here, saying in its own words that no entry named it yet. The recipes, the half names being the preparation's to hold to the tag grammar: `run38-gheadnospec` is, from this directory, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1 LOOP_EXITSPAN=1 cabal build micro --project-file=cabal.project.ghead --builddir=db-r38a --ghc-options="-fobject-determinism" --ghc-options="-pgma $PWD/align-as.py -fforce-recomp"`, then `cp $(cabal list-bin micro --project-file=cabal.project.ghead --builddir=db-r38a) run38-gheadnospec` and `rm -rf db-r38a`; `run38-gheadtwopass` is the same on the same source with `--builddir=db-r38b` and `--ghc-options="-fspec-constr -fliberate-case -fobject-determinism"`. `LOOP_MAXSKIP` and `LOOP_LOOKTHROUGH` are inert under the dead-spot form and stay on both lines so that the lines differ in the regime flags alone; `LOOP_ENTRIES`, `LOOP_BLOCKRULES`, `LOOP_PIN` and `LOOP_TRACE` are unset on both. Every build wants `-fforce-recomp` and a fresh `--builddir`, the switches being an environment change cabal does not see. **WHAT IS NEW IS THE SOURCE AND THE BOOT, and nothing else is**: `Main.hs` has moved fourteen commits since Run 37 built from `05cfe93`, taking the roster from 608 benches and 32 timed arms to 646 and 34 --- `lib-stage3-lean`, `liblist-stage5-sum` and `libunord-stage14-sum` in, with their fills liblist-stage5 and libunord-stage14 rostered `Only` beside them, liblist-stage4-list-sum parked, and 31 survivors in the same order --- and the box has been rebooted once since that run's evening, up since 2026-09-21T11:09:53+02:00 by `/proc/uptime`. The compiler, the shim, the project file and the launch are Run 37's to the commit, the resolved plan reading criterion 1.6.5.0, vector 0.13.2.0 with `boundschecks` True, base 4.23.0.0, ghc-prim 0.14.0, hashable 1.5.0.0 and aeson 2.3.2.0 as that run's did, so this run's step off Run 37 carries TWO terms where Run 37's off Run 36 carried three and two reboots. **WHY A THIRD READING**: Runs 36 and 37 are this same pair and their `list` figures STRADDLE the `-O2` level --- 1.3129 and 1.2950 over the eighteen shapes without Run 36's wild cell, against Run 31's whole-level 1.2974 --- parting by 1.79 points, wider than the distance from either to the level, so the two runs disagree about whether the level's other passes hand `list` back. A third draw is what names the outlier, and Run 37's own section asked for one. **What the run predicts, registered 2026-09-21 before it runs. EVERY PRIOR BELOW IS RUN 37'S OWN MEASURED READING OF THIS SAME PAIR**, re-derived from `run37-gheadnospec-main.json` against `run37-gheadtwopass-main.json`, and for the counts from `run37-counts-gheadnospec.txt` against its twin, rather than quoted from prose --- and the spread the bands rest on is not a floor but a REPETITION: the probe of 2026-09-20 re-ran the main set on Run 37's own two binaries, `probe-third-run37-gheadnospec-main.json` and its twin, and reads `list` 1.2986 against that run's 1.2960 and `bq-expand` 1.3091 against its 1.3101, so ONE DRAW READ TWICE parts by 0.26 and 0.10 of a point where the two DRAWS part by 1.79 on `list` over the eighteen shapes and 1.21 on `bq-expand` over the nineteen, `bq-expand` having no cell to set aside; read at eighteen on both sides that second figure is 1.20.

(1) *Run 37's reading of `list` is the pair's, and Run 36's is the outlier.* `predict: cross list 1.2950 within 1% on main basis excluding stretch-coprime-r7`, and over all nineteen shapes `predict: cross list 1.2960 within 1.5% on main basis`. THE NARROW SPAN IS THE ONE THAT DECIDES and it is built to discriminate: 1% around 1.2950 runs 1.2821 to 1.3080, and Run 36's 1.3129 lies OUTSIDE it, so a reading inside the span names Run 36's reading the outlier and one at 1.313 names Run 37's. The band is the within-draw spread above, 0.26 of a point, with 0.51 beside it --- the widest an arm and its own A/A duplicate part in that run's cross-half table, which `--compare` prints and which is a different quantity from the 0.59% population floor `--aa` gives --- 0.77 rounded up to one; the wide span adds to it whatever wild cell this run draws, the 2.31 points one cell moved Run 36's whole-set figure being the scale. THE KILL CONDITION IS A READING OUTSIDE THE NARROW SPAN, and it has two shapes that mean different things: at 1.313 the pair's figure is Run 36's and Run 37's was the outlier, while BETWEEN the two priors it is neither run's and the pair's `list` figure is not repeatable to two points across draws --- which is itself an answer to the straddle, and is why both spans are registered rather than the narrow one alone.

**Read by --predictions, item (1):** `cross list 1.2950 within 1% on main basis excluding stretch-coprime-r7`: HELD on main basis, read 1.2907 over 18 shape(s), 0.43 point(s) off, within 1.00% --- `cross list 1.2960 within 1.5% on main basis`: HELD on main basis, read 1.2889 over 19 shape(s), 0.71 point(s) off, within 1.50%.

(2) *`bq-expand` repeats a third time, on the arm where five accounts now sit inside 1.6 points.* `predict: cross bq-expand 1.3101 within 1.5% on main basis`. The prior is Run 37's own, off `--compare` between `run37-gheadnospec-main.json` and `run37-gheadtwopass-main.json`; the probe on that run's binaries reads 1.3091 a tenth of a point away, by the same mode over `probe-third-run37-gheadnospec-main.json` and its twin, and the band is that tenth with the 0.59% population floor and room for the 1.21 points Runs 36 and 37 parted by on this arm. A reading outside is the first sign that something other than the two passes moved between the runs, and the SOURCE is the only candidate this run has, the compiler, the shim, the project file and the launch being unmoved.

**Read by --predictions, item (2):** `cross bq-expand 1.3101 within 1.5% on main basis`: HELD on main basis, read 1.3032 over 19 shape(s), 0.69 point(s) off, within 1.50%.

(3) *No arm outside the two families joins them on a third reading.* `predict: cross mut-odo-vecdims 1.0 within 2.5% on main basis`, `predict: cross mut-odo-vecdims-aa 1.0 within 2.5% on main basis`, `predict: cross mut-odo-vecdims-aa-distant 1.0 within 2.5% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 2.5% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u2-aa 1.0 within 2.5% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u2-aa-distant 1.0 within 2.5% on main basis`, `predict: cross lib-stage1 1.0 within 2.5% on main basis`, `predict: cross lib-stage2-lean 1.0 within 2.5% on main basis`, `predict: cross lib-stage2-lean-u1 1.0 within 2.5% on main basis`. THESE NINE ARE ONE CLAIM READ NINE WAYS and a single miss is one arm joining the families rather than nine results, which is the tally Run 36's write-up had to disentangle and Run 37's item said again. The band is set on Run 37's measured spread over these same nine, 1.0026 to 1.0160: 2.5% is its widest DEPARTURE FROM 1, 1.60 points --- the extremes part by 1.34, which is the other reading of that pair of figures and not the one the band is drawn on --- with that run's 0.59% floor beside it. `mut-odo-vecdims-add-in-leaf-u1` is deliberately NOT among them and is item (4), Run 37 having read it far outside this set.

**Read by --predictions, item (3):** `cross mut-odo-vecdims 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0074 over 19 shape(s), 0.74 point(s) off, within 2.50% --- `cross mut-odo-vecdims-aa 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0029 over 19 shape(s), 0.29 point(s) off, within 2.50% --- `cross mut-odo-vecdims-aa-distant 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0073 over 19 shape(s), 0.73 point(s) off, within 2.50% --- `cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0045 over 19 shape(s), 0.45 point(s) off, within 2.50% --- `cross mut-odo-vecdims-add-in-leaf-u2-aa 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0057 over 19 shape(s), 0.57 point(s) off, within 2.50% --- `cross mut-odo-vecdims-add-in-leaf-u2-aa-distant 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0028 over 19 shape(s), 0.28 point(s) off, within 2.50% --- `cross lib-stage1 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0043 over 19 shape(s), 0.43 point(s) off, within 2.50% --- `cross lib-stage2-lean 1.0 within 2.5% on main basis`: HELD on main basis, read 0.9974 over 19 shape(s), 0.26 point(s) off, within 2.50% --- `cross lib-stage2-lean-u1 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0177 over 19 shape(s), 1.77 point(s) off, within 2.50%.

(4) *`mut-odo-vecdims-add-in-leaf-u1`'s move the BASIS's way belongs to Run 37's BUILD and not to the pair, so a rebuilt pair reads it back among its siblings.* `predict: cross mut-odo-vecdims-add-in-leaf-u1 1.0 within 2.5% on main basis`. Run 37 read 0.9630 where its nine siblings read 1.0026 to 1.0160, and the probe on that run's own two binaries reads 0.9598 --- so the move reproduces on that DRAW, and what no reading has yet asked is whether it survives a REBUILD, which is what this run supplies. THE SPAN IS TWO-SIDED AND THE SENTENCE MEANS THE LOW SIDE: a reading near 0.96 again refutes it and makes the movement a property of the pair's code rather than of one build, which is the question the entry led *Four arms moved past 3% against Run 32 on ONE half each* asks, and this arm is the first mover that entry names, read here on the main set where it read it on `scaled`; a reading above 1.025 refutes it too and is the other finding, the arm joining the families, which item (3) is written for. Run 36 read this arm at 1.0033, mid-pack inside its own null spread of 0.9920 to 1.0119, so the two prior draws already disagree about it.

**Read by --predictions, item (4):** `cross mut-odo-vecdims-add-in-leaf-u1 1.0 within 2.5% on main basis`: HELD on main basis, read 1.0205 over 19 shape(s), 2.05 point(s) off, within 2.50%.

(5) *The counted work parts on the arms the clock parts on, and it repeats across a moved source where the clock need not.* `predict: counts list 1.2930 within 1% on main basis`, `predict: counts bq-expand 1.5063 within 1% on main basis`, `predict: counts mut-odo-vecdims 1.0383 within 1% on main basis`, `predict: counts lib-stage2-lean 1.0522 within 1% on main basis`. The four priors are Run 37's own MEASURED counts and not its predictions, off `--compare --counts` between `run37-counts-gheadnospec.txt` and `run37-counts-gheadtwopass.txt`. THIS IS THE ITEM RUN 37'S OWN SECTION ASKED THE NEXT REGISTRATION TO COPY: its four bands, drawn at one and two percent off Run 36's counts, came in 0.00, 0.00, 0.00 and 0.04 of a point out, where its time bands drawn from the same run were out by 1.21 to 4.00. So 1% is more than an order of magnitude wider than the miss that history records --- and what this run adds to the question is the SOURCE, fourteen commits where Run 37 carried three, which is the term those four figures have never been read across at this size. A miss here, with item (1) inside its span, is the counted work having stopped carrying and is the one outcome that would retire this item's form.

**Read by --predictions, item (5):** `counts list 1.2930 within 1% on main basis`: HELD on main basis, read 1.2932 over 19 shape(s), 0.02 point(s) off, within 1.00% --- `counts bq-expand 1.5063 within 1% on main basis`: HELD on main basis, read 1.5063 over 19 shape(s), 0.00 point(s) off, within 1.00% --- `counts mut-odo-vecdims 1.0383 within 1% on main basis`: HELD on main basis, read 1.0383 over 19 shape(s), 0.00 point(s) off, within 1.00% --- `counts lib-stage2-lean 1.0522 within 1% on main basis`: HELD on main basis, read 1.0531 over 19 shape(s), 0.09 point(s) off, within 1.00%.

(6) *The fill numbering the pairing of 2026-09-21 added is worth nothing outside the floor.* `predict: pair lib-stage3-lean lib-stage2-lean 1.0 within 2% on main basis`, `predict: pair liblist-stage5-sum liblist-stage4-sum 1.0 within 2% on main basis`, `predict: pair libunord-stage14-sum libunord-stage13-sum 1.0 within 2% on main basis`. The three new arms are ONE VARIABLE READ THREE WAYS --- each is its counterpart's route under the fill numbered innermost first, where the counterpart keeps `fillStage2Axes` and its outermost-first numbering --- and they have NO prior in any run, so these are within-run comparisons on the basis half and not repetitions; the band is 2%, which is between three and four times Run 37's 0.59% population floor, there being no measurement of this variable to draw a band from. THE THREE SPANS ARE NOT READ ON ONE QUANTITY, and the reader says so on its own line: `lib-stage3-lean` and `lib-stage2-lean` are fills and their pair is read on the corrected net, while the two `-sum` pairs are REDUCING CONSUMERS, which carry no corrected time by the ruling of 2026-09-10 and whose `--pair` line prints *compared RAW, one arm has no corrected time* --- so those two ratios are damped by the forcing pass both arms share, and a miss on one of them is a larger real difference than the same miss on the first. **NOT REGISTERED, and each for a reason**: the two reboot readings Run 37 carried are the owner's to rule and are not inherited, `hugebin/` being a tmpfs that took the Run 34 instance with it, and this run raises no mount and takes step 16a as skipped; and no item is read on a stride class, every span above naming `main`, the class JSONs of the evening being what the write-up reads the new arms' classes off rather than what any prediction turns on. **AND ONE FINDING THE PREPARATION MET rather than predicted**, recorded here because it bears on every cross-run absolute this run publishes: `hugebin/` was MOUNTED when the preparation began, raised 2026-09-21 21:11 and empty, and `half-bin.sh` copies an ELF half into the mount whenever `mountpoint -q` succeeds, so every driver would have launched from it and taken the placement term --- 15 percent on one arm of Run 33's basis, by the placement section --- back into both halves. The owner unmounted it on being asked, before any half was copied anywhere. Nothing in the list or in any script refused a mounted `hugebin/` on a run that was not a placement run, and the `launch` row of a pair note was the only place it would have shown --- as the `./` a session reads as expected. **That is no longer so, and this finding is why**: preflight's 10f, added 2026-09-22, FAILs where a half would launch from the mount on a run that set no `PLACEMENT`.

**Read by --predictions, item (6):** `pair lib-stage3-lean lib-stage2-lean 1.0 within 2% on main basis`: HELD on main basis, read 1.0061 over 19 shape(s), 0.61 point(s) off, within 2.00% --- `pair liblist-stage5-sum liblist-stage4-sum 1.0 within 2% on main basis`: HELD on main basis, read 1.0027 over 19 shape(s), 0.27 point(s) off, within 2.00% --- `pair libunord-stage14-sum libunord-stage13-sum 1.0 within 2% on main basis`: HELD on main basis, read 0.9996 over 19 shape(s), 0.04 point(s) off, within 2.00%.

**ALL SIX items hold their sentences, over 20 spans of which 20 held and none was killed.** Every verdict below is its item's KILL CONDITION applied across the population and half it names, which on this registration is `main basis` for all twenty spans; every figure is re-derived from this run's own JSONs, by `--predictions` over the main set on the basis half and by `--counts` where a clause names instructions. **One thing governs how the list reads**: this pair is Runs 36's and 37's read a third time, so every item asks whether a figure repeats, and this time all of them do.

(1) *Run 37's reading of `list` is the pair's, and Run 36's is the outlier.* **HELD on both spans, and the narrow one --- which the item registered as the one that would decide --- lands 0.43 of a point from its prior.** The narrow span asked for 1.2950 within 1 point over the eighteen shapes without `stretch-coprime-r7` and the run reads **1.2907**; the wide span asked for 1.2960 within 1.5 over all nineteen and it reads **1.2889**. The kill condition was a reading outside the narrow span, and the item gave it two shapes: at 1.313 the pair's figure would have been Run 36's, and between the two priors it would have been neither run's and not repeatable to two points across draws. It is neither. The reading sits inside the narrow span and **2.22 points below Run 36's 1.3129**, so Run 36's draw is named the outlier and Run 37's the pair's. Against Run 31's whole `-O2` level at 1.2974 the two passes read 0.67 of a point UNDER it here and 0.24 under on Run 37, where Run 36 read 1.55 above --- so two draws of three put the two passes just below the level, and **the level's other passes do not measurably hand `list` back**, which is Run 36's conclusion refuted rather than repeated.

(2) *`bq-expand` repeats a third time, on the arm where five accounts now sit inside 1.6 points.* **HELD**, at **1.3032** against 1.3101 within 1.5 points, 0.69 out. The item registered a reading outside this span as the first sign that something other than the two passes had moved between the runs, and named the SOURCE as this run's only candidate, the compiler, the shim, the project file and the launch being Run 37's to the commit. It is inside, so fourteen commits of `Main.hs` do not show on this arm. The five accounts are 1.2943 from Run 31's level, 1.2967 from the composition of Runs 29's and 30's single passes, and 1.2980, 1.3101 and 1.3032 from the three HEAD draws --- a spread of 1.58 points.

(3) *No arm outside the two families joins them on a third reading.* **HELD on all nine spans**, from 0.26 of a point on `lib-stage2-lean` to 1.77 on `lib-stage2-lean-u1`, every one inside the 2.5% band Run 37's measured spread set. No arm outside the `list` and `bq-expand` families joins them, and the item's own reading of itself holds too: a single miss would have been one arm joining rather than nine results, and there is none.

(4) *`mut-odo-vecdims-add-in-leaf-u1`'s move the BASIS's way belongs to Run 37's BUILD and not to the pair, so a rebuilt pair reads it back among its siblings.* **HELD at 1.0205**, 2.05 points from 1 inside a 2.5% band. The span was two-sided and the sentence meant the low side: a reading near 0.96 would have made the movement a property of the pair's code, and one above 1.025 would have been the arm joining the families. It is neither, though nearer the upper line than the lower. **So the movement was that build's**: Run 37 read this arm at 0.9630 and the probe on its own two binaries at 0.9598, and a pair built again from the same two recipes reads it at 1.0205, back among the nine siblings item (3) covers. The classes say the same thing in a second vocabulary --- the arm was the low extreme in NINE of Run 37's ten classes, down to 0.7053 on `bcast`, and in THREE of this run's, at 0.9931, 0.9880 and 0.9897 --- so the file-instance account Run 37's own entry reached, from three fresh copies of each control half, has its independent test: that movement does not survive a rebuild.

(5) *The counted work parts on the arms the clock parts on, and it repeats across a moved source where the clock need not.* **HELD on all four spans, two of them at 0.00 of a point.** `bq-expand` reads **1.5063** against 1.5063 and `mut-odo-vecdims` **1.0383** against 1.0383, each to the fourth decimal; `list` reads **1.2932** against 1.2930 and `lib-stage2-lean` **1.0531** against 1.0522, 0.02 and 0.09 of a point out. The item was registered on the argument that counted work carries across runs where the clock need not, and on what this run adds to it --- fourteen commits of source where Run 37 carried three. A miss here with item (1) inside its span would have retired the item's form; instead the four bands drawn at one percent are out by at most nine hundredths of a point, and the claim is now carried by two runs and eight spans.

(6) *The fill numbering the pairing of 2026-09-21 added is worth nothing outside the floor.* **HELD on all three spans**, and the three arms are one variable read three ways. `lib-stage3-lean` against `lib-stage2-lean` reads **1.0061** on the corrected net, 0.61 of a point from 1 and four hundredths outside the basis half's 0.57% floor, with the published column at 1.0007 and 11 of 19 shapes to the new arm --- a margin the floor cannot call a difference. The two reducing-consumer pairs read **1.0027** and **0.9996** on raw `slope`, both inside the floor, and both damped by the forcing pass the two arms of each share, so a miss on one of them would have been a larger real difference than the same miss on the first. **The numbering buys nothing and costs nothing**, which is what the item registered and what a within-run comparison on one half can say.
