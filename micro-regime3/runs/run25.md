# Run 25 (SpecConstr)

One run's write-up: its head, its Results, what the next run compares against, the claims that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 25 (SpecConstr), and what a second compiler does to a roster cut to the family's shipping question: the unroll is priced apart from the bound and the answer is the same on both compilers --- `mut-odo-vecdims-add-in-leaf-u1` sits between the counted-bound leaf and the unrolled one, 0.9360 and 0.9407 of `-add-in-leaf` and 1.0367 and 1.0479 of `-u2`, every reading past both halves' floors --- while nothing published moved for the retirement or the prune, Run 24's basis column reading this one arm for arm once the shape set is pinned.** Criterion, **`--ghc-options=-fspec-constr`**; the dead-spot form on BOTH halves, one source, one shim commit, one shim environment, one roster and one shape set, and **what moved is the compiler and nothing else a freeze, a source diff or a shim switch can see**: 432 benches, 24 timed arms over 18 main-set shapes, and 1176 more over 49 class views in **ten** classes. **The basis is `run25-g912`**, ghc-9.12.4, the default on PATH here and the lineage every figure in this README was measured through, built `-fspec-constr -fobject-determinism` with `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of `align-as.py`; md5 `6c898ba25314415dec76fdb8ec2c426c`, `.text` 20570309 B. **The other half is `run25-ghead`**, the same source and the same shim environment through `cabal.project.ghead`, which selects the in-tree stage1 of the GHC checkout --- `10.1.20260803`, Run 24's compiler unmoved, and the binaries carry `ghc-internal-9.1204.0` against `ghc-internal-10.100.0` --- md5 `7f2ae9b95db61f9b5adc27e72903e5a6`, `.text` 20711231 B, 140922 B the larger, which is Run 24's difference to the byte. So the halves differ in the compiler and in the boot libraries that come with it, and in nothing else; both under `WILDLOG=1 SATURATE=1`; `Main.hs` at `f04c5dc`, `align-as.py` at `b3a1aca`, the tree at launch `86f2041` with 39 untracked scratch paths and nothing modified. The same desktop, Zen 3, a Ryzen 7 5800X, and the same BIOS Run 18 re-baselined onto. The two main processes read *0h37m23s* and *0h37m29s*, at *177 MiB* in use and *64 MiB* max residency on the basis against *181 MiB* and *65 MiB* on the HEAD half.

**There is NO repetition this run either, and what stands in its place is the tightest cross-run column this file has carried.** `Main.hs` moved between Run 24 and this run, by the five roster commits of 2026-09-04, so neither half reproduces an earlier binary and the exact repetition Runs 19 and 23 could take is unavailable. What can be read is `run25-g912` against `run24-g912`, the previous build of THIS recipe: over the 18 shapes and 16 non-degenerate arms the two runs share, **fifteen read within 1%**, from `lib-stage1` at 0.9985 to `list-aa-adjacent` at 1.0069, and the one outside is `liblist-stage1` at 0.9891. The two `libunord` arms are apart --- 0.7124 and 0.6942 --- and they are the pair that is its own forcing term on half the shape set. **Why this column is tighter than any before it is the prune and not the evening**: `build`, `mut-odo`, `gen-unsafe` and their six twins are the arms whose figure is where their bytes landed, they carried every cross-run column this file has published past a point, and every one of them was parked on 2026-09-04. A roster change still puts a layout term back where a repetition had none; what it lands on is now the arms the library ships, and they do not move.

**What holds the build to something is the gate and three machine checks, there being no repetition to do it.** The gate ran both halves twice in a palindrome and read SOUND on both passes: no arm separated between the halves, `bq-expand` reading 0.9924 and 0.9930 at 14 of 18 both times, `mut-odo-vecdims` 0.9970 and 0.9990, and `list` changing sign between the passes at 0.9940 and 1.0099, which is what a tie read twice looks like. Its machine check read **-0.08%** on `list`'s net against the fingerprint Run 24 kept, over 18 of 18 shapes --- the eighteen this roster keeps, the other eight having been retired from timing the day before --- worst `stretch-square-1341` -1.70% and none past 5%; the run's own two main-set processes read the same comparison at **+0.32%** and **-0.77%**, worst `stretch-coprime-r7` +1.99% and `stretch-square-1341` -2.82%. Three readings against the same kept fingerprint, all inside 3%, so the box measures as it did and no absolute is re-baselined. Each half's own six A/A pairs give it a floor, and the counted work, taken over every population on both halves, is what separates a compiler's instructions from a slot its bytes landed in throughout what follows.

**The manifest's one live claim is new this run and it held on both halves.** Claim 1, the ladder the `needs` column drew, retired on 2026-09-04 with the prune that parked every rung below its top, and claim 10 was registered in its place on the roster's own question: what the leaf fusion buys over the family root. `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims` reads **0.6394 at 17 wins of 18, sign p 0.00014** on the basis and **0.6393** on the HEAD half --- a tenth of a percent apart between two compilers --- and, pinned to these eighteen shapes, Run 24's two halves read **0.6398 and 0.6391**, so the four figures span four ten-thousandths across two runs and two codegens. The shipped code is 36% ahead of the code it was refined from, and that is the one thing the roster still prices which the library has taken.

**What the second compiler is worth on this roster, and the counted work says it is almost nothing --- and that the baseline's own move is not instructions.** Read as `--compare` prints it, basis over HEAD, the counts geomean over the 20 arms carrying a corrected time is **1.0019**, and not one arm's count ratio is a full percent from 1: the fills and the library routes read 1.0061 to 1.0079, so 9.12 emits about three quarters of a percent more work there; `bq-expand` and its twins 0.9949; `mut-odo-vecdims` and its twins 0.9998; and the two `libunord` arms **1.0000 exactly**. **Where Run 24 found 5.7% and 6.8% of the instructions between the compilers, this roster finds 0.8%**, because every arm that carried those two figures --- the pure mul-back family --- is parked. **And `list` is the reading that matters**: it moves **1.0110 in time at a count ratio of 0.9967**, so 9.12 executes a third of a percent FEWER instructions on the baseline and is a percent slower at it, a time-over-counts of 1.0144 and the largest such residual on the roster. The move that costs this pair its subtraction is therefore where the code landed or what the runtime did with it, and not what either compiler emitted. **The two `libunord` arms' time entries are not read at all**: `--compare` puts them at 0.6891 and 0.7317, over 9 and 10 shapes of 18 and as ratios of nets two parts in a hundred thousand of their cells, which is the same arithmetic the sinking count above is.

**What this pair can no longer separate is placement from codegen on the arms that used to show it, and the prune is why.** `gen-unsafe`, `build`, `mut-odo` and their six twins were the arms whose count ratio read 1.0000 while their times moved four to six percent, and Run 24 called that reading the cleanest of that term this file had had; every one of them was parked on 2026-09-04, so no arm on today's roster is there to be read for its slot alone. What is left in their place is the A/A pairs, which are still two placements of one strategy and give this run its floor, and the counted work, which still says of any arm whether a movement is instructions or where they landed. **So a compiler pair still prices two things at once here; what has gone is the family that made the second one legible on its own.**

**The unroll is priced apart from the bound, which is what this run was built for, and the answer is the same on both compilers.** `mut-odo-vecdims-add-in-leaf-u1` --- the shipped fill's leaf with the bound merged in and the body left un-unrolled --- reads **0.9360 at 17 of 18, sign p 0.00014** against `mut-odo-vecdims-add-in-leaf` on the basis and **0.9407 at 17 of 18** on HEAD, and **1.0367 at 2 of 18** and **1.0479 at 3 of 18** against `-u2`, every one of the four past both halves' floors of 0.40% and 0.61%. So the merged bound buys six to six and a half percent over a counted one and the unrolling buys three and a half to five on top of it, and the shipped fill's margin is the unrolling's: the probe of 2026-08-24 read the bound as a wash on a scratch build with no shim, and under the dead-spot shim on two compilers it is not a wash but it is also not the margin. **And the run reads that against a body carrying a reload the shipped one does not**, `-u1`'s run-level copy being seven instructions to its rank-1 copy's six, so a `-u1` lead would have been in spite of it and the loss is not evidence about the merged bound until the reload is taken out.

**The classes break property 2 in all ten, and nine of the ten are led by an arm the library either ships or is one decision from shipping.** `libunord-stage2` leads `rev` at 0.001 and `flip` at 0.000, its one-block test firing on every view of those two and collapsing them to a single slice, so it prices dispatch and not filling --- and on `flip` it is its own forcing term on one shape of three, so that lead is not priced at all. `lib-stage2-lean` leads `bcastmid`, `window` and `small` by 47.45%, 54.31% and 19.91% against those populations' floors of 0.80%, 1.74% and 0.27%; `lib-stage2-disp` leads `bcast`, `runs` and `compose` by 40.04%, 22.46% and 34.11% against 0.83%, 2.79% and 0.41%; and `lib-stage1`, the shipped route, leads `scaled` by 11.34% against 0.56%. **The tenth is `block`, and it is the one population where the leader is a `mut-odo-vecdims` sibling** --- `mut-odo-vecdims-add-in-leaf-u2-down` at 9.74% against 0.60% --- so the first clause of the property does not break there, as it did not on two of Run 23's. Property 3 holds in all ten and property 1 holds in all ten of the arm it is stated of.

**Across the halves the classes read as the main set does, and the split is no longer even.** Of the 200 arm-comparisons the ten classes carry, one is degenerate and not voted --- `libunord-stage2` on `flip` --- and **124 put the basis faster against 75 the HEAD half**, the ten geomeans running **0.9793 on `small` to 1.0079 on `rev`**, nine of them below 1. The extremes over all ten are `libunord-stage1` at 0.9346 on `flip` and `libunord-stage2` at 1.0651 on `rev`, and the low extreme is `libunord-stage1` in three of the ten --- the unordered pair at both ends, which is what an arm that returns a single slice on some views and fills on others reads like across two codegens. **Two classes disqualify their own cross-half line**, where Run 24 had five: `bcastmid` at 1.0175 and `block` at 1.0088 move `list` past the 0.7% bar, so those two are ordered and not subtracted, and the eight inside it are `rev`, `bcast`, `window`, `scaled`, `runs`, `flip`, `small` and `compose`.

**The seven registrations came back five HELD, one killed by the one span a mode can read, and one that is four class predictions --- and four of the seven lost a clause to an arm the roster had parked, which is the finding.** (1) the box: **HELD**, `list`'s net against Run 24's kept fingerprint reading -0.08% at the gate, +0.32% on the basis main set and -0.77% on HEAD, all inside 3%. (2) the anchors: **HELD**, `cnn-L2-24x24-c32` at 3.41 ms against 3.46 ms, -1.45%. (3) the pair: **KILLED**, `list` moving 1.0110 between the halves where 0.7% was allowed --- and its second clause could not be read, all three orderings it names being stated against the parked `lib-stage2`. (4) the additions of 2026-09-03: **`flip` KILLED** by every fill on both halves, a reversed run costing the regime-3 fill about twice its forward twin at the same length; **`block` SPLIT**, the stage-two route ahead at every gap but by less as the gap grows, which is that item's own kill; **`small` UNREADABLE**, both arms its clauses turn on being parked; **`compose` SPLIT**, the compositions within a point of their twins once the view's own term is out and `compose-scalar` sixth rather than the fastest cell of any population. (5) the retirement: **HELD**, nothing published moving for it. (6) the prune: **HELD**, the three crossed pairs inside Run 24's floor on both halves with their spans shortened from 42, 8 and 7 slots to 16, 1 and 3. (7) the unroll: **HELD on both halves**. **The four lost clauses are one event**: the prune and the parking of 2026-09-04 landed the day after the additions were registered and the same day the pair's registration was written, and they parked `lib-stage2` and `canon-vecdims`, which four of these clauses are stated against. Nothing checked it before the machine was spent, for the second run running.

**Every one of the twenty-two processes gated clean**, `--selftest` and both `--aa` gates, so no time column here is uncorrected, and the plateau gate puts all twenty-two inside **19.6498 to 20.0908 ms/iter, a 2.24% spread** against the 5% band, where Run 24 read 3.08% over twenty and Run 23 4.63%. **This run's floor is 0.40% on the basis half and 0.61% on the control, and it is read over SIX A/A pairs where every run since Run 10 read sixteen**: the prune deleted ten of them with the arms they doubled, so the sixteen-pair figure this file has quoted at the head of every run since has no successor and the series 1.51%, 2.92%, 2.12%, 2.03%, 1.26% ends at Run 24. Of the six that remain, four carry back to Run 10, and 0.40% and 0.61% read on the six pairs and on those four alike --- `bq-expand-aa-distant` carrying both restricted figures --- against Run 24's 0.34% and 0.19% over its own six. **So one floor is quoted here and not two**, where the last five runs quoted a sixteen-pair figure for an arm against its own duplicate and a restricted one for two different arms; what a margin is judged against from here is 0.40% and 0.61%. The worst A/A cell of either main set is **2.04%** on `stretch-bigstride` on the basis against **5.48%** on `stretch-wide-2xM` on the other, where Run 24 read 16.66% and 18.37% --- and those two are cells and not floors, and are not to be quoted as any.

**The two halves' cells no longer resolve alike, and the basis is the worse-resolved half.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **1.09** on the basis against the HEAD half across the roster, **20 arms wider here and 4 narrower**, where Run 24 read 1.00 at 31 and 21, Run 23 1.01 at 31 and 24 and Run 22 1.01 at 28 and 27. Three runs of an even split and then a one-sided one, on a roster a quarter of the size: with 24 arms rather than 52 the sign test behind that split has a third of the evidence, so read it as a difference to be re-taken rather than as a property of the compilers. It remains a different quantity from the floor: sampling error inside one bench against agreement between two placements of one strategy.

**The sequence ran in ONE window and nothing happened on the machine during it.** Twenty-two processes from 01:15:34 to 05:55:57, every one exiting 0 at its roster's count --- 432 twice, 336 twice, 144 twice, 120 twice, 96 six times and 72 eight times --- the HEAD half first throughout, which is the driver's order, and no process reported a selection it did not ask for. The per-sample instrument's foreign-CPU column, read over each main process's own 432 benches and over the six class processes whose worst A/A cell was widest, puts **not one bench at or above 0.25** on any of them --- so nothing else was running during any sample read, post-run step 3 did not fire and no population was rerun. The gate that preceded it was launched once, on a box reading 0.4% non-idle, and the alarm before the sequence read 0.1%.

**Fifteen cells sink below the shared forcing pass on the basis where Run 24 sank three on the same eighteen shapes, and the count is not a property of anything.** Every one of the fifteen is a `libunord` arm, so `libunord-stage1` is a geomean on 11 of 18 shapes and `libunord-stage2` on 10, and every other row covers all 18; on the HEAD half eleven sink over the same two rows, 13 of 18 and 12 of 18. **The reason the count moved is that there is nothing there to measure**: where the one-block test fires the arm returns a single `VS.slice`, so its cell IS the forcing pass, and on `stretch-bigstride` the basis reads `libunord-stage1` at 0.00110461 against a term of 0.00110463 --- a net of -2.3e-08 against 1.1e-03, two parts in a hundred thousand --- where Run 24's same cell read +7.9e-07 and stayed positive. Which side of zero such a subtraction lands on is decided at the fourth significant figure of a quantity that is all forcing pass, so a sinking count is arithmetic and not a finding, and the two rows it costs a population are the two rows whose figures say least. **What did change with the roster is that the `lib-stage2` family no longer sinks anywhere on either half**, the canonicalizing arms that sank with it having been parked a run ago and `stretch-inner1`, the shape they sank on, retired this one.

**The counted work covers every population, and on this pair it says the compiler is not what moved.** Twenty-two sweeps, both halves over all eleven populations, and no cell perf refused anywhere in the twenty-two files. **Across the halves every class reads as the main set does and none moves a percent**: the counts geomean over the arms with a corrected time runs **0.9955 on `small` to 1.0068 on `window`**, against 1.0019 on the main set. **What the counts are for this run is the within-half reading, which is what registration 7 turns on**, and it has no mode: differencing each arm's instructions per iteration against the shared `sum-only` term, shape by shape, and taking the geomean over the eighteen --- the same correction the time column applies --- `mut-odo-vecdims-add-in-leaf-u1` executes **0.8456** of `mut-odo-vecdims-add-in-leaf`'s corrected instructions on the basis and 0.8444 on HEAD, and **1.0892** and 1.0900 of `-u2`'s; `-u2` itself reads 0.7763 and 0.7747 of `-add-in-leaf`; and `lib-stage1` reads 1.0134 and 1.0138 of `-u2`. **So the merged bound removes fifteen percent of the work and buys six percent of the time, and the un-unrolled body executes nine percent more instructions than the unrolled one and loses four** --- which is the reload the `-g3` twin read, priced. The two compilers agree to a thousandth on every one of those four ratios, so what this pair prices about the family is the family and not the codegen. **That within-half computation is a gap in the reader**, `--counts` reading only a cross-half pair, and it is [under the recommended tasks](../README.md#recommended-tasks-after-run-25).

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The in-situ forcing term --- an arm minus its `-nosum` twin, against the `sum-only` the correction actually subtracts, read off `--aa`'s median column --- reads 1.0199 and 1.0344 on the basis and 1.0242 and 1.0375 on the HEAD half, on `mut-odo-vecdims` and `bq-expand`, the two arms whose `-nosum` twins the prune left standing; the two `sum-only` halves agree at 1.0002 on the basis and 0.9999 on the other, on worst cells of 0.39% and 0.49%. Pinned to these eighteen shapes Run 24's four read 1.0204, 1.0464, 1.0228 and 1.0321, so all four tilt the same way on both runs and the halves are within half a point of each other on each arm. A margin between these halves is not carrying a correction bias.

**The ceiling cannot be read this run, and that is the prune's doing rather than a movement.** `mut-odo-vecdims` against `bq-scan-rem-gm-mulback`, the fastest arm needing nothing at all, has been this file's ceiling reading for eight runs --- 0.5469, 0.5466, 0.5449, 0.5424, 0.5479, 0.5572, 0.5577 and 0.5446 back to Run 17 --- and the denominator was parked to `Only` on 2026-09-04 with every other rung below the top of claim 1, so no process this run times it and the series ends at Run 24. **What stands in its place is claim 10**, `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims` at 0.6394 and 0.6393 on the two halves: a ratio inside the family rather than across the ladder, measuring what the leaf fusion buys rather than what a pure arm costs. The arms are still rostered and `check`ed, so the old reading is one run that re-times them away and not lost; what it is not is extendable from here.

**This run's two columns may NOT be differenced, and that is the first time a `-fspec-constr` pair has been refused since Run 22.** `list` moves **1.10%** between the halves on the main set against the 0.7% bar --- over all 18 shapes, the basis faster on 3 of them --- where Run 24 read 0.25%, Run 23 0.33% and Run 22 0.81% and was refused. So the cross-half figures here are read as an ORDERING and not as a subtraction: a row's distance between the columns carries the baseline's own move as well as the arm's, and the counted work is what says which. Two of the ten classes are past the bar with it, `bcastmid` at 1.0175 and `block` at 1.0088, and their blocks say so; the other eight are inside it, where Run 24 had four of nine inside.

**The straddling loops are the outer loops of the branch's fills on both halves, and the reading is Run 24's on each half exactly.** Each half carries four self-loops straddling a cache line in `Main`-compiled code, at the two body lengths Run 23's dead-spot half had. On the basis post-run step 0's `-g3` twin names **all four** by byte identity --- `fillStage2Short` at `0x42426a`, `fillStage2` at `0x42682a` and `fbMutOdoVecdimsAddInLeafU2` at `0x42e82a`, 55-byte bodies at offset 42, and `fbMutOdoVecdimsAddInLeafU2Down` at `0x42c82f`, 53 bytes at offset 47 --- the branch's own fill, its short-body variant and the two leaf fills, every one of them the OUTER loop of a rotated pair, so **no fill's inner loop straddles on the basis**, read rather than inferred. On HEAD the twin names two, `fillStage2Short` at `0x422d6a` and `fillStage2` at `0x42526a`, and REFUSES the other two, at `0x42b2b1` (53 bytes, offset 49) and `0x42d16c` (55 bytes, offset 44), holding no byte-identical copy of either; their two body lengths are the leaf fills' own, and the count check saying so rather than guessing is what makes the negative honest. **The roster's new arm owns none of them**: `mut-odo-vecdims-add-in-leaf-u1`'s three loops sit at `0x42d780`, `0x42d800` and `0x42d816`, at offsets 0, 0 and 22, all fitting inside their lines. That the outer loop is the one left to straddle is the dead-spot form's own order and not this build's luck, its containment test placing the inner head of a rotated pair and letting the outer yield.

**The regime was confirmed in the binary before the hours were spent**, which nothing afterwards can: a `diag` in the run's own regime puts `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512` on the basis, `run24-g912`'s two figures to the byte, where plain -O1 separates the two tenfold. With no rebuild between the gate and the sequence, that is the only check standing between a mistyped regime and the hours.

**Run 25 records every population twice** --- the main set and all ten stride classes from each half, one process each --- **in one window**, as Runs 23 and 24 did. Every one of the twenty-two exited 0 at the bench count its roster holds --- 432 twice, 336 twice, 144 twice, 120 twice, 96 six times and 72 eight times --- and no process reported a selection it did not ask for. The twenty class processes span **0h6m16s to 0h29m24s**, the two `runs` processes accounting for the whole top of that range at 0h29m18s and 0h29m24s and the two main sets sitting between them at 0h37m23s and 0h37m29s. **The HEAD half ran first throughout**, `ghead` before `g912` on the main set and on each class in turn, which is the driver's order. **The alone-leg riders followed the sequence**, 84 single-bench processes over four invocations of 21 --- 18 main-set shapes and the three anchors on a second reps pass --- each half clean and saturated, every invocation exiting 0.

**The decomposition reproduces on both halves for a seventh run.** The riders time each shape's `list` by itself, one bench per process on that half's own binary, `SAT=` off and on: the saturated legs split the deflation into the state the preamble puts on a clean process --- **+12.19%** on the basis and **+12.46%** on the HEAD half --- and the rest the roster adds on top of it, **+0.55%** and **+0.27%**. Run 24 read +11.83% and +11.33% for the state and -0.99% and +0.20% for the rest, Run 23 +11.70% and +12.37% and +0.39% and -0.28%. So the state term has now reproduced across seven runs inside a point and a half, while the roster's own contribution has stayed under a point on every half of every run that measured it --- and it stays under a point on a roster a quarter of the size, which is the first evidence here that the second term is not a function of how many benches share the process. **The tail is the same shape on both halves and it is the shape Runs 19 to 24 all named**, `stretch-tall-Mx2` at 1.0996 and 1.0951 --- a roster effect on one shape, now on six rosters and three compilers.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition, which Run 19 was in a position to be firm about, having repeated one binary on one box and moved its floor by 1.7x. **What this run cannot repeat, and says so rather than implying otherwise, is that demonstration**: its roster moved, so neither half reproduces an earlier binary. What the cross-run column does carry is the same lesson in the other direction --- fifteen of sixteen non-degenerate arms within a point of the previous build of this recipe, on a roster with no placement-exposed arm left in it.

## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a `-fspec-constr` table**: it is not the regime `Data/Array/Internal.hs` compiles under. **A row's distance from Run 24's column carries a shape-set change as well as a roster term and an evening**, `Main.hs` having moved by the five roster commits of 2026-09-04: eight main-set shapes went out of the timed set with three whole classes, sixteen arms were parked with twelve of their controls, two were added, and the basis half is Run 24's basis recipe with the source moved and not its binary. **Pin the shape set back and that distance is nothing at all**: over these eighteen shapes Run 24's basis reads `mut-odo-vecdims` 0.049, `lib-stage1` 0.028, `mut-odo-vecdims-add-in-leaf-u2` 0.028, `lib-stage2-lean` 0.027, `liblist-stage2` 0.048 and `bq-expand` 0.108, which is this table's column to the digit on every arm the two runs share. **This pair's halves differ in the compiler and in nothing else** --- one source, one shim, one shim environment, one roster --- so a cross-half distance is codegen and layout together; but `list` moved 1.10% between them, past the 0.7% bar, so those distances are read as an ordering and not as a subtraction, and the counted work is what separates the two terms inside one.

**And it is the basis half's**, `run25-g912`, as every published table here is from Run 11 on: the HEAD half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these two dozen rows. That the published half is the 9.12 one is this pair's own decision --- it keeps the lineage, being the compiler every figure in README was measured through, and the dead-spot form it carries is the basis recipe decided on 2026-09-02 --- and the HEAD half is the reading a form about to be published is owed on a second compiler. **Two rows here are first readings**: `mut-odo-vecdims-add-in-leaf-u1` is an arm no run has timed, and `mut-odo-vecdims-add-in-leaf` is timed again after a run in `Only`, as `-u1`'s bound control and for this run alone.

**Comparing runs?** The table below is Run 25's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the claims to test are [the ones after it](#the-claims-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than 0.40% from 1 on the basis half or 0.61% on the control, the HEAD half, read over the six pairs the prune leaves where the last five runs read sixteen, and the same two figures over the four of those six that carry back to Run 10, beside which the worst SINGLE cells of 2.04% and 5.48% are not floors at all and are not to be quoted as any --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What DOES cost a row its population on this run is the correction, not the estimator**: two rows on the basis and three on the HEAD half carry cells the shared forcing pass is not smaller than, and such a cell cannot be corrected at all --- so on those rows the geomean is over 25, 24 or 23 shapes of 26, they are named in the head, and every other row covers all 26. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what claim 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, which claim 7 now carries on a live basis, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.66* | *56* | *2.06x* | *its base arm, forced with one element* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.42* | *76* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.01* | *80* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.01* | *80* | *0.00x* | *the same, at the other end* |
| libunord-stage1 | 0.000 | 0.032 | 0.02 | 80 | 0.00x | new mutating `Vector` method -- stage one behind the unordered one-block test |
| libunord-stage2 | 0.000 | 0.051 | 0.01 | 80 | 0.00x | new mutating `Vector` method -- the branch's driver behind the unordered one-block test |
| lib-stage2-lean | 0.027 | 0.126 | 0.60 | 68 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage2-disp | 0.027 | 0.126 | 0.61 | 68 | 1.00x | new mutating `Vector` method -- the branch's driver, slice route above a run-length threshold |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.028 | 0.126 | 0.48 | 68 | 1.00x | new mutating `Vector` method |
| mut-odo-vecdims-add-in-leaf-u2 | 0.028 | 0.126 | 0.54 | 68 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| lib-stage1 | 0.028 | 0.126 | 0.48 | 68 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.123 | 0.54 | 68 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| mut-odo-vecdims-add-in-leaf | 0.034 | 0.121 | 0.55 | 66 | 1.00x | new mutating `Vector` method -- the same leaf with a counted bound, `-u1`'s control for this run |
| liblist-stage2 | 0.048 | 0.153 | 0.95 | 62 | 2.00x | new mutating `Vector` method -- the branch at the list entry point |
| liblist-stage1 | 0.048 | 0.153 | 0.92 | 63 | 2.00x | new mutating `Vector` method -- stage one at the list entry point |
| *mut-odo-vecdims-aa* | *0.049* | *0.125* | *0.43* | *63* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.049** | 0.125 | 0.38 | 63 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| *mut-odo-vecdims-aa-distant* | *0.049* | *0.125* | *0.42* | *63* | *1.00x* | *A/A control* |
| bq-expand | 0.108 | 0.176 | 0.68 | 52 | 2.06x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.108* | *0.177* | *0.66* | *51* | *2.06x* | *A/A control* |
| *bq-expand-aa-distant* | *0.108* | *0.176* | *0.39* | *52* | *2.06x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.79 | 22 | 22.38x | -- |
| *list-aa-distant* | *1.001* | *1.014* | *0.81* | *22* | *22.38x* | *A/A control* |
| *list-aa-adjacent* | *1.004* | *1.017* | *0.52* | *22* | *22.38x* | *A/A control* |

`concat-runs` has no row, and neither do the other 60 arms the roster holds and checks without timing --- 61 of its 85 in all: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). So a movement below is a movement on the 16 arms this run and Run 24 both give a corrected time, on two builds of one recipe a day apart, and it is a roster change and an evening and not a code change.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table lost eighteen rows to the prune and gained two to the roster**: `mut-odo-vecdims` reads 0.049 with **eleven** timed arms below it and **none** level --- where Run 24, pinned to these same eighteen shapes, read twelve below and four level. The eleven are the two `libunord` arms at 0.000, `lib-stage2-lean` and `lib-stage2-disp` at 0.027, `mut-odo-vecdims-add-in-leaf-u2` with its `-down` twin and `lib-stage1` at 0.028, `mut-odo-vecdims-add-in-leaf-u1` at 0.030, `mut-odo-vecdims-add-in-leaf` at 0.034, and `liblist-stage2` and `liblist-stage1` at 0.048; the four that were level are `canon-vecdims`, `mut-odo-vecdims-add-in`, `mid-copy` and `liblist-stage1`, and three of them are parked. **The ceiling that ran for eight runs cannot be read at all**: `bq-scan-rem-gm-mulback`, the denominator, is parked, and what stands where it stood is claim 10 inside the family. **And the `alloc` column is Run 24's to the digit once the shape set is pinned**: the fills at 1.00x, the two `libunord` arms at 0.00x, `list` at 22.38x and `bq-expand` at 2.06x, every one of them the figure Run 24's own basis gives over these eighteen shapes --- where the column Run 24 published over its twenty-six read 23.50x and 2.40x. The median moved with its population and not with what any arm allocates, which is the third run running that this file has had to say of that column.

**The leaf block's ordering is what this run was built to read, and the unrolling owns the margin.** `genericFillStrided` in `Data/Array/Internal.hs` is a bang-for-bang port of `mut-odo-vecdims-add-in-leaf-u2`. Against `mut-odo-vecdims` that arm reads **0.6394 at 17 of 18, sign p 0.00014** on the basis and **0.6393** on the HEAD half, so the shipped code is 36% ahead of the code it was refined from on both compilers alike, and pinned to these shapes Run 24's two halves read 0.6398 and 0.6391. **Between it and the counted-bound leaf sits `-u1`**, the fill with the bound merged in and the body not unrolled: 0.9360 and 0.9407 of `mut-odo-vecdims-add-in-leaf` and 1.0367 and 1.0479 of `-u2`, all four past both halves' floors. **The count-down variant is level with `-u2` again**, `-u2` reading 1.0003 of it on the basis at 10 of 18, sign p 0.81, and 1.0034 on HEAD at 11 of 18, p 0.48, both intervals covering 1, as on Run 24 and on Run 23's dead-spot half. **What the dispatch around the fill costs came back down**: `lib-stage1`, that same fill reached through the library's own regime test, reads **1.0199** of the bare arm on the basis and **1.0214** on HEAD, against Run 24's 1.0640 and 1.0702 over its own twenty-six shapes --- and pinned to these eighteen Run 24 reads 1.0240 and 1.0268, so four of the four and a half points are the shape set and not the evening, and a user's `toVectorT` pays two percent over the kernel on the shapes this roster keeps.

**The two standing placement controls are gone with the prune, and this is the first run since Run 10 with neither.** `mut-odo-vecdims-add-in` against the arm it varies, and `build` against `mut-odo`, were the two pairs this file read every run for what a slot costs an identical body; both arms of the first and both of the second went to `Only` on 2026-09-04, so neither pair is timed and neither is re-read here. What is left that speaks to placement is the six A/A pairs, which are two placements of one strategy and give the floor, and post-run step 0's twin, which puts `fbMutOdoVecdims` and `fbCanonVecdims` at offset 24 on the basis and `fbMutOdoVecdimsAddBoth` at 4, with the other five tracked heads at 0 --- so the offsets are read and the arms that used to turn them into a time are not. **The 3% [the open list][open] has asked about since Run 10 is not readable on this roster at all**, and the entry says so.


## What the next run compares against

**Run 26's regime and pair are NOT settled, and nothing here settles them: whoever asks for the run decides, as whoever asked for this one did on 2026-09-04.** The regime is `-fspec-constr`, as every run since Run 8, and it is the regime the claims decide in; the shipped file does not set the flag ([the ceiling](../README.md#the-mutable-ceiling-taken)). **What this run settles about the pair it ran is that a second compiler was worth asking twice and is not worth asking a third time on this form**: Run 24 read the dead-spot basis surviving a change of codegen on a roster of 52 arms, and this run reads it surviving on the roster the prune leaves, with `-u1`, `-u2` and `-add-in-leaf` ordering the same way on both halves and claim 10 a tenth of a percent apart between them. A third compiler pair over the same form would re-ask a question answered twice. **What it leaves live** is one thing and it is in the code rather than in the roster: `-u1`'s run-level fill carries a reload of the source base per element that its rank-1 copy does not, read on the `-g3` twin before the run, so the three and a half to five points it gives up to `-u2` are the unrolling AND that spill together, and a pair that takes the spill out is the measurement that would separate them. **What is NOT a candidate** is unchanged: a pair varying the allocation area, closed 2026-08-21, and one varying the roster between its halves, refused because it would break `preflight`'s `check` comparison and both drivers' bench counts.

**The compiler variable was asked a second time on this form and it paid again, on a roster that shares 16 arms with the one that first paid.** Run 24 varied the compiler over 52 timed arms and found the two compilers 5.7% and 6.8% apart on the instructions of the two pure mul-back arms, about one percent apart on the branch's fills, and four to six percent apart in time on arms whose count ratios were 1.0000 exactly. **Every arm that carried the first of those readings is parked and every arm that carried the last is parked**, so what this pair can still see of the compiler is the fills, the library routes and the unordered entry point --- and there it reads a baseline that moved 1.10%, which is the one thing Run 24's pair did not have to contend with. So the answer this pair gives is an ordering rather than a subtraction, and the counted work is what carries the claim about why.

**What Run 25 leaves the next run to read against, and the first item is not a figure.** **The box did not change**, its gate machine check reading -0.08% against the fingerprint Run 24 kept over 18 of 18 shapes and the run's own two main-set processes reading +0.32% and -0.77% against that same fingerprint, worst -1.70%, +1.99% and -2.82% and none past 5%; so absolutes cross from Run 24 to Run 25 freely and the boundary that matters is still the BIOS change before Run 18. **The floor is 0.40% on the basis and 0.61% on the control, and it is ONE figure now rather than two**: the prune left six A/A pairs where the last five runs had sixteen, four of the six carry back to Run 10, and the restricted figure over those four is the same 0.40% and 0.61%. A Run 26 margin is judged against those two and against nothing older, the sixteen-pair series having no successor. **The two columns below may NOT be differenced**: `list` moved **1.10%** between the halves over all 18 shapes, against the 0.7% bar, where Run 24 read 0.25% and Run 23 0.33%; two of the ten classes are past the bar with it and their blocks say so. What the columns price is the compiler, and the counted-work column says which movements that reaches.

**Registered with the pair.** Run 25's seven registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, transcribed into Provenance below before that note goes with the pair. **What Run 26 inherits is the same five riders, routine, and one instrument that spoke for the first time**: the alone legs, the counted-work sweeps over every population, the saturating preamble, the per-sample load fields and `--counts` all ran to form, and `run-evening.sh` drove the gate, the alarm, the sequence and the four rider invocations as one command with the session asleep, which is what it was written for. **What it inherits as a warning is the warning Run 24 left, fired again**: registration 3's second clause asked that no ordering of Run 24's own registrations (1) to (3) reverse, and all three of those are stated against `lib-stage2`, an arm the prune parked on the day the registration was written --- so a clause of a registered prediction was unreadable before the run started, for the second run running, and nothing checked it. `--lint` refuses a live CLAIM that names a parked arm; it still does not read a registration.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at this run's own area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 25 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, not by reading a column across. This run's own two halves, on the rows nearest the decisions --- `dead-spot +lookrts` in the column heads being this run's basis recipe, and the compiler being this pair's only variable:

| strategy | Run 25 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) | Run 25 (SpecConstr, dead-spot +lookrts, -A32m, GHC HEAD) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.049** | 0.050 |
| `mut-odo-vecdims-add-in-leaf` | **0.034** | 0.034 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.030** | 0.031 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.028** | 0.029 |
| `lib-stage1` | **0.028** | 0.029 |
| `lib-stage2-lean` | **0.027** | 0.027 |
| `lib-stage2-disp` | **0.027** | 0.027 |
| `bq-expand` | **0.108** | 0.110 |

**A published geomean is over the same 18 shapes, and two halves of one SpecConstr run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered, which is what an -O1 reading cannot do at an 8% baseline shift. **THE TABLE ABOVE IS NOT SUCH A PAIR**, `list` having moved 1.10% on this run's main set, so the two columns are ORDERED and not differenced: every row above is a percent or so higher on the HEAD half than the basis, and about a percent of that is the baseline rather than the arm. Read the direction and the order, take the size from the counted work, and note that the two rows the pair was built to separate --- `-u1` against its two neighbours --- are read WITHIN each half, where the baseline cancels exactly. **A pair that varies the allocation area is the other exception, and its two halves may never be subtracted from each other.** `list` moved **9.20%** between Run 14's halves, **5.13%** between Run 15's and **16.51%** between Run 16's. **Run 16's is the largest of the three and was registered to be the smallest**, on the reasoning that its two halves both sit at enlarged areas where the earlier two each crossed the default --- a prediction refuted by its own run, and the refutation is the finding: what moves the baseline is not the distance from the default but the in-process deflation, which at roster scale is worse at 64 MB than at 32 MB by more than the whole default-to-32 MB step was worth. So the exception widens rather than narrowing, and it covers every pair that varies the area at all. Every cell of such a pair's second column is scaled by a denominator the pairing moved: read it for the pairing's direction, take no strategy quality off it, and read the arm-by-arm comparison at the head of this file instead, which divides absolutes rather than ratios.

**The HEAD half's own standings on the arms this run's roster brought, which no table here carries, every published table being the basis half's.** Read off `run25-ghead-*.json` with `--pair`, paired geomeans over all 18 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u1` against `mut-odo-vecdims-add-in-leaf` **0.9407** at 17 of 18 (0.9360) and against `-u2` **1.0479** at 3 of 18 (1.0367); `-u2` against `mut-odo-vecdims` 0.6393 (0.6394) and against `-u2-down` 1.0034 (1.0003); `lib-stage1` against `-u2` 1.0214 (1.0199); `lib-stage2-lean` against `lib-stage1` **0.9515** at 15 of 18, sign p 0.0075 (0.9696 at 11 of 18, p 0.48) and against `-u2` 0.9719 at 14 of 18 (0.9889 at 12 of 18); `lib-stage2-disp` against `lib-stage2-lean` 1.0140 (1.0119); and `liblist-stage2` against `liblist-stage1` 0.9787 (0.9851). **So every ordering the basis half reads holds in direction on the second compiler**, and the one place they differ in strength is the lean dispatch against the shipped route, which HEAD reads two points wider and at a sign test that separates where the basis's does not --- on a half whose baseline is a percent below the basis's, so the direction is the reading and the two points are not.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section](../README.md#per-shape-where-the-geomean-hides-the-ordering). The two tables below were regenerated on 2026-09-04 from this run's own JSONs in the per-shape form the README describes; the fourteen-column form they carried until then, and the installer's membership note of that day, are in this file's history.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 5.93 us | 0.085 | `libunord-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.060 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 114 us | 0.068 | `libunord-stage2` 0.003 | `mut-odo-vecdims-add-in-leaf-u2` 0.046 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 365 us | 0.049 | `libunord-stage1` 0.001 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `gather48-src-50` | 3 | 22500 | 435 us | 0.053 | `libunord-stage1` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.022 |
| `stretch-coprime-r7` | 13 | 60060 | 1.02 ms | 0.035 | `libunord-stage2` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.025 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.41 ms | 0.059 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.033 |
| `stretch-primes` | 89 | 250357 | 4.01 ms | 0.029 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 16 ms | 0.044 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.025 |
| `vgg-14-c512-k3` | 3 | 903168 | 18.6 ms | 0.058 | `lib-stage1` 0.033 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.032 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 18.4 ms | 0.035 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `stretch-inner256` | 256 | 1750784 | 33 ms | 0.031 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u1` 0.028 |
| `stretch-pow2stride` | 64 | 1769472 | 28.5 ms | 0.125 | `lib-stage2-disp` 0.126 | `mut-odo-vecdims-add-in-leaf` 0.121 |
| `stretch-r5-8x432` | 8 | 1769472 | 33.4 ms | 0.032 | `lib-stage2-disp` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `stretch-square-1341` | 1341 | 1798281 | 29.5 ms | 0.090 | `lib-stage2-disp` 0.080 | `mut-odo-vecdims-add-in-leaf-u2` 0.080 |
| `stretch-bigstride` | 3 | 1800000 | 49.8 ms | 0.035 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `stretch-tab7MB` | 2 | 1800000 | 37.6 ms | 0.064 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 39.7 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `stretch-wide-2xM` | 2 | 1800000 | 37.7 ms | 0.063 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.023 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 855 us | 0.033 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.018 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 27.5 ms | 0.022 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 37.1 ms | 0.063 | `lib-stage1` 0.023 | `mut-odo-vecdims-add-in-leaf-u1` 0.023 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.42 ms | 0.058 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.033 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 3.92 ms | 0.022 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf` 0.021 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 45.8 ms | 0.037 | `lib-stage1` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.015 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 40 ms | 0.024 | `lib-stage2-disp` 0.018 | `mut-odo-vecdims-add-in-leaf-u1` 0.021 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.03 ms | 0.023 | `lib-stage2-disp` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.022 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.07 ms | 0.029 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.026 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.07 ms | 0.029 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.026 |
| `block-run64-page` | `block` | 64 | 131072 | 2.15 ms | 0.035 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.11 ms | 0.023 | `lib-stage2-disp` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.021 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 868 us | 0.033 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.018 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 867 us | 0.033 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 27.3 ms | 0.021 | `lib-stage2-disp` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.021 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 27.4 ms | 0.025 | `lib-stage2-disp` 0.016 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.021 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.43 ms | 0.058 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.032 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 27.2 ms | 0.027 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.027 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 29.4 ms | 0.054 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2` 0.043 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 113 us | 0.068 | `libunord-stage2` 0.003 | `mut-odo-vecdims-add-in-leaf-u2` 0.046 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 431 us | 0.053 | `libunord-stage2` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.022 |
| `rev-primes` | `rev` | 89 | 250357 | 4.03 ms | 0.029 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-65536` | `runs` | 65536 | 1769472 | 26.1 ms | 0.027 | `liblist-stage2` 0.024 | `mut-odo-vecdims` 0.027 |
| `runs-16384` | `runs` | 16384 | 1785856 | 26 ms | 0.028 | `lib-stage1` 0.025 | `mut-odo-vecdims` 0.028 |
| `runs-4096` | `runs` | 4096 | 1798144 | 26.2 ms | 0.028 | `liblist-stage2` 0.026 | `mut-odo-vecdims` 0.028 |
| `runs-1024` | `runs` | 1024 | 1799168 | 26.1 ms | 0.028 | `lib-stage2-disp` 0.028 | `mut-odo-vecdims` 0.028 |
| `runs-512` | `runs` | 512 | 1799680 | 26.5 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims` 0.028 |
| `runs-256` | `runs` | 256 | 1799936 | 26.3 ms | 0.029 | `lib-stage2-disp` 0.028 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.028 |
| `runs-7` | `runs` | 7 | 1799994 | 29.8 ms | 0.038 | `lib-stage2-disp` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.026 |
| `runs-2` | `runs` | 2 | 1800000 | 37.6 ms | 0.063 | `lib-stage2-disp` 0.027 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `runs-3` | `runs` | 3 | 1800000 | 33.9 ms | 0.053 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.026 |
| `runs-4` | `runs` | 4 | 1800000 | 31.8 ms | 0.046 | `lib-stage2-disp` 0.027 | `mut-odo-vecdims-add-in-leaf-u1` 0.027 |
| `runs-5` | `runs` | 5 | 1800000 | 31 ms | 0.043 | `lib-stage2-disp` 0.026 | `mut-odo-vecdims-add-in-leaf-u2` 0.026 |
| `runs-9` | `runs` | 9 | 1800000 | 29.8 ms | 0.034 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `runs-96` | `runs` | 96 | 1800000 | 27 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.028 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 26.7 ms | 0.030 | `liblist-stage2` 0.027 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.028 |
| `scaled-r5` | `scaled` | 13 | 15015 | 250 us | 0.033 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 942 us | 0.028 | `lib-stage1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.025 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 4.78 ms | 0.034 | `lib-stage1` 0.034 | `mut-odo-vecdims-add-in-leaf` 0.034 |
| `small-patch-k5` | `small` | 5 | 150 | 2.74 us | 0.082 | `libunord-stage1` 0.069 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.062 |
| `small-bcast32` | `small` | 32 | 256 | 4.07 us | 0.056 | `lib-stage2-lean` 0.071 | `mut-odo-vecdims-add-in-leaf-u2` 0.053 |
| `small-flat64` | `small` | 64 | 256 | 4.09 us | 0.064 | `lib-stage2-lean` 0.014 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.063 |
| `small-row96` | `small` | 96 | 384 | 5.95 us | 0.046 | `lib-stage2-lean` 0.058 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.045 |
| `window-28x28-k5` | `window` | 5 | 14400 | 258 us | 0.045 | `lib-stage1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 862 us | 0.096 | `lib-stage2-lean` 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.28 ms | 0.058 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.032 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 8.98 ms | 0.056 | `lib-stage1` 0.031 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.031 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.28 ms | 0.056 | `lib-stage1` 0.031 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.031 |
| `window-128x128-k7` | `window` | 7 | 729316 | 12.7 ms | 0.035 | `lib-stage1` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |

**One row to read first, and it is a property of the shape set and not of any arm**: `stretch-inner1`, the `sInner` 1 shape that made a population of its own out of anything special-casing a unit dimension, was retired from timing on 2026-09-04, so no figure below excludes a shape and every row of the main table is over all eighteen but two. **The two are the `libunord` pair**, whose cells are the shared forcing pass wherever the one-block test fires: fifteen cells sink on the basis and eleven on HEAD, all of them theirs, so `libunord-stage1` is a geomean on 11 and 13 of 18 shapes on the two halves and `libunord-stage2` on 10 and 12. The head of this file prices what that means --- a net of two parts in a hundred thousand, whose sign is arithmetic --- and the consequence for reading the table is that those two rows alone are over a smaller population than the rest, and that the `lib-stage2` family sinks nowhere on either half.


## The claims the next run should test

**Run 25's verdicts first**, since a run reports breaks rather than re-deriving the table. **The manifest's one live claim is claim 10, registered 2026-09-04 in claim 1's place, and this run is the first to read it as a claim**: it held on both halves, 0.6394 on the 9.12 basis and 0.6393 on the HEAD half, and pinned to these eighteen shapes Run 24's two halves read 0.6398 and 0.6391, so four figures across two runs and two codegens span four ten-thousandths. Claim 7 held on every level it still has, and both of the levels it lost this run went with arms the prune parked rather than with anything either arm allocates. Every arm claim 10 names is timed, and `--pair` recovers any retired ordering in one call whenever it is wanted.

**The seven retired claims are not re-read here.** Claims 3, 4, 5 and 9 left the manifest at Run 19's write-up, on a sweep in which all thirteen held on both of that run's halves; claims 2 and 6 left on 2026-08-28 with the parking of the arms their surviving links turned on; and claim 1 left on 2026-09-04 with the prune, which parked every rung below its top. The numbered items below say what each was in a clause. This run does not re-derive any of them and quotes none as its own --- and for claim 1 that is now the only way it could be read at all, three of the four arms its links name being untimed, which is what retiring it gave up and what a run that re-times them would get back.

**Claim 7 held on every level it still has, and its pinned shape set had to be re-cut because six of the shapes it named are no longer timed.** The claim reads its levels over *the 24 main-set shapes Runs 15 to 23 shared*, and the retirement of 2026-09-04 took six of those twenty-four out of the timed set, leaving the eighteen this run reads --- a strict subset, every one of this run's shapes being among the 24. **Re-pinned to those eighteen the levels are unmoved and three runs agree to the digit**: the mutable fills at **1.00x**, the list consumers at **2.00x**, `bq-expand` at **2.06x**, `list` at **22.38x** and the two `libunord` arms at **0.00x**, and Run 24's basis and Run 23's basis, read over the same eighteen, give every one of those five figures exactly. What the published columns of those runs showed instead --- 2.35x and 23.5x on Run 23's twenty-four, 2.40x and 23.50x on Run 24's twenty-six --- is the median's population and not what any arm allocates, which is the third run running that this file has had to say. **Two levels went with the prune and neither was lost**: the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x have no timed arm left, and `offtab-scan-rem`'s 2.00x tier is carried by `liblist-stage1` and `liblist-stage2`, which allocate the list and one concatenation. The class blocks read the tiers unbroken in all ten, `bq-expand` running 1.02x on `flip` to 2.81x on `window` where that class's own `m` shows through, and `list` 19.12x to 24.76x. **One class breaks the fill tier and it is the class built to make it break**: on `small`, at 150 to 384 elements, every fill reads 1.23x and `bq-expand` 1.27x, the per-call constant that 8*l hides everywhere else being a fifth of the call there. **The cross-half agreement is a two-compiler figure and reads as one**: **360 of the 396** main-set cells that allocate in earnest agree to 1e-4, where Run 24's two-compiler pair agreed on 1118 of 1300 and Run 23's one-compiler pair on 1245 of 1272, and the worst disagreement is **5.23e-03 on `cnn-slice-c32/lib-stage1`**. Allocation is deterministic per call, so what the 36 cells apart are is one compiler allocating differently within a tier, which is what Run 19 separated from a tier moving and what no level moving here confirms.

**Claim 10 is the manifest's only live ordering and Run 25 is the first to read it as a claim.** What the leaf fusion buys over the family root: `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims`, registered 2026-09-04 with the prune in claim 1's place, and read here at geomean **0.6394, 17 of 18, sign p 0.00014** on the basis with the HEAD half at **0.6393** --- a margin no floor here approaches, so what the registration asserts is the direction and the win count and not the size. Pinned to these eighteen shapes Run 24's two halves read 0.6398 and 0.6391, so the ordering has now been read on two runs, two rosters and two compilers within four ten-thousandths, and the one shape it does not win, `stretch-pow2stride`, is the same shape on all four readings.

**Readings:** `mut-odo-vecdims-add-in-leaf-u2` / `mut-odo-vecdims` 0.6394, 17 of 18, sign p 0.00014. 1 of 1 registered ordering held.

Restated as the predicates the next run checks, and carrying no reading of its own: the figures each was last measured at are in the `Readings:` paragraphs above, so an entry here changes when a claim is re-aimed and not when a run moves a margin. **All of them are `-fspec-constr` claims, the regime they are read in** --- the shipped file does not set the flag, measured irrelevant to the shipped family --- so they are the set that decides, and a run at -O1 would test Run 7's instead, the two differing in more than their numbers. **They are read in the caller's allocation regime now**, every figure here being taken at the `-A32m` Run 16 promoted to the basis and every horde-ad test and benchmark bakes since 2026-08-21; the gap Runs 14, 15 and 16 priced against a prevailing `-A1G` is closed, and no claim below needs qualifying by it. **And all of them are read against a measured drift band rather than a layout span**, which is what the last three runs bought. A roster *order* change alone moved arms 0.966 to 1.142 between Run 9 and Run 10, and that is what a margin used to have to clear; with the layout pinned, a repetition moves an arm by at most 3.3% on Run 11's reading and 2.1% on Run 23's, most of them by under a point, so a margin above a few percent is now evidence of a strategy. **Run 13 is the first pair here to hold every tracked loop at one offset in both halves**, which is what lets its arm-by-arm comparison be read as the package costing nothing rather than as two terms cancelling. A claim resting on an arm whose own loop the shim skipped --- `list`'s, which is library code --- is still decidable nowhere until that loop is read. **And the pinning claim is measured only in its weak form**: adding `mut-flat-gm-nosum` left every tracked loop at the same address, but a `Force` arm reuses a rostered function and emits no code for emission order to move. The strong form wants an arm that emits its own, and until one is added the claim covers additions that cost nothing to place.

**The list DID need reading against a moved roster this run, and what moved is the list and not a reading.** Eighteen arms went to `Only` and two landed, so claim 1's four links lost three of the four arms they name and the claim retired with them; claim 7 lost two of its levels outright and had to be re-pinned to a shape set that still exists; and claim 10 is a first reading. **What the roster raises is what Run 22 and Run 24 both raised, smaller again**: eleven arms read below `mut-odo-vecdims` where fourteen did, and the shipped library route is still among them, at 0.028 against 0.049. That is left to the next run rather than re-aimed here, and it is [under the recommended tasks](../README.md#recommended-tasks-after-run-25).

1. **Retired 2026-09-04** with the prune, which parked every rung below `mut-odo-vecdims`; its last reading is Run 24's, above, where it held on both halves. What it was: `mut-odo-vecdims` < `mut-flat-gm` < `bq-mut-runs-gm-mulback` < each of `bq-scan-rem-gm-mulback` and `bq-odo-gm-mulback`, the whole ordering read on unconditional arms --- **the ladder the `needs` column draws**, each link pricing one thing the implementation is allowed to ask for. **The foot rung retired 2026-08-29**, on a decision rather than on a reading: it registered the two arms needing nothing at all as a tie, so that either was what would ship if the mutating `Vector` method were refused upstream, and it had read as a tie on every run that carried it. Both arms stay rostered and stay in the rung above, `--pair bq-scan-rem-gm-mulback bq-odo-gm-mulback` recovers the reading whenever the upstream answer wants it, and its last is Run 21's, above. The middle link is the one the README has seen a layout term move --- 0.9708 at 15 of 24 on Run 10's unaligned half against 0.9293 at 22 on its aligned one --- and on a placed layout it has now read the aligned figure five runs running. The ordering has survived nine runs, two changes of basis, two repetitions and three compilers.
2. **Retired 2026-08-28** with the parking of `offtab`, the arm its surviving link turned on --- the other, `bq-scan-rem-gm-mulback`, is timed still. What it asked, since the settlement of 2026-08-24 re-aimed it, was where the arms needing something other than the fix sit --- `offtab` behind `bq-scan-rem-gm-mulback`, a mutable `Int` scratch priced against needing nothing --- and its last reading is Run 20's, above. **Its second link, `bq-expand` behind `mut-odo-vecdims`, had gone already, 2026-08-26** on the reading above, its condition having been spent since 2026-08-24 --- what it priced was the branch's own code against its replacement, and the replacement is now the branch's own code. It was the widest ordering the manifest carried, and `--pair` recovers it, both arms staying rostered and timed.
3, 4, 5. **Retired at Run 19's write-up**, on a last reading in which all three held on both compilers, and for the reasons in the settlement paragraph at the foot of this section. Their numbers are left standing here rather than reused: a verdict recorded against *claim 4* in an earlier run's file still means what it said. What they were, in a clause each, so that nothing is recoverable only from a manifest diff --- claim 3, that a mul-back output pays on the `bq-expand` build; claim 4, that the scan build ties its own build control while beating `bq-expand`, a tie Run 17 promoted to an ordering and three runs then read as one; claim 5, that `bq-expand` beats `bq-gen`, whose refutation of the generate-per-element build stands on Runs 7 and 8. **What none of the three could still foreclose is the point**: every one asks where `bq-expand` sits among arms nothing ships, on a branch whose fix is the `mut-odo-vecdims` family. The arms all stay rostered and timed, so any of these orderings is one `--pair` call away.
6. **Retired 2026-08-28** with the parking of `gen-quotrem`, the arm its only link turned on. What it asked: `gen-quotrem` ties `list`, the first attempt's arithmetic ceasing to be dearer than the list's allocation once the flag takes its own allocation to 1.00x against the list's 23.5x --- the mixed picture this suite exists to have refuted, arriving by a route nobody proposed. Its last reading is Run 20's, above; the `cm-gather` < `list` half was untimed throughout and stands as Run 8's. What goes with it is the standing advice to check `list` as an anchor before blaming a strategy, which is now nobody's claim and is why the machine check reads `list` net per shape every run.
7. Allocation, median multiples of the result **over a pinned shape set**: the mutable fills 1.00x, the list consumers 2.00x, `bq-expand` 2.06x, `list` 22.38x, every one of them read over **the eighteen main-set shapes this roster times**, which are a strict subset of the 24 Runs 15 to 23 shared --- **re-pinned 2026-09-05, six of those 24 having been retired from timing the day before**, so the old population no longer exists and the predicate could not otherwise be evaluated; re-listed 2026-08-28 with the parking, and 2026-09-04 with the prune, which parked the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x outright and left `offtab-scan-rem`'s 2.00x tier to the two `liblist` arms. Every level has reproduced over the eighteen on Runs 23, 24 and 25, which is what makes this the claim to check first when anything else moves. **The pinning is the predicate and not a caveat on it**: Run 24's published column reads 2.40x for `bq-expand` on its own 26, 2.35x on the 24 and 2.06x on these 18 --- so a level compared across two shape sets moves for a reason that is neither a code change nor a slot, and only a level that moves ON ONE POPULATION is a code change. **Read the levels and the cells as two questions**, which Run 19 is the run that separated: its levels all returned while the cross-half cell agreement fell to 1016 of 1080, where the 9.14 pair had 1072 --- so a compiler can reallocate within a tier without moving any tier, and only the cell count sees it.
8. **Retired 2026-08-29**, on a decision and one day after its last re-aiming. What it asked: that every pure arm in the fast tier run its output through the single in-order `vGenerate` over an `m`-length table, so a `bq-*` arm falling behind loses on its table build and not on its output. Its last reading is Run 21's, which found it true and its subject smaller again --- nine arms at or below `mut-odo-vecdims` and not one of them a `bq-*`, so the structure it described governed a tier starting a third of the way down. What retires it is that subject: the pure tier is no longer what ships, and a claim quantified over it was being re-aimed and re-read every run to say so. It was also the one claim with no named invocation, read off the table by eye throughout, so nothing mechanical goes with it.
9. **Retired at Run 19's write-up with 3, 4 and 5**, and for a reason of its own worth keeping: its per-shape half was answered rather than abandoned. `bq-expand-b`'s two best cells were `stretch-inner1` and `stretch-wide-2xM` in every run from Run 8 to Run 16 --- the rank-2 views with one huge outer dimension where seeding from `enumFromStepN` replaces the whole `concatMap` build --- until **Run 17 read `stretch-inner1` and `stretch-square-1341` instead**, which registered the follow-up. Runs 18 and 19 both read the original pair back, on four compilers between them, so the excursion was one run's and the mechanism is cleared of picking the wrong shapes. The geomeans were never the stable part, and the series is why: across Runs 8 to 13 `bq-expand-b` / `bq-expand` read 0.996, 0.9678, 0.9943, 0.9819, 0.9923 and 0.9909, its sign test crossing into significance only on the last of them, while `bq-expand-zf` / `bq-expand` went 3.6% behind, then level at 1.0028, then 1.0325, 1.0197, 1.0256 and 1.0265. Both series were closed at Run 13 and not extended per run, and a closed series in a live manifest is maintenance without a question --- which, with the follow-up spent, is the whole case for retiring it.
10. `mut-odo-vecdims-add-in-leaf-u2` < `mut-odo-vecdims`, the shipped fill against the family root --- **what the leaf fusion buys**, the one thing the roster still prices that the library has taken. Registered 2026-09-04 with the prune, in claim 1's place.

**What the parking of 2026-08-28 did to this set**, recorded in prose because a live item carries a predicate and no reading, and because naming a parked arm inside one is now a `--lint` failure. Claims 2 and 6 retired with `offtab` and `gen-quotrem`, the arms their surviving links turned on. Claim 7 lost three of the arms that carried its levels --- `gen-quotrem` at 1.00x, `bq-mut` at 1.33x and `offtab` at 2.00x, every figure of theirs ending at Run 20 --- but no level with them: `offtab-scan-rem` carries the 2.00x tier, the mutable fills the 1.00x and the scan family the 1.33x, so the list is the same six levels it was. Claim 8 was re-aimed off `bq-expand-zf` and `bq-gen`, its span running from the leading tier to `offtab-scan-rem` instead; the readings above are the last that name the two. Nothing was retired on a reading here, the parking being a decision about what is worth a bench, and the two claims that went had no link left that a run could measure.

**What the prune of 2026-09-04 did to this set** ([what the benchmark does](../README.md#what-the-benchmark-does)), read on the first run after it. Claim 1 retired, every rung below its top parked, and claim 10 is registered in its place, the one live ordering. Claim 7 lost the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x, which have no timed arm left, and keeps `offtab-scan-rem`'s 2.00x through the two `liblist` arms; and it lost its pinned population as well, six of the 24 shapes it named having been retired the same day, which this run re-cut to the eighteen that remain. The ruling of the same day on the short bodies parked `lib-stage2-short` and `lib-stage2-short-lean`, whose last readings stand as Run 24's. The class properties keep their arms, `mut-odo-vecdims` and `bq-expand` both staying timed --- **and property 1's two counter-examples are down to one**: `gen-unsafe`, which carried a `worst` above 1 in seven of ten populations as the baseline's own control, is parked, so the only arms above 1 now are the library-shaped ones and the `list` twins.

**Two homes, and which carries what.** Each live claim has a prose paragraph here and a numbered predicate at the foot, and they divide: the PROSE carries this run's figures and what moved, the ITEM carries the predicate the next run checks and no reading at all. A RETIRED claim keeps its numbered item, which says in a clause what it was, and gets no prose paragraph --- Run 20 wrote two and they restated their own items clause for clause, because stripping the figures had removed the only thing that distinguished them. Each ordering is one line of `--claims`, whose manifest now carries the registered expectation --- the direction of the geomean, a tie by sign test, or claim 9's two best shapes --- and prints HELD or BROKE beside the paired geomean, interval and sign test. `--claims --in-place` then installs that arithmetic as each claim's `Readings:` paragraph above, so a run no longer transcribes it at all; what stays the reading's is whether a HELD margin moved and whether a movement clears the floor, and a BROKE is what obliges the paragraph above its reading to be rewritten rather than requoted. **A claim with no named invocation is a gap in this list, not a claim to be checked by hand**: where a session has to invent the computation it will invent a wrong one, which is how claim 7 came to be read off the raw fitted bytes, explained by a mechanism the previous pair refutes, and then "corrected" onto a rounded print. It has `--compare --alloc` now, and no live claim is without one since claim 8 retired. **The general form, and it is a standing instruction rather than an observation: if a write-up hand-rolls a script to answer something the reader should answer, that is a defect report against the reader** --- fix it there, before the sentence it was written for, or the next run invents its own wrong version. **Two riders, both bought on Run 19.** A new MODE joins the guards its siblings already have, and is checked against them rather than written beside them: `--counts` shipped able to be given without the `--compare` it reads, silently printing the default table and exiting 0 --- the unread-flag family exactly, added next to four sibling readings of `--compare` that were every one of them already guarded, and joining none. **And an instrument may be BACKED OUT, which is not a failure of the write-up but a result of it.** Measure what it flags before shipping it: the obvious mechanical repair for stale paragraphs was built and returned 100 for the four that mattered, so it went, and the refutation with its numbers is under the tasks heading --- worth more than the mode, since it stops the next session building the same thing. A report that never empties is one nobody reads, which this file already knows about hints.

**And for each stride class, the same three properties, now carrying Run 25's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1.** Held in every one of the eleven populations, on both halves, in every regime, roster and layout the README has run --- so it was never slower than the `list` it replaced, on any shape of any class the library can produce. This is the property the classes exist to test, no geomean can state it, and a break would be the one result here to bear on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims` since 2026-08-22: **on Run 25 its worst is 0.125 on the main set and 0.096 in a class (`window`), both read on the basis half, with the HEAD half at 0.125 and 0.096** --- so the property holds for the arm decided, on both compilers, and neither end is within a tenth of 1: the main-set end is a factor of 8.0 inside it and the class end 10.4. Both halves are quoted because one is not enough: Run 18's entry here read a floor-level figure from whichever half happened to be lower, which is the defect this phrasing exists to prevent.

Beside property 1, and the case has simplified since the prune of 2026-09-04 parked the arm that used to be half of it: **only one thing breaks it now, and it is not a baseline control.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. What is left is the other break, and it is a fill the library would ship. **The library-shaped arms break it on `runs` alone and all at `runs-2`**, five of the eleven and on both halves: `libunord-stage1` at **1.241**, `liblist-stage1` 1.230, `lib-stage1` --- the shipped route --- at **1.216**, `liblist-stage2` 1.072 and `libunord-stage2` 1.070 on the basis, and 1.208, 1.216, 1.191, 1.060 and 1.059 on HEAD. So on 900000 runs of two elements five of the ten library-shaped arms are slower than the `list` baseline they replace --- every route that takes a slice per run at that length --- while `lib-stage2-lean` and `lib-stage2-disp` fill every run whatever its length and are the two that are not. `lib-stage1` reads 1.0133 at `runs-3` on the basis besides, and nothing else anywhere reaches 1. **The property is stated of `mut-odo-vecdims` and holds of it; it does NOT hold of what the library actually calls.** A reader taking property 1 as clearance for the code that ships is reading it wider than it is stated, and the class that catches the difference is `runs`.

2. **The top of the table keeps its order**: `mut-odo-vecdims` fastest, `bq-expand` behind it. **The first clause breaks in nine of the ten CLASS populations --- the main set is the eleventh and is counted separately throughout this section --- and in the tenth, `block`, the leader is a `mut-odo-vecdims` sibling, which counts as the family's and not as a break, as it did on two of Run 23's.**

Beside property 2, and every arm named here is timed on this roster: the nine breaks divide three ways. **Two are degenerate**: `libunord-stage2` leads `rev` at 0.001 and `flip` at 0.000 of `mut-odo-vecdims`, its one-block test firing on every view of those classes and collapsing them to a single slice, so it prices dispatch and not filling --- and on `flip` its cell is its own forcing term on one shape of three, so that lead is not priced at all. **Three are the lean dispatch**: `lib-stage2-lean` leads `bcastmid`, `window` and `small` by 47.45%, 54.31% and 19.91% against those populations' floors of 0.80%, 1.74% and 0.27%. **Three are the re-cut dispatch**: `lib-stage2-disp` leads `bcast`, `runs` and `compose` by 40.04%, 22.46% and 34.11% against 0.83%, 2.79% and 0.41%. **And one is the shipped route**: `lib-stage1` leads `scaled` by 11.34% against 2.09%'s successor, this run's 0.56%. So where a run ago the classes were led by candidates, a composite and the unordered arm, seven of the nine breaks are now led by two arms the library is one decision from shipping and one by the route it ships today. The third clause reads the last candidate `bq-expand` behind `mut-odo-vecdims` and holds in all ten on both halves.

3. **The allocation tiers survive, and every level is Run 23's and Run 24's once the shape set is pinned**: the mutable fills at the result vector, `bq-expand` between 1.02x and 2.81x it, `list` an order of magnitude above. On the main set the fills read 1.00x, the two `liblist` arms 2.00x, `bq-expand` 2.06x and `list` 22.38x, and Run 23's and Run 24's own basis halves give every one of those five over the same eighteen shapes --- the columns those runs published, 2.35x and 23.5x and 2.40x and 23.50x, being their own populations and not their arms. Where a level moves it is the class's own `m` or its own size showing through, exactly as this property warned --- `bq-expand` at 1.02x on `flip` and 2.81x on `window`, and on `small` **every fill at 1.23x**, the per-call constant that dividing by `8*l` hides at a million elements and cannot at 256 --- with the ordering of tiers unbroken in all ten and `list` running 19.12x to 24.76x across them, on both halves. The two `libunord` arms sit below the result vector at 0.00x on the MAIN SET, and in the classes they mostly do not: on six --- `bcast`, `bcastmid`, `window`, `scaled`, `compose` and `small` --- both read about 2.00x, `libunord-stage1` reaching 2.47x on `small`; on `rev` and `flip`, `libunord-stage1` reads about 2.00x while `libunord-stage2` stays under the vector at 0.01x and 0.00x; and on `runs` and `block` they read 1.19x and 1.14x, and 1.42x and 1.31x. So the one-block test's 0.00x tier is the main set's alone, `libunord-stage1` never reaching it in any class, and a class reading of that tier is a different quantity. On a pair whose two halves are two compilers' code this is the property that says the difference is not a change of what gets allocated: every tier is identical on both halves, and the 36 main-set cells of 396 that disagree past 1e-4 are one compiler allocating differently INSIDE a tier, which is the distinction Run 19 is the run that separated.

**SETTLED 2026-08-24 and APPLIED 2026-08-25, at Run 19's write-up rather than at the settlement**, so that the retiring orderings got one last cross-compiler reading and the retirement is recorded with it --- which is how Run 17 retired claim 4's tie, in prose at its write-up with the manifest taking it the same day. **They got it**: all thirteen held on both of Run 19's halves, and the eight that remain hold on both too, so each of the four retires on a reading rather than on a decision. `CLAIMS` in `read-run.py` carried claims 1, 2 and 6 alone after that settlement, and **claim 1 alone since 2026-08-28**, `offtab` and `gen-quotrem` having been parked. The test applied: an ordering stays only if it forecloses something anyone would propose again *and* can still break. What fails both is a figure, and figures live in the tables above. **Claims 3, 5 and 9 retire outright.** Claim 3 sets one output form against another on a build nothing ships, where every leading pure arm is a `-gm-mulback` already; claim 5's `bq-expand` / `bq-gen` says of itself that the refutation stands on Runs 7 and 8, and claim 6 keeps that family guarded through `gen-quotrem`; claim 9's two series are closed at Run 13 by this section's own words, and a closed series in a live manifest is maintenance without a question. **Claim 4 retires with them, its tie moving into claim 1** --- and what goes with it is the one place the manifest reads a *builder* apart from its output, which `--pair` recovers whenever it is wanted, both arms staying rostered. **Claim 1 becomes the ladder the `needs` column already draws**, gaining `bq-scan-rem-gm-mulback`, the best arm needing nothing at all, between `bq-mut-runs-gm-mulback` and `bq-odo-gm-mulback`: the first ahead of it at **0.9060** and **0.9171** on Run 18's two halves, 19 and 17 of 24, which is what a mutable `Int` scratch buys; and the second **tied** with it at **0.9902** and **0.9936**, 13 and 12 of 24 at p 0.84 and 1, so the two fastest pure arms were indistinguishable and either was what would ship if the mutating method were refused --- the rung this installed, and retired again on 2026-08-29. Its three existing links stay, the middle one redundant with the two new ones and carrying seven runs of history they do not. **Claim 2 keeps its number and changes its question** to where the arms needing something other than the fix sit: `offtab`, which needs only that `Int` scratch, behind `bq-scan-rem-gm-mulback` at **1.36** and **1.44**; and `bq-expand` behind `mut-odo-vecdims` at **2.09** and **2.13**, kept only while `Data/Array/Internal.hs` carried `bq-expand`, and to retire with the three `TODO: retarget` markers, which were one decision with it. **That condition was spent the same day and the link outlived it by two runs**: the file went to `vFillStrided` on 2026-08-24 and the markers went with the prose they marked, but nothing read the condition back, so Run 20 registered and read the link like any other and it retired only on 2026-08-26. Thirteen registered orderings become eight here, and seven when that second link finally goes; claims 7 and 8 stayed unmanifested prose. **The rewriting the ask paired with this was already done**: the eight *What the class says* paragraphs were written to the re-aimed properties at Run 18's write-up, and only the sentence asking for it survived.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 25 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) records every class on BOTH halves**, one process each, in [the sequence](../README.md#making-a-major-benchmark-run), all twenty-two processes in one window. Every table below is the **basis half**'s, the 9.12 one, the half that keeps the lineage; what the second half buys is that the pair's variable --- the compiler --- can be read on a class, with the counted work beside it on every population. **Read across the halves and the two compilers are close and unevenly split.** Of the 200 arm-comparisons the ten classes carry, one (`libunord-stage2` on `flip`, a basis cell of which the correction does not leave positive) sits out the vote and the geomeans as degenerate; **124 put the basis half faster and 75 the HEAD half**, and the ten geomeans run **0.9793 on `small` to 1.0079 on `rev`**, nine of them below 1. **The extremes are the unordered pair at both ends**, `libunord-stage1` at 0.9346 on `flip` and `libunord-stage2` at 1.0651 on `rev`, and the low extreme is `libunord-stage1` in three of the ten --- an arm that returns a single slice on some views of a class and fills on others, read across two codegens. **Two classes disqualify their own cross-half line**, `bcastmid` at 1.0175 and `block` at 1.0088 moving `list` past the 0.7% bar, where Run 24 had five of nine past it; the eight inside are `rev`, `bcast`, `window`, `scaled`, `runs`, `flip`, `small` and `compose`.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name, since which arm leads is half of what the column says --- so where property 2 breaks the two name different arms and the gap between them is what the break is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's sixteen A/A controls. A cell that breaks one of [the three properties](#the-claims-the-next-run-should-test) is bolded, and the class's own paragraph says what broke.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the nine geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the nine cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the nine geomeans and the extreme arms are each an aggregate over the nine `--block --compare` lines below, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- `rev`, `revsome`, `bcast`, `bcastmid`, `reshape1`, `slice`, `window`, `scaled`, `runs` --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own sixteen pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now --- six at three, `bcastmid` and `reshape1` at four and `runs` at eleven --- so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 2 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.047 | 0.068 | **`libunord-stage2`** 0.001 | `mut-odo-vecdims-add-in-leaf-u2` 0.026 | 0.77% |
| `bcast` | 3 | 0.035 | 0.063 | **`lib-stage2-disp`** 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.021 | 0.83% |
| `bcastmid` | 4 | 0.032 | 0.058 | **`lib-stage2-lean`** 0.017 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.022 | 0.80% |
| `window` | 6 | 0.055 | 0.096 | **`lib-stage2-lean`** 0.027 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 | 1.74% |
| `scaled` | 3 | 0.032 | 0.034 | **`lib-stage1`** 0.027 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.027 | 0.56% |
| `runs` | 14 | 0.032 | 0.063 | **`lib-stage2-disp`** 0.027 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.027 | 2.79% |
| `flip` | 3 | 0.050 | 0.058 | **`libunord-stage2`** 0.000 | `mut-odo-vecdims-add-in-leaf-u2` 0.034 | 1.16% |
| `block` | 5 | 0.027 | 0.035 | `lib-stage2-disp` 0.025 | **`mut-odo-vecdims-add-in-leaf-u2-down`** 0.025 | 0.60% |
| `small` | 4 | 0.061 | 0.082 | **`lib-stage2-lean`** 0.053 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.055 | 0.27% |
| `compose` | 4 | 0.027 | 0.033 | **`lib-stage2-disp`** 0.018 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 | 0.41% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` on `reshape1` alone.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.09* | *134* | *2.52x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *157* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *157* | *0.00x* |
| libunord-stage2 | 0.001 | 0.003 | 0.04 | 157 | 0.01x |
| lib-stage1 | 0.025 | 0.048 | 0.10 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.046 | 0.08 | 146 | 1.00x |
| lib-stage2-disp | 0.026 | 0.035 | 0.07 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.026 | 0.047 | 0.07 | 146 | 1.00x |
| lib-stage2-lean | 0.026 | 0.035 | 0.08 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.047 | 0.15 | 146 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.032 | 0.052 | 0.08 | 144 | 1.00x |
| liblist-stage2 | 0.040 | 0.045 | 0.28 | 142 | 2.01x |
| libunord-stage1 | 0.041 | 0.061 | 0.84 | 142 | 2.03x |
| liblist-stage1 | 0.042 | 0.058 | 0.58 | 142 | 2.01x |
| *mut-odo-vecdims-aa-distant* | *0.047* | *0.068* | *0.08* | *137* | *1.00x* |
| **mut-odo-vecdims** | **0.047** | 0.068 | 0.08 | 137 | 1.00x |
| *mut-odo-vecdims-aa* | *0.047* | *0.069* | *0.13* | *137* | *1.00x* |
| bq-expand | 0.103 | 0.176 | 0.11 | 128 | 2.52x |
| *bq-expand-aa-adjacent* | *0.104* | *0.176* | *0.11* | *128* | *2.52x* |
| *bq-expand-aa-distant* | *0.104* | *0.176* | *0.12* | *128* | *2.52x* |
| list (baseline) | 1.000 | 1.000 | 0.24 | 87 | 23.43x |
| *list-aa-adjacent* | *1.006* | *1.009* | *0.31* | *86* | *23.43x* |
| *list-aa-distant* | *1.012* | *1.012* | *0.29* | *86* | *23.43x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0077, worst cell 1.18% on `rev-primes`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 1.0028 on a worst cell of 0.66% on `rev-primes`, its interval covering 1. The in-situ term reads 1.0117, 1.0159 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0075, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h6m18s, peak 96 MiB in use, 24 MiB max residency; the reader reads 24 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.18 ms per call raw, 4.03 ms net.

**Per shape, in the lead's order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.068/0.053/0.029

**Across the halves:** 6 of the 20 arms are faster on this half and 14 slower, at a geomean of 1.0079, from `libunord-stage1` at 0.9794 to `libunord-stage2` at 1.0651, with `list` itself at 0.9992.

**What the class says:** properties 1 and 3 hold for `mut-odo-vecdims` --- `worst` 0.068 against a `list` it never loses to, and the tiers at 1.00x, 2.52x and 23.43x --- and property 2 breaks outright to `libunord-stage2` at 0.001, **0.0157 of `mut-odo-vecdims` on 3 of 3 shapes**, a 98.44% margin against this class's 0.77% floor: the unordered one-block test firing on every view of the class and collapsing it to a single `VS.slice`, so the break prices dispatch and not filling, as on Runs 22, 23 and 24. **What the class adds is across the halves**, where it is the one population of the ten whose geomean sits above 1: 6 of the 20 arms are faster on the basis and 14 on HEAD, at 1.0079, from `libunord-stage1` at 0.9794 to `libunord-stage2` at 1.0651 --- both ends the unordered pair again --- on a `list` that moved 0.08%, so the line is read for the pair's variable and what it says is that HEAD is the faster half here.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.70* | *52* | *1.38x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *82* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *69* | *0.00x* |
| lib-stage2-disp | 0.020 | 0.027 | 0.59 | 61 | 1.00x |
| lib-stage2-lean | 0.020 | 0.027 | 0.48 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.021 | 0.023 | 0.41 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.023 | 0.41 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.021 | 0.023 | 0.44 | 60 | 1.00x |
| lib-stage1 | 0.021 | 0.023 | 0.49 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.022 | 0.023 | 0.43 | 60 | 1.00x |
| *mut-odo-vecdims-aa* | *0.035* | *0.063* | *0.37* | *60* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.035* | *0.063* | *0.31* | *60* | *1.00x* |
| **mut-odo-vecdims** | **0.035** | 0.063 | 0.14 | 60 | 1.00x |
| liblist-stage2 | 0.040 | 0.048 | 0.85 | 55 | 2.00x |
| liblist-stage1 | 0.042 | 0.048 | 0.83 | 53 | 2.00x |
| libunord-stage1 | 0.042 | 0.048 | 0.94 | 53 | 2.00x |
| libunord-stage2 | 0.045 | 0.048 | 0.91 | 54 | 2.00x |
| *bq-expand-aa-distant* | *0.091* | *0.096* | *0.35* | *46* | *1.38x* |
| bq-expand | 0.092 | 0.095 | 0.66 | 46 | 1.38x |
| *bq-expand-aa-adjacent* | *0.093* | *0.095* | *0.70* | *46* | *1.38x* |
| list (baseline) | 1.000 | 1.000 | 0.89 | 18 | 20.62x |
| *list-aa-adjacent* | *1.006* | *1.011* | *0.69* | *18* | *20.62x* |
| *list-aa-distant* | *1.008* | *1.020* | *0.88* | *18* | *20.62x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0083, worst cell 1.96% on `bcast-tall-Mx2`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 1.0004 on a worst cell of 0.36% on `bcast-tall-Mx2`, its interval covering 1. The in-situ term reads 1.0144, 1.0096 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0081, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h6m20s, peak 141 MiB in use, 42 MiB max residency; the reader reads 24 benchmarks over 3 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 28.6 ms per call raw, 27.5 ms net.

**Per shape, in the lead's order (bcast-inner8, bcast-inner900, bcast-tall-Mx2):** `mut-odo-vecdims` 0.033/0.022/0.063

**Across the halves:** 17 of the 20 arms are faster on this half and 3 slower, at a geomean of 0.9933, from `liblist-stage2` at 0.9769 to `libunord-stage2` at 1.0122, with `list` itself at 0.9955.

**What the class says:** the re-cut dispatch leads it, and the arm that led it a run ago is parked. `lib-stage2-disp` reads 0.020 of the `list` baseline against `mut-odo-vecdims`'s 0.035, a **40.04%** margin on a 0.83% floor, where Run 24's leader `lib-stage2-short-lean` read 39.79% and went to `Only` with the short bodies --- so the class's lead is the same size and belongs to an arm that is one decision from shipping rather than to one nothing will. Properties 1 and 3 hold, `worst` 0.063 and the tiers at 1.00x, 1.38x and 20.62x. Across the halves the basis is faster on 17 of 20 arms at a geomean of 0.9933, from `liblist-stage2` at 0.9769 to `libunord-stage2` at 1.0122, on a `list` that moved 0.45%, so the line is read.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.37* | *69* | *1.57x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.30* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *88* | *0.00x* |
| lib-stage2-lean | 0.017 | 0.033 | 0.31 | 80 | 1.00x |
| lib-stage2-disp | 0.017 | 0.033 | 0.33 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.022 | 0.033 | 0.32 | 79 | 1.00x |
| lib-stage1 | 0.022 | 0.033 | 0.33 | 79 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.033 | 0.28 | 79 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.024 | 0.034 | 0.41 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.024 | 0.037 | 0.25 | 78 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.058* | *0.27* | *76* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.032* | *0.058* | *0.36* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.032** | 0.058 | 0.22 | 76 | 1.00x |
| liblist-stage2 | 0.033 | 0.047 | 0.50 | 74 | 2.00x |
| libunord-stage2 | 0.033 | 0.049 | 0.52 | 74 | 2.00x |
| libunord-stage1 | 0.037 | 0.044 | 0.57 | 74 | 2.00x |
| liblist-stage1 | 0.038 | 0.044 | 0.60 | 74 | 2.00x |
| bq-expand | 0.086 | 0.132 | 0.41 | 64 | 1.57x |
| *bq-expand-aa-adjacent* | *0.086* | *0.132* | *0.44* | *64* | *1.57x* |
| *bq-expand-aa-distant* | *0.087* | *0.132* | *0.33* | *64* | *1.57x* |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.72* | *29* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 0.76 | 29 | 21.22x |
| *list-aa-distant* | *1.003* | *1.009* | *1.03* | *29* | *21.22x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0080, worst cell 1.85% on `bcastmid-b200k`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 0.9999 on a worst cell of 0.03% on `bcastmid-c32-cnn`, its interval covering 1. The in-situ term reads 1.0211, 1.0362 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0058, which the correction amplifies by 1.35x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m22s, peak 134 MiB in use, 35 MiB max residency; the reader reads 24 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 46.9 ms per call raw, 45.8 ms net.

**Per shape, in the lead's order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.058/0.022/0.037/0.024

**Across the halves:** 9 of the 20 arms are faster on this half and 11 slower, at a geomean of 1.0038, from `bq-expand-aa-adjacent` at 0.9883 to `libunord-stage2` at 1.0332, with `list` itself at 1.0175. **The baseline moved 1.75% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** the lean dispatch leads it, as it did a run ago and by the same margin. `lib-stage2-lean` reads 0.017 against `mut-odo-vecdims`'s 0.032, a **47.45%** margin on a 0.80% floor, against Run 24's 47.28% --- so the one class the lean route led outright before the prune it still leads, and the arms that were level with it there are parked. Properties 1 and 3 hold, `worst` 0.058 and the tiers at 1.00x, 1.57x and 21.22x. **Its cross-half line is one of the two this run does not read**: `list` moved 1.75% between the halves, the widest of the ten and past the 0.7% bar, so the 1.0038 geomean above is an ordering and not a subtraction.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.25* | *82* | *2.81x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *98* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *111* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *111* | *0.00x* |
| lib-stage2-lean | 0.027 | 0.035 | 0.17 | 96 | 1.00x |
| lib-stage2-disp | 0.027 | 0.035 | 0.16 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.029 | 0.032 | 0.15 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.029 | 0.032 | 0.17 | 96 | 1.00x |
| lib-stage1 | 0.029 | 0.032 | 0.15 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.035 | 0.18 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.034 | 0.037 | 0.12 | 94 | 1.00x |
| liblist-stage1 | 0.040 | 0.043 | 0.39 | 92 | 2.00x |
| libunord-stage1 | 0.040 | 0.043 | 0.36 | 92 | 2.00x |
| libunord-stage2 | 0.045 | 0.097 | 0.40 | 92 | 2.00x |
| liblist-stage2 | 0.045 | 0.098 | 0.24 | 92 | 2.00x |
| *mut-odo-vecdims-aa* | *0.055* | *0.096* | *0.17* | *89* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.055* | *0.096* | *0.16* | *89* | *1.00x* |
| **mut-odo-vecdims** | **0.055** | 0.096 | 0.12 | 89 | 1.00x |
| *bq-expand-aa-distant* | *0.120* | *0.129* | *0.19* | *77* | *2.81x* |
| bq-expand | 0.121 | 0.130 | 0.21 | 77 | 2.81x |
| *bq-expand-aa-adjacent* | *0.121* | *0.130* | *0.21* | *77* | *2.81x* |
| *list-aa-adjacent* | *0.999* | *1.021* | *0.40* | *42* | *24.76x* |
| list (baseline) | 1.000 | 1.000 | 0.50 | 42 | 24.76x |
| *list-aa-distant* | *1.017* | *1.044* | *0.34* | *42* | *24.76x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0174, worst cell 4.37% on `window-128x128-k7`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 0.9988 on a worst cell of 1.15% on `window-64x64-k1x9`, its interval covering 1. The in-situ term reads 1.0072, 1.0517 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0168, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m29s, peak 121 MiB in use, 24 MiB max residency; the reader reads 24 benchmarks over 6 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 13.1 ms per call raw, 12.7 ms net.

**Per shape, in the lead's order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2):** `mut-odo-vecdims` 0.045/0.056/0.096/0.035/0.058/0.056

**Across the halves:** 13 of the 20 arms are faster on this half and 7 slower, at a geomean of 1.0026, from `bq-expand-aa-adjacent` at 0.9853 to `lib-stage2-lean` at 1.0429, with `list` itself at 1.0034.

**What the class says:** the lean dispatch leads it too, and this is the first run to read the class at six views. `lib-stage2-lean` reads 0.027 against `mut-odo-vecdims`'s 0.055, a **54.31%** margin on this class's 1.74% floor --- the widest floor of the ten and the margin still clears it thirtyfold --- where Run 24's leader on four views was the parked composite at 63.79%. **The two views that landed 2026-09-03 read as the four did**: `window-224x224-k3-s2` and `window-224x224-k3-d2` put `mut-odo-vecdims` at 0.058 and 0.056 against `window-224x224-k3`'s 0.056, so striding and dilating a k3 patch changes nothing this table can see, and the arm they were registered for was parked before they ran. Properties 1 and 3 hold, `worst` 0.096 --- the highest `worst` cell of any population --- and the tiers at 1.00x, 2.81x and 24.76x, `bq-expand`'s being the widest of the ten where that class's own `m` shows through. Across the halves the basis is faster on 13 of 20 at 1.0026, on a `list` that moved 0.34%.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.08* | *119* | *1.14x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.08* | *144* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *137* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *137* | *0.00x* |
| lib-stage1 | 0.027 | 0.034 | 0.14 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.034 | 0.11 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.034 | 0.11 | 127 | 1.00x |
| lib-stage2-lean | 0.028 | 0.034 | 0.19 | 127 | 1.00x |
| lib-stage2-disp | 0.028 | 0.034 | 0.11 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.039 | 0.17 | 126 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.030 | 0.034 | 0.11 | 126 | 1.00x |
| **mut-odo-vecdims** | **0.032** | 0.034 | 0.07 | 126 | 1.00x |
| *mut-odo-vecdims-aa* | *0.033* | *0.034* | *0.06* | *126* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.033* | *0.034* | *0.11* | *126* | *1.00x* |
| libunord-stage1 | 0.040 | 0.062 | 0.32 | 123 | 2.01x |
| liblist-stage1 | 0.041 | 0.062 | 0.22 | 123 | 2.00x |
| libunord-stage2 | 0.042 | 0.062 | 0.25 | 123 | 2.01x |
| liblist-stage2 | 0.043 | 0.062 | 0.24 | 123 | 2.00x |
| *bq-expand-aa-distant* | *0.096* | *0.099* | *0.06* | *112* | *1.14x* |
| bq-expand | 0.096 | 0.098 | 0.10 | 111 | 1.14x |
| *bq-expand-aa-adjacent* | *0.097* | *0.098* | *0.08* | *111* | *1.14x* |
| list (baseline) | 1.000 | 1.000 | 0.28 | 71 | 19.43x |
| *list-aa-adjacent* | *1.001* | *1.002* | *0.17* | *71* | *19.43x* |
| *list-aa-distant* | *1.006* | *1.015* | *0.27* | *71* | *19.43x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0056, worst cell 1.47% on `scaled-super-r3`, and 6 of 6 intervals cover 1. The `sum-only` halves agree at 1.0017 on a worst cell of 0.46% on `scaled-super-r3`, its interval covering 1. The in-situ term reads 1.0126, 1.0130 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0054, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h6m16s, peak 109 MiB in use, 24 MiB max residency; the reader reads 24 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 4.97 ms per call raw, 4.78 ms net.

**Per shape, in the lead's order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.028/0.034/0.033

**Across the halves:** 17 of the 20 arms are faster on this half and 3 slower, at a geomean of 0.9882, from `mut-odo-vecdims-aa-distant` at 0.9721 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0246, with `list` itself at 1.0066.

**What the class says:** the shipped route leads it, as it did a run ago. `lib-stage1` reads 0.027 against `mut-odo-vecdims`'s 0.032, an **11.34%** margin on a 0.56% floor, against Run 24's 9.07% on a 2.09% one --- the floor having come in with the prune, so the same lead now clears it twentyfold. Properties 1 and 3 hold, `worst` 0.034 and the tiers at 1.00x, 1.14x and 19.43x, `bq-expand`'s the second narrowest of the ten. **The class's own slot is what to read here, not the class**: `mut-odo-vecdims` moved 1.0198 against Run 24 and its two A/A twins moved with it, 1.0143 and 1.0189, which is the disturbance [the open list](../README.md#what-is-open) has carried since Run 12 and is a slot's and not an arm's. Across the halves the basis is faster on 17 of 20 at 0.9882, the lowest geomean but one, on a `list` that moved 0.66% --- inside the bar by four hundredths of a point.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.03* | *52* | *1.08x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.12* | *76* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *69* | *0.00x* |
| lib-stage2-disp | 0.027 | 0.028 | 0.13 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.028 | 0.09 | 59 | 1.00x |
| lib-stage2-lean | 0.027 | 0.028 | 0.09 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.029 | 0.09 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.030 | 0.13 | 58 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.029 | 0.030 | 0.09 | 58 | 1.00x |
| **mut-odo-vecdims** | **0.032** | 0.063 | 0.10 | 58 | 1.00x |
| *mut-odo-vecdims-aa* | *0.032* | *0.064* | *0.07* | *58* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.033* | *0.064* | *0.06* | *58* | *1.00x* |
| libunord-stage2 | 0.067 | 1.070 | 0.19 | 56 | 1.14x |
| liblist-stage2 | 0.067 | 1.072 | 0.16 | 57 | 1.14x |
| *bq-expand-aa-distant* | *0.097* | *0.102* | *0.02* | *46* | *1.08x* |
| *bq-expand-aa-adjacent* | *0.097* | *0.103* | *0.04* | *46* | *1.08x* |
| bq-expand | 0.097 | 0.103 | 0.03 | 46 | 1.08x |
| liblist-stage1 | 0.108 | 1.230 | 0.20 | 54 | 1.19x |
| libunord-stage1 | 0.108 | 1.241 | 0.15 | 54 | 1.19x |
| lib-stage1 | 0.108 | 1.216 | 0.18 | 54 | 1.19x |
| list (baseline) | 1.000 | 1.000 | 2.65 | 17 | 19.27x |
| *list-aa-distant* | *1.026* | *1.059* | *0.59* | *18* | *19.27x* |
| *list-aa-adjacent* | *1.028* | *1.050* | *0.20* | *18* | *19.27x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0279, worst cell 4.96% on `runs-r3-48x30`, and 2 of 6 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.41% on `runs-256`, its interval covering 1. The in-situ term reads 1.0304, 1.0309 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0268, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h29m18s, peak 581 MiB in use, 247 MiB max residency; the reader reads 24 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 38.7 ms per call raw, 37.6 ms net.

**Per shape, in the lead's order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.063/0.053/0.046/0.043/0.038/0.034/0.028/0.029/0.028/0.028/0.028/0.028/0.027/0.030

**Across the halves:** 14 of the 20 arms are faster on this half and 6 slower, at a geomean of 0.9985, from `mut-odo-vecdims-add-in-leaf-u2-down` at 0.9923 to `lib-stage1` at 1.0067, with `list` itself at 1.0005.

**What the class says:** the re-cut dispatch leads it, which is what Run 24's registration 6 settled and this run reproduces. `lib-stage2-disp` reads 0.027 against `mut-odo-vecdims`'s 0.032, a **22.46%** margin on a 2.79% floor, against Run 24's 22.87% on 3.07%. **And this is the one class where property 1 breaks for arms the library ships**: five of the ten library-shaped arms carry a `worst` above 1, every one of them at `runs-2` and on both halves --- `libunord-stage1` 1.241, `liblist-stage1` 1.230, `lib-stage1` 1.216, `liblist-stage2` 1.072 and `libunord-stage2` 1.070 on the basis --- so on 900000 runs of two elements every route that takes a slice per run is slower than the `list` it replaces, while the two that fill every run whatever its length are not. `lib-stage1` reads 1.0133 at `runs-3` besides, and nothing anywhere else reaches 1. Property 3 holds, the tiers at 1.00x, 1.08x and 19.27x, `bq-expand`'s the narrowest of the ten where `m` runs from 2 to 65536. Across the halves the basis is faster on 14 of 20 at 0.9985, on a `list` that moved 0.05%, the tightest baseline of any population.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes: `flip-whole-square` (`l` 1798281, `sInner` 1341), `flip-last-c32` (`l` 165888, `sInner` 3), `flip-last-rows` (`l` 1800000, `sInner` 96).
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.60* | *52* | *1.02x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.15* | *77* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| libunord-stage2 | 0.000 | 0.000 | 0.01 | 69 | 0.00x |
| lib-stage2-lean | 0.028 | 0.048 | 0.55 | 59 | 1.00x |
| lib-stage2-disp | 0.028 | 0.049 | 0.45 | 59 | 1.00x |
| lib-stage1 | 0.034 | 0.042 | 0.47 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.034 | 0.043 | 0.45 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.035 | 0.048 | 0.42 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.037 | 0.052 | 0.48 | 58 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.039 | 0.057 | 0.28 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.050* | *0.058* | *0.36* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.050** | 0.058 | 0.10 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.058* | *0.15* | *59* | *1.00x* |
| liblist-stage2 | 0.053 | 0.082 | 0.74 | 51 | 2.00x |
| liblist-stage1 | 0.057 | 0.075 | 0.78 | 52 | 2.00x |
| libunord-stage1 | 0.057 | 0.074 | 0.80 | 52 | 2.00x |
| *bq-expand-aa-adjacent* | *0.112* | *0.130* | *0.66* | *46* | *1.02x* |
| bq-expand | 0.112 | 0.131 | 0.66 | 46 | 1.02x |
| *bq-expand-aa-distant* | *0.112* | *0.130* | *0.20* | *46* | *1.02x* |
| list (baseline) | 1.000 | 1.000 | 1.37 | 18 | 19.12x |
| *list-aa-distant* | *1.002* | *1.011* | *0.94* | *18* | *19.12x* |
| *list-aa-adjacent* | *1.012* | *1.022* | *0.64* | *18* | *19.12x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0116, worst cell 2.17% on `flip-last-rows`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.03% on `flip-last-rows`, its interval covering 1. The in-situ term reads 1.0326, 1.0749 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0112, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h6m20s, peak 188 MiB in use, 62 MiB max residency; the reader reads 24 benchmarks over 3 shapes of the flip class. Anchor: `flip-last-rows`, `list` at 30.5 ms per call raw, 29.4 ms net.

**Per shape, in the lead's order (flip-whole-square, flip-last-c32, flip-last-rows):** `mut-odo-vecdims` 0.027/0.058/0.054

**Across the halves:** 14 of the 19 voting arms are faster on this half and 5 slower, at a geomean of 0.9906, from `libunord-stage1` at 0.9346 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0419, `libunord-stage2` sitting out as degenerate, a basis cell of theirs not left positive by the correction, with `list` itself at 1.0059.

**What the class says:** it kills the prediction it was built for, and it is the only new class that changed a belief. **A reversed innermost axis costs the regime-3 fill about twice the forward run at the same length**: `flip-last-rows` and `runs-96` are the same `l` of 1800000 at the same `sInner` of 96, one reversed and one not, and per call the basis reads `mut-odo-vecdims` at 2.120 of its forward cell, `mut-odo-vecdims-add-in-leaf-u2` at 1.683, `lib-stage2-lean` at 1.903 and `lib-stage2-disp` at 1.907, against a `list` that pays 1.091 --- with 2.073, 1.851, 1.845 and 1.846 on HEAD. Registration 4's `flip` clause asked that the fills read a reversed run at its forward speed and is **KILLED by every fill on both halves**. **The one arm that is faster reversed is the shipped route**, `lib-stage1` at 0.859 and 0.939, which does not take the slice route there. Property 2 breaks to `libunord-stage2` at 0.000 and is NOT priced, that arm being its own forcing term on `flip-whole-square`; properties 1 and 3 hold, `worst` 0.058 and the tiers at 1.00x, 1.02x and 19.12x, `bq-expand`'s the narrowest of the ten. Across the halves 14 of the 19 voting arms are faster on the basis at 0.9906, `libunord-stage2` sitting out as degenerate, on a `list` that moved 0.59%.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.10* | *103* | *1.03x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *128* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *121* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *121* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.025 | 0.030 | 0.10 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.025 | 0.030 | 0.10 | 111 | 1.00x |
| lib-stage2-disp | 0.025 | 0.030 | 0.09 | 110 | 1.00x |
| lib-stage2-lean | 0.025 | 0.030 | 0.08 | 110 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.026 | 0.032 | 0.09 | 110 | 1.00x |
| **mut-odo-vecdims** | **0.027** | 0.035 | 0.09 | 110 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.035* | *0.08* | *110* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.027* | *0.035* | *0.16* | *110* | *1.00x* |
| mut-odo-vecdims-add-in-leaf | 0.029 | 0.034 | 0.08 | 109 | 1.00x |
| libunord-stage2 | 0.040 | 0.041 | 0.16 | 106 | 1.31x |
| liblist-stage2 | 0.040 | 0.041 | 0.16 | 107 | 1.31x |
| libunord-stage1 | 0.056 | 0.065 | 0.15 | 103 | 1.42x |
| lib-stage1 | 0.056 | 0.064 | 0.13 | 103 | 1.42x |
| liblist-stage1 | 0.056 | 0.064 | 0.12 | 103 | 1.42x |
| bq-expand | 0.094 | 0.095 | 0.11 | 96 | 1.03x |
| *bq-expand-aa-adjacent* | *0.094* | *0.096* | *0.11* | *96* | *1.03x* |
| *bq-expand-aa-distant* | *0.095* | *0.096* | *0.07* | *96* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.30 | 56 | 19.19x |
| *list-aa-distant* | *1.000* | *1.013* | *0.19* | *56* | *19.19x* |
| *list-aa-adjacent* | *1.003* | *1.004* | *0.20* | *56* | *19.19x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0060, worst cell 1.15% on `block-run64-page`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 0.9995 on a worst cell of 0.35% on `block-r3-vol64`, its interval covering 1. The in-situ term reads 1.0212, 1.0072 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0043, which the correction amplifies by 1.41x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m25s, peak 135 MiB in use, 26 MiB max residency; the reader reads 24 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.27 ms per call raw, 4.11 ms net.

**Per shape, in the lead's order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.023/0.029/0.035/0.029/0.023

**Across the halves:** 4 of the 20 arms are faster on this half and 16 slower, at a geomean of 1.0073, from `libunord-stage2` at 0.9949 to `mut-odo-vecdims-aa` at 1.0200, with `list` itself at 1.0088. **The baseline moved 0.88% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** it is the one population of the ten whose leader is a `mut-odo-vecdims` sibling, so the first clause of property 2 does not break here. `mut-odo-vecdims-add-in-leaf-u2-down` reads 0.025 against `mut-odo-vecdims`'s 0.027, a **9.74%** margin on a 0.60% floor at 5 of 5 shapes --- the family's own leaf ahead of its root, which is claim 10's ordering on a class rather than a break. **What the gap sweep says is that the stage-two route's advantage SHRINKS as the gap grows**, which is registration 4's `block` kill condition: standing in for the parked `lib-stage2`, `lib-stage2-lean` reads 0.3946 of `lib-stage1` at a gap of one element, 0.4757 at 64 and 0.5428 at a page on the basis, and 0.3859, 0.4706 and 0.5402 on HEAD --- monotone on both halves, where the registration predicted the margin widening. It is ahead by most on the rank-3 block, 0.3449 and 0.3365, which the registration also predicted. **And the offset clause holds**: from `block-run64-gap64` to `block-run64-off7` the shipped route moves 1.0198 and 1.0454 where the stage-two route moves 0.9981 and 1.0291, so an eight-element misalignment costs stage one and not stage two. Properties 1 and 3 hold, `worst` 0.035 --- the lowest of the ten --- and the tiers at 1.00x, 1.03x and 19.19x. **Its cross-half line is the second this run does not read**: `list` moved 0.88%, past the bar.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64).
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.21* | *224* | *1.27x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.41* | *240* | *1.23x* |
| *sum-only-early* | *--* | *--* | *0.02* | *249* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.02* | *249* | *0.01x* |
| lib-stage2-lean | 0.053 | 0.100 | 0.23 | 230 | 1.34x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.055 | 0.063 | 0.35 | 230 | 1.23x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.055 | 0.063 | 0.27 | 230 | 1.23x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.057 | 0.066 | 0.25 | 230 | 1.23x |
| mut-odo-vecdims-add-in-leaf | 0.059 | 0.067 | 0.27 | 229 | 1.23x |
| lib-stage2-disp | 0.060 | 0.130 | 0.28 | 226 | 1.49x |
| *mut-odo-vecdims-aa* | *0.060* | *0.082* | *0.26* | *230* | *1.23x* |
| *mut-odo-vecdims-aa-distant* | *0.060* | *0.082* | *0.34* | *230* | *1.23x* |
| **mut-odo-vecdims** | **0.061** | 0.082 | 0.35 | 230 | 1.23x |
| liblist-stage2 | 0.084 | 0.158 | 0.34 | 222 | 2.13x |
| lib-stage1 | 0.087 | 0.117 | 0.19 | 224 | 1.97x |
| libunord-stage2 | 0.088 | 0.155 | 0.23 | 223 | 1.98x |
| liblist-stage1 | 0.099 | 0.128 | 0.25 | 222 | 2.49x |
| libunord-stage1 | 0.103 | 0.161 | 0.27 | 218 | 2.47x |
| *bq-expand-aa-adjacent* | *0.126* | *0.140* | *0.28* | *219* | *1.27x* |
| bq-expand | 0.126 | 0.139 | 0.18 | 219 | 1.27x |
| *bq-expand-aa-distant* | *0.126* | *0.139* | *0.26* | *219* | *1.27x* |
| *list-aa-adjacent* | *0.999* | *1.003* | *0.18* | *181* | *19.50x* |
| list (baseline) | 1.000 | 1.000 | 0.21 | 181 | 19.50x |
| *list-aa-distant* | *1.002* | *1.004* | *0.28* | *181* | *19.50x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9973, worst cell 0.59% on `small-patch-k5`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 0.9997 on a worst cell of 0.08% on `small-bcast32`, its interval covering 1. The in-situ term reads 0.9665, 0.9752 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9983, which the correction amplifies by 1.61x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m23s, peak 91 MiB in use, 28 MiB max residency; the reader reads 24 benchmarks over 4 shapes of the small class. Anchor: `small-row96`, `list` at 6.18 us per call raw, 5.95 us net.

**Per shape, in the lead's order (small-row96, small-patch-k5, small-bcast32, small-flat64):** `mut-odo-vecdims` 0.046/0.082/0.056/0.064

**Across the halves:** 17 of the 20 arms are faster on this half and 3 slower, at a geomean of 0.9793, from `libunord-stage1` at 0.9485 to `list-aa-distant` at 1.0066, with `list` itself at 1.0051.

**What the class says:** the class built to expose a per-call cost exposes one, and it is in the allocation column rather than in the time. **Every fill here allocates 1.23x the result vector and `bq-expand` 1.27x**, where every other population reads the fills at 1.00x: at 150 to 384 elements the constant that dividing by `8*l` hides at a million is a fifth of the call, so property 3's fill tier is broken by a size and not by any arm, which is what this class exists to say. The lean dispatch leads it, `lib-stage2-lean` at 0.053 against `mut-odo-vecdims`'s 0.061, a **19.91%** margin on a 0.27% floor --- the tightest floor of the ten, this being the fastest population and the one whose cells resolve best. Its two clauses of registration 4 are unreadable, both arms they turn on having been parked. Property 1 holds, `worst` 0.082 --- the second highest of the ten --- and the tier ORDERING holds, 1.23x under 1.27x under 19.50x. **Across the halves it is the widest of the ten**: the basis is faster on 17 of 20 arms at a geomean of 0.9793, from `libunord-stage1` at 0.9485 to `list-aa-distant` at 1.0066, on a `list` that moved 0.51%, so the line is read and what it says is that this is where the two compilers differ most.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.34* | *86* | *1.21x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.28* | *112* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *104* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *104* | *0.00x* |
| lib-stage2-disp | 0.018 | 0.019 | 0.28 | 97 | 1.00x |
| lib-stage2-lean | 0.018 | 0.019 | 0.28 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.020 | 0.021 | 0.31 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.021 | 0.26 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.020 | 0.021 | 0.32 | 96 | 1.00x |
| lib-stage1 | 0.020 | 0.021 | 0.29 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf | 0.022 | 0.023 | 0.25 | 96 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.033* | *0.27* | *94* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.033* | *0.25* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.033 | 0.20 | 94 | 1.00x |
| libunord-stage2 | 0.038 | 0.047 | 0.86 | 91 | 2.00x |
| liblist-stage2 | 0.038 | 0.047 | 0.49 | 91 | 2.00x |
| liblist-stage1 | 0.038 | 0.050 | 0.56 | 90 | 2.00x |
| libunord-stage1 | 0.039 | 0.049 | 0.59 | 90 | 2.00x |
| *bq-expand-aa-adjacent* | *0.094* | *0.096* | *0.36* | *80* | *1.21x* |
| bq-expand | 0.094 | 0.096 | 0.37 | 80 | 1.21x |
| *bq-expand-aa-distant* | *0.094* | *0.096* | *0.26* | *80* | *1.21x* |
| list (baseline) | 1.000 | 1.000 | 0.74 | 46 | 19.87x |
| *list-aa-distant* | *1.002* | *1.007* | *0.73* | *46* | *19.87x* |
| *list-aa-adjacent* | *1.003* | *1.007* | *0.75* | *46* | *19.87x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9959, worst cell 0.59% on `compose-scalar`, and 2 of 6 intervals cover 1. The `sum-only` halves agree at 1.0012 on a worst cell of 0.43% on `compose-slice-bcast`, its interval covering 1. The in-situ term reads 1.0163, 1.0137 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9983, which the correction amplifies by 2.38x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m23s, peak 122 MiB in use, 33 MiB max residency; the reader reads 24 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 28.5 ms per call raw, 27.4 ms net.

**Per shape, in the lead's order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.033/0.033/0.025/0.021

**Across the halves:** 13 of the 20 arms are faster on this half and 7 slower, at a geomean of 0.9985, from `liblist-stage1` at 0.9613 to `mut-odo-vecdims` at 1.0236, with `list` itself at 1.0058.

**What the class says:** compositions cost what their parts cost, and the one clause of registration 4 that is refuted is a superlative rather than a mechanism. The re-cut dispatch leads it, `lib-stage2-disp` at 0.018 against `mut-odo-vecdims`'s 0.027, a **34.11%** margin on a 0.41% floor. **Each composition reads within about a point of its single-mechanism twin once the view's own term is taken out**: `compose-rev-bcast` over `bcast-inner8` runs 1.0064 to 1.0295 across the fill family on the basis and 1.0025 to 1.0110 on HEAD, and `list` itself reads 1.0155 and 1.0096 between the same two views, so relative to the baseline every fill is within 1.4% and most within 1% --- reversing a broadcast, or slicing one to an offset, costs the fill nothing the view does not cost everything. `compose-zero-mid` reads 0.87 to 1.14 of `bcast-inner900` and 0.63 to 0.71 of `bcastmid-block150k`, so a second zero stride prices as a broadcast and not as a block copy, as predicted. **What is refuted is that `compose-scalar` is the fastest cell of any population**: ranked by net time per element over every view of every population, excluding the two `libunord` arms whose cells are the forcing pass, it is sixth at 0.2778 ns, behind `bcastmid-primes` 0.2029, `small-flat64` 0.2168, `bcastmid-c32-cnn` 0.2276, `compose-zero-mid` 0.2470 and `bcast-inner900` 0.2760 --- the fourth of those a `compose` view itself, so the class holds two of the six fastest cells here and not the fastest. Properties 1 and 3 hold, `worst` 0.033 and the tiers at 1.00x, 1.21x and 19.87x. Across the halves the basis is faster on 13 of 20 at 0.9985, on a `list` that moved 0.58%.

## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 25's halves differ in the compiler and in the boot libraries that come with it, and in nothing else.** One source, `Main.hs` at `f04c5dc`, one shim, `align-as.py` at `b3a1aca`, set the same way on both halves, and one shim environment --- `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of `-pgma`, with `LOOP_NOOVERLAP` unset --- and one pair of `--ghc-options`, `-fspec-constr -fobject-determinism` with `-fforce-recomp`, into a fresh `--builddir` apiece. The basis took ghc-9.12.4, the default on PATH here; the other half took the in-tree stage1 of the GHC checkout through `cabal.project.ghead` and its own freeze at the same index-state, which reports `10.1.20260803` --- **Run 24's compiler unmoved**, so this run's HEAD half and Run 24's are the same compiler and their absolutes are comparable in the way the basis halves' are. The binaries carry `ghc-internal-9.1204.0` against `ghc-internal-10.100.0`, both bake `-A32m -I0 -T -M8G` read back by `+RTS --info`, both carry exactly one `@@wild` and one `@@saturate` string, and both report criterion 1.6.5.0. The shim moved from Run 24's `38bb3bb` by two commits whose whole diff to `align-as.py` is nine inserted and three deleted lines, every one inside a docstring, so nothing it emits moved.

**The sequence was launched once and ran to the end in one window**, 2026-09-05T01:15:34 to 2026-09-05T05:55:57, twenty-two processes, every one exiting 0 at the count its roster holds --- 432 twice, 336 twice, 144 twice, 120 twice, 96 six times and 72 eight times --- each carrying its `@@saturate` line and none reporting a selection it did not ask for. The two main-set processes read 0h37m23s and 0h37m29s at 177 and 181 MiB in use and 64 and 65 MiB max residency; the twenty class processes span 0h6m16s to 0h29m24s, the two `runs` processes taking the whole top of that range. The HEAD half ran first throughout, which is the driver's order. **The gate preceded it**, four processes from 00:43:51 to 01:15:31 at 90 benches each, on a box reading 0.4% non-idle, and the alarm between the gate and the sequence read 0.1%. **The riders followed it**, 84 single-bench processes over four invocations of 21 --- 18 main-set shapes and the three anchors on a second reps pass --- each half clean and saturated, from 05:55:57 to 06:08:01 and every invocation exiting 0. **The counted work followed them on a box that no longer had to be quiet**, twenty-two sweeps from 06:08:34 to 06:41:45, both halves over all eleven populations at 50 iterations a cell, no cell refused by perf anywhere. The whole evening ran as one command apiece, `run-evening.sh` and `run-counts-all.sh`, with the session asleep between them.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run25-g912`, md5 `6c898ba25314415dec76fdb8ec2c426c`, and `run25-ghead`, md5 `7f2ae9b95db61f9b5adc27e72903e5a6`, with `.text` of 20570309 and 20711231 bytes --- the HEAD half 140922 bytes larger, which is Run 24's difference between the same two recipes to the byte. **Neither reproduces an earlier binary and neither was meant to**: `Main.hs` moved between Run 24 and this run by the five roster commits of 2026-09-04 --- `ef0fd83` and `94a1dcc` retiring three classes and eight main-set shapes from timing, `41d3bad` the prune, `84aba1f` the `-u1` arm and `f04c5dc` parking the short bodies --- so no md5 here matches one on record and the repetition reading Runs 19 and 23 could take is not available. Their `--list` outputs are identical at 432 and their `classes --list` at 1176 over 49 views, and their `check` logs are byte-identical, which two compilers did not have to be, a float-printing difference being the one thing such a difference could innocently have been. **The regime was read back out of the basis binary before the hours were spent**: `diag` puts `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512`, `run24-g912`'s two figures to the byte, where plain -O1 separates the two tenfold. The tree at launch was `86f2041` with 39 untracked scratch paths and nothing modified, and nothing landed on the branch during the evening.

**The roster and the source moved, which is what would put a layout term in the cross-run column, and the fills say how little.** The eight tracked 28-byte loops were read at the build against `run24-g912`, the previous build of this recipe: **every mod-64 offset is preserved in both groups**, no address survives to the byte, and the eight heads move by two constants, the first head of each group by `0x3D00` and the other six by `0x4CC0`, both whole multiples of 64. The basis holds its six-copy group at offsets [0, 0, 24, 0, 4, 24] and its two-copy group at [0, 0]; post-run step 0's `-g3` twin names all eight, in order `fbMidCopy`, `fbMutOdoVecdimsAddIn`, `fbMutOdoVecdims`, `fbMutOdoVecdimsAddOut`, `fbMutOdoVecdimsAddBoth` and `fbCanonVecdims`, then `fbBuild` and `fbMutOdo` --- Run 24's eight names in Run 24's order. `run25-ghead` holds a six-copy group at [18, 31, 7, 9, 26, 7], a three-copy group at [0, 0, 21] and a two-copy group at [0, 0], and against `run24-ghead` reads the same shape: every mod-64 offset preserved, no address to the byte, two constants. **Within the pair no tracked address is shared and the group structure itself differs**, HEAD carrying a third group the basis has not, which is the compiler. `--library` reads 13.9% same offset in line and 66.0% same straddle state, inside the band [the halves differed by more than Main's alignment](../README.md#what-moves-a-figure-when-no-strategy-changed) gives a pair that varies a shim or a compiler. **And the cross-run column came out tighter than any before it**, fifteen of sixteen non-degenerate arms within a point of `run24-g912`: the arms whose figure used to be where their bytes landed --- `build`, `mut-odo`, `gen-unsafe` and their six twins --- were all parked on 2026-09-04, so the layout term this paragraph exists to price has no arm left to show it.

**The three main-set anchors** read **5.93 us** on `cnn-slice-c32`, **3.41 ms** on `cnn-L2-24x24-c32` and **37.7 ms** on `stretch-wide-2xM`, net of the forcing pass, on the basis half, against **5.91 us**, **3.43 ms** and **37.4 ms** on the HEAD half --- the middle one being `cnn-L2-24x24-c32` since `cifar-L2-16-c64-k3` was retired from timing on 2026-09-04, which is a change of anchor and not of box. Against Run 24's own three the two ends read -0.67% and +0.27% and the new middle -1.45% against Run 24's row for that shape, all three inside the 3% registration 2 allows, so the per-shape record is continuous across the shape-set change.

| shape | `l` | `list`, per call | net | HEAD, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.1 us | 5.93 us | 5.91 us |
| `cnn-L2-24x24-c32` | 165888 | 3.51 ms | 3.41 ms | 3.43 ms |
| `stretch-wide-2xM` | 1800000 | 38.8 ms | 37.7 ms | 37.4 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at 38.7 ms raw and 37.6 ms net, **Run 24's two figures to the printed digit** on a class whose population did not move and whose own floor is 2.79%, across a roster change and two builds; the four classes that landed 2026-09-03 are anchored here for the first time and have nothing to be read against yet. Read a class anchor against the same class's anchor in an earlier run and against nothing else, and against that class's own floor rather than the main set's.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0002** on the basis and **0.9999** on the HEAD half, both within a hundredth of a point and on worst cells of 0.39% and 0.49%. The in-situ term the `-nosum` arms measure reads 1.0199 and 1.0344 of it on the basis and 1.0242 and 1.0375 on the other, on `mut-odo-vecdims` and `bq-expand`, the two arms whose `-nosum` twins the prune left standing --- and pinned to these eighteen shapes Run 24's four read 1.0204, 1.0464, 1.0228 and 1.0321, so the correction sits where it has sat since Run 17 on both halves and on two runs of this roster's lineage.

[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-04, on the pair [the next-run section](#what-the-next-run-compares-against) settles --- Run 24's, ghc-9.12.4 against GHC HEAD, both under `LOOP_DEADSPOT=1`, decided 2026-09-04 by whoever asked for the run --- over the roster as the retirements and the prune of the same day leave it: ten classes and eighteen main-set shapes, 24 timed arms and 432 benches, the three classes, eight shapes and eighteen arms retired kept in `check` and timed nowhere ([the classes section](../README.md#the-stride-classes-and-what-they-cover), [the shape set](../README.md#the-shape-set)). Each with a prediction and a kill condition, and the verdicts move to Run 25's file with them. (1) *The box.* The gate's machine check and the run's main-set process read `list`'s net against the fingerprint Run 24 kept inside 3% over the eighteen shared shapes, none past 5%, so absolutes cross from Run 24; killed by either reading past 3%, which re-baselines every absolute and reads nothing else. (2) *The anchors.* `cnn-L2-24x24-c32`, the middle anchor since `cifar-L2-16-c64-k3` was retired, reads its `list` net within that same 3% of Run 24's fingerprint row for it, 3.46 ms in `runs/run24.md`, so its anchor history starts continuous with the per-shape record; killed by a move past 3% on that shape alone with the other two anchors inside it. (3) *The pair.* `list` moves under 0.7% between the halves, as it did on Run 24, so the two columns may be differenced, `predict: cross list 1.0 within 0.7%`; and no ordering of Run 24's registration (1) to (3) reverses past either half's floor; killed by a reversal past the floor on both halves. (4) *The additions of 2026-09-03.* Task 10's, read there: the four classes, each with its own prediction and kill condition, none repeated here; the two window views landed with them and their item there was withdrawn on 2026-09-04 with the arm it was about, so this run times them for the other arms' sanity alone. (5) *The retirement.* Nothing published moves for it: every class that stays reads its Run 24 ordering, and the rows the retired shapes and classes leave in Run 24's fingerprint are read nowhere; killed by an ordering on a kept population that Run 24 read the other way past both floors, which would be the population's doing and not an arm's. (6) *The prune.* Nothing published moves for it either: the three crossed pairs read inside Run 24's floor on both halves with their spans shortened, and the floor is read over six pairs from here on; killed by a crossed pair reading past Run 24's floor on both halves, which would be the roster's doing, a slot having moved under a control. (7) *The unroll, priced apart from the bound.* `mut-odo-vecdims-add-in-leaf-u1`, the shipped fill's un-unrolled form, is timed with `mut-odo-vecdims-add-in-leaf` re-timed as its bound control for this run alone: the probe of 2026-08-24 read the merged bound as a wash against the counted leaf, 0.9967 at 5 of 9, on a scratch build with no shim, and this pair reads it under the dead-spot shim on both compilers. Predicted: `-u1` between `-add-in-leaf` and `-u2` on both halves, `-u2` ahead of it past the floor, so the shipped fill's margin is the unrolling's and the wash stands; killed by `-u1` at or ahead of `-u2` past the floor on both halves, which would put the whole margin on the bound and the epilogue in the library for nothing, or by `-u1` behind `-add-in-leaf` past the floor, which would name the probe's wash a placement accident in the other direction. Before the run, `loop-offsets.py` on a `-g3` twin and the counts read say whether the body is the six instructions the probe read and whether the shim pads its head; a body that is not six is codegen's answer, and the run then reads nothing the dump did not. **Read the same day on the dead-spot `-g3` twin of this source**: the rank-1 copy of `-u1`'s fill is the six instructions the probe read, load, store, stride add, cursor increment, compare and branch, its head at a cache-line start behind a dead-spot pad; the run-level copy, the one every shape of rank 2 or more executes, is seven, reloading the source base from the stack once per element, a spill of the kind [the ceiling](../README.md#the-mutable-ceiling-taken) records for `-u2`'s run-level copy before that arm's own fix. So the run reads a body carrying one reload per element against a shipped fill carrying none, and a `-u1` lead would be in spite of it; a wash or a loss says nothing about the merged bound alone until that reload is taken out, which is the follow-up if the run reads one.

**Five held, one was killed by the one span a mode can read, and the seventh is four class predictions of which one was killed, two split and one could not be read at all --- and the reason four of the seven lost a clause is one reason, which is this run's largest finding about its own apparatus.** Every figure below is re-derived from this run's own JSONs and counts files rather than carried from the registration; margins are judged against the population's own floor on the half they are read on --- 0.40% and 0.61% on the main set and the class figures in the summary table above --- because that is the threshold the registrations state for themselves. **The one reason**: the prune and the parking of 2026-09-04 landed the day after the additions of 2026-09-03 were registered and the same day the pair's own registration was written, and between them they parked `lib-stage2` and `canon-vecdims`. Four of the clauses below are stated against one of those two, so they were unreadable before the machine was started and nothing checked it --- `--lint` refuses a live CLAIM that names a parked arm and reads no registration at all, which is the entry Run 24 left open and which this run fires four more times.

(1) *The box.* **HELD.** `list`'s net against the fingerprint Run 24 kept reads **-0.08%** on the gate's own process, **+0.32%** on the run's basis main set and **-0.77%** on the HEAD one, over 18 of 18 shapes each, worst `stretch-square-1341` -1.70%, `stretch-coprime-r7` +1.99% and `stretch-square-1341` -2.82%; none past 3%, let alone 5%. So absolutes cross from Run 24 and nothing here is re-baselined.

(2) *The anchors.* **HELD.** `cnn-L2-24x24-c32`, the middle anchor since `cifar-L2-16-c64-k3` was retired, reads its `list` net at **3.41 ms** against Run 24's fingerprint row of 3.46 ms, **-1.45%**, well inside the 3% the registration allows; the other two anchors read 5.93 us against 5.97 us and 37.7 ms against 37.6 ms, **-0.67%** and **+0.27%**. So the per-shape record is continuous across the shape-set change on all three.

(3) *The pair.* **KILLED on its span, and its second clause could not be read.** `list` moved **1.0110** between the halves over all eighteen shapes where the registration allowed 0.7%, so the two columns are ordered and not differenced --- the first `-fspec-constr` pair refused since Run 22 --- and `--predictions` reads that span and kills it without a judgement. **The counted work says what the move is not**: `list`'s count ratio is 0.9967, so 9.12 executes fewer instructions on the baseline and is a percent slower at it. **The second clause is unreadable**: it asks that no ordering of Run 24's registrations (1) to (3) reverse, and all three of those are stated against `lib-stage2`, which the prune parked. What can be read in their place is `lib-stage2-lean` against `lib-stage1`, 0.9696 on the basis and 0.9515 on HEAD, and against `mut-odo-vecdims-add-in-leaf-u2`, 0.9889 and 0.9719 --- the lean route ahead of both on both halves, which is the direction Run 24's (2) had.

(4) *The additions of 2026-09-03.* **Task 10's four, read here: one KILLED, two SPLIT, one UNREADABLE.** **`flip` is KILLED, and decisively.** It predicted that the fills read a reversed run at its forward speed; `flip-last-rows` and `runs-96` are the same `l` of 1800000 at the same `sInner` of 96, one reversed and one not, and every direct fill pays for the reversal: `mut-odo-vecdims` **2.120** of its forward cell on the basis and 2.073 on HEAD, `mut-odo-vecdims-add-in-leaf-u2` 1.683 and 1.851, `lib-stage2-lean` **1.903** and 1.845, `lib-stage2-disp` 1.907 and 1.846, against a `list` that pays 1.091 and 1.076. **The only arm faster on the reversed run is `lib-stage1`**, at 0.859 and 0.939, which is the shipped route not taking the slice route there. So a reversed innermost axis costs the regime-3 fill about twice the forward run at the same length, which is exactly the reverse copy the class was built to price, and the second clause --- `lib-stage2` against `lib-stage1` a tie on all three views --- is one of the four the prune made unreadable; its live stand-in `lib-stage2-lean` reads 0.5736, 1.1366 and 0.9177 of `lib-stage1` on the three views, and no tie. **`block` is SPLIT.** `lib-stage2-lean` standing for the parked `lib-stage2` is ahead of `lib-stage1` at every gap on both halves, 0.3946, 0.4757 and 0.5428 at one element, 64 elements and a page on the basis and 0.3859, 0.4706 and 0.5402 on HEAD, and ahead by most on the rank-3 block, 0.3449 and 0.3365 --- but **the margin SHRINKS as the gap grows, which is the registration's own kill condition**, and it shrinks on both halves and monotonically. The offset clause holds: from `gap64` to `off7` the shipped route moves 1.0198 and 1.0454 where the stage-two route moves 0.9981 and 1.0291, so `lib-stage1` is the one the offset costs. **`small` is UNREADABLE**, both arms its two clauses turn on --- `lib-stage2` and `canon-vecdims` --- having been parked. What the class does say is that `lib-stage2-lean` leads it at 0.8009 of `mut-odo-vecdims` and that **every fill allocates 1.23x the result vector there**, the per-call constant showing at 256 elements exactly as the class was built to make it. **`compose` is SPLIT.** Each composition does read within a floor of its single-mechanism twin once the view's own term is taken out: `compose-rev-bcast` over `bcast-inner8` reads 1.0064 to 1.0295 across the fill family on the basis and 1.0025 to 1.0110 on HEAD, and `list` itself reads 1.0155 and 1.0096 between the same two views, so relative to the baseline every fill is within 1.4% and most within 1%; `compose-zero-mid` reads 0.87 to 1.14 of `bcast-inner900` and 0.63 to 0.71 of `bcastmid-block150k`, so it prices as a broadcast and not as a block copy, as predicted. **What is refuted is the third clause**: `compose-scalar` is not the fastest cell of any population. Ranked by net time per element over every view of every population, excluding the two `libunord` arms whose cells are the forcing pass, it is **sixth** at 0.2778 ns --- behind `bcastmid-primes` 0.2029, `small-flat64` 0.2168, `bcastmid-c32-cnn` 0.2276, `compose-zero-mid` 0.2470 and `bcast-inner900` 0.2760, the fourth of those being a `compose` view itself.

(5) *The retirement.* **HELD.** Nothing published moved for it. On the main set, Run 24's basis column read over these eighteen shapes gives `mut-odo-vecdims` 0.049, `lib-stage1` 0.028, `mut-odo-vecdims-add-in-leaf-u2` 0.028, `lib-stage2-lean` 0.027, `liblist-stage2` 0.048 and `bq-expand` 0.108 --- this run's column to the digit on every arm the two share. On the five kept classes whose population did not move, every arm the two runs share is within 2.8% cross-run and each class's leader is Run 24's; the widest is `scaled`'s `mut-odo-vecdims` at 1.0198, whose two A/A twins moved with it at 1.0143 and 1.0189, which is that class's standing slot and not an ordering. No kept population reads an ordering the other way past both floors.

(6) *The prune.* **HELD.** The three crossed pairs read inside Run 24's floor on both halves: `bq-expand-aa-distant` 1.0040 and 1.0061, `mut-odo-vecdims-aa-distant` 1.0004 and 0.9990, `list-aa-distant` 1.0012 and 1.0022, against Run 24's 1.26% and 2.11%. **And their spans shortened as predicted**, 42, 8 and 7 slots on Run 24 against **16, 1 and 3** here, the prune having deleted the arms that stood between each twin and its base. The floor is read over six pairs from here on, and over the four of those six that carry back to Run 10 it is the same 0.40% and 0.61%.

(7) *The unroll, priced apart from the bound.* **HELD on both halves, and the counted work says why.** `mut-odo-vecdims-add-in-leaf-u1` sits between its two neighbours exactly as registered: **0.9360** of `mut-odo-vecdims-add-in-leaf` at 17 of 18, sign p 0.00014, and **1.0367** of `-u2` at 2 of 18, p 0.0013, on the basis; **0.9407** at 17 of 18 and **1.0479** at 3 of 18 on HEAD. All four readings are past both halves' floors of 0.40% and 0.61%, so `-u2` is ahead of `-u1` past the floor and the shipped fill's margin is the unrolling's --- and the merged bound is not the wash the probe of 2026-08-24 read on a shimless scratch build, being worth six and a half percent here. **The counts locate the rest of it**: corrected against the shared `sum-only` term, `-u1` executes 0.8456 of `-add-in-leaf`'s instructions on the basis and 0.8444 on HEAD, and 1.0892 and 1.0900 of `-u2`'s, the two compilers agreeing to a thousandth. So the merged bound removes fifteen percent of the work for six percent of the time, and the un-unrolled body executes nine percent more instructions than the unrolled one for four percent of the time --- which is the per-element reload the `-g3` twin read before the run, priced, and not a verdict on unrolling alone. **What the run does not separate**, and what a follow-up would: the unrolling removes the loop overhead AND that reload together, so a build that takes only the reload out is the measurement that would say which of the two the four percent is.
