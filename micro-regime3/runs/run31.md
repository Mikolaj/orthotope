# Run 31 (-O2 against plain -O1)

One run's write-up: its head, its Results, what the next run compares against, the properties that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 31 (-O2 against plain -O1): the level is worth three tenths to the reference and to `bq-expand` alike, and it leaves the shipped fill alone, which is the finding.** The pair is Run 30's basis recipe with and without `-O2` --- one source, one compiler, one shim, one roster and one bench order, the plain -O1 half publishing as Run 30's did, so `the basis` below is the half at the level `Data/Array/Internal.hs` actually compiles under and every `cross` figure reads basis over control --- so every figure that moves between the halves moved because one optimisation level was raised on one command line. Over the sixteen main-set arms this run times that carry a cross-half figure --- not the same sixteen as the shared set the cross-run reading below uses, though the two coincide --- NINE sit within 1% of 1 and the seven outside are two families entire and one arm alone: `list` with its two A/A copies at **1.2974**, 1.2921 and 1.2887, `bq-expand` with its two at **1.2943**, 1.2937 and 1.2967, and `mut-odo-vecdims-add-in-leaf-u1` at **0.9745**, 2.55 points the BASIS's way. **Nothing else moves past 3%**: `--movers` at that bar names the six and no other, so the leaf's un-unrolled twin is the one arm that moves more than a percent without moving three, and the only arm outside 1% that moves the BASIS's way, the other six outside being the two families entire. **What that says against Run 30 is this run's own finding.** `-fliberate-case` alone was worth 17.10% to `list`, 1.27% to `bq-expand` and cost the shipped leaf 1.09%; the whole level is worth **29.74%** and **29.43%** to the two and costs the leaf **0.44%**, with `lib-stage1` at 0.9972 and `lib-stage2-lean` at 0.9997 beside it. So registration (1) HOLDS where Run 30's kill fell on it, and registration (2) is killed the other way about, `list` moving 12.74 points past the 1.17 that item carried over. **The level is not its two passes added up**: on `bq-expand` it is worth twenty-three times what LiberateCase was, and on the fill family the LEVEL's widest of the three arms is 0.44 of a point, where `-fspec-constr`'s was 0.55 and `-fliberate-case`'s 1.09 --- though on the shipped leaf read alone it is 0.44 against SpecConstr's 0.01.

**The roster is 31 timed arms over 19 main-set shapes and 589 benches, with 58 class views over ten classes for 1798 more, and it is NOT Run 30's.** `./roster-delta.py run30-nospec run31-nospec` reads 36 arms to 31 over 19 shapes to 19: SIX out and ONE in, the 30 survivors in the same order and no shape moved. Out, parked `Only` at `c753fff`, are `libunord-stage2-sum`, `-stage3-sum`, `-stage5-sum`, `libunord-stage6-list-sum`, `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr`; in, landing at `13cbd0d`, is `libunord-stage10-list-sum`, base's `sum` over stage ten's list. NO CLASS VIEW MOVES: `runs` stays at fourteen, `window` at eight, `bcast` and `flip` at six, `block` and `small` at five, `bcastmid` and `compose` at four, `rev` and `scaled` at three. **And a third commit moved the source between those two, under unmoved names**: `a22677b` bangs the stage-ten walker's vector and the offset its empty carry ignores, which reaches the `Route` stages' lists and their consumers wherever a view routes to runs. So this pair's roster term is ABSENT WITHIN the pair, both halves being one source at `13cbd0d`, and PRESENT ACROSS runs: a figure that moves between this basis and `run30-nospec` carries a source term as well as the box's, and the two are not separable from inside this run. The L1 roster pass was therefore OWED, step 12's condition asking for a membership change where Run 30 had none; all eleven legs were taken and every one came back clean.

**NOT ONE CELL SINKS BELOW THE SHARED FORCING PASS ON EITHER HALF**, as on Runs 28, 29 and 30 and for the same two reasons: the ruling of 2026-09-10 that a reducing consumer has no corrected time, so the eleven `-sum` rows read `--` in `time` and `worst` by design, and the retirement of every Fill arm over a list to `check`. `read-all.sh --brief-facts` reports an empty sunk list for both halves, so every one of the sixteen rows that carries a corrected time at all is a geomean over all nineteen shapes. The L1 roster pass had said so a day early, and on ELEVEN populations rather than Run 30's six: the reader's own warning string, `forcing term is not smaller`, is absent from all eleven of its legs and from the smoke sweep beside them.

**The gate read SOUND, the machine check did NOT fire, and the box has not moved.** The gate's soundness is read as Runs 29's and 30's was, on the two palindrome passes AGREEING rather than on any of them sitting near 1: `mut-odo-vecdims` 0.9975 against 1.0026, `list` 1.2937 against 1.3014, `bq-expand` 1.3116 against 1.3017, the narrowest apart being `mut-odo-vecdims` at 0.51% of itself and the widest `bq-expand` at 0.76% --- tighter than Run 30's three, which parted by 1.13% to 3.13%, and tighter than any figure Run 29 recorded. The two passes settle the sign without a crossing: `list` and `bq-expand` each read 0 of 19 shapes faster on the basis in both, while `mut-odo-vecdims` crosses at 12 of 19 and 6 of 19, which is a tie at 1 seen through five benches. **The MACHINE CHECK then did not fire**, reading `list`'s net at a geomean of **-0.23%** against the fingerprint `runs/run30.md` keeps, worst `stretch-r5-8x432` at -1.44% and 0 of 19 shapes past 5%. That is a LIKE-FOR-LIKE reading where Run 30's was not: Run 30's published half is plain -O1 dead-spot 9.12.4, which is this basis's recipe exactly, so the fingerprint and the half reading against it are one recipe and the check measures the box alone. **And the cross-run reading agrees and adds the term the check cannot see.** Against `run30-nospec`, the previous build of this very recipe, the sixteen shared timed arms span **0.9909 to 1.0179** --- every one but `lib-stage2-lean-u1` inside 1% --- with `list` at **0.9959**. That distance is not the box's alone, `Main.hs` having moved three times on 2026-09-13 under an unchanged recipe; what it says is that neither the box nor those three commits moved any timed arm of the main set by more than 1.8%.

**Every one of the twenty-two processes gated clean --- `--selftest` and both `--aa` gates --- and the PLATEAU GATE FIRED on this pair's own variable, as Runs 29's and 30's did.** It reports the preamble's victim spanning 16.7228 to 21.5002 ms/iter across the run, a **28.57%** spread against a 5% band. The same three readings say what that is. WITHIN each half the plateau is flat --- `nospec` 21.0584 to 21.5002, **2.10%**, and `o2` 16.7228 to 16.9534, **1.38%** --- and the split is exactly by half, eleven processes each. The victim is `vgg-14-c512-k3/list` on all twenty-two, the one bench this pair's variable moves most, and the two halves' medians stand 1.2592 apart --- near `list`'s own cross-half figures without being either of them, the main set reading 1.2974 and the raw ratio on that very shape 1.2733. So the gate bands a TIME across two halves whose variable changes that time, which is the third pair here able to do that to it; the run is sound and the reading is per half. **What is new is the saturation STATE, which parts by half where Run 30's did not**: `keep=8.19844333056e12` on all twenty-two, but `inuse=95420416` on every `nospec` process and `inuse=74448896` on every `o2` one, one distinct value per half and none within one. Run 30 read a single pair on all twenty-two, so this is `-O2` reaching what the spray leaves resident and not only what it costs to run. TWO PROCESSES carry an A/A worst cell above 5%, both on the control half --- `read-all.sh`'s column being one figure a process: `o2-main` at **17.06%** on `cnn-slice-c32`, where the shipped leaf's two copies read 17.06% and 15.29%, and `o2-runs` at **5.87%** on `runs-512`. The basis half has none above 5% in any of its eleven. **What sets the control's floor at 1.58% is the DISTANT copy's cell alone**, the 15.29%; dropping that pair leaves the adjacent one at 1.30%, and it takes dropping both to leave `bq-expand-aa-distant`'s 0.43% standing. They are WILD cells and not an intrusion --- `--wild` clears all one hundred and fourteen of this run's logs, the worst bench anywhere reaching 0.24 of a core.

**The sequence ran in ONE window and NOT ONE intrusion was found**, as on Runs 28 and 29 and where Run 30's own write-up session intruded on two benches of its control half. Twenty-two processes from 2026-09-14T01:20:50+02:00 to 08:14:07+02:00, every one exiting 0 at its roster's count --- 589 twice, 434 twice, 248 twice, and 186, 155, 124 and 93 four times each, 4774 benches in all --- with no hole between one process finishing and the next starting, no complaint, and no `!!` line anywhere in the wall-clock record. The control half ran first throughout. `--wild` clears all twenty-two, and the eighty-eight alone-leg logs and the gate's four besides: no bench of the hundred and fourteen reaches 0.25 of a core --- the worst is 0.24, on `cnn-L1-24x24-c1/list` in a gate process --- so nothing else was running on this machine while any of it was measured, the driver included. **What that leaves is wild cells, which are a different thing and this run has two**, both on the control half's main process and both on `cnn-slice-c32`: the shipped leaf's two A/A copies at 17.06% and 15.29%, with the RTS totals flat and no foreign CPU beside them. No ITEM's verdict in this file turns on either, and what they cost is one figure: the control half's floor. The DISTANT copy's cell sets it at 1.58%, the adjacent one would leave 1.30%, and only dropping both leaves the 0.43% `bq-expand-aa-distant` carries.

**What the level is worth is the same shape on every population, and the fill family is inside its band on all but one of thirty readings.** `list` moves by about three tenths everywhere, 23.72% at its narrowest --- on `bcastmid` --- and then, per class, 27.49% on `rev`, 29.04% on `window`, 29.90% on `small`, 31.94% on `runs`, 32.57% on `scaled`, 33.60% on `flip`, 34.08% on `block`, 35.32% on `compose` and 38.35% on `bcast`, its widest, with the main set at 29.74%. **The three shipped-fill arms registration (1) names hold inside the registered 4% on every class but `small`**, where `lib-stage2-lean` reads 1.0494; ten of their thirty class readings sit outside 1%, scattered over eight populations and reading both ways --- `lib-stage1` on `rev` (0.9865), `runs` (0.9894), `block` (1.0235), `small` (1.0238) and `compose` (0.9853); `lib-stage2-lean` on `flip` (0.9899) and `small` (1.0494); the leaf on `bcastmid` (1.0126), `window` (1.0305) and `small` (1.0153). That is the same count Run 30 read under `-fliberate-case` alone, and the difference is where it falls: the item is adjudicated on the MAIN SET, and there this run's three read 0.9956, 0.9972 and 0.9997 where Run 30's read 0.9891, 1.0003 and 1.0093 and lost. So the whole level reaches the shipped fill no further than one of its passes did, and on the population the item is read on it reaches it less.

**The counted work covers every population, no cell was refused anywhere, and its finding is that `-O2` thins EVERY arm and pays off on two.** Twenty-two sweeps from 08:26:56 to 09:04:43, `runs` longest at 400s and 330s and `small` shortest at 13s and 8s for the pair apiece, `rev` level with it on the basis at 13s and 10s on the control, with no `perf could not count` line in any of the eleven pairs of files --- and every one of the twenty-two came in below its Run 30 counterpart, which is what the note's derived scale asked of a roster that lost five timed arms net. Over the sixteen main-set arms that carry a corrected time the counts geomean is **1.1601**. **Split per arm it is a partition, and not Run 30's.** NOT ONE arm reads 1.0000: the ten fill-family arms run **1.0380 to 1.0530**, `list` with its two A/A copies **1.3118 to 1.3120**, and `bq-expand` with its two **1.4422 to 1.4423**. **What separates the three groups is the conversion.** the level spares `bq-expand` 30.7% of its instructions and 22.7% of its time, and `list` 23.8% and 22.9%, `time/counts` reading **0.8974** and **0.9889**; the ten fill arms are spared three and a half to five percent and convert NONE of it, `time/counts` running 0.9287 to 0.9620 while nine of their clocks sit within a point of level --- and the tenth, `mut-odo-vecdims-add-in-leaf-u1`, moves the WRONG WAY: 4.7% fewer instructions cost the -O2 half 2.6% of its time on that arm. **That is Run 29's shape and not Run 30's.** `-fspec-constr` took about a twentieth of the instructions off every arm and only two converted any of it; `-fliberate-case` touched two arms alone and left ten bit-identical. The level does what the first of its passes did, which is what a level containing it should do --- and the fill family's five percent going nowhere is the same non-conversion Run 29 recorded, read again under the whole level and with `bq-expand`'s conversion far better than either pass alone managed.

**This run's two columns may NOT be differenced, and that is true of every one of the eleven populations.** With `list` having moved **29.74 points** on this run's main set, and from 23.72 on `bcastmid` to 38.35 on `bcast` across the ten classes, `read-all.sh --brief-facts` marks all eleven PAST the 0.7% bar and each class block says so in its own cross-half line. Every arm-by-arm figure across the halves in this file is therefore an ORDERING and not a subtraction, which is what Runs 29 and 30 also had to say, under one -O2 pass apiece, and what this run says under the level that contains both. What survives the bar untouched is everything read WITHIN one half --- every `pair` span and every class property --- and those carry the run.

**The fastest timed arm on the main set is `lib-stage2-lean`, and this run it leads on the column and paired alike, which Run 30's did not.** `lib-stage2-lean` reads **0.025** in the published `time` column, alone at the head, with the shipped leaf and its two A/A copies behind it at 0.027, `mut-odo-vecdims-add-in-leaf-u1` at 0.028 and `lib-stage1` and `lib-stage2-lean-u1` at 0.029, against `mut-odo-vecdims`'s 0.045 and `bq-expand`'s 0.128. Paired, the same ordering holds: over `lib-stage1` it is **0.9348** at 16 of 19 and p 0.0044, over its own unrolling twin **0.9374** at 15 of 19 and p 0.019, and over the shipped leaf **0.9863** at 13 of 19 and p 0.17, which is a tie rather than a lead. What made Run 30's column and pair part was `mut-odo-vecdims-add-in-leaf-u2-ptr`, ahead of the lean fill by better than five points on the statistic a margin should be judged by while sitting behind it on the column; **that arm and `mut-odo-vecdims-add-in-leaf-u1-ptr` beside it were parked on 2026-09-13**, so the divergence is not available to this run and the head of the table is not contested by anything the roster still times.

**Stage ten composes its two changes where both fire and costs a pass where neither does, as on Run 30 --- but on `window` the two changes now CANCEL on both halves, and that kills registration (5).** Over stage seven it reads **0.5497** on the basis and 0.5491 on the control on `bcast` and **0.5715** and 0.5686 on `bcastmid`, against 0.59 and 0.62 registered, with `compose` at 0.6870 and 0.6851 and no verdict, as registered; over stage nine on `window`, where no view carries a zero stride, it reads **0.9557** on the basis, inside the registered 25% of 0.73, and **0.9847** on the control, 25.47 points off and outside it. **The kill fires on the second condition.** Three `window` views put stage ten BEHIND stage nine past the floor on BOTH halves --- `window-128x128-k7` at **1.3666** and **1.3689**, `window-64x64-k1x9` at 1.2355 and 1.2359, `window-224x224-k3` at 1.0785 and 1.1985, against floors of 0.58% and 0.23% --- where Runs 29 and 30 found the first two parting in SIGN and the item survived on that. So the two reorderings do not compose on those views, and what the numbers say is sharper than that. No `window` view carries a zero stride, so stage nine keeps stage six's order exactly and the pair against stage seven prices the TIE-BREAK alone --- and on `window-128x128-k7` the tie-break COSTS 27%, `libunord-stage9-sum` over `libunord-stage7-sum` reading **0.7321** on the basis and 0.7322 on the control. Stage ten carries that tie-break, so it inherits the loss entire and reads **1.0006** over stage seven on that view, and 1.0023 on the control: the item's second kill condition catches the same fact from the composed side. No zero-stride view has stage ten behind stage seven past the floor on both halves, the closest being `compose-scalar` at 1.0002 and 1.0004 against floors of 0.32% and 1.10%, so the first condition does not fire. **Where neither change fires the pass still costs and registration (6) holds**: `rev` reads **1.0244** on the basis and 1.0241 on the control --- Run 30 read 1.0244 and 1.0245 --- with `scaled` at 1.0046 and 1.0024, `runs` at 1.0014 and 0.9991 and `block` at 1.0018 and 1.0021, all inside the 3% that item was amended to. `small` prints 1.1910 and 1.1768 and no verdict, and its per-view clause lands exactly: behind on `small-flat64`, `small-patch-k5`, `small-patch-r5` and `small-row96` and ahead on `small-bcast32` at 0.7294 and 0.7099.

**What `-fliberate-case` was worth to the fused list, the bangs of 2026-09-13 have taken back, and two items read that from opposite sides.** Read raw over every shape, as an arm with no corrected time must be, `libunord-stage7-sum` across the halves is **1.0243** on `runs`, **1.0142** on `window` and **1.0004** on `block` --- so the whole level, which turns that pass on among others, is worth 2.43, 1.42 and 0.04 points to the consumer where Run 30 measured the pass ALONE worth 18.27, 10.22 and 6.33 on the same three populations and the same recipe. On the five classes where the route fills or is one block it is level, 0.9976 to 1.0022. Registration (3) quoted Run 30's figures as its prediction and missed on `runs` by 15.57 points; neither of its kills fires, nothing sitting inside 1% of level on `runs` and `block` both and nothing past 1.3. **Item (15) reads the same change from the other side, on the arm that landed for it.** `libunord-stage10-list-sum` over `libunord-stage10-sum` --- base's `sum` over stage ten's list against the harness's own fold through `sumLazyRuns`, one route met two ways --- reads **1.0002** on the main set, **1.0132** on `runs` and **1.0154** on `window` on the basis, where Run 30's stage-six pair read 0.9999, **0.8495** and **0.7610** on this very recipe, the user's own fold fifteen and twenty-four points AHEAD of the shared loop. That lead is gone on every population, and the item's own watch-clause --- *the reading to watch is `runs` and `window`, where it staying ahead would say the bangs did not reach the shared loop* --- says what that means. The kill wants the pair outside its band on both halves on some named population, and only `bcast` has one half outside, the control at 1.0347 against the basis's 1.0070.

**The three class properties hold in all ten classes on both halves --- and property 1 BREAKS on the main set, on both halves, which is new.** `mut-odo-vecdims`'s `worst` stays under 1 everywhere: 0.113 on the main set and between 0.028 and 0.087 in the classes, lowest on `scaled` and `compose` and highest on `small`. It is ahead of `bq-expand` on every shape of every class, closest `flip-last-rows` at 0.4844, then `small-patch-k5`'s 0.4811, with `bcast-tall-Mx2`'s 0.4050 and `runs-2`'s 0.4039 behind them --- but on the MAIN SET it is BEHIND on `stretch-pow2stride`, at **1.0009** on the basis and **1.0109** on the control. Run 30 read 0.9997 on that shape, which its own head called a win by three ten-thousandths inside a 0.57% floor; a run later the same cell reads a loss by nine, and under `-O2` a loss by more than a point. So the clause property 1 has carried since Run 8 is broken by the one shape that was already nearly a tie, and it breaks on BOTH halves rather than on the candidate one. Both allocation clauses hold with the 1% margin, closest at `scaled-rank1-m1`'s 1.00003 against `bq-expand` and `stretch-tall-Mx2`'s 1.00000 on the main set. The tier ordering is unbroken in all ten on both halves: the fills at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 20.99x to 27.66x --- and on the control half at 1.00x to 2.75x and 19.00x to 26.55x, which is registration (10)'s kill showing in the class blocks. **And on `small` the word `fills` will not carry the plural**: the `mut-odo-vecdims` family reads 1.27x on each half, the per-call constants showing through a call under a microsecond, but `lib-stage2-lean` reads 1.40x there and `lib-stage1` **2.32x**, which is ABOVE `bq-expand`'s 1.44x --- so on that class property 3's tier reading is the `mut-odo-vecdims` family's alone, which is what its own block says and what `--block` computes. `lib-stage1` sits above `bq-expand` on `runs` and `block` too, at 1.19x against 1.13x and 1.42x against 1.06x. The ORDER the property asserts is over the three arms the reader names, and it is unbroken in all ten.

**Across the halves every population's geomean puts the basis slower, not one of the eleven may be read for the pair's variable, and dropping two families takes every one of them to level.** The basis is SLOWER on 16 of 16 arms in `small`, 15 in `bcast` and `window`, 14 in `bcastmid` and `flip`, 12 in `rev`, 11 in `compose` and 9 in `scaled`, `runs`, `block` and the main set, at geomeans from **1.0686** on `scaled` to **1.1446** on `window`, the main set at 1.1004. But each population's `list` moved 23.72 to 38.35 points between the halves, past the 0.7% that lets two columns be differenced, so every one of those lines is disqualified as a reading of the level and stands only as an ordering. What the vote does show is WHOSE the movement is, and it shows it as cleanly as Run 30's did: drop the `list` and `bq-expand` families --- six arms, the two leads and their four A/A copies --- and the same eleven geomeans run **0.9969** on `rev` to **1.0269** on `small`, with the main set at **0.9986**, ten arms apiece and four of the eleven below 1. The whole seven-to-fourteen percent gap goes with those six arms, and what is left is a level that reaches nothing else on this roster.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The two `sum-only` halves agree to within a seventh of a percent on every population, from 0.9986 to 1.0003 on the basis, `bcastmid` to `small`, and 0.9996 to 1.0003 on the control, `window` to `bcastmid` and `compose` together, so the term subtracted from one half is the term subtracted from the other and no ratio here is an artefact of a moved forcing pass. That matters more on this pair than on most: the two halves' absolutes part by three tenths on the reference, and a correction that had parted with them would have put that three tenths into every corrected column.

**The two halves do NOT resolve alike, the basis resolves better, and the two instruments that say so disagree about by how much.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **0.95** over the 31 arms, 8 of them wider on the basis and 23 narrower, where Run 30's pair read 0.92 and split 14 against 22 and Run 29's 0.97 and 16 against 20. Read it beside the A/A floor and not off it: this column is sampling error INSIDE one bench where the floor is agreement BETWEEN two placements of one arm, and Run 18 moved the two in opposite directions. Here the floors put the basis ahead by a factor of two and a half, 0.61% against 1.58%, where `CI%` puts it ahead by a twentieth --- and that gap is one pair's, the control's floor being set by the shipped leaf's wild `cnn-slice-c32` cell and nothing else. **No cell of either MAIN process sits below R2 0.99**, the worst being 0.9912 on the basis and 0.9913 on the control, both on a `-nosum` control row; three class cells of the basis half do, one apiece on `bcast`, `runs` and `window`, worst 0.9805 on `runs-7/libunord-stage6-loop-sum`.

**The straddling loops stand at EIGHT on each half, and the two halves' self-loop counts part by seventy-eight.** `--survey` reads 299 self-loops of at most 64 B on the basis, 125 of them at offset 0 and 8 still straddling, against 221 on the control, 79 at offset 0 and 8 straddling. The halves part by 78 loops where Run 30's parted by 4 and Run 29's by 62, and it is the -O2 half that carries the FEWER while its `.text` is the LARGER, by 12288 bytes --- 12 KiB exactly. That is Run 30's pairing again with a wider gap, and this file does not explain it either: `--survey` counts self-loops of at most 64 B, so both a loop removed and a loop grown past the cutoff leave it. Within the pair, `--library` reads **916** library self-loops in common, **4.4%** at the same offset in line and **58.7%** in the same straddle state --- the most movement of the three readings this chapter has taken on halves that share a compiler and differ in a compile setting, against Run 29's 39.4% of 914 for `-fspec-constr` and Run 30's 100.0% of 925 for `-fliberate-case`. There is no band for such a pair: README bands a pair that varies a COMPILER at a tenth to a quarter, and these halves share one, so the figure is recorded and not judged. The basis's 299 is 15 above `run30-nospec`'s 284, which is the source and not the level, that recipe being unchanged between the two runs. **Post-run step 0 names SIX of the basis's eight straddlers off the `-g3` twins by byte identity and FIVE of the control's** --- `fillStage2Short`, `fbMutOdoVecdimsAddInLeafU2` twice on the basis and once on the control, and its `Down`, `Last` and `Ptr` forms --- and refuses the rest, two on the basis and three on the control, at mod-64 offsets 53 and 42 where no twin holds a byte-identical copy and `--loose` offers only a signature family with more than one member. `fbMutOdoVecdimsAddInLeafU2Ptr` is among the named on BOTH halves though nothing times it, which is parking taking an arm off the roster and not out of the binary; its `-u1` twin is in both binaries too, its loop simply not among the eight that straddle. **And the count check refuses the basis's population comparison**, which is the case that check exists for: the basis's own `-g3` twin holds 297 self-loops where the timed binary holds 299, so every name off it rests on its own byte match alone; the control's twin holds 221, its timed binary's count exactly.

**The regime was confirmed in this run's own binaries before the hours were spent, and the two halves read OPPOSITE sides of it, which is where this pair differs from Runs 29's and 30's.** `diag` reads `baseOffsetsScan` against `baseOffsetsMut` on `vgg-14-c512` at 24066407 against 2408530 on the basis, **9.992 times apart** --- the ten times this README has attributed to plain -O1 since Run 8 --- and at 2408938 against 2408514 on the control, equal to three figures, which is SpecConstr ON. So the basis is plain -O1, the control is -O2, and `micro.cabal`'s own `-O1` was OVERRIDDEN rather than added to, which is the one thing this recipe had to get right and the one stop the pair note carried. Pre-run steps 9 and 9b are ONE reading on this pair for that reason: the pair's variable is exactly what `diag` can see, where `-fliberate-case` moves neither of those two rows and Run 30 had to stand its md5 pair in for 9b.

**Run 31 records every population twice** --- the main set and all ten stride classes from each half, one process apiece, twenty-two in all --- so every class figure below has a control-half twin and every one of the ten was gated on both. That is what lets a class say something the main set cannot, and on this pair it is what shows the level acting alike on all eleven: the reference moves by about three tenths everywhere, 23.72 points at its narrowest, while the ten arms outside the `list` and `bq-expand` families sit level on every population. It is also what makes property 1's break readable, the clause holding in all ten classes on both halves and failing on the main set on both.

**The decomposition reproduces on both halves, and its two columns part where the level does.** The riders time each shape's `list` alone, one bench to a process, clean and then saturated, and the state the preamble puts on a process comes back at a geomean of **1.1162** on the basis and **1.1651** on the control --- the basis's within half a point of Run 30's 1.1205 for this same recipe, and the control's the level reaching the alone leg as it reaches everything else `list` does. What the roster then adds on top of that state is **1.0187** on the basis, 5 of 19 shapes above 1, and **1.0032** on the control, 8 of 19; the basis's rest runs 0.9824 on `gather48-src-50` to 1.2310 on `stretch-r5-8x432`, the control's 0.9681 on `stretch-bigstride` to 1.1220 on `stretch-tall-Mx2`, so the wider of the two spreads is the plain -O1 half's this time where on Run 30 it was the flagged half's.

**There is NO repetition this run and none was available**, `Main.hs` having moved from `7685375` to `13cbd0d` in three commits on 2026-09-13, so neither half's inputs are an earlier binary's, no md5 here can reproduce one and an unequal one names nothing: the basis is `bbce438866c7a20652b96a60f4e1748d` and the control `0bb3b8cc28592fbe592195d71435d1ab`. What the two md5s DO say is that they differ from each other, which is the level having reached a pass, and `.text` says the same --- 20775109 bytes on the basis against 20787397 on the control, the -O2 half larger by 12288, with the basis 8192 above `run30-nospec`'s 20766917 and all three deltas exact multiples of 4096. **And the fills are the pinning claim in the form the README states it, not in the tighter form Run 30's identical source gave.** `--delta` against `run30-nospec`, the previous build of this recipe, taken at the build and before anything else changed, finds two tracked groups of 6 and 2 copies of a 28 B, 8-instruction loop with every mod-64 offset PRESERVED --- [0, 24, 8, 0, 4, 0] and [0, 0] --- and the same two displacements on each group, 0x300 and 0x1a00, and NO address surviving to the byte. A timed function landed this run and six left, so this is the claim read over a source that gained and lost functions, which is the form it is stated in; Run 30 read the other form, every address surviving and no displacement at all, and that was the repetition's rather than any arm's.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. **What this run leaves the next one is that a level is not its passes added up.** Runs 29 and 30 priced `-fspec-constr` and `-fliberate-case` one at a time on this roster; this run prices the level that turns both on, and in one orientation the three read `list` at 1.1379, 1.1710 and 1.2974 and `bq-expand` at 1.2804, 1.0127 and 1.2943 --- so on `bq-expand` the two passes multiply to 1.2967 against a measured 1.2943 and on `list` to 1.3325 against 1.2974. **And the lesson the last three runs agreed on has an exception.** Twelve of the fifteen items held; the three kills fell on (2), (5) and (10), and only the last predicted a no-op, where Run 28's four, Run 29's three and Run 30's two all did. So an argument that a change cannot reach a population is still the cheapest clause a registration can carry, and this run adds two that are as cheap: a magnitude carried forward from the last run, which is what killed (2), and an argument that two changes compose, which is what killed (5). **What it says about the library is one sentence.** `-O2` buys `list` and `bq-expand` about three tenths apiece --- the baseline and the class default `vFillStrided` still carries --- and leaves the mutable fill the branch ships where it is, so under a level `Data/Array/Internal.hs` does not set, the gap between the route it ships and the two it is read against NARROWS by that much. Where that shows worst is `lib-stage1` at `runs-2`, which goes from 12 points behind its shape's `list` on the plain -O1 half to 69 on the `-O2` one.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a PLAIN -O1 table**, as Run 30's was and for the reason Run 30 was moved onto it: it is the regime `Data/Array/Internal.hs` actually compiles under. **A row's distance from the last published column IS readable this run**, which it was not on Run 30: that column is Run 30's own published binary's and this basis is that recipe built again, so the comparable predecessor is the previous run's own published binary for once. **Against it the raw distance is almost nothing**: over the 16 arms that carry a corrected time and survive into this roster, this basis runs **0.9909 to 1.0179** --- every one but `lib-stage2-lean-u1` inside 1% --- with `list` itself at **0.9959**. It is NOT the tightest such reading this chapter has taken, and it could not be: `Main.hs` moved three times on 2026-09-13 under an unchanged recipe, so that span carries a source term where Run 30's 0.9937-to-1.0069 carried none. **The `alloc` column is a median over this run's own nineteen shapes**, `bq-expand` at 2.78x and `list` at 25.20x, so it is a statistic of a strategy and a shape set together and does not cross to a run that timed a different set.

**And it is the basis half's**, `run31-nospec`, as every published table here is from Run 11 on: the control half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-one rows. That the published half is the UNFLAGGED one is Run 30's decision inherited rather than this run's to make --- the shipped file sets no optimisation level by hand and compiles at -O1, so the recipe that matches what the library actually compiles under is the plain -O1 one, and `o2` is the candidate reading. **Thirty of the thirty-one rows are not first readings**: they have a twin in Run 29's file under `-fspec-constr`, in Run 30's under `-fliberate-case` and in Run 30's basis column under this very recipe. The thirty-first, `libunord-stage10-list-sum`, landed at `13cbd0d` and is read here for the first time.

**Comparing runs?** The table below is Run 31's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the properties to test are [the ones after it](#the-properties-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.61%** from 1 on the basis half or **1.58%** on the control, read over the eight pairs this roster carries, and over the four pairs that carry back to Run 10 at **0.61%** and **0.43%** --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **The two halves' floors are not alike and the difference is one pair's**: the basis's is `bq-expand-aa-distant`'s and equals its carry-back figure, as Run 30's did, while the control's is the shipped leaf's distant copy at 1.58%, carried there by a wild `cnn-slice-c32` cell that reads 15.29%. Beside those, the worst SINGLE A/A cells of the two MAIN-SET processes --- 3.24% on `alexnet-L1-55-c3-k11` on the basis and **17.06%** on `cnn-slice-c32` on the control --- are not floors at all and are not to be quoted as any; the control main's is this run's worst cell anywhere, ahead of `o2-runs`'s 5.87% on `runs-512`, and the basis half has no cell above 5% in any of its eleven processes. **And one thing this run does NOT have**: its two columns may not be differenced on any population, `list` having moved by at least 23.72% between the halves everywhere, so every cross-half figure here is an ordering.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What cost a row its population on Run 27 was the correction, not the estimator, and on THIS run nothing does**: that run had nine rows on each half carrying cells the shared forcing pass is not smaller than --- seventy such cells on its basis and forty-three on its HEAD half, all of them `libunord` arms --- and this roster has none, so every row here that carries a corrected time is a geomean over all nineteen shapes. What replaced those rows is the `no_net` ruling and the retirement of the Fill arms over a list, named in the head and in *What the next run compares against*, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what property 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.61* | *55* | *2.78x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.59 | 69 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.57 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.55 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage10-list-sum | -- | -- | 0.02 | 83 | 0.00x | the same, base's `sum` over stage ten's list -- the fold a library user brings, over the two reorderings composed |
| libunord-stage10-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's tie-break with stage nine's zero-stride axes moved outermost -- the two reorderings composed |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage9-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.36* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.03* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.02* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.025 | 0.114 | 0.60 | 70 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.113* | *0.54* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.114 | 0.47 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.113* | *0.57* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.111 | 0.51 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| lib-stage1 | 0.029 | 0.113 | 0.47 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| lib-stage2-lean-u1 | 0.029 | 0.111 | 0.60 | 67 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| *mut-odo-vecdims-aa* | *0.045* | *0.112* | *0.47* | *66* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-aa-distant* | *0.045* | *0.112* | *0.52* | *66* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.045** | 0.113 | 0.43 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| bq-expand | 0.128 | 0.252 | 0.63 | 50 | 2.78x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.128* | *0.252* | *0.68* | *50* | *2.78x* | *A/A control* |
| *bq-expand-aa-distant* | *0.128* | *0.252* | *0.38* | *50* | *2.78x* | *A/A control* |
| *list-aa-distant* | *0.998* | *1.005* | *0.73* | *21* | *25.20x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.74 | 21 | 25.20x | -- |
| *list-aa-adjacent* | *1.001* | *1.009* | *0.43* | *21* | *25.20x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and a pair still parts in SIGN between the two on this run. `mut-odo-vecdims-add-in-leaf-u2-aa-distant` against its base divides to **1.0019** on the published column, saying the distant copy is the slower, where the paired figure is **0.9981** at 12 of 19 wins, saying it is the faster; both sit inside this half's 0.61% floor, so nothing turns on it, and it is the cleanest demonstration this run has that the two are different statistics. **The widest disagreements are at the head of the table.** `lib-stage2-lean` against `lib-stage2-lean-u1` divides to **0.8528** on the column where the paired figure is **0.9374** at 15 of 19, eight and a half points apart; against `lib-stage1` it is **0.8636** against **0.9348**, seven apart; and against the shipped leaf **0.9292** against **0.9863**, close to six. That is why the head paragraph reads the table's leader off `--pair` rather than off the column. A margin between two arms is `--pair`'s paired figure, which is also the statistic the floor is defined in; `--pair` and `--predictions` print both side by side and say when they part. The column is for reading the table, not for differencing it.

`concat-runs` has no row, and neither do the other 84 arms the roster holds and checks without timing --- **85 of its 116** in all, where Run 30 checked 79 of 115: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). **Six arms were parked to `check` this run and one was added.** The six went on 2026-09-13 at `c753fff` --- `libunord-stage2-sum`, `-stage3-sum`, `-stage5-sum`, `libunord-stage6-list-sum`, and the two pointer leaves `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr` --- and `libunord-stage10-list-sum` landed at `13cbd0d`; the thirteen Fill arms over a list that went to `check` on 2026-09-09 stay where they are. **What parking the two pointer leaves takes out of this file is the ceiling Run 27 made readable**: they are the shipped fill's leaf and its unrolled form rewritten to walk a `Ptr`, refused for the library ([dead ideas][dead]) and timed as a ceiling and nothing else, and Runs 29 and 30 between them settled that neither -O2 pass reaches it. Nothing on this roster times that ceiling any more. So a movement against Run 30's basis column is a movement on the **16 shared arms that carry a corrected time**, with a roster term and a source term between the two runs where Run 30 had neither.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is `lib-stage2-lean` at 0.025**, with the shipped leaf and its two A/A copies at 0.027, `mut-odo-vecdims-add-in-leaf-u1` at 0.028 and `lib-stage1` and `lib-stage2-lean-u1` at 0.029 --- **five timed non-control arms below `mut-odo-vecdims`'s 0.045**, every one of them a fill that writes the result, where Runs 29 and 30 had seven and the two that are gone are the parked pointer leaves. **The column and the pair AGREE about which of them leads**, which is the second finding and the reverse of Run 30's: paired on the basis, `lib-stage2-lean` over `lib-stage1` is **0.9348** at 16 of 19 and p 0.0044, over its own unrolling twin **0.9374** at 15 of 19 and p 0.019, and over the shipped leaf **0.9863** at 13 of 19 and p 0.17, a tie rather than a lead --- and the arm that made the two disagree on Run 30, `mut-odo-vecdims-add-in-leaf-u2-ptr`, is off the roster, so the disagreement is not available rather than resolved. **The third is that the leaf fusion is unmoved by the level**: `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6417** on the basis and **0.6436** on the control, nineteen ten-thousandths apart, where Run 30 read 0.6446 and 0.6525 for one of the level's passes alone.

**The two standing placement controls are still gone with the prune, the straddlers stand at eight on each half, and this pair --- unlike Run 30's --- DOES move tracked fill copies.** Within the pair `./loop-offsets.py run31-o2 run31-nospec` puts the tracked six-copy group at **[0, 24, 8, 0, 4, 0]** on the basis and **[0, 0, 0, 24, 0, 4]** on the control, and the two-copy group at **[0, 0]** against **[0, 12]**, each half carrying 33 self-loops of 28 B in 26 distinct byte-sequences. So the level displaces fill copies where `-fliberate-case` displaced none, which is what `--library` says over the whole library too: 916 self-loops in common at **4.4%** the same offset in line, against Run 30's 100.0% of 925. Against the previous build of the basis's own recipe, which is Run 30's published binary, `--delta` reads every one of the basis's offsets PRESERVED --- [0, 24, 8, 0, 4, 0] and [0, 0] --- with no address surviving to the byte and one displacement per group, 0x300 and 0x1a00. So the two comparisons this run can make disagree, and they disagree in the direction the pinning claim predicts: raising the level moves fill copies, and rebuilding one recipe over a source that gained and lost functions does not move one of them modulo 64.


## What the next run compares against

**Run 32's pair is DECLARED, 2026-09-14, and it is not the one this run's result asks for.** It is this run's own basis recipe as the basis --- ghc-9.12.4 in the dead-spot form at plain `-O1`, `nospec` still naming what that half is --- against GHC HEAD at plain `-O1` and nothing else, `ghead`, built through `cabal.project.ghead` as Run 19's and Run 27's HEAD halves were, on `Main.hs` as it stands after 2026-09-14: the shared loop `sumLazyRuns` summing each run through `sumNoSpec`, without vector's `SPEC` argument, which gives the consumers -O2's counts at either level, and `liblist-stage4-list-sum` landed beside `libunord-stage10-list-sum`, so that a fold keeping vector's `sum` stands against the shared loop on the lean route as on the composed one in every run from here. What the pair varies is the compiler; what it re-reads is this run's fifteen registrations, carried to [README's open list](../README.md#what-is-open): nine copied with `ghead` written where they said `o2`, the four `cross` items re-aimed at a compiler pair on Run 27's readings, (5) re-registered at this run's reading, (15) edited for the loop change, and (16) added for the new arm. The pair this run's result asks for --- the basis as both halves, one with the two -O2 passes added by hand, `--ghc-options="-fspec-constr -fliberate-case -fobject-determinism"`, three flags and one variable --- is what the next paragraph's arithmetic wants and stays wanted; it is deferred behind Run 32, not refused.

**The REGIME variable has now been asked three times --- one pass, the other pass, and the whole level --- and on one of its two moving arms the passes compose almost exactly.** Five pairs before Run 29 varied the compiler: Run 24 read the two 5.7% and 6.8% apart on the instructions of two pure arms; Run 25 read `list` 1.10% apart in time and 0.33% in counts; Run 26 found twenty-four of twenty-six arms inside a percent in counts and the two `Ptr` arms outside at 0.6219 and 0.9295, which was GHC [#27778](https://gitlab.haskell.org/ghc/ghc/-/work_items/27778); Run 27 worked it around and read every arm inside a percent; Run 28 confirmed it at a counted geomean of 1.0027. **Then Run 29 varied `-fspec-constr`, Run 30 varied `-fliberate-case` and this run varies `-O2`, which turns both on among others.** Put in one orientation --- the unflagged half over the flagged, which is this run's and Run 30's and the inverse of Run 29's --- the three read `list` at **1.1379**, **1.1710** and **1.2974**, and `bq-expand` at **1.2804**, **1.0127** and **1.2943**. Run 29's two are RE-DERIVED in that orientation by `./read-run.py run29-nospec-main.json --compare run29-spec-main.json` and not inverted from the 0.8788 and 0.7810 its own file publishes, which is a different statistic's reciprocal and need not agree past three places; here it does. So on `bq-expand` the two passes MULTIPLY to 1.2967 against a measured 1.2943, 0.24 of a point apart, while on `list` they multiply to 1.3325 against a measured 1.2974 and overshoot by 2.7 points. **That arithmetic is an observation and not a subtraction**, three pairs on two rosters and one box era, and it is worth making only because `list` and `bq-expand` are arms no roster change here has touched. What it cannot settle is whether the level's remaining passes are worth anything at all on this roster, the two readings being consistent both with the passes being the whole of `-O2` and with something else giving `list` back what their product overshoots. The two-passes pair settles it in one evening, and Run 32, declared above, is not it.

**What Run 31 leaves the next run to read against, and the first item is a check that did NOT fire. The box is where Runs 28, 29 and 30 left it**, and this run's gate says so at first reading rather than needing an argument: the machine check reads `list`'s net at **-0.23%** against the fingerprint Run 30 installed, worst `stretch-r5-8x432` at -1.44% and 0 of 19 shapes past 5%. That is a LIKE-FOR-LIKE reading where Run 30's was not --- Run 30's published half is plain -O1 dead-spot 9.12.4 and this basis is that recipe built again --- so no argument stands between the check and the box. Against `run30-nospec` itself the sixteen shared timed arms span **0.9909 to 1.0179** with `list` at **0.9959**. So this run publishes INSIDE the third machine era rather than opening a fourth, an absolute crosses between it and Run 30's basis unadjusted, and the fingerprint below is the one a Run 32 machine check reads against. **What a Run 32 reading must not take from those sixteen arms is a box figure**: `Main.hs` moved three times on 2026-09-13 between the two builds, so their 1.8% is the box's and those commits' together and nothing here separates the two. Only a pair whose halves share a source does that, which is what every pair here is for.

**Registered with the pair.** Run 31's fifteen registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. Twelve held and three were killed. **What a next registration should take from this one is that the pattern the last three runs found has BROKEN.** Run 28's four kills, Run 29's three and Run 30's two all fell on items predicting a NO-OP --- something argued from the code to be unreachable, tested where the argument said nothing could move, and moving --- and not one fell on an item predicting an EFFECT. Two of this run's three do. (2) predicted `list` would move 17% and it moved **29.74%**; (5) predicted stage ten's two reorderings would compose and on three `window` views they CANCEL, stage nine's 27-point gain on `window-128x128-k7` being handed back entire by stage seven's tie-break. Only (10) is a no-op killed, having predicted that the allocation levels would reproduce under `-O2`. **So the lesson generalises rather than repeating**: a clause that cannot be reached by the evidence that would refute it is what fails, and a magnitude carried forward from the last run is such a clause as surely as an unreachability argument is. Item (2) quoted Run 30's own figure for the flag and was killed by the level being bigger; item (3) quoted the same figures and missed its `runs` span by 15.57 points for the opposite reason, the bangs of 2026-09-13 having removed what it predicted.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at a run's OWN area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 31 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 30 to Run 31 --- **and it is a step between PUBLISHED bases for once**, Run 30 having moved its published half onto this very recipe, where Run 29's step had to be read between control recipes. The box not having moved, that step is a SUBTRACTION and not a bridge. Over the **16 arms both rosters time and both give a corrected time** --- not all of them, six having been parked and one added --- the basis column runs from **0.9909** on `bq-expand` to **1.0179** on `lib-stage2-lean-u1`, fifteen of the sixteen inside 1% of 1 and `list` itself at **0.9959**. **But it is NOT a box reading**, and that is where this step differs from the one Run 30 recorded: `Main.hs` moved three times on 2026-09-13 under an unchanged recipe, so the span carries a source term, and `lib-stage2-lean-u1`'s 1.79% is the largest thing either the box or those three commits did to a timed arm. **The table below is this run's own two halves and no earlier run's**, seven strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in ONE OPTIMISATION LEVEL and in nothing else. **Its two columns may NOT be subtracted, and the second column is HIGHER for every row but one**: every cell is a ratio to that half's own `list`, and `-O2` speeds `list` up by about three tenths, so the `-O2` half divides by a smaller denominator and every arm's ratio rises with it. The exception is `bq-expand`, which prints 0.128 in both columns because it moved by almost exactly what the reference moved by --- 1.2943 against 1.2974 --- so the division cancels. Read the columns as two orderings, never as a difference.
| strategy | Run 31 (plain -O1, dead-spot, -A32m, 9.12.4) | Run 31 (`-O2`, dead-spot, -A32m, 9.12.4) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.045** | 0.058 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.028** | 0.036 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.027** | 0.033 |
| `lib-stage1` | **0.029** | 0.035 |
| `lib-stage2-lean` | **0.025** | 0.032 |
| `lib-stage2-lean-u1` | **0.029** | 0.033 |
| `bq-expand` | **0.128** | 0.128 |

**READ THE SECOND COLUMN AS A RATIO AND NOT AS A SPEED.** Every entry is that arm's net over `list`'s net in ITS OWN half, and `list` is 29.74 points faster at `-O2` --- so the control column reads higher for six of the seven rows because its denominator is faster, not because the arm is slower. **That is the DIRECTION and not an identity**: each entry is winsorized per row within its own half, so dividing an arm's two entries does not reproduce its `--compare` figure and is not meant to --- on `lib-stage1` the two entries divide to about 0.83 where the cross reads 0.9972, which is the denominator and the capping together and not a disagreement. The two columns may not be subtracted, and the arm-by-arm reading of what the level is worth is in the head, off `--compare`, where the reference is not divided out.

**A published geomean is over the same 19 shapes, and two halves of one run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered. **THE TABLE ABOVE IS NOT SUCH A PAIR**, `list` having moved **29.74 points** on this run's main set --- which is the same fact as `list` being 29.74% slower at plain -O1, read the other way round, the two appearing in this file four sections apart: the two columns may NOT be differenced. Six of the seven rows print higher on the control and the seventh prints level, and the cells are ratios to each half's own `list`, so the -O2 half's smaller denominator lifts every arm that did not move with the reference. Read down a column and the ordering is the same on both, head and foot alike: `lib-stage2-lean` leads each at 0.025 and 0.032, `mut-odo-vecdims` sits between at 0.045 and 0.058, and `bq-expand` is at the foot at 0.128 twice. So the level, unlike `-fliberate-case`, does not change which arm heads the column.

**The control half's own standings on the arms this run's roster carries, which no table here holds, every published table being the basis half's.** Read off the control half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9473** at 15 of 19 (0.9679 at 13 of 19) and against `mut-odo-vecdims` **0.6436** at 17 of 19 (0.6417 at 17 of 19); `lib-stage1` against `-u2` 1.0534 at 9 of 19 (1.0551 at 7 of 19); `lib-stage2-lean` against `-u2` **0.9823** at 15 of 19 (0.9863 at 13 of 19) and against `lib-stage1` **0.9325** at 16 of 19 (0.9348 at 16 of 19). **Every one of the five holds its direction across the halves**, and the widest the two readings part is two points, on the shipped leaf against its un-unrolled twin --- so the WITHIN-half orderings this run publishes are not the level's, which is the half of the run the 0.7% bar leaves standing.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section][pershape]. The two tables below are installed from this run's own JSONs in the per-shape form the README describes, which replaced a fourteen-column one on 2026-09-04.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 6.32 us | 0.077 | `lib-stage2-lean` 0.075 | `mut-odo-vecdims-add-in-leaf-u2` 0.055 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.61 us | 0.089 | `lib-stage2-lean` 0.069 | `mut-odo-vecdims-add-in-leaf-u2` 0.068 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 118 us | 0.064 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u2` 0.042 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 386 us | 0.044 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `gather48-src-50` | 3 | 22500 | 454 us | 0.049 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-coprime-r7` | 13 | 60060 | 1.13 ms | 0.029 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.71 ms | 0.051 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `stretch-primes` | 89 | 250357 | 4.31 ms | 0.026 | `lib-stage1` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 16.9 ms | 0.040 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `vgg-14-c512-k3` | 3 | 903168 | 19.8 ms | 0.053 | `lib-stage1` 0.031 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 20 ms | 0.030 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-inner256` | 256 | 1750784 | 45.1 ms | 0.023 | `lib-stage2-lean-u1` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `stretch-pow2stride` | 64 | 1769472 | 31.1 ms | 0.113 | `lib-stage2-lean-u1` 0.111 | `mut-odo-vecdims-add-in-leaf-u1` 0.111 |
| `stretch-r5-8x432` | 8 | 1769472 | 48.2 ms | 0.021 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `stretch-square-1341` | 1341 | 1798281 | 30.7 ms | 0.087 | `lib-stage2-lean` 0.077 | `mut-odo-vecdims-add-in-leaf-u2` 0.077 |
| `stretch-bigstride` | 3 | 1800000 | 50.9 ms | 0.033 | `lib-stage2-lean` 0.014 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 39.4 ms | 0.058 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1` 0.022 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 41 ms | 0.021 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims` 0.021 |
| `stretch-wide-2xM` | 2 | 1800000 | 39.3 ms | 0.057 | `lib-stage2-lean-u1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.021 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 941 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 29.1 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 29.4 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 29.2 ms | 0.019 | `lib-stage1` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 35.7 ms | 0.015 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 39.2 ms | 0.057 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.68 ms | 0.053 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 4.27 ms | 0.019 | `lib-stage2-lean-u1` 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 47.6 ms | 0.034 | `lib-stage1` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 42.1 ms | 0.022 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.23 ms | 0.019 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.27 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.26 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `block-run64-page` | `block` | 64 | 131072 | 2.35 ms | 0.029 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.47 ms | 0.020 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 950 us | 0.028 | `lib-stage2-lean` 0.014 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 940 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 29.4 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 29.8 ms | 0.022 | `lib-stage1` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.32 ms | 0.026 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.31 ms | 0.026 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.69 ms | 0.052 | `lib-stage2-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 29.7 ms | 0.024 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims` 0.024 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 29.9 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims` 0.024 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 32.3 ms | 0.049 | `lib-stage1` 0.039 | `mut-odo-vecdims-add-in-leaf-u2` 0.044 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 119 us | 0.063 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u2` 0.042 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 455 us | 0.048 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `rev-primes` | `rev` | 89 | 250357 | 4.33 ms | 0.025 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `runs-65536` | `runs` | 65536 | 1769472 | 28 ms | 0.024 | `lib-stage1` 0.022 | `mut-odo-vecdims` 0.024 |
| `runs-16384` | `runs` | 16384 | 1785856 | 28.7 ms | 0.024 | `lib-stage1` 0.022 | `mut-odo-vecdims` 0.024 |
| `runs-4096` | `runs` | 4096 | 1798144 | 28.9 ms | 0.024 | `lib-stage1` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-1024` | `runs` | 1024 | 1799168 | 28.9 ms | 0.024 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.024 |
| `runs-512` | `runs` | 512 | 1799680 | 28.8 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.025 |
| `runs-256` | `runs` | 256 | 1799936 | 28.9 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.025 |
| `runs-7` | `runs` | 7 | 1799994 | 32.8 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `runs-2` | `runs` | 2 | 1800000 | 39.5 ms | 0.058 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `runs-3` | `runs` | 3 | 1800000 | 35.9 ms | 0.047 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 34.2 ms | 0.041 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u1` 0.023 |
| `runs-5` | `runs` | 5 | 1800000 | 33.2 ms | 0.039 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-9` | `runs` | 9 | 1800000 | 31.9 ms | 0.030 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-96` | `runs` | 96 | 1800000 | 29.2 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.025 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 29.5 ms | 0.026 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `scaled-r5` | `scaled` | 13 | 15015 | 270 us | 0.028 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 1.04 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 5.15 ms | 0.028 | `lib-stage2-lean-u1` 0.030 | `mut-odo-vecdims` 0.028 |
| `small-patch-k5` | `small` | 5 | 150 | 2.94 us | 0.076 | `lib-stage2-lean` 0.095 | `mut-odo-vecdims-add-in-leaf-u2` 0.058 |
| `small-bcast32` | `small` | 32 | 256 | 4.45 us | 0.049 | `lib-stage2-lean` 0.063 | `mut-odo-vecdims-add-in-leaf-u2` 0.043 |
| `small-flat64` | `small` | 64 | 256 | 4.41 us | 0.057 | `lib-stage2-lean-u1` 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.055 |
| `small-patch-r5` | `small` | 4 | 256 | 5.33 us | 0.087 | `lib-stage2-lean-u1` 0.093 | `mut-odo-vecdims-add-in-leaf-u1` 0.066 |
| `small-row96` | `small` | 96 | 384 | 6.47 us | 0.041 | `lib-stage2-lean` 0.053 | `mut-odo-vecdims` 0.041 |
| `window-28x28-k5` | `window` | 5 | 14400 | 281 us | 0.040 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 959 us | 0.084 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.45 ms | 0.051 | `lib-stage1` 0.031 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 9.69 ms | 0.051 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.91 ms | 0.050 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 11.8 ms | 0.052 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 12.6 ms | 0.053 | `lib-stage2-lean` 0.031 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `window-128x128-k7` | `window` | 7 | 729316 | 13.8 ms | 0.031 | `lib-stage1` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |

**One row to read first, and it is a property of the shape set and not of any arm**: NONE of the thirty-one rows is a geomean over fewer shapes than the rest, where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, against **seventy on Run 27's basis and forty-three on its control** as that run's write-up recorded them --- counts predating the 2026-09-10 `no_net` ruling, which today's reader gives as 25 and 8 --- so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the eleven `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows --- `libunord-stage5-sum` on four, `-stage2-sum` on two, `-stage4-sum` on one --- this one names `lib-stage2-lean` on seven, `lib-stage1` on two and `lib-stage2-lean-u1` on one and nothing else, and the cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The properties the next run should test

**Each stride class carries the same three properties, now with Run 31's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** **The first held in every one of the eleven populations on both halves and the second did NOT**, which is the first break either clause has taken here: on the MAIN SET `mut-odo-vecdims` is behind `bq-expand` on `stretch-pow2stride`, at **1.0009** on the basis and **1.0109** on the control, while the clause holds on all ten classes on both halves. The `worst` clause holds in every regime, roster and layout the README has run, so `mut-odo-vecdims` --- and this is a statement about THAT arm and not about the route the library ships, which the paragraph below reads separately --- was never slower than the `list` it replaced, on any shape of any population. The `bq-expand` clause folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and read that way here for the fourth time; **it was already nearly a tie on that one shape**, Run 30 reading 0.9997 there and calling it a win by three ten-thousandths, so what a run changed is which side of 1 the same cell falls on rather than the size of anything. This is the property the classes exist to test, no geomean can state it, and this break is the one result here that bears on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims` its `worst` is 0.113 on the main set and between 0.028 and 0.087 in the classes, highest on `small` at 0.087 and then `window` at 0.084, lowest on `scaled` and `compose` at 0.028 apiece; the `bq-expand` clause is closest at `flip-last-rows`, 0.4844, among the classes that hold it, with `compose-rev-bcast`'s 0.2783 the widest of the ten per-class closest readings.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm now breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. Of the five library-shaped arms Run 27 found breaking it at `runs-2` --- `libunord-stage1` 1.237, `liblist-stage1` 1.214, `lib-stage1` 1.198, `liblist-stage2` 1.078 and `libunord-stage2` 1.076 --- four are retired to `check` and are not timed here. Those five figures are RECOMPUTED from Run 27's own cells, that run's prose and its own installed table having printed different ones, and the five are FIVE of NINE library-shaped arms over `list` there rather than the largest five, which is a ranking this file had wrong until 2026-09-14: sorted, that run's `runs-2` cells put `libunord-stage1` at 1.2368 first and then `liblist-stage4` 1.2311, `libunord-stage4` 1.2255, `libunord-stage5` 1.2252 and `liblist-stage3` 1.2191 above the other four named. A tenth row above `list` is `list-aa-adjacent`, its own A/A copy, at 1.0111. **What is left is `lib-stage1`, on two cells of the control half and one of the basis**: at `runs-2` it reads **1.1156** on the basis and **1.6936** on the control, and at `runs-3` 1.1333 on the control against a basis figure under 1. A sweep of every cell of all eleven populations on both halves --- 22 processes, every timed non-control arm against its own shape's `list` --- finds exactly those three cells above 1 and no other, the baseline's own A/A copies aside. **And the level is what widens it, as the flag did on Run 30, only further**: `-O2` makes `list` 31.94% faster on `runs` and leaves `lib-stage1` within a point of where it was, so the arm that has to beat `list` on the shortest runs meets a far harder baseline on the -O2 half --- 69 points behind at `runs-2` where the plain -O1 half is 12 behind, and Run 30's flagged half was 29. So the shipped route is slower than `list` on runs of two elements on both halves and of three on one, and on nothing else the library can produce, and how much slower is a question the optimisation level answers rather than the route.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves, and they are the only one of the three properties this run leaves entirely alone.** The `list` clause is closest at `small-patch-k5`, 0.05918, and under 0.048 everywhere else. The `bq-expand` clause is closest at `scaled-rank1-m1`, 1.00003, then `stretch-tall-Mx2` at 1.00000 and `bcast-src8` at 0.99999 --- the same three cells Run 30 read, inside the margin the strict form would have failed on, which is why the margin is there and why it is not widened further.

3. **The allocation tiers survive and their ORDER is unbroken in all ten classes and on the main set; what `small` is outside is the LEVEL clause and not the order one**, by the ruling of 2026-09-07, it being the class built to break it and read here for what it shows: the mutable fills at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 20.99x to 27.66x. On the main set the fills read 1.00x, `bq-expand` **2.78x** and `list` **25.20x**, which are a plain -O1 half's levels as Run 30's were. **But the LEVELS are not the same on the two halves this run, and that is registration (10) killed**: on the `-O2` half `bq-expand` reads **2.11x** and `list` **23.45x** on the main set, and 1.00x to 2.75x and 19.00x to 26.55x across the classes, so `-O2` changes what those two arms allocate and not only how fast they run. The fills and the `liblist` consumers read 1.00x and the `libunord` consumers 0.00x on both halves, unmoved. **What this roster removes is the 2.00x tier**, whose members were the list Fill arms now checked and not timed, and what it adds below the fills ON THE MAIN SET is the `libunord` consumers at 0.00x and the `liblist` consumers at 1.00x, the latter falling to a fill there. **Those two added levels are the main set's and no class's**: on `bcast` the `libunord` consumers spread from 0.00x to 1.00x and on `small` from 1.14x to 2.07x, the per-call constants of a short call swamping a consumer that allocates nothing per element. So `property 3 holds` in a class is the THREE published tiers holding --- the fills, `bq-expand`, `list` --- and never the five. `small` reads its fills at 1.27x on each half and `bq-expand` at **1.44x** on the basis and 1.34x on the control, the per-call constants showing through a call under a microsecond. That is the one exception the lead names, and it is a level and not an order: `small`'s order is unbroken like the rest.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 31 (-O2 against plain -O1, dead-spot, -A32m, ghc-9.12.4) records every class twice**, one process per class per half, so each block below has a control-half twin and the cross-half line under it is derived from both. **This run those lines say less than usual, and they say why.** `list` moved between the halves by 23.72 points on `bcastmid` at narrowest and 38.35 on `bcast` at widest, past the 0.7% that lets two columns be differenced, so every class's cross-half reading is disqualified as a reading of the pair's variable and stands only as an ordering --- as every one of Runs 29's and 30's was, for the same reason under one of this level's passes apiece. Over the ten classes the reader counts **160 arm-comparisons, 36 putting the basis faster and 124 slower**, with no degenerate arm excluded, at geomeans from 1.0686 on `scaled` to 1.1446 on `window` and extremes of `mut-odo-vecdims-add-in-leaf-u1` at 0.9444 on `scaled` and `bq-expand-aa-distant` at 1.5252 on `window`; `lib-stage2-lean` holds the low extreme in 3 of the 10. What each class still decides, and decides on both halves separately, is the three properties, its own floor, and whichever registrations name it. **Four items read EVERY class** --- (1), (2), (8) and (15) --- so each block below has those four whatever else it carries.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- and both are read over the POPULATION, so the arm named here may lead on no single shape and the per-shape fingerprint below may name another, which is [the README's per-shape section][pershape]'s own point and not a disagreement --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the properties](#the-properties-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on SEVEN of the ten --- `rev`, where `lib-stage1` leads, `bcastmid`, where `lib-stage2-lean-u1` does, and `bcast`, `runs`, `flip`, `block` and `small`, where `lib-stage2-lean` does --- and the family's own ceiling on `window`, `scaled` and `compose`, where the ceiling is faster. **The two pointer leaves held five of Run 30's six bolded ceilings and are parked**, so `runs`, `flip` and `block` now bold a lean fill where they bolded a pointer leaf, while `window` and `scaled` bold the family's next arm. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 3 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. Item 2's own table is NOT among what that call prints, which is why the list above starts where it does: it comes from the separate `--block --in-place` call item 2 itself names. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.043 | 0.063 | **`lib-stage1`** 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 | 0.39% |
| `bcast` | 6 | 0.021 | 0.057 | **`lib-stage2-lean`** 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 | 0.95% |
| `bcastmid` | 4 | 0.029 | 0.053 | **`lib-stage2-lean-u1`** 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 | 0.96% |
| `window` | 8 | 0.051 | 0.084 | `lib-stage2-lean` 0.028 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.027 | 0.58% |
| `scaled` | 3 | 0.027 | 0.028 | `lib-stage1` 0.022 | **`mut-odo-vecdims-add-in-leaf-u1`** 0.022 | 0.40% |
| `runs` | 14 | 0.027 | 0.058 | **`lib-stage2-lean`** 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 | 3.26% |
| `flip` | 6 | 0.028 | 0.052 | **`lib-stage2-lean`** 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.026 | 1.06% |
| `block` | 5 | 0.023 | 0.029 | **`lib-stage2-lean`** 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 | 0.44% |
| `small` | 5 | 0.060 | 0.087 | **`lib-stage2-lean`** 0.052 | `mut-odo-vecdims-add-in-leaf-u2` 0.052 | 0.78% |
| `compose` | 4 | 0.024 | 0.028 | `lib-stage2-lean` 0.015 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.015 | 0.32% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On SIX rows --- `window`, `scaled`, `runs`, `flip`, `block` and `compose` --- the bold sits in the CEILING column instead**, and it names three arms rather than one: `mut-odo-vecdims-add-in-leaf-u2-ptr` on `window`, `runs` and `block`, `-u1-ptr` on `scaled` and `flip`, and the shipped leaf `-u2` itself on `compose`. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship. **THREE rows tie at three decimals** --- `bcast` at 0.016, `flip` at 0.023 and `compose` at 0.015 --- and the bold on each is `--block`'s own, computed on the unrounded values where the printed ones cannot separate: `bcast` to the arm outside the family, `flip` and `compose` to the ceiling.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.21* | *126* | *3.22x* |
| liblist-stage1-sum | -- | -- | 0.08 | 147 | 1.01x |
| liblist-stage2-sum | -- | -- | 0.15 | 147 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.11 | 147 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.09 | 147 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.11 | 146 | 1.03x |
| libunord-stage10-list-sum | -- | -- | 0.07 | 157 | 0.02x |
| libunord-stage10-sum | -- | -- | 0.06 | 157 | 0.02x |
| libunord-stage6-loop-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.07 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.07 | 157 | 0.02x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.10* | *148* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *158* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.042* | *0.09* | *147* | *1.00x* |
| lib-stage1 | 0.022 | 0.047 | 0.10 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.042 | 0.08 | 147 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.042* | *0.08* | *147* | *1.00x* |
| lib-stage2-lean | 0.023 | 0.032 | 0.07 | 147 | 1.01x |
| lib-stage2-lean-u1 | 0.026 | 0.032 | 0.10 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.043 | 0.31 | 146 | 1.00x |
| *mut-odo-vecdims-aa* | *0.043* | *0.063* | *0.10* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.043** | 0.063 | 0.08 | 138 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.043* | *0.063* | *0.09* | *138* | *1.00x* |
| bq-expand | 0.139 | 0.233 | 0.12 | 122 | 3.22x |
| *bq-expand-aa-distant* | *0.139* | *0.233* | *0.12* | *122* | *3.22x* |
| *bq-expand-aa-adjacent* | *0.139* | *0.232* | *0.17* | *122* | *3.22x* |
| list (baseline) | 1.000 | 1.000 | 0.28 | 85 | 26.11x |
| *list-aa-distant* | *1.003* | *1.009* | *0.24* | *85* | *26.11x* |
| *list-aa-adjacent* | *1.004* | *1.007* | *0.19* | *85* | *26.11x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0039, worst cell 0.92% on `rev-gather48-src-50`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.01% on `rev-cnn-L1-24x24-c1`, its interval missing 1. The in-situ term reads 0.9965, 1.0117 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0038, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m6s, peak 96 MiB in use, 24 MiB max residency; the reader reads 31 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.48 ms per call raw, 4.33 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.063/0.048/0.025

**Across the halves:** 4 of the 16 arms are faster on this half and 12 slower, at a geomean of 1.0981, from `lib-stage2-lean-u1` at 0.9739 to `bq-expand-aa-adjacent` at 1.3055, with `list` itself at 1.2749. **The baseline moved 27.49% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.063, tiers at 1.00x, 3.22x and 26.11x --- and `lib-stage1` leads outside the family at 0.022, priced against `mut-odo-vecdims` at 0.6412 over 3 of 3 shapes at sign p 0.25, a margin of 35.88% against this class's floor of 0.39%. The column resolves that lead at 0.021871 against the ceiling's 0.022280, so the table's tie is the printing and not the reading. **What is this class's own is the extra pass over the axes, visible here for the fourth run running.** `rev` negates every stride and carries no zero stride and no pair of axes tied on stride, so `zerosOutermost` hands its list back unchanged and `routeUnord10` is `routeUnord7` there --- yet `libunord-stage10-sum` over `libunord-stage7-sum` reads **1.0244** against this class's 0.39% floor on the basis and **1.0241** against 0.48% on the control, past the floor on both halves. Run 30 read 1.0244 and 1.0245 on the same recipe, which is the same figure to four places on one half and to three on the other, so this is the steadiest reading in the file. Registration (6)'s 3% bar clears it, as it did there. Across the halves the class is disqualified as a reading of the level, `list` having moved 27.49 points --- the narrowest of the ten after `bcastmid`'s and still thirty-nine times the bar.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09, for Run 28 --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and Run 28's registration (7) read no crossover on it; this run reads the class for stage ten instead, at item (6).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.59* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.53 | 62 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.52 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 74 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 74 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.29* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| lib-stage2-lean | 0.016 | 0.020 | 0.51 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.016* | *0.020* | *0.51* | *62* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.016* | *0.020* | *0.43* | *62* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.016 | 0.020 | 0.41 | 62 | 1.00x |
| lib-stage1 | 0.016 | 0.020 | 0.43 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.024 | 0.52 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.018 | 0.020 | 0.45 | 61 | 1.00x |
| *mut-odo-vecdims-aa* | *0.021* | *0.057* | *0.31* | *61* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.021* | *0.057* | *0.38* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.021** | 0.057 | 0.12 | 61 | 1.00x |
| *bq-expand-aa-adjacent* | *0.092* | *0.142* | *0.69* | *46* | *1.00x* |
| *bq-expand-aa-distant* | *0.092* | *0.144* | *0.07* | *46* | *1.00x* |
| bq-expand | 0.092 | 0.142 | 0.70 | 46 | 1.00x |
| list (baseline) | 1.000 | 1.000 | 1.04 | 17 | 20.99x |
| *list-aa-distant* | *1.006* | *1.013* | *0.87* | *17* | *20.99x* |
| *list-aa-adjacent* | *1.010* | *1.018* | *0.79* | *17* | *20.99x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0095, worst cell 1.84% on `bcast-src8`, and 3 of 8 intervals cover 1. The `sum-only` halves agree at 1.0002 on a worst cell of 0.06% on `bcast-src512`, its interval covering 1. The in-situ term reads 1.0212, 1.0169 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0092, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m9s, peak 169 MiB in use, 42 MiB max residency; the reader reads 31 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 30.5 ms per call raw, 29.4 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.028/0.019/0.057/0.015/0.019/0.019

**Across the halves:** 1 of the 16 arms are faster on this half and 15 slower, at a geomean of 1.0928, from `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9947 to `list-aa-distant` at 1.3922, with `list` itself at 1.3835. **The baseline moved 38.35% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.057, tiers at 1.00x, 1.00x and 20.99x --- and `lib-stage2-lean` leads outside the family at 0.016, priced against `mut-odo-vecdims` at 0.6616 over 6 of 6 shapes at sign p 0.031, a margin of 33.84% against this class's floor of 0.95%. **This is where stage ten pays off best.** `libunord-stage10-sum` over `libunord-stage7-sum` reads **0.5497** on the basis and 0.5491 on the control, both inside the registered 0.59 within 20%, and no view has it behind stage seven on either half, the closest being `bcast-inner8` at 0.7093 and 0.7114. Stage nine alone reads the same figures to four places, 0.5497 and 0.5493, which is stage ten's tie-break doing nothing here because no pair of axes is tied on stride --- registration (14)'s `within 0.5%` clause, read at 1.0000 and 0.9997. **And it is where the two fills stay met.** With `lib-stage1` filling through `fillStage2` since 2026-09-11, `lib-stage2-lean` over it reads 0.9896 on the basis and 0.9942 on the control, inside the 3% registration (7) asked for --- so what Run 28 measured as the lean fill's 0.8635 lead was the two bodies the leaf lacked, and a whole optimisation level has not reopened it. Across the halves the class is disqualified, `list` having moved **38.35 points**, the widest of the ten.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.47* | *66* | *1.92x* |
| liblist-stage1-sum | -- | -- | 0.41 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.34 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.34 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.31 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.38 | 82 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.33 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.30 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 96 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.26* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *88* | *0.00x* |
| lib-stage2-lean-u1 | 0.012 | 0.019 | 0.32 | 82 | 1.00x |
| lib-stage1 | 0.012 | 0.018 | 0.35 | 82 | 1.00x |
| lib-stage2-lean | 0.012 | 0.018 | 0.57 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.031 | 0.28 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.020* | *0.030* | *0.44* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.020* | *0.030* | *0.27* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.030 | 0.34 | 78 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.053* | *0.27* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.029* | *0.053* | *0.26* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.053 | 0.22 | 76 | 1.00x |
| *bq-expand-aa-adjacent* | *0.099* | *0.180* | *0.42* | *61* | *1.92x* |
| bq-expand | 0.099 | 0.180 | 0.44 | 61 | 1.92x |
| *bq-expand-aa-distant* | *0.100* | *0.180* | *0.28* | *61* | *1.92x* |
| list (baseline) | 1.000 | 1.000 | 0.83 | 28 | 23.56x |
| *list-aa-adjacent* | *1.002* | *1.005* | *0.74* | *28* | *23.56x* |
| *list-aa-distant* | *1.003* | *1.009* | *1.03* | *28* | *23.56x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0096, worst cell 2.16% on `bcastmid-b200k`, and 2 of 8 intervals cover 1. The `sum-only` halves agree at 0.9986 on a worst cell of 0.49% on `bcastmid-primes`, its interval missing 1. The in-situ term reads 1.0229, 1.0883 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0074, which the correction amplifies by 1.29x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m48s, peak 121 MiB in use, 35 MiB max residency; the reader reads 31 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 48.7 ms per call raw, 47.6 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.053/0.019/0.034/0.022

**Across the halves:** 2 of the 16 arms are faster on this half and 14 slower, at a geomean of 1.0928, from `lib-stage2-lean` at 0.9978 to `bq-expand-aa-distant` at 1.2699, with `list` itself at 1.2372. **The baseline moved 23.72% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.053, tiers at 1.00x, 1.92x and 23.56x --- and `lib-stage2-lean-u1` leads outside the family at 0.012, priced against `mut-odo-vecdims` at 0.4227 over 4 of 4 shapes at sign p 0.12, a margin of 57.73%, the widest of the ten, against this class's floor of 0.96%. **Stage ten pays off here too**, `libunord-stage10-sum` over `libunord-stage7-sum` reading **0.5715** on the basis and 0.5686 on the control against the registered 0.62, with `bcastmid-b200k` widest at 0.3367 and 0.3299 and `bcastmid-primes` closest to level at 0.7040 and 0.7046, and no view behind on either half. Stage nine alone reads 0.5716 and 0.5687, the tie-break again worth nothing where no pair of axes is tied. **And the doubling block copy stays the leaf's as well**: `lib-stage2-lean` over `lib-stage1` reads 0.9972 on both halves to four places, inside registration (7)'s 3%, where Run 28 read 0.6163 for a lean fill the leaf could not match. Across the halves the class is disqualified, `list` having moved 23.72 points --- the narrowest of the ten and still thirty-three times the bar.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09, for Run 28, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.35* | *62* | *3.86x* |
| liblist-stage1-sum | -- | -- | 0.16 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.22 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.18 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.17 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.19 | 82 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.06 | 102 | 0.04x |
| libunord-stage10-sum | -- | -- | 0.06 | 102 | 0.04x |
| libunord-stage6-loop-sum | -- | -- | 1.93 | 90 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.06 | 100 | 0.03x |
| libunord-stage7-sum | -- | -- | 0.05 | 102 | 0.03x |
| libunord-stage9-sum | -- | -- | 0.07 | 100 | 0.04x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.42* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *97* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.031 | 0.15 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.028* | *0.030* | *0.20* | *82* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.028* | *0.030* | *0.15* | *82* | *1.00x* |
| lib-stage2-lean | 0.028 | 0.031 | 0.18 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.029 | 0.031 | 0.17 | 82 | 1.00x |
| lib-stage1 | 0.029 | 0.031 | 0.18 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.029 | 0.032 | 0.21 | 82 | 1.00x |
| **mut-odo-vecdims** | **0.051** | 0.084 | 0.18 | 76 | 1.00x |
| *mut-odo-vecdims-aa* | *0.051* | *0.084* | *0.18* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.084* | *0.16* | *76* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.172* | *0.216* | *0.36* | *57* | *3.86x* |
| bq-expand | 0.172 | 0.214 | 0.33 | 57 | 3.86x |
| *bq-expand-aa-distant* | *0.173* | *0.214* | *0.28* | *57* | *3.86x* |
| list (baseline) | 1.000 | 1.000 | 0.56 | 30 | 27.66x |
| *list-aa-adjacent* | *1.004* | *1.015* | *0.42* | *30* | *27.66x* |
| *list-aa-distant* | *1.005* | *1.019* | *0.52* | *30* | *27.66x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa` at 0.9942, worst cell 3.11% on `window-64x64-c16-k3`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9994 on a worst cell of 0.52% on `window-224x224-k3-d2`, its interval covering 1. The in-situ term reads 0.9991, 1.1649 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9970, which the correction amplifies by 2.01x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h21m27s, peak 116 MiB in use, 28 MiB max residency; the reader reads 31 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 14.3 ms per call raw, 13.8 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.040/0.050/0.084/0.031/0.051/0.051/0.053/0.052

**Across the halves:** 1 of the 16 arms are faster on this half and 15 slower, at a geomean of 1.1446, from `lib-stage2-lean` at 0.9992 to `bq-expand-aa-distant` at 1.5252, with `list` itself at 1.2904. **The baseline moved 29.04% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.084, the second highest of the ten behind `small`'s, and tiers at 1.00x, 3.86x and 27.66x, the widest `bq-expand` and `list` levels of any class --- and the family's own ceiling leads, `mut-odo-vecdims-add-in-leaf-u2` at 0.027 against `lib-stage2-lean`'s 0.028. What the block prices is the BEST ARM OUTSIDE THE FAMILY and not the leader, as every block does --- `lib-stage2-lean` is the priced arm in seven of the ten classes and the leader in five of those seven: `lib-stage2-lean` over `mut-odo-vecdims` at 0.4810 over 8 of 8 shapes at sign p 0.0078, a margin of 51.90% against this class's floor of 0.58%. The ceiling itself reads 0.5302 over the same arm. **This class kills registration (5), and it is the run's sharpest class-level finding.** No window view carries a zero stride, so stage nine's move is the identity here and stage ten over `libunord-stage9-sum` reads stage seven's tie-break alone: **0.9557** on the basis, inside the registered 25% of 0.73, and **0.9847** on the control, 25.47 points off and outside it. **Three views put stage ten BEHIND stage nine past the floor on BOTH halves** --- `window-128x128-k7` at **1.3666** and **1.3689**, `window-64x64-k1x9` at 1.2355 and 1.2359, and `window-224x224-k3` at 1.0785 and 1.1985 --- which is the item's second kill condition exactly, and it says the two reorderings do not compose here. Run 29 and Run 30 both found the first two views parting in SIGN between their halves, which is what let the item survive twice; this run's two halves agree, and what they agree on is the kill. **The mechanism is readable on the worst view**: stage nine beats stage seven at 0.7321 on `window-128x128-k7` and stage ten hands all of it back, `libunord-stage10-sum` over `libunord-stage7-sum` reading **1.0006** there and 1.0023 on the control. Across the halves the class is disqualified, `list` having moved 29.04 points.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.11* | *118* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.13 | 128 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.15 | 128 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.16 | 127 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.17 | 128 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.19 | 127 | 1.01x |
| libunord-stage10-list-sum | -- | -- | 0.16 | 127 | 1.01x |
| libunord-stage10-sum | -- | -- | 0.16 | 127 | 1.01x |
| libunord-stage6-loop-sum | -- | -- | 0.16 | 127 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.16 | 127 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.15 | 127 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.14 | 127 | 1.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.14* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *138* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.030 | 0.18 | 127 | 1.00x |
| lib-stage1 | 0.022 | 0.030 | 0.12 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.023* | *0.030* | *0.16* | *128* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.023 | 0.030 | 0.12 | 128 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.023* | *0.030* | *0.12* | *127* | *1.00x* |
| lib-stage2-lean | 0.024 | 0.030 | 0.14 | 127 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.030 | 0.18 | 126 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.029* | *0.09* | *127* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.027* | *0.028* | *0.09* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.028 | 0.10 | 127 | 1.00x |
| bq-expand | 0.091 | 0.100 | 0.10 | 111 | 1.21x |
| *bq-expand-aa-adjacent* | *0.091* | *0.100* | *0.10* | *111* | *1.21x* |
| *bq-expand-aa-distant* | *0.091* | *0.100* | *0.09* | *111* | *1.21x* |
| list (baseline) | 1.000 | 1.000 | 0.20 | 69 | 21.49x |
| *list-aa-distant* | *1.001* | *1.001* | *0.26* | *69* | *21.49x* |
| *list-aa-adjacent* | *1.002* | *1.002* | *0.18* | *69* | *21.49x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9960, worst cell 0.54% on `scaled-r5`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `scaled-rank1-m1`, its interval covering 1. The in-situ term reads 1.0190, 1.0053 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9982, which the correction amplifies by 2.30x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m5s, peak 111 MiB in use, 28 MiB max residency; the reader reads 31 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 5.33 ms per call raw, 5.15 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.023/0.028/0.028

**Across the halves:** 7 of the 16 arms are faster on this half and 9 slower, at a geomean of 1.0686, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9444 to `list-aa-distant` at 1.3428, with `list` itself at 1.3257. **The baseline moved 32.57% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.028, joint lowest of the ten with `compose`'s, and tiers at 1.00x, 1.21x and 21.49x --- and the family's own ceiling leads, `mut-odo-vecdims-add-in-leaf-u1` at 0.022 against `lib-stage1`'s 0.022, which the column resolves at 0.022206 against 0.022400. What the block prices is `lib-stage1` and not the leader: it reads 0.9352 over `mut-odo-vecdims` at 2 of 3 shapes and sign p 1, a margin of 6.48% against this class's floor of 0.40%, where the ceiling reads 0.9331 over the same arm. That margin is the narrowest of the ten, and one of two whose sign test does not separate it, `runs` printing p 0.79. **The class has no news of its own this run.** Every span registration (6) reads here ties inside the floor on both halves --- 1.0046 and 1.0024 for stage ten against stage seven --- and registration (15)'s pair reads 1.0002 and 1.0048, which is what a class of three dense scaled views is for. The A/A slot the open list has watched on `scaled-super-r3` for eight runs is not the widest cell here on the basis, though it is on the control at 3.79%. Across the halves the class is disqualified, `list` having moved 32.57 points.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.43* | *52* | *1.13x* |
| liblist-stage1-sum | -- | -- | 0.11 | 65 | 0.19x |
| liblist-stage2-sum | -- | -- | 0.07 | 69 | 0.16x |
| liblist-stage3-sum | -- | -- | 0.03 | 71 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.04 | 71 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.13 | 64 | 0.19x |
| libunord-stage10-list-sum | -- | -- | 0.03 | 71 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.04 | 71 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.03 | 69 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.03 | 71 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 71 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 71 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *78* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.026* | *0.46* | *59* | *1.00x* |
| lib-stage2-lean | 0.024 | 0.025 | 0.12 | 59 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.025* | *0.14* | *59* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.026 | 0.10 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.026 | 0.11 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.058* | *0.12* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.058 | 0.10 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.028* | *0.058* | *0.10* | *59* | *1.00x* |
| lib-stage2-lean-u1 | 0.033 | 0.037 | 0.16 | 56 | 1.00x |
| lib-stage1 | 0.094 | 1.116 | 0.19 | 55 | 1.19x |
| *bq-expand-aa-distant* | *0.098* | *0.145* | *0.06* | *46* | *1.13x* |
| *bq-expand-aa-adjacent* | *0.099* | *0.144* | *0.42* | *46* | *1.13x* |
| bq-expand | 0.099 | 0.143 | 0.38 | 46 | 1.13x |
| list (baseline) | 1.000 | 1.000 | 2.49 | 16 | 21.32x |
| *list-aa-distant* | *1.031* | *1.047* | *0.40* | *17* | *21.32x* |
| *list-aa-adjacent* | *1.033* | *1.048* | *0.28* | *17* | *21.32x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0326, worst cell 4.78% on `runs-r3-48x30`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9999 on a worst cell of 0.54% on `runs-16384`, its interval covering 1. The in-situ term reads 1.0310, 1.0392 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0315, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h37m33s, peak 581 MiB in use, 247 MiB max residency; the reader reads 31 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 40.5 ms per call raw, 39.5 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.058/0.047/0.041/0.039/0.032/0.030/0.025/0.025/0.025/0.024/0.024/0.024/0.024/0.026

**Across the halves:** 7 of the 16 arms are faster on this half and 9 slower, at a geomean of 1.1017, from `lib-stage1` at 0.9894 to `list` at 1.3194, with `list` itself at 1.3194. **The baseline moved 31.94% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.058, tiers at 1.00x, 1.13x and 21.32x --- and `lib-stage2-lean` leads outside the family at 0.024, priced against `mut-odo-vecdims` at 0.8004 over 8 of 14 shapes at sign p 0.79, a margin of 19.96% against this class's floor of **3.26%**, the widest floor of the ten. **This class carries four of the run's registrations at once and all four held.** The lazy odometer's consumer over the port's base-offset table, `liblist-stage4-sum` over `liblist-stage2-sum`, reads **0.3853** on the basis and **0.4063** on the control against a registered 0.47; over master's own slice recursion, `liblist-stage1-sum`, **0.3086** and **0.3037** against 0.37; the composed route over master's consumer, `libunord-stage10-sum` over `libunord-stage1-sum`, **0.3089** and **0.3036** against 0.37; and the lean fill over stage one **0.2317** and **0.2302** against 0.23, where the two routes genuinely part, stage one concatenating a slice per run where the lean dispatch fills. So the odometer's lead over master's recursion and over the branch's table is the design's on both halves and not the level's, as Runs 29 and 30 found it was neither pass's. **It is also where the level reaches the fused list hardest and where that reach has all but gone**: `libunord-stage7-sum` moves 2.43 points across the halves here, against the 18.27 Run 30 measured for `-fliberate-case` alone on this same population, the bangs of 2026-09-13 having retired the difference. **And it is where the WIDER property breaks**: `lib-stage1` reads above its shape's `list` at `runs-2` on both halves, **1.1156** and **1.6936**, and at `runs-3` on the control, 1.1333 --- the only cells of any population where a timed arm is slower than the baseline, and the shipped route is the arm. Across the halves the class is disqualified, `list` having moved 31.94 points.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes, in the order they run: `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which landed 2026-09-09 and is `runs-96`'s construction under a `flip` name --- the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two; `flip-whole-square` (`l` 1798281, `sInner` 1341); `flip-last-c32` (`l` 165888, `sInner` 3); `flip-last-rows` (`l` 1800000, `sInner` 96); and the two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed, `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. The control sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.42* | *66* | *1.05x* |
| liblist-stage1-sum | -- | -- | 0.22 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.12 | 88 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.10 | 92 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.10 | 92 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.12 | 82 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.03 | 98 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.03 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 98 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *93* | *0.00x* |
| lib-stage2-lean | 0.023 | 0.039 | 0.43 | 84 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.043* | *0.33* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.044* | *0.10* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.044 | 0.12 | 80 | 1.00x |
| lib-stage2-lean-u1 | 0.027 | 0.048 | 0.32 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.047 | 0.23 | 80 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.028* | *0.052* | *0.12* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.028** | 0.052 | 0.11 | 77 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.052* | *0.11* | *77* | *1.00x* |
| lib-stage1 | 0.034 | 0.048 | 0.24 | 80 | 1.00x |
| *bq-expand-aa-adjacent* | *0.090* | *0.178* | *0.42* | *61* | *1.05x* |
| bq-expand | 0.090 | 0.178 | 0.47 | 61 | 1.05x |
| *bq-expand-aa-distant* | *0.092* | *0.179* | *0.11* | *61* | *1.05x* |
| list (baseline) | 1.000 | 1.000 | 0.71 | 32 | 21.18x |
| *list-aa-adjacent* | *1.008* | *1.021* | *0.43* | *32* | *21.18x* |
| *list-aa-distant* | *1.011* | *1.033* | *0.55* | *32* | *21.18x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0106, worst cell 3.26% on `flip-last-rows`, and 2 of 8 intervals cover 1. The `sum-only` halves agree at 0.9998 on a worst cell of 0.12% on `flip-inner-gap64`, its interval covering 1. The in-situ term reads 1.0239, 1.0262 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0103, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m7s, peak 209 MiB in use, 76 MiB max residency; the reader reads 31 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 31 ms per call raw, 29.9 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.024/0.024/0.052/0.049/0.026/0.026

**Across the halves:** 2 of the 16 arms are faster on this half and 14 slower, at a geomean of 1.0805, from `lib-stage2-lean` at 0.9899 to `list-aa-distant` at 1.3378, with `list` itself at 1.3360. **The baseline moved 33.60% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.052, fourth lowest of the ten and just under `bcastmid`'s 0.053, and tiers at 1.00x, 1.05x and 21.18x --- and `lib-stage2-lean` leads outside the family at 0.023, priced against `mut-odo-vecdims` at 0.7533 over 5 of 6 shapes at sign p 0.22, a margin of 24.67% against this class's floor of 1.06%. **The 12.92% cell Run 30 carried here is gone, and the mechanism that made it has not changed.** `flip-last-rows` is still the bench where the forcing pass is about half the total, so subtracting it still multiplies any disagreement by about two; this run's A/A worst cell on that bench is 3.26% on the basis and 4.19% on the control, and the class floor has halved with it, 1.06% against Run 30's 2.05%. What has not changed is the reading rule: a margin on `flip-last-rows` under about two percent is not readable, and the amplification is the reason rather than any instability. `--wild` clears both processes outright, no bench reaching 0.25 of a core. Across the halves the class is disqualified, `list` having moved 33.60 points.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.08* | *103* | *1.06x* |
| liblist-stage1-sum | -- | -- | 0.22 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.16 | 122 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.03 | 129 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.16 | 113 | 0.42x |
| libunord-stage10-list-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.04 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.05 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.03 | 129 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.17* | *129* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *122* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.024* | *0.11* | *111* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.021* | *0.024* | *0.09* | *111* | *1.00x* |
| lib-stage2-lean | 0.021 | 0.024 | 0.09 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.024 | 0.13 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.026 | 0.11 | 111 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.029* | *0.10* | *111* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.023* | *0.029* | *0.08* | *111* | *1.00x* |
| **mut-odo-vecdims** | **0.023** | 0.029 | 0.11 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.026 | 0.15 | 111 | 1.00x |
| lib-stage1 | 0.050 | 0.058 | 0.13 | 104 | 1.42x |
| *bq-expand-aa-adjacent* | *0.086* | *0.087* | *0.08* | *96* | *1.06x* |
| *bq-expand-aa-distant* | *0.086* | *0.087* | *0.10* | *96* | *1.06x* |
| bq-expand | 0.086 | 0.087 | 0.13 | 96 | 1.06x |
| *list-aa-adjacent* | *1.000* | *1.005* | *0.22* | *54* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 0.32 | 54 | 21.22x |
| *list-aa-distant* | *1.003* | *1.014* | *0.27* | *54* | *21.22x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 0.9956, worst cell 1.87% on `block-r3-vol64`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.03% on `block-run64-page`, its interval covering 1. The in-situ term reads 1.0237, 1.0193 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9984, which the correction amplifies by 2.50x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m25s, peak 132 MiB in use, 35 MiB max residency; the reader reads 31 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.63 ms per call raw, 4.47 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.019/0.024/0.029/0.024/0.020

**Across the halves:** 7 of the 16 arms are faster on this half and 9 slower, at a geomean of 1.0720, from `mut-odo-vecdims-aa-distant` at 0.9937 to `list-aa-distant` at 1.3462, with `list` itself at 1.3408. **The baseline moved 34.08% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.029, third lowest of the ten behind `scaled`'s and `compose`'s 0.028 apiece, and tiers at 1.00x, 1.06x and 21.22x --- and `lib-stage2-lean` leads outside the family at 0.021, priced against `mut-odo-vecdims` at 0.9277 over 5 of 5 shapes at sign p 0.062, a margin of 7.23% against this class's floor of 0.44%. **It is `runs`'s second, and it says the same four things at half the size.** The odometer's consumer over the port's table reads **0.6806** on the basis and **0.7479** on the control against a registered 0.73; over master's recursion **0.4299** and **0.4348** against 0.46; the composed route over master's consumer **0.4319** and **0.4352** against the same 0.46; and the lean fill over stage one **0.4190** and **0.4299** against 0.42 --- four spans registered off Run 30's own readings and four landing inside four points of them, on both halves and now under a whole level rather than one flag. **Where it differs from `runs` is the consumer**: `libunord-stage7-sum` moves **0.04 points** across the halves here, against 6.33 for `-fliberate-case` alone on Run 30, so on this population the level's reach into the fused list is not merely smaller than the flag's but absent. Across the halves the class is disqualified, `list` having moved 34.08 points.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.18* | *222* | *1.44x* |
| liblist-stage1-sum | -- | -- | 0.22 | 228 | 1.64x |
| liblist-stage2-sum | -- | -- | 0.14 | 231 | 1.49x |
| liblist-stage3-sum | -- | -- | 0.21 | 227 | 1.63x |
| liblist-stage4-sum | -- | -- | 0.39 | 231 | 1.45x |
| libunord-stage1-sum | -- | -- | 0.21 | 223 | 2.07x |
| libunord-stage10-list-sum | -- | -- | 0.31 | 227 | 1.12x |
| libunord-stage10-sum | -- | -- | 0.30 | 227 | 1.14x |
| libunord-stage6-loop-sum | -- | -- | 0.30 | 232 | 1.24x |
| libunord-stage6-sum | -- | -- | 0.28 | 231 | 1.26x |
| libunord-stage7-sum | -- | -- | 0.27 | 231 | 1.26x |
| libunord-stage9-sum | -- | -- | 0.21 | 227 | 1.14x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.21* | *239* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.07* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.07* | *250* | *0.01x* |
| lib-stage2-lean | 0.052 | 0.095 | 0.25 | 228 | 1.40x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.052 | 0.066 | 0.26 | 230 | 1.27x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.052* | *0.066* | *0.30* | *230* | *1.27x* |
| lib-stage2-lean-u1 | 0.052 | 0.097 | 0.21 | 228 | 1.40x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.052* | *0.067* | *0.25* | *230* | *1.27x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.055 | 0.066 | 0.27 | 229 | 1.27x |
| *mut-odo-vecdims-aa* | *0.060* | *0.086* | *0.19* | *229* | *1.27x* |
| *mut-odo-vecdims-aa-distant* | *0.060* | *0.086* | *0.22* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.060** | 0.087 | 0.24 | 229 | 1.27x |
| lib-stage1 | 0.087 | 0.107 | 0.22 | 221 | 2.32x |
| *bq-expand-aa-adjacent* | *0.135* | *0.193* | *0.20* | *218* | *1.44x* |
| *bq-expand-aa-distant* | *0.135* | *0.194* | *0.15* | *218* | *1.44x* |
| bq-expand | 0.135 | 0.193 | 0.20 | 218 | 1.44x |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.17* | *180* | *21.57x* |
| list (baseline) | 1.000 | 1.000 | 0.15 | 180 | 21.57x |
| *list-aa-distant* | *1.001* | *1.005* | *0.16* | *180* | *21.57x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa` at 1.0078, worst cell 2.21% on `small-bcast32`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0003 on a worst cell of 0.48% on `small-row96`, its interval covering 1. The in-situ term reads 0.9854, 0.9668 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0048, which the correction amplifies by 1.62x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m30s, peak 134 MiB in use, 53 MiB max residency; the reader reads 31 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.7 us per call raw, 6.47 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.041/0.076/0.049/0.057/0.087

**Across the halves:** 0 of the 16 arms are faster on this half and 16 slower, at a geomean of 1.0915, from `mut-odo-vecdims-add-in-leaf-u2` at 1.0153 to `list-aa-distant` at 1.3037, with `list` itself at 1.2990. **The baseline moved 29.90% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves, and this is the one class where property 3's level clause is read off a `mut-odo-vecdims` above 1.00x --- 1.27x on each half, with `bq-expand` at 1.44x on the basis and 1.34x on the control and `list` at 21.57x and 19.64x, the per-call constants showing through a call under a microsecond, and the ORDER unbroken like the rest. `worst` is 0.087, the highest of the ten. `lib-stage2-lean` leads outside the family at 0.052, priced against `mut-odo-vecdims` at 0.8653 --- **ahead on only 1 of 5 shapes at sign p 0.38**, so the paired figure and the win count disagree here and neither alone is the reading. **Two of the run's readings are this class's own.** Stage ten sits **1.1910** on the basis and **1.1768** on the control behind stage seven, which is the extra pass over the axes on the shortest calls the set has and is what registration (6) said would print a figure and no verdict; per view it lands exactly as that item's clause says, behind on `small-flat64` (1.3819), `small-patch-k5` (1.4592), `small-patch-r5` (1.2926) and `small-row96` (1.2607) and ahead on `small-bcast32` (0.7294). And `small` is the one class where registration (1) falls outside its registered 4%: `lib-stage2-lean` reads **1.0494** across the halves here, the only one of thirty class readings to do so. Across the halves the class is disqualified, `list` having moved 29.90 points.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.39* | *85* | *1.35x* |
| liblist-stage1-sum | -- | -- | 0.32 | 98 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.32 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.32 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.37 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.03 | 110 | 0.01x |
| libunord-stage10-sum | -- | -- | 0.03 | 110 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.31 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.31 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.04 | 110 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.21* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *105* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.015* | *0.016* | *0.34* | *98* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.015* | *0.016* | *0.28* | *98* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.015 | 0.016 | 0.28 | 98 | 1.00x |
| lib-stage2-lean | 0.015 | 0.017 | 0.32 | 98 | 1.00x |
| lib-stage1 | 0.015 | 0.017 | 0.30 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.017 | 0.30 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.017 | 0.018 | 0.41 | 96 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.024* | *0.028* | *0.25* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.024** | 0.028 | 0.18 | 94 | 1.00x |
| *mut-odo-vecdims-aa* | *0.024* | *0.028* | *0.25* | *94* | *1.00x* |
| bq-expand | 0.094 | 0.102 | 0.38 | 79 | 1.35x |
| *bq-expand-aa-adjacent* | *0.094* | *0.102* | *0.38* | *79* | *1.35x* |
| *bq-expand-aa-distant* | *0.094* | *0.102* | *0.22* | *79* | *1.35x* |
| list (baseline) | 1.000 | 1.000 | 0.66 | 44 | 22.01x |
| *list-aa-distant* | *1.001* | *1.001* | *0.73* | *44* | *22.01x* |
| *list-aa-adjacent* | *1.002* | *1.005* | *0.67* | *44* | *22.01x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0032, worst cell 0.65% on `compose-zero-mid`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9989 on a worst cell of 0.48% on `compose-rev-bcast`, its interval covering 1. The in-situ term reads 1.0082, 1.0166 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0022, which the correction amplifies by 1.37x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m51s, peak 126 MiB in use, 33 MiB max residency; the reader reads 31 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 30.9 ms per call raw, 29.8 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.028/0.028/0.022/0.019

**Across the halves:** 5 of the 16 arms are faster on this half and 11 slower, at a geomean of 1.0813, from `lib-stage1` at 0.9853 to `list-aa-adjacent` at 1.3630, with `list` itself at 1.3532. **The baseline moved 35.32% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.028, joint lowest of the ten with `scaled`'s, and tiers at 1.00x, 1.35x and 22.01x --- and the family's own ceiling leads, `mut-odo-vecdims-add-in-leaf-u2` at 0.015 against `lib-stage2-lean`'s 0.015, which the column resolves at 0.014632 against 0.014781. What the block prices is the lean fill and not the leader: `lib-stage2-lean` over `mut-odo-vecdims` at 0.6201 over 4 of 4 shapes at sign p 0.12, a margin of 37.99% against this class's floor of 0.32%, where the ceiling reads 0.6139 over the same arm. **Stage ten's figure here is printed and not adjudicated, as registration (5) asks**: **0.6870** on the basis and 0.6851 on the control over stage seven, between the `bcast` and `bcastmid` readings and made of two kinds of view at once --- `compose-zero-mid` and the two `-bcast` views, where the zero-stride move fires and which read 0.4412, 0.7100 and 0.7110 on the basis, and `compose-scalar`, every stride 0 and the route unchanged, at **1.0002** and 1.0004, inside floors of 0.32% and 1.10% on the two halves. That last cell is the closest any zero-stride view comes to putting stage ten behind stage seven, and it is what registration (5)'s FIRST kill condition wants and does not get; the item dies on `window` instead. Across the halves the class is disqualified, `list` having moved 35.32 points.


## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 31's halves differ in ONE OPTIMISATION LEVEL and in nothing else.** One source, `Main.hs` at `13cbd0d`, one shim at `b3a1aca` and one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of the assembler, one compiler --- ghc-9.12.4 through `cabal.project` and its freeze, both binaries carrying `ghc-internal-9.1204.0` --- one store, one plan and one set of boot libraries, one roster, one shape set, one class list and one bench order, both run under `WILDLOG=1 SATURATE=1`, both with `-A32m -I0 -T -M8G` baked and read back by `+RTS --info`. `-O1` is micro.cabal's own `ghc-options` and the basis leaves it alone; `run31-o2` puts `-O2` on its `--ghc-options` line, which OVERRIDES that rather than adding to it, the later `-O` winning. So this pair is Run 30's published recipe read twice at two levels, and every figure that moves between the halves moved because of that one word.

**The sequence was launched once and ran to the end in ONE window, and no population was rerun**, 2026-09-14T01:20:50 to 2026-09-14T08:14:07, twenty-two processes --- the main set on each half and 20 class processes, one per class per half --- with no hole between one finishing and the next starting. The gate's four ran before them from 00:47:30 and the four rider stages after them to 08:26:36; the counted work followed on a box already handed back, from 08:26:56. Every process exited 0 at its roster's count --- 589 twice, 434 twice, 248 twice, and 186, 155, 124 and 93 four times each, 4774 benches --- with no complaint and no `!!` line in the wall-clock record, and the control half ran first throughout. The two main-set processes are the only ones whose stderr line no block below carries: `run31-nospec-main` took 0h50m48s at a peak of 167 MiB in use and 64 MiB max residency, and `run31-o2-main` 0h50m46s at 177 MiB and 63 MiB, two seconds apart over 589 benches apiece. **NO INTRUSION WAS FOUND ANYWHERE.** `--wild` clears all twenty-two processes outright, and the eighty-eight alone-leg logs and the gate's four with them: no bench of the hundred and fourteen reaches 0.25 of a core, the worst being 0.24 in a gate process, so nothing else was running on this machine while any of it was measured, this session included. **What the run does carry is two WILD cells**, both on `run31-o2-main` and both on `cnn-slice-c32`: the shipped leaf's two A/A copies, whose pairs read 0.9870 and 0.9842 with worst cells of 17.06% and 15.29%. They are wild and not foreign --- the RTS totals are flat and no other process was on the box --- and what they cost is one figure, the control half's floor, which they set at 1.58% where the basis's is 0.61%. **No verdict in this file turns on either**: registration (8), whose two spans they are, holds in all forty-four of its readings and by its own bar holds on the control main set at 1.30 and 1.58 points against that 1.58% floor. Every figure here is over the run's own nineteen main-set shapes, nothing being omitted or requoted.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run31-nospec`, md5 `bbce438866c7a20652b96a60f4e1748d`, and `run31-o2`, md5 `0bb3b8cc28592fbe592195d71435d1ab`, with `.text` at **20775109** bytes on the basis and **20787397** on the control --- they do not agree, the -O2 half being the larger by 12288 bytes, which is 12 KiB exactly and is an observation and not a mechanism. **NEITHER md5 is two-sided this run**: `Main.hs` moved from `7685375` to `13cbd0d` in three commits on 2026-09-13, so neither half's inputs are an earlier binary's, no md5 here can reproduce one and an unequal one names nothing. What they do say is that they differ from each other, which is the level having reached a pass. The basis's `.text` sits 8192 bytes above `run30-nospec`'s 20766917, and all three deltas among the three binaries are exact multiples of 4096. **The control half's `.text` is `run30-libcase`'s to the byte**, 20787397 against 20787397, across a moved source and a different flag set and with the md5s differing --- an observation this file offers no mechanism for. `diag` confirmed the regime in both binaries before the hours were spent: `baseOffsetsScan` against `baseOffsetsMut` on `vgg-14-c512` reads 24066407 against 2408530 on the basis, 9.992 times apart, and 2408938 against 2408514 on the control, equal to three figures. That is plain -O1 and -O2 read straight off the pair, which is why pre-run steps 9 and 9b are one reading here and why this pair needed no md5 to stand in for its variable.

**Both the source and the roster moved, so this run's fills read the pinning claim in the form the README states it and not in the tighter form Run 30 had.** The tracked 28-byte loops were read at the build against the previous build of the basis's recipe, `run30-nospec`, with `--delta`: **every mod-64 offset preserved**, the two groups at [0, 24, 8, 0, 4, 0] and [0, 0], with NO address surviving to the byte and one displacement per group, 0x300 and 0x1a00. A timed function landed at `13cbd0d` and six left at `c753fff`, so what this proves is that a source that gains and loses functions leaves every tracked fill copy at its offset modulo 64 --- which is the claim as stated, where Run 30's identical source gave the control case, every address surviving and no displacement at all. **Within the pair the layout DID move, and that is the reading to set beside Run 30's.** `./loop-offsets.py run31-o2 run31-nospec` puts the six-copy group at [0, 24, 8, 0, 4, 0] on the basis and [0, 0, 0, 24, 0, 4] on the control and the two-copy group at [0, 0] against [0, 12], so tracked fill copies are displaced within this pair; `--library` reads **916** library self-loops in common, **4.4%** at the same offset in line and **58.7%** in the same straddle state. Run 30 read 100.0% and 100.0% of 925 for `-fliberate-case` and Run 29 39.4% of 914 for `-fspec-constr`, so of the three readings taken on halves that share a compiler this is much the widest displacement, and the level moves what either of its passes alone did not.

**The three main-set anchors** read **6.32 us** on `cnn-slice-c32`, **3.71 ms** on `cnn-L2-24x24-c32` and **39.3 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, against **5.03 us**, **2.89 ms** and **31.4 ms** on the control --- the control FASTER on all three, by 20.40%, 22.04% and 20.01% computed from the cells rather than from the printed three figures, which is `list` being what `-O2` buys, beside the 29.74 points its main-set column reads. The direction is Run 30's, its candidate half being the faster one too, and the size is about a fifth of the basis's time against that run's sixth. **Two conventions are in play in this file and a bare percentage does not say which**: a `cross` figure is a RATIO, so `list`'s 1.2974 is 29.74 points, while a saving is read against the slower half, so the same movement is 22.9% of the basis's time. The head quotes the first and the anchors and the counted work the second. **Against `run30-nospec`, the previous build of this recipe, the same three anchors move -0.29%, +0.25% and -1.45%**, computed from both runs' cells --- the box and the three commits of 2026-09-13 together, and the closest thing to a box reading this run has.
| shape | `l` | `list`, per call | net | `o2`, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.49 us | 6.32 us | 5.03 us |
| `cnn-L2-24x24-c32` | 165888 | 3.81 ms | 3.71 ms | 2.89 ms |
| `stretch-wide-2xM` | 1800000 | 40.4 ms | 39.3 ms | 31.4 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at **40.53 ms** raw and **39.45 ms** net on the basis, against Run 30's 40.89 ms and 39.81 ms on the same recipe --- **0.87% raw and 0.90% net faster, computed from the cells rather than from the printed three figures, and it is the box and the three commits of 2026-09-13 together** rather than either alone, this basis and Run 30's being one recipe over a moved source. The control half reads 32.63 ms and 31.55 ms on the same shape, 20.02% below the basis, which is the level and is `list` again. Its own floor is 3.26% here, the widest of the ten. **No class population moved this run at all**: `roster-delta.py` reads 58 class views to 58 with every one of the ten classes unmoved, so each class anchor is read against its own predecessor over the same shapes, and the six arms parked and the one landed changed which arms a class times and not which shapes.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0002** on the basis and **1.0001** on the control, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0341** and **1.0847** on the basis and **1.0337** and **1.0699** on the control for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent over the term it stands for, by nearly the same few percent on both halves, which is where it has run since Run 17. So the level reaches neither the forcing pass nor the gap between the two ways of measuring it.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-13, before the run, on the pair named by the deciding-regime entry in [README's non-urgent TODO list](../README.md#non-urgent-todo-list) --- which is in the OTHER file, this sentence having said `below` while the entry it means has never been in this one: Run 30's basis recipe, ghc-9.12.4 in the dead-spot form at plain -O1, as both halves, `nospec` as it is and `o2` with `-O2` added to its `--ghc-options`, `nospec` the basis, so that `--compare` reads nospec over o2 and every `cross` below is what `-O2` does to one arm; over Run 30's roster less the six arms parked 2026-09-13 and plus `libunord-stage10-list-sum`, base's `sum` over stage ten's list, landing the same day, and with the bangs of that day on the walker's vector and on the offset its empty carry ignores, measured to retire `-fliberate-case` on the lazy stages' consumers --- so item (3) below, written before them, predicts for the `o2` half a gain those bangs were built to remove, and item (15) is what reads them --- `Main.hs` otherwise standing where Run 30 ran it. The first ten items are Run 30's eleven less its (5), which is not carried, both of its arms being parked: eight as they stand in `runs/run30.md` and two, (9) and (10) here, that the retirement of the claims manifest took out of that file on 2026-09-13 and that stand here on their own terms --- each re-aimed at the figure Run 30 read so that every span passes on that run's own JSONs, with `o2` written where they said `libcase`. The last five are new, on `lib-stage2-lean`, `liblist-stage4-sum`, `libunord-stage9-sum`, `libunord-stage10-sum` and `libunord-stage10-list-sum`, every figure they quote read off Run 30's JSONs with `--pair` on 2026-09-13.

(1) *The flag on the shipped fill.* On the main set: `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 2%`, `predict: cross lib-stage1 1.0 within 2%` and `predict: cross lib-stage2-lean 1.0 within 2%`; on every class: `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 4%`, `predict: cross lib-stage1 1.0 within 4%` and `predict: cross lib-stage2-lean 1.0 within 4%` --- Run 30 read the three at 0.9891, 1.0003 and 1.0093 on the main set under `-fliberate-case`, and ten of their thirty-three readings over the eleven populations outside 1%, the widest 1.0326. Killed by any of the three past 2% on the main set, which would say `-O2` reaches the shipped fill by more than either of its passes alone did.

(2) *The flag on the reference.* On the main set: `predict: cross list 1.17 within 3%`; on every class: `predict: cross list 1.19 within 5%` --- Run 30 read 1.1710 on the main set and 1.1726 to 1.2171 across the classes under `-fliberate-case`, which `-O2` includes, `list` being the reference every table is read against. Killed by `list` outside 3% of 1.17 on the main set, which would say `-O2`'s other passes move the reference beyond what LiberateCase alone did; either way the two columns cannot be subtracted, and every cross below carries the reference's own move.

(3) *The flag on the fused list.* The lazy stages' consumers fold one flat loop, `sumLazyRuns`, whose continuations are known, and `-fliberate-case` was worth 6 to 18 points to it on Run 30. On `runs`: `predict: cross libunord-stage7-sum 1.18 within 10%`; on `window`: `predict: cross libunord-stage7-sum 1.10 within 10%`; on `block`: `predict: cross libunord-stage7-sum 1.06 within 10%`; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, where the route fills or is one block: `predict: cross libunord-stage7-sum 1.0 within 1%`. Killed by the consumer inside 1% of level on `runs` and `block`, which would say `-O2`'s other passes take back what LiberateCase gave the list; or by it past 1.3 there, which would say the list's lead over the fill is the regime's.

(4) *The odometer over the table.* `liblist-stage4-sum` over `liblist-stage2-sum`, the lazy odometer's consumer against the port's base-offset table's. On `runs`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.47 within 25%`; on `block`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.73 within 25%` --- Run 30 reading 0.4678 and 0.3947, 0.7281 and 0.6800. Killed by the odometer's consumer behind the table's past the floor on the `o2` half, which would say the lazy list's lead over the branch's list is one regime's and not the design's.

(5) *Stage ten where both of its changes fire, and where one does.* `libunord-stage10-sum` over `libunord-stage7-sum` on `bcast`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.59 within 20%`; on `bcastmid`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.62 within 20%`; on `compose` the same span prints a figure and no verdict, read per view by hand, `compose-zero-mid` and the two `-bcast` views ahead past the floor and `compose-scalar` inside it. Over `libunord-stage9-sum` on `window`, both halves: `predict: pair libunord-stage10-sum libunord-stage9-sum 0.73 within 25%` --- Run 30 reading 0.7326 and 0.8863, the control's `window-128x128-k7` at 1.2538 being what widens the band. Killed by stage ten behind stage seven past the floor on both halves on any zero-stride view, or behind stage nine past the floor on both halves on any unstrided window view, which would say the two changes do not compose.

(6) *Stage ten where neither fires.* On `rev`, `scaled`, `runs` and `block`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 1.0 within 3%` --- `zerosOutermost` hands its list back unchanged where no stride is 0, so `routeUnord10` is `routeUnord7` there to the byte and only the extra pass over the axes separates the dispatches, which `rev` has shown for three runs, at 1.0244 and 1.0245 on Run 30. Killed by the pair past 3% on both halves on any of the four, which would say the pass is visible at a call of tens of microseconds by more than that. On `small` the span prints a figure and no verdict, read per view by hand: behind on the four views other than `small-bcast32` by the pass and ahead on `small-bcast32`.

(7) *`lib-stage1` against the lean fill with one fill under both.* Both fill through `fillStage2` since 2026-09-11, so on `bcast` and `bcastmid` the pair prices the dispatch alone. On `bcast` and on `bcastmid`, both halves: `predict: pair lib-stage2-lean lib-stage1 1.0 within 3%`. Killed by the pair past 3% on both halves on either class, which would say the canonicalization is worth that much to the fill on broadcast views. On `runs` and `block` the routes part, stage one concatenating a slice per run where the lean dispatch fills: `predict: pair lib-stage2-lean lib-stage1 0.23 within 20%` on `runs` and `predict: pair lib-stage2-lean lib-stage1 0.42 within 20%` on `block`, both halves, as Runs 28 to 30 all read; killed by the lean fill not ahead past the floor on either, which would say the slice-and-concatenate route no longer loses on short runs.

(8) *The floor with the fill family's pair for a third run.* On the main set and on every class, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa mut-odo-vecdims-add-in-leaf-u2 1.0` and `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa-distant mut-odo-vecdims-add-in-leaf-u2 1.0`, each inside the population's floor as Run 30's forty-four readings were; read beside them by hand which pair carries the whole-set floor on each half, `bq-expand-aa-distant` on Run 30's basis where Runs 28 and 29 read the fill pair's, which is what the floor entry's open question turns on. Killed by the adjacent copy outside the floor on both halves on the main set.

(9) *What the leaf fusion buys.* The shipped leaf against the family root, the one ordering the roster still prices that the library has taken. On the main set, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2 mut-odo-vecdims 0.64 within 5%` --- read on five runs, Run 29's 0.6441 and 0.6413 and Run 30's 0.6446 and 0.6525 the latest. Killed by the pair outside 5% on the `o2` half, which would say what the fusion buys is the regime's.

(10) *The allocation levels under the flag.* Off `--alloc` on both halves, by hand, over the eighteen main-set shapes other than `cnn-L1-6x6-c1`: on the basis the levels reproduce, the `libunord` consumers 0.00x, the fills and the `liblist` consumers 1.00x, `bq-expand` 2.76x and `list` 24.90x; on the `o2` half the same, `bq-expand` within 2% of 2.76x and `list` within 2% of 24.90x --- `-fliberate-case` took `list` to 25.26x on Run 30 and left `bq-expand` alone, where `-fspec-constr` took them to 22.38x and 2.06x on Run 29. Killed by any level moving past 2% on the `o2` half, which would say `-O2` changes what an arm allocates and not only how fast it runs.

(11) *The lean fill's unrolling and the leaf.* On the main set, both halves: `predict: pair lib-stage2-lean lib-stage2-lean-u1 0.96 within 5%`, the unrolling worth about four points under the lean dispatch, Run 30 reading 0.9622 and 0.9575; and `predict: pair lib-stage2-lean mut-odo-vecdims-add-in-leaf-u2 0.98 within 3%`, Run 30 reading 0.9914 at 13 of 19 and 0.9715 at 15 of 19. Killed by the lean fill behind its un-unrolled twin past the floor on either half, which would say the unrolling is worth nothing under this dispatch; or ahead of the shipped leaf by more than 5% on both halves, which would say the dispatch beats the leaf it wraps.

(12) *The lazy odometer's consumer against master's.* `liblist-stage4-sum` over `liblist-stage1-sum`, the odometer list under the lean dispatch against the slice recursion the library ships, both folded by `sumT`. Both halves, on `runs`: `predict: pair liblist-stage4-sum liblist-stage1-sum 0.37 within 25%`; on `block`: `predict: pair liblist-stage4-sum liblist-stage1-sum 0.46 within 10%`; on `rev`: `predict: pair liblist-stage4-sum liblist-stage1-sum 0.92 within 10%`; on `bcast`: `predict: pair liblist-stage4-sum liblist-stage1-sum 1.0 within 1%` --- Run 30 reading 0.3720 and 0.3205, 0.4616 and 0.4433, 0.9240 and 0.9162, 0.9983 and 0.9985. Killed by the odometer's consumer not ahead of master's past the floor on `runs` or `block` on either half, which would say the lazy list buys a fold nothing over what the library ships; or behind it past the floor on `bcast` on both halves, where both routes fill.

(13) *The zero-stride move alone.* `libunord-stage9-sum` moves every zero-stride axis outermost and otherwise keeps stage six's order. Over `libunord-stage7-sum`, both halves, on `bcast`: `predict: pair libunord-stage9-sum libunord-stage7-sum 0.59 within 20%`; on `bcastmid`: `predict: pair libunord-stage9-sum libunord-stage7-sum 0.62 within 20%`; on `compose`: `predict: pair libunord-stage9-sum libunord-stage7-sum 0.69 within 10%`; on `rev`, where neither reordering fires: `predict: pair libunord-stage9-sum libunord-stage7-sum 1.0 within 3%`; on `window`, where no view has a zero stride and the tie-break is what this stage lacks: `predict: pair libunord-stage9-sum libunord-stage7-sum 1.25 within 15%` --- Run 30 reading 0.5903 and 0.5588, 0.6215 and 0.5828, 0.6941 and 0.6886, 1.0208 and 1.0214, 1.3730 and 1.1344. Over `libunord-stage6-sum` on `bcast` and `bcastmid`, both halves: `predict: pair libunord-stage9-sum libunord-stage6-sum 0.59 within 20%` and `predict: pair libunord-stage9-sum libunord-stage6-sum 0.62 within 20%`, the tie-break moving nothing on those views, Run 30 reading 0.5905 and 0.5585, 0.6211 and 0.5832. Killed by stage nine behind stage seven past the floor on both halves on any `bcast` or `bcastmid` view, which would say the move costs where it should pay; or not behind stage seven past the floor on `window` on both halves, which would say the tie-break is worth nothing there.

(14) *What the composed route buys a fold.* `libunord-stage10-sum` over `libunord-stage1-sum`, stage seven's tie-break under stage nine's move against master's own consumer, both halves: on `runs`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.37 within 25%`; on `block`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.46 within 10%`; on `window`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.42 within 15%`; on `bcast`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.59 within 20%`; on `rev`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.53 within 10%` --- Run 30 reading 0.3720 and 0.3199, 0.4631 and 0.4436, 0.4157 and 0.3762, 0.5899 and 0.5577, 0.5334 and 0.5359. And where no pair of axes is tied on stride the tie-break is the identity and stage ten is stage nine: on `bcast`, `bcastmid` and `compose`, both halves, `predict: pair libunord-stage10-sum libunord-stage9-sum 1.0 within 0.5%`, Run 30 reading 1.0000 and 0.9999, 0.9997 and 0.9999, 1.0001 and 0.9998. Killed by stage ten not ahead of master's consumer past the floor on any of the five populations on either half; or by it parting from stage nine past the floor on both halves on any of the three, which would say the tie-break fires where no pair is tied.

(15) *What the walker's bangs leave the list interface.* `libunord-stage10-list-sum` over `libunord-stage10-sum`, base's `sum` over stage ten's list against the harness's own fold through `sumLazyRuns`: the same route under both, differing in nothing but whether it is inlined into the fold or met through one loop compiled once for every stage --- Run 28's item (12) asked again under the composed route, the arm landing 2026-09-13. Under stage six the user's fold was AHEAD, the shared loop having taken the vector boxed and re-entered it every run for its length and address; the bangs of 2026-09-13 have worker/wrapper pass those fields unboxed and leave the Core byte-identical with `-fliberate-case` and without, so the pair should read level on both halves, as Run 30's flagged half already did where its plain -O1 basis did not. On the main set, both halves: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0 within 2%`; on `runs`, `window`, `block`, `flip` and `small`, the five classes on which Run 30's stage-six pair read below level, both halves: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0 within 3%`; on `bcast`, `bcastmid` and `compose`, where stage ten's move puts a repeated slice on a runs route and Run 30's stage-six pair, level there, gives no prior, both halves: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0 within 3%`; on `rev` and `scaled`, where neither reordering fires and both consumers fold one vector, both halves: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0`, each inside the population's floor --- Run 30 read the stage-six pair at 0.9999 on the main set, 0.8495, 0.7610, 0.9545, 0.9788 and 0.9907 on those five classes and 0.9999 and 1.0011 on `rev` and `scaled`, all on its plain -O1 basis, and at 1.0013, 1.0039, 1.0147, 1.0177, 1.0099, 1.0030, 1.0005 and 0.9977 under `-fliberate-case`. Killed by the pair outside its band on both halves on any population named here; the reading to watch is `runs` and `window`, where the user's fold was fifteen and twenty-four points ahead on that basis, and where it staying ahead would say the bangs did not reach the shared loop.

**Twelve held and three were killed --- and the pattern of the last three runs BREAKS: two of the three kills fell on items predicting an EFFECT.** Run 28's four kills, Run 29's three and Run 30's two all fell on items predicting a NO-OP, and not one on an item predicting a real effect. This run's (2) predicted that `-O2` would move `list` by about the sixth `-fliberate-case` moved it and it moved three tenths; its (5) predicted that stage ten's two reorderings would compose and on three `window` views they cancel. Only (10), which predicted the allocation levels would reproduce under `-O2`, is a no-op killed. Every item's verdict below is its KILL CONDITION applied across the populations that item names, never one sweep of the main set; every figure is re-derived from this run's own JSONs, by `--predictions` over all eleven populations on both halves and by `--pair --per-shape` where an item's clause is per view; and a span is judged against the floor of the population it is read on. **One thing the registration could not foresee governs how it reads.** Eight of its fifteen items quote Run 30's figures as their predictions, and those figures were taken before the bangs of 2026-09-13 reached the stage-ten walker's vector and carry offset --- so an item whose subject those bangs moved missed its span for a reason that is the source's and not the level's, which is (3) exactly.

(1) *The flag on the shipped fill.* **HELD, where Run 30's was killed.** On the main set the three arms read **0.9956**, **0.9972** and **0.9997**, the widest 0.44 of a point off against a 2% bar, so the kill --- any of the three past 2% on the main set --- does not fire. Run 30 read 0.9891, 1.0003 and 1.0093 under `-fliberate-case` and lost on the leaf by 0.09 of a point. **So the whole level costs the shipped leaf LESS than `-fliberate-case` alone did**, 0.44 of a point against 1.09, and MORE than `-fspec-constr` alone, which read 0.01 there; across the three arms the level's widest reading is 0.44 where SpecConstr's was 0.55 and LiberateCase's 1.09. Either way it is the reverse of what the item was re-based to expect, and it is the clearest sign in this file that `-O2` is not its passes added up. **Its class readings are one short of clean**: twenty-nine of the thirty hold inside the registered 4%, and `lib-stage2-lean` on `small` reads **1.0494**, 4.94 points off and the only class reading outside; ten of the thirty sit outside 1%, over eight populations and reading both ways, which is the same count Run 30 read on a different set of populations.

(2) *The flag on the reference.* **KILLED, and it governs the whole file.** `cross list 1.17 within 3%` reads **1.2974** on the main set, 12.74 points off. The class clause, `1.19 within 5%`, fails on nine of ten: `bcastmid` 1.2372 is inside it and `rev` 1.2749, `small` 1.2990, `window` 1.2904, `runs` 1.3194, `scaled` 1.3257, `flip` 1.3360, `block` 1.3408, `compose` 1.3532 and `bcast` 1.3835 are not. So `-O2` moves the reference by three tenths where `-fliberate-case` moved it by a sixth, and the item's own clause follows exactly: the two columns cannot be subtracted, and every `cross` in this run is an ordering.

(3) *The flag on the fused list.* **HELD --- NEITHER KILL FIRES --- and the one span that missed did so because the code moved under it.** `libunord-stage7-sum` across the halves reads **1.0243** on `runs` against a registered 1.18, 15.57 points off and the only miss; **1.0142** on `window` against 1.10 and **1.0004** on `block` against 1.06, both inside the registered 10%; and 0.9976 to 1.0022 on the five fill classes, all inside 1%. The first kill wants the consumer inside 1% of level on `runs` AND `block`: `block` is, at four hundredths of a point, and `runs` is not, at 2.43, so it does not fire; nothing is past 1.3 either. **What moved is the source and not the level.** The item quoted Run 30's 18.27, 10.22 and 6.33 points for `-fliberate-case` ALONE on these three populations, and the bangs of 2026-09-13 were written to retire exactly that bite; the whole level, which turns that pass on among others, is now worth 2.43, 1.42 and 0.04 points there.

(4) *The odometer over the table.* **HELD.** `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.3853** on the basis and **0.4063** on the control on `runs`, against a registered 0.47 within 25%, and **0.6806** and **0.7479** on `block`, against 0.73. The kill wants the odometer's consumer BEHIND the table's on the `o2` half and it is ahead by better than half and by a quarter, so the lazy list's lead over the branch's list is the design's and not this level's either --- which is the third regime it has survived.

(5) *Stage ten where both of its changes fire, and where one does.* **KILLED, on the second of its two conditions, and it is the run's sharpest class-level finding.** Over stage seven it reads **0.5497** and 0.5491 on `bcast` against a registered 0.59 and **0.5715** and 0.5686 on `bcastmid` against 0.62, both halves inside 20%, with `compose` at 0.6870 and 0.6851 and no verdict as registered. Over stage nine on `window` it reads **0.9557** on the basis, inside the registered 25% of 0.73, and **0.9847** on the control, 25.47 points off and outside, where Run 30 read 0.8863 there. The second kill wants stage ten behind stage nine past the floor on both halves on some unstrided `window` view, and THREE views do it: `window-128x128-k7` at **1.3666** and **1.3689**, `window-64x64-k1x9` at 1.2355 and 1.2359 and `window-224x224-k3` at 1.0785 and 1.1985, against floors of 0.58% and 0.23%. **Runs 29 and 30 both found the first two views parting in SIGN between their halves, which is what let the item survive twice**; this run's two halves agree and what they agree on is the kill. The mechanism reads off the worst view: stage nine beats stage seven there at 0.7321 and stage ten hands all of it back, `libunord-stage10-sum` over `libunord-stage7-sum` reading **1.0006** on that shape and 1.0023 on the control. The first kill does not fire, the closest zero-stride view being `compose-scalar` at 1.0002 and 1.0004 against floors of 0.32% and 1.10%.

(6) *Stage ten where neither fires.* **HELD, and `rev` reproduces Run 30's figure to four places.** The pair reads **1.0244** on the basis and **1.0241** on the control on `rev` --- Run 30 read 1.0244 and 1.0245 on the same recipe --- with `scaled` at 1.0046 and 1.0024, `runs` at 1.0014 and 0.9991 and `block` at 1.0018 and 1.0021, every one inside the 3% the item was amended to on 2026-09-13. So the extra pass over the axes is visible on `rev` for the fourth run running, at a call of tens of microseconds, and the item records it rather than dying of it. On `small` the span prints **1.1910** and **1.1768** and no verdict, and its per-view clause lands exactly: behind on `small-flat64` at 1.3819, `small-patch-k5` at 1.4592, `small-patch-r5` at 1.2926 and `small-row96` at 1.2607, and ahead on `small-bcast32` at 0.7294.

(7) *`lib-stage1` against the lean fill with one fill under both.* **HELD, and it reproduces Run 28's figures for a fourth run.** On `bcast` the pair reads **0.9896** and **0.9942** and on `bcastmid` **0.9972** and **0.9972**, all inside the registered 3%, so the canonicalization is worth nothing to the fill on broadcast views once both routes fill through `fillStage2` --- now under a whole optimisation level. Where the routes genuinely part the item registered Run 28's own readings and both come back: **0.2317** and **0.2302** on `runs` against 0.23, and **0.4190** and **0.4299** on `block` against 0.42.

(8) *The floor with the fill family's pair for a third run.* **HELD in every one of its forty-four readings**, two spans over eleven populations on two halves, and the kill --- the adjacent copy outside the floor on both halves on the main set --- does not fire: it reads **0.9983** on the basis against a 0.61% floor and **0.9870** on the control against a 1.58% one. **Read beside it by hand, as the item asks, the two halves answer differently.** On the BASIS the whole-set floor is `bq-expand-aa-distant`'s at **0.61%** and equals that pair's carry-back figure, which is Run 30's answer repeated; on the CONTROL it is the fill pair's, at **1.58%**, and only because two wild `cnn-slice-c32` cells put it there. So the open question in the floor entry has a fourth reading, the answer is now half one and half the other on a single pair of halves, and there is still no trend.

(9) *What the leaf fusion buys.* **HELD.** `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6417** on the basis and **0.6436** on the control against a registered 0.64 within 5%, nineteen ten-thousandths apart, and the kill --- the pair outside 5% on the `o2` half --- does not fire. Run 29 read 0.6441 and 0.6413 and Run 30 0.6446 and 0.6525, so six readings across three runs now lie between 0.6413 and 0.6525, under two flags, a whole level and two rosters.

(10) *The allocation levels under the flag.* **KILLED, and it is the one no-op among this run's three kills.** On the basis the levels reproduce exactly as registered: the `libunord` consumers 0.00x, the fills and the `liblist` consumers 1.00x, `bq-expand` **2.78x** against the registered 2.76x and `list` **25.20x** against 24.90x, both inside 2%. On the `o2` half they do not: `bq-expand` reads **2.11x**, 23.6% below the registered 2.76x, and `list` **23.45x**, 5.8% below the registered 24.90x, where the kill is any level moving past 2% there. The class blocks say the same in their own currency --- `bq-expand` 1.00x to 2.75x on the control against 1.00x to 3.86x on the basis, `list` 19.00x to 26.55x against 20.99x to 27.66x --- while the fills and both consumer families sit unmoved at 1.00x and 0.00x. So `-O2` changes what the two arms it speeds up ALLOCATE and not only how fast they run, which is what the item was written to ask and the answer the two passes read one at a time did not give.

(11) *The lean fill's unrolling and the leaf.* **HELD on both spans and neither kill fires.** `lib-stage2-lean` over `lib-stage2-lean-u1` reads **0.9374** on the basis and **0.9466** on the control against a registered 0.96 within 5%, so the unrolling is worth six points under the lean dispatch and not four; over the shipped leaf it reads **0.9863** and **0.9823** against 0.98 within 3%. The lean fill is not behind its un-unrolled twin on either half, and it is ahead of the shipped leaf by under two points on both, where the kill wants more than five.

(12) *The lazy odometer's consumer against master's.* **HELD on all four of its spans.** `liblist-stage4-sum` over `liblist-stage1-sum` reads **0.3086** and **0.3037** on `runs` against a registered 0.37, **0.4299** and **0.4348** on `block` against 0.46, **0.9172** and **0.9196** on `rev` against 0.92, and **0.9983** and **0.9976** on `bcast` against 1.0 within 1%. Neither kill fires: the odometer's consumer is ahead of master's past the floor on `runs` and `block` on both halves, and it is not behind master's on `bcast` on either.

(13) *The zero-stride move alone.* **HELD on both kills, with the `window` span missed on both halves --- and the miss is the same finding (5) is killed by.** Over stage seven, `libunord-stage9-sum` reads **0.5497** and 0.5493 on `bcast` against a registered 0.59, **0.5716** and 0.5687 on `bcastmid` against 0.62, **0.6872** and 0.6855 on `compose` against 0.69, and **1.0214** and 1.0202 on `rev` against 1.0 within 3%, all inside. On `window` it reads **1.0533** and **1.0197** against a registered 1.25, 19.67 and 23.03 points off and outside the 15% on both halves. Over stage six on `bcast` and `bcastmid` it reads 0.5505 and 0.5491, 0.5715 and 0.5682, the tie-break moving nothing on those views as registered. The first kill wants stage nine behind stage seven on some `bcast` or `bcastmid` view and no view is; the second wants it NOT behind stage seven on `window` on both halves, and it IS behind past both floors, so neither fires. **What the miss says is that the tie-break is worth five points on `window` where Run 30 measured it worth thirty-seven**, which is where stage ten's two changes stopped composing.

(14) *What the composed route buys a fold.* **HELD in every one of its readings.** `libunord-stage10-sum` over `libunord-stage1-sum` reads **0.3089** and **0.3036** on `runs` against a registered 0.37, **0.4319** and **0.4352** on `block` against 0.46, **0.3635** and **0.3573** on `window` against 0.42, **0.5495** and **0.5476** on `bcast` against 0.59 and **0.5372** and **0.5346** on `rev` against 0.53, so the composed route is ahead of master's consumer past the floor on all five populations on both halves. And where no pair of axes is tied on stride, stage ten IS stage nine: 1.0000 and 0.9997 on `bcast`, 0.9999 and 0.9998 on `bcastmid` and 0.9997 and 0.9994 on `compose`, every one inside the registered 0.5%.

(15) *What the walker's bangs leave the list interface.* **HELD, and it answers the question the arm was landed for.** `libunord-stage10-list-sum` over `libunord-stage10-sum` reads **1.0002** on the main set inside 2%, and on the eight classes given a 3% band it reads 1.0132 and 1.0225 on `runs`, 1.0154 and 1.0273 on `window`, 1.0170 and 1.0126 on `block`, 1.0080 and 1.0091 on `flip`, 1.0133 and 1.0187 on `small`, 1.0070 and 1.0347 on `bcast`, 1.0015 and 1.0052 on `bcastmid` and 1.0040 and 1.0028 on `compose`; on `rev` and `scaled` it reads 1.0000 and 0.9997, 1.0002 and 1.0048, inside those populations' floors. The kill wants the pair outside its band on BOTH halves on some named population and only `bcast`'s control half is outside, at 1.0347. **The reading to watch was `runs` and `window`, and it has reversed.** Run 30 read the stage-six pair at **0.8495** and **0.7610** there on this same plain -O1 recipe, the user's own fold fifteen and twenty-four points AHEAD of the harness's shared loop; under stage ten with the bangs of 2026-09-13 the two are level to within two points on every population and the user's fold is marginally the slower. So the bangs reached the shared loop, which is what the item said staying ahead would have denied.
