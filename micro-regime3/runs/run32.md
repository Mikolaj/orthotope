# Run 32 (GHC HEAD against ghc-9.12.4, both at plain -O1)

One run's write-up: its head, its Results, what the next run compares against, the properties that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 32 (GHC HEAD against ghc-9.12.4, both at plain -O1): the compiler is worth at most 1.61 points to any arm, and clears this run's own cross-half noise on three of the eight strategies.** The pair is Run 31's basis recipe built twice --- once with the 9.12.4 on PATH through `cabal.project` and once with the in-tree stage1 of the GHC checkout, `10.1.20260803`, through `cabal.project.ghead` --- one source, one shim, one shim environment, one regime, one roster and one bench order, the 9.12.4 half publishing as Run 31's did, so `the basis` below is that half and every `cross` figure reads basis over control. Over the sixteen main-set arms that carry a cross-half figure, TWELVE sit within 1% of 1 and the four outside reach 1.61 points: `mut-odo-vecdims-add-in-leaf-u1` at **0.9839**, `mut-odo-vecdims` at 0.9896 with its adjacent A/A copy at 0.9898, and `lib-stage2-lean` at **1.0134**. **The bar an arm has to clear to be the compiler's rather than the run's is 0.81 points** --- the widest an arm and its own A/A duplicate part in this same cross-half reading, `mut-odo-vecdims-aa-distant` against `mut-odo-vecdims` --- and THREE of the eight strategies clear it: `mut-odo-vecdims-add-in-leaf-u1`, `lib-stage2-lean` and `mut-odo-vecdims`, where `lib-stage1` at 0.58, `lib-stage2-lean-u1` at 0.64, the shipped leaf at 0.52, `bq-expand` at 0.73 and `list` at 0.43 do not. **Not one arm moves past 3%**, `--movers` naming none over the sixteen, and the geomean over the arms is **0.9985** --- the closest to level of the nine pairs this chapter still holds both halves of, Run 28's compiler pair second at 1.0028 and also naming no mover. **What that says against the six compiler pairs before it is this run's own finding.** Runs 19 and 24 to 28 all varied the compiler with `-fspec-constr` on BOTH halves, that being the published basis's regime at the time; this is the first compiler pair taken at the plain -O1 `Data/Array/Internal.hs` actually compiles under, and at that level HEAD moves no arm of this roster by more than 1.61 points. **And the reference barely moves either**: `list` reads **1.0043**, INSIDE the 0.7% bar that lets two columns be differenced, which is what Runs 29, 30 and 31 could say of none of their eleven populations and what four of the six compiler pairs whose halves are still on disk can, this run among the six and Run 19 not.

**The roster is 32 timed arms over 19 main-set shapes and 608 benches, with 58 class views over ten classes for 1856 more, and it is NOT Run 31's.** `./roster-delta.py run31-nospec run32-nospec` reads 31 arms to 32 over 19 shapes to 19: ONE in and NONE out, the 31 survivors in the same order, no shape moved and no class view moved --- `runs` stays at fourteen, `window` at eight, `bcast` and `flip` at six, `block` and `small` at five, `bcastmid` and `compose` at four, `rev` and `scaled` at three. In, landing at `4488631`, is `liblist-stage4-list-sum`, base's `sum` over stage four's list. **And the source moved under unmoved names besides**: `4488631` also made the shared loop `sumLazyRuns` sum each run through `sumNoSpec`, without vector's `SPEC` argument, which reaches every lazy consumer wherever a view routes to runs, and `08ee255`, `e12b000` and `f95795a` took an unzip immediately undone by a zip out of the zero-stride orders and threaded the canonicalization on pairs. So this pair's source term is ABSENT WITHIN the pair, both halves being `f95795a`, and PRESENT ACROSS runs: a figure that moves between this basis and `run31-nospec` carries a source term as well as the box's, and the two are not separable from inside this run. The L1 roster pass was therefore OWED, step 12's condition asking for a membership change, and it was taken whole --- eleven legs over all ten classes and the main set, every one `pass clean`.

**NOT ONE CELL SINKS BELOW THE SHARED FORCING PASS ON EITHER HALF**, as on Runs 28 to 31 and for the same two reasons: the ruling of 2026-09-10 that a reducing consumer has no corrected time, so the `-sum` rows read `--` in `time` and `worst` by design, and the retirement of every Fill arm over a list to `check`. `read-all.sh --brief-facts` reports an empty sunk list for both halves, so every row that carries a corrected time at all is a geomean over all nineteen shapes. The L1 roster pass had said so a day early, on eleven populations: the reader's own warning string, `forcing term is not smaller`, is absent from all eleven of its legs and from the smoke sweep beside them.

**The gate read SOUND, the machine check did NOT fire, and the box has not moved.** The gate's soundness is read as every pair's here is, on the two palindrome passes AGREEING rather than on any of them sitting near 1: `mut-odo-vecdims` 0.9901 against 0.9949, `bq-expand` 0.9837 against 0.9925 and `list` 0.9902 against 1.0043, apart by **0.48%**, **0.89%** and **1.42%** of themselves --- which is Run 27's spread arm for arm, that compiler pair's three parting by 0.44%, 0.93% and 1.36% in the same order, and narrower than Run 30's 1.13% to 3.13%. All three sit near 1 as every compiler pair's here have, and `list` crosses between the passes, 15 of 19 shapes faster on the basis in the -a pair and 5 of 19 in the -b, which is a tie at 1 seen through five benches. **The MACHINE CHECK then did not fire**, reading `list`'s net at a geomean of **+0.86%** against the fingerprint `runs/run31.md` keeps, inside the 3% bar, worst `cnn-L1-24x24-c1` at +3.03% and 0 of 19 shapes past 5%. That is a LIKE-FOR-LIKE reading, Run 31's published half being plain -O1 dead-spot 9.12.4, which is this basis's recipe exactly. **And the cross-run reading agrees and adds the term the check cannot see.** Against `run31-nospec`, the previous build of this very recipe, the sixteen shared timed arms span **0.9839** on `lib-stage2-lean-u1` to **1.0149** on `lib-stage2-lean`, with `list` at **1.0031**, and read as a ratio to `list` within each run the other fifteen give a `--bridge` geomean of **0.9976**, none of them outside the 3.3% drift band. That distance is not the box's alone, `Main.hs` having moved six times under an unchanged recipe; what it says is that neither the box nor those six commits moved any timed arm of the main set by more than 1.5%.

**Every one of the twenty-two processes gated clean --- `--selftest` and both `--aa` gates --- and the PLATEAU GATE DID NOT FIRE, where Runs 29's, 30's and 31's did.** It reports the preamble's victim spanning 21.0965 to 21.5398 ms/iter across the whole run, a **2.10%** spread against a 5% band, and the two halves OVERLAP rather than sitting apart: `nospec` 21.0965 to 21.4842, 1.84%, and `ghead` 21.1113 to 21.5398, 2.03%. **The saturation STATE does not part by half either.** All twenty-two processes assert ONE `keep` and ONE `inuse`, where Run 31's `inuse` took a distinct value per half because `-O2` changed what the spray left resident. The pair note declined to declare a state gate for exactly that reason --- a compiler pair has no such argument, and none of Runs 24 to 28 declared one --- and the reading bears the declining out. FIVE PROCESSES carry an A/A worst cell above 5%, `read-all.sh`'s column being one figure a process: `ghead-main` at **16.12%** on `cnn-slice-c32`, `nospec-window` at 9.51%, `ghead-flip` at 8.11%, `nospec-main` at **6.62%** on `gather48-src-50` and `ghead-window` at 5.60%. Every one is a WILD cell and not an intrusion: `--wild` clears all twenty-two of the sequence's logs, no bench in any of them reaching 0.25 of a core.

**The sequence ran in ONE window and no process of it was intruded on --- but a GATE process was, by this session, and the instrument that says the sequence is clean is the one that caught it.** Twenty-two processes from 2026-09-15T00:35:17+02:00 to 07:42:54+02:00, every one exiting 0 at the count its population asks for, with no hole between one process finishing and the next starting, no complaint, and no `!!` line anywhere in the wall-clock record; the ghead half ran first throughout. `--wild` over the hundred and fourteen logs this run wrote clears all twenty-two of the sequence's, all eighty-eight alone-leg logs --- twenty-two legs a half in each of the two states, the nineteen main-set shapes and the three anchors on a second reps pass --- and three of the four gate processes. **The fourth is `run32-gate-nospec-a`**, which carries 2 of its 95 benches at or above 0.25 of a core, peak **0.82**, both on `cnn-L1-6x6-c1` --- and the intruder was this session reading documents, which the run list puts *while it runs* and which therefore lands inside the gate, the evening's first half hour, rather than inside the hours the instruction has in view. The gate's verdict survives it: dropping that shape from both passes moves no spread by more than 0.17 of a point. No figure this file publishes comes from a gate process, so what the finding costs is nothing here and belongs to the chapter. **What the clean logs leave is wild cells, which are a different thing and this run has five**, the largest `ghead-main`'s **16.12%** on `cnn-slice-c32` --- the same shape Run 31's two fell on --- every one with the RTS totals flat and no foreign CPU beside it.

**What the compiler is worth is the same little on every population, and the fill family is inside its registered band on all but two of thirty class readings.** `list` moves by under half a point on the main set and by 0.06 to 2.27 points across the ten classes --- `runs` 1.0006, `bcast` 0.9987, `window` 0.9949, `flip` 1.0053, `block` 1.0055, `bcastmid` 1.0059, `scaled` 1.0119, `rev` 0.9855, `small` 1.0157 and `compose` 1.0227 --- where Run 31's level moved it 23.72 to 38.35 points on the same ten. **The three shipped-fill arms registration (1) names hold inside the registered 4% on every class but `window`**, where `lib-stage1` reads **1.0528** and `lib-stage2-lean` **1.0510** while the leaf holds at 1.0262; fifteen of their thirty class readings sit outside 1%, over nine of the ten populations and reading both ways, `lib-stage1` and `lib-stage2-lean` accounting for six apiece and the leaf for three. On the MAIN SET, which is where the item is adjudicated, the three read **1.0052**, **1.0058** and **1.0134**, every one inside the registered 2%.

**The counted work covers every population, no cell was refused anywhere, and its finding is that the two compilers emit the SAME WORK.** Twenty-two sweeps from 07:56:21 to 08:38:33, `runs` longest at 406s and 405s for the pair and `rev` shortest at 13s each, 2532s over the twenty-two, with no `perf could not count` line in any of the eleven pairs of files --- and NINE of the eleven basis legs came in above their `run31-nospec` counterparts with the other two level at the reader's resolution, `flip` at 97s and `rev` at 13s on both runs, which is what the note's derived scale asked of a roster that gained a timed arm and lost none. Over the sixteen main-set arms that carry a corrected time the counts geomean is **1.0053**, and the WHOLE RANGE is **0.9996 to 1.0101**: `mut-odo-vecdims` with its two A/A copies at the bottom and `list` with its two at the top, and the ten fill-family arms running **0.9996 to 1.0081** across that lower part of the range. Run 31's `-O2` half ran 1.0380 to 1.4423 on those same arms and read a partition of three groups; this one reads no partition because there is nothing to partition. **And nothing converts, because there is nothing to convert**: `time/counts` runs 0.9760 to 1.0071 over the sixteen, the widest being `mut-odo-vecdims-add-in-leaf-u1`, the one arm whose time moved past a point and a half, then `bq-expand` and its adjacent copy at 1.03 and 1.09 points and `mut-odo-vecdims` at 1.00, and every one of the other twelve inside a point. **Per half the two legs are within two seconds of each other on every population**, 1269s against 1263s in all, where Run 31's flagged half ran about a fifth below its basis on all eleven. So HEAD walks this roster's cells doing the same instructions in the same number, which is what a null looks like from the one instrument that owes criterion nothing.

**This run's two columns MAY be differenced on seven of the eleven populations**, which is what a pair whose variable does not reach the reference looks like. It rests on `list` having moved **0.43 points** on this run's main set --- the first main set inside the 0.7% bar since Run 28's, Runs 29, 30 and 31 having read 12.12, 17.10 and 29.74 points there, each basis over its own control --- and on `read-all.sh --brief-facts` putting `runs` (1.0006), `bcast` (0.9987), `window` (0.9949), `flip` (1.0053), `block` (1.0055) and `bcastmid` (1.0059) inside it beside the main set. FOUR are past it: `scaled` at 1.0119, `rev` at 0.9855, `small` at 1.0157 and `compose` at 1.0227. On those four every arm-by-arm figure across the halves in this file is an ORDERING and not a subtraction, and each says so in its own cross-half line; on the other seven it is a measurement, which is what Runs 29, 30 and 31 could say of none of their eleven.

**The fastest timed arm on the main set is `lib-stage2-lean`, and this run the published column cannot be read for the order behind it.** `lib-stage2-lean` reads **0.025** in the `time` column with `lib-stage2-lean-u1` printing the same 0.025, the shipped leaf and its two A/A copies at 0.027, `mut-odo-vecdims-add-in-leaf-u1` and `lib-stage1` at 0.028, `mut-odo-vecdims` at 0.045 and `bq-expand` at 0.127. **Paired, the ordering is not the column's.** Over `lib-stage1` the lean fill reads **0.9473** at 12 of 19, over its own unrolling twin **0.9669** at 13 of 19, and over the shipped leaf **0.9983** at 11 of 19 and sign p 0.65, which is a tie rather than a lead. **The two part on `lib-stage2-lean-u1`, and the reason is the column's own estimator.** `time` is a winsorized geomean and that row sits astride a cap: its plain per-shape geomean of net over `list`'s net is **0.02978** where the published figure is 0.02514, the same four of its nineteen cells capped in each run and pulled to a far lower ceiling in this one, and on `run31-nospec` --- the same recipe, the same arm --- the plain geomean was 0.03036 and the published figure 0.029. So the published column moves that arm by FOURTEEN points between two runs whose cross-run paired reading is **0.9839**, one and a half. Dividing two rows of the table for a margin was already forbidden; what this run adds is that one row's own movement between runs is not the arm's either, on a row whose shapes span sevenfold and whose cap BAND halves between the two runs.

**Stage ten composes its two changes where both fire, costs a pass where neither does, and on `window` the cancellation Run 31 found REPRODUCES --- so registration (5), killed on that run and re-registered at what it read, HOLDS here.** Over stage seven it reads **0.5478** on the basis and 0.5545 on the control on `bcast` and **0.5669** and 0.5780 on `bcastmid`, against 0.55 and 0.57 registered, with `compose` at 0.6853 and 0.6859 and no verdict, as registered. **Neither kill fires.** No zero-stride view has stage ten behind stage seven past the floor on both halves, the closest being `compose-scalar` at 0.9986 and 1.0004 against floors of 0.61% and 0.37%. And of the three `window` views Run 31 found cancelling, TWO are behind stage nine past the floor on both halves again --- `window-128x128-k7` at **1.3899** and **1.2707**, `window-64x64-k1x9` at 1.2512 and 1.2166 --- while the third, `window-224x224-k3`, is behind on the basis at 1.1894 and AHEAD on the control at 0.9817, which is not the *on both halves* the kill asks for; the three views Run 31 found ahead are ahead on both halves here, at 0.7549 and 0.7042, 0.6799 and 0.5740, 0.8476 and 0.6883. **What the span itself did is miss on the control half**: `predict: pair libunord-stage10-sum libunord-stage9-sum 0.96 within 5%` reads 0.9868 on the basis, inside, and **0.8982** on the control, 6.18 points off and outside --- HEAD deepening what stage ten hands back rather than removing it. **Where neither change fires the pass still costs and registration (6) holds**: `rev` reads **1.0111** and 1.0097, `scaled` 1.0000 and 1.0017, `runs` 1.0003 and 0.9999 and `block` 1.0009 and 1.0017, every one inside the 3% that item carries. `small` prints 1.0477 and 1.0649 and no verdict, and its per-view clause lands exactly: behind on `small-flat64`, `small-patch-k5`, `small-patch-r5` and `small-row96` and ahead on `small-bcast32`, at 0.6326 and 0.6471.

**The compiler does not move the fused list either --- but registration (3) is KILLED on `runs` by nine hundredths of a point.** Read raw over every shape, as an arm with no corrected time must be, `libunord-stage7-sum` across the halves reads **0.9691** on `runs`, **0.9729** on `window` and 0.9935 on `block`, and 0.9955 to 1.0167 on the other eight populations. The item's kill is the consumer past 3% on `runs` or `block` on either side of 1: `runs` is **3.09** points off and FIRES, `block` at 0.65 does not, and `window` at 2.71 sits inside the 3% its own span carries. So HEAD compiles the walker or its sum some three percent faster on the class whose views route to runs, and the counts are what say which. **Items (15) and (16) read the loop change from the other side, on the two arms landed for it, and both HOLD.** `libunord-stage10-list-sum` over `libunord-stage10-sum` reads **1.0013** and 1.0002 on the main set, 1.0201 and 1.0108 on `runs` and 1.0272 and 1.0215 on `window`; `liblist-stage4-list-sum` over `liblist-stage4-sum`, the arm that landed this run, reads 1.0008 and 1.0002 on the main set, 1.0224 and 1.0105 on `runs` and 1.0041 and 1.0033 on `window`. Neither kill fires --- neither pair is inside 1% of level on `runs` and `window` both --- and the two arms agree with each other to within **0.23 of a point** on `runs` and `block`, inside both populations' floors, which is (16)'s second kill and it does not fire either. So the user's own fold and the harness's shared loop price the same route alike on both compilers, and the `SPEC` argument the shared loop dropped on 2026-09-14 is worth what a run's length shows and no more.

**The three class properties hold in all ten classes on both halves, and on the main set property 1's `bq-expand` clause parts BETWEEN the halves, on the one shape that was already a tie.** `mut-odo-vecdims`'s `worst` stays under 1 everywhere: 0.112 on the basis's main set and 0.113 on the control's, and between 0.028 and 0.086 in the classes, lowest on `compose` and highest on `small`. It is ahead of `bq-expand` on every shape of every class on both halves, the closest any class cell comes being `flip-last-rows` at 0.4849 on the basis and `small-patch-k5` at 0.4939 on the control --- but on the MAIN SET it is ahead by **0.66 of a point** on the basis, at 0.9934 on `stretch-pow2stride`, and BEHIND by three hundredths on the control, at **1.0003** on the same shape. Run 31 read that cell at 1.0009 and 1.0109 and called the clause broken for the first time; this run holds it on one half and breaks it on the other, the basis's margin equal to its own 0.66% floor and the control's a twentieth of its 0.68% one, so neither reading clears the floor it is read against. Both allocation clauses of property 2 hold with their 1% margin on every population and both halves. **And property 3's tiers are IDENTICAL between the halves, cell for cell**: the fills at the result vector, `bq-expand` between 1.00x and 3.86x it and `list` an order of magnitude above at 20.99x to 27.66x, every one of those ten class triples reading the same on the control as on the basis, as the main set's 1.00x, 2.78x and 25.20x do. That is registration (10) HELD --- where `-O2` took `bq-expand` to 2.11x and `list` to 23.45x on Run 31, HEAD moves nothing any arm here allocates. **And `small` keeps the reservation it has carried since 2026-09-07**: the `mut-odo-vecdims` family reads 1.27x on each half, the per-call constants showing through a call under a microsecond, so that class's tier reading is the family's alone.

**Across the halves not one population's geomean is more than a point and a quarter from level, and there is nothing for the vote to attribute.** The basis is the faster half on 9 of the 16 arms on the main set, on 15 of 16 in `bcast`, 11 in `small`, 10 in `rev`, 9 in `block`, 8 in `window`, 5 in `runs`, 3 in `bcastmid` and `flip`, 2 in `compose` and NONE in `scaled`, at geomeans from **0.9923** on `small` to **1.0110** on `compose` and **0.9985** on the main set. Run 31's eleven ran 1.0686 to 1.1446 and every one of them was disqualified by `list` having moved past the differencing bar; seven of these eleven are not disqualified at all, and the four that are move `list` by 1.19 to 2.27 points where Run 31's moved it by thirty. **What Run 31 had to do to find where its movement lived, this run has no use for.** Dropping the `list` and `bq-expand` families --- six arms, two leads and four A/A copies --- took Run 31's main set from 1.1004 to 0.9986; here it takes 0.9985 to **0.9988**, three ten-thousandths, because no family was carrying anything to drop.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The two `sum-only` halves agree to within a third of a percent on every population, from 0.9988 to 1.0002 on the basis, `compose` to `bcastmid`, and 0.9995 to 1.0033 on the control, `bcast` to `small`, so the term subtracted from one half is the term subtracted from the other and no ratio here is an artefact of a moved forcing pass. It matters less on this pair than on the last three, whose halves parted on the reference by tenths; here the two absolutes are within half a point of each other and a correction that had parted with them would have had little to put anywhere.

**The two halves resolve alike, which is new, and the run carries no cell the reader distrusts on the basis at all.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **1.00** over the 32 arms, 16 of them wider on the basis and 16 narrower, where Run 31's pair read 0.95 and split 8 against 23, Run 30's 0.92 and Run 29's 0.97. Read it beside the A/A floor and not off it: this column is sampling error INSIDE one bench where the floor is agreement BETWEEN two placements of one arm. Here the two agree for once --- the floors are **0.66%** on the basis and **0.68%** on the control, a hundredth of a point apart, where Run 31's stood at 0.61% against 1.58%. **And NOT ONE cell of any of the basis's eleven populations sits below R2 0.99**, nor under ten samples, the reader's warning channel staying silent across all of them; the control half carries exactly one, `window-224x224-k3-s2/libunord-stage6-loop-sum` at 0.9867. That channel is not silent for want of a voice: run on Run 31's basis classes it names one cell apiece on `bcast`, `runs` and `window`.

**The straddling loops stand at EIGHT on each half, and the pair sits in the compiler series on both columns --- read off the binaries today rather than off what each run recorded, because those two readings disagree.** `--survey` reads **292** self-loops of at most 64 B on the basis, 134 of them at offset 0 and 8 still straddling, against **311** on the control, 130 at offset 0 and 8 straddling. The halves part by 19 loops where Run 31's parted by 78, and here the HEAD half carries the MORE loops AND the larger `.text` --- which is not the pairing Run 31 read, whose `-O2` half carried fewer loops and a larger `.text` and is an open question on that account. Within the pair, `--library` reads **136** library self-loops in common, **11.0%** at the same offset in line and **65.4%** in the same straddle state, which puts this pair at the bottom edge of the compiler band README gives, a tenth to a quarter, where Runs 17 and 19 to 22 fell. **Re-derived from their own binaries with today's tool, the nearest compiler pairs read 12.5% and 64.7% on Run 27 and 11.8% and 74.3% on Run 28**, with Runs 24, 25 and 26 all reading 14.0% and 66.2% --- one reading for three pairs that share two compilers, which is what a column over LINKED libraries should give. **That re-derivation is a finding of its own.** Runs 27 and 28 recorded 12.8% and 11.3% *of 141*, and Runs 24 to 26 recorded three distinct pairs of figures, where the same binaries now give 12.5% and 11.8% of 136 and one pair of figures; Run 31's 916, 4.4% and 58.7% reproduce to the digit. So the recorded series is not a series, and the figures above are the ones taken under one tool. **Post-run step 0's naming was re-run off the binaries that were timed** and reproduces the note's transcription exactly: six of the basis's eight straddlers named by byte identity --- `fillStage2Short`, `fbMutOdoVecdimsAddInLeafU2` twice, and its `Down`, `Last` and `Ptr` forms --- and three of the control's, with two refusals on the basis and five on the control, two of those five having a single-member signature family and so anchored. **And the count check refuses the population comparison on BOTH halves**, which is the case that check exists for: each `-g3` twin holds one self-loop FEWER than its timed binary, 291 against 292 and 310 against 311, so every name rests on its own byte match alone.

**The regime was confirmed in this run's own binaries before the hours were spent, and BOTH halves read the same side of it, which is where this pair differs from Runs 29 to 31.** `diag` reads `baseOffsetsScan` against `baseOffsetsMut` on `vgg-14-c512` at **24066407** against **2408530** on the basis and 24066455 against 2408530 on the control --- **9.992** times apart on each, the ten times this README has attributed to plain -O1 since Run 8. So both halves are plain -O1 and SpecConstr is off under HEAD at that level as it is under 9.12.4, which is the one thing this recipe had to get right and the one stop the pair note carried. Pre-run steps 9 and 9b are TWO readings on this pair, as they were on Runs 24 to 30 and were not on Run 31: `diag` answers for the REGIME and this pair's variable is the COMPILER, which it cannot see, so 9b reads the version out of the binaries instead --- `ghc-internal-9.1204.0` on the basis and `ghc-internal-10.100.0` on the control, which is what Run 28 read of the same two.

**Run 32 records every population twice** --- the main set and all ten stride classes from each half, one process apiece, twenty-two in all --- so every class figure below has a control-half twin and every one of the ten was gated on both. That is what lets a class say something the main set cannot, and on this pair it is what turns a null into a measured null: the compiler reaches nothing on the main set, and ten classes agree with it rather than one population declining to show a difference.

**The decomposition reproduces on both halves and its two columns do NOT part, which is the compiler leaving the preamble alone.** The riders time each shape's `list` alone, one bench to a process, clean and then saturated, and the state the preamble puts on a process comes back at a geomean of **1.1160** on the basis and **1.1166** on the control --- six ten-thousandths apart, where Run 31's two parted by nearly five points because the level reached the alone leg as it reached everything else `list` does. The basis's figure sits within half a point of Run 30's 1.1205 and Run 31's 1.1162 for this same recipe, which is three runs of one state cost. What the roster then adds on top of that state is **1.0217** on the basis and **1.0199** on the control, 9 of 19 shapes above 1 on each; the basis's rest runs 0.9817 on `stretch-primes` to 1.2143 on `stretch-r5-8x432`, the control's 0.9612 on `stretch-bigstride` to 1.2165 on that same worst shape.

**There is NO repetition this run and none was available**, `Main.hs` having moved from `13cbd0d` to `f95795a` in six commits, so neither half's inputs are an earlier binary's, no md5 here can reproduce one and an unequal one names nothing: the basis is `0fa8e3e39a1ec5d0bc080dd853473916` and the control `9e8074782182dd8128a01309041204a7`. What the two md5s DO say is that they differ from each other, which is the least a pair of compilers could be asked for. **`.text` says the halves are not one build either**, 20766917 bytes on the basis against 20920127 on the control, the HEAD half larger by 153210, no multiple of 4096 --- as no compiler pair's gap here has ever been, Runs 24 to 28 reading 140922, 140922, 145018, 140922 and 145018, where the three FLAG pairs parted by exactly 16384, 20480 and 12288 --- and their LOAD ADDRESSES differ as well, 4218880 against 4214784, where Run 31's halves shared one. **And the basis's `.text` is `run28-g912`'s, `run29-nospec`'s and `run30-nospec`'s to the byte**, 20766917 FOUR times over across every commit `Main.hs` has taken since Run 28 built, an md5 that reproduces none of them and --- on Run 28's --- a different REGIME, that half carrying `-fspec-constr`; `run31-nospec`, a build between them, read 20775109, and `run30-libcase` and `run31-o2` share 20787397 the same way. That is an observation and this file offers no mechanism for it. **The fills are the pinning claim's strong form REOPENED.** `--delta` against `run31-nospec`, the previous build of this recipe, taken at the build and before anything else changed and re-read here off the same two binaries, finds two tracked groups of 6 and 2 copies of a 28 B, 8-instruction loop: the two-copy group keeps every mod-64 offset, `[0, 0]`, and THE SIX-COPY GROUP DOES NOT --- `[0, 24, 8, 0, 4, 0]` becomes `[0, 0, 8, 0, 4, 0]`, its second head moving by 24 --- with no address surviving to the byte on either, and a third displacement on the six-copy group, `0x9928`, which is 40 mod 64 and so not a whole line. README's own paragraph rules that under the dead-spot form the strong form holds for the tracked heads' OFFSETS and says outright that *what a further reading would have to move to reopen this is a tracked head's offset*. This reading moves one. What moved under it is wider than either earlier reading's --- six commits to `Main.hs`, where Runs 24's and 25's roster changes were the whole of what moved under them --- and the verdict is [the floor section][floor]'s to carry, not this file's.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. **What this run leaves the next one is a null with a number on it.** The six compiler pairs before it all carried `-fspec-constr` on both halves, so every one of them priced a compiler at a regime the shipped library does not use; at the regime it does use, HEAD is worth at most 1.61 points to any arm here and clears the run's own cross-half noise on three of eight --- no arm past 3%, an arm geomean of **0.9985**, the reference inside the differencing bar, every allocation tier identical cell for cell, and the preamble's state cost agreeing to six ten-thousandths. **FIFTEEN of the sixteen registrations held and one was killed**, fewer kills than any of Runs 28 to 31, whose were four, three, two and three; the kill is (3), the fused list on `runs`, by **nine hundredths of a point** past its 3% bar. Two of the items re-aimed from `-O2` bands to Run 27's compiler bands, (1) and (2), both HELD, so a compiler band transferred across four runs where the level's bands did not transfer across one. **And the lesson the kills have been teaching keeps its exception.** Run 28's four kills, Run 29's three and Run 30's two all fell on items predicting a NO-OP; Run 31 broke that with two of three falling on items predicting an effect; this run's single kill falls on a no-op prediction again, and it misses by less than a tenth of a point. So an argument that a change cannot reach a population remains the cheapest clause a registration can carry --- and on this pair it was also right fifteen times out of sixteen, because the change reached nothing.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a PLAIN -O1 table**, as Runs 30's and 31's were and for the reason Run 30 was moved onto it: it is the regime `Data/Array/Internal.hs` actually compiles under. **A row's distance from the last published column IS readable this run**, that column being Run 31's own published binary's and this basis being that recipe built again. **Against it the raw distance is small and the reference barely moves**: over the 16 arms that carry a corrected time and stand in both rosters, this basis runs **0.9839 to 1.0149** with `list` itself at **1.0031**, and read as a ratio to `list` within each run --- which cancels a box term exactly, and which drops `list` as its own denominator --- the geomean over the other fifteen is **0.9976**, none of them outside the 3.3% drift band. That span carries a SOURCE term besides the box's: `Main.hs` moved six times between the two builds under an unchanged recipe. **The `alloc` column is a median over this run's own nineteen shapes**, `bq-expand` at 2.78x and `list` at 25.20x, so it is a statistic of a strategy and a shape set together and does not cross to a run that timed a different set.

**And it is the basis half's**, `run32-nospec`, as every published table here is from Run 11 on: the control half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-two rows. That the published half is the ghc-9.12.4 one is the declaration of 2026-09-14 inherited rather than this run's to make --- 9.12.4 is what a default build takes on this machine and what a cross-run absolute is read against, and HEAD is the candidate reading. **Thirty-one of the thirty-two rows are not first readings**: every one of them has a twin in Run 31's file under this very recipe, and most have twins in Runs 29's and 30's besides. The thirty-second, `liblist-stage4-list-sum`, landed at `4488631` and is read here for the first time.

**Comparing runs?** The table below is Run 32's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the properties to test are [the ones after it](#the-properties-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.66%** from 1 on the basis half or **0.68%** on the control, read over the eight pairs this roster carries, and over the four pairs that carry back to Run 10 at **0.66%** and **0.68%** --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **The two halves' floors are alike for once, and the control's names a THIRD pair**: the basis's is `bq-expand-aa-distant`'s and equals its carry-back figure, as Runs 30's and 31's basis figures did, while the control's is `mut-odo-vecdims-aa-distant`'s --- neither the shipped leaf's pair nor `bq-expand`'s, and so a third distinct answer to the open question of which pair carries the whole-set floor, in that question's fifth reading. Beside those, the worst SINGLE A/A cells of the two MAIN-SET processes --- **6.62%** on `gather48-src-50` on the basis and **16.12%** on `cnn-slice-c32` on the control --- are not floors at all and are not to be quoted as any. **And one thing this run DOES have**: its two columns may be differenced on seven of the eleven populations, `list` having moved 0.43 points on the main set, so a cross-half figure here is a measurement on those seven and an ordering on the four past the bar.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What cost a row its population on Run 27 was the correction, not the estimator, and on THIS run nothing does**: that run had nine rows on each half carrying cells the shared forcing pass is not smaller than --- seventy such cells on its basis and forty-three on its HEAD half, all of them `libunord` arms --- and this roster has none, so every row here that carries a corrected time is a geomean over all nineteen shapes. What replaced those rows is the `no_net` ruling and the retirement of the Fill arms over a list, named in the head and in *What the next run compares against*, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what property 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.64* | *55* | *2.78x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.59 | 69 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.57 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.59 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-list-sum | -- | -- | 0.64 | 70 | 1.00x | the same, base's `sum` over stage four's list -- the fold a library user brings, over the lazy odometer under the lean dispatch |
| liblist-stage4-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage10-list-sum | -- | -- | 0.02 | 83 | 0.00x | the same, base's `sum` over stage ten's list -- the fold a library user brings, over the two reorderings composed |
| libunord-stage10-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's tie-break with stage nine's zero-stride axes moved outermost -- the two reorderings composed |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage9-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.39* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.02* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.03* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.025 | 0.113 | 0.66 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage2-lean-u1 | 0.025 | 0.110 | 0.63 | 69 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.113 | 0.44 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.112* | *0.63* | *69* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.113* | *0.56* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.111 | 0.53 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| lib-stage1 | 0.028 | 0.113 | 0.53 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| **mut-odo-vecdims** | **0.045** | 0.112 | 0.35 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| *mut-odo-vecdims-aa* | *0.045* | *0.112* | *0.42* | *66* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-aa-distant* | *0.045* | *0.112* | *0.40* | *66* | *1.00x* | *A/A control* |
| bq-expand | 0.127 | 0.248 | 0.72 | 50 | 2.78x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.127* | *0.249* | *0.71* | *50* | *2.78x* | *A/A control* |
| *bq-expand-aa-distant* | *0.128* | *0.248* | *0.42* | *50* | *2.78x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.74 | 21 | 25.20x | -- |
| *list-aa-adjacent* | *1.001* | *1.013* | *0.48* | *21* | *25.20x* | *A/A control* |
| *list-aa-distant* | *1.001* | *1.025* | *0.58* | *21* | *25.20x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and on this run two pairs part in SIGN between the two statistics. `mut-odo-vecdims-add-in-leaf-u2-aa` against its base divides to **1.0082** on the published column, saying the adjacent copy is the slower, where the paired figure is **0.9974** at 9 of 19 wins, saying it is the faster; the distant copy does the same, **1.0049** against **0.9945** at 14 of 19. Both sit inside this half's 0.66% floor, so nothing turns on them. **The widest disagreements are at the head of the table.** `lib-stage2-lean` against `lib-stage1` divides to **0.8704** on the column where the paired figure is **0.9473**, near eight points apart; against the shipped leaf **0.9278** against **0.9983**, seven; and against its own unrolling twin **0.9796** against **0.9669**, one and a quarter the other way. **And this run adds a third way the column misleads, which no earlier one has recorded**: a SINGLE row's movement between runs is not the arm's either. `lib-stage2-lean-u1` prints 0.025 here and 0.029 on Run 31's basis half, fourteen points, where the cross-run paired reading is **0.9839**; its plain per-shape geomean is 0.02978 against that run's 0.03036, and four of its nineteen cells are capped in each. A margin between two arms is `--pair`'s paired figure, which is also the statistic the floor is defined in; the column is for reading the table, not for differencing it, and not for tracking a row across runs either.

`concat-runs` has no row, and neither do the other 84 arms the roster holds and checks without timing --- **85 of its 117** in all, where Run 31 checked 85 of 116: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). **One arm was added to the timed roster this run and NONE was parked**, where Run 31 parked six and added one. `liblist-stage4-list-sum`, base's `sum` over stage four's list, landed at `4488631`; the six parked on 2026-09-13 stay parked, the two pointer leaves among them, so nothing on this roster times the ceiling Run 27 made readable; and the thirteen Fill arms over a list that went to `check` on 2026-09-09 stay where they are. So a movement against Run 31's basis column is a movement on the **16 shared arms that carry a corrected time**, with a source term between the two runs and no roster term on those sixteen.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is `lib-stage2-lean` at 0.025**, with `lib-stage2-lean-u1` printing the same 0.025, the shipped leaf and its two A/A copies at 0.027 and `mut-odo-vecdims-add-in-leaf-u1` and `lib-stage1` at 0.028 --- **five timed non-control arms below `mut-odo-vecdims`'s 0.045**, every one of them a fill that writes the result, the same five Run 31 had. **The column and the pair DISAGREE about the order behind the leader**, which is the second finding and the reverse of Run 31's: paired on the basis, `lib-stage2-lean` over `lib-stage1` is **0.9473** at 12 of 19 and p 0.36, over its own unrolling twin **0.9669** at 13 of 19 and p 0.17, and over the shipped leaf **0.9983** at 11 of 19 and p 0.65, a tie rather than a lead --- while the column puts the unrolling twin level with the leader and `lib-stage1` three thousandths behind it. The disagreement is the winsorizing and not an arm: `lib-stage2-lean-u1`'s published figure is 0.02514 where its plain per-shape geomean is 0.02978, four of its nineteen cells capped. **The third is that the leaf fusion is unmoved by the compiler**: `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6459** on the basis and **0.6358** on the control, a point apart, where six readings across Runs 29, 30 and 31 lay between 0.6413 and 0.6525 under two flags and a whole level.

**The two standing placement controls are still gone with the prune, the straddlers stand at eight on each half, and this pair --- like Run 31's --- DOES move tracked fill copies.** Within the pair `./loop-offsets.py run32-ghead run32-nospec` puts the tracked six-copy group at **[0, 0, 8, 0, 4, 0]** on the basis and **[18, 0, 0, 31, 9, 2]** on the control, while the two-copy group reads **[0, 0]** on both; the basis carries 33 self-loops of 28 B in 26 distinct byte-sequences and the control 34 in 27. So a compiler displaces fill copies as an optimisation level does, which is what `--library` says over the whole library too: 136 self-loops in common at **11.0%** the same offset in line. Against the previous build of the basis's own recipe, which is Run 31's published binary, `--delta` reads the two-copy group's offsets PRESERVED and the six-copy group's NOT --- its second head moving by 24 --- with no address surviving to the byte on either group. So the two comparisons this run can make agree for once, both saying a tracked head's offset moves; and it is the second of them that reopens the pinning claim, a compiler change never having been covered by it.


## What the next run compares against

**Run 33's pair is DECLARED, 2026-09-15, and it is the one Run 31's result asked for and Run 32 deferred.** It is this run's own basis recipe as BOTH halves --- ghc-9.12.4 in the dead-spot form at plain `-O1`, `nospec` still naming what that half is --- against the same recipe with the two `-O2` passes added by hand, `twopass`, built with `--ghc-options="-fspec-constr -fliberate-case -fobject-determinism"` and nothing else changed: three flags on one command line and one variable, so that `--compare` reads nospec over twopass and every `cross` is what the two passes together do to one arm. What the pair varies is whether `-O2` is its two passes. **It is the pair Run 31's arithmetic wanted and Run 32 could not supply.** Run 31 read `-O2` worth **1.2974** on `list` where `-fspec-constr` and `-fliberate-case`, measured one at a time on Runs 29 and 30, multiply to 1.3325 --- a 2.7-point overshoot that is either the level's other passes handing `list` back or an artefact of composing two runs' readings, and a pair with both passes on one half and neither on the other is the only thing here that tells them apart. **And Run 32's own result is why it comes next rather than another compiler pair.** At plain -O1 HEAD moves no arm of this roster past 3% and only three past what two copies of one arm read across the halves --- an arm geomean of 0.9985, the reference inside the differencing bar --- so a further compiler pair at this level has little left to find, while the level itself is priced only as a whole.

**The COMPILER variable has now been asked seven times, and this run is the first to ask it at the level the library ships.** Runs 19 and 24 to 28 all varied it with `-fspec-constr` on BOTH halves: Run 24 read the two 5.7% and 6.8% apart on the instructions of two pure arms; Run 25 read `list` 1.10% apart in time and 0.33% in counts; Run 26 found twenty-four of twenty-six arms inside a percent in counts and the two `Ptr` arms outside at 0.6219 and 0.9295, which was GHC #27778; Run 27 worked it around and read every arm inside a percent; Run 28 confirmed it at a counted geomean of 1.0027. **Run 32 asks it at plain -O1 and gets the same answer more cleanly than any of them.** Its arm geomean is **0.9985**, the closest to level of the nine pairs this chapter still holds both halves of; no arm moves past 3%; `list` reads **1.0043**, inside the 0.7% differencing bar; every allocation tier is identical cell for cell between the halves; and the preamble's state cost agrees to six ten-thousandths. So six pairs at `-fspec-constr` and one at plain -O1 all say the same thing, and the regime term the first six carried turns out to have been hiding nothing. **The REGIME variable, meanwhile, has been asked three times and the arithmetic it left is still open.** Put in one orientation --- the unflagged half over the flagged --- Runs 29, 30 and 31 read `list` at **1.1379**, **1.1710** and **1.2974** and `bq-expand` at **1.2804**, **1.0127** and **1.2943**; on `bq-expand` the two passes MULTIPLY to 1.2967 against a measured 1.2943, 0.24 of a point apart, while on `list` they multiply to 1.3325 against 1.2974 and overshoot by 2.7 points. That arithmetic is an observation and not a subtraction, and Run 33's pair is what settles it.

**What Run 32 leaves the next run to read against, and the first item is a check that did NOT fire. The box is where Runs 28 to 31 left it**, and this run's gate says so at first reading rather than needing an argument: the machine check reads `list`'s net at **+0.86%** against the fingerprint Run 31 installed, worst `cnn-L1-24x24-c1` at +3.03%, 0 of 19 shapes past 5% and the geomean inside the 3% bar. That is a LIKE-FOR-LIKE reading --- Run 31's published half is plain -O1 dead-spot 9.12.4 and this basis is that recipe built again --- so no argument stands between the check and the box. Against `run31-nospec` itself the sixteen shared timed arms span **0.9839 to 1.0149** with `list` at **1.0031**, and the fifteen that are not `list` give a `--bridge` geomean of **0.9976** with none outside the 3.3% drift band. So this run publishes INSIDE the third machine era rather than opening a fourth, an absolute crosses between it and Run 31's basis unadjusted, and the fingerprint below is the one a Run 33 machine check reads against. **What a Run 33 reading must not take from those sixteen arms is a box figure**: `Main.hs` moved six times between the two builds, so their 1.5% is the box's and those commits' together and nothing here separates the two. Only a pair whose halves share a source does that, which is what every pair here is for.

**Registered with the pair.** Run 32's sixteen registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. FIFTEEN held and ONE was killed. **What a next registration should take from this one is that the pattern the last four runs traced is back, and its exception was Run 31's.** Run 28's four kills, Run 29's three and Run 30's two all fell on items predicting a NO-OP --- something argued from the code to be unreachable, tested where the argument said nothing could move, and moving --- and Run 31 broke that, two of its three falling on items predicting an EFFECT. This run's single kill is a no-op prediction again: (3) argued that with the shared loop's `SPEC` argument gone a compiler is what is left to move the fused list, predicted it would not move past 3% on `runs`, and it moved **3.09**. **And the clause Run 31's kills condemned held here.** Run 31's (2) died of a magnitude carried forward from the run before it; this run's (1) and (2) carry magnitudes forward too --- from Run 27's compiler pair rather than from a level's --- and both HELD. So a carried magnitude is a safe clause when it is carried from a pair that varied the same thing, and Run 31's was not.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at a run's OWN area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 32 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 31 to Run 32 --- **and it is a step between PUBLISHED bases**, Run 31 having published on this very recipe, as Run 30 did. The box not having moved, that step is a SUBTRACTION and not a bridge. Over the **16 arms both rosters time and both give a corrected time** --- not all of them, one having been added --- the basis column runs from **0.9839** on `lib-stage2-lean-u1` to **1.0149** on `lib-stage2-lean`, FOURTEEN of the sixteen inside 1% of 1 and those two the only ones outside, with `list` itself at **1.0031**. **But it is NOT a box reading**: `Main.hs` moved six times between the two builds under an unchanged recipe, so the span carries a source term, and `lib-stage2-lean`'s 1.49% is the largest thing either the box or those six commits did to a timed arm. **The table below is this run's own two halves and no earlier run's**, seven strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in ONE COMPILER and in nothing else. Its two columns MAY be differenced, `list` having moved 0.43 points between them, which is the first such table in this file since Run 28's.
| strategy | Run 32 (plain -O1, dead-spot, -A32m, 9.12.4) | Run 32 (plain -O1, dead-spot, -A32m, GHC HEAD) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.045** | 0.045 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.028** | 0.029 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.027** | 0.027 |
| `lib-stage1` | **0.028** | 0.028 |
| `lib-stage2-lean` | **0.025** | 0.026 |
| `lib-stage2-lean-u1` | **0.025** | 0.025 |
| `bq-expand` | **0.127** | 0.129 |

**READ THE SECOND COLUMN AS A RATIO AND NOT AS A SPEED.** Every entry is that arm's net over `list`'s net in ITS OWN half, and `list` is 0.43 points slower under HEAD --- so a control entry reading higher is very nearly the arm and not the denominator this run, which is what an unmoved reference buys. **That is still not an identity**: each entry is winsorized per row within its own half, so dividing an arm's two entries does not reproduce its `--compare` figure and is not meant to --- on `lib-stage2-lean` the two entries divide to about 0.96 where the cross reads 1.0134, which is the capping and not a disagreement. The arm-by-arm reading of what the compiler is worth is in the head, off `--compare`, where the reference is not divided out.

**A published geomean is over the same 19 shapes, and two halves of one run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered. **THE TABLE ABOVE IS SUCH A PAIR**, `list` having moved **0.43 points** on this run's main set, which is the first time since Run 28 that this section's table could say so: its two columns MAY be differenced. Four of the seven rows print level to three decimals, two print one thousandth higher on the control and `bq-expand` two. Read down a column and the ordering is the same on both, head and foot alike: `lib-stage2-lean` leads each at 0.025 and 0.026, `mut-odo-vecdims` sits between at 0.045 twice, and `bq-expand` is at the foot at 0.127 and 0.129. So the compiler, like `-fspec-constr` and unlike `-fliberate-case`, does not change which arm heads the column --- and here it does not change the column at all.

**The control half's own standings on the arms this run's roster carries, which no table here holds, every published table being the basis half's.** Read off the control half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9475** at 16 of 19 (0.9680 at 13 of 19) and against `mut-odo-vecdims` **0.6358** at 17 of 19 (0.6459 at 17 of 19); `lib-stage1` against `-u2` 1.0534 at 9 of 19 (1.0539 at 7 of 19); `lib-stage2-lean` against `-u2` **0.9903** at 15 of 19 (0.9983 at 11 of 19) and against `lib-stage1` **0.9401** at 16 of 19 (0.9473 at 12 of 19). **Every one of the five holds its direction across the halves**, and the widest the two readings part is two points, on the shipped leaf against its un-unrolled twin --- so the WITHIN-half orderings this run publishes are not the compiler's, which is the half of the run the 0.7% bar leaves standing on every population, the four past the bar included.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section][pershape]. The two tables below are installed from this run's own JSONs in the per-shape form the README describes, which replaced a fourteen-column one on 2026-09-04.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 6.33 us | 0.077 | `lib-stage1` 0.078 | `mut-odo-vecdims-add-in-leaf-u2` 0.054 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.7 us | 0.088 | `lib-stage2-lean` 0.072 | `mut-odo-vecdims-add-in-leaf-u2` 0.068 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 119 us | 0.063 | `lib-stage2-lean-u1` 0.033 | `mut-odo-vecdims-add-in-leaf-u2` 0.043 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 388 us | 0.043 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `gather48-src-50` | 3 | 22500 | 465 us | 0.048 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `stretch-coprime-r7` | 13 | 60060 | 1.11 ms | 0.030 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.73 ms | 0.052 | `lib-stage2-lean-u1` 0.031 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `stretch-primes` | 89 | 250357 | 4.33 ms | 0.025 | `lib-stage1` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 17.1 ms | 0.040 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `vgg-14-c512-k3` | 3 | 903168 | 19.8 ms | 0.052 | `lib-stage2-lean` 0.031 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 20.1 ms | 0.031 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-inner256` | 256 | 1750784 | 44.5 ms | 0.023 | `lib-stage2-lean-u1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |
| `stretch-pow2stride` | 64 | 1769472 | 31.3 ms | 0.112 | `lib-stage2-lean-u1` 0.110 | `mut-odo-vecdims-add-in-leaf-u1` 0.111 |
| `stretch-r5-8x432` | 8 | 1769472 | 48.2 ms | 0.021 | `lib-stage1` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `stretch-square-1341` | 1341 | 1798281 | 30.7 ms | 0.084 | `lib-stage1` 0.076 | `mut-odo-vecdims-add-in-leaf-u2` 0.076 |
| `stretch-bigstride` | 3 | 1800000 | 51.1 ms | 0.032 | `lib-stage1` 0.014 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 39.4 ms | 0.059 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1` 0.022 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 41 ms | 0.021 | `lib-stage1` 0.021 | `mut-odo-vecdims` 0.021 |
| `stretch-wide-2xM` | 2 | 1800000 | 39.8 ms | 0.057 | `lib-stage2-lean-u1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 942 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 29.5 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 29.3 ms | 0.019 | `lib-stage1` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 29.6 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 35.7 ms | 0.015 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 39.5 ms | 0.057 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.65 ms | 0.053 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 4.25 ms | 0.019 | `lib-stage1` 0.012 | `mut-odo-vecdims` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 47.9 ms | 0.034 | `lib-stage2-lean-u1` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 42.2 ms | 0.022 | `lib-stage1` 0.018 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.23 ms | 0.019 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.25 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.26 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `block-run64-page` | `block` | 64 | 131072 | 2.34 ms | 0.029 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.47 ms | 0.019 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 962 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 942 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 29.7 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 30.1 ms | 0.021 | `lib-stage1` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.35 ms | 0.026 | `lib-stage1` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.34 ms | 0.026 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.69 ms | 0.052 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u1` 0.030 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 29.3 ms | 0.024 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims` 0.024 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 30 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims` 0.024 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 33 ms | 0.048 | `lib-stage2-lean` 0.042 | `mut-odo-vecdims-add-in-leaf-u2` 0.043 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 120 us | 0.063 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u1` 0.042 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 457 us | 0.048 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `rev-primes` | `rev` | 89 | 250357 | 4.36 ms | 0.025 | `lib-stage1` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `runs-65536` | `runs` | 65536 | 1769472 | 28.1 ms | 0.024 | `lib-stage1` 0.022 | `mut-odo-vecdims` 0.024 |
| `runs-16384` | `runs` | 16384 | 1785856 | 28.3 ms | 0.024 | `lib-stage1` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-4096` | `runs` | 4096 | 1798144 | 28.5 ms | 0.024 | `lib-stage1` 0.024 | `mut-odo-vecdims` 0.024 |
| `runs-1024` | `runs` | 1024 | 1799168 | 28.6 ms | 0.024 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.024 |
| `runs-512` | `runs` | 512 | 1799680 | 28.7 ms | 0.026 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.026 |
| `runs-256` | `runs` | 256 | 1799936 | 28.9 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.025 |
| `runs-7` | `runs` | 7 | 1799994 | 32.9 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `runs-2` | `runs` | 2 | 1800000 | 39.6 ms | 0.058 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u1` 0.024 |
| `runs-3` | `runs` | 3 | 1800000 | 35.8 ms | 0.047 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 34.1 ms | 0.041 | `lib-stage2-lean-u1` 0.024 | `mut-odo-vecdims-add-in-leaf-u1` 0.024 |
| `runs-5` | `runs` | 5 | 1800000 | 33.1 ms | 0.038 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-9` | `runs` | 9 | 1800000 | 31.9 ms | 0.030 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-96` | `runs` | 96 | 1800000 | 29.2 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.025 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 29.6 ms | 0.025 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `scaled-r5` | `scaled` | 13 | 15015 | 272 us | 0.029 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 1.05 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 5.14 ms | 0.028 | `lib-stage2-lean-u1` 0.030 | `mut-odo-vecdims` 0.028 |
| `small-patch-k5` | `small` | 5 | 150 | 2.96 us | 0.076 | `lib-stage1` 0.101 | `mut-odo-vecdims-add-in-leaf-u2` 0.057 |
| `small-bcast32` | `small` | 32 | 256 | 4.5 us | 0.050 | `lib-stage1` 0.063 | `mut-odo-vecdims-add-in-leaf-u2` 0.043 |
| `small-flat64` | `small` | 64 | 256 | 4.47 us | 0.058 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.054 |
| `small-patch-r5` | `small` | 4 | 256 | 5.33 us | 0.086 | `lib-stage2-lean` 0.097 | `mut-odo-vecdims-add-in-leaf-u2` 0.065 |
| `small-row96` | `small` | 96 | 384 | 6.55 us | 0.041 | `lib-stage2-lean` 0.055 | `mut-odo-vecdims-add-in-leaf-u2` 0.041 |
| `window-28x28-k5` | `window` | 5 | 14400 | 280 us | 0.040 | `lib-stage1` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 962 us | 0.084 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.44 ms | 0.052 | `lib-stage1` 0.031 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 9.63 ms | 0.051 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.81 ms | 0.051 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 11.7 ms | 0.057 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u1` 0.029 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 12.4 ms | 0.053 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u1` 0.031 |
| `window-128x128-k7` | `window` | 7 | 729316 | 13.8 ms | 0.031 | `lib-stage1` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |

**One row to read first, and it is a property of the shape set and not of any arm**: NONE of the thirty-two rows is a geomean over fewer shapes than the rest, where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, against **seventy on Run 27's basis and forty-three on its control** as that run's write-up recorded them --- counts predating the 2026-09-10 `no_net` ruling, which today's reader gives as 25 and 8 --- so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the TWELVE `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers, `liblist-stage4-list-sum` being the twelfth and landing this run --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows --- `libunord-stage5-sum` on four, `-stage2-sum` on two, `-stage4-sum` on one --- this one names `lib-stage2-lean` on seven and `lib-stage1` on three --- `rev`, `bcastmid` and `scaled` --- and nothing else, and the cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The properties the next run should test

**Each stride class carries the same three properties, now with Run 32's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** **The first held in every one of the eleven populations on both halves; the second held in all ten classes on both halves and PARTED BETWEEN THE HALVES on the main set**, which is where Run 31 broke it on both. On the MAIN SET `mut-odo-vecdims` is ahead of `bq-expand` on `stretch-pow2stride` by **0.66 of a point** on the basis, at 0.9934, and behind it by three hundredths on the control, at **1.0003**; Run 31 read that cell at 1.0009 and 1.0109 and Run 30 at 0.9997. So three runs have now read it within a point and a quarter of a tie, and which side of 1 it lands on has changed three times. The `worst` clause holds in every regime, roster, compiler and layout the README has run, so `mut-odo-vecdims` --- and this is a statement about THAT arm and not about the route the library ships, which the paragraph below reads separately --- was never slower than the `list` it replaced, on any shape of any population. The `bq-expand` clause folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and is read that way here for the fifth time.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm now breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `lib-stage1` carries a `worst` of **1.106** on `runs` on the basis and **1.107** on the control, and one cell does it on each: `runs-2`, at **1.1058** and **1.1071**. That is a tighter break than Run 31's, whose control half read 1.6936 on that cell and 1.1333 on `runs-3` besides, and the difference is the level rather than the compiler --- HEAD reads that cell within two thousandths of 9.12.4. `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. Of the five library-shaped arms Run 27 found breaking it at `runs-2` --- `libunord-stage1` 1.237, `liblist-stage1` 1.214, `lib-stage1` 1.198, `liblist-stage2` 1.078 and `libunord-stage2` 1.076 --- four are retired to `check` and are not timed here. Those five figures are RECOMPUTED from Run 27's own cells, that run's prose and its own installed table having printed different ones, and the five are FIVE of NINE library-shaped arms over `list` there rather than the largest five.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves, which is the second run running that this property is the one left entirely alone.** The `list` clause is closest at `small-patch-k5`, 0.05918, with the other four `small` shapes between 0.0530 and 0.0591 and every shape outside `small` under 0.048. The `bq-expand` clause is closest at `scaled-rank1-m1`, 1.00003, then `stretch-tall-Mx2` at 1.00000 and `bcast-src8` at 0.99999 --- the same three cells Runs 30 and 31 read, inside the margin the strict form would have failed on, which is why the margin is there and why it is not widened further.

3. **The allocation tiers survive, their ORDER is unbroken in all ten classes and on the main set, and this run their LEVELS are identical on the two halves cell for cell; what `small` is outside is the LEVEL clause and not the order one**, by the ruling of 2026-09-07, it being the class built to break it and read here for what it shows: the mutable fills at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 20.99x to 27.66x. On the main set the fills read 1.00x, `bq-expand` **2.78x** and `list` **25.20x**, which are a plain -O1 half's levels as Runs 30's and 31's were --- **and the control half reads the same 2.78x and 25.20x, and the same triple in every one of the ten classes.** That is registration (10) HELD, where Run 31's was killed: `-O2` took `bq-expand` to 2.11x and `list` to 23.45x on that run, and a compiler at this level takes them nowhere. The fills and the `liblist` consumers read 1.00x and the `libunord` consumers 0.00x on both halves, unmoved. **What this roster removes is the 2.00x tier**, whose members were the list Fill arms now checked and not timed.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 32 (GHC HEAD against ghc-9.12.4, both at plain -O1, dead-spot, -A32m) records every class twice**, one process per class per half, so each block below has a control-half twin and the cross-half line under it is derived from both. **This run those lines say MORE than usual, and they say why.** `list` moved between the halves by 0.06 of a point on `runs` at narrowest and 2.27 on `compose` at widest, so six of the ten classes sit INSIDE the 0.7% that lets two columns be differenced and their cross-half readings are readings of the pair's variable rather than orderings --- where every one of Runs 29's, 30's and 31's was disqualified. The four past the bar are `rev` at 0.9855, `scaled` at 1.0119, `small` at 1.0157 and `compose` at 1.0227. Over the ten classes the reader counts **160 arm-comparisons, 66 putting the basis faster and 94 slower**, with no degenerate arm excluded, at geomeans from **0.9923** on `small` to **1.0110** on `compose` and extremes of `mut-odo-vecdims-add-in-leaf-u2-aa` at 0.9576 on `small` and `lib-stage1` at 1.0528 on `window`; the low extreme is that A/A copy in 2 of the 10. What each class still decides, and decides on both halves separately, is the three properties, its own floor, and whichever registrations name it. **Four items read EVERY class** --- (1), (2), (8) and (15) --- so each block below has those four whatever else it carries.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- and both are read over the POPULATION, so the arm named here may lead on no single shape and the per-shape fingerprint below may name another, which is [the README's per-shape section][pershape]'s own point and not a disagreement --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the properties](#the-properties-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on FOUR of the ten --- `rev` and `bcastmid`, where `lib-stage1` leads, and `bcast` and `flip`, where `lib-stage2-lean` does --- and the family's own ceiling on the other SIX, `window`, `scaled`, `runs`, `block`, `small` and `compose`, where the ceiling is faster. **The two pointer leaves held five of Run 30's six bolded ceilings and are parked**, so every bolded ceiling here is the shipped leaf `mut-odo-vecdims-add-in-leaf-u2` itself, which is the form the family would ship. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 3 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. Item 2's own table is NOT among what that call prints, which is why the list above starts where it does: it comes from the separate `--block --in-place` call item 2 itself names. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.042 | 0.063 | **`lib-stage1`** 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 | 0.25% |
| `bcast` | 6 | 0.021 | 0.057 | **`lib-stage2-lean`** 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 | 1.17% |
| `bcastmid` | 4 | 0.029 | 0.053 | **`lib-stage1`** 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 | 0.51% |
| `window` | 8 | 0.051 | 0.084 | `lib-stage2-lean` 0.028 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.028 | 1.23% |
| `scaled` | 3 | 0.028 | 0.029 | `lib-stage1` 0.022 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.022 | 0.37% |
| `runs` | 14 | 0.028 | 0.058 | `lib-stage2-lean` 0.024 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.024 | 3.27% |
| `flip` | 6 | 0.029 | 0.052 | **`lib-stage2-lean`** 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 | 1.26% |
| `block` | 5 | 0.023 | 0.029 | `lib-stage2-lean` 0.021 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.021 | 0.42% |
| `small` | 5 | 0.060 | 0.086 | `lib-stage2-lean` 0.056 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.051 | 0.51% |
| `compose` | 4 | 0.024 | 0.028 | `lib-stage2-lean` 0.015 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.014 | 0.61% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On SIX rows --- `window`, `scaled`, `runs`, `block`, `small` and `compose` --- the bold sits in the CEILING column instead**, and this run it names ONE arm on all six, the shipped leaf `mut-odo-vecdims-add-in-leaf-u2`, where Run 30's six named three. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship --- an override no row here exercises, both of them having been parked on 2026-09-13. **FOUR rows tie at three decimals** --- `window` at 0.028, `scaled` at 0.022, `runs` at 0.024 and `block` at 0.021 --- and the bold on each is `--block`'s own, computed on the unrounded values where the printed ones cannot separate: 0.028182 against 0.027841, 0.022411 against 0.022215, 0.024376 against 0.024362 and 0.021363 against 0.021187, every one of the four falling to the ceiling.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.13* | *127* | *3.22x* |
| liblist-stage1-sum | -- | -- | 0.11 | 147 | 1.01x |
| liblist-stage2-sum | -- | -- | 0.14 | 147 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.09 | 147 | 1.01x |
| liblist-stage4-list-sum | -- | -- | 0.12 | 147 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.08 | 147 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.09 | 146 | 1.03x |
| libunord-stage10-list-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage10-sum | -- | -- | 0.04 | 157 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.04 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.03 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.03 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.03 | 157 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.12* | *148* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *158* | *0.00x* |
| lib-stage1 | 0.021 | 0.046 | 0.07 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.042 | 0.09 | 147 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.042* | *0.07* | *147* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.042* | *0.08* | *147* | *1.00x* |
| lib-stage2-lean | 0.023 | 0.032 | 0.09 | 147 | 1.01x |
| lib-stage2-lean-u1 | 0.025 | 0.033 | 0.10 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.042 | 0.11 | 146 | 1.00x |
| *mut-odo-vecdims-aa* | *0.042* | *0.063* | *0.09* | *138* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.042* | *0.063* | *0.07* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.042** | 0.063 | 0.09 | 138 | 1.00x |
| bq-expand | 0.138 | 0.230 | 0.11 | 122 | 3.22x |
| *bq-expand-aa-adjacent* | *0.138* | *0.231* | *0.11* | *122* | *3.22x* |
| *bq-expand-aa-distant* | *0.138* | *0.230* | *0.11* | *122* | *3.22x* |
| list (baseline) | 1.000 | 1.000 | 0.18 | 85 | 26.11x |
| *list-aa-distant* | *1.001* | *1.003* | *0.22* | *85* | *26.11x* |
| *list-aa-adjacent* | *1.001* | *1.003* | *0.24* | *85* | *26.11x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9975, worst cell 0.45% on `rev-gather48-src-50`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.01% on `rev-cnn-L1-24x24-c1`, its interval covering 1. The in-situ term reads 0.9963, 1.0106 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9986, which the correction amplifies by 1.71x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m21s, peak 95 MiB in use, 26 MiB max residency; the reader reads 32 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.51 ms per call raw, 4.36 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.063/0.048/0.025

**Across the halves:** 10 of the 16 arms are faster on this half and 6 slower, at a geomean of 0.9984, from `mut-odo-vecdims-aa` at 0.9812 to `lib-stage1` at 1.0371, with `list` itself at 0.9855. **The baseline moved 1.45% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.063, tiers at 1.00x, 3.22x and 26.11x, identical on the control --- and `lib-stage1` leads outside the family at 0.021, priced against `mut-odo-vecdims` at 0.6438 over 3 of 3 shapes at sign p 0.25, a margin of 35.62% against this class's 0.25% floor (`mut-odo-vecdims-aa`), the tightest floor of the ten. **What is this class's own is the extra pass over the axes, visible here for the fifth run running.** `rev` negates every stride and carries no zero stride and no pair of axes tied on stride, so `zerosOutermost` hands its list back unchanged, `routeUnord10` is `routeUnord7` to the byte, and only the walk over the axes separates the dispatches: `libunord-stage10-sum` over `libunord-stage7-sum` reads **1.0111** on the basis and 1.0097 on the control, inside registration (6)'s 3%, and stage nine alone reads 1.0068 and 1.0074. **This class is one of the four whose two columns may NOT be differenced**, `list` having moved **1.45 points** between the halves against a 0.7% bar, so its cross-half line is an ordering. The two `liblist` spans hold either way, both being read within a half: the odometer's consumer over master's slice recursion reads **0.9196** on the basis and 0.9272 on the control against a registered 0.92, and the composed route over master's consumer **0.5228** and **0.5334** against 0.53.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09, for Run 28 --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and Run 28's registration (7) read no crossover on it; this run reads the class for stage ten instead, at item (6).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.62* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.49 | 62 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.52 | 62 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.48 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.52 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.52 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 74 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *69* | *0.00x* |
| lib-stage2-lean | 0.015 | 0.020 | 0.51 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.015* | *0.020* | *0.44* | *62* | *1.00x* |
| lib-stage1 | 0.015 | 0.020 | 0.42 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.016 | 0.020 | 0.41 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.016* | *0.020* | *0.69* | *62* | *1.00x* |
| lib-stage2-lean-u1 | 0.016 | 0.024 | 0.48 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.018 | 0.020 | 0.42 | 61 | 1.00x |
| *mut-odo-vecdims-aa* | *0.021* | *0.057* | *0.33* | *61* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.021* | *0.057* | *0.31* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.021** | 0.057 | 0.14 | 61 | 1.00x |
| bq-expand | 0.091 | 0.140 | 0.69 | 46 | 1.00x |
| *bq-expand-aa-adjacent* | *0.091* | *0.140* | *0.70* | *46* | *1.00x* |
| *bq-expand-aa-distant* | *0.092* | *0.144* | *0.08* | *46* | *1.00x* |
| list (baseline) | 1.000 | 1.000 | 1.17 | 17 | 20.99x |
| *list-aa-distant* | *1.003* | *1.013* | *1.14* | *17* | *20.99x* |
| *list-aa-adjacent* | *1.007* | *1.011* | *0.78* | *17* | *20.99x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0117, worst cell 2.54% on `bcast-tall-Mx2`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.51% on `bcast-src8`, its interval covering 1. The in-situ term reads 1.0205, 1.0129 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0088, which the correction amplifies by 1.36x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m40s, peak 180 MiB in use, 42 MiB max residency; the reader reads 32 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 30.4 ms per call raw, 29.3 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.028/0.019/0.057/0.015/0.019/0.019

**Across the halves:** 15 of the 16 arms are faster on this half and 1 slower, at a geomean of 0.9939, from `lib-stage1` at 0.9848 to `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 1.0023, with `list` itself at 0.9987.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.057, tiers at 1.00x, 1.00x and 20.99x, identical on the control --- and `lib-stage2-lean` leads outside the family at 0.015, priced against `mut-odo-vecdims` at 0.6575 over 6 of 6 shapes at sign p 0.031, a margin of 34.25% against this class's 1.17% floor (`bq-expand-aa-distant`). **This is where stage ten pays off best.** `libunord-stage10-sum` over `libunord-stage7-sum` reads **0.5478** on the basis and 0.5545 on the control, both inside registration (5)'s 0.55 within 20%, and no view has it behind stage seven on either half, the closest being `bcast-inner8` at 0.7096 and 0.7085. Stage nine alone reads 0.5461 and 0.5549 and over stage six 0.5459 and 0.5550, so on these views the tie-break moves nothing and the zero-stride move is the whole of it, which is registration (13) held. **And the dispatch is worth nothing to the fill here**, both routes filling through `fillStage2` since 2026-09-11: `lib-stage2-lean` over `lib-stage1` reads 0.9930 and 0.9905, inside registration (7)'s 3% on both halves for the fifth run running. `list` moved **0.13 of a point** between the halves, the second closest to level of the eleven populations, so this class's two columns may be differenced.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.39* | *66* | *1.92x* |
| liblist-stage1-sum | -- | -- | 0.34 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.37 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.34 | 82 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.30 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.35 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.31 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 96 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.27* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *88* | *0.00x* |
| lib-stage1 | 0.012 | 0.018 | 0.37 | 82 | 1.00x |
| lib-stage2-lean | 0.012 | 0.018 | 0.34 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.012 | 0.019 | 0.33 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.020* | *0.031* | *0.51* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.020* | *0.031* | *0.26* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.031 | 0.29 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.030 | 0.41 | 78 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.053* | *0.27* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.029* | *0.053* | *0.25* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.053 | 0.21 | 76 | 1.00x |
| bq-expand | 0.099 | 0.180 | 0.45 | 61 | 1.92x |
| *bq-expand-aa-adjacent* | *0.099* | *0.180* | *0.42* | *61* | *1.92x* |
| *bq-expand-aa-distant* | *0.099* | *0.180* | *0.28* | *61* | *1.92x* |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.79* | *28* | *23.56x* |
| list (baseline) | 1.000 | 1.000 | 0.77 | 28 | 23.56x |
| *list-aa-distant* | *1.005* | *1.009* | *1.02* | *28* | *23.56x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9949, worst cell 1.39% on `bcastmid-primes`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0002 on a worst cell of 0.04% on `bcastmid-block150k`, its interval missing 1. The in-situ term reads 1.0198, 1.0584 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9980, which the correction amplifies by 2.37x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h11m8s, peak 135 MiB in use, 35 MiB max residency; the reader reads 32 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 48.9 ms per call raw, 47.9 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.053/0.019/0.034/0.022

**Across the halves:** 3 of the 16 arms are faster on this half and 13 slower, at a geomean of 1.0044, from `bq-expand-aa-distant` at 0.9953 to `mut-odo-vecdims-add-in-leaf-u2-aa` at 1.0132, with `list` itself at 1.0059.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.053, tiers at 1.00x, 1.92x and 23.56x, identical on the control --- and `lib-stage1` leads outside the family at 0.012, priced against `mut-odo-vecdims` at 0.4168 over 4 of 4 shapes at sign p 0.12, a margin of **58.32%**, the widest of the ten, against this class's 0.51% floor. **Stage ten pays off here too**, `libunord-stage10-sum` over `libunord-stage7-sum` reading **0.5669** on the basis and 0.5780 on the control against registration (5)'s 0.57, with `bcastmid-b200k` widest at 0.3238 and 0.3508 and `bcastmid-primes` closest to level at 0.7033 and 0.7025, and no view behind on either half. Stage nine alone reads 0.5690 and 0.5789 and over stage six 0.5690 and 0.5795, the tie-break again moving nothing where every view carries a zero stride. **The dispatch is worth nothing to the fill here either**, `lib-stage2-lean` over `lib-stage1` reading 1.0075 and 1.0056 inside registration (7)'s 3%. `list` moved 0.59 of a point between the halves, so this class's two columns may be differenced.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09, for Run 28, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.32* | *62* | *3.86x* |
| liblist-stage1-sum | -- | -- | 0.18 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.17 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.16 | 82 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.20 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.20 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.24 | 82 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.05 | 102 | 0.03x |
| libunord-stage10-sum | -- | -- | 0.06 | 102 | 0.03x |
| libunord-stage6-loop-sum | -- | -- | 0.24 | 90 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.11 | 102 | 0.03x |
| libunord-stage7-sum | -- | -- | 0.07 | 102 | 0.03x |
| libunord-stage9-sum | -- | -- | 0.10 | 102 | 0.03x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.15* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *97* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.028 | 0.031 | 0.15 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.028* | *0.031* | *0.17* | *82* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.028* | *0.031* | *0.19* | *82* | *1.00x* |
| lib-stage2-lean | 0.028 | 0.032 | 0.18 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.029 | 0.031 | 0.18 | 82 | 1.00x |
| lib-stage1 | 0.029 | 0.032 | 0.17 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.029 | 0.032 | 0.21 | 82 | 1.00x |
| **mut-odo-vecdims** | **0.051** | 0.084 | 0.15 | 76 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.084* | *0.16* | *76* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.051* | *0.084* | *0.14* | *76* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.173* | *0.213* | *0.35* | *57* | *3.86x* |
| bq-expand | 0.174 | 0.212 | 0.32 | 57 | 3.86x |
| *bq-expand-aa-distant* | *0.175* | *0.213* | *0.23* | *57* | *3.86x* |
| *list-aa-adjacent* | *1.000* | *1.004* | *0.36* | *30* | *27.66x* |
| list (baseline) | 1.000 | 1.000 | 0.47 | 30 | 27.66x |
| *list-aa-distant* | *1.000* | *1.003* | *0.46* | *30* | *27.66x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9877, worst cell 9.51% on `window-32x32-c64-k3`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.07% on `window-224x224-k3`, its interval covering 1. The in-situ term reads 1.0065, 1.1937 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9916, which the correction amplifies by 1.55x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h22m8s, peak 126 MiB in use, 51 MiB max residency; the reader reads 32 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 14.3 ms per call raw, 13.8 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.040/0.051/0.084/0.031/0.052/0.051/0.053/0.057

**Across the halves:** 8 of the 16 arms are faster on this half and 8 slower, at a geomean of 1.0101, from `bq-expand-aa-adjacent` at 0.9861 to `lib-stage1` at 1.0528, with `list` itself at 0.9949.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.084, the second highest of the ten behind `small`'s, and tiers at 1.00x, 3.86x and 27.66x, the widest `bq-expand` and `list` levels of any class and identical on the control --- and the family's own ceiling ties the lean fill, both printing 0.028 where the column resolves 0.028182 against 0.027841. What the block prices is the BEST ARM OUTSIDE THE FAMILY: `lib-stage2-lean` over `mut-odo-vecdims` at 0.4782 over 8 of 8 shapes at sign p 0.0078, a margin of 52.18% against this class's 1.23% floor. **This is the class the compiler touches, and the only one where the fill family leaves registration (1)'s class band.** `lib-stage1` reads **1.0528** and `lib-stage2-lean` **1.0510** across the halves against a registered 4%, the leaf holding at 1.0262, which is two of the thirty class readings outside and both of them here. **And it is where stage ten's two changes still cancel.** Over stage nine the span reads **0.9868** on the basis, inside registration (5)'s 0.96 within 5%, and **0.8982** on the control, 6.18 points off and outside; per view, `window-128x128-k7` puts stage ten behind stage nine at **1.3899** and **1.2707** and `window-64x64-k1x9` at 1.2512 and 1.2166, both halves, reproducing Run 31's cancellation on two of its three views, while `window-224x224-k3` is behind on the basis at 1.1894 and ahead on the control at 0.9817. `list` moved 0.51 of a point between the halves, so the columns may be differenced.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.10* | *118* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.15 | 128 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.15 | 127 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.15 | 127 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.14 | 128 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.14 | 128 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.14 | 127 | 1.01x |
| libunord-stage10-list-sum | -- | -- | 0.15 | 127 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.13 | 127 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.12 | 128 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.13 | 127 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.16 | 127 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.12 | 128 | 1.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.18* | *146* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *138* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.031 | 0.12 | 128 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.030* | *0.15* | *128* | *1.00x* |
| lib-stage1 | 0.022 | 0.031 | 0.13 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.023* | *0.031* | *0.14* | *128* | *1.00x* |
| lib-stage2-lean | 0.023 | 0.031 | 0.16 | 128 | 1.00x |
| lib-stage2-lean-u1 | 0.024 | 0.030 | 0.11 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.024 | 0.030 | 0.17 | 127 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.029* | *0.11* | *127* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.029* | *0.10* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.028** | 0.029 | 0.08 | 127 | 1.00x |
| *bq-expand-aa-adjacent* | *0.090* | *0.100* | *0.06* | *111* | *1.21x* |
| *bq-expand-aa-distant* | *0.090* | *0.100* | *0.07* | *111* | *1.21x* |
| bq-expand | 0.091 | 0.101 | 0.08 | 111 | 1.21x |
| *list-aa-distant* | *0.999* | *1.004* | *0.16* | *69* | *21.49x* |
| *list-aa-adjacent* | *1.000* | *1.004* | *0.16* | *69* | *21.49x* |
| list (baseline) | 1.000 | 1.000 | 0.23 | 69 | 21.49x |

**Controls:** The largest A/A pair is `bq-expand-aa-adjacent` at 0.9963, worst cell 0.96% on `scaled-r5`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9998 on a worst cell of 0.03% on `scaled-super-r3`, its interval covering 1. The in-situ term reads 1.0209, 1.0227 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9973, which the correction amplifies by 1.38x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m22s, peak 111 MiB in use, 28 MiB max residency; the reader reads 32 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 5.32 ms per call raw, 5.14 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.023/0.028/0.029

**Across the halves:** 0 of the 16 arms are faster on this half and 16 slower, at a geomean of 1.0087, from `mut-odo-vecdims-add-in-leaf-u2-aa` at 1.0001 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0297, with `list` itself at 1.0119. **The baseline moved 1.19% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.029, joint second lowest of the ten with `block`'s, and tiers at 1.00x, 1.21x and 21.49x, identical on the control --- and the family's own ceiling ties `lib-stage1`, both printing 0.022 where the column resolves 0.022411 against 0.022215. What the block prices is `lib-stage1` and not the leader: it reads 0.9294 over `mut-odo-vecdims` at 2 of 3 shapes and sign p 1, a margin of 7.06% against this class's 0.37% floor. That margin is the third narrowest of the ten, behind `block`'s 6.64% and `small`'s 6.84%, and its sign test separates nothing at all, p reading 1 over 2 of 3 shapes, as `runs`' 0.79 and `small`'s 0.38 do not separate theirs. **This class is one of the four whose two columns may NOT be differenced**, `list` having moved **1.19 points** between the halves; and it is the class whose A/A slot the open list keeps, `scaled-super-r3` reading an A/A worst cell of 1.13% on the control and 0.26% on the basis here --- present as the entry says and small as the entry says its size never repeats; the basis's widest `scaled` cell is 0.99% and sits on `scaled-r5`. Where neither of stage ten's changes fires the pass still costs, `libunord-stage10-sum` over `libunord-stage7-sum` reading 1.0000 and 1.0017 inside registration (6)'s 3%.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.45* | *52* | *1.13x* |
| liblist-stage1-sum | -- | -- | 0.10 | 65 | 0.19x |
| liblist-stage2-sum | -- | -- | 0.08 | 69 | 0.16x |
| liblist-stage3-sum | -- | -- | 0.03 | 71 | 0.00x |
| liblist-stage4-list-sum | -- | -- | 0.02 | 71 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 71 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.17 | 65 | 0.19x |
| libunord-stage10-list-sum | -- | -- | 0.03 | 71 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 71 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 69 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 71 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.03 | 71 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.03 | 71 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.15* | *77* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *69* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.026* | *0.56* | *59* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.026 | 0.13 | 59 | 1.00x |
| lib-stage2-lean | 0.024 | 0.025 | 0.11 | 59 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.027* | *0.11* | *59* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.027 | 0.13 | 59 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.026 | 0.14 | 59 | 1.00x |
| **mut-odo-vecdims** | **0.028** | 0.058 | 0.11 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.028* | *0.058* | *0.09* | *59* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.028* | *0.058* | *0.11* | *59* | *1.00x* |
| lib-stage1 | 0.094 | 1.106 | 0.21 | 55 | 1.19x |
| *bq-expand-aa-adjacent* | *0.097* | *0.143* | *0.49* | *46* | *1.13x* |
| bq-expand | 0.097 | 0.143 | 0.44 | 46 | 1.13x |
| *bq-expand-aa-distant* | *0.097* | *0.144* | *0.05* | *46* | *1.13x* |
| list (baseline) | 1.000 | 1.000 | 2.56 | 16 | 21.32x |
| *list-aa-distant* | *1.030* | *1.045* | *0.45* | *17* | *21.32x* |
| *list-aa-adjacent* | *1.033* | *1.045* | *0.22* | *17* | *21.32x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0327, worst cell 4.47% on `runs-65536`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 0.9998 on a worst cell of 0.46% on `runs-2`, its interval covering 1. The in-situ term reads 1.0286, 1.0300 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0316, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h38m44s, peak 566 MiB in use, 247 MiB max residency; the reader reads 32 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 40.7 ms per call raw, 39.6 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.058/0.047/0.041/0.038/0.032/0.030/0.025/0.025/0.026/0.024/0.024/0.024/0.024/0.025

**Across the halves:** 5 of the 16 arms are faster on this half and 11 slower, at a geomean of 1.0003, from `lib-stage2-lean` at 0.9883 to `mut-odo-vecdims-add-in-leaf-u2-aa` at 1.0039, with `list` itself at 1.0006.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.058, tiers at 1.00x, 1.13x and 21.32x, identical on the control --- and the family's own ceiling ties the lean fill, both printing 0.024 where the column resolves 0.024376 against 0.024362. What the block prices is `lib-stage2-lean` over `mut-odo-vecdims` at 0.8031 over 8 of 14 shapes at sign p 0.79, a margin of 19.69% against this class's **3.27%** floor, the widest floor of the ten, and one of the two margins whose sign test does not separate it. **This class carries six of the run's registrations and is where the run's only kill falls.** Registration (3) wanted `libunord-stage7-sum` inside 3% across the halves here and it reads **0.9691**, 3.09 points off, so HEAD compiles the fused list some three percent faster on this class and the item dies by nine hundredths of a point. The other five hold: the lazy odometer's consumer over the port's base-offset table reads **0.3803** on the basis and **0.3950** on the control against a registered 0.47; over master's own slice recursion **0.3035** and **0.3122** against 0.37; the composed route over master's consumer **0.3032** and **0.3117**; the lean fill over `lib-stage1` **0.2316** and **0.2342** against 0.23, reproducing Runs 28 to 31; and where neither of stage ten's changes fires the pass costs 1.0003 and 0.9999. **And the two `-list-sum` arms read the class the loop change was landed for**: `libunord-stage10-list-sum` over its shared-loop twin reads 1.0201 and 1.0108 and `liblist-stage4-list-sum` over its own 1.0224 and 1.0105, the two within a quarter of a point of each other and inside this class's floor. `list` moved **0.06 of a point** between the halves, the closest to level of the eleven populations.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes, in the order they run: `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which landed 2026-09-09 and is `runs-96`'s construction under a `flip` name --- the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two; `flip-whole-square` (`l` 1798281, `sInner` 1341); `flip-last-c32` (`l` 165888, `sInner` 3); `flip-last-rows` (`l` 1800000, `sInner` 96); and the two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed, `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. The control sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.43* | *66* | *1.05x* |
| liblist-stage1-sum | -- | -- | 0.15 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.13 | 88 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.11 | 92 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.17 | 92 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.07 | 92 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.11 | 82 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.01 | 98 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.01 | 98 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 98 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.12* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *93* | *0.00x* |
| lib-stage2-lean | 0.023 | 0.042 | 0.30 | 84 | 1.00x |
| lib-stage2-lean-u1 | 0.024 | 0.047 | 0.29 | 83 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.042* | *0.32* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.043 | 0.09 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.043* | *0.12* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.045 | 0.28 | 80 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.028* | *0.052* | *0.09* | *77* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.028* | *0.052* | *0.23* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.052 | 0.10 | 77 | 1.00x |
| lib-stage1 | 0.034 | 0.048 | 0.34 | 80 | 1.00x |
| *bq-expand-aa-adjacent* | *0.091* | *0.179* | *0.43* | *61* | *1.05x* |
| bq-expand | 0.091 | 0.178 | 0.43 | 61 | 1.05x |
| *bq-expand-aa-distant* | *0.092* | *0.179* | *0.13* | *61* | *1.05x* |
| list (baseline) | 1.000 | 1.000 | 0.62 | 32 | 21.18x |
| *list-aa-distant* | *1.006* | *1.016* | *0.47* | *32* | *21.18x* |
| *list-aa-adjacent* | *1.007* | *1.019* | *0.25* | *32* | *21.18x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0126, worst cell 2.61% on `flip-last-rows`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9995 on a worst cell of 0.48% on `flip-last-c32`, its interval covering 1. The in-situ term reads 1.0203, 1.0219 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0093, which the correction amplifies by 1.35x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m39s, peak 209 MiB in use, 76 MiB max residency; the reader reads 32 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 31.1 ms per call raw, 30 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.024/0.024/0.052/0.048/0.026/0.026

**Across the halves:** 3 of the 16 arms are faster on this half and 13 slower, at a geomean of 1.0053, from `bq-expand` at 0.9931 to `mut-odo-vecdims-aa-distant` at 1.0230, with `list` itself at 1.0053.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.052, fourth lowest of the ten and just under `bcastmid`'s 0.053, and tiers at 1.00x, 1.05x and 21.18x, identical on the control --- and `lib-stage2-lean` leads outside the family at 0.023, priced against `mut-odo-vecdims` at 0.7617 over 5 of 6 shapes at sign p 0.22, a margin of 23.83% against this class's 1.26% floor. **`flip-last-rows` is still the bench where the forcing pass is about half the total**, so subtracting it multiplies any disagreement by about two; this run's A/A worst cell on this class is 3.76% on the basis and **8.11%** on the control, the control's on that very bench, and it is a wild cell rather than an intrusion --- `--wild` clears both processes. **The class says nothing else this run**, which is a result: `list` moved 0.53 of a point between the halves, the cross-half geomean is 1.0053, and `libunord-stage10-list-sum` reads 1.0091 and 1.0082 against its shared-loop twin on the two halves and `liblist-stage4-list-sum` 1.0015 and 1.0042 against its own, all four inside their registered 3%.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.13* | *103* | *1.06x* |
| liblist-stage1-sum | -- | -- | 0.11 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.14 | 122 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.02 | 129 | 0.00x |
| liblist-stage4-list-sum | -- | -- | 0.02 | 129 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.13 | 113 | 0.42x |
| libunord-stage10-list-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.03 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 129 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.15* | *130* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *122* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.024 | 0.08 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.021* | *0.024* | *0.11* | *111* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.025* | *0.13* | *111* | *1.00x* |
| lib-stage2-lean | 0.021 | 0.025 | 0.14 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.026 | 0.09 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.022 | 0.026 | 0.08 | 111 | 1.00x |
| **mut-odo-vecdims** | **0.023** | 0.029 | 0.07 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.023* | *0.029* | *0.08* | *111* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.029* | *0.09* | *111* | *1.00x* |
| lib-stage1 | 0.050 | 0.058 | 0.15 | 103 | 1.42x |
| bq-expand | 0.086 | 0.087 | 0.14 | 96 | 1.06x |
| *bq-expand-aa-adjacent* | *0.086* | *0.086* | *0.11* | *96* | *1.06x* |
| *bq-expand-aa-distant* | *0.086* | *0.087* | *0.13* | *96* | *1.06x* |
| *list-aa-distant* | *0.999* | *1.001* | *0.24* | *54* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 0.25 | 54 | 21.22x |
| *list-aa-adjacent* | *1.000* | *1.003* | *0.19* | *54* | *21.22x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 1.0042, worst cell 0.83% on `block-run64-page`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.07% on `block-run64-page`, its interval covering 1. The in-situ term reads 1.0219, 1.0115 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0016, which the correction amplifies by 2.63x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m51s, peak 133 MiB in use, 35 MiB max residency; the reader reads 32 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.62 ms per call raw, 4.47 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.019/0.024/0.029/0.024/0.019

**Across the halves:** 9 of the 16 arms are faster on this half and 7 slower, at a geomean of 1.0003, from `mut-odo-vecdims` at 0.9952 to `list` at 1.0055, with `list` itself at 1.0055.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.029, joint second lowest of the ten with `scaled`'s, and tiers at 1.00x, 1.06x and 21.22x, identical on the control --- and the family's own ceiling ties the lean fill, both printing 0.021 where the column resolves 0.021363 against 0.021187. What the block prices is `lib-stage2-lean` over `mut-odo-vecdims` at 0.9336 over 5 of 5 shapes at sign p 0.062, a margin of 6.64% against this class's 0.42% floor, the narrowest margin of the ten. **It is `runs`'s second, and it says the same things at half the size.** The odometer's consumer over the port's table reads **0.6799** on the basis and **0.6861** on the control against a registered 0.73; over master's recursion **0.4317** and **0.4358** against 0.46; the composed route over master's consumer **0.4330** and **0.4368**; and the lean fill over `lib-stage1` **0.4188** and **0.4152** against 0.42, where the two routes genuinely part. Where neither of stage ten's changes fires the pass costs 1.0009 and 1.0017, inside registration (6)'s 3%. **And registration (3)'s second population holds here where `runs` killed it**: `libunord-stage7-sum` across the halves reads 0.9935, 0.65 of a point off, where the class whose views route to runs read 3.09. `list` moved 0.55 of a point between the halves, so the columns may be differenced.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.18* | *222* | *1.44x* |
| liblist-stage1-sum | -- | -- | 0.21 | 228 | 1.64x |
| liblist-stage2-sum | -- | -- | 0.19 | 229 | 1.55x |
| liblist-stage3-sum | -- | -- | 0.23 | 227 | 1.69x |
| liblist-stage4-list-sum | -- | -- | 0.22 | 230 | 1.51x |
| liblist-stage4-sum | -- | -- | 0.21 | 230 | 1.51x |
| libunord-stage1-sum | -- | -- | 0.18 | 223 | 2.07x |
| libunord-stage10-list-sum | -- | -- | 0.29 | 233 | 0.57x |
| libunord-stage10-sum | -- | -- | 0.26 | 233 | 0.55x |
| libunord-stage6-loop-sum | -- | -- | 0.16 | 234 | 0.81x |
| libunord-stage6-sum | -- | -- | 0.19 | 234 | 0.83x |
| libunord-stage7-sum | -- | -- | 0.19 | 234 | 0.83x |
| libunord-stage9-sum | -- | -- | 0.26 | 233 | 0.55x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.24* | *238* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.05* | *249* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.02* | *250* | *0.01x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.051* | *0.066* | *0.23* | *230* | *1.27x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.051* | *0.065* | *0.25* | *230* | *1.27x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.051 | 0.065 | 0.27 | 230 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.054 | 0.066 | 0.26 | 229 | 1.27x |
| lib-stage2-lean | 0.056 | 0.102 | 0.20 | 227 | 1.46x |
| lib-stage2-lean-u1 | 0.056 | 0.101 | 0.18 | 227 | 1.46x |
| *mut-odo-vecdims-aa* | *0.060* | *0.086* | *0.22* | *229* | *1.27x* |
| *mut-odo-vecdims-aa-distant* | *0.060* | *0.086* | *0.23* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.060** | 0.086 | 0.21 | 229 | 1.27x |
| lib-stage1 | 0.093 | 0.106 | 0.16 | 221 | 2.32x |
| bq-expand | 0.134 | 0.194 | 0.13 | 218 | 1.44x |
| *bq-expand-aa-distant* | *0.135* | *0.194* | *0.25* | *217* | *1.44x* |
| *bq-expand-aa-adjacent* | *0.135* | *0.194* | *0.16* | *218* | *1.44x* |
| *list-aa-adjacent* | *0.999* | *1.002* | *0.16* | *179* | *21.57x* |
| *list-aa-distant* | *1.000* | *1.003* | *0.15* | *179* | *21.57x* |
| list (baseline) | 1.000 | 1.000 | 0.20 | 179 | 21.57x |

**Controls:** The largest A/A pair is `bq-expand-aa-adjacent` at 1.0051, worst cell 2.89% on `small-patch-k5`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9990 on a worst cell of 0.45% on `small-flat64`, its interval covering 1. The in-situ term reads 0.9934, 0.9929 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0043, which the correction amplifies by 1.24x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m54s, peak 131 MiB in use, 52 MiB max residency; the reader reads 32 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.77 us per call raw, 6.55 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.041/0.076/0.050/0.058/0.086

**Across the halves:** 11 of the 16 arms are faster on this half and 5 slower, at a geomean of 0.9923, from `mut-odo-vecdims-add-in-leaf-u2-aa` at 0.9576 to `lib-stage2-lean` at 1.0293, with `list` itself at 1.0157. **The baseline moved 1.57% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves, and this is the one class where property 3's level clause is read off a `mut-odo-vecdims` above 1.00x --- **1.27x on each half**, with `bq-expand` at 1.44x and `list` at 21.57x on BOTH, the per-call constants showing through a call under a microsecond, and the ORDER unbroken like the rest. `worst` is 0.086, the highest of the ten. `lib-stage2-lean` is the best arm outside the family at 0.056 where the family's ceiling leads at 0.051, priced against `mut-odo-vecdims` at 0.9316 --- **ahead on only 1 of 5 shapes at sign p 0.38**, so the paired figure and the win count disagree here and neither alone is the reading --- a margin of 6.84% against this class's 0.51% floor. **This class is one of the four whose two columns may NOT be differenced**, `list` having moved **1.57 points** between the halves, and it is where the only class miss of registration (15) falls: `libunord-stage10-list-sum` over its shared-loop twin reads 0.9991 on the basis, inside the registered 1.04 within 5%, and **0.9866** on the control, 5.34 points off and outside, while `liblist-stage4-list-sum` reads 1.0187 and 1.0032 and holds on both. Where neither of stage ten's changes fires the span prints 1.0477 and 1.0649 and no verdict, as registered, and its per-view clause lands exactly: behind on `small-flat64`, `small-patch-k5`, `small-patch-r5` and `small-row96` and ahead on `small-bcast32`, at 0.6326 and 0.6471.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.36* | *85* | *1.35x* |
| liblist-stage1-sum | -- | -- | 0.33 | 98 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.36 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.30 | 98 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.45 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage10-list-sum | -- | -- | 0.01 | 110 | 0.00x |
| libunord-stage10-sum | -- | -- | 0.01 | 110 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.40 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 110 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.33* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.014* | *0.016* | *0.29* | *98* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.014 | 0.016 | 0.33 | 98 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.014* | *0.016* | *0.35* | *98* | *1.00x* |
| lib-stage2-lean | 0.015 | 0.017 | 0.32 | 98 | 1.00x |
| lib-stage1 | 0.015 | 0.019 | 0.34 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.017 | 0.28 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.017 | 0.018 | 0.35 | 96 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.028* | *0.25* | *94* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.024* | *0.028* | *0.25* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.024** | 0.028 | 0.20 | 94 | 1.00x |
| *bq-expand-aa-adjacent* | *0.093* | *0.103* | *0.40* | *79* | *1.35x* |
| bq-expand | 0.093 | 0.103 | 0.36 | 79 | 1.35x |
| *bq-expand-aa-distant* | *0.094* | *0.102* | *0.27* | *79* | *1.35x* |
| *list-aa-distant* | *0.995* | *0.999* | *0.74* | *44* | *22.01x* |
| *list-aa-adjacent* | *0.998* | *1.004* | *0.65* | *44* | *22.01x* |
| list (baseline) | 1.000 | 1.000 | 0.88 | 44 | 22.01x |

**Controls:** The largest A/A pair is `list-aa-distant` at 0.9939, worst cell 1.66% on `compose-rev-bcast`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 0.9988 on a worst cell of 0.55% on `compose-rev-bcast`, its interval covering 1. The in-situ term reads 1.0063, 1.0214 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9941, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h11m11s, peak 126 MiB in use, 33 MiB max residency; the reader reads 32 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 31.2 ms per call raw, 30.1 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.028/0.028/0.021/0.019

**Across the halves:** 2 of the 16 arms are faster on this half and 14 slower, at a geomean of 1.0110, from `lib-stage2-lean` at 0.9941 to `lib-stage1` at 1.0243, with `list` itself at 1.0227. **The baseline moved 2.27% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold on both halves --- `worst` 0.028, the lowest of the ten, and tiers at 1.00x, 1.35x and 22.01x, identical on the control --- and the family's own ceiling leads at 0.014 against `lib-stage2-lean`'s 0.015, which the column resolves at 0.014409 against 0.014514. What the block prices is the lean fill and not the leader: `lib-stage2-lean` over `mut-odo-vecdims` at 0.6145 over 4 of 4 shapes at sign p 0.12, a margin of 38.55% against this class's 0.61% floor. **Stage ten's figure here is printed and not adjudicated, as registration (5) asks**: **0.6853** on the basis and 0.6859 on the control over stage seven, with `compose-zero-mid` at 0.4400 and 0.4404 and the two `-bcast` views at 0.7090 and 0.7086, 0.7079 and 0.7089, every one ahead past this class's floor, and `compose-scalar` inside it at 0.9986 and 1.0004 --- which is the closest any zero-stride view comes to putting stage ten behind stage seven and is why registration (5)'s first kill does not fire. Stage nine alone reads 0.6855 and 0.6860, so the tie-break moves nothing here either. **This class is one of the four whose two columns may NOT be differenced, and its `list` moved the FURTHEST of the eleven**, **2.27 points**, which is still a fifteenth of what the level moved it on Run 31.


## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 32's halves differ in ONE COMPILER and in nothing else.** One source, `Main.hs` at `f95795a`, one shim at `b3a1aca` and one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of the assembler, one roster, one shape set, one class list and one bench order, both run under `WILDLOG=1 SATURATE=1`, both with `-A32m -I0 -T -M8G` baked and read back by `+RTS --info`. What differs is the compiler and everything a compiler brings with it: the basis takes ghc-9.12.4 through `cabal.project` and its freeze, the control the in-tree stage1 of the GHC checkout, `10.1.20260803`, through `cabal.project.ghead` and its own freeze pinned at the same index-state --- TWO stores, TWO plans and TWO sets of boot libraries, which the binaries carry back as `ghc-internal-9.1204.0` and `ghc-internal-10.100.0`. **Neither half adds a regime flag**: `-O1` is micro.cabal's own `ghc-options` and no `--ghc-options` here overrides it, so both halves are plain -O1 and `diag` reads the ten-times-apart row on each. So this pair is Run 31's published recipe built by two compilers, and every figure that moves between the halves moved because one of them emitted it.

**The sequence was launched once and ran to the end in ONE window, and no population was rerun**, 2026-09-15T00:35:17 to 2026-09-15T07:42:54, twenty-two processes --- the main set on each half and 20 class processes, one per class per half --- with no hole between one finishing and the next starting. The gate's four ran before them from 00:01:51 to 00:35:14, the four rider stages after them to 07:56:01, and the counted work followed on a box already handed back, 07:56:21 to 08:38:33. Every process exited 0 at the count its population asks for, with no complaint and no `!!` line in the wall-clock record, and the ghead half ran first throughout. The two main-set processes are the only ones whose stderr line no block below carries: `run32-ghead-main` took 0h52m42s at a peak of 177 MiB in use and 60 MiB max residency and `run32-nospec-main` 0h52m25s at 174 MiB and 62 MiB, seventeen seconds and 3 MiB apart. **ONE process of this run was intruded on and it is not one of the twenty-two.** `run32-gate-nospec-a` carries 2 of its 95 benches at or above 0.25 of a core, peak 0.82, both on `cnn-L1-6x6-c1`, and the intruder was this session reading documents during the evening's first stage; `--wild` clears the other three gate processes, all twenty-two sequence processes and all eighty-eight alone-leg logs, so exactly ONE log of the hundred and fourteen reaches 0.25 of a core and it is a gate's. No figure this file publishes comes from a gate process, and the gate's verdict survives dropping that shape from both passes.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run32-nospec`, md5 `0fa8e3e39a1ec5d0bc080dd853473916`, and `run32-ghead`, md5 `9e8074782182dd8128a01309041204a7`, with `.text` at **20766917** bytes on the basis and **20920127** on the control --- the HEAD half larger by 153210, which is no multiple of 4096 --- as no compiler pair's gap here has been, Runs 24 to 28 reading 140922, 140922, 145018, 140922 and 145018, where the flag pairs' were exact multiples --- and their load addresses differ too, 4218880 against 4214784, where Run 31's halves shared one. **NEITHER md5 is two-sided this run**: `Main.hs` moved from `13cbd0d` to `f95795a` in six commits, so neither half's inputs are an earlier binary's, no md5 here can reproduce one and an unequal one names nothing. What they do say is that they differ from each other, which is two compilers having emitted two binaries. **And the basis's `.text` is `run28-g912`'s, `run29-nospec`'s and `run30-nospec`'s to the byte**, 20766917 FOUR times over across every commit `Main.hs` has taken since Run 28 built, an md5 that reproduces none of them and, on Run 28's, a different REGIME; `run30-libcase` and `run31-o2` share 20787397 the same way. That is an observation and this file offers no mechanism for it.

**The source moved and a timed function landed, so this run's fills read the pinning claim in the form the README states it --- and they REOPEN its strong form.** The tracked 28-byte loops were read at the build against the previous build of the basis's recipe, `run31-nospec`, with `--delta`, and re-read here off the same two binaries: the two-copy group keeps every mod-64 offset, `[0, 0]`, and **the six-copy group does NOT** --- `[0, 24, 8, 0, 4, 0]` becomes `[0, 0, 8, 0, 4, 0]`, its second head moving by 24 --- with NO address surviving to the byte on either and a third displacement on the six-copy group, `0x9928`, which is 40 mod 64 and so not a whole line. README's ruling is that under the dead-spot form the strong form holds for the tracked heads' OFFSETS, and says outright that what would reopen it is a tracked head's offset moving. This moves one, over six commits to `Main.hs` where Runs 24's and 25's readings had a roster change and nothing else under them; the verdict is [that section][floor]'s to write. **Within the pair the layout moved too, and by more.** `./loop-offsets.py run32-ghead run32-nospec` puts the six-copy group at `[0, 0, 8, 0, 4, 0]` on the basis and `[18, 0, 0, 31, 9, 2]` on the control, the two-copy group at `[0, 0]` on both, with 33 self-loops of 28 B in 26 distinct byte-sequences on the basis and 34 in 27 on the control. So a compiler displaces fill copies as an optimisation level does, and neither displacement is what the claim covers.

**The three main-set anchors** read **6.33 us** on `cnn-slice-c32`, **3.73 ms** on `cnn-L2-24x24-c32` and **39.8 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, against **6.29 us**, **3.68 ms** and **39.8 ms** on the control --- the control faster by **0.66%** and **1.24%** on the first two and level to three figures on the third, computed from the cells rather than from the printed figures. That is `list` being what a compiler buys, beside the 0.43 points its main-set column reads, and it is two orders of magnitude below what Run 31's anchors read for a level, which were 20.40%, 22.04% and 20.01% apart. **Two conventions are in play in this file and a bare percentage does not say which**: a `cross` figure is a RATIO, so `list`'s 1.0043 is 0.43 points, while a saving is read against the slower half, so the same movement is 0.43% of the basis's time --- the two coincide here only because the movement is small. The head quotes the first and the anchors and the counted work the second. The figures below are over the run's own nineteen main-set shapes, nothing omitted or requoted.
| shape | `l` | `list`, per call | net | `ghead`, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.5 us | 6.33 us | 6.29 us |
| `cnn-L2-24x24-c32` | 165888 | 3.83 ms | 3.73 ms | 3.68 ms |
| `stretch-wide-2xM` | 1800000 | 40.9 ms | 39.8 ms | 39.8 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at **40.71 ms** raw and **39.63 ms** net on the basis, against Run 31's 40.53 ms and 39.45 ms on the same recipe --- 0.45% slower raw and net alike, computed from the cells, and it is the box and the six commits since together rather than either alone. **The control half reads the same two figures to four significant digits**, 40.71 ms and 39.63 ms, where Run 31's control read 20.02% below its basis on this shape; that is the compiler doing nothing to the reference on the class whose floor is the widest of the ten, 3.27%. **No class population moved this run at all**: `roster-delta.py` reads 58 class views to 58 with every one of the ten classes unmoved, so each class anchor is read against its own predecessor over the same shapes, and the one arm that landed changed which arms a class times without changing which shapes it times.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0000** on the basis and **0.9998** on the control, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0245** and **1.0967** on the basis and **1.0268** and **1.1000** on the control for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent over the term it stands for, by nearly the same few percent on both halves, which is where it has run since Run 17. The term itself is a median **2.7%** of `list` and **37.6%** of `mut-odo-vecdims` over the shapes on the basis, 37.8% on the control. So the compiler reaches neither the forcing pass nor the gap between the two ways of measuring it.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-14, before the run, on the pair declared in [Run 31's file](run31.md#what-the-next-run-compares-against): Run 31's basis recipe, ghc-9.12.4 in the dead-spot form at plain -O1, as the basis, `nospec` still naming what that half is, against GHC HEAD at plain -O1 and nothing else, `ghead`, built through `cabal.project.ghead` as Run 19's and Run 27's HEAD halves were, so that `--compare` reads nospec over ghead and every `cross` below is what the compiler does to one arm at one level; over Run 31's roster plus `liblist-stage4-list-sum`, base's `sum` over stage four's list, landing 2026-09-14, and with the shared loop `sumLazyRuns` summing each run through `sumNoSpec` since the same day, a loop without vector's `SPEC` argument, which at -O1 reads -O2's counts on every runs shape, within a hundred instructions a call --- so the consumers carry -O2's counts on both halves and the two `-list-sum` arms, keeping vector's `sum`, are what price the argument. Items (1) to (14) are Run 31's as they stand in [that run's file](run31.md#what-this-run-was-built-to-answer-and-what-it-answered): (4), (6) to (9) and (11) to (14) copied unchanged but for `ghead` written where they said `o2`; (1), (2), (3) and (10), whose `cross` bands were `-O2`'s, re-aimed at a compiler pair, their bands read off Run 27's 9.12.4-against-HEAD JSONs with `--compare` on 2026-09-14; and (5), killed on Run 31, re-registered at what that run read, the cancellation on three `window` views included, so that a kill here says the reading did not reproduce. (15) is edited for the loop change and (16) is new, every figure the two quote read off Run 31's JSONs on 2026-09-14 as the ratio of the `-list-sum` arm on the basis to the shared loop's consumer on the `o2` half, per shape and geomeaned, which is what the pair should read now that the shared loop is -O2's at -O1. **Four of the spans below lost their prior on 2026-09-14, after this list was written**, and are registered at figures the tree no longer produces: `08ee255` and `e12b000` took an unzip immediately undone by a zip out of the orders, which is worth 392 bytes an axis to stage nine and stage ten, 168 to stage six and stage seven and nothing to `libunord-stage1-sum`, so (5)'s two spans over stage seven, (6)'s four, (13)'s seven and (14)'s five over master's consumer price a dispatch that has changed under them. What the change is worth to them is NOT settled here: a probe of the two binaries over a reduced class roster moves those spans between 7% below and 25% above the same probe's reading of the pristine binary, and `libunord-stage1-sum`, whose source neither commit touched, moves 0.956 to 1.057 across the nine classes in the same pair of processes, so that probe's own context term is wider than the 3% two of the spans are registered at and it bounds nothing. It also puts (5)'s three cancelling `window` views at 1.03, 0.96 and 0.85 where Run 31 read 1.3666, 1.2355 and 1.0785, which would retire the cancellation (5) expects to reproduce; that reading wants an A/A floor before it is believed. So a miss on those four is the source's until a pair with a floor says otherwise, and what re-aims them is Run 32's own basis half.

(1) *The compiler on the shipped fill.* On the main set: `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 2%`, `predict: cross lib-stage1 1.0 within 2%` and `predict: cross lib-stage2-lean 1.0 within 2%`; on every class: `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 4%`, `predict: cross lib-stage1 1.0 within 4%` and `predict: cross lib-stage2-lean 1.0 within 4%` --- Run 27's compiler pair, 9.12.4 over HEAD, read the three at 0.9995, 0.9956 and 1.0197 on the main set, the lean fill the one of them past 1%, and its class readings of the leaf and of `lib-stage1` between 0.9693 and 1.0310; Run 31 read the three at 0.9956, 0.9972 and 0.9997 under `-O2`. Killed by any of the three past 2% on the main set, which would say HEAD reaches the shipped fill where neither -O2 pass nor the level did.

(2) *The compiler on the reference.* On the main set: `predict: cross list 1.0 within 2%`; on every class: `predict: cross list 1.0 within 3%` --- Run 27's compiler pair read `list` at 0.9967 on the main set, inside the 0.7% differencing bar, and 0.9768 to 1.0062 across the classes, where Run 25's read it 1.10% apart and outside the bar, `list` being the reference every table is read against. Killed by `list` outside 2% of 1 on the main set, which would say HEAD moves the reference by more than any compiler pair here has; and the two columns are differenced only where it sits inside the bar, which the two compiler pairs straddle.

(3) *The compiler on the fused list.* The lazy stages' consumers fold one flat loop, `sumLazyRuns`, whose continuations are known and which sums each run through `sumNoSpec` since 2026-09-14, so the loop carries nothing a level would remove and a compiler is what is left to move it. On `runs`, `window` and `block`: `predict: cross libunord-stage7-sum 1.0 within 3%`; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, where the route fills or is one block: `predict: cross libunord-stage7-sum 1.0 within 2%` --- Run 27's compiler pair read the consumer it then had, stage five's over the level-form walker, at 0.9963, 1.0127 and 1.0044 on those three and between 0.9945 and 1.0035 on the five. Killed by the consumer past 3% on `runs` or `block` on either side of 1, which would say HEAD compiles the walker or its sum differently, the counts then saying which.

(4) *The odometer over the table.* `liblist-stage4-sum` over `liblist-stage2-sum`, the lazy odometer's consumer against the port's base-offset table's. On `runs`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.47 within 25%`; on `block`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.73 within 25%` --- Run 30 reading 0.4678 and 0.3947, 0.7281 and 0.6800. Killed by the odometer's consumer behind the table's past the floor on the `ghead` half, which would say the lazy list's lead over the branch's list is one regime's and not the design's.

(5) *Stage ten where both of its changes fire, and where one does.* `libunord-stage10-sum` over `libunord-stage7-sum` on `bcast`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.55 within 20%`; on `bcastmid`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.57 within 20%`; on `compose` the same span prints a figure and no verdict, read per view by hand, `compose-zero-mid` and the two `-bcast` views ahead past the floor and `compose-scalar` inside it. Over `libunord-stage9-sum` on `window`, both halves: `predict: pair libunord-stage10-sum libunord-stage9-sum 0.96 within 5%` --- Run 31 reading 0.5497 and 0.5491, 0.5715 and 0.5686, 0.9557 and 0.9847, and on `window` the two reorderings CANCEL on three views, stage ten behind stage nine on both halves at 1.3666 and 1.3689 on `window-128x128-k7`, 1.2355 and 1.2359 on `window-64x64-k1x9` and 1.0785 and 1.1985 on `window-224x224-k3`, ahead past the floor on `window-28x28-k5`, `window-32x32-c64-k3` and `window-64x64-c16-k3` and within a percent on the two remaining views, which is what killed the item as Run 31 registered it and what this registration expects to reproduce. Killed by stage ten behind stage seven past the floor on both halves on any zero-stride view, which would say the two changes do not compose where both fire; or by any of the three cancelling `window` views not behind stage nine past the floor on both halves, or any of the three ahead views behind it past the floor on both, which would say Run 31's cancellation was that run's own.

(6) *Stage ten where neither fires.* On `rev`, `scaled`, `runs` and `block`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 1.0 within 3%` --- `zerosOutermost` hands its list back unchanged where no stride is 0, so `routeUnord10` is `routeUnord7` there to the byte and only the extra pass over the axes separates the dispatches, which `rev` has shown for three runs, at 1.0244 and 1.0245 on Run 30. Killed by the pair past 3% on both halves on any of the four, which would say the pass is visible at a call of tens of microseconds by more than that. On `small` the span prints a figure and no verdict, read per view by hand: behind on the four views other than `small-bcast32` by the pass and ahead on `small-bcast32`.

(7) *`lib-stage1` against the lean fill with one fill under both.* Both fill through `fillStage2` since 2026-09-11, so on `bcast` and `bcastmid` the pair prices the dispatch alone. On `bcast` and on `bcastmid`, both halves: `predict: pair lib-stage2-lean lib-stage1 1.0 within 3%`. Killed by the pair past 3% on both halves on either class, which would say the canonicalization is worth that much to the fill on broadcast views. On `runs` and `block` the routes part, stage one concatenating a slice per run where the lean dispatch fills: `predict: pair lib-stage2-lean lib-stage1 0.23 within 20%` on `runs` and `predict: pair lib-stage2-lean lib-stage1 0.42 within 20%` on `block`, both halves, as Runs 28 to 30 all read; killed by the lean fill not ahead past the floor on either, which would say the slice-and-concatenate route no longer loses on short runs.

(8) *The floor with the fill family's pair for a third run.* On the main set and on every class, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa mut-odo-vecdims-add-in-leaf-u2 1.0` and `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa-distant mut-odo-vecdims-add-in-leaf-u2 1.0`, each inside the population's floor as Run 30's forty-four readings were; read beside them by hand which pair carries the whole-set floor on each half, `bq-expand-aa-distant` on Run 30's basis where Runs 28 and 29 read the fill pair's, which is what the floor entry's open question turns on. Killed by the adjacent copy outside the floor on both halves on the main set.

(9) *What the leaf fusion buys.* The shipped leaf against the family root, the one ordering the roster still prices that the library has taken. On the main set, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2 mut-odo-vecdims 0.64 within 5%` --- read on five runs, Run 29's 0.6441 and 0.6413 and Run 30's 0.6446 and 0.6525 the latest. Killed by the pair outside 5% on the `ghead` half, which would say what the fusion buys is the regime's.

(10) *The allocation levels across compilers.* Off `--alloc` on both halves, by hand, over all nineteen main-set shapes, `cnn-L1-6x6-c1` included: on the basis the levels reproduce Run 31's, the `libunord` consumers 0.00x, the fills and the `liblist` consumers 1.00x, `bq-expand` 2.78x and `list` 25.20x; on the `ghead` half the same, `bq-expand` within 2% of 2.78x and `list` within 2% of 25.20x --- Run 27's compiler pair agreed to 1e-4 on 505 of its 627 allocating cells on the main set with a worst disagreement of 1.6%, where `-O2` took `bq-expand` to 2.11x and `list` to 23.45x on Run 31. Killed by any level moving past 2% on the `ghead` half, which would say HEAD changes what an arm allocates and not only how fast it runs.

(11) *The lean fill's unrolling and the leaf.* On the main set, both halves: `predict: pair lib-stage2-lean lib-stage2-lean-u1 0.96 within 5%`, the unrolling worth about four points under the lean dispatch, Run 30 reading 0.9622 and 0.9575; and `predict: pair lib-stage2-lean mut-odo-vecdims-add-in-leaf-u2 0.98 within 3%`, Run 30 reading 0.9914 at 13 of 19 and 0.9715 at 15 of 19. Killed by the lean fill behind its un-unrolled twin past the floor on either half, which would say the unrolling is worth nothing under this dispatch; or ahead of the shipped leaf by more than 5% on both halves, which would say the dispatch beats the leaf it wraps.

(12) *The lazy odometer's consumer against master's.* `liblist-stage4-sum` over `liblist-stage1-sum`, the odometer list under the lean dispatch against the slice recursion the library ships, both folded by `sumT`. Both halves, on `runs`: `predict: pair liblist-stage4-sum liblist-stage1-sum 0.37 within 25%`; on `block`: `predict: pair liblist-stage4-sum liblist-stage1-sum 0.46 within 10%`; on `rev`: `predict: pair liblist-stage4-sum liblist-stage1-sum 0.92 within 10%`; on `bcast`: `predict: pair liblist-stage4-sum liblist-stage1-sum 1.0 within 1%` --- Run 30 reading 0.3720 and 0.3205, 0.4616 and 0.4433, 0.9240 and 0.9162, 0.9983 and 0.9985. Killed by the odometer's consumer not ahead of master's past the floor on `runs` or `block` on either half, which would say the lazy list buys a fold nothing over what the library ships; or behind it past the floor on `bcast` on both halves, where both routes fill.

(13) *The zero-stride move alone.* `libunord-stage9-sum` moves every zero-stride axis outermost and otherwise keeps stage six's order. Over `libunord-stage7-sum`, both halves, on `bcast`: `predict: pair libunord-stage9-sum libunord-stage7-sum 0.59 within 20%`; on `bcastmid`: `predict: pair libunord-stage9-sum libunord-stage7-sum 0.62 within 20%`; on `compose`: `predict: pair libunord-stage9-sum libunord-stage7-sum 0.69 within 10%`; on `rev`, where neither reordering fires: `predict: pair libunord-stage9-sum libunord-stage7-sum 1.0 within 3%`; on `window`, where no view has a zero stride and the tie-break is what this stage lacks: `predict: pair libunord-stage9-sum libunord-stage7-sum 1.25 within 15%` --- Run 30 reading 0.5903 and 0.5588, 0.6215 and 0.5828, 0.6941 and 0.6886, 1.0208 and 1.0214, 1.3730 and 1.1344. Over `libunord-stage6-sum` on `bcast` and `bcastmid`, both halves: `predict: pair libunord-stage9-sum libunord-stage6-sum 0.59 within 20%` and `predict: pair libunord-stage9-sum libunord-stage6-sum 0.62 within 20%`, the tie-break moving nothing on those views, Run 30 reading 0.5905 and 0.5585, 0.6211 and 0.5832. Killed by stage nine behind stage seven past the floor on both halves on any `bcast` or `bcastmid` view, which would say the move costs where it should pay; or not behind stage seven past the floor on `window` on both halves, which would say the tie-break is worth nothing there.

(14) *What the composed route buys a fold.* `libunord-stage10-sum` over `libunord-stage1-sum`, stage seven's tie-break under stage nine's move against master's own consumer, both halves: on `runs`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.37 within 25%`; on `block`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.46 within 10%`; on `window`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.42 within 15%`; on `bcast`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.59 within 20%`; on `rev`: `predict: pair libunord-stage10-sum libunord-stage1-sum 0.53 within 10%` --- Run 30 reading 0.3720 and 0.3199, 0.4631 and 0.4436, 0.4157 and 0.3762, 0.5899 and 0.5577, 0.5334 and 0.5359. And where no pair of axes is tied on stride the tie-break is the identity and stage ten is stage nine: on `bcast`, `bcastmid` and `compose`, both halves, `predict: pair libunord-stage10-sum libunord-stage9-sum 1.0 within 0.5%`, Run 30 reading 1.0000 and 0.9999, 0.9997 and 0.9999, 1.0001 and 0.9998. Killed by stage ten not ahead of master's consumer past the floor on any of the five populations on either half; or by it parting from stage nine past the floor on both halves on any of the three, which would say the tie-break fires where no pair is tied.

(15) *What the shared loop's SPEC-free sum leaves the list interface.* `libunord-stage10-list-sum` over `libunord-stage10-sum`, base's `sum` over stage ten's list against the harness's own fold through `sumLazyRuns`: the same route under both, and since 2026-09-14 differing in two things rather than one --- the user's fold is inlined into the route where the harness meets one loop compiled once for every stage, which is what Run 31 asked, and the user's fold sums each run through vector's `sum`, which carries the `SPEC` argument SpecConstr is meant to specialise away and at -O1 re-passes it every element and once a run, where the harness's loop now sums through `sumNoSpec` and carries nothing. Run 31 read the pair at 1.0002 on the main set and 1.0132, 1.0154, 1.0170, 1.0080 and 1.0133 on `runs`, `window`, `block`, `flip` and `small`, both arms then paying the argument; what the argument is worth is what `-O2` was worth to the harness's loop there, 1.0267 on `runs` and 1.0168 on `window` on that run, and the two multiply per shape into the figures below. On the main set, both halves: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0 within 2%`, read at 1.0061; on `runs`: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.04 within 4%`, read at 1.0403, `runs-2` at 1.42 and `runs-3` at 1.14 carrying it and every shape from `runs-5` up inside 1.2%; on `window`: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.03 within 4%`, read at 1.0325; on `block`: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.02 within 3%`, read at 1.0171; on `small`: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.04 within 5%`, read at 1.0580, where the level's per-call constants are part of what `-O2` read and the loop alone is expected to buy less; on `flip`, `bcast`, `bcastmid` and `compose`: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0 within 3%`, read at 1.0093, 1.0103, 1.0063 and 1.0044; on `rev` and `scaled`, where both consumers fold one vector and the argument is paid on a loop the add chain paces: `predict: pair libunord-stage10-list-sum libunord-stage10-sum 1.0 within 1%`, read at 1.0020 and 1.0040. Killed by the pair inside 1% of level on `runs` and `window` on both halves, which would say the SPEC-free sum bought the shared loop nothing a run's length shows; or by it past its band there on both halves, which would say the argument costs more in the run than the probe's counts and Run 31's cross say.

(16) *The same fold on the lean route.* `liblist-stage4-list-sum` over `liblist-stage4-sum`, base's `sum` over stage four's list against the harness's fold through the same loop, the arm landing 2026-09-14 so that the `-list-sum` pair stands on both routes in every run from here. Stage four and stage ten walk one loop where the canonical axes are already in stride order, which Run 31's counts put at the `runs` and `block` classes, the two consumers counting alike there to a tenth of a percent; on every other population stage ten's sort or its zero-stride move gives the fold a different walk, the main set among them, and the counts part by up to 3.7x. So on `runs` and `block` this pair is registered at stage ten's figures, and elsewhere at stage ten's bands as the only prior: on the main set, both halves: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.0 within 2%`; on `runs`: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.04 within 4%`; on `window`: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.03 within 4%`; on `block`: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.02 within 3%`; on `small`: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.04 within 5%`; on `flip`: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.0 within 3%`; on `rev` and `scaled`: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.0 within 1%`. On `bcast`, `bcastmid` and `compose` the routes part, stage ten moving a zero-stride axis outermost where stage four leaves it where the canonical view puts it, and no reading of stage four's list arm exists: `predict: pair liblist-stage4-list-sum liblist-stage4-sum 1.0 within 3%`, the band stage ten's pair read inside on Run 31. Killed by the pair inside 1% of level on `runs` and `window` on both halves, as (15) is; or by this pair and (15)'s parting past the floor on `runs` or `block` on both halves, which would say the two routes do not walk one loop where the counts say they do.

**FIFTEEN of the sixteen held and ONE was killed, fewer than any of Runs 28 to 31 recorded --- four, three, two and three --- and the pattern the kills have traced since Run 28 is back.** Run 28's four, Run 29's three and Run 30's two all fell on items predicting a NO-OP; Run 31 broke that with two of three falling on items predicting an EFFECT; this run's single kill predicts a no-op again and misses by nine hundredths of a point. Every item's verdict below is its KILL CONDITION applied across the populations that item names, never one sweep of the main set; every figure is re-derived from this run's own JSONs, by `--predictions` over all eleven populations on both halves --- the basis half read as `run32-nospec-POP.json --compare run32-ghead-POP.json` and the control half as the same call with the two swapped, so that a `pair` span is read within each half in turn --- and by `--pair --per-shape` where an item's clause is per view; a span is judged against the floor of the population it is read on. **Two things govern how the whole list reads.** The first is that the pair's variable reaches almost nothing, so an item predicting level is predicting what the run produced everywhere: ELEVEN of the sixteen are inside their bands on both halves in every population they name, and the five that are not miss on one population each --- (1) on `window`, where two of the three fill arms leave the class band; (3) on `runs`, which is the kill; (5) and (15) on the CONTROL half's `window` and `small`; and (13) on the BASIS half's `window`. The second is the disclosure the entry carries about four of its spans, whose priors the unzip-and-zip removal of 2026-09-14 retired; all four --- (5)'s over stage seven, (6)'s, (13)'s and (14)'s over master's consumer --- come back INSIDE their registered bands anyway, so the source change the entry could not price did not move them past what was registered.

(1) *The compiler on the shipped fill.* **HELD, and by more than the item asked.** On the main set the three arms read **1.0052**, **1.0058** and **1.0134** against a 2% bar, the widest 1.34 points off, so the kill --- any of the three past 2% on the main set --- does not fire. Run 27's compiler pair read them at 0.9995, 0.9956 and 1.0197 and Run 31's level at 0.9956, 0.9972 and 0.9997, so three regimes now put HEAD and the level alike within a point and a third of the shipped fill. **Its class readings are two short of clean**: twenty-eight of the thirty hold inside the registered 4%, and both misses are on `window` --- `lib-stage1` at **1.0528** and `lib-stage2-lean` at **1.0510**, with the leaf holding there at 1.0262. Fifteen of the thirty sit outside 1%, over nine of the ten populations and reading both ways, `lib-stage1` and `lib-stage2-lean` six apiece and the leaf three; Run 31 read ten of thirty outside 1% on a different set of populations.

(2) *The compiler on the reference.* **HELD, where Run 31's was killed, and it governs the whole file.** `cross list 1.0 within 2%` reads **1.0043** on the main set, 0.43 of a point off, and the class clause, `1.0 within 3%`, holds on all ten: `runs` 1.0006, `bcast` 0.9987, `window` 0.9949, `flip` 1.0053, `block` 1.0055, `bcastmid` 1.0059, `scaled` 1.0119, `rev` 0.9855, `small` 1.0157 and `compose` 1.0227. Run 27's compiler pair read 0.9967 on the main set and Run 25's 1.0110; Run 31's `-O2` read 1.2974. **What the item's own clause then delivers is the differencing bar**: the main set and six of the ten classes sit inside the 0.7% that lets two columns be subtracted, so seven of this run's eleven populations carry cross-half readings that are measurements rather than orderings --- which no run since Run 28 could say of any population.

(3) *The compiler on the fused list.* **KILLED, by nine hundredths of a point, and it is this run's only kill.** The kill wants the consumer past 3% on `runs` or `block` on either side of 1. `libunord-stage7-sum` across the halves reads **0.9691** on `runs`, **3.09** points off, and the item dies there; `block` reads 0.9935 at 0.65 and does not fire it. On the five classes given a 2% band the consumer reads `rev` 1.0007, `bcast` 0.9955, `bcastmid` 0.9981, `scaled` 1.0041 and `compose` 0.9989, every one inside, and `window` reads 0.9729 inside its own 3%. **So HEAD compiles the walker or its sum about three percent faster where a view routes to runs, and nowhere else**; the counts say which, and they say codegen: `runs`' counted legs come in at 406s and 405s for the pair, within a second of each other, so the instructions are the same and the time is not.

(4) *The odometer over the table.* **HELD.** `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.3803** on the basis and **0.3950** on the control on `runs`, against a registered 0.47 within 25%, and **0.6799** and **0.6861** on `block`, against 0.73. The kill wants the odometer's consumer BEHIND the table's on the `ghead` half and it is ahead by better than half and by a third, so the lazy list's lead over the branch's list is the design's and not any compiler's --- which is the fourth regime it has survived.

(5) *Stage ten where both of its changes fire, and where one does.* **HELD, and the cancellation Run 31 was killed by REPRODUCES on two of its three views.** Over stage seven it reads **0.5478** and 0.5545 on `bcast` against a registered 0.55 and **0.5669** and 0.5780 on `bcastmid` against 0.57, both halves inside 20%, with `compose` at 0.6853 and 0.6859 and no verdict as registered. The first kill wants stage ten behind stage seven past the floor on both halves on some zero-stride view and none is: the closest is `compose-scalar` at 0.9986 and **1.0004**, inside that class's floors of 0.61% and 0.37%. The second wants one of the three cancelling `window` views NOT behind stage nine on both halves, or one of the three ahead views behind it: `window-128x128-k7` reads **1.3899** and **1.2707** and `window-64x64-k1x9` 1.2512 and 1.2166, both behind on both halves as registered, while `window-224x224-k3` is behind on the basis at 1.1894 and ahead on the control at 0.9817 --- one half each, which is not the *both halves* the kill asks for --- and the three ahead views read 0.7549 and 0.7042, 0.6799 and 0.5740, 0.8476 and 0.6883, ahead on both. **The span itself misses on the control half**: `predict: pair libunord-stage10-sum libunord-stage9-sum 0.96 within 5%` reads 0.9868 on the basis, inside, and **0.8982** on the control, 6.18 points off, HEAD deepening what stage ten hands back rather than removing it.

(6) *Stage ten where neither fires.* **HELD, and `rev` reproduces for the fifth run running.** The pair reads **1.0111** on the basis and 1.0097 on the control on `rev` --- Run 30 read 1.0244 and 1.0245 and Run 31 1.0244 and 1.0241 on the same recipe --- with `scaled` at 1.0000 and 1.0017, `runs` at 1.0003 and 0.9999 and `block` at 1.0009 and 1.0017, every one inside the registered 3% on both halves, so the kill does not fire. The extra pass over the axes is therefore visible on `rev` at a call of tens of microseconds and on nothing else, and it is a point rather than the two and a half Runs 30 and 31 read. On `small` the span prints **1.0477** and **1.0649** and no verdict, and its per-view clause lands exactly: behind on `small-flat64` at 1.1732 and 1.1974, `small-patch-k5` at 1.2631 and 1.2684, `small-patch-r5` at 1.1808 and 1.2206 and `small-row96` at 1.1405 and 1.1413, and ahead on `small-bcast32` at 0.6326 and 0.6471.

(7) *`lib-stage1` against the lean fill with one fill under both.* **HELD, and it reproduces Run 28's figures for a fifth run.** On `bcast` the pair reads **0.9930** and **0.9905** and on `bcastmid` **1.0075** and **1.0056**, all inside the registered 3%, so the canonicalization is worth nothing to the fill on broadcast views once both routes fill through `fillStage2` --- now under a second compiler as well. Where the routes genuinely part both come back: **0.2316** and **0.2342** on `runs` against 0.23, and **0.4188** and **0.4152** on `block` against 0.42. The kill wants the lean fill not ahead past the floor on either and it is ahead by three quarters and by three fifths.

(8) *The floor with the fill family's pair for a third run.* **HELD in every one of its forty-four readings**, two spans over eleven populations on two halves, and the kill --- the adjacent copy outside the floor on both halves on the main set --- does not fire: it reads **0.9974** on the basis against a 0.66% floor and **1.0049** on the control against a 0.68% one. **Read beside it by hand, as the item asks, the two halves name DIFFERENT pairs and neither is the fill family's.** On the BASIS the whole-set floor is `bq-expand-aa-distant`'s at **0.66%**, which is Run 30's and Run 31's answer repeated; on the CONTROL it is `mut-odo-vecdims-aa-distant`'s at **0.68%** --- the family ROOT's pair, which no half has carried before. So the open question in the floor entry has a fifth reading and a third distinct answer, and the two halves' floors are a hundredth of a point apart where Run 31's stood at 0.61% against 1.58%.

(9) *What the leaf fusion buys.* **HELD.** `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6459** on the basis and **0.6358** on the control against a registered 0.64 within 5%, a point apart, and the kill --- the pair outside 5% on the `ghead` half --- does not fire. Run 29 read 0.6441 and 0.6413, Run 30 0.6446 and 0.6525 and Run 31 0.6417 and 0.6436, so eight readings across four runs now lie between 0.6358 and 0.6525, under two flags, a whole level, two rosters and two compilers.

(10) *The allocation levels across compilers.* **HELD, and more exactly than the item asked, which is where Run 31's was killed.** On the basis the levels reproduce Run 31's: the `libunord` consumers 0.00x, the fills and the `liblist` consumers 1.00x, `bq-expand` **2.78x** and `list` **25.20x**. On the `ghead` half they reproduce them EXACTLY, `bq-expand` at 2.78x and `list` at 25.20x, and every one of the ten class triples reads the same on the control as on the basis to the printed precision. `--alloc` over the two main-set processes puts 448 of the 570 allocating cells inside 1e-4, the worst disagreement 1.9e-2 on `stretch-inner256/libunord-stage7-sum`, with the 38 cells under 100 bytes a call set aside, 33 of them disagreeing at a fit that resolves nothing. So HEAD changes nothing an arm allocates, where `-O2` took `bq-expand` to 2.11x and `list` to 23.45x on the run before.

(11) *The lean fill's unrolling and the leaf.* **HELD on both spans and neither kill fires.** `lib-stage2-lean` over `lib-stage2-lean-u1` reads **0.9669** on the basis and **0.9602** on the control against a registered 0.96 within 5%, so the unrolling is worth three to four points under the lean dispatch; over the shipped leaf it reads **0.9983** and **0.9903** against 0.98 within 3%. The lean fill is not behind its un-unrolled twin on either half, and it is ahead of the shipped leaf by under a point on both, where the kill wants more than five.

(12) *The lazy odometer's consumer against master's.* **HELD on all four of its spans.** `liblist-stage4-sum` over `liblist-stage1-sum` reads **0.3035** and **0.3122** on `runs` against a registered 0.37, **0.4317** and **0.4358** on `block` against 0.46, **0.9196** and **0.9272** on `rev` against 0.92, and **0.9990** and **0.9985** on `bcast` against 1.0 within 1%. Neither kill fires: the odometer's consumer is ahead of master's past the floor on `runs` and `block` on both halves, and it is not behind master's on `bcast` on either.

(13) *The zero-stride move alone.* **HELD on both kills, with the `window` span missed on the BASIS half alone --- which is the reverse of Run 31, where the miss was on both.** Over stage seven, `libunord-stage9-sum` reads **0.5461** and 0.5549 on `bcast` against a registered 0.59, **0.5690** and 0.5789 on `bcastmid` against 0.62, **0.6855** and 0.6860 on `compose` against 0.69 and **1.0068** and 1.0074 on `rev` against 1.0 within 3%, all inside. On `window` it reads **1.0176** on the basis, 23.24 points off the registered 1.25 and outside the 15%, and **1.1162** on the control, 13.38 points off and INSIDE it. Over stage six on `bcast` and `bcastmid` it reads 0.5459 and 0.5550, 0.5690 and 0.5795, the tie-break moving nothing on those views as registered. The first kill wants stage nine behind stage seven on some `bcast` or `bcastmid` view and no view is; the second wants it NOT behind stage seven on `window` on both halves, and it IS behind past both floors, 1.76 points against a 1.23% floor on the basis and 11.62 against 0.48% on the control. **So the tie-break is worth two points on `window` under 9.12.4 and twelve under HEAD**, where Run 30 measured it worth thirty-seven and Run 31 five.

(14) *What the composed route buys a fold.* **HELD in every one of its readings.** `libunord-stage10-sum` over `libunord-stage1-sum` reads **0.3032** and **0.3117** on `runs` against a registered 0.37, **0.4330** and **0.4368** on `block` against 0.46, **0.3563** and **0.3764** on `window` against 0.42, **0.5465** and **0.5535** on `bcast` against 0.59 and **0.5228** and **0.5334** on `rev` against 0.53, so the composed route is ahead of master's consumer past the floor on all five populations on both halves. And where no pair of axes is tied on stride, stage ten IS stage nine: 1.0031 and 0.9991 on `bcast`, 0.9963 and 0.9985 on `bcastmid` and 0.9997 and 0.9998 on `compose`, every one inside the registered 0.5%.

(15) *What the shared loop's SPEC-free sum leaves the list interface.* **HELD, and it answers what the loop change was made for.** `libunord-stage10-list-sum` over `libunord-stage10-sum` reads **1.0013** and **1.0002** on the main set inside 2%; **1.0201** and **1.0108** on `runs` against a registered 1.04 within 4%; **1.0272** and **1.0215** on `window` against 1.03; **1.0186** and **1.0118** on `block` against 1.02; 1.0091 and 1.0082 on `flip`, 1.0102 and 1.0067 on `bcast`, 1.0125 and 1.0077 on `bcastmid` and 1.0030 and 1.0026 on `compose`, all inside 3%; and 0.9989 and 1.0000 on `rev`, 0.9995 and 0.9964 on `scaled`, inside 1%. The first kill wants the pair inside 1% of level on `runs` AND `window` on both halves and no half of either is; the second wants it past its band there and none is. **One class reading misses**: `small` reads 0.9991 on the basis, inside the registered 1.04 within 5%, and **0.9866** on the control, 5.34 points off --- the population where the item's own text said the level's per-call constants were part of what `-O2` had read and the loop alone should buy less, and under a compiler it buys nothing there at all.

(16) *The same fold on the lean route.* **HELD, and the arm it was landed for says what it was landed to say.** `liblist-stage4-list-sum` over `liblist-stage4-sum` reads **1.0008** and **1.0002** on the main set inside 2%; **1.0224** and **1.0105** on `runs` against a registered 1.04 within 4%; 1.0041 and 1.0033 on `window` against 1.03; **1.0190** and **1.0125** on `block` against 1.02; 1.0187 and 1.0032 on `small` against 1.04 within 5%; 1.0015 and 1.0042 on `flip`, 1.0002 and 1.0009 on `bcast`, 1.0151 and 1.0016 on `bcastmid` and 1.0015 and 1.0015 on `compose`, inside 3%; and 0.9987 and 0.9994 on `rev`, 0.9962 and 1.0009 on `scaled`, inside 1%. Neither kill fires. **The second is the one worth reading**: it wanted this pair and (15)'s to agree on `runs` and `block`, where the counts say the two routes walk one loop, and they part by **0.23 of a point** at worst on `runs` and 0.07 on `block`, inside both populations' floors on both halves. So the two `-list-sum` arms price the `SPEC` argument alike on the lean route and on the composed one, which is what having both of them in every run from here is for.
