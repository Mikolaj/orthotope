# Run 28 (SpecConstr)

One run's write-up: its head, its Results, what the next run compares against, the claims that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 28 (SpecConstr), and what a roster is worth when the pair under it does not move: thirteen timed arms land and nine leave, the lazy walk is rebuilt around one flat loop, and of the sixteen registrations this run carries ELEVEN HELD, FOUR WERE KILLED and one was withdrawn with its arms before the run.** The pair is Run 27's unchanged --- ghc-9.12.4 against the same in-tree HEAD stage1 `10.1.20260803`, one source, one shim at `b3a1aca`, both halves the dead-spot form --- because Run 27 closed the compiler question and the request put the roster in its place. **The four kills are one finding said four times**: not one kill fell on an item predicting a REAL effect, and all four fell on items predicting a NO-OP --- two routes argued from the code to be the same past a dispatch, tested on the populations the argument said the reordering could not reach, and moving there. The converse does not hold and the file does not claim it: registrations (2), (4), (10) and (15) predict no-ops too and all four HELD, (15) being an A/A pair against its own copy rather than two routes argued to be one, so what the four kills say is where such an argument is unreliable and not that it always fails. Registration (3) fails on allocation, (6) on `window` and `small-patch-r5`, (7) on `rev`, and (11) on `main`, `rev` and `small` --- each on both halves and each by a margin the population's own floor cannot absorb.

**The roster is 39 timed arms over 19 main-set shapes and 741 benches, with 58 class views over ten classes for 2262 more --- and SIX class views land, the most since Run 25's eight.** Thirteen arms in and nine out is four net, and the thirteen are all consumers or controls: the four `-sum` consumers of `libunord-stage6` through `-stage9`, the fold entry point `libunord-stage6-loop-sum`, base's own fold `libunord-stage6-list-sum`, the ceiling's consumer `libunord-stage3-sum`, the ordered list's four `liblist-*-sum`, and the shipped fill's two A/A copies. The four stages themselves are checked and never timed. What left is every arm that concatenates a list the library would fold --- `liblist-stage1` to `-stage4` and `libunord-stage1` to `-stage9` --- retired to `check` by the ruling of 2026-09-09, a list concatenated being the API's failure mode. The six new views are `bcast-src8`, `-src64` and `-src512`, a repeat ladder; `window-64x64-c16-k3` and `window-32x32-c64-k3`, patch views with channels; and `flip-fwd-rows96`, a forward control. The main-set shapes did not move, so every cross-run figure here is over all nineteen.

**NOT ONE CELL SINKS BELOW THE SHARED FORCING PASS ON EITHER HALF, where Run 27's write-up recorded seventy on the basis and forty-three on the control.** Those two were counted before the 2026-09-10 `no_net` ruling and do not reproduce: today's reader gives 25 and 8 on the same JSONs. Every row that carries a corrected time at all is a geomean over all 19 shapes; none is read over fewer, as five of Run 27's basis rows still are, over 13 to 15 of 19. Two changes did it together: the ruling of 2026-09-10 that a reducing consumer has no corrected time at all --- it returns a scalar and never runs the pass being subtracted, so `no_net` is the predicate and the fifteen `-sum` rows read `--` in `time` and `worst` --- and the retirement of every Fill arm over a list, which took the arms whose corrected net was a small difference of two large numbers. **What that costs a reader is comparability, and it costs it in one place**: the `best outside family` column can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows --- `libunord-stage5-sum` on four, `-stage2-sum` on two, `-stage4-sum` on one --- this one names `lib-stage2-lean` and `lib-stage1` and nothing else, and a cross-run reading of that column is not like for like.

**The gate read SOUND and the machine check did NOT fire, so absolutes cross between Run 27 and this run with no box term --- which is the opposite of what Run 27 had to say for itself.** The gate took five arms over the shape set from each half twice in a palindrome, ghead-a, g912-a, g912-b, ghead-b, and the two `--compare` readings agree in MAGNITUDE on every arm that carries a corrected time, which is the criterion the pair note records and not agreement in sign: `mut-odo-vecdims` reads 0.9959 in the -a pair and 1.0012 in the -b, a gate this tight not being askable to agree in sign. The machine check, which is the separate answer, reads `list`'s net at **+0.37%** against Run 27's own fingerprint over **19 of 19 shapes**, worst `stretch-r5-8x432` at +3.31% and none past 5% --- inside the 3% that would make it a box move, where Run 27's read -3.66% against Run 26's and opened a third machine era. So this run publishes inside that era rather than opening a fourth, and nothing below is bridged.

**Every one of the twenty-two processes gated clean --- `--selftest` and both `--aa` gates --- and the plateau puts all twenty-two inside 19.0625 to 19.7032 ms/iter, a 3.36% spread against the 5% band.** That `--selftest` passes on all of them is this run's own result and not a formality: the roster pass of 2026-09-09 had the required mode REFUSE two legs, five rows having gone entirely non-positive under the correction, and the `no_net` ruling was taken before the gate rather than during it precisely so the refusal would not land on `bcast` and `flip` of both halves eight hours in. **This run's floor is 0.50% on the basis half and 0.65% on the control**, and the basis figure is set by `mut-odo-vecdims-add-in-leaf-u2-aa-distant` --- the A/A pair registration (15) landed, so the floor is now measured on the arm the library actually runs rather than on `mut-odo-vecdims`, `list` or `bq-expand`. Four processes carry an A/A cell past 5%: `g912-runs` 6.17%, `ghead-main` 5.38%, `ghead-runs` 5.17% and `g912-bcast` 5.04%.

**The sequence ran in ONE window and no intrusion was found, where Run 27 needed two windows and a rerun.** Twenty-two processes from **01:02:39 to 09:42:43**, every one exiting 0 at its roster's count and the largest hole between one finishing and the next starting being zero; the gate's four ran before them from 00:29:20 and the four rider stages after them to 09:55:12. The per-sample instrument reads the foreign CPU during every bench, as a ratio of one core: the highest reading anywhere is **0.21** on `ghead-main`, below the 0.25 at which the reader calls an intrusion, so post-run step 3's rerun is not owed and every population here is one window's.

**The manifest's two live claims both held, and claim 7 lost a level to the roster rather than to a measurement.** `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims` reads **0.6381 at 17 wins of 19, sign p 0.00073** on the basis and **0.6418 at 17 of 19** on HEAD, against Run 27's 0.6380 and 0.6392, Run 26's 0.6438 and 0.6467 and Run 24's, pinned to these nineteen shapes, 0.6464 and 0.6454 --- eight readings over four runs and two compilers now spanning nine thousandths. Claim 7's levels reproduce to the digit over the eighteen shapes it is pinned to: the mutable fills **1.00x**, `bq-expand` **2.06x**, `list` **22.38x** and the `libunord` consumers **0.00x**, identical on both halves for a second run. **What it lost is the 2.00x list-consumer tier**, which has no timed member here: the ruling of 2026-09-09 took the ports off concatenating a one-element list, so they read 1.00x, and the same day retired every Fill arm over a list. The level did not move; its population left the roster, and the claim wants re-aiming rather than retiring.

**What the second compiler is worth on this roster is almost nothing, and this time the time column says so on its own.** Basis over HEAD, over the 20 arms carrying a corrected time, **19 sit within 1% of 1** and the geomean is 1.0028; the single arm outside is `mut-odo-vecdims-add-in-leaf-u2-ptr` at **1.0141**. Run 27 had to say this with the counts column in hand, because nine of its rows read tens of percent apart in time for a reason that was the correction and not the codegen. This run has no such rows, so the time column and the counted work answer the same question and agree.

**The counted work covers every population, no cell was refused anywhere, and its finding is that the compiler term is instructions and nothing else.** Twenty-two sweeps, both halves over all eleven populations, 22 files and not one `!!` line in any of them. On the main set, basis over HEAD, the counted geomean is **1.0027** over the 20 arms that carry a corrected time and 1.0021 over all 39 the sweep covers with the controls --- and the TIME geomean over those same twenty is **1.0028**. The two agree to a ten-thousandth. The counted band runs 0.9936 to 1.0089, every arm inside 0.9% of 1, and `time/counts` between 0.9871 on `-u1` and 1.0123 on `list-aa-distant`. **The one arm whose time is more than a point off 1 is not a counted outlier**: `mut-odo-vecdims-add-in-leaf-u2-ptr` reads 1.0141 in time against 1.0054 in counts, a time-over-counts of 1.0086, so whatever moved on it is not the instructions it executes.

**This run's two columns MAY be differenced, as Run 27's could.** `list` moves **0.46%** between the halves on the main set against the 0.7% bar. Four of the ten class populations are past it and are read as an ordering rather than a subtraction --- `bcastmid` 1.0185, `bcast` 1.0138, `small` 1.0136 and `block` 0.9921 --- and the other six are inside, with the main set, `runs` widest of the seven at 1.0068.

**The fastest timed arm on the main set is no longer in the `mut-odo-vecdims` family.** `lib-stage2-lean` reads **0.027** where the shipped `mut-odo-vecdims-add-in-leaf-u2` reads 0.029 and the family root 0.051, and it leads outside the family in seven of the ten classes on this half and eight on HEAD besides. Its margin over `lib-stage1` --- the route the library ships, and NOT the `mut-odo-vecdims-add-in-leaf-u2` called shipped a clause earlier, over which it reads 0.9991 on `runs` --- is what registration (14) measures and sorted over the ten classes it is widest on `runs`, **0.2316 and 0.2357**, then `block` at 0.4200 and 0.4257, `small` at 0.6046 and 0.6016 and `bcastmid` at **0.6163 and 0.6420**. `bcastmid` is the one the fills' own change was for: Run 27 read that pair BEHIND at 2.33 and 2.28 on `bcastmid-b200k`, the view registration (14) scopes that reading to, though ahead of the leaf on the class. What changed is two edits to `fillStage2` of 2026-09-09 --- a doubling block copy for a zero-stride level, and the broadcast run unrolled by two --- and the second is visible on its own: `bcast-tall-Mx2`, a run of 2, reads the un-unrolled twin at **1.2061 and 1.2058**.

**The tie-break is the roster's clearest win and the longest chain is its clearest loss, and they are one change apart.** Stage seven puts the longer extent innermost on equal absolute strides, and on `window` its consumer reads **0.7425 and 0.7237** of stage six's against a registered 0.7 within 15% --- the runs going from the kernel's width to the output's. Stage eight chooses the run as the longest contiguous one instead, and was argued to move nothing stage seven does not: it reads **1.0518 and 1.0474** on `window`, past floors of 0.37% and 0.40%, and on `small-patch-r5`, the one view it was registered AHEAD on, it is behind at **1.2593 and 1.2152**. Stage nine moves every zero-stride axis outermost and does what its probe said wherever one exists --- `compose-zero-mid` **0.4600 and 0.4601** against a probe's 0.46, `bcast-src8` 0.4732 and 0.4993 against 0.48 --- and is killed on `rev`, which has no zero stride and should have been stage six exactly.

**The fused list survives every consumer this run points at it, which is what the overhaul of 2026-09-09 was taken for.** The hand-written loop with no list between producer and consumer is BEHIND it on `window`, **1.7249 and 1.7422**, and base's own `sum` --- the fold a library user brings rather than the one the library writes --- is ahead of the harness's fold at **0.7176 and 0.7314** on `window` and **0.8349 and 0.8415** on `runs`. Both are the flat walker's doing: every continuation in `lazyRunsFB` is `go`, `carry` or `nil` and all of them known, so the fold fuses whoever writes it. The ordered list's consumers say the same in logical order as Run 27 read in address order --- **0.3631 and 0.3750** against master's slice recursion on `runs` --- while the two dispatches under them part on `small`, `main` and `rev`, which is registration (11)'s kill.

**The three class properties hold in all ten classes on both halves.** `mut-odo-vecdims`'s `worst` stays under 1 and it is ahead of `bq-expand` on every shape of every class, closest at `stretch-pow2stride` on the main set and at `window-64x64-k1x9` in a class; its allocation is at most 1% over `list` and over `bq-expand` on every shape, closest at `small-row96`; and the allocation tiers survive with their order unbroken, on the main set and on every class but `small`, which has sat outside the level clause since 2026-09-07 because its calls are short enough for the per-call constants to show. Across the halves 528 of the MAIN SET's 703 arm-cells that allocate agree to 1e-4, that being 741 cells less the two `sum-only` arms' 38.

**Across the halves the classes do not agree, and the vote is nearer even than the class count suggests.** Three classes put the basis ahead --- `scaled` at 19 of 20 arms, `block` at 17 and `flip` at 13 --- and seven put the HEAD half ahead, `bcastmid` most one-sidedly at 19 of 20. Over the ten classes together **86 of the 200 arm-comparisons put the basis faster and 114 the control**, and NONE is degenerate, where Run 27 had nine it could not vote because the correction had not left the basis cell positive.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The two `sum-only` halves --- the same term timed twice under two names --- agree at **0.9998** on the basis and **0.9998** on the HEAD half, so the quantity subtracted from every cell is the same quantity in both columns. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads 1.0345 on `mut-odo-vecdims` and 1.0832 on `bq-expand` on the basis, and 1.0265 and 1.0705 on the control.

**The two halves resolve alike.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **0.99** across the roster with **20 arms wider on the basis and 19 narrower**. A cell resolving worse is not a cell measuring something different: this column is sampling error inside one bench where the A/A floor is agreement between two placements of one strategy, and Run 18 moved the two in opposite directions.

**The straddling loops stand at EIGHT on the basis and SEVEN on the control, and this run ends Run 27's streak of naming every one of them on the basis half.** Each half's self-loops of at most 64 bytes in `Main`-compiled code number **220 on the basis and 209 on HEAD**, against Run 27's 184 and 181 --- the thirteen arms and the fusion overhaul in the layout. Post-run step 0's twin names **five of the basis's eight** by byte identity, each straddling in the twin at the same offset: `fillStage2Short`, `fbMutOdoVecdimsAddInLeafU2Down`, `fbMutOdoVecdimsAddInLeafU2Last`, `fbMutOdoVecdimsAddInLeafU2` and `fbMutOdoVecdimsAddInLeafU2Ptr`. Three are refused for want of a byte-identical copy, and they are recorded as refusals: `loop-offsets.py` has already applied its own flow test and its `(bad)` filter to everything it counts, so a refusal here is not the info-table artefact those two catch and this run took no evidence of what it is. **The HEAD twin names one of seven**, `fillStage2Short`, and its copy straddles at a different offset --- `-g3` being a different program on that compiler, which Run 27 read the same way at two of six.

**The regime was confirmed in this run's own binary before the hours were spent**, which nothing afterwards can, and it reads what Run 27's read because the recipe did not move: a `diag` on the basis half puts `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512`, a ratio of **1.000** where plain -O1 is ten times apart. That is `-fspec-constr` in the binary rather than on a command line, which is the only place the question can be settled, and the figures standing unchanged from Run 27 is the check passing rather than a paragraph carried.

**Run 28 records every population twice** --- the main set and all ten stride classes from each half, one process each --- **in one window**, as Runs 23 to 26 did and Run 27 could not. Every one of the twenty-two exited 0 at the bench count its roster holds, and the roster's CLASS side is larger than any run's before it while its main set is not: **2262 class cells against Run 27's 1820**, over 58 views where Run 27 had 52 and Run 24 had 41, against **741 benches on the main set** where Run 27 had 665 but Run 24 had 1352, from 52 arms over a main set of its own that the prune of 2026-09-04 has since cut.

**The decomposition reproduces on both halves for a tenth run.** The riders time each shape's `list` by itself, one bench per process on that half's own binary, `SAT=` off and on: the saturated legs split the deflation into the state the preamble puts on a clean process --- **+11.51%** on the basis and **+12.07%** on HEAD --- and everything else the roster does to a cell, which is **+0.55%** on both. `--deflation`'s own totals are **+12.13%** and **+12.69%**, over all 19 shapes -- read off the mode rather than multiplied out of the two halves above, whose rounded product gives +12.12% on the basis.

**There is NO repetition this run and none was owed.** `Main.hs` moved from Run 27's `bb6d113` to `bdf06c8` by the commits of 2026-09-09, so neither half can reproduce an earlier binary and an unequal md5 would name nothing; the pair note says so at its `repetition` row rather than recording a comparison it cannot make. What a repetition would have given came instead from the gate's machine check, and this time it came back small: +0.37% on `list`'s net over all nineteen shapes, inside 3%, so the box is where Run 27 left it. `Main.hs` has moved AGAIN since those binaries were built, and by COMMENTS ALONE --- no commit since `bdf06c8` changes a line of its code, which is the property to check rather than a list of hashes to keep current --- so the pair was not rebuilt: post-run step 8 asks that such a move be said here rather than rebuilt away, and this is the saying. **A comment-only move is not nothing to the binary, though it is nothing to a placement.** Measured 2026-09-11, both halves rebuilt twice: from `bdf06c8` each comes back md5-identical to the binary that was timed, from a scratch directory two days on, so the recipe and not the path fixes them; from the TIP the HEAD half's `.text` is still byte-identical and the basis half's is not, 145622 bytes of it differing. No loop moved even there --- every short loop of that half comes back at the same address, all but fourteen of them byte-identical and those fourteen differing only in a rip-relative displacement --- so what the comments cost is the md5, and a rebuild meant for comparison is taken at the commit.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. What this run leaves the next one is a roster whose no-op arguments have been tested four times and refuted four times, which is the reason to read registrations (3), (6), (7) and (11) before proposing a fifth reordering.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a `-fspec-constr` table**: it is not the regime `Data/Array/Internal.hs` compiles under. **A row's distance from Run 27's column carries a roster term and an evening and NO box change**, `Main.hs` having moved from `bb6d113` to `bdf06c8` by the commits of 2026-09-09 --- thirteen timed arms landed and nine left, four net, the nineteen shapes unmoved and six class views added --- while the gate's machine check read `list`'s net +0.37% against the fingerprint Run 27 installed, inside 3%. **So the raw distance IS readable this run**, which Run 27's was not: over the 18 arms both rosters time and both give a corrected time the basis column runs 0.9867 to 1.0125, every one inside 1.4% of 1, and `--bridge` against Run 27, each arm as a ratio to `list` in its own run, reads 1.0066 over seventeen of those eighteen with none outside the 3.3% drift band, `list` being the denominator it divides by rather than an arm it reads. **The `alloc` column is pinned as always**: over the eighteen shapes claim 7 keeps, this run reads the fills 1.00x, `bq-expand` 2.06x, `list` 22.38x and the `libunord` consumers 0.00x, the same figures for a sixth run and the same on both halves --- while the 2.00x list-consumer tier has no timed member here at all, its arms having been retired to `check` on 2026-09-09.

**And it is the basis half's**, `run28-g912`, as every published table here is from Run 11 on: the HEAD half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-nine rows. That the published half is the 9.12 one is this pair's own decision --- it keeps the lineage, being the compiler every figure in README was measured through, and the dead-spot form it carries is the basis recipe decided on 2026-09-02 --- and the HEAD half is the reading a form about to be published is owed on a second compiler. **Thirteen rows here are first readings at full budget**, all of them landing on 2026-09-09: `libunord-stage6-sum` through `-stage9-sum`, `libunord-stage6-loop-sum`, `libunord-stage6-list-sum`, `libunord-stage3-sum`, the four `liblist-*-sum` consumers, and the shipped fill's two A/A copies. **Nine rows Run 27 published are gone**: `liblist-stage1` to `-stage4` and `libunord-stage1` to `-stage5`, every one of them an arm that concatenates a list the library would fold, retired to `check` by the ruling of the same day.

**Comparing runs?** The table below is Run 28's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the claims to test are [the ones after it](#the-claims-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.50%** from 1 on the basis half or **0.65%** on the control, the HEAD half, read over the eight pairs this roster carries and over the four pairs that carry back to Run 10 at **0.39%** and **0.65%**, beside which the worst SINGLE A/A cells of the two MAIN-SET processes, 3.91% on `stretch-wide-2xM` and 5.38% on `vgg-14-c512-k3`, are not floors at all and are not to be quoted as any --- and are not this run's worst, `g912-runs` carrying 6.17% --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **And one thing this run has that its predecessor did not**: its gate's machine check did NOT fire, so an absolute here is comparable with Run 27's directly and needs no bridge.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What cost a row its population on Run 27 was the correction, not the estimator, and on THIS run nothing does**: that run had nine rows on each half carrying cells the shared forcing pass is not smaller than --- seventy such cells on its basis and forty-three on its HEAD half, all of them `libunord` arms --- and this roster has none, so every row here that carries a corrected time is a geomean over all nineteen shapes. What replaced those rows is the `no_net` ruling and the retirement of the Fill arms over a list, named in the head and in *What the next run compares against*, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what claim 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, which claim 7 now carries on a live basis, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.59* | *60* | *2.11x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.60 | 69 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.60 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-sum | -- | -- | 0.60 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage2-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over the branch's port |
| libunord-stage3-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over the ceiling's fill -- stage five with its runs turned into fills |
| libunord-stage4-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage four's list |
| libunord-stage5-sum | -- | -- | 0.02 | 83 | 0.00x | new mutating `Vector` method -- `sumT` over stage five's list, one slice at a time and no concatenation |
| libunord-stage6-list-sum | -- | -- | 0.01 | 83 | 0.00x | the same, base's `sum` over stage six's list, the fold a library user brings |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage8-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage eight's list -- the run chosen as the longest contiguous one |
| libunord-stage9-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.36* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.02* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.02* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.027 | 0.128 | 0.59 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage2-lean-u1 | 0.028 | 0.125 | 0.60 | 69 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| lib-stage1 | 0.028 | 0.128 | 0.58 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.028 | 0.128 | 0.62 | 69 | 1.00x | new mutating `Vector` method |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.029* | *0.128* | *0.56* | *69* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.029* | *0.128* | *0.51* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.029 | 0.128 | 0.47 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.030 | 0.128 | 0.62 | 70 | 1.00x | the same, unrolled -- the shipped fill's ceiling under an allocator that spills nothing |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.031 | 0.127 | 0.64 | 69 | 1.00x | nothing the library would take -- the merged-bound leaf walking a `Ptr`, a ceiling and not a candidate |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.125 | 0.59 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.031 | 0.128 | 0.50 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill with its look-ahead hoisted out of the loop's guard |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.127* | *0.39* | *66* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.051** | 0.127 | 0.32 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| *mut-odo-vecdims-aa* | *0.051* | *0.127* | *0.47* | *66* | *1.00x* | *A/A control* |
| bq-expand | 0.113 | 0.206 | 0.66 | 53 | 2.11x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.113* | *0.206* | *0.63* | *53* | *2.11x* | *A/A control* |
| *bq-expand-aa-distant* | *0.113* | *0.208* | *0.37* | *53* | *2.11x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.88 | 22 | 22.64x | -- |
| *list-aa-adjacent* | *1.002* | *1.013* | *0.60* | *22* | *22.64x* | *A/A control* |
| *list-aa-distant* | *1.003* | *1.016* | *0.86* | *22* | *22.64x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and pairs still part in SIGN between the two on this run. `mut-odo-vecdims-add-in-leaf-u2-ptr` against its parent is one: the rows divide to 1.034 where the paired figure is **0.9532**. `mut-odo-vecdims-add-in-leaf-u2-aa` against its base is another, the paired figure 1.0003 and the published column 0.9967, which `--predictions` names as parting when it adjudicates registration (15). A margin between two arms is `--pair`'s paired figure, which is also the statistic the floor is defined in; `--pair` and `--predictions` print both side by side and say when they part. The column is for reading the table, not for differencing it.

`concat-runs` has no row, and neither do the other 73 arms the roster holds and checks without timing --- **74 of its 113** in all, where Run 27 checked 61 of 96: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). The thirteen retired to `check` this run are the Fill arms over a list, `liblist-stage1` to `-stage4` and `libunord-stage1` to `-stage9` --- of which NINE had a row in Run 27's table, `libunord-stage6` to `-stage9` never having been timed at all --- and the ruling behind them is that a list concatenated is the API's failure mode, so the entry points are timed only as they are used. So a movement against Run 27's table is a movement on the **18 arms both runs time and both give a corrected time**, one evening apart, with a roster of thirteen in and nine out between them and no box change --- which is why the cross-run reading here is a subtraction and not `--bridge`'s.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is nine real fills where Run 27's was twenty-one rows of which nine were an artefact**: `mut-odo-vecdims` reads 0.051 with nine timed arms below it --- `lib-stage2-lean` at 0.027, `lib-stage2-lean-u1`, `lib-stage1` and `-u2-down` at 0.028, `-u2` at 0.029, `-u2-ptr` at 0.030, and `-u1-ptr`, `-u1` and `-u2-last` at 0.031 --- and every one of them writes the result vector the family's arms write. Run 27's twenty-one included nine `libunord` rows whose cells the arm emptied, which sat at the head of its table for the correction and not for a speed; those rows read `--` here and cannot mislead. **The fastest is no longer in the family**: `lib-stage2-lean` leads the table and leads outside the family in seven of the ten classes on this half, eight on HEAD. **The ceiling that ran for eight runs still cannot be read at all**: `bq-scan-rem-gm-mulback`, the denominator, is parked, and what stands where it stood is claim 10 inside the family, 0.6381 and 0.6418.

**The pointer fills are not what this run was built to read, and they are here as the ceiling Run 27 made readable.** `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr` are the shipped fill's leaf and its unrolled form rewritten to walk a `Ptr`, which takes out the `0x40(%rsp)` reload the twentieth ceiling reading found; they are refused for the library ([dead ideas][dead]) and timed as a ceiling and nothing else. On the basis half the three spans --- `-u1-ptr` over `-u1`, `-u2-ptr` over `-u2` and `-u2-ptr` over `-u1-ptr` --- read **0.9769** at 15 of 19, **0.9532** at 14 of 19 and **0.9403** at 16 of 19, and on HEAD **0.9672**, **0.9407** and **0.9322** --- every one holding its direction and its size across the compilers for a second run, where Run 26 read 1.3084, 2.6731 and 1.9710 before the GHC #27778 workaround. Both fills read 1.00x on both halves. **What the pair prices is the RATE, and this run reads it lower at every rung than Run 27 did.** The counted work puts the three spans at **0.8945, 0.8359 and 0.8605** of their parents in corrected instructions, reproducing Run 27's 0.8944, 0.8358 and 0.8605 to three figures --- so the instructions are identical and the times are not: **21.9%, 28.5% and 42.8%** of each saving reaches the clock where Run 27 read 27%, 35% and 46%. The band [the open list][open] recorded was a third to a half; the lowest rung now sits below a third, so that entry's band was opened to a fifth by this run, which is a movement in the rate and not in the arms. Read from outside the family beside them: `lib-stage2-lean-u1` converts at **40.9%**, where Run 27 read 59%, and `mut-odo-vecdims-add-in-leaf-u2-last` converts a real 3.1% instruction saving into a **1.4% LOSS** for a second run.

**The two standing placement controls are still gone with the prune, and this run ends Run 27's streak of naming every straddler on the basis half.** `mut-odo-vecdims-add-in` against the arm it varies, and `build` against `mut-odo`, were the two pairs this file read every run for what a slot costs an identical body; both arms of each went to `Only` on 2026-09-04, so nothing on this roster turns an offset into a time. What is left that speaks to placement is the A/A pairs --- **eight of them now**, the shipped fill's two having landed with registration (15) --- and post-run step 0's two twins. **The basis's tracked fills did not move at all**, which thirteen arms landing makes worth saying: its six tracked fill heads sit at offsets **0, 0, 0, 24, 0 and 4** and HEAD's at **18, 0, 31, 7, 9 and 26**, where Run 27 read 18, 7, 31, 7, 9 and 26 --- the HEAD half's second copy having moved from offset 7 to 0, the one tracked fill on either half that did move, with the basis's second group of two at 0 and 0, HEAD carrying the same two-copy group at 0 and 0 and a third group of three 7-instruction copies at 0, 0 and 21 beside it. **The straddlers are eight on the basis and seven on HEAD**, against six and six, and the twin names five of the basis's eight and one of HEAD's seven; the three and six it refuses hold no byte-identical copy and are recorded as refusals. **The 3% [the open list][open] has asked about since Run 10 is not readable on this roster either**, and the entry says so.


## What the next run compares against

**Run 29's pair is settled by the request of 2026-09-11: both halves are this run's basis, ghc-9.12.4 in the dead-spot form, one with `-fspec-constr` and one without --- the regime every run since Run 8 has published in against the one `Data/Array/Internal.hs` compiles under, on one compiler --- tagged `spec` and `nospec` by the chapter's rule that a tag names what a half is, the second recipe being the first with the flag left off its `--ghc-options`, which the pair note carries as it carries every recipe.** What this run leaves settled is the basis recipe, the dead-spot form decided on 2026-09-02, which has now published five runs; the regime is the question. **The compiler question stays closed**, as Run 27 closed it: this run put in front of it a roster the HEAD codegen had never compiled at full budget --- one flat walker, one compiled-once `sumLazyRuns`, the ordered consumers beside it --- and the two halves still agree at a counted geomean of **1.0027** with no arm 0.9% off 1. A sixth compiler pair would be asking a question two runs have answered. **What the roster leaves open is where the next run's work is, and this run names it rather than deciding it**: four of the sixteen registrations were killed and every one of them was an argument that a reordering could not reach a population, so (3), (6), (7) and (11) are the record of which such arguments failed and where. Registration (14) names a code change besides, the shipped leaf `fbMutOdoVecdimsAddInLeafU2` gaining the broadcast unroll `fillStage2` took on 2026-09-09, which would return `bcast-tall-Mx2` and with it the `bcast` class to a tie. Taken 2026-09-11 in the arm, after this write-up, so that leaf and its two A/A copies are not this run's code on any view whose innermost stride is 0; the same day the library took `fillStage2` whole and `lib-stage1` and `liblist-stage1-sum` with it, so those two are not this run's code on any view their route fills; Run 29 is the first to read all of them.

**The compiler variable was asked a fifth time, on a roster built since it was last answered, and it answers the same.** Run 24 varied the compiler over 52 timed arms and read the two 5.7% and 6.8% apart on the instructions of two pure arms; Run 25 read `list` 1.10% apart in time and 0.33% in counts; Run 26 found twenty-four of twenty-six arms inside a percent in counts and the two `Ptr` arms outside at 0.6219 and 0.9295, which was GHC #27778; Run 27 worked it around and read every arm inside a percent. **This run reads the counted geomean at 1.0027 and the TIME geomean at 1.0028 over the same twenty arms** --- the two statistics agreeing to a ten-thousandth, which is the plainest answer this form has given: what a second codegen is worth on this workload is instructions, and it has none to spare. What does not close is the class level: four of the ten classes carry a `list` move past the 0.7% bar --- `bcastmid` 1.0185, `bcast` 1.0138, `small` 1.0136 and `block` 0.9921 --- so on those four a class-level comparison of the halves is an ordering and not a subtraction.

**What Run 28 leaves the next run to read against, and the first item is a figure that did NOT move. The box is where Run 27 left it**, its gate machine check reading **+0.37%** on `list`'s net against Run 27's own fingerprint, over all 19 shapes, worst `stretch-r5-8x432` at +3.31% and none past 5% --- inside the 3% that would make it a box move, where Run 27's read -3.66% against Run 26's. So this run publishes INSIDE the third machine era rather than opening a fourth, an absolute crosses between it and Run 27 unadjusted, and the fingerprint below is the one a Run 29 machine check reads against. **The floor is 0.50% on the basis and 0.65% on the control**, read over the EIGHT A/A pairs this roster carries where Run 27 had six, and the restricted figure over the four pairs that carry back to Run 10 is **0.39%** and **0.65%**. The basis figure is `mut-odo-vecdims-add-in-leaf-u2-aa-distant`'s, the pair registration (15) landed, so a clause about the arm the library actually runs is now judged against a floor measured on that arm. A Run 29 margin is judged against the WHOLE-SET pair, 0.50% and 0.65%, which is what every verdict in this file uses and the conservative reading of two that this run separated for the first time since 2026-09-04; the restricted pair is the older series and what README's standing rule still names, and reconciling the two is [the open list][open]'s. **The two columns below MAY be differenced**, `list` having moved 0.46% on this run's main set against the 0.7% bar, as Run 27's could and as no run between Runs 24 and 27 could.

**Registered with the pair.** Run 28's sixteen registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. Eleven held, four were killed and one was withdrawn with its arms before the run. **What a next registration should take from this one is the SHAPE of the four killings and not their subjects**: not one fell on an item predicting an EFFECT, and all four fell on items predicting a NO-OP --- two routes argued from the code to be identical past a dispatch, tested on the populations the argument said could not move, and moving. On this roster an argument that a reordering cannot reach a population was the cheapest kind of clause to write and the least reliable, and (3), (6), (7) and (11) are the four instances. **Nothing here was killed by the box**, which is what Run 27's (3) was and what this run's machine check makes impossible to repeat.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at this run's own area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 28 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 27 to Run 28 --- which, the box not having moved, is a SUBTRACTION and not the bridge Run 27 could only offer. Over the **18 arms both rosters time and both give a corrected time** the basis column runs from **0.9867** on `lib-stage2-lean-u1` to **1.0125** on `-u2-ptr`, every one inside 1.4% of 1 and `list` itself at 0.9946; the reader names twenty-two more as being in one run only --- thirteen of them this run's and nine Run 27's. **The table below is this run's own two halves and no earlier run's**, eleven strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in the compiler and in nothing else:

| strategy | Run 28 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) | Run 28 (SpecConstr, dead-spot +lookrts, -A32m, GHC HEAD) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.051** | 0.051 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.031** | 0.030 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.029** | 0.029 |
| `mut-odo-vecdims-add-in-leaf-u2-down` | **0.028** | 0.029 |
| `mut-odo-vecdims-add-in-leaf-u2-last` | **0.031** | 0.031 |
| `mut-odo-vecdims-add-in-leaf-u1-ptr` | **0.031** | 0.030 |
| `mut-odo-vecdims-add-in-leaf-u2-ptr` | **0.030** | 0.029 |
| `lib-stage1` | **0.028** | 0.029 |
| `lib-stage2-lean` | **0.027** | 0.030 |
| `lib-stage2-lean-u1` | **0.028** | 0.028 |
| `bq-expand` | **0.113** | 0.113 |

**A published geomean is over the same 19 shapes, and two halves of one SpecConstr run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered, which is what an -O1 reading cannot do at an 8% baseline shift. **THE TABLE ABOVE IS SUCH A PAIR**, `list` having moved 0.46% on this run's main set: the two columns may be differenced. Three of the eleven rows are higher on the basis, three are higher on HEAD and five print the same at three decimals --- and the cells are ratios to `list`, so higher on the basis means the basis SLOWER there. **So the reading of this table is that there is nothing in it**, and that is the run's result rather than a shortfall. The one row worth a second look is `lib-stage2-lean`, 0.027 against 0.030: it is the widest gap in the table and a paired reading inside four tenths of a point, **1.0038** basis over HEAD and so the basis marginally SLOWER --- the opposite sign to the column's 0.027 against 0.030, which is the parting over a per-shape range of 0.834 to 1.085, the widest spread any arm here has. The column and the pair part on it because that much per-shape variation is being summarised two ways, and the paired figure is the one a margin is judged on. Take any size from the counted work, which owes criterion nothing and puts every arm within 0.9% of 1. `--compare --bridge` reads **0.9981 over the nineteen arms it covers, with NONE outside its 3.3% band**, where Run 27's write-up read 1.0468 with nine outside. That pair was counted before the 2026-09-10 `no_net` ruling and does not reproduce: today's reader gives Run 27 1.0737 over 26 arms with SIX outside, five of them `libunord` rows whose denominator the correction had emptied, which this roster does not have. **A pair that varies the allocation area is the other exception, and its two halves may never be subtracted from each other.** `list` moved **9.20%** between Run 14's halves, **5.13%** between Run 15's and **16.51%** between Run 16's. **Run 16's is the largest of the three and was registered to be the smallest**, on the reasoning that its two halves both sit at enlarged areas where the earlier two each crossed the default --- a prediction refuted by its own run, and the refutation is the finding: what moves the baseline is not the distance from the default but the in-process deflation, which at roster scale is worse at 64 MB than at 32 MB by more than the whole default-to-32 MB step was worth. So the exception widens rather than narrowing, and it covers every pair that varies the area at all. Every cell of such a pair's second column is scaled by a denominator the pairing moved: read it for the pairing's direction, take no strategy quality off it, and read the arm-by-arm comparison at the head of this file instead, which divides absolutes rather than ratios.

**The HEAD half's own standings on the arms this run's roster brought, which no table here carries, every published table being the basis half's.** Read off the HEAD half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9585** at 14 of 19 (0.9636 at 13 of 19) and against `mut-odo-vecdims` **0.6418** (0.6381); `-u2` against `-u2-down` 1.0019 at 5 of 19 (0.9967 at 10 of 19), a tie on both halves and inside each floor; `lib-stage1` against `-u2` 1.0366 (1.0392); `lib-stage2-lean` against `-u2` 0.9899 at 13 of 19 (0.9929 at 12 of 19) and against `lib-stage1` **0.9550** (0.9554). **The three pointer spans hold their direction and their size on the second compiler for a second run**, which is what Run 27 established and this roster does not disturb: `-u1-ptr` against `-u1` reads **0.9672** at 15 of 19 (0.9769 at 15 of 19), `-u2-ptr` against `-u2` **0.9407** at 15 of 19 (0.9532 at 14 of 19) and `-u2-ptr` against `-u1-ptr` **0.9322** at 15 of 19 (0.9403 at 16 of 19) --- where Run 26, before the GHC #27778 workaround, read 1.3084, 2.6731 and 1.9710 on this half. **Where the two halves differ at all it is in strength and not direction**, and every ordering the basis half reads holds on the second compiler.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section](../README.md#per-shape-where-the-geomean-hides-the-ordering). The two tables below are installed from this run's own JSONs in the per-shape form the README describes, which replaced a fourteen-column one on 2026-09-04.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 5.8 us | 0.086 | `lib-stage2-lean-u1` 0.081 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.059 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.05 us | 0.097 | `lib-stage2-lean-u1` 0.072 | `mut-odo-vecdims-add-in-leaf-u1` 0.073 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 109 us | 0.070 | `lib-stage2-lean` 0.035 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.043 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 353 us | 0.048 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `gather48-src-50` | 3 | 22500 | 419 us | 0.052 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `stretch-coprime-r7` | 13 | 60060 | 986 us | 0.033 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.36 ms | 0.058 | `lib-stage1` 0.033 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `stretch-primes` | 89 | 250357 | 3.86 ms | 0.028 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 15.6 ms | 0.044 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `vgg-14-c512-k3` | 3 | 903168 | 18.2 ms | 0.057 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 17.9 ms | 0.034 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-inner256` | 256 | 1750784 | 31.7 ms | 0.032 | `lib-stage2-lean-u1` 0.028 | `mut-odo-vecdims-add-in-leaf-u1` 0.027 |
| `stretch-pow2stride` | 64 | 1769472 | 27.6 ms | 0.127 | `lib-stage2-lean-u1` 0.125 | `mut-odo-vecdims-add-in-leaf-u1` 0.125 |
| `stretch-r5-8x432` | 8 | 1769472 | 32.6 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-square-1341` | 1341 | 1798281 | 28.1 ms | 0.096 | `lib-stage1` 0.085 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.076 |
| `stretch-bigstride` | 3 | 1800000 | 47.4 ms | 0.035 | `lib-stage1` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 36.9 ms | 0.063 | `lib-stage1` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 37.9 ms | 0.022 | `lib-stage1` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `stretch-wide-2xM` | 2 | 1800000 | 36.4 ms | 0.062 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 846 us | 0.032 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 25.8 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 26.4 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 26.4 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 26.2 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 36 ms | 0.063 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.32 ms | 0.058 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.028 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 3.84 ms | 0.021 | `lib-stage2-lean` 0.014 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 45.1 ms | 0.036 | `lib-stage2-lean-u1` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.012 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 38.8 ms | 0.024 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.021 |
| `block-run64-gap1` | `block` | 64 | 131072 | 1.99 ms | 0.022 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.018 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.02 ms | 0.028 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.01 ms | 0.027 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `block-run64-page` | `block` | 64 | 131072 | 2.08 ms | 0.033 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `block-r3-vol64` | `block` | 64 | 262144 | 3.95 ms | 0.022 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.019 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 843 us | 0.032 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 840 us | 0.032 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 26.3 ms | 0.022 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 26.5 ms | 0.024 | `lib-stage2-lean-u1` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.06 ms | 0.029 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.06 ms | 0.029 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.31 ms | 0.061 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.028 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 26 ms | 0.027 | `lib-stage2-lean-u1` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 26.8 ms | 0.027 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 28.1 ms | 0.056 | `lib-stage1` 0.048 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.047 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 110 us | 0.069 | `lib-stage2-lean` 0.034 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.039 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 416 us | 0.053 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `rev-primes` | `rev` | 89 | 250357 | 3.89 ms | 0.028 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `runs-65536` | `runs` | 65536 | 1769472 | 25 ms | 0.027 | `lib-stage1` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `runs-16384` | `runs` | 16384 | 1785856 | 25.1 ms | 0.027 | `lib-stage1` 0.026 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-4096` | `runs` | 4096 | 1798144 | 25.3 ms | 0.027 | `lib-stage1` 0.027 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-1024` | `runs` | 1024 | 1799168 | 25.5 ms | 0.027 | `lib-stage2-lean` 0.029 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-512` | `runs` | 512 | 1799680 | 25.5 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-256` | `runs` | 256 | 1799936 | 25.9 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-7` | `runs` | 7 | 1799994 | 29 ms | 0.037 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-2` | `runs` | 2 | 1800000 | 36.4 ms | 0.063 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-3` | `runs` | 3 | 1800000 | 32.7 ms | 0.052 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 31 ms | 0.045 | `lib-stage2-lean-u1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `runs-5` | `runs` | 5 | 1800000 | 30.3 ms | 0.043 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-9` | `runs` | 9 | 1800000 | 28.7 ms | 0.033 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-96` | `runs` | 96 | 1800000 | 25.9 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 26.4 ms | 0.029 | `lib-stage1` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `scaled-r5` | `scaled` | 13 | 15015 | 239 us | 0.032 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 916 us | 0.026 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 4.58 ms | 0.031 | `lib-stage2-lean-u1` 0.034 | `mut-odo-vecdims` 0.031 |
| `small-patch-k5` | `small` | 5 | 150 | 2.65 us | 0.083 | `lib-stage2-lean` 0.104 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.061 |
| `small-bcast32` | `small` | 32 | 256 | 3.95 us | 0.056 | `lib-stage2-lean-u1` 0.069 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.053 |
| `small-flat64` | `small` | 64 | 256 | 3.95 us | 0.064 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.064 |
| `small-patch-r5` | `small` | 4 | 256 | 4.92 us | 0.094 | `lib-stage2-lean-u1` 0.100 | `mut-odo-vecdims-add-in-leaf-u1` 0.072 |
| `small-row96` | `small` | 96 | 384 | 5.75 us | 0.046 | `lib-stage2-lean` 0.060 | `mut-odo-vecdims-add-in-leaf-u2` 0.046 |
| `window-28x28-k5` | `window` | 5 | 14400 | 253 us | 0.044 | `lib-stage1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 853 us | 0.095 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.2 ms | 0.058 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 8.62 ms | 0.057 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.12 ms | 0.055 | `lib-stage1` 0.031 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 10.5 ms | 0.058 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 11.2 ms | 0.059 | `lib-stage1` 0.034 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.029 |
| `window-128x128-k7` | `window` | 7 | 729316 | 12.2 ms | 0.036 | `lib-stage1` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 |

**One row to read first, and it is a property of the shape set and not of any arm**: NONE of the thirty-nine rows is a geomean over fewer shapes than the rest, where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, against **seventy on Run 27's basis and forty-three on its control** as that run's write-up recorded them -- counts predating the 2026-09-10 `no_net` ruling, which today's reader gives as 25 and 8 -- so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the fifteen `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows --- `libunord-stage5-sum` on four, `-stage2-sum` on two, `-stage4-sum` on one --- this one names `lib-stage2-lean` and `lib-stage1` and nothing else, and the cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The claims the next run should test

**Run 28's verdicts first**, since a run reports breaks rather than re-deriving the table. **Both live claims held, and one of them lost a level to the roster rather than to a measurement.** Claim 10, the manifest's only live ordering, reads **0.6381 on the 9.12 basis and 0.6418 on the HEAD half**, both at 17 wins of 19 with sign p 0.00073; Run 27's two read 0.6380 and 0.6392, Run 26's 0.6438 and 0.6467, and Run 24's, pinned to these nineteen, 0.6464 and 0.6454 --- eight figures across four runs and two codegens spanning nine thousandths. Claim 7 held on every level that has a timed member, its pinned eighteen shapes giving back the same figures for a sixth run and on both halves alike. **What it lost is the 2.00x list-consumer level**, whose arms were retired to `check` on 2026-09-09 and whose ports the same day took to 1.00x, so the level has nothing to be read on. It did not move; its population left. Every arm claim 10 names is timed, and `--pair` recovers any retired ordering in one call whenever it is wanted.

**The eight retired claims are not re-read here.** Claims 3, 4, 5 and 9 left the manifest at Run 19's write-up, on a sweep in which all thirteen held on both of that run's halves; claim 8 left on 2026-08-29 on a decision, its subject the pure tier being no longer what ships; claims 2 and 6 left on 2026-08-28 with the parking of the arms their surviving links turned on; and claim 1 left on 2026-09-04 with the prune, which parked every rung below its top. The numbered items below say what each was in a clause. This run does not re-derive any of them and quotes none as its own --- and for claim 1 that is still the only way it could be read at all, three of the four arms its links name being untimed, which is what retiring it gave up and what a run that re-times them would get back. **Eight is the count, and it was written as seven for two runs**: the enumeration above skipped claim 8, which the numbered list below has carried as retired since 2026-08-29.

**Claim 7 held on every level that still has a timed member, and its pinned shape set did not have to move this run.** The claim reads its levels over the eighteen main-set shapes it was re-cut to on 2026-09-05; this run times nineteen, `cnn-L1-6x6-c1` being the nineteenth and sitting outside the pin. **Read on the eighteen, four of the claim's five levels are unmoved and six runs agree to the digit**: the mutable fills at **1.00x**, `bq-expand` at **2.06x** and `list` at **22.38x**, every one of them Run 27's, Run 26's, Run 25's, Run 24's and Run 23's figure over the same eighteen, and the `libunord` consumers at **0.00x** beside them. What this run's own nineteen show instead --- 2.11x and 22.64x --- is the median's population and not what any arm allocates, which is the sixth run running that this file has had to say. **The fifth level has no timed member and that is the claim's news.** The 2.00x tier was the list consumers, `liblist-stage1` and `-stage2`; the ruling of 2026-09-09 took every port off concatenating a one-element list, so they read 1.00x, and the same day retired every Fill arm over a list to `check`. **The thirteen new arms land where the claim would put them**: the seven new `libunord-*-sum` consumers at 0.00x, beside the four the roster already had, the four `liblist-*-sum` at 1.00x with the fills they fall to on the main set, and the shipped fill's two A/A copies at 1.00x with their base. So claim 7 is re-aimed at the levels this roster has, on 2026-09-11 and in the item at the foot, and Run 29 is the first to read it in that form.

**Claim 10 is the manifest's only live ordering and this is the fourth run to read it as a claim.** What the leaf fusion buys over the family root: `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims`, registered 2026-09-04 with the prune in claim 1's place, and read here at geomean **0.6381, 17 of 19, sign p 0.00073** on the basis with the HEAD half at **0.6418, 17 of 19** --- a margin no floor here approaches, so what the registration asserts is the direction and the win count and not the size. Run 27's halves read 0.6380 and 0.6392, Run 26's 0.6438 and 0.6467, and Run 24's, pinned to these nineteen shapes, 0.6464 and 0.6454: eight readings over four runs, four rosters and two compilers within nine thousandths --- across a box change of 3.66% between Runs 26 and 27 that moved every absolute and this ratio not at all, which is what a within-run ordering is for.

**Readings:** `mut-odo-vecdims-add-in-leaf-u2` / `mut-odo-vecdims` 0.6381, 17 of 19, sign p 0.00073. 1 of 1 registered ordering held.

Restated as the predicates the next run checks, and carrying no reading of its own: the figures each was last measured at are in the `Readings:` paragraphs above, so an entry here changes when a claim is re-aimed and not when a run moves a margin. **All of them are `-fspec-constr` claims, the regime they are read in** --- the shipped file does not set the flag, measured irrelevant to the shipped family --- so they are the set that decides, and a run at -O1 would test Run 7's instead, the two differing in more than their numbers. **They are read in the caller's allocation regime now**, every figure here being taken at the `-A32m` Run 16 promoted to the basis and every horde-ad test and benchmark bakes since 2026-08-21; the gap Runs 14, 15 and 16 priced against a prevailing `-A1G` is closed, and no claim below needs qualifying by it. **And all of them are read against a measured drift band rather than a layout span**, which is what the last three runs bought. A roster *order* change alone moved arms 0.966 to 1.142 between Run 9 and Run 10, and that is what a margin used to have to clear; with the layout pinned, a repetition moves an arm by at most 3.3% on Run 11's reading and 2.1% on Run 23's, most of them by under a point, so a margin above a few percent is now evidence of a strategy. **Run 13 is the first pair here to hold every tracked loop at one offset in both halves**, which is what lets its arm-by-arm comparison be read as the package costing nothing rather than as two terms cancelling. A claim resting on an arm whose own loop the shim skipped --- `list`'s, which is library code --- is still decidable nowhere until that loop is read. **And the pinning claim is measured only in its weak form**: adding `mut-flat-gm-nosum` left every tracked loop at the same address, but a `Force` arm reuses a rostered function and emits no code for emission order to move. The strong form wants an arm that emits its own, and until one is added the claim covers additions that cost nothing to place.

**The list needed reading against a moved roster again this run, and this time the roster took a level off a claim.** Thirteen arms landed and nine left, and claim 10's pair is timed on both halves; claim 7 keeps three of its four levels and loses the fourth to the retirement, as the paragraph above records. **What the roster raises is what Runs 22, 24, 26 and 27 all raised, and this run answers it rather than deferring it**: nine arms read below `mut-odo-vecdims`, and unlike Run 27's twenty-one every one of them is a fill that writes the result. The shipped library route is among them, `lib-stage1` at 0.028 against 0.051, and the fastest of them is `lib-stage2-lean` at 0.027. **So the honest count of arms faster at the same work is nine and needs no discount this run**, where Run 27's twenty-one had to be read down to twelve because nine were rows the correction had emptied. What is left for the next run is not an arithmetic correction but a decision: `lib-stage2-lean` is the branch's own driver and it leads the table.

1. **Retired 2026-09-04** with the prune, which parked every rung below `mut-odo-vecdims`; its last reading is Run 24's, above, where it held on both halves. What it was: `mut-odo-vecdims` < `mut-flat-gm` < `bq-mut-runs-gm-mulback` < each of `bq-scan-rem-gm-mulback` and `bq-odo-gm-mulback`, the whole ordering read on unconditional arms --- **the ladder the `needs` column draws**, each link pricing one thing the implementation is allowed to ask for. **The foot rung retired 2026-08-29**, on a decision rather than on a reading: it registered the two arms needing nothing at all as a tie, so that either was what would ship if the mutating `Vector` method were refused upstream, and it had read as a tie on every run that carried it. Both arms stay rostered and stay in the rung above, `--pair bq-scan-rem-gm-mulback bq-odo-gm-mulback` recovers the reading whenever the upstream answer wants it, and its last is Run 21's, above. The middle link is the one the README has seen a layout term move --- 0.9708 at 15 of 24 on Run 10's unaligned half against 0.9293 at 22 on its aligned one --- and on a placed layout it has now read the aligned figure five runs running. The ordering has survived nine runs, two changes of basis, two repetitions and three compilers.
2. **Retired 2026-08-28** with the parking of `offtab`, the arm its surviving link turned on --- the other, `bq-scan-rem-gm-mulback`, is timed still. What it asked, since the settlement of 2026-08-24 re-aimed it, was where the arms needing something other than the fix sit --- `offtab` behind `bq-scan-rem-gm-mulback`, a mutable `Int` scratch priced against needing nothing --- and its last reading is Run 20's, above. **Its second link, `bq-expand` behind `mut-odo-vecdims`, had gone already, 2026-08-26** on the reading above, its condition having been spent since 2026-08-24 --- what it priced was the branch's own code against its replacement, and the replacement is now the branch's own code. It was the widest ordering the manifest carried, and `--pair` recovers it, both arms staying rostered and timed.
3, 4, 5. **Retired at Run 19's write-up**, on a last reading in which all three held on both compilers, and for the reasons in the settlement paragraph at the foot of this section. Their numbers are left standing here rather than reused: a verdict recorded against *claim 4* in an earlier run's file still means what it said. What they were, in a clause each, so that nothing is recoverable only from a manifest diff --- claim 3, that a mul-back output pays on the `bq-expand` build; claim 4, that the scan build ties its own build control while beating `bq-expand`, a tie Run 17 promoted to an ordering and three runs then read as one; claim 5, that `bq-expand` beats `bq-gen`, whose refutation of the generate-per-element build stands on Runs 7 and 8. **What none of the three could still foreclose is the point**: every one asks where `bq-expand` sits among arms nothing ships, on a branch whose fix is the `mut-odo-vecdims` family. The arms all stay rostered and timed, so any of these orderings is one `--pair` call away.
6. **Retired 2026-08-28** with the parking of `gen-quotrem`, the arm its only link turned on. What it asked: `gen-quotrem` ties `list`, the first attempt's arithmetic ceasing to be dearer than the list's allocation once the flag takes its own allocation to 1.00x against the list's 23.5x --- the mixed picture this suite exists to have refuted, arriving by a route nobody proposed. Its last reading is Run 20's, above; the `cm-gather` < `list` half was untimed throughout and stands as Run 8's. What goes with it is the standing advice to check `list` as an anchor before blaming a strategy, which is now nobody's claim and is why the machine check reads `list` net per shape every run.
7. Allocation, median multiples of the result **over a pinned shape set**: the `libunord` consumers 0.00x, the mutable fills and the `liblist` consumers 1.00x, `bq-expand` 2.06x, `list` 22.38x, every one of them read over **the eighteen main-set shapes claim 7 is pinned to**, of the nineteen this roster times, which are a strict subset of the 24 Runs 15 to 23 shared --- **re-pinned 2026-09-05, six of those 24 having been retired from timing the day before**, so the old population no longer exists and the predicate could not otherwise be evaluated; re-listed 2026-08-28 with the parking, and 2026-09-04 with the prune, which parked the two tiers between the fills and `bq-expand` outright and left the 2.00x one to the two `liblist` arms, named in the paragraph above; **re-aimed 2026-09-11 at the levels the roster times**, the 2.00x level having lost its two arms to the ruling of 2026-09-09, which took the ports to 1.00x and then off the timed roster, and the 0.00x level, read beside the claim since Run 23 on the `libunord` Fill arms, being their consumers now that those arms are checked and not timed, a consumer returning no result at all. Every level has reproduced over the eighteen on Runs 23, 24 and 25, which is what makes this the claim to check first when anything else moves. **The pinning is the predicate and not a caveat on it**: Run 24's published column reads 2.40x for `bq-expand` on its own 26, 2.35x on the 24 and 2.06x on these 18 --- so a level compared across two shape sets moves for a reason that is neither a code change nor a slot, and only a level that moves ON ONE POPULATION is a code change. **Read the levels and the cells as two questions**, which Run 19 is the run that separated: its levels all returned while the cross-half cell agreement fell to 1016 of 1080, where the 9.14 pair had 1072 --- so a compiler can reallocate within a tier without moving any tier, and only the cell count sees it.
8. **Retired 2026-08-29**, on a decision and one day after its last re-aiming. What it asked: that every pure arm in the fast tier run its output through the single in-order `vGenerate` over an `m`-length table, so a `bq-*` arm falling behind loses on its table build and not on its output. Its last reading is Run 21's, which found it true and its subject smaller again --- nine arms at or below `mut-odo-vecdims` and not one of them a `bq-*`, so the structure it described governed a tier starting a third of the way down. What retires it is that subject: the pure tier is no longer what ships, and a claim quantified over it was being re-aimed and re-read every run to say so. It was also the one claim with no named invocation, read off the table by eye throughout, so nothing mechanical goes with it.
9. **Retired at Run 19's write-up with 3, 4 and 5**, and for a reason of its own worth keeping: its per-shape half was answered rather than abandoned. `bq-expand-b`'s two best cells were `stretch-inner1` and `stretch-wide-2xM` in every run from Run 8 to Run 16 --- the rank-2 views with one huge outer dimension where seeding from `enumFromStepN` replaces the whole `concatMap` build --- until **Run 17 read `stretch-inner1` and `stretch-square-1341` instead**, which registered the follow-up. Runs 18 and 19 both read the original pair back, on four compilers between them, so the excursion was one run's and the mechanism is cleared of picking the wrong shapes. The geomeans were never the stable part, and the series is why: across Runs 8 to 13 `bq-expand-b` / `bq-expand` read 0.996, 0.9678, 0.9943, 0.9819, 0.9923 and 0.9909, its sign test crossing into significance only on the last of them, while `bq-expand-zf` / `bq-expand` went 3.6% behind, then level at 1.0028, then 1.0325, 1.0197, 1.0256 and 1.0265. Both series were closed at Run 13 and not extended per run, and a closed series in a live manifest is maintenance without a question --- which, with the follow-up spent, is the whole case for retiring it.
10. `mut-odo-vecdims-add-in-leaf-u2` < `mut-odo-vecdims`, the shipped fill against the family root --- **what the leaf fusion buys**, the one thing the roster still prices that the library has taken. Registered 2026-09-04 with the prune, in claim 1's place.

**What the parking of 2026-08-28 did to this set**, recorded in prose because a live item carries a predicate and no reading, and because naming a parked arm inside one is now a `--lint` failure. Claims 2 and 6 retired with `offtab` and `gen-quotrem`, the arms their surviving links turned on. Claim 7 lost three of the arms that carried its levels --- `gen-quotrem` at 1.00x, `bq-mut` at 1.33x and `offtab` at 2.00x, every figure of theirs ending at Run 20 --- but no level with them: `offtab-scan-rem` carries the 2.00x tier, the mutable fills the 1.00x and the scan family the 1.33x, so the list is the same six levels it was. Claim 8 was re-aimed off `bq-expand-zf` and `bq-gen`, its span running from the leading tier to `offtab-scan-rem` instead; the readings above are the last that name the two. Nothing was retired on a reading here, the parking being a decision about what is worth a bench, and the two claims that went had no link left that a run could measure.

**What the prune of 2026-09-04 did to this set** ([what the benchmark does](../README.md#what-the-benchmark-does)), read on the first run after it. Claim 1 retired, every rung below its top parked, and claim 10 is registered in its place, the one live ordering. Claim 7 lost the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x, which have no timed arm left, and keeps `offtab-scan-rem`'s 2.00x through the two `liblist` arms; and it lost its pinned population as well, six of the 24 shapes it named having been retired the same day, which this run re-cut to the eighteen that remain. The ruling of the same day on the short bodies parked `lib-stage2-short` and `lib-stage2-short-lean`, whose last readings stand as Run 24's. The class properties keep their arms, `mut-odo-vecdims` and `bq-expand` both staying timed --- **and property 1's two counter-examples are down to one**: `gen-unsafe`, which carried a `worst` above 1 in seven of ten populations as the baseline's own control, is parked, so the only arms above 1 now are the library-shaped ones and the `list` twins.

**Two homes, and which carries what.** Each live claim has a prose paragraph here and a numbered predicate at the foot, and they divide: the PROSE carries this run's figures and what moved, the ITEM carries the predicate the next run checks and no reading at all. A RETIRED claim keeps its numbered item, which says in a clause what it was, and gets no prose paragraph --- Run 20 wrote two and they restated their own items clause for clause, because stripping the figures had removed the only thing that distinguished them. Each ordering is one line of `--claims`, whose manifest now carries the registered expectation --- the direction of the geomean, a tie by sign test, or claim 9's two best shapes --- and prints HELD or BROKE beside the paired geomean, interval and sign test. `--claims --in-place` then installs that arithmetic as each claim's `Readings:` paragraph above, so a run no longer transcribes it at all; what stays the reading's is whether a HELD margin moved and whether a movement clears the floor, and a BROKE is what obliges the paragraph above its reading to be rewritten rather than requoted. **A claim with no named invocation is a gap in this list, not a claim to be checked by hand**: where a session has to invent the computation it will invent a wrong one, which is how claim 7 came to be read off the raw fitted bytes, explained by a mechanism the previous pair refutes, and then "corrected" onto a rounded print. It has `--compare --alloc` now, and no live claim is without one since claim 8 retired. **The general form, and it is a standing instruction rather than an observation: if a write-up hand-rolls a script to answer something the reader should answer, that is a defect report against the reader** --- fix it there, before the sentence it was written for, or the next run invents its own wrong version. **Two riders, both bought on Run 19.** A new MODE joins the guards its siblings already have, and is checked against them rather than written beside them: `--counts` shipped able to be given without the `--compare` it reads, silently printing the default table and exiting 0 --- the unread-flag family exactly, added next to four sibling readings of `--compare` that were every one of them already guarded, and joining none. **And an instrument may be BACKED OUT, which is not a failure of the write-up but a result of it.** Measure what it flags before shipping it: the obvious mechanical repair for stale paragraphs was built and returned 100 for the four that mattered, so it went, and the refutation with its numbers is under the tasks heading --- worth more than the mode, since it stops the next session building the same thing. A report that never empties is one nobody reads, which this file already knows about hints.

**And for each stride class, the same three properties, now carrying Run 28's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** Both held in every one of the eleven populations, on both halves --- the first in every regime, roster and layout the README has run, so it was never slower than the `list` it replaced, on any shape of any class the library can produce; the second folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and read that way here for the third time. This is the property the classes exist to test, no geomean can state it, and a break would be the one result here to bear on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims` its `worst` is 0.127 on the main set and between 0.032 and 0.095 in the classes, highest on `window`; the `bq-expand` clause is closest at `stretch-pow2stride` on the main set and at `window-64x64-k1x9`, 0.8089, in a class --- `scaled`'s 0.3343 being the widest of the ten per-class closest readings rather than the narrowest.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm now breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. Of the five library-shaped arms Run 27 found breaking it at `runs-2` --- `libunord-stage1` 1.237, `liblist-stage1` 1.214, `lib-stage1` 1.198, `liblist-stage2` 1.078 and `libunord-stage2` 1.076 --- four are retired to `check` and are not timed here. Those five figures are RECOMPUTED from Run 27's own cells, that run's prose and its own installed table having printed different ones, and the five are the largest of NINE library-shaped arms over `list` there; a tenth row above it is `list-aa-adjacent`, `list`'s own A/A copy, at 1.0111. **What is left is `lib-stage1` at `runs-2`, 1.219 on the basis and 1.218 on the control, and at `runs-3`, 1.015 and 1.002.** A sweep of every cell of all eleven populations on both halves finds no other timed arm above its shape's `list` anywhere, the baseline's own A/A copies aside. So the shipped route is slower than `list` on runs of two and three elements and on nothing else the library can produce, which is a sharper statement of the same defect than any run before this one could make.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves.** The `list` clause is closest at `small-patch-k5`, 0.06580, and under 0.053 everywhere else. The `bq-expand` clause is closest at `small-row96`, 1.00441, then `scaled-rank1-m1` at 1.00003 and `stretch-tall-Mx2` at 1.00000 --- three cells inside the margin the strict form would have failed on, which is why the margin is there and why it is not widened further.

3. **The allocation tiers survive and their ORDER is unbroken in all ten, on the main set and on every class but `small` --- outside the level clause since 2026-09-07, being the class built to break it, and read here for what it shows**: the mutable fills at the result vector, `bq-expand` between 1.00x and 2.75x it, `list` an order of magnitude above. On the main set the fills read 1.00x, `bq-expand` 2.06x and `list` 22.38x over the pinned eighteen, and Runs 23 to 27's own basis halves give every one of those levels over the same shapes. **What this roster removes is the 2.00x tier**, whose members were the list Fill arms now checked and not timed, and what it adds below the fills is the `libunord` consumers at 0.00x and the `liblist` consumers at 1.00x --- the latter falling to a fill on the main set, which is what registration (11) says they do there. `small` reads its fills at 1.27x and `bq-expand` at 1.34x, the per-call constants showing through a call under a microsecond, and its ORDER is unbroken like the rest.

**SETTLED 2026-08-24 and APPLIED 2026-08-25, at Run 19's write-up rather than at the settlement**, so that the retiring orderings got one last cross-compiler reading and the retirement is recorded with it --- which is how Run 17 retired claim 4's tie, in prose at its write-up with the manifest taking it the same day. **They got it**: all thirteen held on both of Run 19's halves, and the eight that remain hold on both too, so each of the four retires on a reading rather than on a decision. `CLAIMS` in `read-run.py` carried claims 1, 2 and 6 alone after that settlement, and **claim 1 alone since 2026-08-28**, `offtab` and `gen-quotrem` having been parked. The test applied: an ordering stays only if it forecloses something anyone would propose again *and* can still break. What fails both is a figure, and figures live in the tables above. **Claims 3, 5 and 9 retire outright.** Claim 3 sets one output form against another on a build nothing ships, where every leading pure arm is a `-gm-mulback` already; claim 5's `bq-expand` / `bq-gen` says of itself that the refutation stands on Runs 7 and 8, and claim 6 keeps that family guarded through `gen-quotrem`; claim 9's two series are closed at Run 13 by this section's own words, and a closed series in a live manifest is maintenance without a question. **Claim 4 retires with them, its tie moving into claim 1** --- and what goes with it is the one place the manifest reads a *builder* apart from its output, which `--pair` recovers whenever it is wanted, both arms staying rostered. **Claim 1 becomes the ladder the `needs` column already draws**, gaining `bq-scan-rem-gm-mulback`, the best arm needing nothing at all, between `bq-mut-runs-gm-mulback` and `bq-odo-gm-mulback`: the first ahead of it at **0.9060** and **0.9171** on Run 18's two halves, 19 and 17 of 24, which is what a mutable `Int` scratch buys; and the second **tied** with it at **0.9902** and **0.9936**, 13 and 12 of 24 at p 0.84 and 1, so the two fastest pure arms were indistinguishable and either was what would ship if the mutating method were refused --- the rung this installed, and retired again on 2026-08-29. Its three existing links stay, the middle one redundant with the two new ones and carrying seven runs of history they do not. **Claim 2 keeps its number and changes its question** to where the arms needing something other than the fix sit: `offtab`, which needs only that `Int` scratch, behind `bq-scan-rem-gm-mulback` at **1.36** and **1.44**; and `bq-expand` behind `mut-odo-vecdims` at **2.09** and **2.13**, kept only while `Data/Array/Internal.hs` carried `bq-expand`, and to retire with the three `TODO: retarget` markers, which were one decision with it. **That condition was spent the same day and the link outlived it by two runs**: the file went to `vFillStrided` on 2026-08-24 and the markers went with the prose they marked, but nothing read the condition back, so Run 20 registered and read the link like any other and it retired only on 2026-08-26. Thirteen registered orderings become eight here, and seven when that second link finally goes; claims 7 and 8 stayed unmanifested prose. **The rewriting the ask paired with this was already done**: the eight *What the class says* paragraphs were written to the re-aimed properties at Run 18's write-up, and only the sentence asking for it survived.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 28 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) records every class on BOTH halves**, one process each, in [the sequence](../README.md#making-a-major-benchmark-run), twenty-two processes in ONE window with no population re-measured. Every table below is the **basis half**'s, the 9.12 one, the half that keeps the lineage; what the second half buys is that the pair's variable --- the compiler --- can be read on a class, with the counted work beside it on every population. **Read across the halves the two compilers are close everywhere and the vote is nearer even than the class count suggests.** Of the **200 arm-comparisons** the ten classes carry --- twenty apiece, being the arms with a corrected time --- **NONE sits out the vote**, where Run 27 had nine it could not count because the correction had not left the basis cell positive; **86 put the basis faster and 114 the HEAD half**, and the ten geomeans run **0.9913 on `scaled` to 1.0105 on `bcastmid`**, three of them below 1 and seven above. **The low extreme over all ten is `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9444 on `block`** and the high `mut-odo-vecdims-add-in-leaf-u1` at 1.0636 on `scaled`; the pointer fills stay inside a few points of 1 on every class for a second run, which is what the GHC #27778 workaround did. **Four classes have `list` past the 0.7% bar** --- `bcastmid` 1.0185, `bcast` 1.0138, `small` 1.0136 and `block` 0.9921 --- so those four lines are orderings and say so, and the seven read for the pair's variable are `rev`, `window`, `scaled`, `runs`, `flip`, `compose` and the main set.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the claims](#the-claims-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on five of the ten --- `rev`, `bcast`, `bcastmid`, `small` and `compose` --- and the family's own ceiling on the other five, where the ceiling is faster. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 3 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. Item 2's own table is NOT among what that call prints, which is why the list above starts where it does: it comes from the separate `--block --in-place` call item 2 itself names. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.047 | 0.069 | **`lib-stage1`** 0.025 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.025 | 0.41% |
| `bcast` | 6 | 0.022 | 0.063 | **`lib-stage2-lean`** 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 | 1.33% |
| `bcastmid` | 4 | 0.032 | 0.058 | **`lib-stage2-lean`** 0.013 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 | 0.46% |
| `window` | 8 | 0.056 | 0.095 | `lib-stage1` 0.030 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.026 | 0.37% |
| `scaled` | 3 | 0.031 | 0.032 | `lib-stage1` 0.025 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.025 | 0.38% |
| `runs` | 14 | 0.030 | 0.063 | `lib-stage2-lean` 0.027 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.026 | 3.36% |
| `flip` | 6 | 0.032 | 0.061 | `lib-stage2-lean` 0.026 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.025 | 0.71% |
| `block` | 5 | 0.026 | 0.033 | `lib-stage2-lean` 0.024 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.022 | 0.40% |
| `small` | 5 | 0.066 | 0.094 | **`lib-stage2-lean`** 0.057 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.058 | 0.59% |
| `compose` | 4 | 0.027 | 0.032 | **`lib-stage2-lean`** 0.017 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 | 0.56% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On five rows --- `window`, `scaled`, `runs`, `flip` and `block` --- the bold sits in the CEILING column instead**, and it names three arms rather than one: `mut-odo-vecdims-add-in-leaf-u2-ptr` on `window` and `block`, `-u1-ptr` on `scaled`, `runs` and `flip`. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship. `rev` is the close case and goes the other way: its ceiling `-u2-last` and `lib-stage1` both print 0.025, and the column separates them at the fourth decimal in `lib-stage1`'s favour. So the table's bolding and the head's count of ten differ by convention and not by measurement, and this sentence is where the two are reconciled.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.15* | *135* | *2.52x* |
| liblist-stage1-sum | -- | -- | 0.07 | 147 | 1.01x |
| liblist-stage2-sum | -- | -- | 0.07 | 147 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.09 | 147 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.07 | 147 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.07 | 147 | 1.03x |
| libunord-stage2-sum | -- | -- | 0.08 | 157 | 0.01x |
| libunord-stage3-sum | -- | -- | 0.07 | 157 | 0.01x |
| libunord-stage4-sum | -- | -- | 0.08 | 157 | 0.01x |
| libunord-stage5-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-list-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.08 | 157 | 0.01x |
| libunord-stage8-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.05 | 157 | 0.02x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.11* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *158* | *0.00x* |
| lib-stage1 | 0.025 | 0.047 | 0.13 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.025 | 0.045 | 0.07 | 147 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.025 | 0.039 | 0.10 | 148 | 1.01x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.025* | *0.045* | *0.08* | *147* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.045 | 0.09 | 147 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.045* | *0.11* | *147* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.026 | 0.046 | 0.09 | 147 | 1.00x |
| lib-stage2-lean | 0.026 | 0.034 | 0.10 | 147 | 1.01x |
| lib-stage2-lean-u1 | 0.028 | 0.035 | 0.07 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.030 | 0.043 | 0.07 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.046 | 0.13 | 146 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.047* | *0.069* | *0.09* | *138* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.047* | *0.069* | *0.06* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.047** | 0.069 | 0.06 | 138 | 1.00x |
| *bq-expand-aa-distant* | *0.104* | *0.177* | *0.14* | *129* | *2.52x* |
| bq-expand | 0.104 | 0.177 | 0.12 | 129 | 2.52x |
| *bq-expand-aa-adjacent* | *0.104* | *0.177* | *0.15* | *129* | *2.52x* |
| list (baseline) | 1.000 | 1.000 | 0.23 | 87 | 23.43x |
| *list-aa-distant* | *1.000* | *1.002* | *0.27* | *87* | *23.43x* |
| *list-aa-adjacent* | *1.004* | *1.008* | *0.27* | *87* | *23.43x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0041, worst cell 0.78% on `rev-cnn-L1-24x24-c1`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9984 on a worst cell of 0.46% on `rev-cnn-L1-24x24-c1`, its interval covering 1. The in-situ term reads 0.9950, 1.0068 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0039, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m8s, peak 95 MiB in use, 24 MiB max residency; the reader reads 39 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.04 ms per call raw, 3.89 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.069/0.053/0.028

**Across the halves:** 8 of the 20 arms are faster on this half and 12 slower, at a geomean of 1.0027, from `lib-stage2-lean-u1` at 0.9891 to `lib-stage2-lean` at 1.0249, with `list` itself at 1.0031.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.069, tiers at 1.00x, 2.52x and 23.43x --- and `lib-stage1` leads outside the family at 0.025, 0.6237 paired against `mut-odo-vecdims` and ahead on 3 of 3 shapes, a 37.63% margin against this class's 0.41% floor. **This is the class that kills registration (7)**, and it is the population that had least business moving: `rev` has no zero stride, so stage nine's route is stage six's exactly, and `libunord-stage9-sum` reads **1.0191 and 1.0164** against floors of 0.41% and 0.48%, past them on both halves. It is also where registration (4)'s tie-break sits outside the floor on the basis alone, 1.0047 against 0.41% where HEAD reads 1.0034 against 0.48%, and where registration (11)'s two ordered dispatches part, 0.9950 on each half. Three different reorderings show on this three-shape class and on no other population with no zero stride; that they show together here, and not one at a time, is the reading to carry. Across the halves 8 of the 20 arms with a corrected time are faster on the basis and 12 slower.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09 for this run --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and registration (7) reads no crossover on it.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.60* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.52 | 61 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.48 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.51 | 61 | 1.00x |
| libunord-stage2-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage4-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.52 | 62 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.54 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage8-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 74 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| lib-stage2-lean | 0.018 | 0.022 | 0.49 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.018 | 0.027 | 0.41 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.018 | 0.026 | 0.49 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.018 | 0.022 | 0.48 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.021 | 0.022 | 0.50 | 61 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.022* | *0.52* | *61* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.021 | 0.022 | 0.50 | 61 | 1.00x |
| lib-stage1 | 0.021 | 0.022 | 0.50 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.022 | 0.40 | 61 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.021* | *0.022* | *0.40* | *61* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.021* | *0.063* | *0.34* | *61* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.022* | *0.063* | *0.32* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.022** | 0.063 | 0.06 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.022 | 0.027 | 0.40 | 61 | 1.00x |
| bq-expand | 0.096 | 0.098 | 0.68 | 46 | 1.00x |
| *bq-expand-aa-adjacent* | *0.096* | *0.098* | *0.72* | *46* | *1.00x* |
| *bq-expand-aa-distant* | *0.096* | *0.099* | *0.22* | *46* | *1.00x* |
| list (baseline) | 1.000 | 1.000 | 1.06 | 18 | 18.99x |
| *list-aa-adjacent* | *1.007* | *1.015* | *0.82* | *18* | *18.99x* |
| *list-aa-distant* | *1.007* | *1.014* | *0.91* | *18* | *18.99x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0133, worst cell 3.81% on `bcast-tall-Mx2`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9990 on a worst cell of 0.53% on `bcast-src64`, its interval covering 1. The in-situ term reads 1.0233, 1.0117 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0096, which the correction amplifies by 1.40x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h20m18s, peak 141 MiB in use, 42 MiB max residency; the reader reads 39 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 27.5 ms per call raw, 26.4 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.032/0.021/0.063/0.021/0.021/0.021

**Across the halves:** 4 of the 20 arms are faster on this half and 16 slower, at a geomean of 1.0041, from `bq-expand` at 0.9846 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0145, with `list` itself at 1.0138. **The baseline moved 1.38% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.063, tiers at 1.00x, 1.00x and 18.99x --- and `lib-stage2-lean` leads outside the family at 0.018, 0.6584 paired against `mut-odo-vecdims` and ahead on 6 of 6 shapes, a 34.16% margin against this class's 1.33% floor, the second widest of the ten. **The repeat ladder landed here and answered what it was added for**: `bcast-src8`, `-src64` and `-src512` hold 1.8 million elements fixed and vary the source, and registration (7) reads stage nine's consumer over stage six's at 0.4732 and 0.4993, 0.4587 and 0.4616, 0.6565 and 0.6571 --- no crossover between the repeated slice and the fill anywhere on the ladder, which is the question the rungs were cut for. The broadcast unroll shows here too: `bcast-tall-Mx2`, a run of 2, reads `lib-stage2-lean-u1` at **1.2061 and 1.2058**, so not unrolling costs a fifth where the pre-run probe priced it at 1.08, and `bcast-inner8` reads the same way. That is why registration (13)'s class span misses in the arm's favour, 1.0818 and 1.0822 against a registered 1.03 within 3%. Across the halves 4 of the 20 arms are faster on the basis and 16 slower.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.43* | *70* | *1.57x* |
| liblist-stage1-sum | -- | -- | 0.37 | 80 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.44 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.35 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.43 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.36 | 80 | 1.00x |
| libunord-stage2-sum | -- | -- | 0.34 | 82 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.33 | 82 | 1.00x |
| libunord-stage4-sum | -- | -- | 0.34 | 82 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.33 | 82 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.35 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.55 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.34 | 82 | 1.00x |
| libunord-stage8-sum | -- | -- | 0.33 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 94 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.29* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *88* | *0.00x* |
| lib-stage2-lean | 0.013 | 0.019 | 0.41 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.014 | 0.021 | 0.50 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.019 | 0.028 | 0.55 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.022 | 0.033 | 0.35 | 80 | 1.00x |
| lib-stage1 | 0.022 | 0.033 | 0.33 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.033* | *0.43* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.033* | *0.29* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.033 | 0.30 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.022 | 0.033 | 0.37 | 79 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.024 | 0.031 | 0.52 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.034 | 0.40 | 78 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.058* | *0.30* | *76* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.032* | *0.058* | *0.30* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.032** | 0.058 | 0.33 | 76 | 1.00x |
| *bq-expand-aa-adjacent* | *0.087* | *0.133* | *0.44* | *64* | *1.57x* |
| bq-expand | 0.087 | 0.132 | 0.43 | 64 | 1.57x |
| *bq-expand-aa-distant* | *0.087* | *0.132* | *0.39* | *64* | *1.57x* |
| *list-aa-adjacent* | *0.995* | *1.000* | *0.79* | *30* | *21.22x* |
| *list-aa-distant* | *0.997* | *0.999* | *0.91* | *30* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 0.83 | 30 | 21.22x |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 0.9954, worst cell 1.08% on `bcastmid-primes`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `bcastmid-block150k`, its interval covering 1. The in-situ term reads 1.0184, 1.0466 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9956, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m34s, peak 132 MiB in use, 35 MiB max residency; the reader reads 39 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 46.2 ms per call raw, 45.1 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.058/0.021/0.036/0.024

**Across the halves:** 1 of the 20 arms are faster on this half and 19 slower, at a geomean of 1.0105, from `lib-stage2-lean` at 0.9690 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0326, with `list` itself at 1.0185. **The baseline moved 1.85% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.058, tiers at 1.00x, 1.57x and 21.22x --- and `lib-stage2-lean` leads outside the family at 0.013, the best any arm outside the family reads in any class, 0.4132 paired against `mut-odo-vecdims` and ahead on 4 of 4 shapes, a 58.68% margin against this class's 0.46% floor. **This is the doubling block copy's class and the clearest thing the fills changed this run.** Where Run 27 read the lean fill BEHIND the shipped leaf on the zero-stride views, 2.33 and 2.28 on `bcastmid-b200k`, registration (14) now reads `lib-stage2-lean` over `lib-stage1` at **0.6163 and 0.6420** across the class, against a registered 0.85 within 10% --- the span misses because the doubling buys nearly twice what the filtered probe priced, not because anything failed. The zero-stride axis is the middle one here, so the copy runs where `bcast`'s cannot, which is the asymmetry the two classes exist to separate. Across the halves 1 of the 20 arms is faster on the basis and 19 slower, the most one-sided reading of the ten.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09 for this run, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.32* | *68* | *2.75x* |
| liblist-stage1-sum | -- | -- | 0.19 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.19 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.16 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.18 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.17 | 82 | 1.00x |
| libunord-stage2-sum | -- | -- | 0.18 | 82 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.19 | 88 | 1.00x |
| libunord-stage4-sum | -- | -- | 0.05 | 96 | 0.04x |
| libunord-stage5-sum | -- | -- | 0.07 | 96 | 0.04x |
| libunord-stage6-list-sum | -- | -- | 0.05 | 100 | 0.04x |
| libunord-stage6-loop-sum | -- | -- | 1.12 | 90 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.06 | 96 | 0.04x |
| libunord-stage7-sum | -- | -- | 0.06 | 100 | 0.04x |
| libunord-stage8-sum | -- | -- | 0.06 | 100 | 0.04x |
| libunord-stage9-sum | -- | -- | 0.10 | 96 | 0.04x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.23* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *97* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.029 | 0.25 | 84 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.029 | 0.032 | 0.21 | 83 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.030 | 0.033 | 0.18 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.030* | *0.033* | *0.17* | *82* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.030* | *0.034* | *0.20* | *82* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.030 | 0.034 | 0.17 | 82 | 1.00x |
| lib-stage1 | 0.030 | 0.034 | 0.18 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.031 | 0.033 | 0.16 | 82 | 1.00x |
| lib-stage2-lean | 0.031 | 0.035 | 0.18 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.035 | 0.17 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.031 | 0.036 | 0.17 | 82 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.056* | *0.095* | *0.16* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.056** | 0.095 | 0.16 | 76 | 1.00x |
| *mut-odo-vecdims-aa* | *0.057* | *0.096* | *0.16* | *76* | *1.00x* |
| bq-expand | 0.123 | 0.131 | 0.31 | 64 | 2.75x |
| *bq-expand-aa-adjacent* | *0.123* | *0.131* | *0.27* | *64* | *2.75x* |
| *bq-expand-aa-distant* | *0.123* | *0.131* | *0.24* | *64* | *2.75x* |
| *list-aa-distant* | *1.000* | *1.004* | *0.55* | *32* | *24.77x* |
| list (baseline) | 1.000 | 1.000 | 0.52 | 32 | 24.77x |
| *list-aa-adjacent* | *1.002* | *1.007* | *0.46* | *32* | *24.77x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9963, worst cell 1.42% on `window-64x64-k1x9`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 0.9994 on a worst cell of 0.45% on `window-64x64-k1x9`, its interval covering 1. The in-situ term reads 1.0018, 1.0800 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9981, which the correction amplifies by 2.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h26m56s, peak 131 MiB in use, 53 MiB max residency; the reader reads 39 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 12.7 ms per call raw, 12.2 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.044/0.055/0.095/0.036/0.058/0.057/0.059/0.058

**Across the halves:** 4 of the 20 arms are faster on this half and 16 slower, at a geomean of 1.0068, from `bq-expand` at 0.9896 to `lib-stage2-lean` at 1.0513, with `list` itself at 1.0047.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.095, the highest of the ten, and tiers at 1.00x, 2.75x and 24.77x --- and `lib-stage1` leads outside the family at 0.030, 0.5190 paired against `mut-odo-vecdims` and ahead on 8 of 8 shapes at sign p 0.0078, a 48.10% margin against this class's 0.37% floor, the tightest of the ten and one the shipped fill's own A/A pair sets. **This class carries more of the registration than any other.** It is where the tie-break fires as designed: registration (5) reads `libunord-stage7-sum` over `-stage6-sum` at **0.7425 and 0.7237** against a registered 0.7 within 15%, and the two channel views added for it, `window-64x64-c16-k3` and `window-32x32-c64-k3`, are the shape the tie exists in. It is where the fold entry point is priced, registration (8) reading the hand-written loop at **1.7249 and 1.7422** behind the fused list, so the flat walker's known continuations survive a full budget. It is where base's own `sum` is ahead of the harness's fold, registration (12) at **0.7176 and 0.7314**. And it is where registration (6) is killed: stage eight, argued to keep stage six's order and move no run here, reads **1.0518 and 1.0474** past floors of 0.37% and 0.40%. Across the halves 4 of the 20 arms are faster on the basis and 16 slower.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.08* | *119* | *1.14x* |
| liblist-stage1-sum | -- | -- | 0.14 | 128 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.17 | 127 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.15 | 128 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.19 | 127 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.13 | 127 | 1.01x |
| libunord-stage2-sum | -- | -- | 0.22 | 127 | 1.01x |
| libunord-stage3-sum | -- | -- | 0.20 | 127 | 1.01x |
| libunord-stage4-sum | -- | -- | 0.29 | 127 | 1.01x |
| libunord-stage5-sum | -- | -- | 0.24 | 127 | 1.01x |
| libunord-stage6-list-sum | -- | -- | 0.24 | 127 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.16 | 127 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.24 | 128 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.18 | 128 | 1.00x |
| libunord-stage8-sum | -- | -- | 0.25 | 128 | 1.01x |
| libunord-stage9-sum | -- | -- | 0.12 | 128 | 1.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.19* | *146* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *138* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.025 | 0.034 | 0.17 | 128 | 1.00x |
| lib-stage1 | 0.025 | 0.034 | 0.19 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.035 | 0.13 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.026 | 0.041 | 0.26 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.034* | *0.13* | *128* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.026 | 0.035 | 0.14 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.034 | 0.12 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.026 | 0.034 | 0.19 | 128 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.034* | *0.13* | *128* | *1.00x* |
| lib-stage2-lean | 0.027 | 0.035 | 0.17 | 127 | 1.00x |
| lib-stage2-lean-u1 | 0.027 | 0.034 | 0.15 | 127 | 1.00x |
| **mut-odo-vecdims** | **0.031** | 0.032 | 0.09 | 127 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.032* | *0.10* | *127* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.031* | *0.032* | *0.12* | *127* | *1.00x* |
| bq-expand | 0.096 | 0.099 | 0.06 | 112 | 1.14x |
| *bq-expand-aa-adjacent* | *0.096* | *0.099* | *0.08* | *112* | *1.14x* |
| *bq-expand-aa-distant* | *0.096* | *0.099* | *0.07* | *112* | *1.14x* |
| list (baseline) | 1.000 | 1.000 | 0.24 | 71 | 19.43x |
| *list-aa-adjacent* | *1.001* | *1.002* | *0.21* | *71* | *19.43x* |
| *list-aa-distant* | *1.001* | *1.004* | *0.21* | *71* | *19.43x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9962, worst cell 0.60% on `scaled-super-r3`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.02% on `scaled-rank1-m1`, its interval missing 1. The in-situ term reads 1.0180, 1.0069 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9985, which the correction amplifies by 2.43x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m9s, peak 109 MiB in use, 30 MiB max residency; the reader reads 39 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 4.75 ms per call raw, 4.58 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.026/0.031/0.032

**Across the halves:** 19 of the 20 arms are faster on this half and 1 slower, at a geomean of 0.9913, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9675 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0636, with `list` itself at 0.9963.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.032, the joint lowest of the ten with `compose`'s 0.032, and tiers at 1.00x, 1.14x and 19.43x --- and `lib-stage1` leads outside the family at 0.025, 0.9320 paired against `mut-odo-vecdims`, ahead on 2 of 3 shapes at sign p 1, a 6.80% margin against this class's 0.38% floor. The family's own ceiling is level with it at 0.025 and ahead on the paired reading, 1.0483 the other way, so this is one of the classes where the ceiling still beats everything outside the family: it leads on five by the published column, on five paired, and on four by both. **Nothing in the registration is decided here and that is the result**: the three superincreasing views have no zero stride and no run, so stage six through stage nine are one route on all of them, and registrations (2), (4) and (7) all hold, the widest of the six readings being 1.0053. Across the halves 19 of the 20 arms are faster on the basis and 1 slower, the most one-sided reading in the other direction.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.43* | *52* | *1.08x* |
| liblist-stage1-sum | -- | -- | 0.11 | 64 | 0.19x |
| liblist-stage2-sum | -- | -- | 0.27 | 69 | 0.14x |
| liblist-stage3-sum | -- | -- | 0.03 | 70 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.03 | 70 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.09 | 64 | 0.19x |
| libunord-stage2-sum | -- | -- | 0.18 | 69 | 0.14x |
| libunord-stage3-sum | -- | -- | 0.18 | 59 | 1.00x |
| libunord-stage4-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage5-sum | -- | -- | 0.03 | 70 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.03 | 71 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.05 | 69 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.04 | 70 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.03 | 70 | 0.00x |
| libunord-stage8-sum | -- | -- | 0.03 | 70 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 70 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.17* | *78* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *69* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.028 | 0.15 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.028 | 0.12 | 59 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.029* | *0.50* | *59* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.029* | *0.13* | *59* | *1.00x* |
| lib-stage2-lean | 0.027 | 0.029 | 0.12 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.029 | 0.13 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.029 | 0.14 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.027 | 0.029 | 0.13 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.030 | 0.13 | 59 | 1.00x |
| lib-stage2-lean-u1 | 0.028 | 0.030 | 0.12 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.030* | *0.063* | *0.11* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.030** | 0.063 | 0.10 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.063* | *0.11* | *59* | *1.00x* |
| bq-expand | 0.097 | 0.101 | 0.43 | 46 | 1.08x |
| *bq-expand-aa-adjacent* | *0.097* | *0.101* | *0.45* | *46* | *1.08x* |
| *bq-expand-aa-distant* | *0.097* | *0.101* | *0.06* | *46* | *1.08x* |
| lib-stage1 | 0.106 | 1.219 | 0.21 | 55 | 1.19x |
| list (baseline) | 1.000 | 1.000 | 2.64 | 18 | 19.27x |
| *list-aa-adjacent* | *1.030* | *1.046* | *0.41* | *18* | *19.27x* |
| *list-aa-distant* | *1.034* | *1.062* | *0.55* | *18* | *19.27x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0336, worst cell 6.17% on `runs-16384`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9999 on a worst cell of 0.47% on `runs-65536`, its interval covering 1. The in-situ term reads 1.0293, 1.0214 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0324, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h47m6s, peak 563 MiB in use, 247 MiB max residency; the reader reads 39 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 37.5 ms per call raw, 36.4 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.063/0.052/0.045/0.043/0.037/0.033/0.028/0.028/0.028/0.027/0.027/0.027/0.027/0.029

**Across the halves:** 7 of the 20 arms are faster on this half and 13 slower, at a geomean of 1.0006, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.9866 to `list-aa-distant` at 1.0074, with `list` itself at 1.0068.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.063, tiers at 1.00x, 1.08x and 19.27x --- and `lib-stage2-lean` leads outside the family at 0.027, 0.8053 paired against `mut-odo-vecdims` but ahead on only 7 of 14 shapes at sign p 1, a 19.47% margin against this class's 3.36% floor, the widest of the ten and the one place a margin has to clear a bar rather than a hairline. **The ordered list's consumers are decided here.** Registration (11) reads `liblist-stage3-sum` over `liblist-stage1-sum` at **0.3631 and 0.3750** against a registered 0.4 within 15%, and `liblist-stage4-sum` over `liblist-stage2-sum` at **0.4951 and 0.5177** against 0.63 within 25%, so the lazy odometer's fold beats master's slice recursion and the port's base-offset table alike in logical order, as Run 27 read it in address order. The lean fill's own margin over the shipped leaf is the widest anywhere, registration (14) reading **0.2316 and 0.2357**. Beside them registration (12) reads base's fold at 0.8349 and 0.8415 and registration (8) the hand-written loop at 1.3166 and 1.3055. Across the halves 7 of the 20 arms are faster on the basis and 13 slower.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes: `flip-whole-square` (`l` 1798281, `sInner` 1341), `flip-last-c32` (`l` 165888, `sInner` 3), `flip-last-rows` (`l` 1800000, `sInner` 96), and two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed --- `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. One more landed 2026-09-09, `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which is `runs-96`'s construction under a `flip` name: it is the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two. It sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.39* | *70* | *1.03x* |
| liblist-stage1-sum | -- | -- | 0.16 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.17 | 90 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.10 | 92 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.08 | 92 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.12 | 82 | 1.00x |
| libunord-stage2-sum | -- | -- | 0.04 | 92 | 0.10x |
| libunord-stage3-sum | -- | -- | 0.06 | 90 | 0.50x |
| libunord-stage4-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage5-sum | -- | -- | 0.01 | 97 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage8-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 97 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *93* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.025 | 0.053 | 0.33 | 81 | 1.00x |
| lib-stage2-lean | 0.026 | 0.051 | 0.28 | 84 | 1.00x |
| lib-stage2-lean-u1 | 0.027 | 0.054 | 0.33 | 83 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.027 | 0.047 | 0.32 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.029* | *0.047* | *0.32* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.029 | 0.048 | 0.12 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.029* | *0.048* | *0.10* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.029 | 0.050 | 0.33 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.030 | 0.049 | 0.24 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.047 | 0.29 | 80 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.058* | *0.09* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.032** | 0.061 | 0.13 | 76 | 1.00x |
| *mut-odo-vecdims-aa* | *0.032* | *0.058* | *0.12* | *77* | *1.00x* |
| lib-stage1 | 0.038 | 0.054 | 0.38 | 80 | 1.00x |
| bq-expand | 0.102 | 0.132 | 0.44 | 64 | 1.03x |
| *bq-expand-aa-adjacent* | *0.102* | *0.132* | *0.45* | *64* | *1.03x* |
| *bq-expand-aa-distant* | *0.103* | *0.133* | *0.16* | *64* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.77 | 33 | 19.15x |
| *list-aa-distant* | *1.005* | *1.022* | *0.72* | *32* | *19.15x* |
| *list-aa-adjacent* | *1.007* | *1.027* | *0.48* | *33* | *19.15x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0071, worst cell 2.67% on `flip-last-rows`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9995 on a worst cell of 0.47% on `flip-fwd-rows96`, its interval covering 1. The in-situ term reads 1.0295, 1.0178 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0068, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h20m14s, peak 209 MiB in use, 76 MiB max residency; the reader reads 39 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 27.9 ms per call raw, 26.8 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.027/0.027/0.061/0.056/0.029/0.029

**Across the halves:** 13 of the 20 arms are faster on this half and 7 slower, at a geomean of 0.9980, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9725 to `lib-stage2-lean` at 1.0272, with `list` itself at 0.9960.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.061, tiers at 1.00x, 1.03x and 19.15x --- and `lib-stage2-lean` leads outside the family at 0.026, 0.7664 paired against `mut-odo-vecdims` and ahead on 5 of 6 shapes, a 23.36% margin against this class's 0.71% floor. The column and the paired reading part on this class and on `rev`, where `lib-stage1` over `mut-odo-vecdims-add-in-leaf-u2-last` reads 1.0282 paired against a published 0.9972: the table puts the family's ceiling ahead at 0.025 against 0.026, while `lib-stage2-lean` over `mut-odo-vecdims-add-in-leaf-u1-ptr` reads 0.9368 paired --- the two statistics are a winsorized geomean of per-shape ratios and a paired geomean, and six shapes are few enough for them to disagree. **What the class gained this run is `flip-fwd-rows96`**, and registration (16) is what it was for: `mut-odo-vecdims` on `flip-last-rows` over the same arm on the new forward view reads **2.1892 and 2.1252**, inside the registered 1.8-to-2.3 band, where Run 25 read 2.120 and 2.073 of the same quantity across two processes over two baselines. So the class's own finding --- a reversed innermost axis costing this fill about twice the forward run at the same length --- survives being read inside one process, and the cross-process reading it replaces carried no term the two processes contributed. Across the halves 13 of the 20 arms are faster on the basis and 7 slower.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.11* | *104* | *1.03x* |
| liblist-stage1-sum | -- | -- | 0.11 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.14 | 124 | 0.31x |
| liblist-stage3-sum | -- | -- | 0.04 | 128 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.14 | 113 | 0.42x |
| libunord-stage2-sum | -- | -- | 0.16 | 123 | 0.31x |
| libunord-stage3-sum | -- | -- | 0.16 | 111 | 1.00x |
| libunord-stage4-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage5-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.04 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 128 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage8-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.03 | 128 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.17* | *130* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *122* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.022 | 0.025 | 0.14 | 112 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.022 | 0.028 | 0.11 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.024 | 0.027 | 0.12 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.024 | 0.028 | 0.12 | 111 | 1.00x |
| lib-stage2-lean | 0.024 | 0.028 | 0.10 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.028* | *0.13* | *111* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.028* | *0.12* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.028 | 0.12 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.029 | 0.09 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.029 | 0.12 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.026* | *0.033* | *0.09* | *111* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.026* | *0.033* | *0.09* | *111* | *1.00x* |
| **mut-odo-vecdims** | **0.026** | 0.033 | 0.10 | 111 | 1.00x |
| lib-stage1 | 0.057 | 0.065 | 0.12 | 103 | 1.42x |
| *bq-expand-aa-adjacent* | *0.094* | *0.095* | *0.11* | *97* | *1.03x* |
| bq-expand | 0.094 | 0.095 | 0.14 | 97 | 1.03x |
| *bq-expand-aa-distant* | *0.094* | *0.095* | *0.12* | *97* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.38 | 56 | 19.19x |
| *list-aa-distant* | *1.000* | *1.005* | *0.30* | *56* | *19.19x* |
| *list-aa-adjacent* | *1.001* | *1.007* | *0.31* | *56* | *19.19x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9960, worst cell 1.31% on `block-run64-gap64`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9989 on a worst cell of 0.52% on `block-run64-gap1`, its interval missing 1. The in-situ term reads 1.0227, 1.0153 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9984, which the correction amplifies by 2.49x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m54s, peak 136 MiB in use, 58 MiB max residency; the reader reads 39 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.11 ms per call raw, 3.95 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.022/0.028/0.033/0.027/0.022

**Across the halves:** 17 of the 20 arms are faster on this half and 3 slower, at a geomean of 0.9930, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9444 to `lib-stage2-lean-u1` at 1.0110, with `list` itself at 0.9921. **The baseline moved 0.79% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.033, tiers at 1.00x, 1.03x and 19.19x --- and `lib-stage2-lean` leads outside the family at 0.024, 0.9249 paired against `mut-odo-vecdims` and ahead on 5 of 5 shapes, a 7.51% margin against this class's 0.40% floor. The family's ceiling is ahead of it at 0.022, so this is a class the fill still owns. **What it settles is the second half of registration (11)**: `liblist-stage3-sum` over `liblist-stage1-sum` reads **0.4563 and 0.4605** and `liblist-stage4-sum` over `liblist-stage2-sum` **0.7831 and 0.8018**, both inside their registered spans, which with `runs` is the whole of that item's positive evidence. Registration (10) reads the fused list against the fill it replaces at **0.4457 and 0.4460**, the largest such lead in any class, and registration (14) the lean fill over the shipped leaf at 0.4200 and 0.4257. Across the halves 17 of the 20 arms are faster on the basis and 3 slower.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.19* | *223* | *1.34x* |
| liblist-stage1-sum | -- | -- | 0.22 | 228 | 1.61x |
| liblist-stage2-sum | -- | -- | 0.27 | 230 | 1.49x |
| liblist-stage3-sum | -- | -- | 0.30 | 228 | 1.63x |
| liblist-stage4-sum | -- | -- | 0.24 | 231 | 1.45x |
| libunord-stage1-sum | -- | -- | 0.21 | 222 | 2.06x |
| libunord-stage2-sum | -- | -- | 0.23 | 226 | 2.04x |
| libunord-stage3-sum | -- | -- | 0.25 | 221 | 1.84x |
| libunord-stage4-sum | -- | -- | 0.20 | 229 | 1.94x |
| libunord-stage5-sum | -- | -- | 0.30 | 228 | 1.84x |
| libunord-stage6-list-sum | -- | -- | 0.21 | 231 | 1.24x |
| libunord-stage6-loop-sum | -- | -- | 0.29 | 232 | 1.24x |
| libunord-stage6-sum | -- | -- | 0.25 | 231 | 1.26x |
| libunord-stage7-sum | -- | -- | 0.33 | 231 | 1.26x |
| libunord-stage8-sum | -- | -- | 0.25 | 228 | 1.58x |
| libunord-stage9-sum | -- | -- | 0.28 | 228 | 1.07x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.28* | *239* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.05* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.08* | *250* | *0.01x* |
| lib-stage2-lean | 0.057 | 0.104 | 0.28 | 228 | 1.40x |
| lib-stage2-lean-u1 | 0.057 | 0.105 | 0.19 | 228 | 1.40x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.058* | *0.072* | *0.32* | *229* | *1.27x* |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.058 | 0.073 | 0.27 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.059 | 0.072 | 0.27 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.059 | 0.073 | 0.32 | 229 | 1.27x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.059* | *0.073* | *0.32* | *229* | *1.27x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.060 | 0.072 | 0.32 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.065 | 0.081 | 0.25 | 228 | 1.44x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.065 | 0.082 | 0.25 | 228 | 1.44x |
| *mut-odo-vecdims-aa-distant* | *0.066* | *0.094* | *0.35* | *229* | *1.27x* |
| *mut-odo-vecdims-aa* | *0.066* | *0.094* | *0.38* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.066** | 0.094 | 0.30 | 229 | 1.27x |
| lib-stage1 | 0.094 | 0.119 | 0.28 | 221 | 2.30x |
| *bq-expand-aa-adjacent* | *0.134* | *0.165* | *0.16* | *218* | *1.34x* |
| *bq-expand-aa-distant* | *0.134* | *0.167* | *0.17* | *218* | *1.34x* |
| bq-expand | 0.134 | 0.165 | 0.16 | 218 | 1.34x |
| list (baseline) | 1.000 | 1.000 | 0.31 | 182 | 19.50x |
| *list-aa-adjacent* | *1.001* | *1.004* | *0.27* | *182* | *19.50x* |
| *list-aa-distant* | *1.001* | *1.011* | *0.23* | *182* | *19.50x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9941, worst cell 1.58% on `small-patch-r5`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9996 on a worst cell of 0.11% on `small-bcast32`, its interval covering 1. The in-situ term reads 0.9975, 0.9889 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9962, which the correction amplifies by 1.61x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m54s, peak 135 MiB in use, 54 MiB max residency; the reader reads 39 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 5.98 us per call raw, 5.75 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.046/0.083/0.056/0.064/0.094

**Across the halves:** 8 of the 20 arms are faster on this half and 12 slower, at a geomean of 1.0027, from `bq-expand-aa-adjacent` at 0.9824 to `mut-odo-vecdims-add-in-leaf-u2-ptr` at 1.0344, with `list` itself at 1.0136. **The baseline moved 1.36% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold, and this is the one class where property 3's level clause is read off a `mut-odo-vecdims` that is not 1.00x --- `worst` 0.094 and tiers at 1.27x, 1.34x and 19.50x, the class having sat outside that clause since 2026-09-07 because its calls are short enough for the per-call constants to show. `lib-stage2-lean` leads outside the family at 0.057, 0.8557 paired against `mut-odo-vecdims` but ahead on only 1 of 5 shapes, a 14.43% margin against this class's 0.59% floor. **This class is where the first canonicalization is priced and where two registrations die.** Registration (1) holds here and only here: stage six's consumer over stage five's reads **0.9073 and 0.9363** against a registered 0.92 within 4%, and four of the item's five per-view clauses land --- ahead on `small-row96` (0.8515, 0.8700), `small-patch-k5` (0.7350, 0.7755) and `small-bcast32` (0.8756, 0.8868), behind on `small-flat64` (1.1913, 1.2477) where the sort collapses the rank. The fifth misses: `small-patch-r5` was registered INSIDE the floor and reads 0.9418 and 0.9640. Registration (6) is killed on `small-patch-r5`, stage eight reading **1.2593 and 1.2152** behind stage seven where it was registered ahead, and registration (11) is killed on the class, its two dispatches parting at **0.8831 and 0.8891**. All five views also differ in allocation between stage six and stage five, which is registration (3)'s kill. Across the halves 8 of the 20 arms are faster on the basis and 12 slower.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.35* | *88* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.33 | 96 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.34 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.36 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.34 | 96 | 1.00x |
| libunord-stage2-sum | -- | -- | 0.58 | 98 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.51 | 98 | 1.00x |
| libunord-stage4-sum | -- | -- | 0.47 | 98 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.45 | 98 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage8-sum | -- | -- | 0.31 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.03 | 109 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.20* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *105* | *0.00x* |
| lib-stage2-lean | 0.017 | 0.019 | 0.38 | 98 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.017 | 0.018 | 0.35 | 98 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.018 | 0.018 | 0.40 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.018 | 0.019 | 0.27 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.019 | 0.021 | 0.34 | 96 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.020* | *0.021* | *0.30* | *96* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.020 | 0.021 | 0.37 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.021 | 0.27 | 96 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.020* | *0.021* | *0.26* | *96* | *1.00x* |
| lib-stage1 | 0.020 | 0.021 | 0.47 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.021 | 0.021 | 0.28 | 96 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.032* | *0.27* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.032 | 0.20 | 94 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.032* | *0.31* | *94* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.095* | *0.096* | *0.41* | *80* | *1.21x* |
| bq-expand | 0.095 | 0.096 | 0.38 | 80 | 1.21x |
| *bq-expand-aa-distant* | *0.095* | *0.097* | *0.23* | *80* | *1.21x* |
| *list-aa-distant* | *0.999* | *1.003* | *0.89* | *46* | *19.87x* |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.71* | *46* | *19.87x* |
| list (baseline) | 1.000 | 1.000 | 0.77 | 46 | 19.87x |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa` at 1.0056, worst cell 2.01% on `compose-zero-mid`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9985 on a worst cell of 0.56% on `compose-rev-bcast`, its interval missing 1. The in-situ term reads 1.0120, 1.0117 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0019, which the correction amplifies by 2.95x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m38s, peak 126 MiB in use, 33 MiB max residency; the reader reads 39 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 27.6 ms per call raw, 26.5 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.032/0.032/0.024/0.022

**Across the halves:** 5 of the 20 arms are faster on this half and 15 slower, at a geomean of 1.0060, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.9943 to `mut-odo-vecdims-aa-distant` at 1.0336, with `list` itself at 1.0019.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.032, the joint lowest of the ten with `scaled`'s, and tiers at 1.00x, 1.21x and 19.87x --- and `lib-stage2-lean` leads outside the family at 0.017, 0.6162 paired against `mut-odo-vecdims` and ahead on 4 of 4 shapes, a 38.38% margin against this class's 0.56% floor. It is level with the family's own ceiling rather than ahead of it: `mut-odo-vecdims-add-in-leaf-u2-ptr` also reads 0.017, and the two are 0.9995 paired, INSIDE this class's floor, so the emphasis in the summary table above marks a nominal ordering and not a lead. **What this class contributes is registration (7)'s sharpest agreement with its probe**: `compose-zero-mid`, 20000 runs of 90, reads stage nine's consumer over stage six's at **0.4600 and 0.4601** where the flat-walker probe read 0.46, and `compose-rev-bcast` and `compose-slice-bcast` at 0.7101 and 0.7102 on the basis against a probe's 0.71 --- while `compose-scalar`, every stride 0 and so nothing for the reordering to move, reads 0.9996 and 1.0038 as registered. Across the halves 5 of the 20 arms are faster on the basis and 15 slower.


## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 28's halves differ in the compiler and in the boot libraries that come with it, and in nothing else.** One source, `Main.hs` at `bdf06c8`, one shim at `b3a1aca` and one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of the assembler, both halves built `-fspec-constr -fobject-determinism` and run under `WILDLOG=1 SATURATE=1`. The basis is ghc-9.12.4 through `cabal.project` and its freeze; the control is the in-tree GHC HEAD stage1 under `~/r/horde-ad/ghc` through `cabal.project.ghead` and its own freeze at the same index-state. The stage1 still reports **10.1.20260803**, unmoved since Run 24, so this run's control half and Runs 24 to 27's are the same compiler. What the binaries carry, read back from them rather than off the project file: `ghc-internal-9.1204.0` on the basis and `ghc-internal-10.100.0` on the control.

**The sequence was launched once and ran to the end in ONE window, and no population was rerun**, 2026-09-11T01:02:39 to 2026-09-11T09:42:43, twenty-two processes with no hole between one finishing and the next starting. The gate's four ran before them from 00:29:20 and the four rider stages after them to 09:55:12; the counted work followed on a box already handed back, 09:55:55 to 10:38:12. Every process exited 0 at the bench count its roster holds --- 741 twice, 546 twice, 312 twice, 234 four times, 195 four times, 156 four times and 117 four times --- and the driver's own log recorded no complaint about any of them. That is the main set twice and **20 class processes**, one per class per half. The per-sample instrument reads the foreign CPU during every bench as a ratio of one core, and the highest anywhere is **0.21** on `ghead-main`, below the 0.25 at which the reader calls an intrusion, so nothing here was measured beside something else. **The machine is the same desktop as every run since Run 10, Zen 3, a Ryzen 7 5800X**, and its BIOS has not been touched since the change before Run 27 --- which is why this run reads inside that run's era rather than opening a fourth, and why the figures here are this box's and portable to no other.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run28-g912`, md5 `1804213242a48324a107c595e49e5608`, and `run28-ghead`, md5 `91ad04ad34eecbc7428e2578c4e626e2`, with `.text` at **20766917** bytes on the basis and **20911935** on the control --- they do not agree, two compilers being this pair's variable. Both bake `-A32m -I0 -T -M8G`, read back by `+RTS --info` and not by `strings`, and each carries one `@@wild` and one `@@saturate` instrument. `check` agreed on every shape on both halves and the two logs are byte-identical, on the first build of this pair. There is no repetition reading and none was owed: `Main.hs` moved from `bb6d113` to `bdf06c8`, so no md5 here can reproduce an earlier binary.

**The roster and the source moved, which is what would put a layout term in the cross-run column, and this run's fills say the layout did not.** The tracked 28-byte loops were read at the build against `run27-g912`, the previous build of this recipe, with `--delta`: **every mod-64 offset preserved**, the two groups at [0, 0, 0, 24, 0, 4] and [0, 0], and **no address surviving to the byte**, three displacements in the first group and two in the second. So thirteen timed arms landing and nine leaving moved the code without moving where any tracked fill sits in its cache line --- and this time the cross-run column agrees, every one of the 18 shared arms reading inside 1.4% of 1 with the gate's machine check saying the same of `list` alone. Within the pair, `--library` reads **11.3% of the 141 library self-loops at the same offset in line and 73.8% in the same straddle state**, inside the tenth-to-a-quarter band this README gives a pair varying a compiler.

**The three main-set anchors** read **5.80 us** on `cnn-slice-c32`, **3.36 ms** on `cnn-L2-24x24-c32` and **36.4 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, against **5.73 us**, **3.32 ms** and **36.2 ms** on the control --- the control faster on all three, by 1.29%, 1.02% and 0.70%, the same direction as `list`'s own main-set move of 0.46%, which is the basis slower once every shape is taken together. They are absolutes and so the first thing a later run checks when a whole column moves; the gate's machine check is the same question asked of nineteen shapes at once, and it did not fire.

| shape | `l` | `list`, per call | net | HEAD, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 5.97 us | 5.80 us | 5.73 us |
| `cnn-L2-24x24-c32` | 165888 | 3.46 ms | 3.36 ms | 3.32 ms |
| `stretch-wide-2xM` | 1800000 | 37.5 ms | 36.4 ms | 36.2 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at 37.5 ms raw and 36.4 ms net, against Run 27's 37.9 ms and 36.9 ms on a class whose population has not moved for five runs --- about a percent lower, with no box move behind it this time and no second window --- and its own floor is 3.36% here, the widest of the ten. Three class populations gained views this run --- `bcast` 3 to 6, `window` 6 to 8 and `flip` 5 to 6 --- and ONE of the six IS an anchor: `flip`'s moved from Run 27's `flip-last-rows` to `flip-fwd-rows96`, so that class's anchor reads against no earlier run's, while the other nine classes' anchors are on the shapes they were on. Read a class anchor against the same class's anchor in an earlier run and against nothing else, and against that class's own floor rather than the main set's.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **0.9998** on the basis and **0.9998** on the HEAD half, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0345** and **1.0832** on the basis and **1.0265** and **1.0705** on HEAD for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent under the term it stands for, by the same few percent on both compilers, which is where it has run since Run 17. What the correction cannot do is rescue a cell it is larger than, and this run has NO such cell on either half, where Run 27's write-up recorded seventy on the basis and forty-three on HEAD -- counts taken before the 2026-09-10 `no_net` ruling, which today's reader gives as 25 and 8.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-09, before the run, on Run 27's pair --- ghc-9.12.4 as the basis against the same in-tree HEAD stage1, `10.1.20260803`, both under `LOOP_DEADSPOT=1`, the same recipe and the same shim; the pair is the request's, Run 27 having closed the compiler question, and this run's question is its roster --- over Run 27's roster plus eleven arms: libunord-stage6, stage five with the first canonicalization dropped; three reorderings of the run, each one change over stage six, libunord-stage7 the tie-break, libunord-stage8 the longest chain and libunord-stage9 the zero-stride axes outermost; the four reducing consumers, `-sum` each; `libunord-stage6-loop-sum`, the fold entry point over stage six; `libunord-stage6-list-sum`, base's `sum` over stage six's list as a library user writes it, added later the same day, item (12); and `libunord-stage3-sum`, the ceiling's consumer --- and, by the ruling of the same day, less thirteen and plus four: every arm that concatenates a list the library would fold, liblist-stage1 to `-stage4` and libunord-stage1 to `-stage9`, is checked and not timed, a list concatenated being the API's failure mode and `toVectorT` the fill for it, and the ordered list's consumers `liblist-stage1-sum` to `-stage4-sum` land in their place, so the entry points are timed only as they are used --- and plus two more, the shipped fill's own A/A copies, item (15). 39 timed arms over 19 main-set shapes, 741 benches, and 58 class views of ten classes, six of them new: `window-64x64-c16-k3` and `window-32x32-c64-k3`, patch views with channels, the shape the tie-break exists for; `bcast-src8`, `bcast-src64` and `bcast-src512`, an 8-, 64- and 512-element source each repeated to 1.8 million elements, the ladder on which stage nine's repeated slice meets the fill; and `flip-fwd-rows96`, item (16)'s forward control. Eight changes of code: the eleven arms, each landing beside stage five's, and the thirteen retired with the four ordered consumers in their place; the fusion overhaul of 2026-09-09, which touches arms Run 27 timed --- `lazyRuns` in build form under libunord-stage4, `-stage5`, liblist-stage3 and `-stage4`, and every `-sum` consumer in fold form, the lazy stages' through one shared fused loop, and, later that day, `lazyRuns` one flat loop in build form with the route's case inside --- so their Run 27 figures are not this roster's, and README's open list carries the before and after, its `ANSWERED` fusion entry among them, this registration having been moved out of that list at post-run step 5 so that `above` and `below` no longer name anything here; and the ruling of the same day that a port's Fill arm no longer concatenates a one-element list, `toVectorT`'s own shape, so liblist-stage1 to `-stage4` and libunord-stage1 and `-stage2` read 1.00x allocation and pay no result copy on every view the library fills once, where Run 27 read 2.00x --- item (9)'s premise, and the reason the lazy stages' dispatch became a value read by four shared readers; and two changes to `fillStage2`, the branch's fill under `lib-stage2-lean`, under every lazy stage's fill route and under `liblist-stage2-sum` where its list falls to a fill, and under the parked lib-stage2, lib-stage2-concat and lib-stage2-disp, so no arm copies a zero-stride level once per block as the branch's fill does: a doubling block copy for a zero-stride level, shared with `fillStage2U1`, and the broadcast run unrolled by two, not shared --- items (13) and (14), and the reason Run 27's `bcast` and `bcastmid` figures for those arms are not this roster's; and two more of 2026-09-09, the shipped fill's own A/A copies, `mut-odo-vecdims-add-in-leaf-u2-aa` and `-aa-distant`, item (15), and the forward control `flip-fwd-rows96`, item (16); beside all of them `check` gained a class it alone reads, edge, three broadcast-mid views at the extents a doubling copy can get wrong, 2, 3 and 5, none of which any timed view has. Two things are known before the run and are the premise rather than predictions. A `check`-mode comparison of 2026-09-09 read stage five's sorted canonical dims and the sort-first form's equal on all 91 checked views, so the two arms differ from stage five in dispatch cost alone; the comparison was removed with its reading, `check` holding the arms to the reference as it holds every arm. And a filtered probe the same day on the 9.12.4 build, the pair's basis --- criterion means over one process, stage four, five and six alone on `small` and `compose`, not a run --- read, before the overhaul and with its change reaching both arms of each pair alike, libunord-stage6 over libunord-stage5 at 0.960 on `small` and 0.999 on `compose`, the consumers at 0.922 and 0.997, with `small-patch-k5` at 0.786 and `small-flat64` at 1.239: the pass saved where it merges nothing, [6, 5, 5] on strides [25, 1, 5], against the longer sort where it collapsed the rank, [4, 1, 64] on [64, 0, 1], rank one before the sort under stage five and three axes sorted under stage six. And the fusion probe of 2026-09-09, whose decision is README's `ANSWERED` entry *Does list fusion pay in this harness* and whose account is this: Run 27 read the lazy candidates' consumer at 145 to 183 bytes a run and master's at 24 to 56, so the question was whether a fusible producer and consumer would take the list's cost out. Four probe arms over stage six crossed two producers, `lazyRuns` as it was and in build form, with two consumers, the harness's recursive sum and `foldl'`, on `window`, `bcast`, `compose` and `small`: no pair fused, every consumer reading 145 bytes a run on the old producer and 161 on the build one whichever its own form. The real Core said why: `foldl'` partially applied to its step and to zero is a closed expression, full laziness floats it to the top level, and the rule that fuses `foldr` with build never sees the two together; an isolated module of the same shapes fused, which is why the dump of the real one was needed. Two shapes keep them together and fused in the harness: the list returned from each branch of an inlined route with the fold applied to the call, master's own shape, and the fold taken into the route and applied at the producer. A third arm folded as a strict loop over the levels with no list at all. Over every class and the main set at a three-second budget, geomeans of each arm over stage six's consumer, time then allocation, on the classes where the list route fires: the fused list 0.79 and 0.72 on `window`, 0.90 and 0.73 on `runs`, 0.85 and 0.72 on `block`, 0.94 and 0.83 on `flip`, 0.93 and 0.89 on `small`; the loop 0.55 and 0.001 on `window`, 0.77 and 0.001 on `runs`, 0.61 and 0.004 on `block`, 0.82 and 0.11 on `flip`, 0.87 and 0.72 on `small`; a tie within a percent on the main set and on the classes that fill. Per run, medians over the views with runs of nine or fewer: unfused 18.7 ns and 145 bytes, fused 14.4 and 104, the loop 10.3 and none. What the fused form paid per run under base's `foldl'` was a thunk for the accumulator handed to an unknown continuation, its boxed result and a partial application for the next step: fusion lets the producer call the consumer's step with the rest of the list as an argument, a left fold's step returns a function of the accumulator, so the rest of an odometer level, resume at the next index, has to be built as a value before the step can run, and the accumulator crosses it as an argument to a function the compiler cannot see, hence boxed and passed lazily. A hand-written `foldr` with a strict application, tried first, did not fuse at all on 9.12.4 and read the unfused 145 bytes a run, so the loop was `foldl'` --- and it still is, the three objects having gone later the same day at the producer. A Core dump located them: the accumulator is handed lazily because the continuation at a level's exit is the level walker's `rest`, lambda-bound and so unknown, and demand is the meet of the run step and the exit. Two fixes were measured. A fold forcing the new accumulator before the continuation sees it, written as base writes its own, fused and read none a run and 4.3 ns on `runs-9` where base's read 80 bytes and 10.7; but it reaches only a fold the library writes, and base's `sum` over the same list, the consumer a user writes, still read 96 bytes a run. `lazyRunsFB` as one flat loop --- the innermost outer level a counter and a cursor, the levels above an odometer touched only on a carry, every continuation `go`, `carry` or `nil` and all of them known --- reaches every fold: base's `sum` over the list reads none a run and 2.6 ns on `runs-9`, the harness's own fold none and 3.7, the strict loop with no list 5.9, and the forcing fold over the flat walker read the same as base's to a hundredth of a nanosecond, so it is not kept and `sumLazyRuns` is base's `foldl'`. The readings are under `closure-probe/` beside `fusion-probe/`, and items (5), (8), (10) and (11) below are re-based on a filtered probe of the flat-walker build at a two-second budget, one process a class, item (12) on a probe of its own the same hour and item (7) on one of the fills build, whose fill its denominator takes on every zero-stride view, their JSONs in the same directory. GHC HEAD's Core of the old loop, `10.1.20260803` through `cabal.project.ghead`, had the same three objects in the same places; the flat walker's HEAD Core is unread, and Run 28's HEAD half is where it is first met. The overhaul's first form inlined each stage's list function into its consumer, and two of the six copies of one identical loop came out 8 bytes and several ns a run dearer than the others, stage nine's by half on the k3 window --- per-copy code generation, which a pair of stages would have read as a design's cost; so the loop is compiled once, `sumLazyRuns`, and reached through each stage's `Route`, at which every consumer reads 88 bytes a run and the same time on the same dims, `fusion-probe/log-probe-fusion-final.json`. Every probe build was the 9.12.4 basis; the fused code meets GHC HEAD for the first time in Run 28. Last, two odometer tricks from the fills, each as a producer under stage six's route and the fused consumer: the leaf fused, the innermost outer level consing its slices itself, read 9.3 ns and 80 bytes a run on the k3 window against 12.9 and 88; the levels as unboxed tables indexed by level, on top of it, 7.8 ns, and the fused list then ahead of the loop arm as it stood, whose odometer walked its levels as lists too. RULED 2026-09-09: the tables are too much code for how little the entry point is used and how little any of this moves most shapes; the fused leaf is borderline and taken, into `lazyRuns` and into the loop arm's odometer alike, so item (8) reads the interface and not the odometer. With it the fused list reads 8.8 ns and 80 bytes a run on the k3 window, the loop 5.1 and none, its leaf now calling the step with the accumulator unboxed, and the Fill arms, concatenating the same producer, 24.7 ns and 184 bytes a run against 28.8 and 169: faster, and 16 bytes a run more, the one figure the leaf moved the wrong way. Master's list under an inlined fold gained nothing, 0.997 in time and 1.000 in allocation, and the port's table list gained time and no allocation, 0.87 on `block`. The build form costs a consumer that cannot fuse when the consumer is inlined beside it: the probe's Fill arms, `VS.concat` under them, read 16 bytes and about 3 ns a run more, 1.025 and 1.077 on `window`; compiled once as `concatLazyRuns` the Fill arms read what they read before, 169 bytes a run on the k3 window, so the fills pay nothing for the form. The probe's JSONs stay under `fusion-probe/` beside this file until Run 28 is written up, out of the reader's corpus, which is every JSON in this directory and which a filtered run fails by design. Each item names its populations and carries a prediction and a kill condition.

(1) *The dispatch cost of the first canonicalization.* On `small`, both halves: `predict: pair libunord-stage6-sum libunord-stage5-sum 0.92 within 4%`; the Fill pair's 0.96 within 3%, registered before the retirement, is read on no run. Read per view beside it, by hand, the consumers: stage six ahead past the floor on `small-row96`, `small-patch-k5` and `small-bcast32`, behind past the floor on `small-flat64`, and inside the floor on `small-patch-r5`. Killed by stage six's consumer behind stage five's past the floor on both halves on the class, which would say the longer sort costs more than the pass it saves; or by `small-flat64` inside the floor on both halves, which would say the sort's length is not what the probe read.

(2) *The tie everywhere else.* On `rev`, `bcast`, `bcastmid`, `window`, `scaled`, `runs`, `block` and `compose`, both halves: `predict: pair libunord-stage6-sum libunord-stage5-sum 1.0`, the two routes being the same code past the dispatch on every view and the probe reading 0.997 on `compose`. The main set and `flip` are read raw by hand, the unordered arms' cells being their own forcing term there, as Run 27's items (6) and (7) read them. Killed by the pair past the floor on both halves on any of the eight, which would say the dispatch is visible at a call of tens of microseconds or that the routes are not the same code after all.

(3) *Allocation.* `libunord-stage6-sum` reads the same allocation multiple as `libunord-stage5-sum` on every view, off the allocation column, neither dispatch allocating a result-sized buffer; killed by any view where the two multiples differ at the column's own precision.

(4) *The tie-break where it cannot fire, stage seven over stage six.* Stage seven puts the larger extent innermost on equal absolute strides, so the run is the longest unit-stride axis. Equal absolute strides alias, so the tie exists only in a self-overlapping view: of the timed views the six unstrided `window` views and `small-patch-r5`, item (5)'s, and no other. On `rev`, `bcast`, `bcastmid`, `scaled`, `runs`, `block` and `compose`, both halves: `predict: pair libunord-stage7-sum libunord-stage6-sum 1.0`; the same span prints a figure and no verdict on `window` and `small`, and the main set and `flip` are read raw by hand as item (2) reads them. Killed by the pair past the floor on both halves on any of the seven, which would say the tie-break moved a route it cannot reach.

(5) *The tie-break where it fires.* On `window` the runs go from the kernel's width to the output's: 3 to 222 on `-k3`, 7 to 122 on `-k7`, 9 to 56 on `-k1x9` and 5 to 24 on `-k5`, with `-s2` unmoved, its stride-2 axis unable to run, and `-d2` unmoved, its long axis already alone at stride 1; the two channel views added for this run, `window-64x64-c16-k3` and `window-32x32-c64-k3`, go from 3 to 62 and from 3 to 30, the channel axis standing untied between the tied pairs. On the flat-walker probe stage seven's consumer over stage six's reads 0.64 on `-k3`, 0.96 on `-k7`, 0.96 on `-k1x9`, 0.59 on `-k5`, 1.00 on `-s2` and `-d2`, 0.49 on `window-64x64-c16-k3` and 0.43 on `window-32x32-c64-k3`, 0.72 as the class geomean --- the cheaper the run, the less the tie-break saves, and the run's cost has fallen twice since the account of nine nanoseconds a run this item first carried, at the leaf and then at the walker. As a class span on `window`, both halves: `predict: pair libunord-stage7-sum libunord-stage6-sum 0.7 within 15%`, printing a figure and no verdict elsewhere. `small-patch-r5`, runs of 16 for 8 on a 256-element view, is read by hand: stage seven ahead past the floor. Killed by stage seven's consumer not ahead of stage six's past the floor on both halves on any of the six unstrided views, which would say the per-run cost is not what the dilated control read; by it off stage six's past the floor on both halves on `-s2` or `-d2`, where the route is unchanged; or by it behind on `small-patch-r5` on both halves.

(6) *The longest chain, stage eight over stage six and over stage seven.* Stage eight chooses the run as the longest contiguous one over every order of absorbing axes whose stride equals the run's length, leaves the other axes in stage six's order, and takes stage six's route where a stride is 0 or none is 1. Against stage six it moves what stage seven moves and `small-patch-r5` further, 16 for 8; against stage seven its run differs on `small-patch-r5` alone, an axis with the run's length sitting away from the run after sorting being impossible in an injective view, and its outer levels differ only where two of them tie on stride, the `window` views, where stage eight keeps stage six's order and stage seven the reverse, the run count moving on none. So on `rev`, `bcast`, `bcastmid`, `scaled`, `runs`, `block`, `window` and `compose`, both halves: `predict: pair libunord-stage8-sum libunord-stage7-sum 1.0`, and on `small` the class span prints a figure and no verdict, `small-patch-r5` read by hand: stage eight's consumer ahead of stage seven's past the floor, the runs halving on a call the per-run cost dominates. Killed by stage eight's consumer off stage seven's past the floor on both halves on any view but `small-patch-r5`, which would say the chain moved a route the argument says it cannot; or by it behind on `small-patch-r5` on both halves.

(7) *Zero-stride axes outermost, stage nine over stage six.* Stage nine moves every zero-stride axis outermost on the list route, so a broadcast lists one real slice as many times as the axis is long where stage six falls to the fill; the fill route and every view with no zero stride are stage six's. On `rev`, `scaled`, `runs`, `block` and `window`, both halves: `predict: pair libunord-stage9-sum libunord-stage6-sum 1.0`, the main set and `flip` read raw by hand. Where a zero stride exists the reading is per view, by hand: stage nine ahead where the repeated slice is long --- the three older `bcast` views, 6400 elements eight times, 2000 elements 900 times and 900000 twice; `bcastmid-c32-cnn`, `bcastmid-primes` and `bcastmid-block150k`, the whole source as one slice of 5184, 2813 and 450000 elements repeated 32, 89 and 4 times, the sort having merged the transposed block with what stands outside the zero-stride axis; `compose-rev-bcast` and `compose-slice-bcast`, 6400 elements eight times --- the consumer skipping the result's write; and, since the walker stopped allocating a run, ahead on the short slices too: the fills-build probe reads stage nine's consumer over stage six's at 0.45 on `bcastmid-b200k`, a 9-element block repeated 200000 times, 0.97 on `small-bcast32`, 32 runs of eight and the one zero-stride view where the lead is slight, 0.46 on `compose-zero-mid`, 20000 runs of 90, and 0.58 to 0.70 on the long-slice `bcast`, `bcastmid` and `compose` views; `compose-scalar` unchanged, every stride 0. The `bcast` repeat ladder added for this run, `bcast-src8`, `bcast-src64` and `bcast-src512`, the same 1.8 million elements as 8 elements 225000 times, 64 elements 28125 times and 512 elements 3515 times, was added as the rungs between which the repeated slice would meet the fill, and the probe reads no crossover: 0.48, 0.46 and 0.62. Killed by stage nine's consumer behind stage six's past the floor on both halves on any zero-stride view, `bcast-src8` and `small-bcast32` above all, which would say a per-run cost the walker no longer pays is back; or by it off stage six's past the floor on both halves on any population with no zero stride. On `small`, whose four other views have no zero stride and take stage six's route, the probe reads stage nine's consumer 1.16 to 1.36 behind, its dispatch's extra pass on a call under a microsecond, so that class prints a figure and no verdict. Read beside it off the allocation column: stage nine's consumer allocates neither the result nor, since the walker went flat, the list's runs, so against stage six's 1.00x it reads near 0.00x on every zero-stride view whose call the per-call constants do not dominate, `bcast-src8` and `bcastmid-b200k` at 0.0001x and 0.0002x in the probe; a zero-stride view of thousands of runs reading above 0.01x would say a per-run allocation is back.

(8) *The fold entry point, `libunord-stage6-loop-sum` over `libunord-stage6-sum`.* The same dispatch and a fold written as a strict loop over the levels, no list between producer and consumer. Registered while the fused list paid a thunk, a box and a partial application per run, the fusion probe reading the two at 10 ns and no allocation against 14 ns and 88 bytes, and with the leaf fused 5.1 ns against 8.8 on the k3 window; the flat walker then took the three objects out and the order reversed: the flat-walker probe reads the loop over the fused list at 1.70 on `window`, 1.26 on `runs`, 1.06 on `block`, 1.02 on `flip` and 1.01 on `small`, the fused list at 3.7 ns a run on `runs-9` against the loop's 5.9. Why the hand-written loop is the slower of two allocation-free loops is not priced; a counts reading is what would. On `window`, both halves: `predict: pair libunord-stage6-loop-sum libunord-stage6-sum 1.7 within 15%`; on `runs`, `block`, `flip` and `small` the same span prints a figure and no verdict, read against the probe's geomeans by hand. On `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, both halves: `predict: pair libunord-stage6-loop-sum libunord-stage6-sum 1.0`, the two being the same code on a view that fills or is one block. Killed by the loop ahead of the fused list past the floor on both halves on `window` or `runs`, which would say the walker's known continuations did not reach the built binary or do not survive a full budget; or by the pair past the floor on both halves on any of the five fill classes. Read beside it off the allocation column: both consumers near 0.00x on every runs-route view, the list's per-run bytes gone with the thunk. What the arm cannot answer is the ruling's own ground, that `anyT` and `allT` stop at the first deciding slice, which a strict loop cannot; it prices what that laziness costs a reduction.

(9) *The ports without the copy --- WITHDRAWN 2026-09-09 with the arms.* Its subject was the copy the port Fill arms paid concatenating a one-element list, taken out that day; the same day's ruling then retired every Fill arm over a list, so the spans it registered, liblist-stage1 against `lib-stage1` and liblist-stage2 against `lib-stage2-lean` on the main set and the six list Fill arms at 1.00x on the fill classes, are read on no run. What stands is the change itself, held by `check` on every view, and the allocation column of the checked arms, which now reads what the library allocates.

(10) *The ceiling's consumer, `libunord-stage5-sum` over `libunord-stage3-sum`.* Stage three is stage five with its runs turned into fills, and its consumer sums the fill, so the pair is the fused list against the fill it replaces with nothing else different. On `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, both halves, where both fill or both return one block: `predict: pair libunord-stage5-sum libunord-stage3-sum 1.0`; on `runs`, `block`, `flip`, `window` and `small` the span prints a figure and no verdict, stage five's consumer ahead there by what the write and the list's per-run cost differ, Run 27 having read it at 0.45 of the vecdims fill on `runs`, and, since the walker went flat, ahead on the short runs too: the flat-walker probe reads it over stage three's on `window` at 0.86 on `-k3`, 0.87 on `-s2`, 0.71 on `-k5`, 0.88 on `window-64x64-c16-k3` and 0.96 on `window-32x32-c64-k3`, 0.54 on `-k7`, 0.59 on `-d2` and 0.48 on `-k1x9`, 0.71 as the class geomean, and on `small` level on the three views that fill, 0.85 on `small-patch-r5` and 0.57 on `small-row96`. Killed by the pair past the floor on both halves on any of the five fill classes, which would say the fill's consumer and the ceiling are not the same code.

(11) *The ordered list's consumers, `liblist-stage1-sum` to `liblist-stage4-sum`.* `sumT`'s shape over the ordered list of each stage: master's slice recursion, the port's base-offset table, and the lazy odometer under the natural-strides and the lean dispatch, stages three and four through their routes and the fused loop stages four to nine share. On `runs` and `block` the ordered list and the address-ordered one walk the same runs, so Run 27's unordered readings transfer: `libunord-stage5-sum` over `libunord-stage1-sum` read 0.6051 and 0.6137 on `runs` and 0.6164 and 0.6396 on `block`, and the leaf fusion and then the flat walker have since taken the odometer's side down twice. The flat-walker probe on the 9.12.4 build puts the odometer's consumer at 0.14 of master's and 0.25 of the table's on `runs-9`, all four within a percent from `runs-4096` up, where the element loop is the call, and as class geomeans 0.37 against master's on `runs` and 0.45 on `block`, 0.51 against the table's on `runs` and 0.77 on `block`. On `runs` and `block`, both halves: `predict: pair liblist-stage3-sum liblist-stage1-sum 0.4 within 15%`, and `predict: pair liblist-stage4-sum liblist-stage2-sum 0.63 within 25%`, the odometer's consumer against the table's, the probe's 0.51 and 0.77 read per class by hand; and `predict: pair liblist-stage4-sum liblist-stage3-sum 1.0`, the lean and the natural-strides dispatch being one route on every run view. On the main set, `rev`, `bcast`, `bcastmid`, `scaled` and `compose` every ordered list fills or is one block, so the four read as their fills do and the spans print a figure and no verdict, ties by hand. On `window`, `flip` and `small` the spans print a figure and no verdict too, read per view by hand: on `window` every ordered list fills but at `window-64x64-k1x9`, where canonicalization drops the unit axis and stages three and four list 3584 runs of 9 that master fills once --- the view Run 27 read the lazy ordered fill behind master's on, 3.0 corrected on both halves, and where the consumers now meet with no allocation a run; on `flip` an ordered list fills wherever the innermost axis is reversed or strided and lists on `flip-outer-gap64`; on `small` the dispatch is a visible share of a call under a microsecond, which is what Run 27's third kill of the ordered pair priced. Killed by `liblist-stage3-sum` not ahead of `liblist-stage1-sum` past the floor on both halves on `runs` or `block`, which would say the odometer's fold does not beat master's recursion in logical order as it does in address order; or by `liblist-stage4-sum` off `liblist-stage3-sum` past the floor on both halves on any population, the two dispatches parting.

(12) *Base's `sum` over the list, `libunord-stage6-list-sum` over `libunord-stage6-sum`.* The consumer as a library user writes it, base's `sum` over `map VS.sum` over stage six's list, no route in hand, against the harness's own fold through `sumLazyRuns`, so the pair prices what the list's shape gives a fold the library does not write. Under the level-form walker it read 96 bytes a run against the harness's 80; under the flat one both read none, and the user's fold is the faster where runs are short, which is not priced --- the harness's fold is one function compiled once over the route's lists, the user's inlines the route into the fold --- and a counts reading is what would. The flat-walker probe reads it over stage six's consumer at 0.70 on `window`, 0.59 to 0.99 by view, and 0.81 on `runs`, 0.47 at `runs-2` rising to 1.00 from `runs-1024` up; 0.96 on `block`, 0.99 on `flip` and 0.98 on `small`; and within a fifth of a percent of 1 on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, where the route is one slice or one fill and the fold sums one vector. On `window`, both halves: `predict: pair libunord-stage6-list-sum libunord-stage6-sum 0.7 within 15%`; on `runs`, both halves: `predict: pair libunord-stage6-list-sum libunord-stage6-sum 0.8 within 15%`; on `block`, `flip` and `small` the span prints a figure and no verdict, read against the probe's geomeans by hand; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, both halves: `predict: pair libunord-stage6-list-sum libunord-stage6-sum 1.0`. Killed by the user's fold behind the harness's past the floor on both halves on `window` or `runs`, which would say the flat walker's known continuations reach the fold the library writes and not the one a user brings, the case the arm exists to see; or by the pair past the floor on both halves on any of the five fill classes. Read beside it off the allocation column: the two consumers within 0.01x of each other on every view but `small`'s, both allocating nothing a run and differing by per-call constants alone; the user's fold above the harness's by 0.05x or more on a runs-route view of thousands of runs would say base's fold is allocating a run again.

(13) *The two unrollings, `lib-stage2-lean-u1` over `lib-stage2-lean`.* Since 2026-09-09 the shipped fill's twin differs from it in two places and not one: the stepping run not unrolled, as since Run 27, and the broadcast run not unrolled, `fillStage2` having unrolled its own by two that day while `fillStage2U1` kept one write and a compare per element; the doubling block copy of the same day is in both. So the pair prices the stepping unroll where the innermost stride is not 0, Run 27's item (14) reading it at 1.0516 and 1.0414 on the main set, and the broadcast unroll where it is: a filtered probe of the changed build on the 9.12.4 basis, raw, one process a class at a two-second budget, reads `lib-stage2-lean-u1` over `lib-stage2-lean` at 1.08 on `bcast-tall-Mx2`, a run of 2, 1.05 on `bcast-inner8`, 1.04 and 1.05 on `compose-rev-bcast` and `compose-slice-bcast`, and within two percent of 1 on the other `bcast` views, on `bcastmid`, where the zero stride is an outer level and the run steps, and on `small`. On the main set, both halves: `predict: pair lib-stage2-lean-u1 lib-stage2-lean 1.04 within 2%`, Run 27's reading carried over, the main set having no zero stride and the fills' changes touching no stepping run; on `bcast`, both halves: `predict: pair lib-stage2-lean-u1 lib-stage2-lean 1.03 within 3%`, `bcast-tall-Mx2` read per view by hand as the view the unroll is for; elsewhere the span prints a figure and no verdict, read against Run 27's per-class readings of the same pair by hand. Killed by `lib-stage2-lean-u1` not behind `lib-stage2-lean` past the floor on both halves on `bcast-tall-Mx2`, which would say the broadcast unroll buys nothing at a run of 2; or by the main-set pair outside 2% of 1.04 on both halves, which would say the fills' changes moved a stepping run they do not touch.

(14) *The lean fill ahead of the shipped leaf on the broadcast classes, `lib-stage2-lean` over `lib-stage1`.* `lib-stage1`'s fill is `fbMutOdoVecdimsAddInLeafU2`, the leaf as it shipped, with no block copy and its stepping body at stride 0 for a broadcast run; `fillStage2` copies a zero-stride level by doubling and unrolls its broadcast run since 2026-09-09, so where Run 27 read the lean fill behind the leaf on the views a zero stride reaches --- 2.33 and 2.28 on `bcastmid-b200k`, 1.20 and 1.20 on `bcast-tall-Mx2`, corrected, both halves --- the same probe reads it ahead: `lib-stage2-lean` over `lib-stage1` at 0.90 on `bcastmid-b200k`, 0.68 on `bcastmid-c32-cnn`, 0.88 on `bcastmid-primes` and 0.99 on `bcastmid-block150k`, 0.85 as the class geomean; 0.94 to 0.96 on the five `bcast` views with a long broadcast run, where Run 27 read the lean fill ahead already on the two views it had, the hoisted broadcast read being older than this day, and 1.00 on `bcast-tall-Mx2` where Run 27 read 1.20, 0.95 as the class geomean; 0.95 on `compose-rev-bcast`, 0.92 on `compose-slice-bcast` and 1.04 on `compose-zero-mid`, whose zero-stride level is the middle one and the one view the probe reads the lean fill behind on. The doubling is what moves `bcastmid`, the odometer's writes replaced by copies of growing size, `bcast` having its zero stride innermost, where no copy runs; the broadcast unroll is what brings `bcast-tall-Mx2` level. On `bcastmid`, both halves: `predict: pair lib-stage2-lean lib-stage1 0.85 within 10%`; on `bcast`, both halves: `predict: pair lib-stage2-lean lib-stage1 0.95 within 4%`; `compose` prints a figure and no verdict, `compose-zero-mid` read by hand. Killed by the lean fill behind the leaf past the floor on both halves on `bcastmid-b200k` or `bcastmid-c32-cnn`, which would say the doubling does not beat the odometer's writes at a full budget; or by the pair moving from Run 27's reading past the floor on both halves on any population with no zero stride, read through `--bridge`, which the two changes cannot reach. Run 29 is to give `fbMutOdoVecdimsAddInLeafU2` the second unrolling, the library's leaf being what the change is for, which returns `bcast-tall-Mx2` and with it the `bcast` class to a tie and reads this item the other way there; the doubling has no counterpart in the leaf, so `bcastmid` stays the lean fill's until the leaf gains a block copy of its own.

(15) *The shipped fill's own A/A pair, landed 2026-09-09.* `mut-odo-vecdims-add-in-leaf-u2-aa` sits beside its base and `-aa-distant` at the roster's tail --- the fill family's first placement controls since the prune of 2026-09-04 took both of the old ones --- so that a clause about the arm the library actually runs is read against a floor measured on that arm and not on `mut-odo-vecdims`, `list` or `bq-expand`, whose A/A triples sit within five slots of each other while the fill family sits elsewhere in the process. Both copies are their base's code on every population. On the main set and on every class, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa mut-odo-vecdims-add-in-leaf-u2 1.0` and `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa-distant mut-odo-vecdims-add-in-leaf-u2 1.0`, each inside the population's floor; where either falls outside it the margin IS that population's floor for this arm, read as the measurement it is rather than as a finding. Killed by the adjacent copy outside the floor on both halves on the main set, which would say a slot two apart already costs what a run reads as a strategy's margin. Read beside them by hand and with no prediction: the two copies against each other on `flip-last-rows`, where eighteen legs of 2026-09-09 put that view's A/A spread anywhere between 0.46% and 11.31% and where one run takes a single draw of it.

(16) *The reversal reading inside one process, `flip-fwd-rows96` landed 2026-09-09.* The class's own finding --- a reversed innermost axis costing the regime-3 fill about twice the forward run at the same length --- has been read across two processes, `flip-last-rows` in the flip run against `runs-96` in the runs run, each over its own `list`; the new view is `runs-96`'s construction under a `flip` name, so both sides now sit in one process over one baseline. Read by hand off the two views' cells, with no span, on both halves: `mut-odo-vecdims` on `flip-last-rows` over the same arm on `flip-fwd-rows96` between 1.8 and 2.3, where Run 25 read 2.120 and 2.073 across the two processes, and the same ratio taken on `-u2`, `lib-stage2-lean` and `list`, which Run 25 read at 1.683 and 1.851, 1.903 and 1.845, and 1.091 and 1.076. Killed by the one-process figure outside that band on both halves, which would say the cross-process reading carried a term the two processes contributed rather than the reversal.

**Eleven held, four were killed and one was withdrawn with its arms before the run --- and the four kills are one finding said four times.** Every item that predicted a REAL effect landed, and there are SEVEN: the dispatch saving (1), the tie-break where it fires (5), the fold entry point (8), the user's own fold (12), both unrollings (13), the lean fill on the broadcast classes (14) and the reversal read inside one process (16) --- two of them past their registered tolerance in the arm's favour, (13) on `bcast` and (14) on both its spans, the kill conditions deciding. (15) stood here until 2026-09-11 and does not belong: the shipped fill's A/A pair predicts 1.0 against its own copy, which is a no-op prediction, and it held as one. Every one of the four KILLS fell on an item predicting a NO-OP --- two routes argued from the code to be the same past a dispatch --- and fell on a population chosen because the argument says the reordering cannot reach it: (3) on allocation, (6) on `window` and `small-patch-r5`, (7) on `rev`, and (11) on `main`, `rev` and `small`. **The converse is not true and is not claimed**: (2), (4), (10) and (15) are no-op predictions too and all FOUR held --- (2) over eight populations, and (15) an A/A pair read against its own copy rather than two routes argued to be one past a dispatch, which is the kind every kill fell on --- so what the four kills say is where such an argument is unreliable rather than that it always is. Each of those four is refuted on both halves --- (3) on allocation, which is deterministic per call and argues from that rather than from a floor, and the other three by margins the population's own floor cannot absorb --- so none is a noise reading. Every figure below is re-derived from this run's own JSONs rather than carried from the registration; a span is judged against the floor of the population it is read on, which is what `--predictions` uses; and an item's verdict is its KILL CONDITION applied across the populations that item names, never one sweep of the main set. **Two things the registration could not foresee govern how it reads.** The reducing consumers lost their corrected time on 2026-09-10, so EVERY paired figure quoted anywhere in this file for a `-sum` arm --- in the class paragraphs and in the verdicts alike --- is a RAW ratio of two uncorrected slopes, and is judged against the same floor as a corrected one, the floor being a ratio of two placements of one arm and so unaffected by the correction. And the gate's machine check did NOT fire this run, against Run 27's own fingerprint and over all nineteen shapes, where Run 27's fired at -3.66% against Run 26's --- so nothing here carries a box term and no reading below is bridged.

(1) *The dispatch cost of the first canonicalization.* **HELD on both halves.** On `small` the consumers read **0.9073** on the basis and **0.9363** on HEAD, against a registered 0.92 within 4%. Neither kill fires: stage six's consumer is ahead of stage five's rather than behind it, and `small-flat64` --- whose sitting inside the floor would have said the sort's length is not what the probe read --- is far outside it at **1.1913 and 1.2477**, and outside in the direction the longer sort predicts. Four of the five per-view clauses land: stage six ahead past the floor on `small-row96` (0.8515, 0.8700), `small-patch-k5` (0.7350, 0.7755) and `small-bcast32` (0.8756, 0.8868), and behind on `small-flat64`. The fifth misses, and is the item's one wrong clause: `small-patch-r5` was registered INSIDE the floor and reads **0.9418 and 0.9640**, ahead by ten times the basis floor of 0.59% and five times the control's 0.79%.

(2) *The tie everywhere else.* **HELD, on all eight populations and both halves --- sixteen readings and not one outside its own floor.** `rev` 1.0029 and 1.0040, `bcast` 1.0000 and 0.9993, `bcastmid` 0.9997 and 1.0002, `window` 0.9965 and 0.9980, `scaled` 1.0002 and 0.9999, `runs` 0.9998 and 0.9999, `block` 0.9983 and 0.9990, `compose` 0.9990 and 0.9997. The widest is `rev`'s **1.0040** on HEAD against that class's 0.48%. So the two routes ARE the same code past the dispatch on every view that is not `small`'s, which is what item (1)'s saving is measured against, and the dispatch is invisible at a call of tens of microseconds.

(3) *Allocation.* **KILLED, and it is the first of the four no-op items to fall.** The item asked that `libunord-stage6-sum` read the same allocation multiple as `libunord-stage5-sum` on every view, and be killed by any view where the two differ at the column's own precision. Over the eleven populations, **16 of the 77 views differ at the published two decimals, and each of them on BOTH halves** --- 32 of the 154 view-readings --- which is what says this is the two dispatches and not a measurement: allocation is deterministic per call. All five `small` views differ, most widely `small-patch-k5` at 1.26x against 1.84x and `small-row96` at 0.43x against 0.59x; on the main set `cnn-L1-6x6-c1` reads 1.09x against 0.89x and `cnn-slice-c32` 0.66x against 0.96x. The differences run both ways, so this is not one dispatch allocating a buffer the other does not; it is the first canonicalization changing what the walker has to hold.

(4) *The tie-break where it cannot fire.* **HELD, and by the width of one half.** Six of the seven named populations tie inside their own floors on both halves: `bcast` 0.9998 and 1.0004, `bcastmid` 0.9994 and 0.9988, `scaled` 0.9999 and 1.0015, `runs` 1.0002 and 0.9997, `block` 1.0001 and 1.0000, `compose` 0.9994 and 0.9998. `rev` reads **1.0047** on the basis against a 0.41% floor and **1.0034** on HEAD against 0.48% --- outside on one half, inside on the other --- and the kill wants both, so it does not fire. It is the same population item (7) is killed on, which is the reading to carry: whatever `rev` does to these dispatches, it does it to more than one of them.

(5) *The tie-break where it fires.* **HELD on both halves, and it is the closest the run comes to reproducing a probe.** On `window` the consumers read **0.7425 and 0.7237** against a registered 0.7 within 15%, where the flat-walker probe read a class geomean of 0.72. `small-patch-r5`, the item's one hand-read view, is ahead past the floor on both halves at **0.9669 and 0.9395**, as registered. None of the three kills fires: the per-run cost is what the dilated control read, `-s2` and `-d2` are not moved past their floor, and the arm is not behind on `small-patch-r5`.

(6) *The longest chain.* **KILLED on both of its conditions, which no other item this run manages.** On `window`, where stage eight was argued to keep stage six's order and move no run at all, it reads **1.0518 and 1.0474** against floors of 0.37% and 0.40% --- off stage seven past the floor on both halves, the item's own first kill. And on `small-patch-r5`, the single view where the chain was registered AHEAD because the runs halve, stage eight is **behind** at **1.2593 and 1.2152**, the second kill --- and the runs do not halve: stage seven already lists sixteen runs of 16 there, item (5)'s own reading, so the registration's clause that stage eight's run differs from stage seven's on that view was false, the two routes parting there as on `window` in the order of two levels tied on stride, and the reading is the chain's dispatch, an exhaustive search over lists, on a call under a microsecond, as item (7) reads stage nine's extra pass on the class. A probe of 2026-09-09, `fusion-probe/log-probe-fusion-final.json`, had already read the pair at 1.21 on `small-patch-r5` and 1.012 on `window-224x224-k3`, and was not read for this item, which cites no probe. Six of the eight tie populations do hold; `scaled` sits outside on the basis alone, 1.0061 against 0.38%, and inside on HEAD. So on both views it was killed on the chain moved no run stage seven had not, and what the argument called free --- the order of two tied levels and the dispatch that chooses it --- is what the binary prices.

(7) *Zero-stride axes outermost.* **KILLED on `rev` --- and every positive prediction it made held, several of them to the second digit.** Where a zero stride exists the arm does what the flat-walker probe said it would, on both halves: `bcast-src8` **0.4732 and 0.4993** against a probe's 0.48, `bcast-src64` **0.4587 and 0.4616** against 0.46, `bcast-src512` **0.6565 and 0.6571** against 0.62, `bcastmid-b200k` **0.4451 and 0.4676** against 0.45, and `compose-zero-mid` **0.4600 and 0.4601** against 0.46. The repeat ladder added for this run shows no crossover between the repeated slice and the fill, which is what it was added to look for. `small-bcast32` reads **0.7538 and 0.7426** where the probe read 0.97, so the short slice is further ahead at a full budget than at two seconds. `compose-scalar`, every stride 0, is unchanged at 0.9996 and 1.0038. What kills the item is its null half: `rev` has no zero stride and should therefore be stage six exactly, and reads **1.0191 and 1.0164** past floors of 0.41% and 0.48% on both halves.

(8) *The fold entry point.* **HELD.** On `window` the hand-written loop reads **1.7249 and 1.7422** against a registered 1.7 within 15%, so the flat walker's known continuations survive a full budget and the fused list stays ahead of the loop written to avoid it. The five fill classes tie as registered: `rev` 1.0010 and 0.9990, `bcast` 1.0003 and 1.0009, `bcastmid` 1.0001 and 0.9985, `scaled` 1.0012 and 1.0004, `compose` 1.0001 and 0.9994. Neither kill fires. The populations the item prints a figure for and no verdict: `runs` 1.3166 and 1.3055, `block` 1.0556 and 1.0508, `flip` 1.0207 and 1.0144, `small` 1.0195 and 0.9767 --- the only one of the four where the two halves part in sign, and inside `small`'s own floor on neither reading alone.

(9) *The ports without the copy.* **WITHDRAWN 2026-09-09 with the arms**, before the run: the same day's ruling retired every Fill arm over a list to `check`, so the spans it registered are read on no process here. What stands is the change itself, held by `check` on every view, and the allocation column of the checked arms.

(10) *The ceiling's consumer.* **HELD, on all five named populations and both halves.** Where both fill or both return one block the pair is a tie inside the floor: `rev` 0.9994 and 1.0000, `bcast` 0.9998 and 1.0003, `bcastmid` 1.0003 and 0.9994, `scaled` 0.9976 and 1.0000, `compose` 0.9986 and 0.9986. So the fill's consumer and the ceiling are the same code, which is the kill's own negation. Where runs exist the fused list is ahead by what the write and the per-run cost differ, printed and not adjudicated: `block` **0.4457 and 0.4460**, `runs` **0.5681 and 0.5810**, `flip` 0.6521 and 0.6562, `window` 0.6930 and 0.7152 against the probe's 0.71, and `small` 0.8713 and 0.8732.

(11) *The ordered list's consumers.* **KILLED by its second kill, and its two primary spans held handsomely --- which is the whole of what this item found.** The odometer's consumer beats master's slice recursion as registered: `liblist-stage3-sum` over `liblist-stage1-sum` reads **0.3631 and 0.3750** on `runs` and **0.4563 and 0.4605** on `block`, against 0.4 within 15%. It beats the port's base-offset table too: `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.4951 and 0.5177** on `runs` and **0.7831 and 0.8018** on `block`, against 0.63 within 25%. Those are two readings and not two kills: the registration names TWO kill conditions, and the first -- `liblist-stage3-sum` not ahead of `liblist-stage1-sum` -- does not fire. The second does. `liblist-stage4-sum` was registered level with `liblist-stage3-sum` on every population, the lean and the natural-strides dispatch being one route on every run view, and the two part past the floor on both halves on **`small` (0.8831 and 0.8891 against 0.59% and 0.79%), on the main set (0.9885 and 0.9817 against 0.50% and 0.65%) and on `rev` (0.9950 and 0.9950 against 0.41% and 0.48%)**, with `compose` outside on HEAD alone. The strides comparison is priced, which is exactly what Run 27's item (8) found of the same two dispatches under the fill.

(12) *Base's `sum` over the list.* **HELD.** The consumer a library user writes is ahead of the harness's own fold where the runs are short: `window` **0.7176 and 0.7314** against a registered 0.7 within 15%, and `runs` **0.8349 and 0.8415** against 0.8 within 15%. The five fill classes tie as registered: `rev` 1.0008 and 0.9991, `bcast` 1.0004 and 1.0008, `bcastmid` 1.0006 and 0.9984, `scaled` 1.0025 and 1.0012, `compose` 0.9997 and 0.9990. Neither kill fires --- the user's fold is nowhere behind the harness's past a floor on `window` or `runs`, which is the case the arm was landed to see. So the flat walker's known continuations reach the fold base writes and not only the one the library writes, which is the ruling the overhaul was taken for. Printed without a verdict: `block` 0.9619 and 0.9584, `flip` 0.9850 and 0.9825, `small` 0.9968 and 0.9614.

(13) *The two unrollings.* **HELD on both halves.** On the main set `lib-stage2-lean-u1` over `lib-stage2-lean` reads **1.0359 and 1.0423**, inside 2% of the registered 1.04 and so reproducing Run 27's 1.0516 and 1.0414 for the stepping unroll on a roster that rebuilt the fill around it. The broadcast unroll is what the item added, and `bcast-tall-Mx2` --- the view it exists for, a run of 2 --- reads **1.2061 and 1.2058**, so not unrolling costs a fifth there where the filtered probe read 1.08; the kill wanted the twin NOT behind and it is behind by twenty points. `bcast-inner8` reads the same way, 1.2137 and 1.2181. The class span misses its tolerance in the arm's favour, **1.0818 and 1.0822** against a registered 1.03 within 3%, because those two views carry it; that is a prediction read short and not a kill.

(14) *The lean fill ahead of the shipped leaf on the broadcast classes.* **HELD on its kills, and both of its spans are wrong in the arm's favour.** On `bcastmid` `lib-stage2-lean` over `lib-stage1` reads **0.6163 and 0.6420** where 0.85 within 10% was registered, so the doubling block copy buys nearly twice what the probe priced; on `bcast` it reads **0.8635 and 0.8625** where 0.95 within 4% was registered. Both miss their tolerance and neither kill fires: the lean fill is nowhere behind the leaf on `bcastmid-b200k` or `bcastmid-c32-cnn`, which is what would have said the doubling does not beat the odometer's writes. Where Run 27 read the lean fill BEHIND the leaf on the zero-stride views, 2.33 and 2.28 on `bcastmid-b200k`, this run reads it ahead across the class. `compose`, printed without a verdict, reads 0.8378 and 0.8471.

(15) *The shipped fill's own A/A pair.* **HELD in every one of its forty-four readings**, which is two spans over eleven populations on two halves and the largest set of ties this file records. `mut-odo-vecdims-add-in-leaf-u2-aa` against its base runs from 0.9964 to 1.0056 and `-aa-distant` from 0.9879 to 1.0018, each inside its population's own floor. The pair does what it was landed for: on the basis half's main set and on its `window`, `scaled`, `small` and `compose` the A/A distance the fill family measures IS the floor, so a clause about the arm the library actually runs is now judged against a floor measured on that arm rather than on `mut-odo-vecdims`, `list` or `bq-expand`. The kill --- the adjacent copy outside the floor on both halves on the main set --- does not fire: it reads 1.0003 and 1.0005 paired against 0.50% and 0.65%. The PUBLISHED column parts in sign from both, at 0.9967 and 0.9925 -- inside the floor either way, which is why the verdict stands, but the brief's own rule is to quote both.

(16) *The reversal reading inside one process.* **HELD on both halves, and the cross-process reading it was landed to check carried no term the two processes contributed.** `mut-odo-vecdims` on `flip-last-rows` over the same arm on `flip-fwd-rows96`, both now in the `flip` process over one baseline, reads **2.1892 and 2.1252**, inside the registered 1.8-to-2.3 band and beside Run 25's 2.120 and 2.073 taken across two processes. The same ratio on the other three arms: `mut-odo-vecdims-add-in-leaf-u2` 1.8774 and 1.8383 against Run 25's 1.683 and 1.851, `lib-stage2-lean` 1.9290 and 1.7278 against 1.903 and 1.845, and `list` 1.0489 and 1.0668 against 1.091 and 1.076. So a reversed innermost axis costs the regime-3 fill about twice the forward run at the same length, and the class's own finding survives being read the honest way.
