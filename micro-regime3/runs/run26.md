# Run 26 (SpecConstr)

One run's write-up: its head, its Results, what the next run compares against, the claims that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 26 (SpecConstr), and what a second compiler does to a roster that invents three arms and lifts four out of parking: the two pointer fills deliver the instruction saving they were registered on and about a THIRD of it reaches the clock, so all three of their spans are killed while all three of their directions hold --- `mut-odo-vecdims-add-in-leaf-u2-ptr` executes 0.8357 of `-u2`'s corrected instructions, exactly the 0.8356 the registration derived, and reads 0.9479 in time where 0.88 was predicted --- and on GHC HEAD the same two arms lose the saving outright, `-u2-ptr` executing 1.8842 of its parent's instructions and allocating 2.61x the result vector where the basis allocates 1.00x.** Criterion, **`--ghc-options=-fspec-constr`**; the dead-spot form on BOTH halves, one source, one shim commit, one shim environment, one roster and one shape set, and **what moved is the compiler and nothing else a freeze, a source diff or a shim switch can see**: 570 benches, 30 timed arms over 19 main-set shapes, and 1560 more over 52 class views in **ten** classes. **The basis is `run26-g912`**, ghc-9.12.4, the default on PATH here and the lineage every figure in README was measured through, built `-fspec-constr -fobject-determinism` with `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of `align-as.py`; md5 `e2c371228e33b63057d5a7af30b2be97`, `.text` 20594885 B. **The other half is `run26-ghead`**, the same source and the same shim environment through `cabal.project.ghead`, which selects the in-tree stage1 of the GHC checkout --- `10.1.20260803`, Run 24's and Run 25's compiler unmoved, and the binaries carry `ghc-internal-9.1204.0` against `ghc-internal-10.100.0` --- md5 `0493a65368a2509c4ed0c074ebf8b53d`, `.text` 20739903 B, 145018 B the larger, where Run 24's and Run 25's difference was 140922 B to the byte and this run's is one page more. So the halves differ in the compiler and in the boot libraries that come with it, and in nothing else; both under `WILDLOG=1 SATURATE=1`; **`Main.hs` at `f04c5dc` is Run 25's and this run's is `659d898`**, `align-as.py` at `b3a1aca` unmoved, the tree at launch `9702f20` with 52 untracked scratch paths and nothing modified --- and the commit the driver stamped is the TIP and not what the pair was built from, `9702f20` having landed after the build and moved `Main.hs` by comments alone, which the pair note proves in the binary and which is why the provenance below names `659d898`. The same desktop, Zen 3, a Ryzen 7 5800X, and the same BIOS Run 18 re-baselined onto. The two main processes read *0h49m15s* and *0h49m26s*, at *177 MiB* in use and *66 MiB* max residency on the basis against *167 MiB* and *66 MiB* on the HEAD half.

**There is NO repetition this run either, and what stands in its place is a cross-run column as tight as Run 25's on a roster that moved by seven arms.** `Main.hs` moved between Run 25 and this run, by the eight commits of 2026-09-05 and 2026-09-06, so neither half reproduces an earlier binary and the exact repetition Runs 19 and 23 could take is unavailable. What can be read is `run26-g912` against `run25-g912`, the previous build of THIS recipe, pinned to the 18 shapes they share: over the 19 arms both runs give a corrected time, **seventeen read within 1%**, from `liblist-stage2` at 0.9863 to `list` at 1.0042, and the two outside are the `libunord` pair at 1.2760 and 1.3610 --- the pair that is its own forcing term on 8 and 8 of the 18, whose net is four parts in a hundred thousand of its cell at the smallest and two to three parts in a thousand at the median and whose ratio is therefore arithmetic rather than a movement. **Eight arms have no cross-run cell at all**, `canon-vecdims`, `lib-stage2`, `lib-stage2-short`, `libunord-stage3`, `mut-odo-vecdims-add-in-leaf`, `-add-in-leaf-down` and the two pointer fills, four of them lifted out of parking for this run and three of them new. **What keeps this column tight is what kept Run 25's tight**: the arms whose figure was where their bytes landed were parked on 2026-09-04 and none came back, so a roster change that adds seven timed arms still moves no surviving arm's geomean by a point.

**What holds the build to something is the gate and three machine checks, there being no repetition to do it.** The gate ran both halves twice in a palindrome and read SOUND on both passes: every arm fell the same side of 1 in both and at a like win count, `bq-expand` reading 0.9928 and 0.9917 at 11 and 14 of 19, `mut-odo-vecdims` 1.0006 and 1.0037 at 9 and 7 of 19, and `list` 1.0088 and 1.0149 at 5 and 2 of 19 --- where Run 25's gate read a sign change on `list` between its passes and was sound, so this palindrome is the quieter of the two. Within the basis half its two passes agree to a ten-thousandth, `bq-expand` over `mut-odo-vecdims` reading 2.1965 and 2.1967 at 0 of 19 wins apiece. Its machine check read **+0.72%** on `list`'s net against the fingerprint Run 25 kept, over **18 of 19** shapes --- the eighteen that fingerprint has, this roster timing `cnn-L1-6x6-c1` again --- worst `stretch-square-1341` +3.43% and none past 5%; the run's own two main-set processes read the same comparison at **+0.42%** and **-0.91%**, worst `stretch-square-1341` +4.20% and `stretch-bigstride` -3.15%. Three readings against the same kept fingerprint, every geomean inside 3%, so the box measures as it did and no absolute is re-baselined. Each half's own six A/A pairs give it a floor, and the counted work, taken over every population on both halves, is what separates a compiler's instructions from a slot its bytes landed in throughout what follows.

**The manifest's two live claims both held, and claim 10's four readings across two runs and two codegens now span three thousandths.** `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims` --- what the leaf fusion buys over the family root --- reads **0.6438 at 18 wins of 19, sign p 7.6e-05** on the basis and **0.6467 at 18 of 19** on the HEAD half, and pinned to these nineteen shapes Run 24's two halves read **0.6464 and 0.6454**; the shipped code is 35% ahead of the code it was refined from on both compilers alike. That is the one thing the roster still prices which the library has taken. **Claim 7 held on every level it has**, its pinned shape set unmoved from Run 25's eighteen and read on those eighteen: the mutable fills at 1.00x the result vector, the two `liblist` arms at 2.00x, `bq-expand` at 2.06x, `list` at 22.38x and the three `libunord` arms at 0.00x, where this run's own nineteen give 2.11x and 22.64x for the last two --- and **the two pointer fills join the 1.00x tier on the basis and break it on HEAD**, `-u1-ptr` reading 1.41x and `-u2-ptr` 2.61x there, which is the only allocation level either half moves and is this run's largest single finding about the second compiler.

**What the second compiler is worth on this roster is almost nothing on twenty-four of its twenty-six arms and everything on the other two.** Read as `--compare` prints it, basis over HEAD, **twenty-four of the twenty-six arms carrying a corrected time read their counts within 1% of 1** --- the fills and the library routes 1.0064 to 1.0089, so 9.12 emits under a percent more work there; `bq-expand` and its twins 0.9936; `mut-odo-vecdims` and its twins 0.9996; and the three `libunord` arms **1.0000 exactly**. **The two that do not are the pointer fills**: `mut-odo-vecdims-add-in-leaf-u2-ptr` reads **0.6219** in counts and **0.3521** in time, and `-u1-ptr` **0.9295** and **0.7359** --- so HEAD emits sixty percent more instructions on the unrolled pointer fill and takes nearly three times as long at it, and the allocation column says where the instructions went. **And `list` is the reading that decides what the two columns may be used for**: it moves **1.0116 in time at a count ratio of 0.9968**, so 9.12 executes a third of a percent FEWER instructions on the baseline and is a percent slower at it, a time-over-counts of 1.0149 --- which is Run 25's 1.0110 and 0.9967 and 1.0144 reproduced to the third digit on a roster six arms larger. The move that costs this pair its subtraction is therefore where the code landed or what the runtime did with it, and not what either compiler emitted, and it is now the second run running that says so.

**What this pair can no longer separate is placement from codegen on the arms that used to show it, and lifting four parkings did not bring one back.** `gen-unsafe`, `build`, `mut-odo` and their six twins were the arms whose count ratio read 1.0000 while their times moved four to six percent, and Run 24 called that reading the cleanest of that term this file had had; every one of them was parked on 2026-09-04 and none of the four this run lifted is among them --- `mut-odo-vecdims-add-in-leaf-down`, `canon-vecdims`, `lib-stage2` and `lib-stage2-short` were lifted for the clauses they carry, not for their slots. What is left in their place is the A/A pairs, which are still two placements of one strategy and give this run its floor, and the counted work, which still says of any arm whether a movement is instructions or where they landed --- and on this pair it says so loudly, twenty-four arms inside a percent in counts against two outside it by thirty-eight and seven points. **So a compiler pair still prices two things at once here; what has gone is the family that made the second one legible on its own.**

**The leaf block reads the same ordering on both compilers for a second run, and this run reads it with the counted bound back on the roster rather than through a stand-in.** `mut-odo-vecdims-add-in-leaf-down`, parked after Run 23 and lifted for this run alone, is the counted bound; `-u1` is the merged bound un-unrolled and `-u2` the shipped fill. `-u1` reads **0.9556 at 18 of 19, sign p 7.6e-05** against `-down` on the basis and **0.9566 at 16 of 19** on HEAD, and `-u2` reads **0.9644** and **0.9648** against `-u1`, at 16 of 19 and p 0.0044 on both halves --- every one of the four past both halves' floors of 0.31% and 0.46%, and the two compilers apart by a thousandth on each pair. So the merged bound buys four and a half percent over a counted one and the unrolling three and a half on top of it, and the shipped fill's margin is still the unrolling's. **The count-down variant is level with `-u2` again**, `-u2` reading 0.9972 of it on the basis at 14 of 19, p 0.064, and 1.0019 on HEAD at 7 of 19, p 0.36, as on Runs 23, 24 and 25 --- the tie [the ceiling](../README.md#the-mutable-ceiling-taken) records. **What the dispatch around the fill costs went back up**: `lib-stage1`, that same fill reached through the library's own regime test, reads **1.0386** of the bare arm on the basis and **1.0417** on HEAD, against Run 25's 1.0199 and 1.0214 over eighteen shapes --- and the whole of the rise is on the two smallest shapes: `cnn-L1-6x6-c1`, which this run times again, reads **1.4040** and `cnn-slice-c32` **1.3540**, where the other seventeen run 0.9837 to 1.0549 and ten of them sit within a point of 1. So the dispatch is a per-call cost that shows only where the call is small, and what moved between the runs is the shape set rather than the route.

**The classes break property 2's first clause in ALL TEN this run, where Run 25 broke nine, and the tenth broke to an arm this run invented.** `libunord-stage3`, the unordered candidate that joined the roster on 2026-09-05, leads `rev`, `bcastmid`, `window` and `flip` --- by 98.69%, 54.60% and 68.10% against those populations' floors of 0.30%, 0.88% and 1.07%, and on `flip` its cell is its own forcing term on two views of five, so that lead is not priced. `mut-odo-vecdims-add-in-leaf-u2-ptr`, the unrolled pointer fill, leads `runs`, `block` and `compose` by 24.61%, 16.68% and 38.03% against 2.89%, 0.39% and 0.44%. `lib-stage2-lean` leads `small` by 15.93% against 0.52%, `lib-stage2-disp` leads `bcast` by 40.43% against 0.71%, and `lib-stage1`, the shipped route, leads `scaled` by 11.31% against 2.38%. **`block` was the one population Run 25 did not break and this run does**, to `-u2-ptr` where Run 25's leader was `-u2-down`, a `mut-odo-vecdims` sibling that counted as the family's; the pointer fill is not the family's, being the design [dead ideas][dead] refuses. Property 3's tier ORDERING holds in all ten and property 1 holds in all ten --- but `small` breaks the fill tier's LEVEL again, the five non-pointer fills there reading **1.27x** the result vector rather than 1.00x against Run 25's 1.23x, and the two pointer fills 1.44x, which is that class doing what it was built to do.

**Across the halves every class puts the basis ahead, and one arm does all of it.** Of the 260 arm-comparisons the ten classes carry, two are degenerate and not voted --- `libunord-stage2` and `libunord-stage3` on `flip` --- and **154 put the basis faster against 104 the HEAD half**, the ten geomeans running **0.9353 on `bcast` to 0.9559 on `flip`**, every one below 1 where Run 25 had six below and four above. **`mut-odo-vecdims-add-in-leaf-u2-ptr` is the low extreme of all ten**, from 0.2571 on `bcast` to 0.5671 on `small`, so the whole shift is the one arm HEAD compiles badly; the high extreme over all ten is `lib-stage2-lean` at 1.0427 on `window`. Take that arm out and the split is ordinary, and the counted work says so: the per-class counts geomean runs **0.9769 on `bcast` to 0.9886 on `block`** over the 26 arms with a corrected time, against 0.9811 on the main set --- ten classes inside two and a half percent of each other and of it, with the same one arm carrying the deficit in every one. **SIX classes disqualify their own cross-half line**, where Run 25 had two: `window` at 1.0232, `block` at 0.9848, `small` at 0.9857, `rev` at 1.0088, `runs` at 0.9923 and `flip` at 1.0072 move `list` past the 0.7% bar, so those six are ordered and not subtracted, and the four inside it are `bcast`, `bcastmid`, `scaled` and `compose`. `small` is the one population where every one of the 26 arms is faster on the basis.

**The eight registrations came back four HELD, one KILLED, one SPLIT, one that is three class clauses of which one is read for the first time, and one whose three spans are all killed with all three of their directions standing.** (1) the dispatch pair: **KILLED**, and on one population --- `lib-stage2-disp` over `lib-stage2-lean` reads 1.0171 and 1.0316 on `small` against that class's floors of 0.52% and 0.96%, past them on both halves, which is the item's own kill; it is inside the floor on eight of the other nine, and on `cnn-L1-6x6-c1`, timed again for this item, the HEAD half reads 1.139 where the basis reads 1.013, so Run 24's gap survives the lean ruling on one compiler and not on the other. (2) the roster change cross-run: **SPLIT**, its arm-level clause held exactly --- `lib-stage2-disp` 1.1550 on `cnn-slice-c32` against the 1.1719 predicted and 1.2177 over `small`'s four views, `liblist-stage2` the same way by less at 1.1110 and 1.1635, and every one of the nineteen shared arms within 1% at the geomean --- while its per-shape kill fires, six untouched arms moving past 1% on at least one shape and `liblist-stage1` reaching 0.8895 on `stretch-coprime-r7`. (3) the unordered candidate: **HELD**, `libunord-stage3` ahead of `libunord-stage2` by 20 to 33 points wherever stage two falls back to its list and inside the floor at 0.9997 to 1.0000 wherever both slice, allocating 1.00x where it fills against stage two's 2.00x --- with one clause wrong in the arm's favour, `flip-outer-gap64` predicted a tie and read 0.8111 and 0.8210, stage two allocating 1.31x there and so falling back after all. (4) Run 24's registration 5: **HELD**, `-u2` ahead of `-add-in-leaf-down` in ALL ELEVEN populations on BOTH halves, by 4.7% to 14.2%, the clause Run 24 could not read at all. (5) Run 25's orderings clause: **HELD** in both its parts. (6) the class clauses of 2026-09-03: **`flip` KILLED** again and **`block` KILLED** on its own condition again, both as Run 25 read them through a stand-in and now on the arms themselves; **`small` HELD in both parts and read for the first time**, both arms it turns on having been parked when it was written. (7) the two window views: **HELD**, the short body leading on all three k3 views of both halves. (8) the pointer fills: **three spans KILLED and three directions HELD** on the basis, and every one of the three reversed on HEAD. **What the eight cost in unreadable clauses is nothing, where Run 25 lost five**: items (4) to (7) were re-registered in their own words for exactly that reason, and every clause of all eight was readable on this roster.

**Every one of the twenty-two processes gated clean**, `--selftest` and both `--aa` gates, so no time column here is uncorrected, and the plateau gate puts all twenty-two inside **19.5526 to 20.0375 ms/iter, a 2.48% spread** against the 5% band, where Run 25 read 2.24% over twenty-two and Run 24 3.08% over twenty. **This run's floor is 0.31% on the basis half and 0.46% on the control**, read over the same SIX A/A pairs Run 25 read and tighter than its 0.40% and 0.61% on both halves: `bq-expand-aa-distant` carries both figures, and 0.31% and 0.46% read on the six pairs and on the four of those six that carry back to Run 10 alike, so one floor is quoted here and not two. **Five of the six intervals cover 1 on each half**, the one missing being `list-aa-adjacent` here and `bq-expand-aa-distant` there. The worst A/A cell of either main set is **2.54%** on `cnn-L1-24x24-c1` on the basis against **3.06%** on `lenet-L1-28-c1-k5` on the other, where Run 25 read 2.04% and 5.48% --- and those two are cells and not floors, and are not to be quoted as any. **Five A/A cells sit past 5%** and are read rather than averaged: `scaled-rank1-m1` at 6.63% and `window-128x128-k7` at 6.19% on the basis, and `runs-r3-48x30` three times over --- 5.13% and 5.70% on the two halves' worst-cell column and 5.07% on `list-aa-distant` on the basis besides. That one shape carrying the worst cell of BOTH halves, hours apart, is the class and not the evening.

**The two halves resolve alike again, and Run 25's one-sided reading was the one to be re-taken.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **0.98** on the basis against the HEAD half across the roster, **14 arms wider here and 16 narrower**, where Run 25 read 1.09 at 20 and 4 on a roster of 24 arms and said in this paragraph's place that a one-sided split on a third of the evidence was a difference to re-take rather than a property of the compilers. Six arms more and it is even again, as Run 24 read 1.00 at 31 and 21, Run 23 1.01 at 31 and 24 and Run 22 1.01 at 28 and 27. It remains a different quantity from the floor: sampling error inside one bench against agreement between two placements of one strategy.

**The sequence ran in ONE window, and the TWO foreign-CPU episodes in it are recorded here rather than averaged away.** Twenty-two processes from 02:29:46 to 08:40:03, every one exiting 0 at its roster's count --- 570 twice, 420 twice, 180 twice, 150 six times, 120 four times and 90 six times --- the HEAD half first throughout, which is the driver's order, and no process reported a selection it did not ask for. The gate that preceded it was launched once and the alarm between it and the sequence read 0.8% busy. **The per-sample instrument's foreign-CPU column names two episodes over all twenty-two logs and no others.** The first is two adjacent benches of `run26-ghead-main`, `alexnet-L1-55-c3-k11/sum-only-late` at 0.42 with a worst sample of 166.5 ms foreign and `alexnet-L2-27-c48-k5/sum-only-early` at 0.80 with 426.3 ms. **The second is one bench of `run26-g912-runs` and it IS on a timed arm**, `runs-96/mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.26 with a worst sample of 219.8 ms, which is one of the two arms this run was built to read. Everywhere else the column runs 0.00 to 0.15. **Neither moved a published figure, and each was checked on the artifact that could show it.** For the first, criterion's reading of the two cells puts the `sum-only-late` over `sum-only-early` A/A pair at +0.09% and -0.56% on those shapes, where three shapes nothing touched read -0.50%, -0.44% and +0.68%, that last being the pair's worst cell. For the second, the intruded cell is checked against the half nothing touched: `-u1-ptr` over `-u1` on `runs-96` reads **0.9355** on the basis against **0.9281** on HEAD, agreeing to eight tenths of a point where that class's floor is 2.89%, and its mutator clock is the LOWEST of its six neighbours on that view rather than raised. Post-run step 3's rerun was therefore not taken on either, and what would reopen it is a per-shape reading on `runs-96` or on either `alexnet` shape landing within half a point of a decision. **The five A/A cells past 5% are a different thing and no episode is on one of them**: foreign CPU reads 0.02 to 0.05 across `scaled`, `window` and both `runs` processes at those cells, so they are wild cells.

**Ten cells sink below the shared forcing pass on the basis and eleven on the control, and the count is not a property of anything.** Every one of them is a `libunord` arm, so on the basis `libunord-stage1` is a geomean on 15 of 19 shapes, `-stage2` on 16 and `-stage3` on 16, and on the HEAD half 16, 15 and 15; every other row covers all 19. **The reason is that there is nothing there to measure**: where the one-block test fires the arm returns a single `VS.slice`, so its cell IS the forcing pass, and the raw ratio of stage three to stage two on those shapes reads 0.9996 to 1.0016 on the basis and 0.9968 to 1.0004 on HEAD --- which side of zero the subtraction lands on is decided below that. So a sinking count is arithmetic and not a finding, and the three rows it costs a population are the three rows whose corrected figures say least; what those arms are worth is read raw and per view, which registration (3) does. **The new arm sinks with its two siblings and not more often**, `libunord-stage3` matching `-stage2` cell for cell on the shapes where both slice.

**The counted work covers every population, and its finding this run is a RATE rather than a ratio.** Twenty-two sweeps, both halves over all eleven populations, and no cell perf refused anywhere in the twenty-two files. Within the basis half, differencing each arm's instructions per iteration against the shared `sum-only` term shape by shape and taking the geomean over the nineteen --- the same correction the time column applies --- **the registration's three derived figures come back to a ten-thousandth**: `mut-odo-vecdims-add-in-leaf-u1-ptr` executes **0.8944** of `-u1`'s corrected instructions against the 0.8945 it was registered on, `-u2-ptr` **0.8357** of `-u2`'s against 0.8356, and `-u2-ptr` **0.8605** of `-u1-ptr`'s against 0.8604. **What does not come back is the rate at which a saving reaches the clock.** [The ceiling](../README.md#the-mutable-ceiling-taken)'s nineteenth reading prices it at about three quarters, and the three spans above turn 10.56%, 16.43% and 13.95% of instructions into 3.07%, 5.21% and 5.69% of time --- **29%, 32% and 41%**. The two spans of the leaf family itself do better and still fall short: `-u1` over `-add-in-leaf-down` reads 0.9154 in counts and 0.9556 in time, **52%**, and `-u2` over `-u1` 0.9208 and 0.9644, **45%**. So five spans on one roster put the rate between a third and a half where the ruling assumes three quarters, and every span registered off that rate missed low for that reason and not because the fill did less than it was built to do. The two compilers agree on the leaf family's counts to a thousandth --- 0.9145 and 0.9199 on HEAD against 0.9154 and 0.9208 here, and `lib-stage1` over `-u2` at 1.0238 and 1.0247 --- so what this pair prices about the family is the family and not the codegen, and the two arms where that fails are the two pointer fills.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The in-situ forcing term --- an arm minus its `-nosum` twin, against the `sum-only` the correction actually subtracts, read off `--aa`'s median column --- reads 1.0227 and 1.0316 on the basis and 1.0280 and 1.0334 on the HEAD half, on `mut-odo-vecdims` and `bq-expand`, the two arms whose `-nosum` twins the prune left standing; the two `sum-only` halves agree at 1.0007 on the basis and 0.9995 on the other, on worst cells of 0.52% and 0.68%. Run 25 read the same four at 1.0199, 1.0344, 1.0242 and 1.0375, re-derived here off its own JSONs, so all four tilt the same way on both runs and the halves are within six tenths of a point of each other on each arm. A margin between these halves is not carrying a correction bias.

**The ceiling still cannot be read, and what stands in its place gained a second rung this run.** `mut-odo-vecdims` against `bq-scan-rem-gm-mulback`, the fastest arm needing nothing at all, was this file's ceiling reading for eight runs back to Run 17, and the denominator went to `Only` on 2026-09-04 with every other rung below the top of claim 1, so no process this run times it and the series still ends at Run 24. What stands in its place is claim 10 inside the family, at 0.6438 and 0.6467 --- and, new this run, **the two pointer fills, which are a ceiling of a different kind**: not a candidate the library could take, the `Ptr`-walking form being refused for it ([dead ideas][dead]), but a measurement of what each parent would reach under an allocator that spilled nothing. On the basis they reach it, `-u2-ptr` 5.2% ahead of `-u2` and `-u1-ptr` 3.1% ahead of `-u1`, so the spill the parents pay is worth about a twentieth of the fill on `-u2` and a thirtieth on `-u1`; on HEAD they do not reach it at all. **What the run adds to the ceiling section is therefore not a number but a correction to its rate**, the nineteenth reading's three quarters against the third to a half five spans read here.

**This run's two columns may NOT be differenced either, and that is now two `-fspec-constr` pairs running.** `list` moves **1.16%** between the halves on the main set against the 0.7% bar --- over all 19 shapes, the basis faster on 3 of them --- where Run 25 read 1.10% and was refused, Run 24 0.25%, Run 23 0.33% and Run 22 0.81% and was refused. So the cross-half figures here are read as an ORDERING and not as a subtraction: a row's distance between the columns carries the baseline's own move as well as the arm's, and the counted work is what says which. **Six of the ten classes are past the bar with it**, `window` at 2.32%, `block` at 1.52%, `small` at 1.43%, `rev` at 0.88%, `runs` at 0.77% and `flip` at 0.72%, and their blocks say so; the other four are inside it, where Run 25 had eight of ten inside. **That the refusal has now happened twice on the same recipe is itself a reading**: the counted work puts `list` at 0.9968 and 0.9967 on the two runs, so both times the baseline moved in time and not in instructions, and both times by about a percent in the same direction.

**The straddling loops went from four to SIX on the basis half, the first reading past four this file has, and the two new ones arrived with the three new fills.** Each half's self-loops of at most 64 bytes in `Main`-compiled code number 174 on the basis and 161 on HEAD, 71 and 67 of them at offset 0. On the basis SIX straddle a cache line, where every earlier binary still on disk --- Runs 24 and 25, both halves, re-surveyed with today's reader --- reads exactly four. Post-run step 0's `-g3` twin names five of the six by byte identity: `fillStage2Short` at `0x42452a`, `fillStage2` at `0x426aaa` and `fbMutOdoVecdimsAddInLeafU2` at `0x433f2a`, 55-byte bodies at offset 42; `fbMutOdoVecdimsAddInLeafU2Down` at `0x431f2f`, 53 bytes at offset 47; and **`fbMutOdoVecdimsAddInLeafU2Ptr` at `0x47005d`, 48 bytes at offset 29, which is new** --- and it REFUSES the sixth, 45 bytes at offset 32 and `0x42c660`, holding no byte-identical copy of it. `mut-odo-vecdims-add-in-leaf-u1-ptr` is the one arm of the four the pointer registration prices that appears among neither the twin's straddlers nor the named five, which is suggestive and not a naming: `-g3` moves register allocation, and the refusal is what makes the negative honest. **On HEAD four straddle and the twin names two**, `fillStage2Short` at `0x4230aa` and `fillStage2` at `0x4255aa`, refusing `0x4309b1` (53 bytes, offset 49) and `0x43286c` (55 bytes, offset 44); so the two new straddlers are the BASIS half's alone. **What the two twins settle that the pair note could not** is the fill groups' membership: both halves carry a 6-copy group of the same six functions in the same address order --- `fbMidCopy`, `fbCanonVecdims`, `fbMutOdoVecdimsAddIn`, `fbMutOdoVecdims`, `fbMutOdoVecdimsAddOut`, `fbMutOdoVecdimsAddBoth` --- so what differs between the compilers is the pinning and not the membership, at `[0, 0, 0, 24, 0, 4]` here against `[18, 0, 31, 7, 9, 26]` there. **And HEAD's third group, which the basis has not, is named for the first time**: the mul-back trio, `fbBQmutRunsGmMulback`, `fbBQmutLemireMulback` and `fbBQmutRunsMulback`, at `[0, 0, 21]`. The `statistics` Quantile pair straddles on the basis at `[39, 47]` and fits on HEAD at `[7, 15]`, which is Runs 24's and 25's reading on both halves exactly.

**The regime was confirmed in the binary before the hours were spent**, which nothing afterwards can: a `diag` in the run's own regime puts `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512` on the basis, a ratio of 1.000 and `run25-g912`'s and `run24-g912`'s two figures to the byte, where plain -O1 separates the two tenfold. With no rebuild between the gate and the sequence, that is the only check standing between a mistyped regime and the hours.

**Run 26 records every population twice** --- the main set and all ten stride classes from each half, one process each --- **in one window**, as Runs 23, 24 and 25 did. Every one of the twenty-two exited 0 at the bench count its roster holds --- 570 twice, 420 twice, 180 twice, 150 six times, 120 four times and 90 six times --- and no process reported a selection it did not ask for. The twenty class processes span **0h7m50s to 0h36m40s**, the two `runs` processes accounting for the whole top of that range at 0h36m31s and 0h36m40s and the two main-set processes running longer than any of them at 0h49m15s and 0h49m26s. **The HEAD half ran first throughout**, `ghead` before `g912` on the main set and on each class in turn, which is the driver's order. **The alone-leg riders followed the sequence**, 88 single-bench processes over four invocations of 22 --- 19 main-set shapes and the three anchors on a second reps pass --- each half clean and saturated, every invocation exiting 0 with its `DONE-ALONELEGS` line.

**The decomposition reproduces on both halves for an eighth run.** The riders time each shape's `list` by itself, one bench per process on that half's own binary, `SAT=` off and on: the saturated legs split the deflation into the state the preamble puts on a clean process --- **+11.92%** on the basis and **+11.09%** on the HEAD half --- and the rest the roster adds on top of it, **+0.76%** and **+0.80%**. Run 25 read +12.19% and +12.46% for the state and +0.55% and +0.27% for the rest, Run 24 +11.83% and +11.33% and -0.99% and +0.20%. So the state term has now reproduced across eight runs inside a point and a half, while the roster's own contribution has stayed under a point on every half of every run that measured it --- and it stays under a point on a roster six timed arms LARGER than Run 25's, where Run 25's evidence was that it stayed under a point on one a quarter of Run 24's size, so neither direction moves it. **The tail is the same shape on both halves and it is the shape Runs 19 to 25 all named**, `stretch-tall-Mx2` at 1.0972 and 1.1193 --- a roster effect on one shape, and every run since Run 19 has named it, on both of this pair's compilers as on every pair before it.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition, which Run 19 was in a position to be firm about, having repeated one binary on one box and moved its floor by 1.7x. **What this run cannot repeat, and says so rather than implying otherwise, is that demonstration**: its roster moved by eight commits, so neither half reproduces an earlier binary. What the cross-run column does carry is the same lesson in the other direction --- seventeen of nineteen comparable arms within a point of the previous build of this recipe, on a roster that gained seven timed arms and still has no placement-exposed arm left in it.

## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a `-fspec-constr` table**: it is not the regime `Data/Array/Internal.hs` compiles under. **A row's distance from Run 25's column carries a roster term and an evening and, for one shape, a shape-set change**, `Main.hs` having moved by the eight commits of 2026-09-05 and 2026-09-06: seven timed arms landed and one left, six net, `cnn-L1-6x6-c1` came back from the retired list, three class views landed, and the basis half is Run 25's basis recipe with the source moved and not its binary. **Pin the shape set back and that distance is all but nothing**: over Run 25's eighteen shapes seventeen of the nineteen arms the two runs share read within 1%, from `liblist-stage2` at 0.9863 to `list` at 1.0042, and the two outside are the `libunord` pair, whose cells are their own forcing term on eight shapes apiece. **The `alloc` column makes the same point exactly**: pinned to those eighteen this run reads `bq-expand` 2.06x and `list` 22.38x, Run 25's two figures to the digit, where over its own nineteen it reads 2.11x and 22.64x --- the median moved with its population and not with what any arm allocates, which is the fourth run running that this file has had to say of that column. **This pair's halves differ in the compiler and in nothing else** --- one source, one shim, one shim environment, one roster --- so a cross-half distance is codegen and layout together; but `list` moved 1.16% between them, past the 0.7% bar, so those distances are read as an ordering and not as a subtraction, and the counted work is what separates the two terms inside one.

**And it is the basis half's**, `run26-g912`, as every published table here is from Run 11 on: the HEAD half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty rows. That the published half is the 9.12 one is this pair's own decision --- it keeps the lineage, being the compiler every figure in README was measured through, and the dead-spot form it carries is the basis recipe decided on 2026-09-02 --- and the HEAD half is the reading a form about to be published is owed on a second compiler. **Seven rows here are first readings at full budget**: `libunord-stage3` and the two pointer fills are arms no run has timed, and `mut-odo-vecdims-add-in-leaf-down`, `canon-vecdims`, `lib-stage2` and `lib-stage2-short` are timed again after parkings taken since Run 23, for this run alone and for the clauses they carry. One row Run 25 published is gone, `mut-odo-vecdims-add-in-leaf` having moved to `Only` on 2026-09-05.

**Comparing runs?** The table below is Run 26's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the claims to test are [the ones after it](#the-claims-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.31%** from 1 on the basis half or **0.46%** on the control, the HEAD half, read over the six pairs the prune leaves and over the four of those six that carry back to Run 10 alike, beside which the worst SINGLE cells of 2.54% and 3.06% are not floors at all and are not to be quoted as any --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What DOES cost a row its population on this run is the correction, not the estimator**: three rows on each half carry cells the shared forcing pass is not smaller than, and such a cell cannot be corrected at all --- so the three `libunord` rows are geomeans on 15, 16 and 16 of 19 shapes on the basis and 16, 15 and 15 on the HEAD half, they are named in the head, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what claim 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, which claim 7 now carries on a live basis, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.63* | *59* | *2.11x* | *its base arm, forced with one element* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.60* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.01* | *82* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.01* | *82* | *0.00x* | *the same, at the other end* |
| libunord-stage3 | 0.000 | 0.048 | 0.01 | 82 | 0.00x | new mutating `Vector` method -- the unordered entry point walking the destination in address order |
| libunord-stage1 | 0.000 | 0.051 | 0.02 | 82 | 0.00x | new mutating `Vector` method -- stage one behind the unordered one-block test |
| libunord-stage2 | 0.000 | 0.052 | 0.02 | 82 | 0.00x | new mutating `Vector` method -- the branch's driver behind the unordered one-block test |
| lib-stage2-short | 0.027 | 0.123 | 0.60 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, a short canonical run written by a body of its length |
| mut-odo-vecdims-add-in-leaf-u2 | 0.028 | 0.124 | 0.50 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| lib-stage2-lean | 0.028 | 0.123 | 0.65 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage2-disp | 0.028 | 0.123 | 0.61 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, slice route above a run-length threshold |
| lib-stage2 | 0.028 | 0.123 | 0.64 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.029 | 0.123 | 0.48 | 69 | 1.00x | new mutating `Vector` method |
| lib-stage1 | 0.029 | 0.123 | 0.60 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.030 | 0.122 | 0.60 | 68 | 1.00x | nothing the library would take -- the merged-bound leaf walking a `Ptr`, a ceiling and not a candidate |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.030 | 0.124 | 0.48 | 69 | 1.00x | the same, unrolled -- the shipped fill's ceiling under an allocator that spills nothing |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.121 | 0.59 | 68 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| mut-odo-vecdims-add-in-leaf-down | 0.034 | 0.119 | 0.51 | 68 | 1.00x | new mutating `Vector` method -- the leaf fill with a counted bound, `-u1`'s control |
| liblist-stage1 | 0.049 | 0.150 | 0.96 | 63 | 2.00x | new mutating `Vector` method -- stage one at the list entry point |
| canon-vecdims | 0.049 | 0.121 | 0.65 | 65 | 1.00x | new mutating `Vector` method -- a canonicalizing pass above the fill, `mut-odo-vecdims` its control |
| liblist-stage2 | 0.050 | 0.150 | 0.92 | 63 | 2.00x | new mutating `Vector` method -- the branch at the list entry point |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.122* | *0.44* | *65* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-aa* | *0.051* | *0.122* | *0.40* | *65* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.051** | 0.123 | 0.38 | 65 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| bq-expand | 0.111 | 0.208 | 0.67 | 53 | 2.11x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.111* | *0.208* | *0.58* | *53* | *2.11x* | *A/A control* |
| *bq-expand-aa-distant* | *0.111* | *0.205* | *0.42* | *53* | *2.11x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.65 | 22 | 22.64x | -- |
| *list-aa-distant* | *1.000* | *1.012* | *0.66* | *22* | *22.64x* | *A/A control* |
| *list-aa-adjacent* | *1.002* | *1.025* | *0.52* | *22* | *22.64x* | *A/A control* |

`concat-runs` has no row, and neither do the other 58 arms the roster holds and checks without timing --- 59 of its 89 in all: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). So a movement below is a movement on the 19 arms this run and Run 25 both give a corrected time, on two builds of one recipe a day apart, and it is a roster change and an evening and not a code change.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table gained seven rows and lost one**: `mut-odo-vecdims` reads 0.051 with **seventeen** timed arms below it and **none** level --- where Run 25, over its eighteen shapes, read eleven below and none level. The seventeen are the three `libunord` arms at 0.000, `lib-stage2-short` at 0.027, `mut-odo-vecdims-add-in-leaf-u2` with `lib-stage2-lean`, `lib-stage2-disp` and `lib-stage2` at 0.028, `-u2-down` and `lib-stage1` at 0.029, the two pointer fills at 0.030, `-u1` at 0.031, `-add-in-leaf-down` at 0.034, `liblist-stage1` and `canon-vecdims` at 0.049 and `liblist-stage2` at 0.050. **The ceiling that ran for eight runs still cannot be read at all**: `bq-scan-rem-gm-mulback`, the denominator, is parked, and what stands where it stood is claim 10 inside the family. **And the `alloc` column is Run 25's to the digit once the shape set is pinned**, which the first paragraph above reads back --- the fills at 1.00x, the three `libunord` arms at 0.00x, the two `liblist` arms at 2.00x, `list` at 22.38x and `bq-expand` at 2.06x, every one of them Run 25's figure over its own eighteen shapes. **What breaks that tier is not this half**: on the HEAD half the two pointer fills read 1.41x and 2.61x where every other fill reads 1.00x, which is the one allocation level either half of this pair moves.

**The pointer fills are what this run was built to read, and what they price is the RATE and not the fill.** `genericFillStrided` in `Data/Array/Internal.hs` is a bang-for-bang port of `mut-odo-vecdims-add-in-leaf-u2`, and the two `-ptr` arms are that fill and its un-unrolled parent rewritten to walk a `Ptr`, a form [dead ideas][dead] refuses for the library and which is timed here as a ceiling: what each parent would reach under an allocator that spilled nothing. **On the basis both reach a ceiling and neither reaches the one predicted.** `-u2-ptr` reads **0.9479** of `-u2` where 0.88 was registered, `-u1-ptr` **0.9693** of `-u1` where 0.92 was, and `-u2-ptr` **0.9431** of `-u1-ptr` where 0.90 was; all three spans are killed and all three directions stand, every one past this half's 0.31% floor. **The counted work says the fills did what they were built to do and the conversion did not.** Their corrected instruction ratios are 0.8357, 0.8944 and 0.8605, which are the 0.8356, 0.8945 and 0.8604 the registration derived, to a ten-thousandth --- so the spill each parent pays is exactly the size it was priced at, and between a third and a half of it reaches the clock where [the ceiling](../README.md#the-mutable-ceiling-taken)'s nineteenth reading puts three quarters. **On HEAD every one of the three reverses**: `-u2-ptr` reads 2.6731 of `-u2` at 0 of 19 and 1.9710 of `-u1-ptr` at 0 of 19, `-u1-ptr` 1.3084 of `-u1` at 2 of 19, its counted work goes from 0.8357 to **1.8842**, and its allocation from 1.00x to **2.61x**. So the second compiler does not merely fail to reproduce the ceiling, it turns the `Ptr` form into an allocating one --- which is a finding about GHC HEAD and not about the fill, and it is why a ceiling read on one codegen is not a ceiling.

**The two standing placement controls are still gone with the prune, and this run adds the first reading past four straddlers.** `mut-odo-vecdims-add-in` against the arm it varies, and `build` against `mut-odo`, were the two pairs this file read every run for what a slot costs an identical body; both arms of each went to `Only` on 2026-09-04, and none of the four parkings this run lifted brings one back. What is left that speaks to placement is the six A/A pairs, which are two placements of one strategy and give the floor, and post-run step 0's two twins --- which this run reads on BOTH halves for the first time on this pair. They put the basis half's six tracked fill heads at offsets 0, 0, 0, 24, 0 and 4 and HEAD's at 18, 0, 31, 7, 9 and 26, under the same six function names in the same address order, so the two compilers pin the same group differently rather than carrying different groups. **And the basis half straddles six loops where every binary on disk straddles four**, the twin naming five of them and refusing the sixth, so the offsets are read and the arms that used to turn them into a time are not. **The 3% [the open list][open] has asked about since Run 10 is not readable on this roster either**, and the entry says so.


## What the next run compares against

**Run 27's regime and pair are NOT settled, and nothing here settles them: whoever asks for the run decides, as whoever asked for this one did.** The regime is `-fspec-constr`, as every run since Run 8, and it is the regime the claims decide in; the shipped file does not set the flag ([the ceiling](../README.md#the-mutable-ceiling-taken)). **What this run settles about the pair it ran is that a third asking DID pay, and not for the reason the pair was refused on**: Run 25 said a second compiler was not worth asking a third time on this form, and it was right about the form and wrong about the roster --- the compiler question itself came back exactly as before, twenty-four of twenty-six arms inside a percent in counts, while the two arms this roster invented read 0.6219 in counts, 0.3521 in time and 2.61x against 1.00x in allocation. **So what a compiler pair is worth here is now a function of what is new on the roster, and the rule that follows is the one to carry**: a form timed on one codegen for the first time is a form read once, and this run is the case that says so. **What it leaves live** is two things. The first is in the code: `-u1`'s run-level fill carries a reload of the source base per element that its rank-1 copy does not, and the pointer fills are what a build without that reload looks like --- on the basis they buy between a third and a half of their instruction saving in time, so the reload is real and the rate is not what the ceiling assumes. The second is upstream: **GHC HEAD compiles the `Ptr` form into an allocating one**, and nothing in this roster says why. **What is NOT a candidate** is unchanged: a pair varying the allocation area, closed 2026-08-21, and one varying the roster between its halves, refused because it would break `preflight`'s `check` comparison and both drivers' bench counts.

**The compiler variable was asked a third time on this form, and what it found is not what the first two found.** Run 24 varied the compiler over 52 timed arms and read the two compilers 5.7% and 6.8% apart on the instructions of the two pure mul-back arms and four to six percent apart in time on arms whose count ratios were 1.0000 exactly; Run 25 read the same variable over the pruned roster and found it worth under a percent everywhere. **Every arm that carried Run 24's readings is still parked**, so what this pair can see of the compiler is the fills, the library routes, the unordered entry point and --- new this run --- the two pointer fills, and only the last of those four separates the compilers at all. There it separates them completely: 0.6219 in counts and 0.3521 in time on `-u2-ptr`, against 0.9996 to 1.0089 on every fill and library route and 0.9936 on `bq-expand` and its twins. **And the baseline moved 1.16%, as it moved 1.10% on Run 25**, so the answer this pair gives is an ordering rather than a subtraction for the second run running, and the counted work is what carries the claim about why.

**What Run 26 leaves the next run to read against, and the first item is not a figure.** **The box did not change**, its gate machine check reading +0.72% against the fingerprint Run 25 kept over 18 of 19 shapes and the run's own two main-set processes reading +0.42% and -0.91% against that same fingerprint, worst +3.43%, +4.20% and -3.15% and none past 5%; so absolutes cross from Run 25 to Run 26 freely and the boundary that matters is still the BIOS change before Run 18. **The floor is 0.31% on the basis and 0.46% on the control, and it is ONE figure as it was on Run 25**: the same six A/A pairs, four of them carrying back to Run 10, and the restricted figure over those four is the same 0.31% and 0.46%. A Run 27 margin is judged against those two and against nothing older. **The two columns below may NOT be differenced**: `list` moved **1.16%** between the halves over all 19 shapes, against the 0.7% bar, where Run 25 read 1.10%, Run 24 0.25% and Run 23 0.33%; six of the ten classes are past the bar with it and their blocks say so. What the columns price is the compiler, and the counted-work column says which movements that reaches --- **and on this pair it reaches two arms and no others**, which is the reading a next run should expect to inherit or to overturn.

**Registered with the pair.** Run 26's eight registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, transcribed into Provenance below before that note goes with the pair. **What Run 27 inherits is the same five riders, routine, and one command that drove the whole evening**: the alone legs, the counted-work sweeps over every population, the saturating preamble, the per-sample load fields and `--counts` all ran to form, and `run-evening.sh` drove the gate, the alarm, the sequence and the four rider invocations as one command with the session asleep, which is what it was written for. **What it inherits as a warning is the warning Runs 24 and 25 both left, NOT fired this run and only because it was answered in advance**: items (4) to (7) of this run's registration are seven clauses of older registrations re-registered in their own words, precisely because a clause that defers to another registration inherits that registration's arms and reaches no check --- and every one of the eight items was readable on this roster where Run 25 lost five clauses. `--lint` still refuses a live CLAIM that names a parked arm and still does not read a registration; what stood in for it here was the preparation writing the clauses out.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at this run's own area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 26 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 25 to Run 26, pinned to the eighteen shapes they share. **The table below is this run's own two halves and no earlier run's**, ten strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in the compiler and in nothing else:

| strategy | Run 26 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) | Run 26 (SpecConstr, dead-spot +lookrts, -A32m, GHC HEAD) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.051** | 0.051 |
| `mut-odo-vecdims-add-in-leaf-down` | **0.034** | 0.035 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.031** | 0.032 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.028** | 0.029 |
| `mut-odo-vecdims-add-in-leaf-u1-ptr` | **0.030** | 0.045 |
| `mut-odo-vecdims-add-in-leaf-u2-ptr` | **0.030** | 0.089 |
| `lib-stage1` | **0.029** | 0.029 |
| `lib-stage2-lean` | **0.028** | 0.028 |
| `lib-stage2-disp` | **0.028** | 0.027 |
| `bq-expand` | **0.111** | 0.113 |

**A published geomean is over the same 19 shapes, and two halves of one SpecConstr run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered, which is what an -O1 reading cannot do at an 8% baseline shift. **THE TABLE ABOVE IS NOT SUCH A PAIR**, `list` having moved 1.16% on this run's main set, so the two columns are ORDERED and not differenced: six of the ten rows are higher on the HEAD half, three print the same at three decimals, and one --- `lib-stage2-disp` --- is lower, while the two pointer fills are higher by half again and by three times over, which is not the baseline and is the run's own finding. Read the direction and the order --- and the direction is the baseline's until it is cancelled: `--compare --bridge`, which reads each arm against `list` in its own run, puts the twenty-five arms it covers at a geomean of 0.9242 with five outside its 3.3% drift band. Take the size from the counted work, and note that the two rows the pair was built to separate --- `-u1` against its two neighbours --- are read WITHIN each half, where the baseline cancels exactly. **A pair that varies the allocation area is the other exception, and its two halves may never be subtracted from each other.** `list` moved **9.20%** between Run 14's halves, **5.13%** between Run 15's and **16.51%** between Run 16's. **Run 16's is the largest of the three and was registered to be the smallest**, on the reasoning that its two halves both sit at enlarged areas where the earlier two each crossed the default --- a prediction refuted by its own run, and the refutation is the finding: what moves the baseline is not the distance from the default but the in-process deflation, which at roster scale is worse at 64 MB than at 32 MB by more than the whole default-to-32 MB step was worth. So the exception widens rather than narrowing, and it covers every pair that varies the area at all. Every cell of such a pair's second column is scaled by a denominator the pairing moved: read it for the pairing's direction, take no strategy quality off it, and read the arm-by-arm comparison at the head of this file instead, which divides absolutes rather than ratios.

**The HEAD half's own standings on the arms this run's roster brought, which no table here carries, every published table being the basis half's.** Read off the HEAD half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u1` against `mut-odo-vecdims-add-in-leaf-down` **0.9566** at 16 of 19 (0.9556 at 18 of 19) and `-u2` against `-u1` **0.9648** at 16 of 19 (0.9644); `-u2` against `mut-odo-vecdims` 0.6467 (0.6438) and against `-u2-down` 1.0019 (0.9972); `lib-stage1` against `-u2` 1.0417 (1.0386); `lib-stage2-lean` against `lib-stage1` **0.9361** at 15 of 19, sign p 0.019 (0.9574 at 9 of 19, p 1) and against `-u2` 0.9751 at 14 of 19 (0.9943 at 11 of 19); `lib-stage2-disp` against `lib-stage2-lean` 1.0071 (1.0007); and `liblist-stage2` against `liblist-stage1` 0.9560 at 16 of 19, p 0.0044 (0.9656 at 12 of 19, p 0.36). **Every ordering the basis half reads holds in direction on the second compiler --- except the three the pointer fills are in.** `-u1-ptr` against `-u1` reads **1.3084** where the basis reads 0.9693, `-u2-ptr` against `-u2` **2.6731** against 0.9479 and `-u2-ptr` against `-u1-ptr` **1.9710** against 0.9431, at 2 of 19 and 0 of 19 twice: the only three orderings on this roster that the two compilers disagree about, and the run's largest finding. Of the rest, the two places they differ in strength are the lean dispatch against the shipped route, which HEAD reads two points wider and at a sign test that separates where the basis's does not, and `liblist-stage2` against `liblist-stage1`, the same shape --- on a half whose baseline is a percent above the basis's, so the direction is the reading and the two points are not.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section](../README.md#per-shape-where-the-geomean-hides-the-ordering). The two tables below were regenerated on 2026-09-04 from this run's own JSONs in the per-shape form the README describes; the fourteen-column form they carried until then, and the installer's membership note of that day, are in this file's history.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 5.96 us | 0.084 | `libunord-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2` 0.060 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.16 us | 0.095 | `libunord-stage3` 0.042 | `mut-odo-vecdims-add-in-leaf-u2` 0.073 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 114 us | 0.068 | `libunord-stage3` 0.003 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.041 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 365 us | 0.048 | `libunord-stage3` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `gather48-src-50` | 3 | 22500 | 432 us | 0.053 | `libunord-stage1` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `stretch-coprime-r7` | 13 | 60060 | 1.02 ms | 0.034 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.47 ms | 0.058 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.028 |
| `stretch-primes` | 89 | 250357 | 3.96 ms | 0.029 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 15.9 ms | 0.045 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `vgg-14-c512-k3` | 3 | 903168 | 18.7 ms | 0.058 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 18.5 ms | 0.035 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `stretch-inner256` | 256 | 1750784 | 33.5 ms | 0.031 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u1` 0.027 |
| `stretch-pow2stride` | 64 | 1769472 | 29 ms | 0.123 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-down` 0.119 |
| `stretch-r5-8x432` | 8 | 1769472 | 33.7 ms | 0.032 | `lib-stage2-short` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-square-1341` | 1341 | 1798281 | 30.7 ms | 0.086 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.070 |
| `stretch-bigstride` | 3 | 1800000 | 49.6 ms | 0.035 | `lib-stage2-short` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 37.7 ms | 0.063 | `libunord-stage1` 0.000 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 39.2 ms | 0.023 | `canon-vecdims` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-wide-2xM` | 2 | 1800000 | 37.6 ms | 0.062 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 865 us | 0.033 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 27.1 ms | 0.022 | `lib-stage2-short` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 37.3 ms | 0.063 | `lib-stage1` 0.023 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.44 ms | 0.058 | `lib-stage2-short` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 3.91 ms | 0.022 | `lib-stage2` 0.013 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 46 ms | 0.037 | `libunord-stage3` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.012 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 40.5 ms | 0.023 | `lib-stage2` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.01 ms | 0.023 | `libunord-stage3` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.020 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.07 ms | 0.029 | `lib-stage2-short` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.07 ms | 0.029 | `lib-stage2-short` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `block-run64-page` | `block` | 64 | 131072 | 2.16 ms | 0.035 | `lib-stage2-short` 0.029 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.06 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.020 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 866 us | 0.033 | `lib-stage2-short` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 865 us | 0.032 | `lib-stage2-short` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 27.2 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 27.4 ms | 0.025 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.15 ms | 0.030 | `libunord-stage3` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.15 ms | 0.030 | `libunord-stage3` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.43 ms | 0.059 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.028 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 27.1 ms | 0.027 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 30.3 ms | 0.053 | `lib-stage2-disp` 0.046 | `mut-odo-vecdims-add-in-leaf-u1` 0.039 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 114 us | 0.067 | `libunord-stage3` 0.003 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.041 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 434 us | 0.053 | `libunord-stage3` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `rev-primes` | `rev` | 89 | 250357 | 4.04 ms | 0.029 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `runs-65536` | `runs` | 65536 | 1769472 | 25.6 ms | 0.028 | `libunord-stage2` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-16384` | `runs` | 16384 | 1785856 | 25.8 ms | 0.028 | `libunord-stage2` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-4096` | `runs` | 4096 | 1798144 | 26.3 ms | 0.028 | `libunord-stage2` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-1024` | `runs` | 1024 | 1799168 | 26.5 ms | 0.028 | `lib-stage2` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-512` | `runs` | 512 | 1799680 | 26.5 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-256` | `runs` | 256 | 1799936 | 26.2 ms | 0.029 | `lib-stage2` 0.029 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.027 |
| `runs-7` | `runs` | 7 | 1799994 | 29.9 ms | 0.038 | `lib-stage2` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-2` | `runs` | 2 | 1800000 | 37.6 ms | 0.064 | `lib-stage2-short` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.027 |
| `runs-3` | `runs` | 3 | 1800000 | 33.7 ms | 0.052 | `lib-stage2-short` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 32.2 ms | 0.046 | `lib-stage2-short` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `runs-5` | `runs` | 5 | 1800000 | 31.2 ms | 0.043 | `lib-stage2-short` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-9` | `runs` | 9 | 1800000 | 29.7 ms | 0.034 | `lib-stage2` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-96` | `runs` | 96 | 1800000 | 27 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 26.7 ms | 0.030 | `libunord-stage2` 0.026 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `scaled-r5` | `scaled` | 13 | 15015 | 249 us | 0.033 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 956 us | 0.027 | `lib-stage2-short` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 4.77 ms | 0.034 | `canon-vecdims` 0.032 | `mut-odo-vecdims-add-in-leaf-down` 0.032 |
| `small-patch-k5` | `small` | 5 | 150 | 2.75 us | 0.083 | `libunord-stage1` 0.068 | `mut-odo-vecdims-add-in-leaf-u2` 0.061 |
| `small-bcast32` | `small` | 32 | 256 | 4 us | 0.057 | `lib-stage2-lean` 0.068 | `mut-odo-vecdims-add-in-leaf-u2` 0.053 |
| `small-flat64` | `small` | 64 | 256 | 4.07 us | 0.065 | `lib-stage2-disp` 0.014 | `mut-odo-vecdims-add-in-leaf-u2` 0.064 |
| `small-patch-r5` | `small` | 4 | 256 | 4.96 us | 0.092 | `lib-stage2-short` 0.095 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.071 |
| `small-row96` | `small` | 96 | 384 | 5.94 us | 0.046 | `lib-stage2-lean` 0.058 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.045 |
| `window-28x28-k5` | `window` | 5 | 14400 | 260 us | 0.045 | `lib-stage2-short` 0.021 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 879 us | 0.094 | `lib-stage2-lean` 0.012 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.28 ms | 0.057 | `libunord-stage3` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 8.98 ms | 0.056 | `libunord-stage3` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.39 ms | 0.055 | `libunord-stage3` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `window-128x128-k7` | `window` | 7 | 729316 | 14.1 ms | 0.032 | `libunord-stage3` 0.016 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.015 |

**One row to read first, and it is a property of the shape set and not of any arm**: `stretch-inner1`, the `sInner` 1 shape that made a population of its own out of anything special-casing a unit dimension, was retired from timing on 2026-09-04 and `cnn-L1-6x6-c1` came back on 2026-09-05, so no figure below excludes a shape and every row of the main table is over all nineteen but three. **The three are the `libunord` arms**, whose cells are the shared forcing pass wherever the one-block test fires: ten cells sink on the basis and eleven on HEAD, all of them theirs, so `libunord-stage1`, `-stage2` and `-stage3` are geomeans on 15, 16 and 16 of 19 shapes on the basis and on 16, 15 and 15 on HEAD. The head of this file prices what that means --- a net four parts in a hundred thousand of its cell at the smallest, whose sign is arithmetic --- and the consequence for reading the table is that those three rows alone are over a smaller population than the rest, that the arm this run added sinks with its two siblings rather than more often, and that the `lib-stage2` family sinks nowhere on either half.


## The claims the next run should test

**Run 26's verdicts first**, since a run reports breaks rather than re-deriving the table. **Both live claims held.** Claim 10, the manifest's only live ordering, reads **0.6438 on the 9.12 basis and 0.6467 on the HEAD half**, and pinned to these nineteen shapes Run 24's two halves read 0.6464 and 0.6454, so four figures across two runs and two codegens span three thousandths. Claim 7 held on every level it has, its pinned eighteen shapes giving back Run 25's five figures exactly --- and it gained a reading it did not ask for, the two arms this run added joining the 1.00x fill tier on the basis and breaking it on the other half. Every arm claim 10 names is timed, and `--pair` recovers any retired ordering in one call whenever it is wanted.

**The eight retired claims are not re-read here.** Claims 3, 4, 5 and 9 left the manifest at Run 19's write-up, on a sweep in which all thirteen held on both of that run's halves; claim 8 left on 2026-08-29 on a decision, its subject the pure tier being no longer what ships; claims 2 and 6 left on 2026-08-28 with the parking of the arms their surviving links turned on; and claim 1 left on 2026-09-04 with the prune, which parked every rung below its top. The numbered items below say what each was in a clause. This run does not re-derive any of them and quotes none as its own --- and for claim 1 that is still the only way it could be read at all, three of the four arms its links name being untimed, which is what retiring it gave up and what a run that re-times them would get back. **Eight is the count, and it was written as seven for two runs**: the enumeration above skipped claim 8, which the numbered list below has carried as retired since 2026-08-29.

**Claim 7 held on every level it has, and its pinned shape set did not have to move this run.** The claim reads its levels over the eighteen main-set shapes it was re-cut to on 2026-09-05; this run times nineteen, `cnn-L1-6x6-c1` having come back from the retired list, and the nineteenth sits outside the pin. **Read on the eighteen the levels are unmoved and four runs agree to the digit**: the mutable fills at **1.00x**, the list consumers at **2.00x**, `bq-expand` at **2.06x**, `list` at **22.38x** and the `libunord` arms at **0.00x**, every one of them Run 25's, Run 24's and Run 23's figure over the same eighteen. What this run's own nineteen show instead --- 2.11x and 22.64x --- is the median's population and not what any arm allocates, which is the fourth run running that this file has had to say. **The new arms land where the claim would put them, on the basis**: `libunord-stage3` at 0.00x with its two siblings, and the two pointer fills at 1.00x with the family. **And the one place the claim would break is the other half**: on HEAD, `mut-odo-vecdims-add-in-leaf-u1-ptr` reads **1.41x** and `-u2-ptr` **2.61x**, so a `Ptr`-walking fill that allocates nothing under 9.12 allocates one and a half and two and a half result vectors under GHC HEAD. The claim is stated on the published basis half and so is not broken by that; what the reading is, is the allocation column catching a codegen difference the time column would have left as a mystery. The class blocks read the tiers unbroken in all ten, `bq-expand` running 1.03x on `flip` and `block` to 2.81x on `window` where that class's own `m` shows through, and `list` 19.19x to 24.76x. **One class breaks the fill tier and it is the class built to make it break**: on `small` the five non-pointer `mut-odo-vecdims` fills read **1.27x**, the two pointer fills 1.44x, `canon-vecdims` 1.40x and `bq-expand` 1.34x, against Run 25's 1.23x on the same class.

**Claim 10 is the manifest's only live ordering and this is the second run to read it as a claim.** What the leaf fusion buys over the family root: `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims`, registered 2026-09-04 with the prune in claim 1's place, and read here at geomean **0.6438, 18 of 19, sign p 7.6e-05** on the basis with the HEAD half at **0.6467, 18 of 19** --- a margin no floor here approaches, so what the registration asserts is the direction and the win count and not the size. Pinned to these nineteen shapes Run 24's two halves read 0.6464 and 0.6454, so the ordering has now been read on three runs, three rosters and two compilers within three thousandths, and the one shape it does not win, `stretch-pow2stride`, is the same shape on all four readings.

**Readings:** `mut-odo-vecdims-add-in-leaf-u2` / `mut-odo-vecdims` 0.6438, 18 of 19, sign p 7.6e-05. 1 of 1 registered ordering held.

Restated as the predicates the next run checks, and carrying no reading of its own: the figures each was last measured at are in the `Readings:` paragraphs above, so an entry here changes when a claim is re-aimed and not when a run moves a margin. **All of them are `-fspec-constr` claims, the regime they are read in** --- the shipped file does not set the flag, measured irrelevant to the shipped family --- so they are the set that decides, and a run at -O1 would test Run 7's instead, the two differing in more than their numbers. **They are read in the caller's allocation regime now**, every figure here being taken at the `-A32m` Run 16 promoted to the basis and every horde-ad test and benchmark bakes since 2026-08-21; the gap Runs 14, 15 and 16 priced against a prevailing `-A1G` is closed, and no claim below needs qualifying by it. **And all of them are read against a measured drift band rather than a layout span**, which is what the last three runs bought. A roster *order* change alone moved arms 0.966 to 1.142 between Run 9 and Run 10, and that is what a margin used to have to clear; with the layout pinned, a repetition moves an arm by at most 3.3% on Run 11's reading and 2.1% on Run 23's, most of them by under a point, so a margin above a few percent is now evidence of a strategy. **Run 13 is the first pair here to hold every tracked loop at one offset in both halves**, which is what lets its arm-by-arm comparison be read as the package costing nothing rather than as two terms cancelling. A claim resting on an arm whose own loop the shim skipped --- `list`'s, which is library code --- is still decidable nowhere until that loop is read. **And the pinning claim is measured only in its weak form**: adding `mut-flat-gm-nosum` left every tracked loop at the same address, but a `Force` arm reuses a rostered function and emits no code for emission order to move. The strong form wants an arm that emits its own, and until one is added the claim covers additions that cost nothing to place.

**The list DID need reading against a moved roster this run, and what moved is the list and not a reading.** Eighteen arms went to `Only` and two landed, so claim 1's four links lost three of the four arms they name and the claim retired with them; claim 7 lost two of its levels outright and had to be re-pinned to a shape set that still exists; and claim 10 is a first reading. **What the roster raises is what Run 22 and Run 24 both raised, and this run raises it further rather than smaller**: seventeen arms read below `mut-odo-vecdims` where Run 25 had eleven, and the shipped library route is still among them, at 0.029 against 0.051. That is left to the next run rather than re-aimed here, and it is [under the recommended tasks](../README.md#recommended-tasks-after-run-26).

1. **Retired 2026-09-04** with the prune, which parked every rung below `mut-odo-vecdims`; its last reading is Run 24's, above, where it held on both halves. What it was: `mut-odo-vecdims` < `mut-flat-gm` < `bq-mut-runs-gm-mulback` < each of `bq-scan-rem-gm-mulback` and `bq-odo-gm-mulback`, the whole ordering read on unconditional arms --- **the ladder the `needs` column draws**, each link pricing one thing the implementation is allowed to ask for. **The foot rung retired 2026-08-29**, on a decision rather than on a reading: it registered the two arms needing nothing at all as a tie, so that either was what would ship if the mutating `Vector` method were refused upstream, and it had read as a tie on every run that carried it. Both arms stay rostered and stay in the rung above, `--pair bq-scan-rem-gm-mulback bq-odo-gm-mulback` recovers the reading whenever the upstream answer wants it, and its last is Run 21's, above. The middle link is the one the README has seen a layout term move --- 0.9708 at 15 of 24 on Run 10's unaligned half against 0.9293 at 22 on its aligned one --- and on a placed layout it has now read the aligned figure five runs running. The ordering has survived nine runs, two changes of basis, two repetitions and three compilers.
2. **Retired 2026-08-28** with the parking of `offtab`, the arm its surviving link turned on --- the other, `bq-scan-rem-gm-mulback`, is timed still. What it asked, since the settlement of 2026-08-24 re-aimed it, was where the arms needing something other than the fix sit --- `offtab` behind `bq-scan-rem-gm-mulback`, a mutable `Int` scratch priced against needing nothing --- and its last reading is Run 20's, above. **Its second link, `bq-expand` behind `mut-odo-vecdims`, had gone already, 2026-08-26** on the reading above, its condition having been spent since 2026-08-24 --- what it priced was the branch's own code against its replacement, and the replacement is now the branch's own code. It was the widest ordering the manifest carried, and `--pair` recovers it, both arms staying rostered and timed.
3, 4, 5. **Retired at Run 19's write-up**, on a last reading in which all three held on both compilers, and for the reasons in the settlement paragraph at the foot of this section. Their numbers are left standing here rather than reused: a verdict recorded against *claim 4* in an earlier run's file still means what it said. What they were, in a clause each, so that nothing is recoverable only from a manifest diff --- claim 3, that a mul-back output pays on the `bq-expand` build; claim 4, that the scan build ties its own build control while beating `bq-expand`, a tie Run 17 promoted to an ordering and three runs then read as one; claim 5, that `bq-expand` beats `bq-gen`, whose refutation of the generate-per-element build stands on Runs 7 and 8. **What none of the three could still foreclose is the point**: every one asks where `bq-expand` sits among arms nothing ships, on a branch whose fix is the `mut-odo-vecdims` family. The arms all stay rostered and timed, so any of these orderings is one `--pair` call away.
6. **Retired 2026-08-28** with the parking of `gen-quotrem`, the arm its only link turned on. What it asked: `gen-quotrem` ties `list`, the first attempt's arithmetic ceasing to be dearer than the list's allocation once the flag takes its own allocation to 1.00x against the list's 23.5x --- the mixed picture this suite exists to have refuted, arriving by a route nobody proposed. Its last reading is Run 20's, above; the `cm-gather` < `list` half was untimed throughout and stands as Run 8's. What goes with it is the standing advice to check `list` as an anchor before blaming a strategy, which is now nobody's claim and is why the machine check reads `list` net per shape every run.
7. Allocation, median multiples of the result **over a pinned shape set**: the mutable fills 1.00x, the list consumers 2.00x, `bq-expand` 2.06x, `list` 22.38x, every one of them read over **the eighteen main-set shapes this roster times**, which are a strict subset of the 24 Runs 15 to 23 shared --- **re-pinned 2026-09-05, six of those 24 having been retired from timing the day before**, so the old population no longer exists and the predicate could not otherwise be evaluated; re-listed 2026-08-28 with the parking, and 2026-09-04 with the prune, which parked the two tiers between the fills and `bq-expand` outright and left the 2.00x one to the two `liblist` arms, named in the paragraph above. Every level has reproduced over the eighteen on Runs 23, 24 and 25, which is what makes this the claim to check first when anything else moves. **The pinning is the predicate and not a caveat on it**: Run 24's published column reads 2.40x for `bq-expand` on its own 26, 2.35x on the 24 and 2.06x on these 18 --- so a level compared across two shape sets moves for a reason that is neither a code change nor a slot, and only a level that moves ON ONE POPULATION is a code change. **Read the levels and the cells as two questions**, which Run 19 is the run that separated: its levels all returned while the cross-half cell agreement fell to 1016 of 1080, where the 9.14 pair had 1072 --- so a compiler can reallocate within a tier without moving any tier, and only the cell count sees it.
8. **Retired 2026-08-29**, on a decision and one day after its last re-aiming. What it asked: that every pure arm in the fast tier run its output through the single in-order `vGenerate` over an `m`-length table, so a `bq-*` arm falling behind loses on its table build and not on its output. Its last reading is Run 21's, which found it true and its subject smaller again --- nine arms at or below `mut-odo-vecdims` and not one of them a `bq-*`, so the structure it described governed a tier starting a third of the way down. What retires it is that subject: the pure tier is no longer what ships, and a claim quantified over it was being re-aimed and re-read every run to say so. It was also the one claim with no named invocation, read off the table by eye throughout, so nothing mechanical goes with it.
9. **Retired at Run 19's write-up with 3, 4 and 5**, and for a reason of its own worth keeping: its per-shape half was answered rather than abandoned. `bq-expand-b`'s two best cells were `stretch-inner1` and `stretch-wide-2xM` in every run from Run 8 to Run 16 --- the rank-2 views with one huge outer dimension where seeding from `enumFromStepN` replaces the whole `concatMap` build --- until **Run 17 read `stretch-inner1` and `stretch-square-1341` instead**, which registered the follow-up. Runs 18 and 19 both read the original pair back, on four compilers between them, so the excursion was one run's and the mechanism is cleared of picking the wrong shapes. The geomeans were never the stable part, and the series is why: across Runs 8 to 13 `bq-expand-b` / `bq-expand` read 0.996, 0.9678, 0.9943, 0.9819, 0.9923 and 0.9909, its sign test crossing into significance only on the last of them, while `bq-expand-zf` / `bq-expand` went 3.6% behind, then level at 1.0028, then 1.0325, 1.0197, 1.0256 and 1.0265. Both series were closed at Run 13 and not extended per run, and a closed series in a live manifest is maintenance without a question --- which, with the follow-up spent, is the whole case for retiring it.
10. `mut-odo-vecdims-add-in-leaf-u2` < `mut-odo-vecdims`, the shipped fill against the family root --- **what the leaf fusion buys**, the one thing the roster still prices that the library has taken. Registered 2026-09-04 with the prune, in claim 1's place.

**What the parking of 2026-08-28 did to this set**, recorded in prose because a live item carries a predicate and no reading, and because naming a parked arm inside one is now a `--lint` failure. Claims 2 and 6 retired with `offtab` and `gen-quotrem`, the arms their surviving links turned on. Claim 7 lost three of the arms that carried its levels --- `gen-quotrem` at 1.00x, `bq-mut` at 1.33x and `offtab` at 2.00x, every figure of theirs ending at Run 20 --- but no level with them: `offtab-scan-rem` carries the 2.00x tier, the mutable fills the 1.00x and the scan family the 1.33x, so the list is the same six levels it was. Claim 8 was re-aimed off `bq-expand-zf` and `bq-gen`, its span running from the leading tier to `offtab-scan-rem` instead; the readings above are the last that name the two. Nothing was retired on a reading here, the parking being a decision about what is worth a bench, and the two claims that went had no link left that a run could measure.

**What the prune of 2026-09-04 did to this set** ([what the benchmark does](../README.md#what-the-benchmark-does)), read on the first run after it. Claim 1 retired, every rung below its top parked, and claim 10 is registered in its place, the one live ordering. Claim 7 lost the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x, which have no timed arm left, and keeps `offtab-scan-rem`'s 2.00x through the two `liblist` arms; and it lost its pinned population as well, six of the 24 shapes it named having been retired the same day, which this run re-cut to the eighteen that remain. The ruling of the same day on the short bodies parked `lib-stage2-short` and `lib-stage2-short-lean`, whose last readings stand as Run 24's. The class properties keep their arms, `mut-odo-vecdims` and `bq-expand` both staying timed --- **and property 1's two counter-examples are down to one**: `gen-unsafe`, which carried a `worst` above 1 in seven of ten populations as the baseline's own control, is parked, so the only arms above 1 now are the library-shaped ones and the `list` twins.

**Two homes, and which carries what.** Each live claim has a prose paragraph here and a numbered predicate at the foot, and they divide: the PROSE carries this run's figures and what moved, the ITEM carries the predicate the next run checks and no reading at all. A RETIRED claim keeps its numbered item, which says in a clause what it was, and gets no prose paragraph --- Run 20 wrote two and they restated their own items clause for clause, because stripping the figures had removed the only thing that distinguished them. Each ordering is one line of `--claims`, whose manifest now carries the registered expectation --- the direction of the geomean, a tie by sign test, or claim 9's two best shapes --- and prints HELD or BROKE beside the paired geomean, interval and sign test. `--claims --in-place` then installs that arithmetic as each claim's `Readings:` paragraph above, so a run no longer transcribes it at all; what stays the reading's is whether a HELD margin moved and whether a movement clears the floor, and a BROKE is what obliges the paragraph above its reading to be rewritten rather than requoted. **A claim with no named invocation is a gap in this list, not a claim to be checked by hand**: where a session has to invent the computation it will invent a wrong one, which is how claim 7 came to be read off the raw fitted bytes, explained by a mechanism the previous pair refutes, and then "corrected" onto a rounded print. It has `--compare --alloc` now, and no live claim is without one since claim 8 retired. **The general form, and it is a standing instruction rather than an observation: if a write-up hand-rolls a script to answer something the reader should answer, that is a defect report against the reader** --- fix it there, before the sentence it was written for, or the next run invents its own wrong version. **Two riders, both bought on Run 19.** A new MODE joins the guards its siblings already have, and is checked against them rather than written beside them: `--counts` shipped able to be given without the `--compare` it reads, silently printing the default table and exiting 0 --- the unread-flag family exactly, added next to four sibling readings of `--compare` that were every one of them already guarded, and joining none. **And an instrument may be BACKED OUT, which is not a failure of the write-up but a result of it.** Measure what it flags before shipping it: the obvious mechanical repair for stale paragraphs was built and returned 100 for the four that mattered, so it went, and the refutation with its numbers is under the tasks heading --- worth more than the mode, since it stops the next session building the same thing. A report that never empties is one nobody reads, which this file already knows about hints.

**And for each stride class, the same three properties, now carrying Run 26's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1.** Held in every one of the eleven populations, on both halves, in every regime, roster and layout the README has run --- so it was never slower than the `list` it replaced, on any shape of any class the library can produce. This is the property the classes exist to test, no geomean can state it, and a break would be the one result here to bear on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims` since 2026-08-22: **on Run 26 its worst is 0.123 on the main set and 0.094 in a class (`window`), both read on the basis half, with the HEAD half at 0.125 and 0.096** --- so the property holds for the arm decided, on both compilers, and neither end is within a tenth of 1: the main-set end is a factor of 8.1 inside it and the class end 10.6. Both halves are quoted because one is not enough: Run 18's entry here read a floor-level figure from whichever half happened to be lower, which is the defect this phrasing exists to prevent.

Beside property 1, and the case has simplified since the prune of 2026-09-04 parked the arm that used to be half of it: **only one thing breaks it now, and it is not a baseline control.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. What is left is the other break, and it is a fill the library would ship. **The library-shaped arms break it on `runs` alone and all at `runs-2`**, five of the ten this roster now carries and on both halves: `libunord-stage1` at **1.228**, `liblist-stage1` 1.223, `lib-stage1` --- the shipped route --- at **1.209**, `liblist-stage2` 1.066 and `libunord-stage2` 1.065 on the basis, and 1.201, 1.188, 1.186, 1.050 and 1.047 on HEAD. **`libunord-stage3`, the arm this run added, is not among them**, so the unordered entry point's third stage is the first library-shaped arm to keep `worst` under 1 on `runs`. So on 900000 runs of two elements five of the seven library-shaped arms are slower than the `list` baseline they replace --- every route that takes a slice per run at that length --- while `lib-stage2-lean` and `lib-stage2-disp` fill every run whatever its length and are the two that are not. `runs-3` is the only other cell of any population where a library-shaped arm crosses 1, and three do there on the basis: `libunord-stage1` 1.0215, `liblist-stage1` 1.0145 and `lib-stage1` 1.0133, against 1.0137, 1.0091 and 1.0014 on HEAD. **The property is stated of `mut-odo-vecdims` and holds of it; it does NOT hold of what the library actually calls.** A reader taking property 1 as clearance for the code that ships is reading it wider than it is stated, and the class that catches the difference is `runs`.

2. **The top of the table keeps its order**: `mut-odo-vecdims` fastest, `bq-expand` behind it. **The first clause breaks in ALL TEN class populations this run --- the main set is the eleventh and is counted separately throughout this section --- where Run 25 broke nine and its tenth, `block`, was led by a `mut-odo-vecdims` sibling. This run's `block` leader is `mut-odo-vecdims-add-in-leaf-u2-ptr`, a pointer fill the library's own dead-ideas ruling refuses, so it is not the family's and the tenth breaks with the rest.**

Beside property 2, and every arm named here is timed on this roster: the ten breaks divide four ways. **Four are the unordered entry point's third stage**, `libunord-stage3` leading `rev` at 0.001, `bcastmid`, `window` and `flip` --- 98.69%, 54.60% and 68.10% against those populations' floors of 0.30%, 0.88% and 1.07%, and on `flip` its cell is its own forcing term on two views of five, so that lead is not priced at all. **Three are the pointer fill**: `mut-odo-vecdims-add-in-leaf-u2-ptr` leads `runs`, `block` and `compose` by 24.61%, 16.68% and 38.03% against 2.89%, 0.39% and 0.44%, and it is the arm the dead-ideas ruling refuses, so none of those three is the family's. **Two are the branch's driver**: `lib-stage2-lean` leads `small` by 15.93% against 0.52% and `lib-stage2-disp` leads `bcast` by 40.43% against 0.71%. **And one is the shipped route**: `lib-stage1` leads `scaled` by 11.31% against this run's 2.38% floor. **What moved since Run 25 is which arms do the leading, not whether the clause breaks**: the lean and re-cut dispatches led six classes there and two here, and the two arms that took their places --- `libunord-stage3` and `-u2-ptr` --- are both new to the timed roster this run. **The property's SECOND clause is untouched**: `bq-expand` stays behind `mut-odo-vecdims` in all ten classes on both halves, as it has since the clause was written.

3. **The allocation tiers survive and their ORDER is unbroken in all ten, and every level on the main set is Run 23's and Run 24's once the shape set is pinned --- with one class breaking the fill tier's LEVEL, which is what that class was built to do**: the mutable fills at the result vector, `bq-expand` between 1.02x and 2.81x it, `list` an order of magnitude above. On the main set the fills read 1.00x, the two `liblist` arms 2.00x, `bq-expand` 2.06x and `list` 22.38x, and Run 23's and Run 24's own basis halves give every one of those five over the same eighteen shapes --- the columns those runs published, 2.35x and 23.5x and 2.40x and 23.50x, being their own populations and not their arms. Where a level moves it is the class's own `m` or its own size showing through, exactly as this property warned --- `bq-expand` at 1.03x on `flip` and `block` and 2.81x on `window`, and on `small` **the five non-pointer fills at 1.27x and the two pointer fills at 1.44x**, the per-call constant that dividing by `8*l` hides at a million elements and cannot at 256 --- with the ordering of tiers unbroken in all ten and `list` running 19.19x to 24.76x across them, on both halves. The two `libunord` arms sit below the result vector at 0.00x on the MAIN SET, and in the classes they mostly do not: on six --- `bcast`, `bcastmid`, `window`, `scaled`, `compose` and `small` --- both read about 2.00x, `libunord-stage1` reaching 2.47x on `small`; on `rev` and `flip`, `libunord-stage1` reads about 2.00x while `libunord-stage2` stays under the vector at 0.01x and 0.00x; and on `runs` and `block` they read 1.19x and 1.14x, and 1.42x and 1.31x. So the one-block test's 0.00x tier is the main set's alone, `libunord-stage1` never reaching it in any class, and a class reading of that tier is a different quantity. On a pair whose two halves are two compilers' code this is the property that says the difference is not a change of what gets allocated: every tier but one is identical on both halves, and the 99 main-set cells of 532 that disagree past 1e-4 are one compiler allocating differently INSIDE a tier, which is the distinction Run 19 is the run that separated. **The one tier that is NOT identical is this run's own finding**: the two pointer fills sit at 1.00x with the family on the basis and at 1.41x and 2.61x on HEAD, and the worst disagreeing cell of the 99 is `stretch-wide-2xM`/`-u2-ptr` at 7.50e-01 --- a change of tier and not a change inside one, which is what Run 19's distinction is for and the first time this file has had to use it that way.

**SETTLED 2026-08-24 and APPLIED 2026-08-25, at Run 19's write-up rather than at the settlement**, so that the retiring orderings got one last cross-compiler reading and the retirement is recorded with it --- which is how Run 17 retired claim 4's tie, in prose at its write-up with the manifest taking it the same day. **They got it**: all thirteen held on both of Run 19's halves, and the eight that remain hold on both too, so each of the four retires on a reading rather than on a decision. `CLAIMS` in `read-run.py` carried claims 1, 2 and 6 alone after that settlement, and **claim 1 alone since 2026-08-28**, `offtab` and `gen-quotrem` having been parked. The test applied: an ordering stays only if it forecloses something anyone would propose again *and* can still break. What fails both is a figure, and figures live in the tables above. **Claims 3, 5 and 9 retire outright.** Claim 3 sets one output form against another on a build nothing ships, where every leading pure arm is a `-gm-mulback` already; claim 5's `bq-expand` / `bq-gen` says of itself that the refutation stands on Runs 7 and 8, and claim 6 keeps that family guarded through `gen-quotrem`; claim 9's two series are closed at Run 13 by this section's own words, and a closed series in a live manifest is maintenance without a question. **Claim 4 retires with them, its tie moving into claim 1** --- and what goes with it is the one place the manifest reads a *builder* apart from its output, which `--pair` recovers whenever it is wanted, both arms staying rostered. **Claim 1 becomes the ladder the `needs` column already draws**, gaining `bq-scan-rem-gm-mulback`, the best arm needing nothing at all, between `bq-mut-runs-gm-mulback` and `bq-odo-gm-mulback`: the first ahead of it at **0.9060** and **0.9171** on Run 18's two halves, 19 and 17 of 24, which is what a mutable `Int` scratch buys; and the second **tied** with it at **0.9902** and **0.9936**, 13 and 12 of 24 at p 0.84 and 1, so the two fastest pure arms were indistinguishable and either was what would ship if the mutating method were refused --- the rung this installed, and retired again on 2026-08-29. Its three existing links stay, the middle one redundant with the two new ones and carrying seven runs of history they do not. **Claim 2 keeps its number and changes its question** to where the arms needing something other than the fix sit: `offtab`, which needs only that `Int` scratch, behind `bq-scan-rem-gm-mulback` at **1.36** and **1.44**; and `bq-expand` behind `mut-odo-vecdims` at **2.09** and **2.13**, kept only while `Data/Array/Internal.hs` carried `bq-expand`, and to retire with the three `TODO: retarget` markers, which were one decision with it. **That condition was spent the same day and the link outlived it by two runs**: the file went to `vFillStrided` on 2026-08-24 and the markers went with the prose they marked, but nothing read the condition back, so Run 20 registered and read the link like any other and it retired only on 2026-08-26. Thirteen registered orderings become eight here, and seven when that second link finally goes; claims 7 and 8 stayed unmanifested prose. **The rewriting the ask paired with this was already done**: the eight *What the class says* paragraphs were written to the re-aimed properties at Run 18's write-up, and only the sentence asking for it survived.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 26 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) records every class on BOTH halves**, one process each, in [the sequence](../README.md#making-a-major-benchmark-run), all twenty-two processes in one window. Every table below is the **basis half**'s, the 9.12 one, the half that keeps the lineage; what the second half buys is that the pair's variable --- the compiler --- can be read on a class, with the counted work beside it on every population. **Read across the halves and the two compilers are close everywhere but on one arm.** Of the 260 arm-comparisons the ten classes carry, two (`libunord-stage2` and `libunord-stage3` on `flip`, basis cells of which the correction does not leave positive) sit out the vote and the geomeans, and **154 put the basis faster against 104 the HEAD half**, the ten geomeans running 0.9353 on `bcast` to 0.9559 on `flip` and every one of them below 1. **`mut-odo-vecdims-add-in-leaf-u2-ptr` holds the low extreme in all ten**, 0.2571 on `bcast` at the widest, and the high extreme over all ten is `lib-stage2-lean` at 1.0427 on `window`; take that one arm out and the classes read as the main set does, their counts geomeans running 0.9769 to 0.9886 against its 0.9811. **Six classes have `list` past the 0.7% bar** --- `rev` 1.0088, `window` 1.0232, `runs` 0.9923, `flip` 1.0072, `block` 0.9848 and `small` 0.9857 --- so those six lines are orderings and say so, and the four read for the pair's variable are `bcast`, `bcastmid`, `scaled` and `compose`.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where property 2 breaks the two name different arms and the gap between them is what the break is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks one of [the three properties](#the-claims-the-next-run-should-test) is bolded, and the class's own paragraph says what broke.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 2 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.047 | 0.067 | **`libunord-stage3`** 0.001 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 | 0.30% |
| `bcast` | 3 | 0.036 | 0.063 | **`lib-stage2-disp`** 0.019 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.020 | 0.71% |
| `bcastmid` | 4 | 0.032 | 0.058 | **`libunord-stage3`** 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 | 0.88% |
| `window` | 6 | 0.054 | 0.094 | **`libunord-stage3`** 0.017 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 | 1.07% |
| `scaled` | 3 | 0.031 | 0.034 | **`lib-stage1`** 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 | 2.38% |
| `runs` | 14 | 0.032 | 0.064 | `lib-stage2-disp` 0.027 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.026 | 2.89% |
| `flip` | 5 | 0.036 | 0.059 | **`libunord-stage3`** 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 | 0.69% |
| `block` | 5 | 0.027 | 0.035 | `lib-stage2-lean` 0.025 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.023 | 0.39% |
| `small` | 5 | 0.067 | 0.092 | **`lib-stage2-lean`** 0.056 | `mut-odo-vecdims-add-in-leaf-u2` 0.058 | 0.52% |
| `compose` | 4 | 0.027 | 0.033 | `lib-stage2-lean` 0.018 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.017 | 0.44% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- that being property 2's first clause breaking. **On three rows --- `runs`, `block` and `compose` --- the bold sits in the CEILING column instead**, the fastest arm there being `mut-odo-vecdims-add-in-leaf-u2-ptr`: the reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break, and this file overrides it for that arm alone, the pointer fill being the design the dead-ideas ruling refuses rather than a form the family could ship. So the table's bolding and the head's count of ten differ by convention and not by measurement, and this sentence is where the two are reconciled.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.12* | *134* | *2.52x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.14* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *157* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *157* | *0.00x* |
| libunord-stage3 | 0.001 | 0.003 | 0.04 | 157 | 0.01x |
| libunord-stage2 | 0.001 | 0.003 | 0.05 | 157 | 0.01x |
| lib-stage2-short | 0.023 | 0.029 | 0.09 | 148 | 1.01x |
| lib-stage1 | 0.025 | 0.047 | 0.07 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.025 | 0.045 | 0.06 | 146 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.026 | 0.046 | 0.08 | 146 | 1.00x |
| lib-stage2-disp | 0.026 | 0.034 | 0.11 | 146 | 1.01x |
| lib-stage2 | 0.026 | 0.034 | 0.08 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.041 | 0.16 | 147 | 1.01x |
| lib-stage2-lean | 0.026 | 0.034 | 0.09 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.030 | 0.042 | 0.17 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.047 | 0.15 | 146 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.032 | 0.048 | 0.06 | 144 | 1.00x |
| liblist-stage2 | 0.040 | 0.045 | 0.24 | 142 | 2.01x |
| liblist-stage1 | 0.044 | 0.058 | 0.35 | 142 | 2.01x |
| libunord-stage1 | 0.045 | 0.061 | 0.26 | 142 | 2.03x |
| *mut-odo-vecdims-aa* | *0.047* | *0.067* | *0.08* | *137* | *1.00x* |
| **mut-odo-vecdims** | **0.047** | 0.067 | 0.08 | 137 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.047* | *0.067* | *0.08* | *137* | *1.00x* |
| canon-vecdims | 0.048 | 0.056 | 0.07 | 137 | 1.01x |
| *bq-expand-aa-distant* | *0.102* | *0.174* | *0.13* | *128* | *2.52x* |
| bq-expand | 0.102 | 0.174 | 0.13 | 128 | 2.52x |
| *bq-expand-aa-adjacent* | *0.103* | *0.175* | *0.12* | *128* | *2.52x* |
| *list-aa-distant* | *0.999* | *1.002* | *0.30* | *86* | *23.43x* |
| list (baseline) | 1.000 | 1.000 | 0.23 | 86 | 23.43x |
| *list-aa-adjacent* | *1.004* | *1.006* | *0.19* | *86* | *23.43x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 0.9970, worst cell 1.24% on `rev-cnn-L1-24x24-c1`, and 6 of 6 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `rev-gather48-src-50`, its interval covering 1. The in-situ term reads 1.0093, 1.0149 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9971, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h7m50s, peak 96 MiB in use, 24 MiB max residency; the reader reads 30 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.19 ms per call raw, 4.04 ms net.

**Per shape, in the lead's order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.067/0.053/0.029

**Across the halves:** 8 of the 26 arms are faster on this half and 18 slower, at a geomean of 0.9523, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.3199 to `liblist-stage2` at 1.0420, with `list` itself at 1.0088. **The baseline moved 0.88% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1 and 3 hold for `mut-odo-vecdims` --- `worst` 0.067 against a `list` it never loses to, and the tiers at 1.00x, 2.52x and 23.43x --- and property 2 breaks outright, `libunord-stage3` leading at 0.001 against this class's 0.30% floor, a 98.69% margin. That is dispatch and not filling: every stride here is negated and the one-block test fires on all three views, so both unordered arms return a single slice and their cells ARE the forcing pass. **Registration (3) reads the pair inside the floor here, as it predicted**: stage three over stage two reads 0.9903, 0.9969 and 0.9995 raw on the three views, which is the same slice taken twice. `mut-odo-vecdims-add-in-leaf-u2` leads its `-down` control by the widest margin of any population, 0.8579 on the basis and 0.8598 on HEAD. **The cross-half line is not read for the pair's variable**, `list` having moved 1.0088.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.62* | *52* | *1.38x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.30* | *82* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *69* | *0.00x* |
| lib-stage2-disp | 0.019 | 0.027 | 0.53 | 61 | 1.00x |
| lib-stage2-lean | 0.019 | 0.027 | 0.54 | 61 | 1.00x |
| lib-stage2-short | 0.019 | 0.027 | 0.52 | 61 | 1.00x |
| lib-stage2 | 0.019 | 0.027 | 0.52 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.020 | 0.022 | 0.52 | 61 | 1.00x |
| libunord-stage3 | 0.020 | 0.027 | 0.49 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.020 | 0.027 | 0.44 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.021 | 0.022 | 0.52 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.021 | 0.023 | 0.42 | 60 | 1.00x |
| lib-stage1 | 0.021 | 0.023 | 0.51 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.023 | 0.42 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.022 | 0.023 | 0.30 | 60 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.035* | *0.063* | *0.38* | *60* | *1.00x* |
| canon-vecdims | 0.035 | 0.062 | 0.58 | 60 | 1.00x |
| *mut-odo-vecdims-aa* | *0.035* | *0.063* | *0.38* | *60* | *1.00x* |
| **mut-odo-vecdims** | **0.036** | 0.063 | 0.05 | 60 | 1.00x |
| libunord-stage1 | 0.040 | 0.048 | 0.86 | 54 | 2.00x |
| liblist-stage1 | 0.040 | 0.049 | 0.93 | 53 | 2.00x |
| liblist-stage2 | 0.042 | 0.044 | 1.00 | 54 | 2.00x |
| libunord-stage2 | 0.044 | 0.045 | 1.06 | 54 | 2.00x |
| *bq-expand-aa-adjacent* | *0.092* | *0.096* | *0.69* | *46* | *1.38x* |
| *bq-expand-aa-distant* | *0.092* | *0.097* | *0.40* | *46* | *1.38x* |
| bq-expand | 0.092 | 0.096 | 0.70 | 46 | 1.38x |
| list (baseline) | 1.000 | 1.000 | 1.00 | 18 | 20.62x |
| *list-aa-distant* | *1.002* | *1.002* | *0.80* | *18* | *20.62x* |
| *list-aa-adjacent* | *1.007* | *1.011* | *0.73* | *18* | *20.62x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0071, worst cell 1.09% on `bcast-tall-Mx2`, and 3 of 6 intervals cover 1. The `sum-only` halves agree at 0.9999 on a worst cell of 0.04% on `bcast-inner8`, its interval missing 1. The in-situ term reads 1.0243, 1.0154 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0069, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h7m52s, peak 141 MiB in use, 42 MiB max residency; the reader reads 30 benchmarks over 3 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 28.2 ms per call raw, 27.1 ms net.

**Per shape, in the lead's order (bcast-inner8, bcast-inner900, bcast-tall-Mx2):** `mut-odo-vecdims` 0.033/0.022/0.063

**Across the halves:** 8 of the 26 arms are faster on this half and 18 slower, at a geomean of 0.9353, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.2571 to `bq-expand` at 1.0186, with `list` itself at 0.9986.

**What the class says:** properties 1 and 3 hold --- `worst` 0.063, tiers at 1.00x, 1.38x and 20.62x --- and property 2 breaks to `lib-stage2-disp` at a 40.43% margin against a 0.71% floor. An innermost stride of 0 makes every run a re-read of one element, so the library's slice route wins on a view the fill has nothing to fill. **This is the class where the two compilers part furthest**, its cross-half geomean 0.9353, the widest of the ten --- and `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.2571 is the whole of it, the low extreme of every population and its widest here. `list` moved 0.14%, well inside the bar, so this line IS read for the pair's variable.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.42* | *69* | *1.57x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.27* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *88* | *0.00x* |
| libunord-stage3 | 0.015 | 0.018 | 0.33 | 81 | 1.00x |
| lib-stage2 | 0.017 | 0.033 | 0.37 | 80 | 1.00x |
| lib-stage2-lean | 0.017 | 0.033 | 0.36 | 80 | 1.00x |
| lib-stage2-disp | 0.017 | 0.033 | 0.34 | 80 | 1.00x |
| lib-stage2-short | 0.017 | 0.036 | 0.38 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.019 | 0.027 | 0.36 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.033 | 0.32 | 79 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.022 | 0.033 | 0.36 | 79 | 1.00x |
| lib-stage1 | 0.022 | 0.033 | 0.34 | 79 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.023 | 0.036 | 0.27 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.023 | 0.031 | 0.39 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.024 | 0.033 | 0.37 | 78 | 1.00x |
| **mut-odo-vecdims** | **0.032** | 0.058 | 0.28 | 76 | 1.00x |
| *mut-odo-vecdims-aa* | *0.032* | *0.058* | *0.27* | *76* | *1.00x* |
| canon-vecdims | 0.032 | 0.058 | 0.38 | 75 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.059* | *0.47* | *76* | *1.00x* |
| libunord-stage2 | 0.033 | 0.048 | 0.60 | 74 | 2.00x |
| liblist-stage2 | 0.033 | 0.049 | 0.72 | 74 | 2.00x |
| liblist-stage1 | 0.037 | 0.044 | 0.62 | 74 | 2.00x |
| libunord-stage1 | 0.038 | 0.044 | 0.59 | 74 | 2.00x |
| bq-expand | 0.086 | 0.131 | 0.40 | 64 | 1.57x |
| *bq-expand-aa-adjacent* | *0.086* | *0.131* | *0.42* | *64* | *1.57x* |
| *bq-expand-aa-distant* | *0.087* | *0.131* | *0.36* | *64* | *1.57x* |
| *list-aa-distant* | *0.996* | *1.004* | *0.81* | *29* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 1.08 | 29 | 21.22x |
| *list-aa-adjacent* | *1.000* | *1.006* | *0.86* | *29* | *21.22x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0088, worst cell 3.66% on `bcastmid-b200k`, and 6 of 6 intervals cover 1. The `sum-only` halves agree at 0.9989 on a worst cell of 0.54% on `bcastmid-block150k`, its interval covering 1. The in-situ term reads 1.0167, 1.0185 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0065, which the correction amplifies by 1.35x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m25s, peak 132 MiB in use, 35 MiB max residency; the reader reads 30 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 47.1 ms per call raw, 46 ms net.

**Per shape, in the lead's order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.058/0.022/0.037/0.023

**Across the halves:** 19 of the 26 arms are faster on this half and 7 slower, at a geomean of 0.9410, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.2960 to `libunord-stage1` at 1.0144, with `list` itself at 1.0059.

**What the class says:** properties 1 and 3 hold --- `worst` 0.058, tiers at 1.00x, 1.57x and 21.22x --- and property 2 breaks to `libunord-stage3`, the arm that joined the roster on 2026-09-05, at a 54.60% margin against a 0.88% floor. A stride of 0 in the middle leaves the innermost run dense, so the one-block test fires and the unordered route returns a slice. **The cross-half line is read**, `list` moving 0.59%, and it puts 19 of 26 arms faster on the basis at a geomean of 0.9410.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.22* | *82* | *2.81x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *98* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *111* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *111* | *0.00x* |
| libunord-stage3 | 0.017 | 0.023 | 0.21 | 101 | 1.00x |
| lib-stage2-short | 0.022 | 0.029 | 0.21 | 98 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.025 | 0.027 | 0.35 | 98 | 1.00x |
| lib-stage2-lean | 0.026 | 0.034 | 0.15 | 96 | 1.00x |
| lib-stage2-disp | 0.026 | 0.034 | 0.18 | 96 | 1.00x |
| lib-stage2 | 0.026 | 0.034 | 0.17 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.027 | 0.033 | 0.31 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.028 | 0.032 | 0.19 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.028 | 0.032 | 0.17 | 96 | 1.00x |
| lib-stage1 | 0.029 | 0.032 | 0.15 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.034 | 0.18 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.032 | 0.036 | 0.14 | 95 | 1.00x |
| liblist-stage1 | 0.039 | 0.044 | 0.47 | 92 | 2.00x |
| libunord-stage1 | 0.040 | 0.043 | 0.30 | 92 | 2.00x |
| liblist-stage2 | 0.043 | 0.093 | 0.31 | 92 | 2.00x |
| canon-vecdims | 0.044 | 0.057 | 0.12 | 89 | 1.00x |
| libunord-stage2 | 0.044 | 0.090 | 0.27 | 92 | 2.00x |
| *mut-odo-vecdims-aa* | *0.054* | *0.095* | *0.15* | *89* | *1.00x* |
| **mut-odo-vecdims** | **0.054** | 0.094 | 0.11 | 89 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.054* | *0.096* | *0.15* | *89* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.117* | *0.130* | *0.24* | *77* | *2.81x* |
| bq-expand | 0.117 | 0.130 | 0.23 | 77 | 2.81x |
| *bq-expand-aa-distant* | *0.118* | *0.130* | *0.21* | *76* | *2.81x* |
| *list-aa-distant* | *0.998* | *1.007* | *0.50* | *42* | *24.76x* |
| list (baseline) | 1.000 | 1.000 | 0.47 | 42 | 24.76x |
| *list-aa-adjacent* | *1.000* | *1.004* | *0.47* | *42* | *24.76x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 0.9893, worst cell 6.19% on `window-128x128-k7`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.46% on `window-128x128-k7`, its interval covering 1. The in-situ term reads 1.0085, 1.0580 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9897, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m35s, peak 135 MiB in use, 24 MiB max residency; the reader reads 30 benchmarks over 6 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 14.5 ms per call raw, 14.1 ms net.

**Per shape, in the lead's order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2):** `mut-odo-vecdims` 0.045/0.055/0.094/0.032/0.057/0.056

**Across the halves:** 16 of the 26 arms are faster on this half and 10 slower, at a geomean of 0.9384, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.2643 to `lib-stage2-lean` at 1.0427, with `list` itself at 1.0232. **The baseline moved 2.32% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1 and 3 hold --- `worst` 0.094, the highest of the ten, and tiers at 1.00x, 2.81x and 24.76x, `bq-expand`'s the highest of the ten where this class's own `m` shows through --- and property 2 breaks to `libunord-stage3` at a 68.10% margin against a 1.07% floor. **Registration (7) is read here and HELD**: the short body fires on all three k3 views and `lib-stage2-short` leads `lib-stage2-lean` on every one of them, 0.8300, 0.8346 and 0.8384 on the basis and 0.8585, 0.8684 and 0.8640 on HEAD --- and it does NOT fire on `window-128x128-k7` or `window-64x64-k1x9`, where the same pair reads 1.0119 and 1.0114, which is the mechanism showing through the cells rather than through the geomean. **The cross-half line is not read for the pair's variable**, `list` having moved 2.32%, the widest of the ten.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.06* | *119* | *1.14x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.12* | *145* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *137* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *137* | *0.00x* |
| lib-stage1 | 0.025 | 0.034 | 0.23 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.034 | 0.15 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.026 | 0.034 | 0.15 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.034 | 0.16 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.034 | 0.14 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.034 | 0.14 | 127 | 1.00x |
| lib-stage2-lean | 0.027 | 0.034 | 0.19 | 127 | 1.00x |
| lib-stage2-disp | 0.027 | 0.034 | 0.18 | 127 | 1.00x |
| lib-stage2-short | 0.027 | 0.034 | 0.11 | 127 | 1.00x |
| lib-stage2 | 0.027 | 0.034 | 0.15 | 127 | 1.00x |
| libunord-stage3 | 0.027 | 0.034 | 0.13 | 127 | 1.01x |
| mut-odo-vecdims-add-in-leaf-down | 0.028 | 0.032 | 0.13 | 126 | 1.00x |
| canon-vecdims | 0.030 | 0.032 | 0.11 | 126 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.034* | *0.08* | *126* | *1.00x* |
| **mut-odo-vecdims** | **0.031** | 0.034 | 0.12 | 126 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.032* | *0.08* | *126* | *1.00x* |
| liblist-stage1 | 0.038 | 0.062 | 0.20 | 123 | 2.00x |
| libunord-stage2 | 0.038 | 0.062 | 0.21 | 123 | 2.01x |
| libunord-stage1 | 0.039 | 0.062 | 0.24 | 123 | 2.01x |
| liblist-stage2 | 0.041 | 0.062 | 0.32 | 123 | 2.00x |
| bq-expand | 0.095 | 0.098 | 0.06 | 112 | 1.14x |
| *bq-expand-aa-adjacent* | *0.095* | *0.098* | *0.06* | *112* | *1.14x* |
| *bq-expand-aa-distant* | *0.096* | *0.098* | *0.09* | *112* | *1.14x* |
| *list-aa-distant* | *0.996* | *1.003* | *0.22* | *71* | *19.43x* |
| *list-aa-adjacent* | *1.000* | *1.003* | *0.22* | *71* | *19.43x* |
| list (baseline) | 1.000 | 1.000 | 0.20 | 71 | 19.43x |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 0.9762, worst cell 6.63% on `scaled-rank1-m1`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 1.0008 on a worst cell of 0.42% on `scaled-rank1-m1`, its interval covering 1. The in-situ term reads 1.0164, 1.0030 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9889, which the correction amplifies by 2.23x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h7m50s, peak 111 MiB in use, 27 MiB max residency; the reader reads 30 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 4.95 ms per call raw, 4.77 ms net.

**Per shape, in the lead's order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.027/0.034/0.033

**Across the halves:** 17 of the 26 arms are faster on this half and 9 slower, at a geomean of 0.9549, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.3794 to `liblist-stage2` at 1.0087, with `list` itself at 1.0057.

**What the class says:** properties 1 and 3 hold --- `worst` 0.034, the second lowest of the ten behind `compose`'s 0.033, and tiers at 1.00x, 1.14x and 19.43x --- and property 2 breaks to `lib-stage1`, the shipped route, at an 11.31% margin against a 2.38% floor, the second-widest floor here. **The A/A slot [the open list][open] has asked about for seven runs is here again and larger**: `mut-odo-vecdims-aa-distant` reads 0.9762, this class's widest pair, on a worst cell of 6.63% at `scaled-rank1-m1` --- and the foreign-CPU column reads 0.02 to 0.04 across that whole process, so it is not an intrusion. That is what makes this class's floor the second-widest of the ten and why an 11.31% lead is still read as one. `list` moved 0.57%, inside the bar.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.32* | *52* | *1.08x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.11* | *76* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *69* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.030 | 0.12 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.027 | 0.18 | 59 | 1.00x |
| lib-stage2-disp | 0.027 | 0.029 | 0.10 | 59 | 1.00x |
| lib-stage2-short | 0.027 | 0.029 | 0.09 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.029 | 0.10 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.029 | 0.11 | 59 | 1.00x |
| libunord-stage3 | 0.027 | 0.029 | 0.12 | 59 | 1.00x |
| lib-stage2-lean | 0.027 | 0.029 | 0.10 | 59 | 1.00x |
| lib-stage2 | 0.027 | 0.029 | 0.09 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.030 | 0.11 | 58 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.030 | 0.031 | 0.10 | 58 | 1.00x |
| canon-vecdims | 0.031 | 0.063 | 0.10 | 58 | 1.00x |
| *mut-odo-vecdims-aa* | *0.032* | *0.064* | *0.08* | *58* | *1.00x* |
| **mut-odo-vecdims** | **0.032** | 0.064 | 0.08 | 58 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.064* | *0.08* | *58* | *1.00x* |
| liblist-stage2 | 0.067 | 1.066 | 0.35 | 56 | 1.14x |
| libunord-stage2 | 0.068 | 1.065 | 0.40 | 56 | 1.14x |
| *bq-expand-aa-adjacent* | *0.096* | *0.102* | *0.38* | *46* | *1.08x* |
| bq-expand | 0.097 | 0.102 | 0.22 | 46 | 1.08x |
| *bq-expand-aa-distant* | *0.097* | *0.103* | *0.03* | *46* | *1.08x* |
| lib-stage1 | 0.105 | 1.209 | 0.18 | 54 | 1.19x |
| libunord-stage1 | 0.107 | 1.228 | 0.37 | 54 | 1.19x |
| liblist-stage1 | 0.107 | 1.223 | 0.37 | 54 | 1.19x |
| list (baseline) | 1.000 | 1.000 | 2.38 | 17 | 19.27x |
| *list-aa-distant* | *1.025* | *1.051* | *0.64* | *18* | *19.27x* |
| *list-aa-adjacent* | *1.029* | *1.051* | *0.31* | *18* | *19.27x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0289, worst cell 5.13% on `runs-r3-48x30`, and 3 of 6 intervals cover 1. The `sum-only` halves agree at 0.9995 on a worst cell of 0.55% on `runs-96`, its interval covering 1. The in-situ term reads 1.0322, 1.0394 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0278, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h36m31s, peak 568 MiB in use, 247 MiB max residency; the reader reads 30 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 38.7 ms per call raw, 37.6 ms net.

**Per shape, in the lead's order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.064/0.052/0.046/0.043/0.038/0.034/0.028/0.029/0.028/0.028/0.028/0.028/0.028/0.030

**Across the halves:** 22 of the 26 arms are faster on this half and 4 slower, at a geomean of 0.9485, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.3399 to `lib-stage1` at 1.0039, with `list` itself at 0.9923. **The baseline moved 0.77% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1 and 3 hold --- `worst` 0.064, tiers at 1.00x, 1.08x and 19.27x --- and property 2 breaks to `mut-odo-vecdims-add-in-leaf-u2-ptr`, the unrolled pointer fill, at a 24.61% margin against this class's 2.89% floor, the widest floor of the ten. **This is the one population where `lib-stage2-disp` and `lib-stage2-lean` are NOT the same code**, which is why registration (1) exempts it, and the two read 0.9768 and 0.9735, the comparison dispatch ahead of the lean one on both halves at a run length its threshold is written for. **The cross-half line is not read for the pair's variable**, `list` having moved 0.77%.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes: `flip-whole-square` (`l` 1798281, `sInner` 1341), `flip-last-c32` (`l` 165888, `sInner` 3), `flip-last-rows` (`l` 1800000, `sInner` 96), and two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed --- `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.19* | *86* | *1.03x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.18* | *103* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *116* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *116* | *0.00x* |
| libunord-stage3 | 0.025 | 0.025 | 0.01 | 111 | 0.00x |
| lib-stage2-disp | 0.026 | 0.046 | 0.10 | 106 | 1.00x |
| lib-stage2-short | 0.026 | 0.048 | 0.12 | 107 | 1.00x |
| lib-stage2-lean | 0.026 | 0.046 | 0.13 | 106 | 1.00x |
| lib-stage2 | 0.026 | 0.046 | 0.11 | 106 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.051 | 0.21 | 102 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.028 | 0.044 | 0.44 | 103 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.029 | 0.048 | 0.10 | 101 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.030 | 0.048 | 0.18 | 101 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.039 | 0.16 | 101 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.033 | 0.048 | 0.10 | 100 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.034* | *0.058* | *0.10* | *94* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.036* | *0.059* | *0.07* | *94* | *1.00x* |
| canon-vecdims | 0.036 | 0.051 | 0.08 | 97 | 1.00x |
| lib-stage1 | 0.036 | 0.053 | 0.14 | 101 | 1.00x |
| **mut-odo-vecdims** | **0.036** | 0.059 | 0.11 | 94 | 1.00x |
| libunord-stage2 | 0.038 | 0.041 | 0.01 | 106 | 0.00x |
| liblist-stage2 | 0.047 | 0.079 | 0.46 | 102 | 2.00x |
| liblist-stage1 | 0.054 | 0.080 | 0.17 | 97 | 2.00x |
| libunord-stage1 | 0.054 | 0.080 | 0.20 | 97 | 2.00x |
| bq-expand | 0.101 | 0.132 | 0.18 | 82 | 1.03x |
| *bq-expand-aa-adjacent* | *0.101* | *0.132* | *0.16* | *82* | *1.03x* |
| *bq-expand-aa-distant* | *0.103* | *0.130* | *0.20* | *82* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.35 | 47 | 19.19x |
| *list-aa-distant* | *1.003* | *1.012* | *0.35* | *47* | *19.19x* |
| *list-aa-adjacent* | *1.006* | *1.020* | *0.25* | *47* | *19.19x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0069, worst cell 1.97% on `flip-whole-square`, and 6 of 6 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.51% on `flip-last-rows`, its interval covering 1. The in-situ term reads 1.0161, 1.0083 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0067, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m1s, peak 186 MiB in use, 62 MiB max residency; the reader reads 30 benchmarks over 5 shapes of the flip class. Anchor: `flip-last-rows`, `list` at 31.4 ms per call raw, 30.3 ms net.

**Per shape, in the lead's order (flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.027/0.059/0.053/0.030/0.030

**Across the halves:** 10 of the 24 voting arms are faster on this half and 14 slower, at a geomean of 0.9559, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.3662 to `lib-stage2-short` at 1.0332, `libunord-stage2`, `libunord-stage3` sitting out as degenerate, a basis cell of theirs not left positive by the correction, with `list` itself at 1.0072. **The baseline moved 0.72% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1 and 3 hold --- `worst` 0.059, tiers at 1.00x, 1.03x and 19.19x --- and property 2 breaks to `libunord-stage3`, whose cell is its own forcing term on two of the five views, so that lead is not priced. **Registration (6)'s `flip` clause is KILLED for the second run running**, and now on the arms it was written against rather than through a stand-in: it asked `lib-stage2-lean` and `lib-stage1` to tie on the three dense views, and they read 0.9186, 0.5769 and 0.9773 on the basis and 0.9578, 0.5758 and 0.9782 on HEAD, `flip-last-c32` a 42% margin against a 0.69% floor. **Registration (3) is read on the two views that landed this run and HELD**: where the rows are gapped, stage two falls back to its list at 2.00x and 1.31x and stage three fills at 1.00x, leading 0.7982 and 0.8111 on the basis and 0.7721 and 0.8210 on HEAD --- including `flip-outer-gap64`, which the item predicted a tie on and which stage two does not slice after all. **The cross-half line is not read for the pair's variable**, `list` having moved 0.72%.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.10* | *103* | *1.03x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *128* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *121* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *121* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.023 | 0.026 | 0.11 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.024 | 0.030 | 0.07 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.025 | 0.029 | 0.10 | 111 | 1.00x |
| lib-stage2-lean | 0.025 | 0.029 | 0.09 | 111 | 1.00x |
| lib-stage2-short | 0.025 | 0.029 | 0.09 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.025 | 0.029 | 0.11 | 110 | 1.00x |
| lib-stage2-disp | 0.025 | 0.029 | 0.07 | 111 | 1.00x |
| libunord-stage3 | 0.025 | 0.029 | 0.10 | 111 | 1.00x |
| lib-stage2 | 0.025 | 0.029 | 0.09 | 110 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.026 | 0.031 | 0.08 | 110 | 1.00x |
| **mut-odo-vecdims** | **0.027** | 0.035 | 0.07 | 110 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.035* | *0.07* | *110* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.035* | *0.08* | *110* | *1.00x* |
| canon-vecdims | 0.028 | 0.035 | 0.07 | 110 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.028 | 0.033 | 0.08 | 109 | 1.00x |
| liblist-stage2 | 0.041 | 0.041 | 0.14 | 107 | 1.31x |
| libunord-stage2 | 0.041 | 0.041 | 0.18 | 106 | 1.31x |
| libunord-stage1 | 0.056 | 0.065 | 0.10 | 103 | 1.42x |
| liblist-stage1 | 0.056 | 0.066 | 0.11 | 103 | 1.42x |
| lib-stage1 | 0.057 | 0.064 | 0.13 | 103 | 1.42x |
| bq-expand | 0.095 | 0.096 | 0.10 | 96 | 1.03x |
| *bq-expand-aa-adjacent* | *0.095* | *0.096* | *0.13* | *96* | *1.03x* |
| *bq-expand-aa-distant* | *0.095* | *0.096* | *0.13* | *96* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.23 | 56 | 19.19x |
| *list-aa-adjacent* | *1.003* | *1.009* | *0.21* | *56* | *19.19x* |
| *list-aa-distant* | *1.004* | *1.010* | *0.19* | *56* | *19.19x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0039, worst cell 0.98% on `block-run64-gap1`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 1.0012 on a worst cell of 0.67% on `block-run64-gap1`, its interval covering 1. The in-situ term reads 1.0233, 1.0071 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0038, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m59s, peak 134 MiB in use, 34 MiB max residency; the reader reads 30 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.22 ms per call raw, 4.06 ms net.

**Per shape, in the lead's order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.023/0.029/0.035/0.029/0.023

**Across the halves:** 20 of the 26 arms are faster on this half and 6 slower, at a geomean of 0.9543, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.3411 to `libunord-stage2` at 1.0046, with `list` itself at 0.9848. **The baseline moved 1.52% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1 and 3 hold --- `worst` 0.035, tiers at 1.00x, 1.03x and 19.19x --- and property 2 breaks to `mut-odo-vecdims-add-in-leaf-u2-ptr` at a 16.68% margin against a 0.39% floor. **Run 25 was the last run this class did not break for an arm outside the family**, its leader then being `-u2-down`, a `mut-odo-vecdims` sibling; the pointer fill is not the family's, being the design [dead ideas][dead] refuses. **Registration (6)'s `block` clause fires its own kill again, on both halves and monotonically**: `lib-stage2-lean` over `lib-stage1` reads 0.3929 at one element, 0.4711 at 64 and 0.5387 at a page on the basis, and 0.3966, 0.4695 and 0.5409 on HEAD, so the margin SHRINKS as the gap grows where the clause asked it to grow --- and it is widest on the rank-3 block, 0.3449 and 0.3410, which the clause did predict. **The cross-half line is not read for the pair's variable**, `list` having moved 1.52%.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.28* | *223* | *1.34x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *238* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.03* | *249* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.03* | *249* | *0.01x* |
| lib-stage2-lean | 0.056 | 0.102 | 0.27 | 228 | 1.40x |
| lib-stage2-short | 0.057 | 0.095 | 0.21 | 228 | 1.40x |
| lib-stage2-disp | 0.057 | 0.102 | 0.28 | 228 | 1.40x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.058 | 0.071 | 0.30 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.058 | 0.071 | 0.23 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.061 | 0.072 | 0.35 | 228 | 1.27x |
| canon-vecdims | 0.062 | 0.119 | 0.19 | 227 | 1.40x |
| mut-odo-vecdims-add-in-leaf-down | 0.062 | 0.073 | 0.39 | 228 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.064 | 0.080 | 0.25 | 228 | 1.44x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.064 | 0.078 | 0.24 | 227 | 1.44x |
| lib-stage2 | 0.066 | 0.124 | 0.22 | 226 | 1.58x |
| *mut-odo-vecdims-aa-distant* | *0.066* | *0.092* | *0.25* | *229* | *1.27x* |
| *mut-odo-vecdims-aa* | *0.066* | *0.092* | *0.23* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.067** | 0.092 | 0.19 | 229 | 1.27x |
| liblist-stage2 | 0.080 | 0.130 | 0.18 | 223 | 2.49x |
| lib-stage1 | 0.092 | 0.118 | 0.23 | 221 | 2.30x |
| libunord-stage3 | 0.093 | 0.160 | 0.19 | 221 | 1.74x |
| libunord-stage2 | 0.103 | 0.212 | 0.25 | 218 | 1.94x |
| liblist-stage1 | 0.105 | 0.135 | 0.24 | 221 | 2.61x |
| libunord-stage1 | 0.118 | 0.199 | 0.35 | 218 | 3.05x |
| *bq-expand-aa-adjacent* | *0.134* | *0.167* | *0.18* | *218* | *1.34x* |
| bq-expand | 0.134 | 0.167 | 0.25 | 218 | 1.34x |
| *bq-expand-aa-distant* | *0.134* | *0.167* | *0.21* | *218* | *1.34x* |
| *list-aa-distant* | *1.000* | *1.003* | *0.15* | *181* | *19.50x* |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.16* | *181* | *19.50x* |
| list (baseline) | 1.000 | 1.000 | 0.18 | 181 | 19.50x |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 0.9948, worst cell 1.56% on `small-bcast32`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 0.9996 on a worst cell of 0.43% on `small-flat64`, its interval covering 1. The in-situ term reads 0.9835, 0.9746 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9969, which the correction amplifies by 1.54x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m2s, peak 131 MiB in use, 52 MiB max residency; the reader reads 30 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.17 us per call raw, 5.94 us net.

**Per shape, in the lead's order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.046/0.083/0.057/0.065/0.092

**Across the halves:** 26 of the 26 arms are faster on this half and 0 slower, at a geomean of 0.9435, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.5671 to `mut-odo-vecdims-aa` at 0.9879, with `list` itself at 0.9857. **The baseline moved 1.43% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 holds and **property 3 breaks its fill tier's LEVEL, which is what this class was built to do** --- the five non-pointer `mut-odo-vecdims` fills read 1.27x the result vector at 150 to 384 elements against Run 25's 1.23x, the two pointer fills 1.44x and `canon-vecdims` 1.40x, with `bq-expand` at 1.34x and `list` at 19.50x, the per-call constant showing where nothing hides it. Property 2 breaks to `lib-stage2-lean` at a 15.93% margin against a 0.52% floor. **Registration (1) is KILLED here and here alone**: `lib-stage2-disp` over `lib-stage2-lean` reads 1.0171 and 1.0316, past this class's floors of 0.52% and 0.96% on both halves, where six of the other nine read them inside on both halves and three fall outside on one half only --- so the lean ruling took most of Run 25's 1.2274 and 1.2368 gap out and not all of it. **Registration (6)'s `small` clause is read for the first time and HELD in both parts**, both arms it turns on having been parked when it was written: `lib-stage2-lean` leads `lib-stage2` on all four of its views, 0.8941, 0.8190, 0.8657 and 0.8016 on the basis and 0.8928, 0.7814, 0.8282 and 0.7689 on HEAD; and `canon-vecdims` sits BEHIND `mut-odo-vecdims` on the regime-3 views, 1.2786, 1.4334 and 1.3463, while collapsing `small-flat64` to 0.2140 --- the pass's per-call cost and its one win, exactly where the clause put them. **This is the one population where all 26 arms are faster on the basis**, and the cross-half line is not read for the pair's variable, `list` having moved 1.43%.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.33* | *86* | *1.21x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.22* | *112* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *104* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *104* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.017 | 0.018 | 0.37 | 97 | 1.00x |
| lib-stage2-lean | 0.018 | 0.019 | 0.29 | 97 | 1.00x |
| lib-stage2-short | 0.018 | 0.019 | 0.27 | 97 | 1.00x |
| lib-stage2-disp | 0.018 | 0.019 | 0.29 | 97 | 1.00x |
| lib-stage2 | 0.018 | 0.019 | 0.30 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.018 | 0.019 | 0.31 | 96 | 1.00x |
| libunord-stage3 | 0.019 | 0.019 | 0.29 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.020 | 0.021 | 0.30 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.020 | 0.021 | 0.30 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.021 | 0.27 | 96 | 1.00x |
| lib-stage1 | 0.020 | 0.021 | 0.27 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-down | 0.021 | 0.023 | 0.28 | 96 | 1.00x |
| canon-vecdims | 0.027 | 0.032 | 0.30 | 94 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.033* | *0.25* | *94* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.027* | *0.033* | *0.24* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.033 | 0.19 | 94 | 1.00x |
| liblist-stage2 | 0.039 | 0.047 | 0.52 | 90 | 2.00x |
| libunord-stage2 | 0.039 | 0.048 | 0.65 | 90 | 2.00x |
| liblist-stage1 | 0.040 | 0.049 | 0.54 | 90 | 2.00x |
| libunord-stage1 | 0.040 | 0.049 | 0.55 | 90 | 2.00x |
| *bq-expand-aa-adjacent* | *0.094* | *0.095* | *0.38* | *80* | *1.21x* |
| bq-expand | 0.094 | 0.096 | 0.36 | 80 | 1.21x |
| *bq-expand-aa-distant* | *0.094* | *0.096* | *0.23* | *80* | *1.21x* |
| *list-aa-distant* | *1.000* | *1.004* | *0.71* | *46* | *19.87x* |
| list (baseline) | 1.000 | 1.000 | 0.70 | 46 | 19.87x |
| *list-aa-adjacent* | *1.002* | *1.010* | *0.71* | *46* | *19.87x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 0.9956, worst cell 0.79% on `compose-zero-mid`, and 3 of 6 intervals cover 1. The `sum-only` halves agree at 0.9980 on a worst cell of 0.58% on `compose-scalar`, its interval covering 1. The in-situ term reads 1.0150, 1.0229 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9983, which the correction amplifies by 2.39x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h10m27s, peak 128 MiB in use, 33 MiB max residency; the reader reads 30 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 28.5 ms per call raw, 27.4 ms net.

**Per shape, in the lead's order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.033/0.032/0.025/0.021

**Across the halves:** 8 of the 26 arms are faster on this half and 18 slower, at a geomean of 0.9532, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.2646 to `liblist-stage1` at 1.0380, with `list` itself at 0.9996.

**What the class says:** properties 1 and 3 hold --- `worst` 0.033, the lowest of the ten, with `scaled`'s 0.034 and `block`'s 0.035 behind it, and tiers at 1.00x, 1.21x and 19.87x --- and property 2 breaks to `mut-odo-vecdims-add-in-leaf-u2-ptr` at a 38.03% margin against a 0.44% floor. **Registration (3) is at its widest here**: stage two falls back to its list on all four views, allocating 2.00x and 2.01x where stage three fills at 1.00x, and stage three leads by 0.6660 to 0.7953 on the basis and 0.6721 to 0.7994 on HEAD --- a fifth to a third, and the clearest reading of what the unordered entry point buys when the destination is not one block. `list` moved 0.04%, the least of the ten, so this line IS read for the pair's variable.

## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 26's halves differ in the compiler and in the boot libraries that come with it, and in nothing else.** One source, `Main.hs` at `659d898`, one shim, `align-as.py` at `b3a1aca`, set the same way on both halves, and one shim environment --- `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of `-pgma`, with `LOOP_NOOVERLAP` unset --- and one pair of `--ghc-options`, `-fspec-constr -fobject-determinism` with `-fforce-recomp`, into a fresh `--builddir` apiece. The basis took ghc-9.12.4, the default on PATH here; the other half took the in-tree stage1 of the GHC checkout through `cabal.project.ghead` and its own freeze at the same index-state, which reports `10.1.20260803` --- **Runs 24's and 25's compiler unmoved**, its `_build/stage1/bin/ghc` dated 2026-08-03 and its checkout at `d415f38a75`, so this run's HEAD half and theirs are the same compiler and their absolutes are comparable in the way the basis halves' are. The binaries carry `ghc-internal-9.1204.0` against `ghc-internal-10.100.0`, both bake `-A32m -I0 -T -M8G` read back by `+RTS --info`, both carry exactly one `@@wild` and one `@@saturate` string, and both report criterion 1.6.5.0. **The shim did not move at all this run**, `b3a1aca` answering for Run 25 and for this one, so a distance from Run 25 carries no shim term where a distance from Run 24 carried two docstring commits. **THE COMMIT IS `659d898` AND NOT THE ONE THE DRIVER STAMPED**: `9702f20` landed at 01:25 on 2026-09-06, after the pair was built, and the wall-clock log therefore opens `tree at 9702f20, Main.hs at 9702f20`. It changes no code --- every changed line in `Main.hs` is a comment --- and the pair note proves it in the binary: two builds of the tip give one md5 to each other and differ from the timed `run26-g912` in 142 bytes, 20 in the build-id, 12 in `.rodata` and 110 in `.data`, NONE in `.text`, which is 20594885 bytes in both; the twelve `.rodata` bytes are incomplete-pattern strings whose line numbers all moved by exactly +8, below the edited comments and not above them.

**The sequence was launched once and ran to the end in one window**, 2026-09-06T02:29:46 to 2026-09-06T08:40:03, twenty-two processes, every one exiting 0 at the count its roster holds --- 570 twice, 420 twice, 180 twice, 150 six times, 120 four times and 90 six times --- each carrying its `@@saturate` line and none reporting a selection it did not ask for. The two main-set processes read 0h49m15s and 0h49m26s at 177 and 167 MiB in use and 66 MiB max residency apiece; the twenty class processes span 0h7m50s to 0h36m40s, the two `runs` processes taking the whole top of that range at 0h36m31s and 0h36m40s and the two main-set processes running longer than any of them. The HEAD half ran first throughout, which is the driver's order. **The gate preceded it**, four processes from 01:56:24 to 02:29:43 at 95 benches each, on a box reading 0.4% non-idle by `machine-busy.sh` before the launch, and the alarm between the gate and the sequence read 0.8%. **The riders followed it**, 88 single-bench processes over four invocations of 22 --- 19 main-set shapes and the three anchors on a second reps pass --- each half clean and saturated, from 08:40:03 to 08:52:40 and every invocation exiting 0. **The counted work followed them on a box that no longer had to be quiet**, twenty-two sweeps from 08:55:18 to 09:32:35, both halves over all eleven populations, with no cell perf refused in any of the twenty-two files. **The whole evening was one command**, `run-evening.sh` driving the gate, the alarm, the sequence and the four rider invocations and appending each stage's verdict as it landed, with `run-counts-all.sh` the second; every stage of both exited 0.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run26-g912`, md5 `e2c371228e33b63057d5a7af30b2be97`, and `run26-ghead`, md5 `0493a65368a2509c4ed0c074ebf8b53d`, with `.text` of 20594885 and 20739903 bytes --- the HEAD half 145018 bytes larger, where Runs 24 and 25 both read 140922 between the same two recipes, so this run is one page more and the source is what moved under them. **Neither reproduces an earlier binary and neither was meant to**: `Main.hs` moved between Run 25 and this run by eight commits --- `1314f34` the lean dispatch, `a7cc09a` the dispatch pair restated, `55cb6a0` `libunord-stage3`, `8796dc6` `cnn-L1-6x6-c1` and three class views, `deb2718` a comment on the one-block test, `9909bbf` `mut-odo-vecdims-add-in-leaf` to `Only`, `1944582` the two pointer fills and `659d898` the four parkings lifted --- so no md5 here matches one on record and the repetition reading Runs 19 and 23 could take is not available. Their `--list` outputs are identical at 570 and their `classes --list` at 1560 over 52 views, and their `check` logs are byte-identical, which two compilers did not have to be, a float-printing difference being the one thing such a difference could innocently have been. **The regime was read back out of the basis binary before the hours were spent**: `diag` puts `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512`, a ratio of 1.000 and `run25-g912`'s and `run24-g912`'s two figures to the byte, where plain -O1 separates the two tenfold. **The tree at launch** was `9702f20` with 52 untracked scratch paths and nothing modified, and nothing landed on the branch during the evening.

**The roster and the source moved, which is what would put a layout term in the cross-run column, and the fills say the two axes parted this run.** The tracked 28-byte loops were read at the build against `run25-g912`, the previous build of this recipe. **On the offset axis this is the weakest reading on record**: the basis half's six-copy group MOVED, from [0, 0, 24, 0, 4, 24] to [0, 0, 0, 24, 0, 4], where every reading before it preserved every offset in every group; its two-copy group kept both. **On the displacement axis it is the strongest**: no address survives to the byte in either group, and the displacements are six distinct values in the first --- `0x-211e8`, `0x-7f80`, `0x2c0`, `0x47e8`, `0x8f6c`, `0xa23c`, four of them not whole lines --- and two in the second, where a common constant would have meant the binary shifted wholesale rather than the shim re-pinning. **What an offset list moving does NOT settle** is which of two worlds it is, `--delta` pairing by sorted address rather than by identity, so the same six copies re-pinned and a changed membership read alike; there is no `-g3` twin of `run25-g912` to name that run's group with, and building one wants its source. **This run leaves the question answerable next time, on both halves**, which is what its two twins are for: `run26-g912` holds its six at offsets [0, 0, 0, 24, 0, 4] under `fbMidCopy`, `fbCanonVecdims`, `fbMutOdoVecdimsAddIn`, `fbMutOdoVecdims`, `fbMutOdoVecdimsAddOut` and `fbMutOdoVecdimsAddBoth`, and a two-copy group at [0, 0] under `fbBuild` and `fbMutOdo`; `run26-ghead` holds THE SAME SIX FUNCTIONS in the same address order at [18, 0, 31, 7, 9, 26], a three-copy group at [0, 0, 21] --- `fbBQmutRunsGmMulback`, `fbBQmutLemireMulback`, `fbBQmutRunsMulback`, named here for the first time --- and the same two-copy group at [0, 0]. **So the membership is the compiler-invariant and the pinning is not**, which no earlier run could say. Within the pair no tracked address is shared and HEAD carries a third group the basis has not, which is the compiler; `--library` reads 14.2% same offset in line and 65.2% same straddle state, inside the band README gives a pair that varies a shim or a compiler. THIS PAIR'S FIGURE AND NOT THE SERIES.

**The three main-set anchors** read **5.96 us** on `cnn-slice-c32`, **3.47 ms** on `cnn-L2-24x24-c32` and **37.6 ms** on `stretch-wide-2xM`, net of the forcing pass, on the basis half, against **5.91 us**, **3.40 ms** and **37.5 ms** on the HEAD half. Against Run 25's own three, read off the nets rather than off the printed figures, they move **+0.44%**, **+1.91%** and **-0.33%**, all three inside the 3% the box registration allows, and the middle one larger than any of Run 25's three, which is the only comparison the cut shape set admits so far. So the per-shape record is continuous, and the shape this run added, `cnn-L1-6x6-c1`, is not an anchor and starts no series.

| shape | `l` | `list`, per call | net | HEAD, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.13 us | 5.96 us | 5.91 us |
| `cnn-L2-24x24-c32` | 165888 | 3.57 ms | 3.47 ms | 3.40 ms |
| `stretch-wide-2xM` | 1800000 | 38.7 ms | 37.6 ms | 37.5 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at 38.7 ms raw and 37.6 ms net, **Runs 24's and 25's two figures to the printed digit** on a class whose population has not moved for three runs and whose own floor is 2.89% here, across two roster changes and three builds; the four classes that landed 2026-09-03 now have a second reading, and `flip` and `small`, whose populations moved this run, have anchors on shapes they kept. Read a class anchor against the same class's anchor in an earlier run and against nothing else, and against that class's own floor rather than the main set's.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0007** on the basis and **0.9995** on the HEAD half, both within a tenth of a point and on worst cells of 0.52% and 0.68%. The in-situ term the `-nosum` arms measure reads 1.0227 and 1.0316 of it on the basis and 1.0280 and 1.0334 on the other, on `mut-odo-vecdims` and `bq-expand`, the two arms whose `-nosum` twins the prune left standing --- and Run 25's own four, re-derived off its JSONs, read 1.0199, 1.0344, 1.0242 and 1.0375, so the correction sits where it has sat since Run 17 on both halves and on three runs of this roster's lineage.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-05 and amended 2026-09-06 with items (4) to (7), both before the run, on Run 25's pair --- ghc-9.12.4 against GHC HEAD, both under `LOOP_DEADSPOT=1`, the same recipe and the same shim --- over Run 25's roster and shape set as the day after it left them: the lean ruling of that day ([the stride classes](../README.md#the-stride-classes-and-what-they-cover)) gave every canonical dispatch the lean form, so `lib-stage2-disp` and `liblist-stage2` are timed under a dispatch Run 25 did not time them under, and the composite of the short bodies left `Main.hs`; and `libunord-stage3` joined the timed roster, every slot below `libunord-stage2` moving by one, with the two pointer fills of 2026-09-05, `mut-odo-vecdims-add-in-leaf-u2-ptr` and `mut-odo-vecdims-add-in-leaf-u1-ptr`, landing beside their parents and moving every slot below each by one more; `cnn-L1-6x6-c1` is timed again, back from the retired list, and three class views landed, `flip-inner-gap64` and `flip-outer-gap64` in `flip` and `small-patch-r5` in `small`; and **four parkings taken after Run 23 are lifted for this run alone** --- `mut-odo-vecdims-add-in-leaf-down`, `canon-vecdims`, `lib-stage2` and `lib-stage2-short`, each back in the slot it used to hold, while the leaf arm re-timed for Run 25 as the `-u1` bound control went to `Only` the same day, its question spent --- so the net shift below the leaf block is three and every slot below `lib-stage2-short` moves by three --- so that the seven clauses those parkings left unread are read on the arms they were written against, with Run 27 parking all four again ([what the benchmark does](../README.md#what-the-benchmark-does) carries the four and the re-parking). So the run is 30 arms over 19 main-set shapes, 570 benches, and its cross-run column against Run 25 is pinned to the eighteen they share. Each with a prediction and a kill condition, and the verdicts move to Run 26's file with them. **Items (4) to (7) are those seven clauses re-registered**, each in its own registration's words with one substitution: where a clause named `lib-stage2` and the lean arm is the same code for that clause's purpose it names `lib-stage2-lean`, the form the lean ruling of 2026-09-05 made canonical, and where the pair itself is the clause's subject both names stay. Each keeps the population it was registered over, so a class that has gained views since is read on the views its clause names and no more. (1) *The dispatch pair.* `lib-stage2-disp` and `lib-stage2-lean` are the same code on every population but `runs`, so they read as an A/A pair there: `predict: pair lib-stage2-disp lib-stage2-lean 1.0` on the main set, and inside the population's own floor on each of the nine other classes, on both halves, where Run 25 read the pair at 1.0119 and 1.0140 on the main set and 1.2274 and 1.2368 on `small`; and the pair reads inside the floor on `cnn-L1-6x6-c1` too, timed again for this, where Run 24 read the lean dispatch 14 to 20 points ahead of the comparison one; killed by a margin past the floor on both halves on any population but `runs`, which would be a slot and not the code. (2) *The roster change, read cross-run.* Against Run 25's basis column over the eighteen shared shapes, `lib-stage2-disp` reads faster on `cnn-slice-c32`, the one small shape Run 25 timed, by about the gap the pair read there on Run 25, 1.1719, and on every `small` view by about that class's 1.17 to 1.31, and within 1% on the other seventeen shapes as the rest of the roster does; `liblist-stage2` moves the same way on the same views, by less, the dispatch being a smaller share of a call that also builds its list; `lib-stage2-lean` and every arm the ruling did not touch read within 1% on all eighteen; the four arms lifted for this run have no cell in Run 25's column and are outside this item, their last cross-run reading being Run 24's. Killed by `lib-stage2-disp` inside the floor on `cnn-slice-c32` and on `small`, which would say the dispatch was not what that gap measured, or by an untouched arm moving past 1% on a shape, which would be a layout term the prune was to have removed. This item is the session's to adjudicate, no span reading a shape subset or another run's column. (3) *The unordered candidate.* `libunord-stage3` reads ahead of `libunord-stage2` past the population's floor on both halves wherever stage two falls back to its list --- the main-set shapes where the two unordered arms fill rather than slice, `compose-rev-bcast`, whose reversed axis stage three walks forward, and `flip-inner-gap64`, whose reversed rows it walks forward --- and inside the floor wherever both slice, on `rev`, the dense `flip` views, `revsome` and the one-block main-set shapes, where the two are the same slice, and on `flip-outer-gap64`, forward runs in reversed order, where the direction is already forward; and it allocates 1.00x the result where it fills, against the list consumer's 2.00x tier. Killed by reading behind `libunord-stage2` past the floor on both halves on any view where stage two fills, which would say address order is not what the `flip` finding measured, or by a tie inside the floor on every such main-set shape, which would say the logical order was already the address order there. The pair is not one geomean over the main set, its cells on the one-block shapes being the forcing pass, so this is read per shape and is the session's to adjudicate. (4) *Run 24's registration 5, the clause no process could read.* `mut-odo-vecdims-add-in-leaf-u2` ahead of `mut-odo-vecdims-add-in-leaf-down` in every population, the standing Run 23 read on both halves; killed by `-down` at or ahead of `-u2` past the population's floor on both halves. Run 24 could read none of it, the arm having gone to `Only` in the same commit the registration was written beside, and what it read in place of it --- `-u2` against `-u2-down`, which Run 24 read as a tie in every population of both halves but `reshape1` on its basis, Run 23's dead-spot half having read it the same way --- is a different pair and settles nothing about this one. (5) *Run 25's registration 3, the orderings clause.* No ordering of Run 24's registrations (1) to (3) reverses past either half's floor; killed by a reversal past the floor on both halves. What each is here: **(1) and (3) are one comparison now**, `lib-stage2-short` against `lib-stage2-lean`, that arm having taken the lean dispatch on 2026-09-05 and being Run 24's `lib-stage2-short-lean` under the surviving name, so (1) is read against the lean arm rather than against `lib-stage2`, ahead past the floor wherever the canonical inner run is 2 to 5 --- its count clause is not an ordering and Run 24 killed it on `stretch-coprime-r7` at 1.0105 and 1.0107, so it is no part of this --- and (3)'s other parent, the plain short arm, is gone from `Main.hs` and cannot be read at all --- which is the binding half rather than the slack one, Run 24 having read the composite at 0.9693 of that parent and 0.9182 of `lib-stage2-lean` on the basis, 0.9718 and 0.9207 on HEAD, so the arm still timed is the slower of the two and `at or below the better` is read against the weaker bound. **(2) is `lib-stage2-lean` at or below `lib-stage2`** within each population's floor and ahead past it on the small shapes, which is the one clause here no substitution can restate, the pair being its subject; of the four smallest main-set shapes Run 24 named it over, this run times two, `cnn-slice-c32` and `cnn-L1-6x6-c1`, the other two having been retired on 2026-09-04. (6) *The class clauses of 2026-09-03, the three the prune left unreadable.* Registered as task 6 under the tasks heading, numbered 10 when Run 25 deferred to it, and its clauses (1) to (3). **`flip`**: `lib-stage2-lean` against `lib-stage1` a tie on the three views the clause was written over, `flip-whole-square`, `flip-last-c32` and `flip-last-rows`, both routes being the regime-3 fill there; killed by a margin past the class's floor on both halves. Run 25 read it through this same arm standing in for the parked one and found no tie, 0.5736, 1.1366 and 0.9177, so what is registered here is that repetition, `flip-inner-gap64` and `flip-outer-gap64` sitting outside the clause. **`block`**: `lib-stage2-lean` ahead of `lib-stage1` at every gap and by more at the page than at one element, ahead by more again on the rank-3 block, and `lib-stage1` moved by the offset more than the stage-two route; killed by the margin shrinking as the gap grows. Run 25 read this through the same stand-in and its kill condition fired, the margin shrinking on both halves and monotonically. **`small`, the clause both parked arms carried and neither half of it read**: `lib-stage2-lean` ahead of `lib-stage2` past the floor on the four views the clause was written over, `small-row96`, `small-patch-k5`, `small-bcast32` and `small-flat64`, and `canon-vecdims` behind `mut-odo-vecdims` past the floor on the two regime-3 views among those four, the pass's per-call cost showing where nothing hides it, while ahead on `small-flat64`, which it collapses; killed by `lean` ahead nowhere. `small-patch-r5`, which joined the class on 2026-09-05, is outside the clause. (7) *The two window views of 2026-09-03, withdrawn with the arm.* Task 6's clause (5). The short body fires on both k3 kernels, so `lib-stage2-short` leads `lib-stage2-lean` on `window-224x224-k3-s2` and `window-224x224-k3-d2` as it does on `window-224x224-k3`; killed by either reading it behind past the floor. Withdrawn 2026-09-04 with the short arm's parking and re-registered here against the lean arm, which is that arm's control since the lean ruling. The ruling that parked it is not reopened by timing it: the short bodies are still too repetitive to ship, and what this run reads is what they would have bought. (8) *The pointer fills, registered 2026-09-06.* Both are the `Ptr`-walking fill the ruling of 2026-08-29 refused for the library and still refuses ([dead ideas][dead]); they are timed for the ceiling each parent would reach under an allocator that spilled nothing, so the spans below are ceilings and not candidates. The reload the twentieth reading names goes in both, and the nineteenth's rate --- about three quarters of an instruction saving reaching the clock --- prices it: `predict: pair mut-odo-vecdims-add-in-leaf-u1-ptr mut-odo-vecdims-add-in-leaf-u1 0.92` on the main set, from 0.8945 in corrected instructions; `predict: pair mut-odo-vecdims-add-in-leaf-u2-ptr mut-odo-vecdims-add-in-leaf-u2 0.88`, from 0.8356; and the unrolling alone, `predict: pair mut-odo-vecdims-add-in-leaf-u2-ptr mut-odo-vecdims-add-in-leaf-u1-ptr 0.90`, from 0.8604 --- each on both halves, Run 25 having read the leaf family within a percent between the compilers. Killed by either pointer fill at or behind its parent past the population's floor on both halves, which would say the reload is not what the parents pay for, or by `-u2-ptr` at or behind `-u1-ptr`, which would say the unrolling's quarter in instructions buys no time. Both are new and have no cell in Run 25's column, so they are outside item (2); and both allocate 1.00x the result, as their parents do, which the run's allocation column reads.

**Four held, one was killed on one population, one split, one is three class clauses of which the long-unreadable one is read at last, and one is three spans killed with all three directions standing --- and, for the first run in three, NOT ONE CLAUSE WAS UNREADABLE.** Every figure below is re-derived from this run's own JSONs and counts files rather than carried from the registration; margins are judged against the population's own floor on the half they are read on --- 0.31% and 0.46% on the main set and each class's own figure in the summary table above --- because that is the threshold the registrations state for themselves. **Why nothing was lost**: items (4) to (7) are seven clauses of older registrations re-registered in their own words, which the preparation did precisely because a clause deferring to another registration inherits that registration's arms and reaches no check; Runs 24 and 25 lost five clauses between them that way.

(1) *The dispatch pair.* **KILLED, on `small` and on `small` alone.** `lib-stage2-disp` over `lib-stage2-lean` reads **1.0171 and 1.0316** there, past that class's floors of 0.52% and 0.96% on both halves, which is the item's own kill condition exactly. Everywhere else it is the A/A pair the item predicted: inside the population's floor on BOTH halves on `bcast`, `bcastmid`, `scaled`, `flip`, `block` and `compose`, and outside on ONE half only on three --- `rev` on the basis at 1.0048 against 0.30%, `window` on HEAD at 1.0067 against 0.34%, and the main set on HEAD at 1.0071 against 0.46% where its basis reads 1.0007 --- none of which the kill reaches, since it wants both halves. **`cnn-L1-6x6-c1`, timed again for this item, splits the halves**: the basis reads 1.013 where the HEAD half reads **1.139**, so Run 24's 14-to-20-point gap survives the lean ruling on one compiler and is all but gone on the other. What the item was written to show it does show --- the ruling took Run 25's 1.2274 and 1.2368 on `small` down to 1.0171 and 1.0316 --- and what it asserted, that the two are the same code everywhere but `runs`, is false at the smallest sizes.

(2) *The roster change, read cross-run.* **SPLIT: the arm-level clause held and the per-shape kill fired.** Against Run 25's basis column over the eighteen shared shapes, `lib-stage2-disp` reads **1.1550** on `cnn-slice-c32` against the "about 1.1719" registered, and **1.2177** over `small`'s four views with a per-view range of 1.13 to 1.29 against the 1.17 to 1.31 registered; `liblist-stage2` moves the same way and by less, 1.1110 and 1.1635, the dispatch being a smaller share of a call that also builds its list. **Seventeen of the nineteen shared arms are within 1% at the geomean**, 0.9863 to 1.0042, the two outside being the `libunord` pair at 1.2760 and 1.3610, whose cells are their own forcing term on eight shapes apiece. **The kill fires per shape, and widely**: ALL SEVENTEEN untouched shared arms have at least one shape outside 1%, the smallest excursion being `mut-odo-vecdims`'s 0.987 --- at a three-percent threshold it is six, worst `liblist-stage1` at 0.8895 on `stretch-coprime-r7` and 1.0526 on `stretch-inner256`, with `list` itself reaching 1.043. So a layout term the prune was meant to have removed survives at the per-shape level while no arm's geomean carries it --- which is a weaker finding than the kill's wording implies and is recorded as one.

(3) *The unordered candidate.* **HELD, with one clause wrong in the arm's favour.** `libunord-stage3` is ahead of `libunord-stage2` wherever stage two falls back to its list: on `compose`'s four views 0.6660 to 0.7953 on the basis and 0.6721 to 0.7994 on HEAD, and on `flip-inner-gap64` 0.7982 and 0.7721 --- a fifth to a third, far past those classes' floors on both halves. It is inside the floor wherever both slice: 0.9997 to 1.0000 raw on the three dense `flip` views, 0.9995 and 1.0009 on `rev-primes`, and 0.9996 to 1.0016 on the basis and 0.9968 to 1.0004 on HEAD over the main-set one-block shapes. **And it allocates 1.00x the result where it fills against the list consumer's 2.00x**, read off the allocation column on every such view. **The clause that is wrong is `flip-outer-gap64`**, predicted a tie because the direction is already forward: stage three leads 0.8111 and 0.8210 there, and the allocation column says why --- stage two allocates 1.31x on that view, so it is falling back after all, partially. The kill --- reading behind stage two past the floor on any view where stage two fills --- fires nowhere.

(4) *Run 24's registration 5, the clause no process could read.* **HELD, and read in full for the first time.** `mut-odo-vecdims-add-in-leaf-u2` is ahead of `mut-odo-vecdims-add-in-leaf-down` in **every one of the eleven populations on both halves**: 0.9216 and 0.9229 on the main set, and 0.8579 to 0.9527 across the ten classes, the widest on `rev` and the narrowest on `scaled`, every one past its population's floor. The two compilers agree to within 0.03 on all eleven, the widest gap being `flip`'s 0.0268 and the next `small`'s 0.0180. Run 24 could read none of it, the arm having gone to `Only` in the same commit its registration was written beside, and the substitute it read then settles nothing about this pair.

(5) *Run 25's registration 3, the orderings clause.* **HELD in both its parts.** `lib-stage2-short` against `lib-stage2-lean`, which is (1) and (3) merged: 0.9244 at 17 of 19 on the basis and 0.9307 at 14 of 19 on HEAD, the short body ahead past both floors. **(2), `lib-stage2-lean` at or below `lib-stage2`**: at or below in all eleven populations on both halves and within each population's floor on eight of them, the exceptions being ahead and not behind --- and ahead past the floor on the small shapes exactly as the clause asks, `cnn-L1-6x6-c1` at 0.8808 and 0.8717 and `cnn-slice-c32` at 0.8658 and 0.8589. Of the four smallest main-set shapes Run 24 named it over, this run times those two.

(6) *The class clauses of 2026-09-03.* **`flip` KILLED, `block` KILLED, `small` HELD --- and `small` is read for the first time.** **`flip`**: no tie on the three dense views, `lib-stage2-lean` over `lib-stage1` reading 0.9186, 0.5769 and 0.9773 on the basis and 0.9578, 0.5758 and 0.9782 on HEAD, `flip-last-c32` a 42% margin against a 0.69% floor --- Run 25's verdict repeated, now on the arm itself rather than a stand-in. **`block`**: the margin shrinks as the gap grows where the clause asked it to grow, 0.3929, 0.4711 and 0.5387 at one element, 64 elements and a page on the basis and 0.3966, 0.4695 and 0.5409 on HEAD, monotonic on both halves, which is that item's own kill --- and ahead by most on the rank-3 block, 0.3449 and 0.3410, which the clause did predict. **`small`, the clause both of whose arms were parked when it was written**: `lib-stage2-lean` leads `lib-stage2` past the floor on all four of its views, 0.8941, 0.8190, 0.8657 and 0.8016 on the basis and 0.8928, 0.7814, 0.8282 and 0.7689 on HEAD; and `canon-vecdims` sits behind `mut-odo-vecdims` past the floor on the regime-3 views among them --- THREE of the four and not the two that clause names, `small-bcast32` being regime 3 as well ---, 1.2786, 1.4334 and 1.3463, while collapsing `small-flat64` to 0.2140 and 0.2298 --- the pass's per-call cost and its one win, both where the clause put them. The kill, `lean` ahead nowhere, fires nowhere.

(7) *The two window views of 2026-09-03, withdrawn with the arm.* **HELD.** `lib-stage2-short` leads `lib-stage2-lean` on all three k3 views of both halves --- `window-224x224-k3` 0.8300 and 0.8585, `-d2` 0.8346 and 0.8684, `-s2` 0.8384 and 0.8640 --- so the short body fires on the strided and dilated kernels as it does on the plain one, and neither half reads it behind. **What the same pair reads where the body does not fire is the control the item did not ask for**: 1.0119 and 1.0114 on `window-128x128-k7` and `window-64x64-k1x9` on the basis, so the lead is the short body and not the arm. The ruling that parked it is not reopened by timing it.

(8) *The pointer fills.* **All three spans KILLED, all three directions HELD, and the reason is the rate rather than the fills.** On the basis `-u1-ptr` over `-u1` reads **0.9693** where 0.92 was predicted, `-u2-ptr` over `-u2` **0.9479** where 0.88 was, and `-u2-ptr` over `-u1-ptr` **0.9431** where 0.90 was --- every one ahead of what it was priced against and past this half's 0.31% floor, and every one short of its span. **The counted work says the derivation was right**: the three corrected instruction ratios read 0.8944, 0.8357 and 0.8605 against the 0.8945, 0.8356 and 0.8604 the registration derived them from, so what missed is the conversion --- 29%, 32% and 41% of each saving reaching the clock where the nineteenth reading's rate assumes about three quarters. **On the HEAD half every one of the three reverses**: 1.3084, 2.6731 and 1.9710, at 2 of 19 and 0 of 19 twice. That is not this item's kill, which wanted both halves, but it is the larger finding: `-u2-ptr`'s counted work goes from 0.8357 to **1.8842** and its allocation from **1.00x to 2.61x**, so GHC HEAD compiles the `Ptr`-walking fill into an allocating one. The item's last clause --- that both allocate 1.00x as their parents do --- holds on the basis and is false on the other half.
