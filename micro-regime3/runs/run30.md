# Run 30 (LiberateCase against plain -O1)

One run's write-up: its head, its Results, what the next run compares against, the properties that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 30 (LiberateCase against plain -O1): the flag is worth a sixth to the reference, and it reaches the shipped fill, which is the finding.** The pair is Run 29's CONTROL recipe with and without `-fliberate-case` --- one source, one compiler, one shim, one roster and one bench order, **the UNFLAGGED half publishing, which reverses six runs of practice, so `the basis` below is the half without the flag and every `cross` figure reads basis over control** --- so every figure that moves between the halves moved because that flag was added to one command line. Over the eighteen main-set arms that carry a corrected time, ten sit within 1% of 1 and the eight outside are two families and one arm with its adjacent copy: `list` with its two A/A copies at **1.1710**, 1.1704 and 1.1743, `bq-expand` with its two at **1.0127**, 1.0126 and 1.0128, and the shipped leaf `mut-odo-vecdims-add-in-leaf-u2` at **0.9891** with `mut-odo-vecdims-add-in-leaf-u2-aa`, its adjacent A/A copy, beside it at 0.9893. **That split is over the run's own nineteen shapes**, and the adjacent copy is the one figure in it the intrusion reaches: requoted over the eighteen, as Provenance explains, it reads 0.9919 and sits INSIDE 1%, which would make the split eleven and seven. The leaf itself is unaffected, reading 0.9891 on nineteen and 0.9900 on eighteen. So the flag buys the reference about a sixth and the branch's own expander a point and a quarter, and costs the shipped leaf a point --- which is registration (1) KILLED, by 0.09 of a point past its own bar --- **a margin that rests on one capped cell and on which statistic is read, as that item's verdict sets out, and which would reverse on the column this table publishes** --- and the first reading here in which an -O2 pass makes the shipped fill slower rather than leaving it alone. What knowing it costs is registration (2), killed as Run 29's was: `list` is the reference every column here divides by, and a reference that moves a sixth is one the two halves cannot be differenced against on any population.

**The roster is 36 timed arms over 19 main-set shapes and 684 benches, with 58 class views over ten classes for 2088 more, and it is Run 29's EXACTLY.** `./roster-delta.py run29-spec run30-nospec` reads 36 arms to 36 over 19 shapes to 19: NOTHING in and NOTHING out, the 36 survivors in the same order and no shape moved. NO CLASS VIEW MOVES either: `runs` stays at fourteen, `window` at eight, `bcast` and `flip` at six, `block` and `small` at five, `bcastmid` and `compose` at four, `rev` and `scaled` at three. `Main.hs` stands at `7685375`, the commit Run 29 built, so there is no commit to list and nothing landed or was parked. This pair's roster term is ABSENT rather than small --- so every figure that moves between these halves, and every figure that moves between this basis and `run29-nospec`, is the variable's or the box's and cannot be the roster's. The L1 roster pass was therefore NOT owed, step 12's condition asking for a membership change; six of its eleven legs were taken anyway because the basis is a recipe no L1 pass had ever run on, and all six came back clean.

**NOT ONE CELL SINKS BELOW THE SHARED FORCING PASS ON EITHER HALF**, as on Runs 28 and 29 and for the same two reasons: the ruling of 2026-09-10 that a reducing consumer has no corrected time, so the fourteen `-sum` rows read `--` in `time` and `worst` by design, and the retirement of every Fill arm over a list to `check`. `read-all.sh --brief-facts` reports an empty sunk list for both halves, so every row that carries a corrected time at all is a geomean over all nineteen shapes. The L1 roster pass had said so a day early on six of the eleven populations, and on a basis the ruling had never been read against --- a plain -O1 half, where every earlier pass ran under `-fspec-constr`: the reader's own warning string is absent from all six of its legs and from the smoke sweep beside them.

**The gate read SOUND, the machine check FIRED, and the box turns out not to have moved at all.** The gate's soundness is read as Run 29's was, on the two palindrome passes AGREEING rather than on any of them sitting near 1: `mut-odo-vecdims` 0.9871 against 0.9983, `bq-expand` 0.9923 against 1.0126, `list` 1.1475 against 1.1834, the narrowest apart being `mut-odo-vecdims` at 1.13% and the widest `list` at 3.13% of itself --- a spread between two passes, not the 3.13% the control half's worst A/A cell reads below --- wider than any figure Run 29 recorded, and recorded rather than explained, five benches at a gate's budget being no instrument for a three-point question. What the two passes do settle is the sign, and they settle it without a crossing: `list` reads 0 of 19 shapes faster on the basis in both passes. The MACHINE CHECK then fired, reading `list`'s net at a geomean of **+12.96%** against the fingerprint `runs/run29.md` keeps, worst `stretch-r5-8x432` at +48.61% and 19 of 19 shapes past 5%. **That is the flag and not the box, and the pair note said so before the run.** The kept fingerprint is Run 29's FLAGGED half's while this basis is that run's CONTROL recipe, and Run 29 measured its two halves 13.79% apart on `list`, so +12.96% is 0.94 of a gap already attributed. What settles it is not that argument but a reading, and the reading costs no machine time: against `run29-nospec`, the previous build of this very recipe, the whole timed roster spans **0.9937 to 1.0069** --- every arm inside 0.7% --- with `list` at **0.9951** and `stretch-r5-8x432`, the shape the check called worst, at **1.0015**. So this run publishes INSIDE the third machine era, no fourth era opens, and no figure below is bridged.

**Every one of the twenty-two processes gated clean --- `--selftest` and both `--aa` gates --- and the PLATEAU GATE FIRED on this pair's own variable, as Run 29's did.** It reports the preamble's victim spanning 18.0291 to 21.4052 ms/iter across the run, an 18.73% spread against a 5% band. Three readings say what that is. WITHIN each half the plateau is flat --- `nospec` 21.1169 to 21.4052, **1.37%**, and `libcase` 18.0291 to 18.3911, **2.01%** --- and the split is exactly by half, eleven processes each. The saturation STATE is identical to the byte on all twenty-two, `inuse=95420416` and `keep=8.19844333056e12`, one distinct value apiece. And the victim is `vgg-14-c512-k3/list`, the one bench whose time this pair's flag moves most. So the gate bands a TIME across two halves whose variable changes that time, which is the second pair here able to do that to it; the run is sound and the reading is per half. THREE A/A worst cells sit above 5%: `nospec-flip` **12.92%** on `flip-last-rows`, `libcase-window` 6.30% and `libcase-runs` 5.90%. The first is not a wild cell but the correction's own arithmetic showing: on that bench the forcing pass is **51.5%** of the total, so subtracting it amplifies any disagreement by 1/(1-f) = 2.06, and this class's floor is published as **2.05%** --- the widest A/A pair being this one --- with its worst cell at 12.92%. That column is `read-all.sh`'s, ONE figure a process over all eight pairs, where a class block's Controls line gives the worst cell of that process's LARGEST pair --- usually a different figure, and on `flip` the same one, the largest pair there being the one that carries the cell.

**The sequence ran in ONE window, and ONE intrusion was found --- the write-up session's own.** Twenty-two processes from 2026-09-13T02:05:18 to 10:04:58, every one exiting 0 at its roster's count --- 684 twice, 504 twice, 288 twice, and 216, 180, 144 and 108 four times each, 5544 benches in all --- with no hole between one process finishing and the next starting, no complaint, and no `!!` line anywhere in the wall-clock record. The control half ran first throughout. `--wild` over all twenty-two logs clears twenty-one of them outright, no bench reaching 0.25 of a core. **The twenty-second does not.** `run30-libcase-main` carries two benches at or above the bar, both on `cnn-L1-6x6-c1`: `mut-odo-vecdims-add-in-leaf-u2-aa` at **0.77** of a core with a worst sample of 260.6 ms foreign, and `-u2-ptr` at **0.61**. They are benches 11 and 12 of 684, about sixty seconds after the sequence began --- dated off the log's own `cpu=` stamps, 5966 jiffies past the process's first, rather than by assuming every bench takes the same time --- and the intruder was `./run-status.sh run30`, which this session ran while reading the gate's verdict and which puts the reader twice over a 600 KB README. The chapter's rule that the rerun window is quiet FOR THE DRIVER TOO was broken by the driver, on the ordinary window rather than a rerun's. **The two cells are OMITTED rather than substituted, and only where they enter**; which figures were requoted, which were not and why, and why substitution was refused are under Provenance. The size of the thing is one A/A cell reading 1.0390 where its clean twin on the other half reads 1.0065, on a pair whose true ratio is exactly 1 --- about 3.2 points --- and no ITEM's verdict in this file turns on either cell. **One SPAN does move**, and it is named here rather than left to be found: `(7)`'s pair reads 1.0388 KILLED over the nineteen and 1.0227 HELD over the eighteen. It changes nothing, because (7) is adjudicated on `rev`, `scaled`, `runs` and `block`, and `cnn-L1-6x6-c1` is a main-set shape that none of them carries --- which is also why no class reading anywhere can be touched by this at all.

**What the flag is worth is the same shape on every population, and the fill family moves on nine of the eleven rather than on four.** `list` moves by about a sixth everywhere, 17.10% at its narrowest --- on the main set --- and then, per class, 17.26% on `bcastmid`, 18.00% on `small`, 18.18% on `rev`, 18.23% on `flip`, 18.65% on `scaled`, 18.71% on `block`, 19.46% on `runs`, 19.60% on `bcast`, 20.27% on `window` and 21.71% on `compose`, its widest. The three shipped-fill arms registration (1) names hold inside 1% on all three arms on `window` and `scaled` alone; exactly one of the three falls outside on seven classes --- `rev`, `bcast`, `bcastmid`, `runs`, `block`, `small` and `compose` --- and two of the three do on `flip`; **ten of their thirty-three arm-by-population readings sit outside it**, and where Run 29's ten fell on four classes and read one way, these are scattered over nine populations and read both ways --- the leaf outside on the main set at 0.9891, on `bcast` at 1.0119, on `flip` at 0.9763 and on `small` at 0.9830; `lib-stage1` on `bcastmid` at 0.9786, on `runs` at 1.0111 and on `block` at 1.0326; `lib-stage2-lean` on `rev` at 1.0110, on `flip` at 0.9869 and on `compose` at 0.9840. So the item's kill fires on the main set, where it is read, and what the class readings add is that `-fliberate-case` reaches the fill everywhere and in no consistent direction.

**The counted work covers every population, no cell was refused anywhere, and its finding is the sharpest in the run: of the eighteen arms the flag moves the instruction counts of EIGHT --- the `bq-expand` and `list` families of three each, and `lib-stage2-lean` with its twin by a tenth of a percent --- and leaves the other ten bit-identical.** Twenty-two sweeps in 43 minutes, `runs` longest at 434s and 392s, `rev` shortest at 14s for the pair apiece, with no `perf could not count` line in any of the eleven pairs of files --- and the pair note's predicted scale, derived from Run 29's own stamps, came back right to a second or two on all eleven `nospec` legs and overshot six of the eleven `libcase` ones --- it reads Run 29's unflagged column for BOTH halves, and the flagged half counts faster, so the scale is an upper bound on one half rather than an estimate of it. Over the eighteen main-set arms that carry a corrected time the counts geomean is **1.0528**. **Split per arm it is not a spread at all but a partition.** TEN of the eighteen read counts **exactly 1.0000** --- the whole shipped fill family, `lib-stage1`, `mut-odo-vecdims` and their A/A copies, not one instruction different between the halves --- and `lib-stage2-lean` and its twin read 1.0011. Only `bq-expand` at **1.1298** and `list` at **1.2050** move by more than that, and `list` converts most of it where `bq-expand` converts a tenth --- the flag saves them 17.0% and 11.5% of their instructions and 14.6% and 1.3% of their time, `time/counts` reading **0.9718** for `list` and **0.8963** for `bq-expand`, against 0.9891 to 1.0019 for the ten unmoved arms --- for which `time/counts` IS the time ratio, their denominator being exactly 1 --- so their time moves with no instruction moving at all. **That is the exact opposite shape of Run 29's counted work**, where `-fspec-constr` took about a twentieth of the instructions off EVERY arm and only two converted any of it. So the two -O2 passes reach this roster in different ways: one thins every arm and pays off on two, the other touches two arms alone and pays off on one of them. Registration (1)'s kill is where that leaves a mark this run cannot resolve --- the shipped leaf's time moves 1.09 points on an instruction count that did not move by one, so what moved it is the memory or the placement and not the codegen.

**This run's two columns may NOT be differenced, and that is true of every one of the eleven populations.** `list` moves between the halves by 17.10% at its narrowest, on the main set, and 21.71% at its widest, on `compose` --- so `read-all.sh --brief-facts` marks all eleven PAST the 0.7% bar and each class block says so in its own cross-half line. Every arm-by-arm figure across the halves in this file is therefore an ORDERING and not a subtraction, which is what Run 29 also had to say and for the same reason under the other -O2 pass. What survives the bar untouched is everything read WITHIN one half --- every `pair` span and every class property --- and those carry the run.

**The fastest timed arm on the main set is `lib-stage2-lean` on the column and is NOT the fastest paired, which is the divergence to read rather than the lead.** `lib-stage2-lean` reads **0.024** in the published `time` column, alone at the head, with `lib-stage2-lean-u1`, `mut-odo-vecdims-add-in-leaf-u1-ptr` and `mut-odo-vecdims-add-in-leaf-u2-ptr` behind it at 0.025 and the shipped leaf at 0.027, against `mut-odo-vecdims`'s 0.045 and `bq-expand`'s 0.128. Paired, the ordering is not the column's: `lib-stage2-lean` over `mut-odo-vecdims-add-in-leaf-u2-ptr` is **1.0564** at 5 of 19 wins, so the `-ptr` leaf is ahead by better than five points on the statistic a margin should be judged by; over its own unrolling twin it is 0.9622 at 14 of 19 and p 0.064; and over the shipped leaf it is 0.9914 at 13 of 19 and p 0.17, a tie. The column winsorizes per row and the pair does not, which is the whole of the difference, and it is why no sentence here puts the lean fill in front of the leaf on the strength of a three-decimal column.

**Stage ten composes its two changes where both fire, costs a pass where neither does, and this run does not kill it for that.** `libunord-stage10-sum` is stage seven's tie-break under stage nine's zero-stride move, and over stage seven it reads **0.5903** on the basis and 0.5588 on the control on `bcast` and **0.6214** and 0.5827 on `bcastmid`, against 0.55 and 0.6 registered, with `compose` at 0.6942 and 0.6885; over stage nine on `window`, where no view carries a zero stride, it reads **0.7326** on the basis against 0.72 registered and **0.8863** on the control, which is outside the registered 15% and is the one span of the four to miss. No zero-stride view has it behind stage seven past the floor on both halves, the closest being `compose-scalar` at 1.0010 and 1.0012 against floors of 0.28% and 1.44%; and no unstrided `window` view has it behind stage nine on both --- `window-128x128-k7` reads 0.9703 on the basis against 1.2538 on the control and `window-64x64-k1x9` 0.9621 against 1.1481, **the same two views Run 29 found parting in sign and now parting by twenty-eight and nineteen points**. **Where neither change fires it is still not free, and the difference from Run 29 is the bar and not the reading.** On `rev`, whose views carry no zero stride and no tied pair, `zerosOutermost` hands its list back unchanged and `routeUnord10` is `routeUnord7` to the byte, yet the pair reads **1.0244** on the basis and **1.0245** on the control --- outside each half's own floor, which is what killed registration (7) on Run 29, and inside the 3% the item was amended to on 2026-09-13, which is what holds it here. `small` prints 1.2202 and 1.2171, inside the 1.18-to-1.40 band stage nine read on Run 28, which the item's own text dropped when it was carried over, and with no verdict, as registered.

**The fused list survives every consumer this run points at it, and the flag is worth far more to it than `-fspec-constr` was.** Against the fill it replaces with nothing else different, `libunord-stage5-sum` over `libunord-stage3-sum` reads **0.5808** and 0.4900 on `runs` and **0.4520** and 0.4237 on `block`, and ties inside its own floor on all five classes where both routes fill --- widest 1.0023, on `bcastmid`. Against the port's base-offset table, `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.4678** and 0.3947 on `runs` and 0.7281 and 0.6800 on `block`, so the odometer's fold beats master's table on the control half as on the basis. **And the flag reaches the consumer hard.** Read raw over every shape, as an arm with no corrected time must be, `libunord-stage7-sum` --- the consumer of `libunord-stage7`, which is checked and not timed --- across the halves is **1.1827** on `runs`, **1.1022** on `window` and **1.0633** on `block` --- 18.27, 10.22 and 6.33 points, in the same measure as the reference's above --- where Run 29 measured `-fspec-constr` worth about two percent to the same arm on the same three populations. On the five classes where the route fills or is one block it is level, 0.9994 to 1.0037. Registration (3) predicted 0.9 there and neither of its kill conditions fires; its span is inverted rather than wrong, the item being Run 29's, whose basis was the FLAGGED half where this run's is the unflagged one, so a clause predicting the flagged half faster reads above 1 here.

**The three class properties hold in all ten classes on both halves, and on the main set too.** `mut-odo-vecdims`'s `worst` stays under 1 everywhere --- 0.113 on the main set and between 0.028 and 0.085 in the classes, lowest on `compose` and highest on `small`. It is ahead of `bq-expand` on every shape of every population, closest on `small-patch-k5` at 0.4861 among the classes, then `flip-last-rows`'s 0.4736 and, on the main set, at **0.9997** on `stretch-pow2stride`, which the column prints as 1.000 and which is a win by three ten-thousandths inside a 0.57% floor --- the one reading here where that clause is nearly a tie rather than a rout. Both allocation clauses hold with the 1% margin, closest at `small-patch-k5`'s 0.05918 against `list` and `scaled-rank1-m1`'s 1.00003 against `bq-expand`. The tier ordering is unbroken in all ten: the fills at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 20.99x to 27.66x. `small` reads its fills at 1.27x, the per-call constants showing through a call under a microsecond, and its order is unbroken like the rest. **But the WIDER statement those properties are read for does break, on one arm, and it is the route the library ships**: `lib-stage1` is slower than `list` at `runs-2` on both halves, 1.0949 and 1.2915, and at `runs-3` on the control, 1.0572 --- the only timed arm above its shape's `list` anywhere, and the properties section beside property 1 is where it is read.

**Across the halves every class's geomean puts the basis slower, though the counts on `window`, `flip` and `block` do not, not one of the ten may be read for the pair's variable, and dropping two families takes every one of them to level.** The basis is SLOWER on 13 of 18 arms in `rev` and `scaled`, 12 in `bcast` and `small`, 11 in `compose`, 10 in `bcastmid` and `runs`, 9 in `flip` and `block` and 8 in `window`, at class geomeans from **1.0198** on `flip` to **1.0319** on `bcast`. But each class's `list` moved 17.26 to 21.71 points between the halves, past the 0.7% that lets two columns be differenced, so every one of those lines is disqualified as a reading of the flag and stands only as an ordering. What the vote does show is WHOSE the movement is, and it shows it more cleanly than Run 29 could: drop the `list` and `bq-expand` families and the same ten geomeans run **0.9868** on `flip` to **1.0020** on `bcast`, with the main set at 0.9987 --- twelve arms apiece, ten of the eleven populations now BELOW 1, and the whole two-to-three percent gap gone with the six arms that carried it.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The two `sum-only` halves agree to within a seventh of a percent on every population, from 0.9993 to 1.0012 on the basis, `flip` to `bcastmid`, and 0.9992 to 1.0002 on the control, so the term subtracted from one half is the term subtracted from the other and no ratio here is an artefact of a moved forcing pass. That matters more on this pair than on most: the two halves' absolutes part by a sixth on the reference, and a correction that had parted with them would have put that sixth into every corrected column.

**The two halves do NOT resolve alike, and the basis resolves better.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **0.92** over the 36 arms, 14 of them wider on the basis and 22 narrower, where Run 29's pair read 0.97 and split 16 against 20. Read it beside the A/A floor and not off it: this column is sampling error INSIDE one bench where the floor is agreement BETWEEN two placements of one arm, and Run 18 moved the two in opposite directions. The floors say the same thing in their own currency, 0.57% on the basis against 0.84% on the control.

**The straddling loops stand at EIGHT on each half, and the two halves' self-loop counts part by four.** `--survey` reads 284 self-loops of at most 64 B on the basis, 118 of them at offset 0 and 8 still straddling, against 280 on the control, 125 at offset 0 and 8 straddling --- and the basis's three figures are `run29-nospec`'s TO THE NUMBER, which is this run's repetition read a fourth way, after the md5, the `.text` and the fills. The halves part by 4 loops where Run 29's parted by 62 and Run 28's two compilers by 11, and it is the LiberateCase half that carries the fewer, where its `.text` is the larger by 20480 bytes --- 20 KiB exactly. Nothing here explains that pairing and this file does not try: `--survey` counts self-loops of at most 64 B, so both a loop removed and a loop grown past the cutoff leave it, and both halves' `.text` deltas are exact multiples of 64, which separates nothing. **Post-run step 0 names SIX of each half's eight off the `-g3` twins by byte identity** --- `fillStage2Short`, `fbMutOdoVecdimsAddInLeafU2` twice, and its `Down`, `Last` and `Ptr` forms --- and refuses the other two on both halves, at mod-64 offsets 53 and 42, where no twin holds a byte-identical copy and `--loose` offers only a signature family with more than one member. The two parked leaf forms are among the named though nothing times them: parking takes an arm off the roster and not out of the binary. That is the same six and the same two refusals on each half, which is what a flag that displaces no tracked loop looks like from the naming side.

**The regime was confirmed in this run's own binaries before the hours were spent, and BOTH halves read the same side of it.** `diag` reads `baseOffsetsScan` against `baseOffsetsMut` on `vgg-14-c512` at 24066407 against 2408530 on the basis and at the SAME TWO FIGURES on the control, 9.992 times apart on each --- the ten times this README has attributed to plain -O1 since Run 8, read twice. So neither half carries SpecConstr, which is what makes this pair Run 29's control read twice, and a half reading equal to three figures would have been the flag still on and the stop the pair note carried. This is also where the pair differs from Run 29's: `-fliberate-case` moves neither of those two rows, so `diag` cannot see the variable at all, and what stands in for pre-run step 9b is the md5 pair --- the basis reproducing `run29-nospec`'s to the character and the control differing from it, which is the flag reaching the compiler and the only reading that says so.

**Run 30 records every population twice** --- the main set and all ten stride classes from each half, one process apiece, twenty-two in all --- so every class figure below has a control-half twin and every one of the ten was gated on both. That is what lets a class say something the main set cannot, and on this pair it is what shows the flag acting alike on all eleven: the reference moves by about a sixth everywhere, 17.10 points at its narrowest, while the twelve arms outside the `list` and `bq-expand` families sit level on every population.

**The decomposition reproduces on both halves, and its two columns part where the flag does.** The riders time each shape's `list` alone, one bench to a process, clean and then saturated, and the state the preamble puts on a process comes back at a geomean of **1.1205** on the basis and **1.1578** on the control --- which is NOT the sixteen ten-thousandths Run 29's two halves agreed to, and is the flag reaching the alone leg as it reaches everything else `list` does. What the roster then adds on top of that state is **1.0157** on the basis, 5 of 19 shapes above 1, and **1.0246** on the control, 9 of 19; the basis's rest runs 0.9729 on `stretch-bigstride` to 1.2336 on `stretch-r5-8x432`, the control's 0.9768 on `vgg-14-c512-k3` to 1.2419 on `stretch-inner256`, so the wider of the two spreads is the flagged half's this time.

**There IS a repetition this run, it is the basis half's, and it reproduced --- the FOURTH this chapter has read, after Run 11's of Run 10's aligned binary, Run 19's of Run 18's basis and Run 23's of Run 22's, and its repeated binary is one the previous run did NOT publish, `run29-nospec` having been Run 29's control.** `Main.hs` did not move from `7685375` and the shim did not move from `b3a1aca`, so `run30-nospec`'s inputs are `run29-nospec`'s entire and its md5 had to reproduce: it is `f507eafbcbbfecf7c2151f390caf9b92`, that binary's to the character, with the control at `66f89adf1838ccfcf63ec11d5a10b047`. So the md5 is TWO-SIDED here rather than one-sided --- the basis had to match and did, the control had to differ and does --- and what that buys is the one input no note records: `cabal.project.freeze` pins 97 versions and NOT the ABI hashes, so a store relinked at unchanged versions would have moved half of `.text` with every other check still passing. It did not. `--delta` against `run29-nospec` then reads every tracked fill's mod-64 offset PRESERVED --- [0, 24, 8, 0, 4, 0] and [0, 0] --- and, unlike every run before this one, EVERY ADDRESS SURVIVING TO THE BYTE, 6 of 6 and 2 of 2, with `nothing moved` on both groups and no displacement at all. That is the pinning claim in its tightest possible form, and it is the repetition's rather than any arm's: the roster gained no timed function, so what the reading proves is that identical inputs place identically, which is the control the claim has never had. Within the pair, `--library` reads 925 library self-loops in common, **100.0%** at the same offset in line and **100.0%** in the same straddle state --- the exact opposite of Run 29's 39.4% of 914, for a flag that grows `.text` by 20 KiB.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. What this run leaves the next one is the same lesson a third time, on a third variable. Seven of its nine items argue a no-op somewhere --- (1), (2), (3), (5), (7), (8) and (9) --- and BOTH of its kills fell among them, on (1) and (2); not one item predicting a REAL effect was killed. Run 28 found that, Run 29 found it again on `-fspec-constr`, and this run finds it on `-fliberate-case`, which makes it three runs and two flags. So an argument that a change cannot reach a population remains the cheapest clause a registration can carry and the least reliable, and (1) is the sharpest instance yet: it argued the shipped fill was out of reach of an optimisation flag, on a measurement taken under the other one, and lost by 0.09 of a point.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a PLAIN -O1 table** --- the first published here since Run 7 that is not a `-fspec-constr` one, and so the first whose regime is the regime `Data/Array/Internal.hs` actually compiles under. **A row's distance from the last published column is NOT a distance from Run 29's basis**: that column is Run 29's FLAGGED half's and this recipe is that run's CONTROL, so the comparable predecessor is `run29-nospec`, which this run's basis reproduces byte for byte, and no earlier run's basis will do. **Against it the raw distance IS readable, and it is almost nothing**: over the 18 arms that carry a corrected time, this basis runs **0.9937 to 1.0069** --- every one inside 0.7% --- with `list` itself at **0.9951**, on a roster that did not move and a source that did not move, which makes this the tightest cross-run reading this chapter has taken. **The `alloc` column is a median over this run's own nineteen shapes**, `bq-expand` at 2.78x and `list` at 25.20x, so it is a statistic of a strategy and a shape set together and does not cross to a run that timed a different set.

**And it is the basis half's**, `run30-nospec`, as every published table here is from Run 11 on: the control half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-six rows. That the published half is the UNFLAGGED one is this pair's own decision and it reverses six runs of practice --- Run 29 answered what `-fspec-constr` is worth and the shipped file does not set it, so the recipe that matches what the library actually compiles under is the unflagged one, and `libcase` is the candidate reading. **No row here is a first reading**: the roster is Run 29's exactly, nothing landed and nothing was parked, so every one of the thirty-six rows has a twin in that run's file measured under the other flag.

**Comparing runs?** The table below is Run 30's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the properties to test are [the ones after it](#the-properties-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.57%** from 1 on the basis half or **0.84%** on the control, read over the eight pairs this roster carries, and over the four pairs that carry back to Run 10 at **0.57%** and **0.56%**, beside which the worst SINGLE A/A cells of the two MAIN-SET processes, 2.53% on `stretch-wide-2xM` and 3.13% on `alexnet-L1-55-c3-k11`, are not floors at all and are not to be quoted as any --- and are not this run's worst, `nospec-flip` carrying **12.92%** on `flip-last-rows`, where the forcing pass is 51.5% of the bench and the correction doubles any disagreement --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **The control half's worst main-set cell is quoted over eighteen shapes, not nineteen**: its `cnn-L1-6x6-c1` cell is one of the two this run's own intrusion spoilt, and Provenance says what was dropped and why. **And one thing this run does NOT have**: its two columns may not be differenced on any population, `list` having moved by a sixth or more between the halves everywhere, so every cross-half figure here is an ordering.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What cost a row its population on Run 27 was the correction, not the estimator, and on THIS run nothing does**: that run had nine rows on each half carrying cells the shared forcing pass is not smaller than --- seventy such cells on its basis and forty-three on its HEAD half, all of them `libunord` arms --- and this roster has none, so every row here that carries a corrected time is a geomean over all nineteen shapes. What replaced those rows is the `no_net` ruling and the retirement of the Fill arms over a list, named in the head and in *What the next run compares against*, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what property 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.60* | *55* | *2.78x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.58 | 69 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.57 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage10-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's tie-break with stage nine's zero-stride axes moved outermost -- the two reorderings composed |
| libunord-stage2-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over the branch's port |
| libunord-stage3-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over the ceiling's fill -- stage five with its runs turned into fills |
| libunord-stage5-sum | -- | -- | 0.02 | 83 | 0.00x | new mutating `Vector` method -- `sumT` over stage five's list, one slice at a time and no concatenation |
| libunord-stage6-list-sum | -- | -- | 0.01 | 83 | 0.00x | the same, base's `sum` over stage six's list, the fold a library user brings |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage9-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.37* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.02* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.02* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.024 | 0.113 | 0.59 | 70 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.025 | 0.113 | 0.62 | 70 | 1.00x | the same, unrolled -- the shipped fill's ceiling under an allocator that spills nothing |
| lib-stage2-lean-u1 | 0.025 | 0.110 | 0.59 | 69 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.025 | 0.112 | 0.58 | 69 | 1.00x | nothing the library would take -- the merged-bound leaf walking a `Ptr`, a ceiling and not a candidate |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.113* | *0.62* | *69* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.113* | *0.50* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.113 | 0.47 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.111 | 0.52 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| lib-stage1 | 0.028 | 0.113 | 0.56 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| *mut-odo-vecdims-aa-distant* | *0.045* | *0.112* | *0.42* | *66* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.045** | 0.113 | 0.37 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| *mut-odo-vecdims-aa* | *0.045* | *0.112* | *0.43* | *66* | *1.00x* | *A/A control* |
| *bq-expand-aa-adjacent* | *0.128* | *0.253* | *0.68* | *50* | *2.78x* | *A/A control* |
| bq-expand | 0.128 | 0.253 | 0.63 | 50 | 2.78x | nothing (pure) -- the last candidate |
| *bq-expand-aa-distant* | *0.129* | *0.253* | *0.40* | *50* | *2.78x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.75 | 21 | 25.20x | -- |
| *list-aa-distant* | *1.001* | *1.008* | *0.79* | *21* | *25.20x* | *A/A control* |
| *list-aa-adjacent* | *1.003* | *1.008* | *0.58* | *21* | *25.20x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and pairs still part in SIGN between the two on this run, at the very head of the table. `lib-stage2-lean` against `mut-odo-vecdims-add-in-leaf-u2-ptr` is the sharpest: the published column divides to **0.9860**, saying the lean fill leads, where the paired figure is **1.0564** at 5 of 19 wins, saying the `-ptr` leaf leads by better than five points. That is a seven-point disagreement between the two statistics on the two fastest rows here, and it is why the head paragraph reads the table's leader off `--pair` rather than off the column. `mut-odo-vecdims-add-in-leaf-u2-aa` against its base is the well-behaved case, paired **0.9989** against a published **0.9988**, and `--predictions` prints both when it adjudicates registration (9). A margin between two arms is `--pair`'s paired figure, which is also the statistic the floor is defined in; `--pair` and `--predictions` print both side by side and say when they part. The column is for reading the table, not for differencing it.

`concat-runs` has no row, and neither do the other 78 arms the roster holds and checks without timing --- **79 of its 115** in all, the same as Run 29 checked of the same 115: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). **Nothing was retired to `check` this run and nothing was parked**; the thirteen Fill arms over a list, `liblist-stage1` to `-stage4` and `libunord-stage1` to `-stage9`, went that way on 2026-09-09 and stay there, and the roster is Run 29's exactly. So a movement against Run 29's CONTROL column --- which is this basis's recipe, and not the column that run published --- is a movement on all **18 arms that carry a corrected time**, a day apart, with no roster term between them and no box change, which is why the cross-run reading here is a subtraction and not `--bridge`'s.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is `lib-stage2-lean` at 0.024**, with `lib-stage2-lean-u1`, `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr` at 0.025 and `mut-odo-vecdims-add-in-leaf-u1` and `-u2` at 0.027, `lib-stage1` at 0.028 --- **seven timed non-control arms below `mut-odo-vecdims`'s 0.045**, every one of them a fill that writes the result, and the same seven Run 29 had. **But the column and the pair disagree about which of them leads**, which is the second finding: paired on the basis, `lib-stage2-lean` over `mut-odo-vecdims-add-in-leaf-u2-ptr` is 1.0564 at 5 of 19, so the `-ptr` leaf is ahead by better than five points where the column puts it second; over the shipped leaf it is 0.9914 at 13 of 19 at p 0.17, a tie; over its own unrolling twin 0.9622 at 14 of 19 at p 0.064. **The third is that the leaf fusion is unmoved by the flag**: `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6446** on the basis and **0.6525** on the control, eight thousandths apart.

**The pointer fills are not what this run was built to read, and they are here as the ceiling Run 27 made readable.** `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr` are the shipped fill's leaf and its unrolled form rewritten to walk a `Ptr`; they are refused for the library ([dead ideas][dead]) and timed as a ceiling and nothing else. The three spans --- `-u1-ptr` over `-u1`, `-u2-ptr` over `-u2` and `-u2-ptr` over `-u1-ptr` --- read **0.9748**, **0.9385** and **0.9323** on the basis and **0.9761**, **0.9268** and **0.9272** on the control, so `-fliberate-case` does not reach them either: the widest of the six pairings across the halves is a point and a sixth, on a main set whose floor is 0.57%. That is the same verdict Run 29 returned about `-fspec-constr` on the same three spans, so the ceiling these arms measure is now known not to be either -O2 pass's.

**The two standing placement controls are still gone with the prune, and the straddlers stand at eight on each half.** Within the pair there is nothing to read: `./loop-offsets.py run30-libcase run30-nospec` puts the tracked six-copy group at offsets **[0, 24, 8, 0, 4, 0]** on BOTH halves and the two-copy group at **[0, 0]** on both, so not one tracked fill copy moved between them --- which is the same verdict `--library` returns over the whole library, 925 self-loops at 100.0% the same offset in line and 100.0% the same straddle state. Against the previous build of the basis's own recipe, `run29-nospec`, which this basis reproduces byte for byte, `--delta` reads every one of those offsets PRESERVED and, unlike every run before this one, EVERY address surviving to the byte, 6 of 6 and 2 of 2 with no displacement at all. So neither comparison this run can make finds a fill that moved: not the pair, and not the repetition.


## What the next run compares against

**Run 31's pair is settled by the request of 2026-09-13: both halves are THIS run's basis recipe, ghc-9.12.4 in the dead-spot form with neither -O2 pass set by hand, one built at `-O1` as it is and one built at `-O2` instead** --- so `run30-nospec` is the basis's previous build and the basis tag stays `nospec`, which still names what that half is. The other half's tag names what it is too and neither is a prefix of the other. **The one thing its recipe must get right is that `-O1` is micro.cabal's own `ghc-options` and not a flag the pair passes**, so the `-O2` half has to override it rather than add to it, and the note's `strings`/`--info` reads are what confirm which level a binary actually carries. **What the three runs then compose into is the point of it.** Run 29 priced `-fspec-constr` alone on this roster and Run 30 priced `-fliberate-case` alone; `-O2` turns on BOTH, among other things, so Run 31 asks whether -O2 is those two passes on this workload or more than them. The two single-flag readings are on record to be composed against: on `list`, the arm both flags move most, Run 29 read its two halves **13.79 points** apart and Run 30 reads **17.10** on the main set. A registration for Run 31 should predict the -O2 half against BOTH of those and name what it would mean for the composed figure to fall short of them, to land between them, or to exceed them --- and it should say on which populations, since this run found the flag reaching every one of the eleven while Run 29's spared none either. **What this run leaves settled for it** is the published basis, which moved for the first time since Run 16: the tables are read in PLAIN -O1, the regime `Data/Array/Internal.hs` actually compiles under, so Run 31's basis needs no rebuild of that decision and its cross-run absolutes go against `run30-nospec`. **The compiler question stays closed**, as Run 27 closed it and Runs 28 and 29 confirmed it; this run did not ask it and Run 31 does not either, both halves being ghc-9.12.4 again. **And two code questions stand named.** Stage ten costs a pass where neither of its changes fires, at 1.0244 and 1.0245 on `rev` and 1.2202 and 1.2171 on `small`, so a route that skipped `zerosOutermost` where no stride is 0 would be worth timing --- now measured under both flags separately and unmoved by either. And `lib-stage1` fills through `fillStage2`, which closed the `bcast` and `bcastmid` gaps registration (8) was written around, leaving `runs` and `block` --- where the two routes genuinely part, at 0.2329 and 0.4160 --- as the only populations that still price the dispatch.

**The REGIME variable has now been asked twice, one pass at a time, and this run is the second half of that.** Five pairs before Run 29 varied the compiler: Run 24 read the two 5.7% and 6.8% apart on the instructions of two pure arms; Run 25 read `list` 1.10% apart in time and 0.33% in counts; Run 26 found twenty-four of twenty-six arms inside a percent in counts and the two `Ptr` arms outside at 0.6219 and 0.9295, which was GHC [#27778](https://gitlab.haskell.org/ghc/ghc/-/work_items/27778); Run 27 worked it around and read every arm inside a percent; Run 28 confirmed it at a counted geomean of 1.0027. **Run 29 then varied `-fspec-constr` and this run varies `-fliberate-case`, the other pass GHC turns on at -O2 and not at -O1, so the two runs price the -O2 pair on one roster and one source.** They do not act alike. `-fspec-constr` left the shipped fill family untouched --- registration (1) held on Run 29 at 1.0001, 0.9945 and 1.0044 --- where `-fliberate-case` reaches it and kills the same item at 0.9891. On the reference the two are the same size and the same sign: 13.79 points for `-fspec-constr` and 17.10 for `-fliberate-case`, both making `list` faster. What does not close at the class level is what Run 29 also could not close: **all eleven populations carry a `list` move past the 0.7% bar**, from the main set's 17.10 points to `compose`'s 21.71, so every class-level comparison of the halves is an ordering and not a subtraction, where Run 28 had four classes past it and six inside.

**What Run 30 leaves the next run to read against, and the first item is a check that fired and should not be inherited. The box is where Run 28 and Run 29 left it**, though this run's gate says otherwise at first reading: the machine check reads `list`'s net at **+12.96%** against the fingerprint Run 29 installed, which is that run's FLAGGED half's while this basis is its control recipe, and Run 29 measured its two halves 13.79% apart on the same arm. What settles it is `run29-nospec`, the previous build of this very recipe, against which the whole timed roster spans **0.9937 to 1.0069** and `list` reads **0.9951**. So this run publishes INSIDE the third machine era rather than opening a fourth, an absolute crosses between it and Run 29's control half unadjusted, and the fingerprint below is the one a Run 31 machine check reads against --- **a Run 31 gate reading near +13% against it, with both halves unflagged, would be the box and not a flag**, which is the reverse of this run's case and the reason this paragraph states which half a fingerprint belongs to. **The floor is 0.57% on the basis and 0.84% on the control**, read over the EIGHT A/A pairs this roster carries, and the restricted figure over the four pairs that carry back to Run 10 is **0.57%** and **0.56%**. **On the basis those two figures are EQUAL and are the same pair's**, `bq-expand-aa-distant`'s --- which is new: they came apart on Run 28 for the first time since the prune and further apart on Run 29, and on this half they close. On the control they still part, 0.84% on `mut-odo-vecdims-add-in-leaf-u2-aa-distant` against 0.56%. A margin ON THE MAIN SET is judged against the whole-set pair, 0.57% and 0.84%; a margin on a class is judged against THAT class's own floor, which its block prints and which runs from `small`'s 0.21% to `runs`'s 3.23% on the basis --- so `rev`'s registration (7) verdict clears 0.49% and not 0.57%. Every verdict in this file uses the whole set of its own population's pairs, which is what `whole-set` names; which of the two figures a margin ought to clear at all is [the open list][open]'s question, and this run gives it a third reading rather than settling it. **The two columns below may NOT be differenced**, `list` having moved **17.10 points** on this run's main set against the 0.7% bar, and by 17.26 to 21.71 points on every class --- so this section's two columns are an ordering, as Run 29's were.

**Registered with the pair.** Run 30's nine registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. Seven held and two were killed. **What a next registration should take from this one is the SHAPE of the killings and not their subjects**: not one fell on an item predicting an EFFECT, and both fell on items predicting a NO-OP --- something argued from the code to be unreachable, tested on the populations the argument said could not move, and moving. That is what Run 28 found of its own four and Run 29 of its three, on different variables and different rosters, so THREE runs now say it. On this roster an argument that a change cannot reach a population was the cheapest kind of clause to write and the least reliable, and (1) and (2) are the two instances; five other no-op arguments held, so the converse does not follow. **Nothing here was killed by the box**, the machine check having fired on the flag rather than on the machine, which `run29-nospec` settles.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at a run's OWN area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 30 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 29 to Run 30 --- **and it is a step between CONTROL recipes rather than between published bases**, this run's basis being `run29-nospec`'s recipe where Run 29 published `run29-spec`. The box not having moved, that step is a SUBTRACTION and not a bridge. Over the **18 arms both rosters time and both give a corrected time** --- which is all of them, the roster not having moved --- the basis column runs from **0.9937** on `mut-odo-vecdims-aa-distant` to **1.0069** on `bq-expand`, every one of the eighteen inside 0.7% of 1 and `list` itself at **0.9951**. No arm is outside, no arm's route changed, and the reader names none as being in one run only: this is the same source, the same shim and the same store built twice a day apart, and it is the tightest cross-run reading this chapter has. **The table below is this run's own two halves and no earlier run's**, nine strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in ONE COMPILE FLAG and in nothing else. **Its two columns may NOT be subtracted, and the second column is HIGHER throughout for that reason**: every cell is a ratio to that half's own `list`, and `-fliberate-case` speeds `list` up by about a sixth, so the flagged half divides by a smaller denominator and every arm's ratio rises with it. Read the columns as two orderings, never as a difference.
| strategy | Run 30 (plain -O1, dead-spot, -A32m, 9.12.4) | Run 30 (plain -O1 + `-fliberate-case`, dead-spot, -A32m, 9.12.4) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.045** | 0.052 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.027** | 0.033 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.027** | 0.031 |
| `mut-odo-vecdims-add-in-leaf-u1-ptr` | **0.025** | 0.030 |
| `mut-odo-vecdims-add-in-leaf-u2-ptr` | **0.025** | 0.028 |
| `lib-stage1` | **0.028** | 0.033 |
| `lib-stage2-lean` | **0.024** | 0.029 |
| `lib-stage2-lean-u1` | **0.025** | 0.031 |
| `bq-expand` | **0.128** | 0.148 |

**READ THE SECOND COLUMN AS A RATIO AND NOT AS A SPEED.** Every entry is that arm's net over `list`'s net in ITS OWN half, and `list` is 17.10 points faster WITH the flag --- so the control column reads HIGHER for every row because its denominator is faster, not because the arm is slower. **That is the DIRECTION and not an identity**: each entry is winsorized per row within its own half, so dividing an arm's two entries does not reproduce its `--compare` figure and is not meant to --- on `lib-stage1` the two entries divide to about 0.85 where the cross reads 1.0003, which is the denominator and the capping together and not a disagreement. The two columns may not be subtracted, and the arm-by-arm reading of what the flag is worth is in the head, off `--compare`, where the reference is not divided out.

**A published geomean is over the same 19 shapes, and two halves of one run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered. **THE TABLE ABOVE IS NOT SUCH A PAIR**, `list` having moved **17.10 points** on this run's main set --- which is the same fact as `list` being 17.10% slower without `-fliberate-case`, read the other way round, the two appearing in this file four sections apart: the two columns may NOT be differenced. All nine rows print higher on the control, without exception, and the cells are ratios to each half's own `list`, so the flagged half's smaller denominator lifts every one of them. Read down a column and the ordering is nearly the same on both --- `bq-expand` at the foot and `mut-odo-vecdims` between --- but NOT at the head: `lib-stage2-lean` leads the basis at 0.024 while `mut-odo-vecdims-add-in-leaf-u2-ptr` leads the control at 0.028 against its 0.029, so the flag changes which arm heads the column.

**The control half's own standings on the arms this run's roster carries, which no table here holds, every published table being the basis half's.** Read off the control half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9765** at 12 of 19 (0.9684 at 12 of 19) and against `mut-odo-vecdims` **0.6525** (0.6446); `lib-stage1` against `-u2` 1.0464 at 7 of 19 (1.0582 at 9 of 19); `lib-stage2-lean` against `-u2` 0.9715 at 15 of 19 (0.9914 at 13 of 19) and against `lib-stage1` **0.9284** at 15 of 19 (0.9368 at 15 of 19). **Every one of the five holds its direction across the halves**, and the widest the two readings part is two points, on `lib-stage2-lean` against the shipped leaf --- so the WITHIN-half orderings this run publishes are not the flag's, which is the half of the run the 0.7% bar leaves standing.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section][pershape]. The two tables below are installed from this run's own JSONs in the per-shape form the README describes, which replaced a fourteen-column one on 2026-09-04.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 6.34 us | 0.077 | `lib-stage2-lean-u1` 0.076 | `mut-odo-vecdims-add-in-leaf-u1` 0.055 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.63 us | 0.088 | `lib-stage2-lean-u1` 0.070 | `mut-odo-vecdims-add-in-leaf-u2` 0.068 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 119 us | 0.064 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.036 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 388 us | 0.043 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `gather48-src-50` | 3 | 22500 | 463 us | 0.048 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 |
| `stretch-coprime-r7` | 13 | 60060 | 1.11 ms | 0.030 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.7 ms | 0.052 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `stretch-primes` | 89 | 250357 | 4.37 ms | 0.025 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 17.1 ms | 0.039 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `vgg-14-c512-k3` | 3 | 903168 | 19.8 ms | 0.052 | `lib-stage2-lean` 0.031 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 19.9 ms | 0.030 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `stretch-inner256` | 256 | 1750784 | 44.6 ms | 0.023 | `lib-stage2-lean-u1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |
| `stretch-pow2stride` | 64 | 1769472 | 31.2 ms | 0.113 | `lib-stage2-lean-u1` 0.110 | `mut-odo-vecdims-add-in-leaf-u1` 0.111 |
| `stretch-r5-8x432` | 8 | 1769472 | 49.1 ms | 0.021 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.014 |
| `stretch-square-1341` | 1341 | 1798281 | 31.1 ms | 0.085 | `lib-stage1` 0.074 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.068 |
| `stretch-bigstride` | 3 | 1800000 | 51.3 ms | 0.033 | `lib-stage2-lean` 0.014 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.013 |
| `stretch-tab7MB` | 2 | 1800000 | 39.5 ms | 0.058 | `lib-stage1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 41 ms | 0.021 | `lib-stage1` 0.021 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `stretch-wide-2xM` | 2 | 1800000 | 39.9 ms | 0.057 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.020 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 941 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 29.1 ms | 0.019 | `lib-stage1` 0.016 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 29.3 ms | 0.019 | `lib-stage1` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 29.2 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 36.1 ms | 0.015 | `lib-stage1` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 39.2 ms | 0.057 | `lib-stage1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.020 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.66 ms | 0.052 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 4.29 ms | 0.018 | `lib-stage2-lean-u1` 0.012 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 48.5 ms | 0.034 | `lib-stage1` 0.010 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.011 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 42.4 ms | 0.022 | `lib-stage1` 0.017 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.23 ms | 0.019 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.016 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.28 ms | 0.025 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.26 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `block-run64-page` | `block` | 64 | 131072 | 2.34 ms | 0.029 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.47 ms | 0.020 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.016 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 946 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 941 us | 0.028 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 29.4 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 29.9 ms | 0.021 | `lib-stage2-lean-u1` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.33 ms | 0.026 | `lib-stage1` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.3 ms | 0.026 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.69 ms | 0.052 | `lib-stage2-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 29.4 ms | 0.024 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 30.3 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 32.3 ms | 0.048 | `lib-stage2-lean` 0.043 | `mut-odo-vecdims-add-in-leaf-u2` 0.039 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 119 us | 0.062 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.036 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 458 us | 0.048 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 |
| `rev-primes` | `rev` | 89 | 250357 | 4.33 ms | 0.025 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `runs-65536` | `runs` | 65536 | 1769472 | 28.5 ms | 0.024 | `lib-stage1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `runs-16384` | `runs` | 16384 | 1785856 | 28.4 ms | 0.025 | `lib-stage1` 0.023 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `runs-4096` | `runs` | 4096 | 1798144 | 28.6 ms | 0.024 | `lib-stage1` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |
| `runs-1024` | `runs` | 1024 | 1799168 | 28.7 ms | 0.024 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |
| `runs-512` | `runs` | 512 | 1799680 | 28.8 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |
| `runs-256` | `runs` | 256 | 1799936 | 29 ms | 0.025 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `runs-7` | `runs` | 7 | 1799994 | 32.9 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `runs-2` | `runs` | 2 | 1800000 | 39.8 ms | 0.058 | `lib-stage2-lean-u1` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `runs-3` | `runs` | 3 | 1800000 | 36 ms | 0.047 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `runs-4` | `runs` | 4 | 1800000 | 34.3 ms | 0.040 | `lib-stage2-lean-u1` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `runs-5` | `runs` | 5 | 1800000 | 33.4 ms | 0.038 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `runs-9` | `runs` | 9 | 1800000 | 32.1 ms | 0.030 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `runs-96` | `runs` | 96 | 1800000 | 29.4 ms | 0.024 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 30.1 ms | 0.025 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `scaled-r5` | `scaled` | 13 | 15015 | 270 us | 0.029 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 1.05 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.021 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 5.14 ms | 0.028 | `lib-stage2-lean-u1` 0.030 | `mut-odo-vecdims` 0.028 |
| `small-patch-k5` | `small` | 5 | 150 | 2.95 us | 0.076 | `lib-stage2-lean` 0.094 | `mut-odo-vecdims-add-in-leaf-u2` 0.057 |
| `small-bcast32` | `small` | 32 | 256 | 4.51 us | 0.050 | `lib-stage2-lean` 0.059 | `mut-odo-vecdims-add-in-leaf-u2` 0.043 |
| `small-flat64` | `small` | 64 | 256 | 4.43 us | 0.060 | `lib-stage2-lean` 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.055 |
| `small-patch-r5` | `small` | 4 | 256 | 5.36 us | 0.085 | `lib-stage2-lean` 0.091 | `mut-odo-vecdims-add-in-leaf-u1` 0.066 |
| `small-row96` | `small` | 96 | 384 | 6.47 us | 0.042 | `lib-stage2-lean` 0.053 | `mut-odo-vecdims-add-in-leaf-u2` 0.041 |
| `window-28x28-k5` | `window` | 5 | 14400 | 280 us | 0.040 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 959 us | 0.084 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.49 ms | 0.051 | `lib-stage2-lean` 0.030 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 9.67 ms | 0.051 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.93 ms | 0.050 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 11.7 ms | 0.052 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 12.5 ms | 0.053 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `window-128x128-k7` | `window` | 7 | 729316 | 13.9 ms | 0.031 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.015 |

**One row to read first, and it is a property of the shape set and not of any arm**: NONE of the thirty-six rows is a geomean over fewer shapes than the rest, where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, against **seventy on Run 27's basis and forty-three on its control** as that run's write-up recorded them -- counts predating the 2026-09-10 `no_net` ruling, which today's reader gives as 25 and 8 -- so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the fourteen `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows --- `libunord-stage5-sum` on four, `-stage2-sum` on two, `-stage4-sum` on one --- this one names `lib-stage2-lean`, `lib-stage1` and `lib-stage2-lean-u1` and nothing else, and the cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The properties the next run should test

**Each stride class carries the same three properties, now with Run 30's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** Both held in every one of the eleven populations, on both halves --- the first in every regime, roster and layout the README has run, so `mut-odo-vecdims` --- and this is a statement about THAT arm and not about the route the library ships, which the paragraph below reads separately --- was never slower than the `list` it replaced, on any shape of any class; the second folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and read that way here for the third time. This is the property the classes exist to test, no geomean can state it, and a break would be the one result here to bear on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims` its `worst` is 0.113 on the main set and between 0.028 and 0.085 in the classes, highest on `small` at 0.085 and then `window` at 0.084, which no longer tie as they did on Run 29; the `bq-expand` clause is closest at `stretch-pow2stride` on the main set, at 0.9997, and at `small-patch-k5`, 0.4861, in a class --- `compose-slice-bcast`'s 0.2771 being the widest of the ten per-class closest readings rather than the narrowest.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm now breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. Of the five library-shaped arms Run 27 found breaking it at `runs-2` --- `libunord-stage1` 1.237, `liblist-stage1` 1.214, `lib-stage1` 1.198, `liblist-stage2` 1.078 and `libunord-stage2` 1.076 --- four are retired to `check` and are not timed here. Those five figures are RECOMPUTED from Run 27's own cells, that run's prose and its own installed table having printed different ones, and the five are the largest of NINE library-shaped arms over `list` there; a tenth row above it is `list-aa-adjacent`, `list`'s own A/A copy, at 1.0111. **What is left is `lib-stage1`, on two cells of the control half and one of the basis**: at `runs-2` it reads 1.0949 on the basis and 1.2915 on the control, and at `runs-3` 0.9002 on the basis --- under 1 there --- against 1.0572 on the control. A sweep of every cell of all eleven populations on both halves finds no other timed arm above its shape's `list` anywhere, the baseline's own A/A copies aside. **And the flag is what widens it**: `-fliberate-case` makes `list` faster and leaves `lib-stage1` where it was, so the arm that has to beat `list` on the shortest runs meets a harder baseline on the flagged half and breaks it on a second cell there. So the shipped route is slower than `list` on runs of two elements on both halves and of three on one, and on nothing else the library can produce, which is a sharper statement of the same defect than any run before this one could make.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves.** The `list` clause is closest at `small-patch-k5`, 0.05918, and under 0.048 everywhere else. The `bq-expand` clause is closest at `scaled-rank1-m1`, 1.00003, then `stretch-tall-Mx2` at 1.00000 and `bcast-src8` at 0.99999 --- three cells inside the margin the strict form would have failed on, which is why the margin is there and why it is not widened further.

3. **The allocation tiers survive and their ORDER is unbroken in all ten, on the main set and on every class but `small` --- outside the level clause since 2026-09-07, being the class built to break it, and read here for what it shows**: the mutable fills at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 20.99x to 27.66x. On the main set the fills read 1.00x, `bq-expand` **2.76x** and `list` **24.90x** over eighteen of the nineteen shapes --- which are Run 29's UNFLAGGED half's levels and not the 2.06x and 22.38x Runs 23 to 29 published, those being a `-fspec-constr` half's and this basis being the unflagged recipe. **What this roster removes is the 2.00x tier**, whose members were the list Fill arms now checked and not timed, and what it adds below the fills ON THE MAIN SET is the `libunord` consumers at 0.00x and the `liblist` consumers at 1.00x, the latter falling to a fill there. **Those two added levels are the main set's and no class's**, which the class blocks show and this paragraph used not to say: on `bcast` the `libunord` consumers spread from 0.00x to 1.00x and on `small` from 1.14x to 2.07x, the per-call constants of a short call swamping a consumer that allocates nothing per element. So `property 3 holds` in a class is the THREE published tiers holding --- the fills, `bq-expand`, `list` --- and never the five. `small` reads its fills at 1.27x and `bq-expand` at **1.44x**, the per-call constants showing through a call under a microsecond. It is outside the LEVEL clause and inside the ORDER one --- that is what `every class but `small`` above means --- and its order is unbroken like the rest.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 30 (LiberateCase against plain -O1, dead-spot, -A32m, ghc-9.12.4) records every class twice**, one process per class per half, so each block below has a control-half twin and the cross-half line under it is derived from both. **This run those lines say less than usual, and they say why.** `list` moved between the halves by 17.26 points on `bcastmid` at narrowest and 21.71 on `compose` at widest, past the 0.7% that lets two columns be differenced, so every class's cross-half reading is disqualified as a reading of the pair's variable and stands only as an ordering --- as every one of Run 29's was, for the same reason under the other flag. What each class still decides, and decides on both halves separately, is the three properties, its own floor, and whichever registrations name it. **Three items read EVERY class** --- (1), (2) and (9) --- so each block below has those three whatever else it carries.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- and both are read over the POPULATION, so the arm named here may lead on no single shape and the per-shape fingerprint below may name another, which is [the README's per-shape section][pershape]'s own point and not a disagreement --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the properties](#the-properties-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on FOUR of the ten --- `rev`, where `lib-stage1` leads, `bcast` and `small`, where `lib-stage2-lean` does, and `bcastmid`, where `lib-stage2-lean-u1` does --- and the family's own ceiling on the other six, where the ceiling is faster. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 3 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. Item 2's own table is NOT among what that call prints, which is why the list above starts where it does: it comes from the separate `--block --in-place` call item 2 itself names. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.042 | 0.062 | **`lib-stage1`** 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 | 0.49% |
| `bcast` | 6 | 0.021 | 0.057 | **`lib-stage2-lean`** 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 | 0.93% |
| `bcastmid` | 4 | 0.029 | 0.052 | **`lib-stage2-lean-u1`** 0.012 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 | 0.42% |
| `window` | 8 | 0.050 | 0.084 | `lib-stage2-lean` 0.028 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.023 | 0.35% |
| `scaled` | 3 | 0.027 | 0.029 | `lib-stage1` 0.022 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.021 | 0.88% |
| `runs` | 14 | 0.027 | 0.058 | `lib-stage2-lean` 0.024 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.023 | 3.23% |
| `flip` | 6 | 0.029 | 0.052 | `lib-stage2-lean` 0.023 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.023 | 2.05% |
| `block` | 5 | 0.023 | 0.029 | `lib-stage2-lean` 0.021 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.019 | 0.97% |
| `small` | 5 | 0.061 | 0.085 | **`lib-stage2-lean`** 0.050 | `mut-odo-vecdims-add-in-leaf-u2` 0.051 | 0.21% |
| `compose` | 4 | 0.024 | 0.028 | `lib-stage2-lean` 0.015 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.015 | 0.28% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On SIX rows --- `window`, `scaled`, `runs`, `flip`, `block` and `compose` --- the bold sits in the CEILING column instead**, and it names three arms rather than one: `mut-odo-vecdims-add-in-leaf-u2-ptr` on `window`, `runs` and `block`, `-u1-ptr` on `scaled` and `flip`, and the shipped leaf `-u2` itself on `compose`. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship. **THREE rows tie at three decimals** --- `bcast` at 0.016, `flip` at 0.023 and `compose` at 0.015 --- and the bold on each is `--block`'s own, computed on the unrounded values where the printed ones cannot separate: `bcast` to the arm outside the family, `flip` and `compose` to the ceiling.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.12* | *127* | *3.22x* |
| liblist-stage1-sum | -- | -- | 0.08 | 147 | 1.01x |
| liblist-stage2-sum | -- | -- | 0.09 | 147 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.08 | 147 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.12 | 147 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.14 | 146 | 1.03x |
| libunord-stage10-sum | -- | -- | 0.05 | 157 | 0.02x |
| libunord-stage2-sum | -- | -- | 0.07 | 157 | 0.01x |
| libunord-stage3-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage5-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage6-list-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.07 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.06 | 157 | 0.02x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.19* | *148* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *158* | *0.00x* |
| lib-stage1 | 0.021 | 0.046 | 0.09 | 147 | 1.01x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.042* | *0.07* | *147* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.042 | 0.09 | 147 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.023* | *0.042* | *0.08* | *147* | *1.00x* |
| lib-stage2-lean | 0.023 | 0.032 | 0.11 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.023 | 0.036 | 0.17 | 148 | 1.01x |
| lib-stage2-lean-u1 | 0.026 | 0.034 | 0.09 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.027 | 0.039 | 0.14 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.042 | 0.14 | 146 | 1.00x |
| **mut-odo-vecdims** | **0.042** | 0.062 | 0.05 | 138 | 1.00x |
| *mut-odo-vecdims-aa* | *0.042* | *0.063* | *0.09* | *138* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.042* | *0.063* | *0.06* | *138* | *1.00x* |
| bq-expand | 0.138 | 0.232 | 0.14 | 122 | 3.22x |
| *bq-expand-aa-adjacent* | *0.139* | *0.232* | *0.12* | *122* | *3.22x* |
| *bq-expand-aa-distant* | *0.139* | *0.232* | *0.11* | *122* | *3.22x* |
| list (baseline) | 1.000 | 1.000 | 0.30 | 85 | 26.11x |
| *list-aa-distant* | *1.002* | *1.002* | *0.18* | *85* | *26.11x* |
| *list-aa-adjacent* | *1.002* | *1.003* | *0.22* | *85* | *26.11x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 1.0049, worst cell 1.38% on `rev-cnn-L1-24x24-c1`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 0.9999 on a worst cell of 0.04% on `rev-primes`, its interval covering 1. The in-situ term reads 1.0002, 1.0125 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0034, which the correction amplifies by 1.71x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m24s, peak 95 MiB in use, 24 MiB max residency; the reader reads 36 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.48 ms per call raw, 4.33 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.062/0.048/0.025

**Across the halves:** 5 of the 18 arms are faster on this half and 13 slower, at a geomean of 1.0263, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9695 to `list-aa-adjacent` at 1.1838, with `list` itself at 1.1818. **The baseline moved 18.18% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.062, tiers at 1.00x, 3.22x and 26.11x --- and `lib-stage1` leads outside the family at 0.021, priced against `mut-odo-vecdims` at 0.6475 over 3 of 3 shapes at sign p 0.25, a margin of 35.25% against this class's floor of 0.49%. **What is this class's own is registration (7) surviving where Run 29 killed it.** `rev` negates every stride and carries no zero stride and no pair of axes tied on stride, so `zerosOutermost` hands its list back unchanged and `routeUnord10` is `routeUnord7` there --- yet `libunord-stage10-sum` over `libunord-stage7-sum` reads **1.0244** against this class's 0.49% floor on the basis and **1.0245** against 0.61% on the control, past the floor on both halves as it was on Run 29. What differs is the bar and not the reading: the item was amended to `within 3%` on 2026-09-13, and 2.44 points clears that. So `rev` is where an argument that a reordering cannot reach a population comes apart for the third run running, and this time the registration survives it by having been re-based rather than by the pass having got cheaper. Across the halves the class is disqualified as a reading of the flag, `list` having moved 18.18 points --- and still twenty-four times the bar at its narrowest anywhere.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09, for Run 28 --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and Run 28's registration (7) read no crossover on it; this run reads the class for stage ten instead, at item (6).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.61* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.52 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 73 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.52 | 62 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 73 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.28* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *69* | *0.00x* |
| lib-stage2-lean | 0.016 | 0.020 | 0.52 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.016* | *0.020* | *0.49* | *62* | *1.00x* |
| lib-stage1 | 0.016 | 0.020 | 0.49 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.016* | *0.020* | *0.42* | *62* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.016 | 0.020 | 0.42 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.016 | 0.024 | 0.44 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.024 | 0.50 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.016 | 0.020 | 0.50 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.018 | 0.020 | 0.45 | 61 | 1.00x |
| *mut-odo-vecdims-aa* | *0.021* | *0.057* | *0.33* | *61* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.021* | *0.057* | *0.31* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.021** | 0.057 | 0.08 | 61 | 1.00x |
| bq-expand | 0.091 | 0.142 | 0.66 | 46 | 1.00x |
| *bq-expand-aa-adjacent* | *0.091* | *0.142* | *0.72* | *46* | *1.00x* |
| *bq-expand-aa-distant* | *0.092* | *0.144* | *0.10* | *46* | *1.00x* |
| list (baseline) | 1.000 | 1.000 | 1.11 | 17 | 20.99x |
| *list-aa-distant* | *1.004* | *1.010* | *0.86* | *17* | *20.99x* |
| *list-aa-adjacent* | *1.011* | *1.013* | *0.73* | *17* | *20.99x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0093, worst cell 1.33% on `bcast-src8`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.03% on `bcast-src512`, its interval missing 1. The in-situ term reads 1.0210, 1.0111 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0090, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h18m47s, peak 180 MiB in use, 42 MiB max residency; the reader reads 36 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 30.4 ms per call raw, 29.3 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.028/0.019/0.057/0.015/0.019/0.019

**Across the halves:** 6 of the 18 arms are faster on this half and 12 slower, at a geomean of 1.0319, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9934 to `list` at 1.1960, with `list` itself at 1.1960. **The baseline moved 19.60% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.057, tiers at 1.00x, 1.00x and 20.99x --- and `lib-stage2-lean` leads outside the family at 0.016, priced against `mut-odo-vecdims` at 0.6663 over 6 of 6 shapes at sign p 0.031, a margin of 33.37% against this class's floor of 0.93%. **This is where stage ten pays off best.** `libunord-stage10-sum` over `libunord-stage7-sum` reads **0.5903** on the basis and 0.5588 on the control, both inside the registered 0.55 within 20%, and no view has it behind stage seven on either half, the closest being `bcast-inner8` at 0.7100 and 0.7119. **And it is where the two fills stay met.** With `lib-stage1` filling through `fillStage2` since 2026-09-11, `lib-stage2-lean` over it reads 0.9964 on the basis and 0.9958 on the control, inside the 3% registration (8) asked for --- so what Run 28 measured as the lean fill's 0.8635 lead was the two bodies the leaf lacked, and a second flag has not reopened it. Across the halves the class is disqualified, `list` having moved 19.60 points, the third widest of the ten behind `compose`'s and `window`'s.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.47* | *66* | *1.92x* |
| liblist-stage1-sum | -- | -- | 0.40 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.36 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.60 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.58 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.40 | 82 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 93 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.33 | 82 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.34 | 82 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.31 | 82 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.30 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.35 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.31 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 93 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.39* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *88* | *0.00x* |
| lib-stage2-lean-u1 | 0.012 | 0.018 | 0.34 | 82 | 1.00x |
| lib-stage1 | 0.012 | 0.017 | 0.33 | 82 | 1.00x |
| lib-stage2-lean | 0.012 | 0.019 | 0.34 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.017 | 0.024 | 0.32 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.020* | *0.031* | *0.27* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.030 | 0.30 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.020* | *0.031* | *0.39* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.022 | 0.028 | 0.37 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.030 | 0.41 | 78 | 1.00x |
| **mut-odo-vecdims** | **0.029** | 0.052 | 0.29 | 76 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.052* | *0.30* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.029* | *0.052* | *0.32* | *76* | *1.00x* |
| *bq-expand-aa-distant* | *0.099* | *0.179* | *0.42* | *61* | *1.92x* |
| *bq-expand-aa-adjacent* | *0.099* | *0.180* | *0.48* | *61* | *1.92x* |
| bq-expand | 0.099 | 0.179 | 0.42 | 61 | 1.92x |
| *list-aa-distant* | *0.999* | *1.007* | *0.86* | *28* | *23.56x* |
| list (baseline) | 1.000 | 1.000 | 0.82 | 28 | 23.56x |
| *list-aa-adjacent* | *1.004* | *1.009* | *0.86* | *28* | *23.56x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0042, worst cell 0.85% on `bcastmid-b200k`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 1.0012 on a worst cell of 0.50% on `bcastmid-c32-cnn`, its interval covering 1. The in-situ term reads 1.0113, 1.1354 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0041, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m31s, peak 129 MiB in use, 35 MiB max residency; the reader reads 36 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 49.5 ms per call raw, 48.5 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.052/0.018/0.034/0.022

**Across the halves:** 8 of the 18 arms are faster on this half and 10 slower, at a geomean of 1.0262, from `lib-stage1` at 0.9786 to `list-aa-adjacent` at 1.1753, with `list` itself at 1.1726. **The baseline moved 17.26% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.052, tiers at 1.00x, 1.92x and 23.56x --- and `lib-stage2-lean-u1` leads outside the family at 0.012, priced against `mut-odo-vecdims` at 0.4213 over 4 of 4 shapes at sign p 0.12, a margin of 57.87%, the widest of the ten, against this class's floor of 0.42%. **Stage ten pays off here too**, `libunord-stage10-sum` over `libunord-stage7-sum` reading **0.6214** on the basis and 0.5827 on the control against the registered 0.6, with the closest to level `bcastmid-c32-cnn` at 0.7018 on the basis and `bcastmid-primes` at 0.7033 on the control and no view behind on either half. **And the doubling block copy stays the leaf's as well**: `lib-stage2-lean` over `lib-stage1` reads 1.0137 on the basis and 0.9890 on the control, inside registration (8)'s 3%, where Run 28 read 0.6163 for a lean fill the leaf could not match. Across the halves the class is disqualified, `list` having moved 17.26 points --- the narrowest of the ten and still twenty-four times the bar.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09, for Run 28, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.35* | *62* | *3.86x* |
| liblist-stage1-sum | -- | -- | 0.19 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.19 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.15 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.16 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.17 | 82 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.05 | 100 | 0.05x |
| libunord-stage2-sum | -- | -- | 0.20 | 82 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.18 | 88 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.06 | 96 | 0.04x |
| libunord-stage6-list-sum | -- | -- | 0.05 | 99 | 0.04x |
| libunord-stage6-loop-sum | -- | -- | 1.20 | 90 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.07 | 95 | 0.04x |
| libunord-stage7-sum | -- | -- | 0.06 | 100 | 0.04x |
| libunord-stage9-sum | -- | -- | 0.08 | 95 | 0.05x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.22* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *97* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.023 | 0.025 | 0.23 | 84 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.029 | 0.24 | 84 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.028* | *0.031* | *0.18* | *82* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.028* | *0.031* | *0.20* | *82* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.028 | 0.031 | 0.18 | 82 | 1.00x |
| lib-stage2-lean | 0.028 | 0.032 | 0.16 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.031 | 0.18 | 82 | 1.00x |
| lib-stage1 | 0.029 | 0.032 | 0.15 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.029 | 0.032 | 0.18 | 82 | 1.00x |
| **mut-odo-vecdims** | **0.050** | 0.084 | 0.16 | 76 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.050* | *0.085* | *0.16* | *76* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.050* | *0.084* | *0.17* | *76* | *1.00x* |
| bq-expand | 0.176 | 0.214 | 0.36 | 57 | 3.86x |
| *bq-expand-aa-adjacent* | *0.177* | *0.214* | *0.40* | *57* | *3.86x* |
| *bq-expand-aa-distant* | *0.177* | *0.214* | *0.28* | *57* | *3.86x* |
| *list-aa-distant* | *1.000* | *1.004* | *0.50* | *30* | *27.66x* |
| list (baseline) | 1.000 | 1.000 | 0.53 | 30 | 27.66x |
| *list-aa-adjacent* | *1.000* | *1.004* | *0.41* | *30* | *27.66x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0035, worst cell 1.83% on `window-224x224-k3-d2`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9997 on a worst cell of 0.16% on `window-224x224-k3-s2`, its interval covering 1. The in-situ term reads 1.0016, 1.2459 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0029, which the correction amplifies by 1.17x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h24m53s, peak 128 MiB in use, 51 MiB max residency; the reader reads 36 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 14.4 ms per call raw, 13.9 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.040/0.050/0.084/0.031/0.051/0.051/0.053/0.052

**Across the halves:** 10 of the 18 arms are faster on this half and 8 slower, at a geomean of 1.0302, from `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.9907 to `list` at 1.2027, with `list` itself at 1.2027. **The baseline moved 20.27% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.084, the second highest of the ten behind `small`'s, and tiers at 1.00x, 3.86x and 27.66x, the widest `bq-expand` and `list` levels of any class --- and `lib-stage2-lean` leads outside the family at 0.028, priced against `mut-odo-vecdims` at 0.4827 over 8 of 8 shapes at sign p 0.0078, a margin of 51.73% against this class's floor of 0.35%, the third tightest of the ten behind `small`'s and `compose`'s. **This is where stage ten's tie-break half is read alone**, no window view carrying a zero stride: over `libunord-stage9-sum` it reads **0.7326** on the basis against the registered 0.72, which is stage seven's tie-break arriving through stage ten intact --- and **0.8863** on the control, outside the registered 15% and the one span of registration (6)'s four to miss. **The same TWO views part in SIGN between the halves that parted on Run 29** --- `window-128x128-k7` at 0.9703 on the basis against 1.2538 on the control, and `window-64x64-k1x9` at 0.9621 against 1.1481 --- and they part far wider than they did, twenty-eight and nineteen points where Run 29 read six and five. That is what saves registration (6) from its second kill, which wants a view behind on both halves, and it is the one place in this run where a class-level reading is unstable enough to want a second evening before anything is built on it. Across the halves the class is disqualified, `list` having moved 20.27 points.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.08* | *118* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.13 | 128 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.17 | 128 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.16 | 127 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.18 | 128 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.16 | 127 | 1.01x |
| libunord-stage10-sum | -- | -- | 0.16 | 127 | 1.01x |
| libunord-stage2-sum | -- | -- | 0.16 | 127 | 1.01x |
| libunord-stage3-sum | -- | -- | 0.11 | 127 | 1.01x |
| libunord-stage5-sum | -- | -- | 0.17 | 127 | 1.01x |
| libunord-stage6-list-sum | -- | -- | 0.32 | 127 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.17 | 127 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.22 | 127 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.14 | 127 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.15 | 127 | 1.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.20* | *146* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *138* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.021 | 0.030 | 0.17 | 128 | 1.00x |
| lib-stage1 | 0.022 | 0.031 | 0.19 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.022 | 0.031 | 0.19 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.030 | 0.18 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.023* | *0.031* | *0.16* | *128* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.023 | 0.031 | 0.22 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.023* | *0.030* | *0.23* | *127* | *1.00x* |
| lib-stage2-lean | 0.023 | 0.031 | 0.15 | 127 | 1.00x |
| lib-stage2-lean-u1 | 0.024 | 0.030 | 0.20 | 127 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.029* | *0.08* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.029 | 0.11 | 127 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.029* | *0.07* | *127* | *1.00x* |
| bq-expand | 0.091 | 0.101 | 0.08 | 111 | 1.21x |
| *bq-expand-aa-adjacent* | *0.091* | *0.101* | *0.11* | *111* | *1.21x* |
| *bq-expand-aa-distant* | *0.091* | *0.101* | *0.08* | *111* | *1.21x* |
| list (baseline) | 1.000 | 1.000 | 0.29 | 69 | 21.49x |
| *list-aa-adjacent* | *1.002* | *1.006* | *0.21* | *69* | *21.49x* |
| *list-aa-distant* | *1.003* | *1.004* | *0.27* | *69* | *21.49x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9912, worst cell 1.35% on `scaled-r5`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `scaled-super-r3`, its interval covering 1. The in-situ term reads 1.0201, 1.0153 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9966, which the correction amplifies by 2.42x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m23s, peak 111 MiB in use, 29 MiB max residency; the reader reads 36 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 5.32 ms per call raw, 5.14 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.023/0.028/0.029

**Across the halves:** 5 of the 18 arms are faster on this half and 13 slower, at a geomean of 1.0257, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9441 to `list-aa-distant` at 1.1934, with `list` itself at 1.1865. **The baseline moved 18.65% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.029, the joint second lowest of the ten with `block`'s, behind `compose`'s 0.028, and tiers at 1.00x, 1.21x and 21.49x --- and `lib-stage1` leads outside the family at 0.022, priced against `mut-odo-vecdims` at 0.9302 over 2 of 3 shapes at sign p 1, a margin of 6.98% against this class's floor of 0.88%: the narrowest lead of the ten, and one of two whose sign test does not separate it, `runs` printing p 1 as well. **The class has no news of its own this run.** Every span registrations (5), (6) and (7) read here ties inside the floor on both halves --- 0.9991 and 1.0001 for the list against the fill it replaces, 1.0025 and 1.0054 for stage ten against stage seven --- which is what a class of three dense scaled views is for. The A/A slot the open list has watched on `scaled-super-r3` for eight runs is not the widest cell here this run either. Across the halves the class is disqualified, `list` having moved 18.65 points.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.42* | *52* | *1.13x* |
| liblist-stage1-sum | -- | -- | 0.16 | 65 | 0.19x |
| liblist-stage2-sum | -- | -- | 0.09 | 69 | 0.16x |
| liblist-stage3-sum | -- | -- | 0.02 | 70 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.03 | 70 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.17 | 64 | 0.19x |
| libunord-stage10-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.09 | 69 | 0.16x |
| libunord-stage3-sum | -- | -- | 0.14 | 59 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 69 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 70 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.14* | *78* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.023 | 0.026 | 0.14 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.023 | 0.027 | 0.16 | 60 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.026* | *0.47* | *59* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.026 | 0.12 | 59 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.026* | *0.11* | *59* | *1.00x* |
| lib-stage2-lean | 0.024 | 0.026 | 0.13 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.027 | 0.13 | 59 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.027 | 0.12 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.058* | *0.09* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.058 | 0.09 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.058* | *0.08* | *59* | *1.00x* |
| lib-stage1 | 0.093 | 1.095 | 0.25 | 55 | 1.19x |
| *bq-expand-aa-adjacent* | *0.097* | *0.143* | *0.47* | *46* | *1.13x* |
| bq-expand | 0.097 | 0.143 | 0.46 | 46 | 1.13x |
| *bq-expand-aa-distant* | *0.097* | *0.144* | *0.06* | *46* | *1.13x* |
| list (baseline) | 1.000 | 1.000 | 2.46 | 16 | 21.32x |
| *list-aa-distant* | *1.029* | *1.042* | *0.55* | *17* | *21.32x* |
| *list-aa-adjacent* | *1.032* | *1.044* | *0.25* | *17* | *21.32x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0323, worst cell 4.44% on `runs-16384`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.52% on `runs-16384`, its interval covering 1. The in-situ term reads 1.0302, 1.0366 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0312, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h43m32s, peak 564 MiB in use, 247 MiB max residency; the reader reads 36 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 40.9 ms per call raw, 39.8 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.058/0.047/0.040/0.038/0.032/0.030/0.024/0.025/0.025/0.024/0.024/0.025/0.024/0.025

**Across the halves:** 8 of the 18 arms are faster on this half and 10 slower, at a geomean of 1.0310, from `lib-stage2-lean-u1` at 0.9931 to `list-aa-adjacent` at 1.1948, with `list` itself at 1.1946. **The baseline moved 19.46% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.058, tiers at 1.00x, 1.13x and 21.32x --- and `lib-stage2-lean` leads outside the family at 0.024, priced against `mut-odo-vecdims` at 0.8052 over 7 of 14 shapes at sign p 1, a margin of 19.48% against this class's floor of **3.23%**, the widest floor of the ten. **This class carries three of the run's registrations at once and all three held.** The lazy odometer's consumer over the port's base-offset table, `liblist-stage4-sum` over `liblist-stage2-sum`, reads **0.4678** on the basis and **0.3947** on the control against a registered 0.5; the fused list over the fill it replaces, `libunord-stage5-sum` over `libunord-stage3-sum`, reads **0.5808** and **0.4900** against 0.57; and the lean fill over stage one reads **0.2329** and **0.2362** against 0.23, where the two routes genuinely part --- stage one concatenating a slice per run where the lean dispatch fills. So the odometer's lead over master's recursion and over the branch's table is the design's on both halves and not the flag's, as Run 29 found it was not `-fspec-constr`'s. **It is also where the flag reaches the fused list hardest**, `libunord-stage7-sum` moving 18.27 points across the halves here against 6.33 on `block`. **And it is where the WIDER property breaks**: `lib-stage1` reads above its shape's `list` at `runs-2` on both halves, 1.0949 and 1.2915, and at `runs-3` on the control, 1.0572 --- the only cells of any population where a timed arm is slower than the baseline, and the shipped route is the arm. Across the halves the class is disqualified, `list` having moved 19.46 points.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes, in the order they run: `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which landed 2026-09-09 and is `runs-96`'s construction under a `flip` name --- the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two; `flip-whole-square` (`l` 1798281, `sInner` 1341); `flip-last-c32` (`l` 165888, `sInner` 3); `flip-last-rows` (`l` 1800000, `sInner` 96); and the two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed, `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. The control sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.41* | *66* | *1.05x* |
| liblist-stage1-sum | -- | -- | 0.17 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.10 | 88 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.09 | 92 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.10 | 92 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.29 | 82 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.04 | 90 | 0.12x |
| libunord-stage3-sum | -- | -- | 0.05 | 90 | 0.50x |
| libunord-stage5-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 97 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.17* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.04* | *93* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.023 | 0.046 | 0.28 | 82 | 1.00x |
| lib-stage2-lean | 0.023 | 0.043 | 0.27 | 84 | 1.00x |
| lib-stage2-lean-u1 | 0.024 | 0.047 | 0.29 | 83 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.024 | 0.040 | 0.40 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.038* | *0.33* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.039* | *0.10* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.039 | 0.13 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.049 | 0.22 | 80 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.029* | *0.052* | *0.11* | *77* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.029* | *0.052* | *0.13* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.052 | 0.12 | 77 | 1.00x |
| lib-stage1 | 0.035 | 0.048 | 0.31 | 80 | 1.00x |
| *bq-expand-aa-adjacent* | *0.089* | *0.180* | *0.43* | *61* | *1.05x* |
| bq-expand | 0.090 | 0.180 | 0.44 | 61 | 1.05x |
| *bq-expand-aa-distant* | *0.091* | *0.180* | *0.11* | *61* | *1.05x* |
| list (baseline) | 1.000 | 1.000 | 0.79 | 32 | 21.18x |
| *list-aa-distant* | *1.006* | *1.017* | *0.50* | *32* | *21.18x* |
| *list-aa-adjacent* | *1.008* | *1.019* | *0.44* | *32* | *21.18x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9795, worst cell 12.92% on `flip-last-rows`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9993 on a worst cell of 0.48% on `flip-fwd-rows96`, its interval covering 1. The in-situ term reads 1.0234, 1.0261 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9878, which the correction amplifies by 2.07x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h18m42s, peak 209 MiB in use, 76 MiB max residency; the reader reads 36 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 31.3 ms per call raw, 30.3 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.024/0.024/0.052/0.048/0.026/0.026

**Across the halves:** 9 of the 18 arms are faster on this half and 9 slower, at a geomean of 1.0198, from `mut-odo-vecdims-add-in-leaf-u2-aa` at 0.9688 to `list` at 1.1823, with `list` itself at 1.1823. **The baseline moved 18.23% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.052, tied with `bcastmid`'s, and tiers at 1.00x, 1.05x and 21.18x --- and `lib-stage2-lean` leads outside the family at 0.023, priced against `mut-odo-vecdims` at 0.7637 over 5 of 6 shapes at sign p 0.22, a margin of 23.63% against this class's floor of **2.05%**, the second widest of the ten. **This class carries the run's widest single A/A cell**, 12.92% on `flip-last-rows` on the basis half --- and it is the correction's arithmetic and not a wild cell: on that bench the forcing pass is 51.5% of the total, so subtracting it multiplies any disagreement by 1/(1-f) = 2.06, and the raw pair is 1.2%. `--wild` clears the process outright, no bench reaching 0.25 of a core. What it means for the reading is unchanged: no margin on `flip-last-rows` under about three percent is readable this run. Across the halves the class is disqualified, `list` having moved 18.23 points.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.12* | *103* | *1.06x* |
| liblist-stage1-sum | -- | -- | 0.20 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.16 | 122 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.02 | 128 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 128 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.17 | 113 | 0.42x |
| libunord-stage10-sum | -- | -- | 0.02 | 128 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.17 | 122 | 0.35x |
| libunord-stage3-sum | -- | -- | 0.12 | 112 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 128 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.22* | *129* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *122* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.019 | 0.022 | 0.13 | 112 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.020 | 0.025 | 0.12 | 111 | 1.00x |
| lib-stage2-lean | 0.021 | 0.025 | 0.12 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.021* | *0.024* | *0.13* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.024 | 0.13 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.025* | *0.14* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.026 | 0.12 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.022 | 0.026 | 0.12 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.023* | *0.029* | *0.12* | *111* | *1.00x* |
| **mut-odo-vecdims** | **0.023** | 0.029 | 0.09 | 111 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.029* | *0.11* | *111* | *1.00x* |
| lib-stage1 | 0.050 | 0.058 | 0.13 | 103 | 1.42x |
| *bq-expand-aa-distant* | *0.086* | *0.087* | *0.09* | *96* | *1.06x* |
| *bq-expand-aa-adjacent* | *0.086* | *0.087* | *0.14* | *96* | *1.06x* |
| bq-expand | 0.087 | 0.088 | 0.14 | 96 | 1.06x |
| list (baseline) | 1.000 | 1.000 | 0.27 | 54 | 21.22x |
| *list-aa-distant* | *1.000* | *1.005* | *0.27* | *54* | *21.22x* |
| *list-aa-adjacent* | *1.001* | *1.003* | *0.22* | *54* | *21.22x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 1.0097, worst cell 3.38% on `block-run64-page`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 1.0004 on a worst cell of 0.53% on `block-run64-page`, its interval covering 1. The in-situ term reads 1.0217, 1.0224 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0039, which the correction amplifies by 2.62x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m36s, peak 135 MiB in use, 37 MiB max residency; the reader reads 36 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.63 ms per call raw, 4.47 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.019/0.025/0.029/0.024/0.020

**Across the halves:** 9 of the 18 arms are faster on this half and 9 slower, at a geomean of 1.0275, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9457 to `list-aa-adjacent` at 1.1887, with `list` itself at 1.1871. **The baseline moved 18.71% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.029, joint second lowest of the ten with `scaled`'s, behind `compose`'s 0.028, and tiers at 1.00x, 1.06x and 21.22x --- and `lib-stage2-lean` leads outside the family at 0.021, priced against `mut-odo-vecdims` at 0.9224 over 5 of 5 shapes at sign p 0.062, a margin of 7.76% against this class's floor of 0.97%. **It is `runs`'s second, and it says the same three things at half the size.** The odometer's consumer over the port's table reads **0.7281** on the basis and **0.6800** on the control against a registered 0.78; the fused list over the fill it replaces reads **0.4520** and **0.4237** against 0.45; and the lean fill over stage one reads **0.4160** and **0.4306** against 0.42 --- three spans registered from Run 28's own readings and three landing inside two points of them, on both halves and now under a second flag. Across the halves the class is disqualified, `list` having moved 18.71 points.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.18* | *222* | *1.44x* |
| liblist-stage1-sum | -- | -- | 0.17 | 228 | 1.64x |
| liblist-stage2-sum | -- | -- | 0.19 | 231 | 1.49x |
| liblist-stage3-sum | -- | -- | 0.20 | 228 | 1.63x |
| liblist-stage4-sum | -- | -- | 0.15 | 231 | 1.45x |
| libunord-stage1-sum | -- | -- | 0.21 | 223 | 2.07x |
| libunord-stage10-sum | -- | -- | 0.29 | 227 | 1.14x |
| libunord-stage2-sum | -- | -- | 0.21 | 225 | 2.04x |
| libunord-stage3-sum | -- | -- | 0.31 | 221 | 1.84x |
| libunord-stage5-sum | -- | -- | 0.20 | 228 | 1.84x |
| libunord-stage6-list-sum | -- | -- | 0.23 | 232 | 1.24x |
| libunord-stage6-loop-sum | -- | -- | 0.31 | 232 | 1.24x |
| libunord-stage6-sum | -- | -- | 0.25 | 231 | 1.26x |
| libunord-stage7-sum | -- | -- | 0.31 | 231 | 1.26x |
| libunord-stage9-sum | -- | -- | 0.32 | 227 | 1.14x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.24* | *238* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.06* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.02* | *250* | *0.01x* |
| lib-stage2-lean | 0.050 | 0.094 | 0.19 | 229 | 1.40x |
| lib-stage2-lean-u1 | 0.051 | 0.096 | 0.24 | 228 | 1.40x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.051 | 0.066 | 0.28 | 230 | 1.27x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.052* | *0.066* | *0.38* | *230* | *1.27x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.052* | *0.066* | *0.39* | *230* | *1.27x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.054 | 0.066 | 0.28 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.059 | 0.078 | 0.37 | 228 | 1.44x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.060 | 0.078 | 0.23 | 228 | 1.44x |
| *mut-odo-vecdims-aa* | *0.061* | *0.085* | *0.30* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.061** | 0.085 | 0.31 | 229 | 1.27x |
| *mut-odo-vecdims-aa-distant* | *0.061* | *0.086* | *0.30* | *229* | *1.27x* |
| lib-stage1 | 0.093 | 0.105 | 0.26 | 221 | 2.32x |
| *bq-expand-aa-adjacent* | *0.135* | *0.193* | *0.14* | *217* | *1.44x* |
| bq-expand | 0.135 | 0.193 | 0.15 | 218 | 1.44x |
| *bq-expand-aa-distant* | *0.136* | *0.193* | *0.21* | *217* | *1.44x* |
| *list-aa-distant* | *1.000* | *1.002* | *0.21* | *179* | *21.57x* |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.20* | *179* | *21.57x* |
| list (baseline) | 1.000 | 1.000 | 0.20 | 179 | 21.57x |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9979, worst cell 0.89% on `small-row96`, and 8 of 8 intervals cover 1. The `sum-only` halves agree at 0.9995 on a worst cell of 0.30% on `small-bcast32`, its interval covering 1. The in-situ term reads 0.9805, 0.9800 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9985, which the correction amplifies by 1.53x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m39s, peak 132 MiB in use, 52 MiB max residency; the reader reads 36 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.7 us per call raw, 6.47 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.042/0.076/0.050/0.060/0.085

**Across the halves:** 6 of the 18 arms are faster on this half and 12 slower, at a geomean of 1.0293, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9597 to `list-aa-adjacent` at 1.1805, with `list` itself at 1.1800. **The baseline moved 18.00% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold, and this is the one class where property 3's level clause is read off a `mut-odo-vecdims` above 1.00x --- 1.27x, with `bq-expand` at 1.44x and `list` at 21.57x, the per-call constants showing through a call under a microsecond, and the ORDER unbroken like the rest. `worst` is 0.085, the highest of the ten. `lib-stage2-lean` leads outside the family at 0.050, priced against `mut-odo-vecdims` at 0.8241 --- **ahead on only 1 of 5 shapes at sign p 0.38**, so the paired figure and the win count disagree here and neither alone is the reading. **Two of the run's readings are this class's own.** Stage ten sits **1.2202** on the basis and **1.2171** on the control behind stage seven, which is the extra pass over the axes on the shortest calls the set has and is what registration (7) said would print a figure and no verdict; and the shipped leaf is one of the arms registration (1)'s three fall outside 1% on, at **0.9830** across the halves. This class also carries the run's TIGHTEST floor, 0.21% on the basis and 0.19% on the control, which is what makes those two readings worth quoting at all. Across the halves the class is disqualified, `list` having moved 18.00 points.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.35* | *85* | *1.35x* |
| liblist-stage1-sum | -- | -- | 0.36 | 98 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.32 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.33 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.34 | 97 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.03 | 109 | 0.01x |
| libunord-stage2-sum | -- | -- | 0.33 | 98 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.36 | 98 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.03 | 109 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.24* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.015* | *0.016* | *0.27* | *98* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.015 | 0.016 | 0.28 | 98 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.015* | *0.016* | *0.44* | *98* | *1.00x* |
| lib-stage2-lean | 0.015 | 0.017 | 0.36 | 98 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.015 | 0.016 | 0.35 | 98 | 1.00x |
| lib-stage1 | 0.015 | 0.017 | 0.36 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.016 | 0.016 | 0.32 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.017 | 0.29 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.017 | 0.018 | 0.30 | 97 | 1.00x |
| *mut-odo-vecdims-aa* | *0.024* | *0.028* | *0.25* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.024** | 0.028 | 0.18 | 94 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.024* | *0.028* | *0.27* | *94* | *1.00x* |
| bq-expand | 0.094 | 0.102 | 0.40 | 79 | 1.35x |
| *bq-expand-aa-adjacent* | *0.094* | *0.102* | *0.38* | *79* | *1.35x* |
| *bq-expand-aa-distant* | *0.094* | *0.102* | *0.22* | *79* | *1.35x* |
| list (baseline) | 1.000 | 1.000 | 0.69 | 44 | 22.01x |
| *list-aa-distant* | *1.002* | *1.007* | *0.85* | *44* | *22.01x* |
| *list-aa-adjacent* | *1.003* | *1.009* | *0.66* | *44* | *22.01x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0028, worst cell 0.52% on `compose-scalar`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.52% on `compose-zero-mid`, its interval covering 1. The in-situ term reads 1.0093, 1.0175 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0020, which the correction amplifies by 1.37x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m34s, peak 126 MiB in use, 33 MiB max residency; the reader reads 36 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 31 ms per call raw, 29.9 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.028/0.028/0.021/0.019

**Across the halves:** 7 of the 18 arms are faster on this half and 11 slower, at a geomean of 1.0306, from `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.9636 to `list-aa-adjacent` at 1.2214, with `list` itself at 1.2171. **The baseline moved 21.71% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.028, the lowest of the ten, and tiers at 1.00x, 1.35x and 22.01x --- and `lib-stage2-lean` leads outside the family at 0.015, priced against `mut-odo-vecdims` at 0.6178 over 4 of 4 shapes at sign p 0.12, a margin of 38.22% against this class's floor of 0.28%. **Stage ten's figure here is printed and not adjudicated, as registration (6) asks**: **0.6942** on the basis and 0.6885 on the control over stage seven, between the `bcast` and `bcastmid` readings and made of two kinds of view at once --- `compose-zero-mid` and the two `-bcast` views, where the zero-stride move fires and which read 0.4596, 0.7101 and 0.7106, and `compose-scalar`, every stride 0 and the route unchanged, which reads 1.0010 on the basis and 1.0012 on the control, inside the floor on the basis and well inside it on the control. That is the item's per-view prediction landing exactly. The class ties on every other span it carries, the list against the fill it replaces at 0.9990 and 1.0003. Across the halves the class is disqualified, `list` having moved 21.71 points, the widest of the ten.


## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 30's halves differ in ONE COMPILE FLAG and in nothing else.** One source, `Main.hs` at `7685375`, one shim at `b3a1aca` and one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of the assembler, one compiler --- ghc-9.12.4 through `cabal.project` and its freeze, both binaries carrying `ghc-internal-9.1204.0` --- one store, one plan and one set of boot libraries, one roster, one shape set, one class list and one bench order, both run under `WILDLOG=1 SATURATE=1`, both with `-A32m -I0 -T -M8G` baked and read back by `+RTS --info`. NEITHER half sets `-fspec-constr`: `-O1` is micro.cabal's own `ghc-options` on both, and `run30-libcase` adds `-fliberate-case` to one `--ghc-options` and nothing else. So this pair is Run 29's CONTROL recipe read twice, and every figure that moves between the halves moved because that one flag was added.

**The sequence was launched once and ran to the end in ONE window, and no population was rerun**, 2026-09-13T02:05:18 to 2026-09-13T10:04:58, twenty-two processes --- the main set on each half and 20 class processes, one per class per half --- with no hole between one finishing and the next starting. The gate's four ran before them from 01:31:57 and the four rider stages after them to 10:17:37; the counted work followed on a box already handed back, 10:19:28 to 11:02:41. Every process exited 0 at its roster's count --- 684 twice, 504 twice, 288 twice, and 216, 180, 144 and 108 four times each, 5544 benches --- with no complaint and no `!!` line in the wall-clock record, and the control half ran first throughout. **ONE INTRUSION WAS FOUND AND IT WAS THE WRITE-UP SESSION'S OWN.** `--wild` clears twenty-one of the twenty-two processes outright, no bench reaching 0.25 of a core; `run30-libcase-main` carries two that do, both on `cnn-L1-6x6-c1` --- `mut-odo-vecdims-add-in-leaf-u2-aa` at 0.77 of a core with a worst sample of 260.6 ms foreign and `-u2-ptr` at 0.61 --- at benches 11 and 12 of 684, about 02:06:14, 58 seconds after the sequence began, when this session ran `./run-status.sh run30` and put the reader twice over a 600 KB README. **The cells are OMITTED and not substituted, and only where they enter.** Omission drops `cnn-L1-6x6-c1` from BOTH halves, which keeps the bases matched and leaves eighteen of the nineteen main-set shapes; substitution was refused because any imputation assumes the arm's cross-half ratio, which is the quantity being measured. Four figures are requoted on the eighteen; three of them stand elsewhere in this file and say so where they do, and the fourth, `-u2-ptr`'s, stands only here: `cross mut-odo-vecdims-add-in-leaf-u2-aa` 0.9893 to **0.9919**, `cross ...-u2-ptr` 1.0016 to **1.0037**, registration (9)'s control-half adjacent pair 0.9987 to **0.9965**, and the control half's worst main-set A/A cell, 3.90% on `cnn-L1-6x6-c1`, to **3.13% on `alexnet-L1-55-c3-k11`**. Nothing else is requoted: the control half's FLOOR stays 0.84% over all nineteen, being carried by `mut-odo-vecdims-add-in-leaf-u2-aa-distant`, which was not intruded, and every other figure in this file is over the run's own nineteen shapes so that it stays comparable with the runs before it. **No verdict turns on either cell** --- registration (1) is killed by the base leaf arm, which reads 0.9891 on nineteen and 0.9900 on eighteen, past its bar either way; `list` moves 1.1710 to 1.1713 and `bq-expand` 1.0127 to 1.0129 --- and the size of the thing is one A/A cell reading 1.0390 where its clean twin on the other half reads 1.0065, on a pair whose true ratio is exactly 1. A rerun of both halves of the main set was offered and declined on 2026-09-13, the correction above being taken instead.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run30-nospec`, md5 `f507eafbcbbfecf7c2151f390caf9b92`, and `run30-libcase`, md5 `66f89adf1838ccfcf63ec11d5a10b047`, with `.text` at **20766917** bytes on the basis and **20787397** on the control --- they do not agree, the LiberateCase half being the larger by 20480 bytes, which is 20 KiB exactly and is an observation and not a mechanism. **The basis md5 is TWO-SIDED**: `Main.hs` and the shim both stand where Run 29 built them, so `run30-nospec` had to reproduce `run29-nospec`'s md5 and does, to the character, while `run30-libcase` had to differ and does. That is a repetition of the recipe Run 29 ran as its CONTROL and did not publish, and what it buys is the one input no note records --- `cabal.project.freeze` pins 97 versions and NOT the ABI hashes, so a store relinked at unchanged versions would have moved half of `.text` with every other check still passing. The basis's `.text` is `run29-nospec`'s to the byte as well. `diag` confirmed the regime in both binaries before the hours were spent, `baseOffsetsScan` against `baseOffsetsMut` on `vgg-14-c512` reading 24066407 against 2408530 on each, 9.992 times apart, which is plain -O1 read twice; `-fliberate-case` moves neither of those rows, so the md5 pair is what stands in for a reading of the variable itself.

**Neither the source nor the roster moved, and this run's fills say the layout did not move either --- which is the tightest form the pinning claim has ever been read in.** The tracked 28-byte loops were read at the build against the previous build of the basis's recipe, `run29-nospec`, with `--delta`: **every mod-64 offset preserved**, the two groups at [0, 24, 8, 0, 4, 0] and [0, 0], and --- unlike every run before this one --- **every address surviving to the byte**, 6 of 6 and 2 of 2, with `nothing moved` on both groups and no displacement at all. `Main.hs` did not move from `7685375` and the shim did not move from `b3a1aca`, so what this proves is that identical inputs place identically, which is the control the claim has never had; the cross-run column agrees, all 18 arms inside 0.7% of 1 with `list` at 0.9951. **Within the pair the layout did not move either, and that is the reading to set beside Run 29's.** `--library` reads **925** library self-loops in common, **100.0%** at the same offset in line and **100.0%** in the same straddle state --- not one tracked library loop displaced by a flag that grows `.text` by 20480 bytes. Run 29 read 39.4% of 914 for `-fspec-constr`. So the two -O2 passes differ here as sharply as they differ in the counted work: one rewrote where the library's loops sit and this one moves none of them.

**The three main-set anchors** read **6.34 us** on `cnn-slice-c32`, **3.7 ms** on `cnn-L2-24x24-c32` and **39.9 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, against **5.36 us**, **3.18 ms** and **33.4 ms** on the control --- the control FASTER on all three, by 15.38%, 14.12% and 16.10%, which is `list` being what `-fliberate-case` buys, beside the 17.10 points its main-set column reads. The direction is the opposite of Run 29's, whose control was the slower half, and for the same reason read the other way round: there the basis carried the flag and here the control does.
| shape | `l` | `list`, per call | net | `libcase`, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.51 us | 6.34 us | 5.36 us |
| `cnn-L2-24x24-c32` | 165888 | 3.8 ms | 3.7 ms | 3.18 ms |
| `stretch-wide-2xM` | 1800000 | 40.9 ms | 39.9 ms | 33.4 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at **40.9 ms** raw and **39.8 ms** net on the basis, against Run 29's 37.6 ms and 36.6 ms on a class whose population has not moved for seven runs. **That is 8.63% raw and 8.90% net slower, computed from the cells rather than from the printed three figures, and it is the flag and not the box**: Run 29's figure is its FLAGGED half's, this one is an unflagged half's, and `list` is the arm both flags move most --- read against Run 29's own unflagged half the same anchor reads 41.2 ms raw and 40.1 ms net, 0.7% from this run's on both. It is the one place in this file where a class anchor must name which half it came from, and the reason the cross-run rule for this run is `run29-nospec` and no earlier basis. Its own floor is 3.23% here, the widest of the ten. **No class population moved this run at all**: `roster-delta.py` reads 58 class views to 58 with every one of the ten classes unmoved, so each class anchor is read against its own predecessor over the same shapes.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0001** on the basis and **0.9997** on the control, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0311** and **1.1049** on the basis and **1.0293** and **1.0996** on the control for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent under the term it stands for, by the same few percent on both halves, which is where it has run since Run 17. So the flag reaches neither the forcing pass nor the gap between the two ways of measuring it.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-12, before the run, on the pair the request of the same day decided: Run 29's control recipe, ghc-9.12.4 in the dead-spot form at plain -O1 with no `-fspec-constr`, as both halves, `nospec` as it is and `libcase` with `-fliberate-case` added to its `--ghc-options`, `nospec` the basis, so that `--compare` reads nospec over libcase and every `cross` below is what that flag does to one arm; over Run 29's roster unchanged, `Main.hs` standing at `7685375` as Run 29 ran it, so `run29-nospec` is the basis recipe's previous build. Nine of Run 29's registration items are carried here, as they stand in [that run's file](run29.md#what-this-run-was-built-to-answer-and-what-it-answered), by the same request and with one amendment --- (7) to within 3% --- and with `libcase` written where they said `nospec`; nothing else in them was rewritten for this pair, so *the flag* in an item is the word Run 29 used for what its halves differed in, the figures they quote are the ones Run 29 registered, Run 28's where they say so, and their spans are read in this pair's orientation. No probe on disk has timed any arm under `-fliberate-case`.

(1) *The flag on the shipped fill.* On the main set and on every class: `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 1%`, `predict: cross lib-stage1 1.0 within 1%` and `predict: cross lib-stage2-lean 1.0 within 1%` --- the flag was measured irrelevant to the shipped family on 2026-08-24, when the shipped file was settled not to set it, and nothing since has measured otherwise. Killed by any of the three past 1% on the main set, which would say the regime the shipped file compiles under is not the regime these tables price the fill in.

(2) *The flag on the reference.* On the main set and on every class: `predict: cross list 1.0 within 3%`, `list` being the reference every table is read against and the halves' comparability bar being read on it. Killed by `list` outside 3% on the main set, which would say the two columns cannot be subtracted and every cross below carries the reference's own move.

(3) *The flag on the fused list.* The lazy stages' consumers fold one flat loop, `sumLazyRuns`, whose continuations are known, and nothing has timed it without the flag. On `runs`, `block` and `window`: `predict: cross libunord-stage7-sum 0.9 within 10%`, the list a tenth dearer without the flag; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, where the route fills or is one block: `predict: cross libunord-stage7-sum 1.0 within 1%`. Killed by the consumer inside 1% of level on `runs` and `block`, which would say the flag is irrelevant to the list too and the regime question closes; or by it past 0.8 there, which would say the list's lead over the fill is the flag's.

(4) *The odometer over the table without the flag.* `liblist-stage4-sum` over `liblist-stage2-sum`, the lazy odometer against the port's base-offset table. On `runs`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.5 within 25%`; on `block`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.78 within 25%` --- Run 28 reading 0.4951 and 0.5177, 0.7831 and 0.8018 under the flag. Killed by the odometer's consumer behind the table's past the floor on the `libcase` half, which would say the lazy list's lead over the branch's list is the flag's and not the design's.

(5) *The list over the fill without the flag.* `libunord-stage5-sum` over `libunord-stage3-sum`, the fused list against the same route's fill. On `runs`, both halves: `predict: pair libunord-stage5-sum libunord-stage3-sum 0.57 within 15%`; on `block`, both halves: `predict: pair libunord-stage5-sum libunord-stage3-sum 0.45 within 15%` --- Run 28 reading 0.5681 and 0.5810, 0.4457 and 0.4460; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, both halves: `predict: pair libunord-stage5-sum libunord-stage3-sum 1.0`, both routes filling or returning one block, the consumer summing what `fillRoute` wrote in either. Killed by the pair past the floor on both halves on any of the five fill classes; or by the list not ahead past the floor on `runs` on the `libcase` half.

(6) *Stage ten where both of its changes fire, and where one does.* `libunord-stage10-sum` over `libunord-stage7-sum` on `bcast`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.55 within 20%`; on `bcastmid`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.6 within 20%`; on `compose` the same span prints a figure and no verdict, read per view by hand, `compose-zero-mid` and the two `-bcast` views ahead past the floor and `compose-scalar` inside it --- stage nine's move alone read 0.46 and 0.71 on those views on Run 28, and stage ten's route is stage nine's on all three classes: no `bcast` or `bcastmid` view carries two axes tied on stride, and `compose`'s tied axes are its zero strides, which `canonView` merges under either order. Over `libunord-stage9-sum` on `window`, both halves: `predict: pair libunord-stage10-sum libunord-stage9-sum 0.72 within 15%`, stage seven's tie-break alone reading 0.7425 and 0.7237 over stage six there and stage nine's route being stage six's on `window`, no window view carrying a zero stride. Killed by stage ten behind stage seven past the floor on both halves on any zero-stride view, or behind stage nine past the floor on both halves on any unstrided window view, which would say the two changes do not compose.

(7) *Stage ten where neither fires.* On `rev`, `scaled`, `runs` and `block`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 1.0 within 3%` --- `zerosOutermost` hands its list back unchanged where no stride is 0, both of its branches doing so, the partition being the identity then, so `routeUnord10` is `routeUnord7` there to the byte and only the extra pass over the axes separates the dispatches. Killed by the pair past 3% on both halves on any of the four, which would say the pass is visible at a call of tens of microseconds. On `small` the span prints a figure and no verdict, read per view by hand: behind on the four views other than `small-bcast32` by the pass --- `small-flat64`'s zero stride sits on a unit axis `canonView` drops, so its route is stage seven's --- and ahead on `small-bcast32`.

(8) *`lib-stage1` against the lean fill with one fill under both.* Since 2026-09-11 both fill through `fillStage2`, so on `bcast` and `bcastmid`, where Run 28 read the lean fill ahead at 0.8635 and 0.6163 for the two bodies the leaf lacked, the pair prices the dispatch alone --- stage one's raw dims against the lean dispatch's canonical ones, which merge `bcast-inner8`'s two outer levels into one. On `bcast` and on `bcastmid`, both halves: `predict: pair lib-stage2-lean lib-stage1 1.0 within 3%`. Killed by the pair past 3% on both halves on either class, which would say the canonicalization is worth that much to the fill on broadcast views. On `runs` and `block` the routes part, stage one concatenating a slice per run where the lean dispatch fills, and Run 28's reading stands as the prediction: `predict: pair lib-stage2-lean lib-stage1 0.23 within 20%` on `runs` and `predict: pair lib-stage2-lean lib-stage1 0.42 within 20%` on `block`, both halves; killed by the lean fill not ahead past the floor on either, which would say the slice-and-concatenate route no longer loses on short runs.

(9) *The floor with the fill family's pair for a second run.* On the main set and on every class, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa mut-odo-vecdims-add-in-leaf-u2 1.0` and `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa-distant mut-odo-vecdims-add-in-leaf-u2 1.0`, each inside the population's floor as Run 28's forty-four readings were; read beside them by hand whether the whole-set floor is the fill pair's again, 0.50% on Run 28 against the carry-back figure's 0.39%, which is what decides the open question in the floor entry. Killed by the adjacent copy outside the floor on both halves on the main set.

**Seven held and two were killed, and BOTH kills fell on an item predicting a NO-OP --- which is the third run running to find that, and the second flag it has been found on.** Seven of the nine items argue somewhere that a change cannot reach a population, and (1) and (2) are the two that failed; not one item predicting a REAL effect was killed. Run 28 found this of its own four, Run 29 of its three, and this run of its two. **The converse is still not claimed**: (3), (5), (7), (8) and (9) argue no-ops too and all five held. Every item's verdict below is its KILL CONDITION applied across the populations that item names, never one sweep of the main set; every figure is re-derived from this run's own JSONs; and a span is judged against the floor of the population it is read on. **Two things the registration could not foresee govern how it reads.** Its `cross` spans are Run 29's, whose basis was the FLAGGED half where this run's is the unflagged one, so an asymmetric span predicting the flagged half faster now reads ABOVE 1 --- which affects (3) alone, (1) and (2) predicting 1.0 and being symmetric. And the gate's machine check DID fire, at +12.96% against a fingerprint belonging to Run 29's flagged half; against `run29-nospec`, this recipe's own previous build, the roster spans 0.9937 to 1.0069, so no reading here carries a box term and none is bridged.

(1) *The flag on the shipped fill.* **KILLED, by 0.09 of a point, and it is the run's largest finding.** On the main set the three arms read **0.9891**, **1.0003** and **1.0093**; the leaf is 1.09 points off its registered 1%, so the kill --- any of the three past 1% on the main set --- fires on it alone. **This is the first reading here in which an -O2 pass makes the shipped fill slower rather than leaving it alone**: Run 29 read the same three at 1.0001, 0.9945 and 1.0044 under `-fspec-constr` and the item held. **Its class spans read both ways**, which Run 29's did not: of the thirty-three arm-by-population readings ten sit outside 1%, scattered over nine populations rather than concentrated on four --- the leaf on `bcast` (1.0119), `flip` (0.9763) and `small` (0.9830), `lib-stage1` on `bcastmid` (0.9786), `runs` (1.0111) and `block` (1.0326), and `lib-stage2-lean` on `rev` (1.0110), `flip` (0.9869) and `compose` (0.9840). **And the counted work says the movement is not codegen**: the leaf's instruction count between the halves is exactly 1.0000, so whatever costs it a point is the memory or the placement. **THE KILL RESTS ON ONE CELL AND ON THE CHOICE OF STATISTIC, and both are said here rather than left to be found.** `--compare`'s cross is a PLAIN geomean of the per-shape net ratios, where the published `time` column winsorizes at 3 MADs; the two are different statistics and this arm is where they part. One shape sits outside the 3-MAD band, `cnn-slice-c32` at 0.8209 against a band of 0.9675 to 1.0367, and it is the whole of the margin: capped, the same span reads 0.9977 and the item would HOLD. So registration (1) is killed on the statistic `--predictions` adjudicates it on, which is the one the item was registered in, and it would not be killed on the column the table publishes. The verdict stands as the item's own reading; what a next run should not do is quote 0.9891 and the table's own figure for the same arm as though they were one number.

(2) *The flag on the reference.* **KILLED, as Run 29's was, and it governs the whole file.** `cross list 1.0 within 3%` reads **1.1710** on the main set, 17.10 points off. It is past the bar on every population: `compose` 1.2171, `window` 1.2027, `bcast` 1.1960, `runs` 1.1946, `block` 1.1871, `scaled` 1.1865, `flip` 1.1823, `rev` 1.1818, `small` 1.1800, `bcastmid` 1.1726. The item's own clause says what follows and it follows exactly: the two columns cannot be subtracted, and every `cross` in this run is an ordering.

(3) *The flag on the fused list.* **NEITHER KILL FIRES, and the three magnitude spans missed on orientation rather than on substance.** Read raw over every shape, as an arm with no corrected time must be, `libunord-stage7-sum` across the halves is **1.1827** on `runs`, **1.1022** on `window` and **1.0633** on `block`, against a registered 0.9 --- the flag worth 18.27, 10.22 and 6.33 points to the fused list, where Run 29 measured `-fspec-constr` worth 2.21, 2.29 and 1.61. The registered 0.9 predicted the unflagged half FASTER, which is Run 29's orientation and not this pair's. The five fill classes, whose spans are symmetric at 1.0 within 1%, all held: `rev` 0.9994, `bcast` 1.0033, `bcastmid` 1.0011, `scaled` 1.0037, `compose` 1.0025. Neither kill fires --- nothing is inside 1% of level on `runs` and `block`, and nothing is past 0.8 --- so the question the item was written to ask is answered the other way round: the list's lead over the fill is the design's, and the flag is worth about four to eight times more to the consumer than the other -O2 pass was, 3.93 on `block` to 8.27 on `runs`.

(4) *The odometer over the table without the flag.* **HELD.** `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.4678** on the basis and **0.3947** on the control on `runs`, against a registered 0.5 within 25%, and **0.7281** and **0.6800** on `block`, against 0.78. The kill wants the odometer's consumer BEHIND the table's on the `libcase` half and it is ahead by better than half and by a third, so the lazy list's lead over the branch's list is the design's and not this flag's either.

(5) *The list over the fill without the flag.* **HELD on both of its conditions.** `libunord-stage5-sum` over `libunord-stage3-sum` reads **0.5808** and **0.4900** on `runs` against a registered 0.57 and **0.4520** and **0.4237** on `block` against 0.45. The five fill classes tie inside their own floors on both halves --- `rev` 1.0008 and 1.0002, `bcast` 1.0001 and 0.9999, `bcastmid` 1.0000 and 1.0023, `scaled` 0.9991 and 1.0001, `compose` 0.9990 and 1.0003 --- so neither the fill-class kill nor the `runs` kill fires.

(6) *Stage ten where both of its changes fire, and where one does.* **HELD on both kills, with one span of the four missing.** Over stage seven it reads **0.5903** and 0.5588 on `bcast` against a registered 0.55 and **0.6214** and 0.5827 on `bcastmid` against 0.6, both halves inside 20%. Over stage nine on `window` it reads **0.7326** on the basis against 0.72 --- and **0.8863** on the control, 16.63 points off and outside the registered 15%, the only span here to miss. The first kill wants stage ten behind stage seven past the floor on both halves on some zero-stride view: the closest is `compose-scalar` at 1.0010 and 1.0012 against floors of 0.28% and 1.44%, inside on both. The second wants it behind stage nine on both halves on some unstrided `window` view: `window-128x128-k7` reads 0.9703 on the basis and 1.2538 on the control and `window-64x64-k1x9` 0.9621 and 1.1481, **the same two views Run 29 recorded parting in sign, now parting by twenty-eight and nineteen points where they parted by six and five**. So neither fires, and the `window` span's miss and that widening are one fact rather than two. `compose` prints **0.6942** and 0.6885 and no verdict, as registered, and its per-view clause lands exactly: `compose-zero-mid` and the two `-bcast` views ahead past the floor at 0.4596, 0.7101 and 0.7106, `compose-scalar` inside it.

(7) *Stage ten where neither fires.* **HELD --- and only because the item was amended.** The pair reads **1.0244** on the basis and **1.0245** on the control on `rev`, against that class's floors of 0.49% and 0.61%: past the floor on both halves, which is exactly what killed this item on Run 29. What differs is the bar. The item was amended to `within 3%` on 2026-09-13, before the run, and 2.44 points clears it; the other three named populations tie well inside --- `scaled` 1.0025 and 1.0054, `runs` 0.9997 and 0.9999, `block` 1.0000 and 1.0022. So the extra pass over the axes IS visible on `rev` for the third run running, at a call of tens of microseconds, and the item now records that rather than dying of it. On `small` the span prints **1.2202** and **1.2171** and no verdict, inside the 1.18-to-1.40 band stage nine read on Run 28, which the item's own text dropped when it was carried over.

(8) *`lib-stage1` against the lean fill with one fill under both.* **HELD, and it reproduces Run 28's figures for a third run.** On `bcast` the pair reads **0.9964** and **0.9958** and on `bcastmid` **1.0137** and **0.9890**, all inside the registered 3%, so the canonicalization is worth nothing to the fill on broadcast views once both routes fill through `fillStage2` --- under a second flag now. Where the routes genuinely part the item registered Run 28's own readings and both come back: **0.2329** and **0.2362** on `runs` against 0.23, **0.4160** and **0.4306** on `block` against 0.42.

(9) *The floor with the fill family's pair for a second run.* **HELD in every one of its forty-four readings**, two spans over eleven populations on two halves, and the kill --- the adjacent copy outside the floor on both halves on the main set --- does not fire, it reading **0.9989** on the basis and **0.9987** on the control there. **One of those forty-four is corrected**: the control half's adjacent-copy cell on `cnn-L1-6x6-c1` is one of the two this run's own intrusion spoilt, and over the eighteen shapes both halves share cleanly the span reads **0.9965**, still well inside that half's floor. **Read beside it by hand, as the item asks, the answer REVERSES Run 29's**: the whole-set floor is NOT the fill pair's this run. `bq-expand-aa-distant` carries it, at **0.57%** on the basis --- equal to the carry-back figure, which the same pair also carries --- where Run 29 read 0.51% against 0.26% and Run 28 0.50% against 0.39%, both on the fill pair. So the two-run answer that the gap was the fill pair's own property does not survive a third run, and the open question in the floor entry now has three readings and no trend.


## Its delta against the run before

Moved here verbatim from README's Provenance delta chain on 2026-09-23.

- Run 30 measured 36 timed arms over 19 main-set shapes and 58 class views in TEN classes, 684 benches and 2088, EIGHT A/A pairs, the `runs` class at FOURTEEN, `window` at EIGHT, `bcast` and `flip` at SIX, `block` and `small` at FIVE, `bcastmid` and `compose` at FOUR and `rev` and `scaled` at THREE. **Its delta against RUN 29 is NOTHING AT ALL**: `Main.hs` stands at `7685375`, the commit Run 29 built, so no arm landed, none was parked, no shape moved and no class view moved --- the roster term in any cross-run figure is ABSENT rather than small. **So `run30-nospec` REPRODUCES `run29-nospec` to the byte**, md5 `f507eafbcbbfecf7c2151f390caf9b92` on both, `.text` 20766917 on both, and `--delta` reads every tracked fill's mod-64 offset preserved AND every address surviving, 6 of 6 and 2 of 2 with no displacement --- a repetition of the recipe Run 29 ran as its control and did not publish, and the control the pinning claim never had. `run30-libcase`, at `66f89adf1838ccfcf63ec11d5a10b047` and 20787397 bytes of `.text`, is that recipe with `-fliberate-case` added and is new here. **The nineteen main-set shapes are unmoved between the two runs, so a cross-run figure is over all nineteen**, and **the box did NOT move**, though its gate machine check says otherwise at first reading: that check fired at +12.96% on `list`'s net against the fingerprint Run 29 installed, which is Run 29's FLAGGED half's while this basis is its control recipe, and against `run29-nospec` --- this recipe's own previous build --- the whole timed roster spans 0.9937 to 1.0069 with `list` at 0.9951 and the check's worst shape, `stretch-r5-8x432`, at 1.0015. So a cross-run absolute here is a subtraction and wants no bridge. What a reader has to carry is which half a figure came from: everything published in its file is `run30-nospec`, ghc-9.12.4 at PLAIN -O1 with neither -O2 pass set, and `run30-libcase` --- the same source, shim, shim environment, compiler, store and plan with `-fliberate-case` added to one `--ghc-options` --- contributes the second column of `runs/run30.md`. **Its `list` moved 17.10 points between the halves, OUTSIDE the 0.7% bar, so its two columns may NOT be subtracted**, nor may any of its ten classes', which moved 17.26 to 21.71 points: every cross-half figure in its file is an ordering. Its sequence ran in ONE window, 02:05:18 to 10:04:58, with ONE intrusion found --- two cells of `run30-libcase-main` on `cnn-L1-6x6-c1`, caused by the write-up session's own reader call 58 seconds in, omitted from the readings they enter and no population rerun. **And its floor is a maximum over EIGHT A/A pairs**, 0.57% and 0.84%, the third run to carry the shipped fill's own pair and the FIRST on which that pair does not carry the basis figure, `bq-expand-aa-distant` carrying it instead; its restricted four-pair reading is 0.57% and 0.56%, equal to the whole-set figure on the basis for the first time since the pair landed.
