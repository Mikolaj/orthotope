# Run 37 (GHC HEAD against itself, plain -O1 against -O1 with -fspec-constr -fliberate-case, under the exit span, launched from disk)

One run's write-up: its head, its Results, what the next run compares against, the properties that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 37 (GHC HEAD `10.1.20260918` against itself, plain -O1 against -O1 with `-fspec-constr -fliberate-case`, under the exit span, launched from disk): the two passes are worth 29.60 points on `list` and 31.01 on `bq-expand`, the counted work repeats Run 36's to the fourth decimal, and the one arm outside those families that moves past 3% moves the OTHER way on level counts.** The pair is Run 36's IN ITS VARIABLE --- one source, `Main.hs` at `05cfe93`, one shim at `f31bd1c` under the four switches with the exit span, ONE compiler, `10.1.20260918`, unmoved from Run 36, one roster, one shape set, every process launched from disk --- with two `-O2` passes added to the control half's command line and nothing else differing. So `the basis` below is the UNFLAGGED half and every `cross` figure reads basis over control, ABOVE 1 meaning the FLAGGED half is the faster. Over the sixteen main-set arms that carry a cross-half figure, SIX move with the families and TEN do not: the six are the `list` and `bq-expand` families entire, at **1.2959** to **1.3129**, and the ten others span **0.9630** to **1.0160**, every one of them a fill. **The bar an arm has to clear to be the passes' rather than the run's is 0.51 points** --- the widest an arm and its own A/A duplicate part in this same cross-half reading, which `--compare` prints under its table and which is NOT this population's floor, that being 0.59% and measured WITHIN one half --- and SIX of the eight strategies clear it, which is four more than Run 36's two and is the bar being four times tighter rather than the arms having moved.

**What this pair was built to settle is whether Run 36's reading of it repeats across a rebooted box, a moved source and a rewritten project file --- and it repeats on everything except the one figure Run 36's own wild cell inflated.** Run 36 read `list` at **1.3360** over nineteen shapes and **1.3129** over the eighteen without that cell; this run reads **1.2960** and **1.2950**, so the corrected figure repeats to 1.79 points and the uncorrected one misses by 4.00 --- and the cell did not recur, which [the Results paragraph on it](#results) shows directly. `bq-expand` reads **1.3101** against Run 36's 1.2980, 1.21 points out and inside the span registered for it. **So what this run measures is the pair's PRECISION, and on `list` that precision is wide enough to move the conclusion**: two readings of one pair, a day apart under three changed inputs and across two reboots, agree to 1.2 points on `bq-expand` and to 1.8 on `list` once Run 36's wild cell is set aside --- which is the scale at which a `cross` figure from this harness can be quoted across runs, and is wider than the 0.24 points that separate this run's `list` from the whole `-O2` level the pair was built to price it against. Run 36 read the two passes past BOTH accounts of them and this run reads them at the level's own figure; [What the next run compares against](#what-the-next-run-compares-against) sets the two side by side.

**The counted work parts far wider than the clock, and it parts on exactly the two families.** `list` retires **1.2930** and `bq-expand` **1.5063** times as many instructions on the unflagged half, against 1.0383 on `mut-odo-vecdims` and 1.0522 on `lib-stage2-lean` --- so the basis retires 50.6 points more instructions than the flagged half on `bq-expand` and takes only 31.0 points more time over it, a `time/counts` of **0.8697**, where `list` cashes almost exactly what it saves at **1.0023**. **And the counted work is where this run repeats Run 36 most exactly**: those four figures were registered as spans off Run 36's own sweeps and all four hold, three of them at **0.00 points** --- 1.5063 against 1.5063, 1.0383 against 1.0383, 1.0522 against 1.0522 --- with `list` 0.04 of a point out. **They move allocation too, as every earlier flag pair did, and by exactly what Run 36 read**: the flagged half allocates **0.8119** of the basis on `bq-expand` and every one of its three twins and **0.9342** on `list` and both of its, on the main set and in the same shape on all ten classes, while every other timed arm reads 1.0000 to within two tenths of a point on the main set and only one moves at all across the classes. That breaks property 3's LEVEL clause in every one of the eleven populations, as it did on Run 36 and as Run 31's whole `-O2` level did --- to the published decimal, 2.11x and 23.45x on the main set on each --- while its ORDER clause holds everywhere.

**THREE of the four registration items hold their sentences, over 17 spans of which 15 held and 2 were killed --- and the two kills say different things.** (1)'s wide `list` span dies at 4.00 points and its narrow one holds at 1.79, which is Run 36's wild cell not recurring rather than a disagreement about the passes; (2) holds on `bq-expand`; (3)'s SENTENCE holds, since no arm outside the two families joins them, while one of its ten spans dies on `mut-odo-vecdims-add-in-leaf-u1` at 0.9630 --- an arm that moved 3.70 points the BASIS's way, which is the direction that sentence does not speak to; (4) holds on all four spans, three of them exactly, and is the item this run vindicates. **The run's one open reading is that `u1` arm, and step 4a names the half**: it is level on the basis against Run 36 and moves on the CONTROL in eight of the eleven populations, up to 1.4164 on `bcast`, with the counted work at 1.0000 on both halves --- so it is this run's control binary or its file instance and not the pair's variable, which [Results](#results) sets out and the open list now carries. Nothing else is wrong with the run: 22 of 22 processes gated clean, no bench of the 119 logs reaches 0.25 foreign, the gate read SOUND with its two passes agreeing to what each half's own legs predict, and the plateau's refusal splits exactly by half with each half flat inside 3.6%.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. **What this run leaves the next one is a basis with a like-for-like predecessor and a question that is not the pair's**: `run37-gheadnospec` is a plain -O1 GHC HEAD build on the recipe Run 36 also built, so those two published columns are one subtraction rather than a chain --- and the question left open is `mut-odo-vecdims-add-in-leaf-u1`, which this run's CONTROL binary runs up to 41.6% slower than Run 36's control did on instruction counts identical to the fourth decimal, and which wants post-run step 4a's copy test on a quiet box. The SPLIT --- which of the two passes carries the regime's points --- is still read on no compiler this series builds with.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a PLAIN -O1 table under the exit span**, as Runs 32 to 35's were, plain -O1 being the regime `Data/Array/Internal.hs` actually compiles under. **On this run that sentence describes the BASIS half and not the pair**: the control half is that same -O1 with `-fspec-constr -fliberate-case` on its command line, two of `-O2`'s passes and nothing else, so the table below is the unflagged half's. **What is new in it is neither the compiler nor the switch**: the compiler is Run 36's in-tree stage1 `10.1.20260918` unmoved, and the shim at `f31bd1c`, the four shim variables and the regime are Run 36's to the commit. What moved is the source, `Main.hs` from `0eda736` to `05cfe93` in three of the owner's commits; the project file, `cabal.project.ghead` rewritten to the minimum that compiles on a current index; and the launch, off the `hugebin/` mount and onto disk, that mount having been suspended on 2026-09-19. **Read against the half Run 36 built by this basis's own recipe, the distance is at most 1.90 points on every arm**: over the 16 arms that carry a corrected time, this run over that one runs from **0.9810** on `list` to **1.0165** on `lib-stage2-lean` --- below 1 meaning this run is the faster --- and read as a ratio to `list` within each run, which cancels a box term exactly, the fifteen others give a `--bridge` geomean of **1.0221** with ONE outside the 3.3% drift band Run 11 measured, `lib-stage2-lean` at 1.0362. That span carries a moved source, a rewritten project file, a launch off the mount and two reboots, and no compiler term at all. **The `alloc` column is a median over this run's own nineteen shapes**, `bq-expand` at 2.78x and `list` at 25.20x, so it is a statistic of a strategy and a shape set together and does not cross to a run that timed a different set.

**And it is the basis half's**, `run37-gheadnospec`, as every published table here is from Run 11 on: the control half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-two rows. Which half publishes is not the re-declaration of 2026-09-15 this run inherits --- that one names ghc-9.12.4, and this pair has ONE compiler on both halves, so it cannot decide anything here. What decides it is the pair's own variable: the UNFLAGGED half is what `Data/Array/Internal.hs` compiles under, the flagged one is the candidate reading, and `--compare` takes the basis first, so every `cross` figure below reads unflagged over flagged and ABOVE 1 means the FLAGGED half is the faster. **None of the thirty-two rows is a first reading**: the roster is Run 36's less three arms --- none in, three out, thirty-two survivors in the same order over the same nineteen shapes, which `roster-delta.py` read off the two runs' binaries --- so every row here has a twin in Run 36's file, and the three that left (`libunord-stage10-list-sum`, `libunord-stage10-sum` and `libunord-stage11-sum`) took no row with them that this table needed.

**Comparing runs?** The table below is Run 37's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the properties to test are [the ones after it](#the-properties-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.59%** from 1 on the basis half or **0.48%** on the control, read over the eight pairs this roster carries --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **The whole-set figure and the carry-back one COINCIDE on both halves this run**: 0.59% and 0.48% are the same over the four pairs that carry back to Run 10 as over the eight the roster carries, `bq-expand-aa-distant` carrying both on each half, where Run 36's two parted on both halves and named three different pairs between them. So the restriction costs nothing here. Beside those, the worst SINGLE A/A cells of the two MAIN-SET processes --- **3.09%** on `stretch-wide-2xM` on the basis and **5.79%** on `alexnet-L2-27-c48-k5` on the control --- are not floors at all and are not to be quoted as any. **And its two columns may be differenced on NONE of the eleven populations**, `list` having moved **29.60 points** on this run's main set and between 25.95 and 36.02 on the ten classes, all of it past the bar, so every cross-half figure in this file is an ORDERING and not a subtraction.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns, and why `time` is a winsorized geomean of slopes rather than criterion's mean, is [README's *Reading a run file*](../README.md#reading-a-run-file).

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.58* | *55* | *2.78x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.55 | 69 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.55 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.57 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-list-sum | -- | -- | 0.54 | 69 | 1.00x | the same, base's `sum` over stage four's list -- the fold a library user brings, over the lazy odometer under the lean dispatch |
| liblist-stage4-sum | -- | -- | 0.56 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage12-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage twelve's list --- stage eleven with the run chosen among tied unit-stride axes by its length |
| libunord-stage13-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage thirteen's list --- stage twelve's route found with fewer passes over the axes |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage9-sum | -- | -- | 0.03 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.39* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.02* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.02* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.023 | 0.112 | 0.54 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage2-lean-u1 | 0.025 | 0.109 | 0.58 | 69 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.111* | *0.57* | *69* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.112* | *0.46* | *69* | *1.00x* | *A/A control* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.112 | 0.46 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| lib-stage1 | 0.027 | 0.112 | 0.50 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.109 | 0.53 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| *mut-odo-vecdims-aa-distant* | *0.045* | *0.111* | *0.44* | *66* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-aa* | *0.045* | *0.111* | *0.40* | *66* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.045** | 0.111 | 0.34 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| bq-expand | 0.128 | 0.260 | 0.68 | 50 | 2.78x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.129* | *0.260* | *0.61* | *50* | *2.78x* | *A/A control* |
| *bq-expand-aa-distant* | *0.129* | *0.260* | *0.37* | *50* | *2.78x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.65 | 21 | 25.20x | -- |
| *list-aa-distant* | *1.001* | *1.008* | *0.74* | *21* | *25.20x* | *A/A control* |
| *list-aa-adjacent* | *1.003* | *1.013* | *0.46* | *21* | *25.20x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and on this run FIVE of the 105 pairs among the fifteen timed arms other than `list` part in SIGN between the two statistics on the basis, where Run 36's basis parted on two, Run 35's on three and Run 34's on six. Four of the five are `lib-stage2-lean-u1` or `lib-stage1` against a leaf arm --- `lib-stage2-lean-u1` over the shipped leaf reads column **0.9588** against a paired **1.0341**, and over that leaf's two A/A copies 0.9662 and 0.9804 against 1.0354 and 1.0353, while `lib-stage1` over `mut-odo-vecdims-add-in-leaf-u1` reads column 0.9872 against a paired 1.0060 --- and the fifth is the shipped leaf's own adjacent copy over its distant one, column 1.0147 against a paired 0.9999. **The widest disagreement is on the row the cap touched most**: `lib-stage2-lean` over `list-aa-adjacent` divides to 0.0232 on the column where the paired figure is 0.0283, the column 18.1% under it, and `lib-stage2-lean` is the widest-capped row of the table, five of its nineteen cells capped and its published 0.02327 sitting 18.1 points under its plain per-shape geomean of 0.02841. Those column ratios are `--pair`'s own `published-column ratio` and not the printed table divided. **And a SINGLE row's movement between runs is not the arm's either**: `--movement` reads fifteen of the sixteen rows moved against Run 36's table and one at the same three decimals, where `--compare` against the JSON of the half Run 36 built by this basis's recipe puts every one of the sixteen within 1.90 points of level and `list` at 0.9810.

**This run's two columns may be differenced on NONE of the eleven populations, as Run 31's could not, and the reason is the pair itself.** The 0.7% bar asks whether `list` --- the denominator every other row is divided by --- sits still between the halves, and here the two passes move `list` by **29.60 points** on the main set and by 25.95 on `bcastmid` to 36.02 on `bcast` over the ten classes, every one of the eleven figures past the bar by a factor of thirty or more. So on every population in this file an arm-by-arm figure across the halves is an ORDERING and not a subtraction, and each says so in its own cross-half line. Run 36, this same pair, marked all eleven past the bar as this one does, and so did Run 31, whose variable was the whole level; Run 35 could difference seven of the eleven and Run 34 nine. **That is the bar working rather than failing**: it exists to stop a margin being read off two columns with different denominators, and a pair built to move the denominator is the case it was written to refuse.

`concat-runs` has no row, and neither do the other 93 arms the roster holds and checks without timing --- **94 of its 126** in all, against Run 36's 88 of 123: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). **THREE arms left the timed roster and THREE untimed twins joined it**, in the owner's own commits and not this run's preparation: `2973582` moved `libunord-stage10-list-sum`, `libunord-stage10-sum` and `libunord-stage11-sum` to `Only`, and `05cfe93` added three Storable-table twins the same way, so `roster-delta.py`, read off the two binaries, reads 35 arms to 32 over 19 shapes to 19, NONE in and THREE out, the thirty-two survivors in the same order and the sixty-one class views unmoved over the same ten classes. So a movement against Run 36's own basis column is a movement on the **16 shared arms that carry a corrected time**, with a source, a project-file and a launch term between the two runs and no compiler term at all --- and a movement across THIS run's two halves is the two passes, with no term of any other kind.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is `lib-stage2-lean` at 0.023**, with `lib-stage2-lean-u1` at 0.025, the shipped leaf's two A/A copies at 0.026, the leaf itself and `lib-stage1` at 0.027 and `mut-odo-vecdims-add-in-leaf-u1` at 0.028 --- **five timed non-control arms below `mut-odo-vecdims`'s 0.045**, every one of them a fill that writes the result, the same five Runs 31 to 36 had. **The column and the pair now DISAGREE about the order on five of the 105 pairs**, where Run 36's basis parted on two, and the widening is the winsorizing rather than an arm: `lib-stage2-lean` is the widest-capped row here, five of its nineteen cells capped and its published 0.02327 sitting 18.1 points under its plain per-shape geomean, where Run 36's widest-capped row sat 4.0 under. Four of the five partings set that row or `lib-stage2-lean-u1` against a leaf arm. Paired on the basis, `lib-stage2-lean` over `lib-stage1` is **0.9429** at 14 of 19 and p 0.064, over its own unrolling twin **0.9581** at 14 of 19 and p 0.064, and over the shipped leaf **0.9908** at 13 of 19 and p 0.17 --- so the pairs put the lean fill first throughout, but separate it from the leaf by under a point at a p that settles nothing, which is the column's 0.023-against-0.027 read as a margin rather than as an order. **The third is that the leaf fusion is untouched by the two passes**: `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6358** on the basis and **0.6377** on the control, 0.19 of a point apart on a pair that moves `list` by thirty points, and both inside the 0.6358 to 0.6525 that Run 34's file records ten readings across Runs 29 to 33 spanning, the basis reading sitting exactly on that span's floor --- a span carried from that file and not re-derived here.

**The two standing placement controls are still gone with the prune, the straddlers stand at TEN on each half, and this pair moves tracked fill copies as a whole compiler does.** Within the pair `./loop-offsets.py run37-gheadnospec run37-gheadtwopass` puts the tracked six-copy group at **[18, 0, 0, 0, 9, 2]** on the basis and **[18, 0, 7, 7, 9, 26]** on the control, and where the basis carries that group beside a seven-instruction two-copy one at [0, 0] and an eight-instruction two-copy one at [0, 0], the control carries a seven-instruction three-copy group at [0, 0, 0], a seven-instruction two-copy one at [0, 0] and an eight-instruction two-copy one at [4, 4]; the basis holds 29 self-loops of 28 B in 21 distinct byte-sequences and the control 32 in 22. So two of `-O2`'s passes displace fill copies as a compiler change and an optimisation level do, which is what `--library` says over the whole library too: 806 self-loops in both, **4.3%** at the same offset in line and 46.4% in the same straddle state --- the 4.3% being Run 36's to the tenth, and a tenth from Run 31's 4.4% for the whole `-O2` level. **Against the previous build of the basis's own recipe, Run 36's basis half, `--delta` reads EVERY mod-64 offset preserved on both groups** --- [18, 0, 0, 0, 9, 2] and [0, 0] --- with no address surviving to the byte and three displacements on the six-copy group and two on the other, so a moved source and a rewritten project file moved every copy and no head's offset; a third group's copy count moved, 1 to 2 at [0] to [0, 0], which is the new code rather than a placement finding. **And the exit spans astride are ZERO on both halves**, as `LOOP_EXITSPAN=1` owes; post-run step 0's naming, taken off the binaries that were timed, puts the same ten bodies at the same ten mod-64 offsets on each half --- 42, 50, 19, 42, 19, 49, 45, 42, 19 and 29 in address order --- so the two passes moved no straddling loop across a cache line either. **Five of the ten are NAMED and five refused, and both halves name and refuse the same ones**: the named are `fillStage2Short` and four leaf bodies, `...U2Down`, `...U2Last` and two of `...U2` itself, each matched byte for byte in a `-g3` twin built to that half's own recipe; of the five refused, four carry a `--loose` family of three or four members and name nothing, and one has a family of ONE, `fbMutOdoVecdimsAddInLeafU2Ptr`. The straddle count moved from Run 36's eight, on both halves alike, which is the moved source and not the regime.

**Run 36's one anomaly did NOT recur, and this run reads that cell where its copies said it should be.** Run 36's basis read `list` on `stretch-coprime-r7` at a net 1.5371e-03 s, a criterion CI of 10.07% and an R2 of **0.9395**, while its own two A/A copies in that same process read 1.1012e-03 and 1.1036e-03 --- the original 39% above duplicates that share its code, which is the 28.36% that file refuses to call a floor. **This run's basis reads the same cell at 1.1174e-03, CI 0.5237% and R2 0.999839**, its copies at 1.1171e-03 and 1.1109e-03: the original is back among its duplicates, 0.02 of a point from the adjacent one, and it lands within 1.5% of where Run 36's copies sat. So Run 36's cell was wild and not the shape --- that run could argue it from its copies and this one shows it by repetition, which is the stronger reading and was not available to it. **It is also why this run's `list` headline falls 4.00 points under the span registered off Run 36's**: the wide span was centred on 1.3360, a figure that cell was worth 2.31 points of, while the narrow span registered beside it --- Run 36's own 1.3129 over the eighteen shapes without it --- is the one this run holds, at 1.2950 over those same eighteen. **And the main set carries NO cell under an R2 of 0.99 this run**: the only one anywhere is `window-64x64-c16-k3/libunord-stage6-loop-sum` at 0.9876, on a reducing consumer that publishes no corrected time.

**What this run has instead is an ARM and not a cell, and its counted work says the codegen is not why.** `mut-odo-vecdims-add-in-leaf-u1` reads **0.9630** across the halves where the nine other arms outside the two families read 1.0026 to 1.0160 --- the one arm outside them to move past the registration's 3% band, 3.70 points the BASIS's way, which is the direction no item anticipated. Its five lowest cells carry most of it, and a sixth sits just behind them: `alexnet-L1-55-c3-k11` at **0.7841**, `stretch-tall-Mx2` at **0.8102**, `cnn-slice-c32` at 0.8851, `cnn-L1-24x24-c1` at 0.9443 and `cnn-L2-24x24-c32` at 0.9558, with `lenet-L1-28-c1-k5` at 0.9590 behind them, where the shipped leaf of its own family reads 1.0108, 1.0102 and 1.0670 on the first three. **Its counted work is level with that family**: 1.0501 against the leaf's 1.0521, `lib-stage2-lean`'s 1.0522 and `mut-odo-vecdims`'s 1.0383, so the flagged half retires the same instructions on this arm as on its siblings and takes 3.7 points longer over them --- a `time/counts` of **0.9170** where every other fill sits between 0.9545 and 0.9706 and the `list` family sits above 1. By the three kinds [post-run step 4b](../README.md#making-a-major-benchmark-run) sorts such rows into, that is TIME-LED with counts level and the family's other arms agreeing, which is one half's binary, its file instance or its process rather than the pair's variable. **And step 4a's half-local reading says WHICH HALF, which no cross-half figure can**: read each half against Run 36's same half, this arm is LEVEL on the basis --- 0.9836 to 1.0085 across the eleven populations --- and moves on the CONTROL in eight of them, at 1.0337 on the main set, 1.1165 on `flip`, 1.1460 on `block`, 1.2677 on `runs`, 1.3571 on `compose` and **1.4164** on `bcast`, with the counted work at **1.0000** on both halves in every one of the eleven. So this run's control binary is slower on that arm than Run 36's control binary was, executing the same instructions to the fourth decimal, and the cross-half 0.9630 is that and not the two passes. **What it is NOT is settled**: the counts rule out codegen and the family's A/A copies rule out the process, and what remains --- this half's binary or its file instance --- wants the copy test post-run step 4a names, which wants the box quiet again and is therefore asked for rather than taken here. It is [an open-list entry](../README.md#what-is-open), not a finding of this pair.


## What the next run compares against

**Run 38's pair is not declared yet: no entry in README's open list names it, and the ruling of 2026-09-19 makes such an entry the ONE declaration site, so whatever it is will be spelled there and not here. What this run leaves as the reference is `run37-gheadnospec`**, the unflagged half whose column stands below: GHC HEAD `10.1.20260918` through `cabal.project.ghead`, `Main.hs` at `05cfe93`, the shim at `f31bd1c` under the four switches with the exit span, every process launched FROM DISK, `hugebin/` being suspended, at plain `-O1`, which is the regime `Data/Array/Internal.hs` compiles under. **It is the second published basis on that compiler and the FIRST with a like-for-like predecessor**: against `run36-gheadnospec`, the same recipe one day earlier, every one of the sixteen timed arms reads within 1.90 points of 1, 0.9810 to 1.0165, at a `--bridge` geomean of 1.0221 with ONE outside the 3.3% drift band, `lib-stage2-lean` at 1.0362. **That step is not a null, and the compiler is not what it carries**: the two builds share their compiler to the commit, so those 1.90 points hold a moved source, a rewritten project file, a launch off the `hugebin/` mount and two reboots, none of them measured apart. **The pair itself is Run 36's, repeated**: the two `-O2` passes together against neither, on one compiler, one shim, one roster and one bench order, which is the first whole-pair repetition this series has run. **What it leaves unasked is the split**, which its own outcome makes a candidate for a later pair: this pair prices `-fspec-constr` and `-fliberate-case` TOGETHER; the single-pass readings that would say which of them carries the regime are Runs 29's and 30's, taken on ghc-9.12.4 and on a roster three sources back, and no reading of either pass alone exists on this compiler. Either half of this pair against the unflagged one would supply it at one variable and one term, and it is [an open question][open] rather than Run 37's, which repeats this pair as registered. **And the arithmetic Run 31 left open has now been read TWICE on one pair, and the second reading moves the answer.** Run 31 read `-O2` worth **1.2974** on `list` where `-fspec-constr` and `-fliberate-case`, measured one at a time on Runs 29 and 30, multiply to **1.3325** --- a 3.51-point overshoot that is either the level's other passes handing `list` back or an artefact of composing two runs' readings. Run 36 read the two passes together at **1.3360**, above both, and concluded that the level's other passes do hand `list` back. **This run reads them at 1.2960 over the nineteen shapes and 1.2950 over the eighteen without Run 36's wild cell --- 0.14 and 0.24 of a point UNDER the level, and 3.65 and 3.75 under the composition.** So the two readings of one pair STRADDLE the level and agree only that the composition overshoots: Run 36's corrected 1.3129 sits 1.55 points above the level and this run's 1.2950 a quarter-point below it, the two parting by 1.79 --- inside the band this run's registration drew, and wider than the distance from either to the level. **What that costs is Run 36's conclusion**, which a pair reading the two passes at the level's own figure does not support; what a third reading would settle is which of 1.3129 and 1.2950 is the outlier, and one clean rerun of the main set on these same two binaries would say. **On `bq-expand`, where the level and the composition agree to a quarter of a point, BOTH readings confirm both accounts**: Run 36's 1.2980 and this run's 1.3101 against 1.2943 from the level and 1.2967 from the composition.

**The COMPILER variable was not this run's to vary --- both halves are one in-tree stage1, `10.1.20260918`, as both of Run 36's were --- and the step this run reads is not a compiler step at all.** The tally of pairs BUILT to ask the compiler stands where Run 35 left it, at TEN. **What this run adds instead is the first whole-pair REPETITION the series has had**: Run 36 and Run 37 are the same two recipes on the same compiler, built from sources a day and three commits apart, and their cross-half readings agree to 1.21 points on `bq-expand`, to 1.79 on `list` once Run 36's wild cell is set aside, and to between 0.00 and 0.04 of a point on all four counted-work figures. That is a repetition of the READING and not of a binary --- no md5 here reproduces one of Run 36's --- so what it bounds is the harness and the box together, not the compiler's determinism, which `-fobject-determinism` and the md5s settle separately. **The REGIME variable has now been asked FIVE times.** Put in one orientation, the unflagged half over the flagged, Runs 29, 30 and 31 read `list` at **1.1379**, **1.1710** and **1.2974** and `bq-expand` at **1.2804**, **1.0127** and **1.2943**, all three on ghc-9.12.4; Run 36 read the two passes together on GHC HEAD at **1.3360** on `list` and **1.2980** on `bq-expand`, and this run reads them at **1.2960** and **1.3101**. On `bq-expand` the single-pass pair multiplies to 1.2967 against Run 31's measured 1.2943, 0.19% apart, and this run reads 1.3101, 1.34 points above the higher of them --- four accounts inside a point and a half, across two compilers. On `list` they multiply to 1.3325 against Run 31's 1.2974, 3.51 points apart, and this run reads 1.2960, on the LEVEL's side of that gap and 0.14 of a point under the level itself, where Run 36 read 1.3360 and landed past the composition. The cross-compiler term the reading carries is bounded rather than assumed: the -O1 baseline moved 0.77 of a point from ghc-9.12.4 to the HEAD of 2026-08-03, by `run35-exit` against `run35-gheadexit`, and under a point from that HEAD to this one.

**What Run 37 leaves the next run to read against, and the first item is a check that did NOT fire. The box was REBOOTED TWICE between Run 36 and this run**, and the gate says it still measures as it did: read against the fingerprint Run 36 installed, the machine check that `run-gate.sh` runs on the gate's basis-half `a` JSON puts `list`'s net at **-2.24%**, worst `stretch-coprime-r7` at **-28.15%**, 1 of 19 shapes past 5% and the geomean inside the 3% bar --- the main-set JSON reads the same check at -1.90% and -27.44%, which is the instrument and not a disagreement, and the gate's is the figure the note carries. **And this reading IS like-for-like, which no earlier run's machine check has been**: Run 36's basis is this basis's own recipe to the compiler, the shim, the four switches, the level and the launch, so the check carries NO compiler term at all, where Run 36's carried two steps and one of them unmeasured. What it does carry instead is a source term, a project-file term and a launch term. **The one shape past 5% is past it DOWNWARD, and it is the fingerprint's anomaly rather than the box's**: the kept fingerprint holds Run 36's wild `stretch-coprime-r7/list` cell, which this run does not repeat, and dropping that one shape takes the gate's check from -2.24% to **-0.55%** with a worst of -3.70% and nothing past 5%, and the main set's from -1.90% to -0.24%. So the box moved by something under half a point, not by two, and this run publishes INSIDE the third machine era rather than opening a fourth. The fingerprint below is this run's own, with that cell at its clean value, so the next run's machine check reads against a fingerprint carrying no wild cell. **What a Run 37 reading may take from those sixteen arms that this one could not take from Run 35's basis is a like-for-like check**: this basis is the SECOND published one on this compiler and the FIRST with a predecessor built to its own recipe, so a next run keeping it compares against this basis with the box as the only term.

**Registered with the pair.** Run 37's four registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. THREE items hold their sentences and one is killed, over 17 spans of which 15 held and 2 were killed. **What a next registration should take from this one is that a WIDE band centred on an uncorrected figure dies with the correction, and that registering both is what made the outcome readable**: item (1) drew its wide span on Run 36's whole-set 1.3360 and its narrow one on that run's own 1.3129 over the eighteen shapes without its wild cell, and said in its own text that the narrow span was the one that would decide. It was right, for the reason it gave --- the cell did not recur, so the whole-set figure landed near the corrected one and the wide band, sitting 2.31 points above it, could not hold. The wide span alone would have reported only that something moved. **And item (4) is the one to copy**: four bands drawn from the previous run's COUNTS sweeps, at one and two percent, held at 0.00, 0.00, 0.00 and 0.04 points, where the time bands drawn from the same run missed by 1.21 to 4.00. Counted work carries across runs of this harness an order of magnitude better than the clock does, which Run 36 argued from two spans and this run confirms on four. **What NO item anticipated is an arm moving the BASIS's way**: item (3)'s ten null spans were written to catch an arm JOINING the two families, and the one that died, `mut-odo-vecdims-add-in-leaf-u1` at 0.9630, went the other direction entirely. A null span is two-sided whether or not its sentence is; say which side the sentence means.

What this section stands on --- the rulings on the position term, the allocation area, a change of basis and a pair's two halves, which of its tables are edited by hand, and why the fingerprint is kept --- is [README's *Reading a run file*](../README.md#reading-a-run-file).

**The next run compares against Run 37**, whose halves were launched FROM DISK and whose basis carries `LOOP_EXITSPAN=1` at plain -O1 on the in-tree stage1 `10.1.20260918`; a run keeping that recipe reads against this basis within the switch, as this run read `run36-gheadnospec` within it. Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. **The step this run records IS basis to basis, which no earlier run's was**: Run 36 published a HEAD half on this basis's own recipe, so the two published columns carry no compiler term and the step is one subtraction rather than a chain. Over the **16 arms both rosters time and both give a corrected time** it runs from **0.9810** on `list` to **1.0165** on `lib-stage2-lean`, below 1 meaning this run is the faster, ALL SIXTEEN within 1.90 points of 1 --- and what those points carry is a moved source, a rewritten project file, a launch off the `hugebin/` mount and two reboots, no one of them measured apart. **The table below is this run's own two halves and no earlier run's**, seven strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in TWO GHC FLAGS and in nothing else. Its two columns may NOT be differenced, `list` having moved 29.60 points between them, so the table is two orderings read side by side.
| strategy | Run 37 (plain -O1, dead-spot, exit span, -A32m, HEAD 10.1.20260918) | Run 37 (that recipe plus `-fspec-constr -fliberate-case`) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.045** | 0.058 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.028** | 0.038 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.027** | 0.031 |
| `lib-stage1` | **0.027** | 0.032 |
| `lib-stage2-lean` | **0.023** | 0.031 |
| `lib-stage2-lean-u1` | **0.025** | 0.032 |
| `bq-expand` | **0.128** | 0.127 |

**READ THE SECOND COLUMN AS A RATIO AND NOT AS A SPEED, and this run it is not even an ordering of speeds.** Every entry is that arm's net over `list`'s net in ITS OWN half, and `list` moved **29.60 points** between the halves --- forty-two times the 0.7% bar --- so the control column reading HIGHER on every row is the denominator having shrunk under it and not one arm of it having slowed. In absolute terms the flagged half is the faster on fifteen of the sixteen timed arms, `mut-odo-vecdims-add-in-leaf-u1` at 0.9630 the one exception; the column cannot say so, and is not asked to. **And it is not an identity either**: each entry is winsorized per row within its own half, so dividing an arm's two entries does not reproduce its `--compare` figure over `list`'s. On `lib-stage2-lean` the two unrounded published entries, **0.02327** and **0.03060**, divide to 0.7605, where the two PLAIN per-shape geomeans, 0.02841 and 0.03623, divide to **0.7841** --- which is the arm's cross-half figure over `list`'s to within 2e-4, 1.0160 over 1.2960 giving 0.7840. The capping is the whole of the difference: five of that row's nineteen cells capped on the basis and four on the control, whose published figure sits 15.5 points under its plain one where the basis's sits 18.1 under. The arm-by-arm reading of what the two passes are worth is in the head, off `--compare`, where the reference is not divided out.

**A published geomean is over the same 19 shapes, and two halves of one run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered. **THE TABLE ABOVE IS NOT SUCH A PAIR**: `list` moved **29.60 points** between these halves, forty times the bar, so the two columns are read side by side as orderings and never differenced. **They print far apart on six of the seven rows, the control higher on each of those six** --- `mut-odo-vecdims` 0.045 against 0.058, `mut-odo-vecdims-add-in-leaf-u1` 0.028 against 0.038, the shipped leaf 0.027 against 0.031, `lib-stage1` 0.027 against 0.032, `lib-stage2-lean` 0.023 against 0.031 and `lib-stage2-lean-u1` 0.025 against 0.032 --- while `bq-expand` prints the other way, 0.128 against 0.127, which is the one arm whose own move outruns the denominator's. Read DOWN a column the head and the foot are the same on both, `lib-stage2-lean` leading each and `bq-expand` at the foot of each --- and on the control the lead is the unrounded values' and not the printed ones', `lib-stage2-lean` and the shipped leaf both printing 0.031 where the column resolves them at 0.03060 against 0.03150. **Those gaps are the moved denominator and the winsorizing together, in that order**: `lib-stage2-lean`'s plain per-shape geomeans are 0.02841 and 0.03623, 27.5 points apart where the published 0.02327 and 0.03060 part by 31.5, the cap touching five of its cells on the basis and four on the control --- so on this row the cap WIDENS the printed gap where on Run 36's it narrowed it.

**The control half's own standings on the arms this run's roster carries, which no table here holds, every published table being the basis half's.** Read off the control half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9176** (0.9574) and against `mut-odo-vecdims` **0.6377** (0.6358); `lib-stage1` against `-u2` **1.0392** (1.0508); `lib-stage2-lean` against `-u2` **0.9798** (0.9908) and against `lib-stage1` **0.9428** (0.9429); and the headline pair README's opening leads with, `bq-expand` against `mut-odo-vecdims`, **2.1900** (2.8471), 0 of 19 shapes either way. **All six hold their direction across the halves**, and five of them move by under four points; the sixth is the headline pair, which the passes move by 65.7 points and which is the pair's own variable rather than an ordering that shifted. The lean fill is ahead of the shipped leaf on both halves, by 0.9 points on the basis and 2.0 on the control, and ahead of `lib-stage1` by 5.7 on each. **The ordering Run 36 read as settled DOES change sides here, and on the arm the open list carries**: `mut-odo-vecdims-add-in-leaf-u1` against `lib-stage1` reads **0.9940** on the basis and **1.0486** on the control, where Run 36 read 1.0144 and 1.0148 four ten-thousandths apart --- the basis putting `u1` ahead and the control behind, which is that arm's control-half movement and not a change in either arm. **What the two passes do move is which arm a class bolds**: three of the ten name a different one on the two halves --- `bcastmid`, `lib-stage2-lean` on the basis against `lib-stage1` on the control, both within the outside-the-family column; and `runs` and `block`, which change COLUMN, the arm outside the family leading `runs` on the basis and the family's ceiling on the control, and the reverse on `block`.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what the next run reads against. **A class figure compared across the Run 11/Run 12 boundary is not compared on one build**: Run 11's class tables are its *aligned* half's and Run 12's its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06, so a point or two of movement across that boundary is the shim rather than the class. From Run 13 on, every run's class tables are its own basis half's, Run 34's included.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 6.29 us | 0.081 | `lib-stage2-lean` 0.078 | `mut-odo-vecdims-add-in-leaf-u2` 0.057 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.6 us | 0.091 | `lib-stage2-lean-u1` 0.071 | `mut-odo-vecdims-add-in-leaf-u2` 0.070 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 119 us | 0.063 | `lib-stage2-lean` 0.031 | `mut-odo-vecdims-add-in-leaf-u2` 0.041 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 386 us | 0.044 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `gather48-src-50` | 3 | 22500 | 453 us | 0.049 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-coprime-r7` | 13 | 60060 | 1.12 ms | 0.030 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.72 ms | 0.052 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `stretch-primes` | 89 | 250357 | 4.44 ms | 0.025 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 16.9 ms | 0.040 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `vgg-14-c512-k3` | 3 | 903168 | 19.7 ms | 0.053 | `lib-stage1` 0.029 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 19.9 ms | 0.031 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `stretch-inner256` | 256 | 1750784 | 44.9 ms | 0.022 | `lib-stage2-lean-u1` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.019 |
| `stretch-pow2stride` | 64 | 1769472 | 31.6 ms | 0.111 | `lib-stage2-lean-u1` 0.109 | `mut-odo-vecdims-add-in-leaf-u1` 0.109 |
| `stretch-r5-8x432` | 8 | 1769472 | 47.8 ms | 0.022 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `stretch-square-1341` | 1341 | 1798281 | 31.1 ms | 0.087 | `lib-stage1` 0.076 | `mut-odo-vecdims-add-in-leaf-u2` 0.076 |
| `stretch-bigstride` | 3 | 1800000 | 50.7 ms | 0.033 | `lib-stage2-lean` 0.014 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 39.6 ms | 0.059 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 41.4 ms | 0.021 | `lib-stage1` 0.021 | `mut-odo-vecdims` 0.021 |
| `stretch-wide-2xM` | 2 | 1800000 | 39.2 ms | 0.058 | `lib-stage2-lean-u1` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 939 us | 0.029 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 29.3 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 29.6 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 29.2 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 35.6 ms | 0.015 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 39 ms | 0.058 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u1` 0.020 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.64 ms | 0.053 | `lib-stage2-lean-u1` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 4.31 ms | 0.019 | `lib-stage2-lean` 0.012 | `mut-odo-vecdims` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 47.6 ms | 0.035 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.014 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 42.3 ms | 0.022 | `lib-stage2-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.25 ms | 0.019 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.26 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.25 ms | 0.024 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `block-run64-page` | `block` | 64 | 131072 | 2.39 ms | 0.030 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 |
| `block-r3-vol64` | `block` | 64 | 262144 | 4.46 ms | 0.019 | `lib-stage2-lean` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.019 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 952 us | 0.029 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 947 us | 0.029 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.013 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 29.6 ms | 0.019 | `lib-stage2-lean` 0.016 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 29.8 ms | 0.019 | `lib-stage2-lean-u1` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.35 ms | 0.026 | `lib-stage1` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.32 ms | 0.026 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.65 ms | 0.053 | `lib-stage2-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 29.4 ms | 0.024 | `lib-stage2-lean-u1` 0.023 | `mut-odo-vecdims` 0.024 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 30.1 ms | 0.023 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims` 0.023 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 32.8 ms | 0.048 | `lib-stage1` 0.040 | `mut-odo-vecdims-add-in-leaf-u2` 0.042 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 119 us | 0.064 | `lib-stage2-lean` 0.031 | `mut-odo-vecdims-add-in-leaf-u2` 0.041 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 455 us | 0.048 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 |
| `rev-primes` | `rev` | 89 | 250357 | 4.45 ms | 0.025 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `runs-65536` | `runs` | 65536 | 1769472 | 28.3 ms | 0.023 | `lib-stage1` 0.022 | `mut-odo-vecdims` 0.023 |
| `runs-16384` | `runs` | 16384 | 1785856 | 28.6 ms | 0.023 | `lib-stage1` 0.022 | `mut-odo-vecdims` 0.023 |
| `runs-4096` | `runs` | 4096 | 1798144 | 28.8 ms | 0.024 | `lib-stage1` 0.023 | `mut-odo-vecdims` 0.024 |
| `runs-1024` | `runs` | 1024 | 1799168 | 28.8 ms | 0.024 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.024 |
| `runs-512` | `runs` | 512 | 1799680 | 28.9 ms | 0.024 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims` 0.024 |
| `runs-256` | `runs` | 256 | 1799936 | 29.1 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims` 0.024 |
| `runs-7` | `runs` | 7 | 1799994 | 33.1 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `runs-2` | `runs` | 2 | 1800000 | 39.5 ms | 0.058 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-3` | `runs` | 3 | 1800000 | 36 ms | 0.047 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-32` | `runs` | 32 | 1800000 | 30.1 ms | 0.025 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 34.4 ms | 0.041 | `lib-stage2-lean-u1` 0.023 | `mut-odo-vecdims-add-in-leaf-u1` 0.023 |
| `runs-48` | `runs` | 48 | 1800000 | 29.7 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-5` | `runs` | 5 | 1800000 | 33.2 ms | 0.039 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.023 |
| `runs-64` | `runs` | 64 | 1800000 | 29.6 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `runs-9` | `runs` | 9 | 1800000 | 31.9 ms | 0.030 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `runs-96` | `runs` | 96 | 1800000 | 29.4 ms | 0.024 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims` 0.024 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 29.7 ms | 0.025 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 |
| `scaled-r5` | `scaled` | 13 | 15015 | 268 us | 0.029 | `lib-stage2-lean` 0.020 | `mut-odo-vecdims-add-in-leaf-u2` 0.021 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 1.04 ms | 0.023 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 5.24 ms | 0.028 | `lib-stage2-lean-u1` 0.029 | `mut-odo-vecdims` 0.028 |
| `small-patch-k5` | `small` | 5 | 150 | 2.98 us | 0.079 | `lib-stage2-lean` 0.098 | `mut-odo-vecdims-add-in-leaf-u2` 0.061 |
| `small-bcast32` | `small` | 32 | 256 | 4.44 us | 0.052 | `lib-stage1` 0.067 | `mut-odo-vecdims-add-in-leaf-u2` 0.045 |
| `small-flat64` | `small` | 64 | 256 | 4.44 us | 0.059 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims` 0.059 |
| `small-patch-r5` | `small` | 4 | 256 | 5.27 us | 0.089 | `lib-stage2-lean` 0.098 | `mut-odo-vecdims-add-in-leaf-u2` 0.069 |
| `small-row96` | `small` | 96 | 384 | 6.49 us | 0.042 | `lib-stage2-lean` 0.056 | `mut-odo-vecdims-add-in-leaf-u2` 0.042 |
| `window-28x28-k5` | `window` | 5 | 14400 | 281 us | 0.040 | `lib-stage1` 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 948 us | 0.088 | `lib-stage2-lean` 0.010 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.43 ms | 0.052 | `lib-stage2-lean` 0.029 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 9.69 ms | 0.051 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2` 0.028 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.81 ms | 0.051 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2` 0.028 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 11.6 ms | 0.052 | `lib-stage1` 0.029 | `mut-odo-vecdims-add-in-leaf-u2` 0.029 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 12.3 ms | 0.053 | `lib-stage1` 0.030 | `mut-odo-vecdims-add-in-leaf-u2` 0.030 |
| `window-128x128-k7` | `window` | 7 | 729316 | 13.9 ms | 0.031 | `lib-stage2-lean` 0.017 | `mut-odo-vecdims-add-in-leaf-u2` 0.017 |

**No row of the table is read over fewer shapes than the rest, which is a property of the shape set and not of any arm**: NONE of the thirty-two rows is a geomean over fewer shapes than the rest, as on Runs 32 to 35 and where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the FIFTEEN `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows, this one names `lib-stage2-lean` on EIGHT and `lib-stage1` on two --- `rev` and `scaled` --- where Run 36 named the lean fill on six, `lib-stage1` on one and the unrolling twin on `bcastmid`. The cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The properties the next run should test

**Each stride class carries the same three properties, now with Run 37's verdicts** over ten classes, the details beside each class's table. **Two held everywhere and the third broke the clause Run 36 broke, in the same eleven populations and at the same figures**, the pair's variable being the same one: the two `-O2` passes change what `list` and `bq-expand` allocate.

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** **Both clauses held in every one of the eleven populations on both halves**, for a second run running: here `mut-odo-vecdims` is ahead of `bq-expand` over 19 of 19 main-set shapes on each, closest at `stretch-pow2stride` at **0.9945** on the basis and **0.9810** on the control, which is the shape and the arm pair Run 36 read at 0.9970 and 0.9826 and Run 35 broke at 1.0030. So the cell that cost Run 35 the clause has now held twice, and by a wider margin on both halves the second time. The `worst` clause holds in every regime, roster, compiler and layout the README has run, this pair's flagged half included, so `mut-odo-vecdims` --- and this is a statement about THAT arm and not about the route the library ships, which the paragraph below reads separately --- was never slower than the `list` it replaced, on any shape of any population. The `bq-expand` clause folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and is read that way here for the tenth time.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm still breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `lib-stage1` is slower than `list` on `runs-2` on both halves, at **1.1166** on the basis and **1.3363** on the control, and on `runs-3` on the CONTROL half alone at **1.1314**; `--over-list` reads every other one of the 1120 timed non-control cells this run carries, over all eleven populations on both halves, at or under 1. **They are the same three cells Run 36 read and within two points of its figures on each** --- 1.0978, 1.3495 and 1.1348 there against 1.1166, 1.3363 and 1.1314 here --- **and it is still `list` moving and not `lib-stage1`**: on `runs-2` the fill's own net moves 1.0233 between the halves while `list` moves 1.2246, so the ratio parts by 20 points; and on `runs-3` the fill moves 0.9951 against `list`'s 1.2496, so a cell sitting at 0.9010 on the basis is carried past 1 by the denominator alone. So the one standing break of the wider statement is not merely still there, it is wider under the passes than without them, it sits on the two shortest runs of the class that exists to price short runs, and it has now reproduced a run later.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves, the seventh run running that this property is the one left entirely alone.** The `list` clause is closest at `small-flat64` on the control, **0.06524**, and every closest shape outside `small` sits under 0.053. The `bq-expand` clause is closest at `small-row96` on the CONTROL half, **1.00441**, then `scaled-rank1-m1` at 1.00003 on the basis and `stretch-tall-Mx2` at 1.00000 on both halves of the main set. **Three of those figures are Run 36's to the digit printed, on the same shape and the same half** --- 0.06524, 1.00441 and 1.00003 --- across a moved source and a rebooted box, which is what allocation being deterministic per call predicts and what no timed column here can show. **The two passes are still what put the closest one where it is**: `small-row96` reads 0.98216 on the basis and 1.00441 on the control, so the clause survives on the flagged half by less than half the margin, and it is the allocation change property 3 records that put it there.

3. **The allocation tiers survive and their ORDER is unbroken in all ten classes and on the main set, on both halves --- and their LEVEL clause BREAKS in every one of the eleven populations, as it did on Run 36, and on Run 31 before it, where registration (10) died on it.** The order clause is untouched: the mutable fills sit at the result vector, `bq-expand` between 1.00x and 3.86x it, `list` an order of magnitude above at 19.00x to 27.66x, on both halves and in every population, `small` outside the LEVEL clause by the ruling of 2026-09-07 as before. What breaks is the level: **the two passes change what `list` and `bq-expand` ALLOCATE, and this run reads that change at Run 36's own figures.** Run 31's flagged half read `bq-expand` at 2.11x and `list` at 23.45x on the main set and 1.00x to 2.75x and 19.00x to 26.55x across the classes; Run 36's flagged half read the same five from two of that level's passes on a different compiler, and so does this one --- three runs and two compilers at one set of multiples, with a reboot and a moved source between the last two. On the main set the fills read 1.00x on both halves while `bq-expand` reads **2.78x** on the basis and **2.11x** on the control and `list` **25.20x** and **23.45x** --- medians over the nineteen shapes, so they are not to be divided. **Read per cell, which is the reading that may be**: over those nineteen shapes the flagged half allocates **0.9342** of the basis on `list` and identically on both its A/A twins, and **0.8119** on `bq-expand` and identically on all three of its, while every other timed arm of the main set reads 1.0000 to within two tenths of a point. Over all eleven populations `list` runs from 0.9073 on `block` to 0.9342 on the main set and `bq-expand` from **0.7263** on `window` to 0.9705 on `block`. **ONE arm outside the two families moves, the same way and less far**: `liblist-stage2-sum`, a reducing consumer, allocates **0.9096** of the basis on `block`, 0.9101 on `runs`, 0.9688 on `flip`, 0.9884 on `window` and 0.9901 on `small`, and 1.0000 on the other six, the main set among them --- a geomean deviation of 2.18% over the eleven populations against `list`'s 7.89% and `bq-expand`'s 12.52%, so between a sixth and a third of what the families get, though the same size as theirs on the two populations where it moves most. Those three deviations are Run 36's to the hundredth of a point, and so is every per-population figure above but `runs`, which reads a ten-thousandth apart. `--alloc` puts 429 of the main set's 570 cells above 100 bytes a call inside 1e-4 between the halves, worst **3.33e-01** on `stretch-wide-2xM/bq-expand-nosum`, the 38 cells under that size being set aside as a property of fitting a near-zero allocation. Allocation is deterministic per call, so a level that moves is a code change and never a slot --- and this run reads the whole of that code change again, unchanged, across a source that moved and a box that rebooted, which is the determinism exercised rather than asserted.

`--pair` within a class JSON, the `needs` column's two class-method tiers and the equal weighting of shapes are [README's *Reading a run file*](../README.md#reading-a-run-file).


## The stride classes, run by run

**Run 37 (GHC HEAD `10.1.20260918` against itself, plain -O1 against -O1 with `-fspec-constr -fliberate-case`, dead-spot, exit span, -A32m, launched from disk) records every class twice**, one process per class per half, so each block below has a control-half twin and the cross-half line under it is derived from both. `list` moved between the halves by 25.95 points on `bcastmid` at narrowest and 36.02 on `bcast` at widest, so NONE of the ten classes sits inside the 0.7% that lets two columns be differenced and every cross-half reading below is an ordering of the pair's variable rather than a measurement of it --- as on Run 36, which read this same pair, and on Run 31, whose variable was the whole level. Over the ten classes the reader counts **160 arm-comparisons, 42 putting the basis faster and 118 slower**, with no degenerate arm excluded, at geomeans from **1.0589** on `block` to **1.1284** on `window` and extremes of `mut-odo-vecdims-add-in-leaf-u1` at **0.7053** on `bcast` and `bq-expand-aa-distant` at **1.5177** on `window`. **The low extreme is ONE arm in NINE of the ten classes**: `mut-odo-vecdims-add-in-leaf-u1` is the arm the basis runs fastest against the control in every class but `small`, from 0.7053 on `bcast` to 0.9863 on `window`, and on the main set it is the single arm outside the two families to break the registration's 3% band --- a reading [Results](#results) sets out and the open list now carries, since its counted work is level with its family and the pair's variable therefore does not explain it. Every `Across the halves` line below reads the basis over the control, ABOVE 1 meaning the control --- the FLAGGED half --- is the faster, as every cross figure in this file does. What each class still decides, and decides on both halves separately, is the three properties, its own floor, and whichever registrations name it. **No registration of this run names a class**: all seventeen of its spans are `on main basis`, so each block below carries its properties, its floor and its own cross-half reading, and the registration verdicts are the main set's alone.

First, one table over all of them, transcribed from each class's own table below, in the columns [README's *Reading a run file*](../README.md#reading-a-run-file) fixes, which also says what the blocks under it carry and what installs them.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- and both are read over the POPULATION, so the arm named here may lead on no single shape and the per-shape fingerprint below may name another, which is [the README's per-shape section][pershape]'s own point and not a disagreement --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the properties](#the-properties-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on SEVEN of the ten --- `rev` and `scaled`, where `lib-stage1` leads, and `bcast`, `bcastmid`, `window`, `runs` and `flip`, where `lib-stage2-lean` does --- and the family's own ceiling on the other THREE, `block`, `small` and `compose`, where the shipped leaf is faster. **The two pointer leaves held five of Run 30's six bolded ceilings and are parked**, so every bolded ceiling here is the shipped leaf `mut-odo-vecdims-add-in-leaf-u2` itself, which is the form the family would ship. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.042 | 0.064 | **`lib-stage1`** 0.021 | `mut-odo-vecdims-add-in-leaf-u2` 0.022 | 0.19% |
| `bcast` | 6 | 0.021 | 0.058 | **`lib-stage2-lean`** 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.016 | 0.80% |
| `bcastmid` | 4 | 0.029 | 0.053 | **`lib-stage2-lean`** 0.012 | `mut-odo-vecdims-add-in-leaf-u2` 0.020 | 1.21% |
| `window` | 8 | 0.051 | 0.088 | **`lib-stage2-lean`** 0.026 | `mut-odo-vecdims-add-in-leaf-u2` 0.027 | 0.75% |
| `scaled` | 3 | 0.026 | 0.029 | **`lib-stage1`** 0.022 | `mut-odo-vecdims-add-in-leaf-u1` 0.022 | 0.34% |
| `runs` | 17 | 0.026 | 0.058 | **`lib-stage2-lean`** 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.024 | 3.18% |
| `flip` | 6 | 0.028 | 0.053 | **`lib-stage2-lean`** 0.023 | `mut-odo-vecdims-add-in-leaf-u2` 0.026 | 0.96% |
| `block` | 5 | 0.023 | 0.030 | `lib-stage2-lean` 0.021 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.021 | 0.61% |
| `small` | 5 | 0.062 | 0.089 | `lib-stage2-lean` 0.056 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.055 | 1.64% |
| `compose` | 4 | 0.023 | 0.029 | `lib-stage2-lean` 0.015 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.014 | 0.83% |

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On THREE rows --- `block`, `small` and `compose` --- the bold sits in the CEILING column instead**, and it names ONE arm on all three, the shipped leaf `mut-odo-vecdims-add-in-leaf-u2`; against Run 36 the rows that changed sides are `scaled` and `runs`, both leaving that set for the arm outside the family, and `small`, which joins it. TWO rows change the arm they name rather than the side: `bcastmid` names `lib-stage2-lean` outside the family where Run 36's named `lib-stage2-lean-u1`, and `scaled`'s ceiling is `mut-odo-vecdims-add-in-leaf-u1` where Run 36's was the shipped leaf. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship --- an override no row here exercises, both of them having been parked on 2026-09-13. **THREE rows tie at three decimals** --- `scaled` at 0.022, `runs` at 0.024 and `block` at 0.021 --- and the bold on each is `--block`'s own, computed on the unrounded values where the printed ones cannot separate: outside the family against the ceiling, 0.022072 against 0.022399, 0.024035 against 0.024042 and 0.021366 against 0.021330, so `scaled` and `runs` fall to the arm outside the family and `block` to the ceiling. **`runs`'s bold is decided by 7e-6**, where `scaled` parts by 3.3e-4 and `block` by 3.6e-5, so on that row the bold marks a tie-break and not a lead.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.10* | *127* | *3.22x* |
| liblist-stage1-sum | -- | -- | 0.10 | 147 | 1.01x |
| liblist-stage2-sum | -- | -- | 0.06 | 147 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.06 | 147 | 1.01x |
| liblist-stage4-list-sum | -- | -- | 0.07 | 147 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.07 | 147 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.06 | 147 | 1.03x |
| libunord-stage12-sum | -- | -- | 0.03 | 157 | 0.01x |
| libunord-stage13-sum | -- | -- | 0.03 | 157 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.02 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.04 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.03 | 157 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.20* | *148* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *158* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.041* | *0.10* | *147* | *1.00x* |
| lib-stage1 | 0.021 | 0.043 | 0.08 | 147 | 1.01x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.041* | *0.07* | *147* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.041 | 0.06 | 147 | 1.00x |
| lib-stage2-lean | 0.022 | 0.031 | 0.09 | 147 | 1.01x |
| lib-stage2-lean-u1 | 0.026 | 0.033 | 0.13 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.041 | 0.08 | 146 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.042* | *0.064* | *0.11* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.042** | 0.064 | 0.10 | 138 | 1.00x |
| *mut-odo-vecdims-aa* | *0.042* | *0.064* | *0.11* | *138* | *1.00x* |
| *bq-expand-aa-distant* | *0.139* | *0.236* | *0.12* | *122* | *3.22x* |
| bq-expand | 0.139 | 0.236 | 0.15 | 122 | 3.22x |
| *bq-expand-aa-adjacent* | *0.139* | *0.236* | *0.11* | *122* | *3.22x* |
| *list-aa-adjacent* | *0.998* | *1.003* | *0.24* | *85* | *26.11x* |
| list (baseline) | 1.000 | 1.000 | 0.29 | 85 | 26.11x |
| *list-aa-distant* | *1.002* | *1.004* | *0.31* | *85* | *26.11x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 0.9981, worst cell 0.51% on `rev-cnn-L1-24x24-c1`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9998 on a worst cell of 0.07% on `rev-cnn-L1-24x24-c1`, its interval covering 1. The in-situ term reads 0.9996, 1.0140 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9981, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m23s, peak 96 MiB in use, 26 MiB max residency; the reader reads 32 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.6 ms per call raw, 4.45 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.064/0.048/0.025

**Across the halves:** 3 of the 16 arms are faster on this half and 13 slower, at a geomean of 1.0984, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9722 to `bq-expand` at 1.2897, with `list` itself at 1.2724. **The baseline moved 27.24% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.064, tiers at 1.00x, 3.22x, 26.11x --- and `lib-stage1` leads outside the family at 0.021, priced against `mut-odo-vecdims` at 0.6188 over 3 of 3 shapes at sign p 0.25, a margin of 38.12% against this class's 0.19% floor (`list-aa-adjacent`). **What this class shows of the pair is the tightest floor of the ten, 0.19%**, against which `lib-stage1`'s 38.12% lead outside the family is two hundred times the noise --- and `rev` is one of only two classes, with `scaled`, where that arm leads rather than `lib-stage2-lean`. Its two columns may NOT be differenced, `list` having moved 27.24 of a point, at a class geomean of 1.0984 over the 16 arms, with 5 of 8 strategies past an A/A bar of 0.63 points. The counted work reads a counts geomean of 1.1645 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 16.45 points in instructions against 9.84 in time.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09, for Run 28 --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and Run 28's registration (7) read no crossover on it; this run reads the class for stage twelve's tie with stage eleven, at registration (2).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.56* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.49 | 62 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.47 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.46 | 62 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.47 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.47 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.48 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.48 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 74 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| lib-stage2-lean | 0.015 | 0.020 | 0.49 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.015* | *0.020* | *0.51* | *62* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.015* | *0.020* | *0.39* | *62* | *1.00x* |
| lib-stage1 | 0.016 | 0.020 | 0.41 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.016 | 0.020 | 0.39 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.024 | 0.48 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.018 | 0.020 | 0.45 | 61 | 1.00x |
| *mut-odo-vecdims-aa* | *0.021* | *0.057* | *0.29* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.021** | 0.058 | 0.07 | 61 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.021* | *0.057* | *0.32* | *61* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.091* | *0.143* | *0.68* | *46* | *1.00x* |
| bq-expand | 0.091 | 0.143 | 0.67 | 46 | 1.00x |
| *bq-expand-aa-distant* | *0.092* | *0.144* | *0.10* | *46* | *1.00x* |
| list (baseline) | 1.000 | 1.000 | 1.09 | 17 | 20.99x |
| *list-aa-distant* | *1.003* | *1.009* | *0.88* | *17* | *20.99x* |
| *list-aa-adjacent* | *1.005* | *1.012* | *0.67* | *17* | *20.99x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0080, worst cell 1.11% on `bcast-src8`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.03% on `bcast-inner900`, its interval covering 1. The in-situ term reads 1.0178, 1.0094 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0059, which the correction amplifies by 1.36x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m42s, peak 169 MiB in use, 42 MiB max residency; the reader reads 32 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 30.7 ms per call raw, 29.6 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.029/0.019/0.058/0.015/0.019/0.019

**Across the halves:** 8 of the 16 arms are faster on this half and 8 slower, at a geomean of 1.0617, from `mut-odo-vecdims-add-in-leaf-u1` at 0.7053 to `list-aa-adjacent` at 1.3682, with `list` itself at 1.3602. **The baseline moved 36.02% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.058, tiers at 1.00x, 1.00x, 20.99x --- and `lib-stage2-lean` leads outside the family at 0.015, priced against `mut-odo-vecdims` at 0.6594 over 6 of 6 shapes at sign p 0.031, a margin of 34.06% against this class's 0.80% floor (`bq-expand-aa-distant`). **What this class shows of the pair is the widest `list` move of the ten**, 36.02 points, and the run's LOW extreme with it: `mut-odo-vecdims-add-in-leaf-u1` at 0.7053, which step 4a puts on the CONTROL half against Run 36 at 1.4164 with the counted work level on both. `bq-expand` also allocates at level with the fills here, 1.00x on both halves, where the passes cut its allocation on every other population. Its two columns may NOT be differenced, `list` having moved 36.02 of a point, at a class geomean of 1.0617 over the 16 arms, with 4 of 8 strategies past an A/A bar of 0.59 points. The counted work reads a counts geomean of 1.1686 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 16.86 points in instructions against 6.17 in time.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.43* | *66* | *1.92x* |
| liblist-stage1-sum | -- | -- | 0.41 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.32 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.33 | 82 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.34 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.37 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.42 | 82 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.01 | 96 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.30 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.29 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.35 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 96 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.37* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *88* | *0.00x* |
| lib-stage2-lean | 0.012 | 0.017 | 0.31 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.012 | 0.018 | 0.40 | 82 | 1.00x |
| lib-stage1 | 0.012 | 0.017 | 0.34 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.030 | 0.33 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.020* | *0.030* | *0.36* | *80* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.020* | *0.030* | *0.28* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.023 | 0.031 | 0.37 | 78 | 1.00x |
| *mut-odo-vecdims-aa* | *0.029* | *0.053* | *0.29* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.029* | *0.053* | *0.28* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.029** | 0.053 | 0.21 | 76 | 1.00x |
| *bq-expand-aa-adjacent* | *0.099* | *0.184* | *0.41* | *60* | *1.92x* |
| bq-expand | 0.099 | 0.184 | 0.44 | 60 | 1.92x |
| *bq-expand-aa-distant* | *0.100* | *0.185* | *0.29* | *60* | *1.92x* |
| list (baseline) | 1.000 | 1.000 | 0.73 | 28 | 23.56x |
| *list-aa-adjacent* | *1.004* | *1.009* | *0.94* | *28* | *23.56x* |
| *list-aa-distant* | *1.005* | *1.011* | *0.85* | *28* | *23.56x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0121, worst cell 4.20% on `bcastmid-b200k`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0013 on a worst cell of 0.52% on `bcastmid-c32-cnn`, its interval covering 1. The in-situ term reads 1.0164, 1.0654 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0097, which the correction amplifies by 1.29x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h11m9s, peak 135 MiB in use, 35 MiB max residency; the reader reads 32 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 48.7 ms per call raw, 47.6 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.053/0.019/0.035/0.022

**Across the halves:** 4 of the 16 arms are faster on this half and 12 slower, at a geomean of 1.0902, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9686 to `bq-expand-aa-distant` at 1.2687, with `list` itself at 1.2595. **The baseline moved 25.95% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.053, tiers at 1.00x, 1.92x, 23.56x --- and `lib-stage2-lean` leads outside the family at 0.012, priced against `mut-odo-vecdims` at 0.4097 over 4 of 4 shapes at sign p 0.12, a margin of 59.03% against this class's 1.21% floor (`bq-expand-aa-distant`). **What this class shows of the pair is the NARROWEST `list` move of the ten**, 25.95 points, which is still thirty-seven times the 0.7% bar --- and the widest lead outside the family anywhere in the run, `lib-stage2-lean` at 0.012 against `mut-odo-vecdims`'s 0.029. Its two columns may NOT be differenced, `list` having moved 25.95 of a point, at a class geomean of 1.0902 over the 16 arms, with 4 of 8 strategies past an A/A bar of 1.18 points. The counted work reads a counts geomean of 1.1677 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 16.77 points in instructions against 9.02 in time.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09, for Run 28, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.33* | *61* | *3.86x* |
| liblist-stage1-sum | -- | -- | 0.16 | 83 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.17 | 83 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.16 | 83 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.16 | 83 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.16 | 83 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.17 | 83 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.05 | 102 | 0.03x |
| libunord-stage13-sum | -- | -- | 0.06 | 102 | 0.03x |
| libunord-stage6-loop-sum | -- | -- | 1.40 | 90 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.07 | 100 | 0.03x |
| libunord-stage7-sum | -- | -- | 0.04 | 102 | 0.03x |
| libunord-stage9-sum | -- | -- | 0.07 | 100 | 0.03x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *97* | *0.00x* |
| lib-stage2-lean | 0.026 | 0.030 | 0.17 | 83 | 1.00x |
| lib-stage1 | 0.027 | 0.030 | 0.17 | 83 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.030 | 0.16 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.030* | *0.17* | *82* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.030* | *0.18* | *82* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.029 | 0.032 | 0.19 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.029 | 0.032 | 0.30 | 82 | 1.00x |
| *mut-odo-vecdims-aa* | *0.051* | *0.087* | *0.18* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.051** | 0.088 | 0.15 | 76 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.088* | *0.16* | *76* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.176* | *0.218* | *0.27* | *57* | *3.86x* |
| bq-expand | 0.176 | 0.218 | 0.33 | 57 | 3.86x |
| *bq-expand-aa-distant* | *0.177* | *0.216* | *0.27* | *57* | *3.86x* |
| *list-aa-distant* | *0.998* | *1.006* | *0.51* | *30* | *27.66x* |
| list (baseline) | 1.000 | 1.000 | 0.49 | 30 | 27.66x |
| *list-aa-adjacent* | *1.001* | *1.011* | *0.41* | *30* | *27.66x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 1.0075, worst cell 6.07% on `window-224x224-k3`, and 8 of 8 intervals cover 1. The `sum-only` halves agree at 0.9993 on a worst cell of 0.52% on `window-128x128-k7`, its interval covering 1. The in-situ term reads 1.0046, 1.1634 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0051, which the correction amplifies by 1.55x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h22m9s, peak 123 MiB in use, 49 MiB max residency; the reader reads 32 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 14.4 ms per call raw, 13.9 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.040/0.051/0.088/0.031/0.052/0.051/0.053/0.052

**Across the halves:** 8 of the 16 arms are faster on this half and 8 slower, at a geomean of 1.1284, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9863 to `bq-expand-aa-distant` at 1.5177, with `list` itself at 1.2738. **The baseline moved 27.38% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.088, tiers at 1.00x, 3.86x, 27.66x --- and `lib-stage2-lean` leads outside the family at 0.026, priced against `mut-odo-vecdims` at 0.4581 over 8 of 8 shapes at sign p 0.0078, a margin of 54.19% against this class's 0.75% floor (`mut-odo-vecdims-aa-distant`). **What this class shows of the pair is the widest reading of the ten on every scale at once**: the widest class geomean, 1.1284, the widest counted work, 1.1765, the run's HIGH extreme in `bq-expand-aa-distant` at 1.5177, and the widest allocation tiers, `bq-expand` at 3.86x the result vector on the basis and `list` at 27.66x. Its two columns may NOT be differenced, `list` having moved 27.38 of a point, at a class geomean of 1.1284 over the 16 arms, with 3 of 8 strategies past an A/A bar of 0.91 points. The counted work reads a counts geomean of 1.1765 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 17.65 points in instructions against 12.84 in time.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.07* | *118* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.12 | 128 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.11 | 128 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.11 | 128 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.18 | 128 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.17 | 128 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.14 | 127 | 1.01x |
| libunord-stage12-sum | -- | -- | 0.25 | 128 | 1.00x |
| libunord-stage13-sum | -- | -- | 0.14 | 128 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.14 | 128 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.11 | 127 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.13 | 127 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.11 | 128 | 1.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.17* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *138* | *0.00x* |
| lib-stage1 | 0.022 | 0.030 | 0.13 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.030* | *0.15* | *128* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.029 | 0.11 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.023 | 0.030 | 0.10 | 128 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.023* | *0.030* | *0.18* | *127* | *1.00x* |
| lib-stage2-lean | 0.024 | 0.030 | 0.16 | 127 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.029 | 0.13 | 127 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.026* | *0.029* | *0.11* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.026** | 0.029 | 0.09 | 127 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.029* | *0.11* | *127* | *1.00x* |
| bq-expand | 0.090 | 0.101 | 0.07 | 111 | 1.21x |
| *bq-expand-aa-adjacent* | *0.090* | *0.101* | *0.08* | *111* | *1.21x* |
| *bq-expand-aa-distant* | *0.090* | *0.101* | *0.08* | *111* | *1.21x* |
| list (baseline) | 1.000 | 1.000 | 0.28 | 69 | 21.49x |
| *list-aa-distant* | *1.001* | *1.003* | *0.19* | *69* | *21.49x* |
| *list-aa-adjacent* | *1.003* | *1.004* | *0.24* | *69* | *21.49x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9966, worst cell 0.71% on `scaled-rank1-m1`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0002 on a worst cell of 0.04% on `scaled-super-r3`, its interval covering 1. The in-situ term reads 1.0202, 1.0064 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9985, which the correction amplifies by 2.42x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h8m24s, peak 111 MiB in use, 36 MiB max residency; the reader reads 32 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 5.42 ms per call raw, 5.24 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.023/0.028/0.029

**Across the halves:** 1 of the 16 arms are faster on this half and 15 slower, at a geomean of 1.0704, from `mut-odo-vecdims-add-in-leaf-u1` at 0.9437 to `list` at 1.3298, with `list` itself at 1.3298. **The baseline moved 32.98% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.029, tiers at 1.00x, 1.21x, 21.49x --- and `lib-stage1` leads outside the family at 0.022, priced against `mut-odo-vecdims` at 0.9261 over 2 of 3 shapes at sign p 1, a margin of 7.39% against this class's 0.34% floor (`mut-odo-vecdims-add-in-leaf-u2-aa-distant`). **What this class shows of the pair is the only ceiling in the run that is not the shipped leaf**: `mut-odo-vecdims-add-in-leaf-u1` leads its own family here at 0.022, and that is the arm whose cross-half figure the main set reads at 0.9630 and the open list disputes, so this class's ceiling is carried by the one arm under question. Its two columns may NOT be differenced, `list` having moved 32.98 of a point, at a class geomean of 1.0704 over the 16 arms, with 4 of 8 strategies past an A/A bar of 0.77 points. The counted work reads a counts geomean of 1.1515 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 15.15 points in instructions against 7.04 in time.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven, on Run 24 to fourteen and on Run 34 to seventeen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-32` (`l` 1800000, `sInner` 32), `runs-48` (`l` 1800000, `sInner` 48) and `runs-64` (`l` 1800000, `sInner` 64) --- the three landed on Run 34, inside the gap from 9 to 96 where a fit to Run 33's stage-eleven curve had put a minimum --- `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.34* | *52* | *1.08x* |
| liblist-stage1-sum | -- | -- | 0.10 | 61 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.07 | 69 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.02 | 72 | 0.00x |
| liblist-stage4-list-sum | -- | -- | 0.02 | 72 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.12 | 60 | 0.42x |
| libunord-stage12-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.07 | 69 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 72 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 72 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.12* | *78* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.026* | *0.44* | *59* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.025* | *0.10* | *59* | *1.00x* |
| lib-stage2-lean | 0.024 | 0.025 | 0.13 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.025 | 0.11 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.026 | 0.11 | 59 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.026 | 0.12 | 59 | 1.00x |
| **mut-odo-vecdims** | **0.026** | 0.058 | 0.09 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.026* | *0.058* | *0.10* | *59* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.026* | *0.058* | *0.09* | *59* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.092* | *0.143* | *0.40* | *46* | *1.08x* |
| bq-expand | 0.092 | 0.143 | 0.38 | 46 | 1.08x |
| *bq-expand-aa-distant* | *0.093* | *0.144* | *0.04* | *46* | *1.08x* |
| lib-stage1 | 0.098 | 1.117 | 0.16 | 51 | 1.42x |
| list (baseline) | 1.000 | 1.000 | 2.43 | 17 | 21.30x |
| *list-aa-distant* | *1.029* | *1.043* | *0.35* | *17* | *21.30x* |
| *list-aa-adjacent* | *1.032* | *1.043* | *0.24* | *17* | *21.30x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0318, worst cell 4.31% on `runs-r3-48x30`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 0.9996 on a worst cell of 0.59% on `runs-7`, its interval covering 1. The in-situ term reads 1.0291, 1.0252 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0307, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h47m12s, peak 660 MiB in use, 289 MiB max residency; the reader reads 32 benchmarks over 17 shapes of the runs class. Anchor: `runs-2`, `list` at 40.6 ms per call raw, 39.5 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-32, runs-48, runs-64, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.058/0.047/0.041/0.039/0.032/0.030/0.025/0.024/0.024/0.024/0.024/0.024/0.024/0.024/0.023/0.023/0.025

**Across the halves:** 9 of the 16 arms are faster on this half and 7 slower, at a geomean of 1.0619, from `mut-odo-vecdims-add-in-leaf-u1` at 0.7777 to `list` at 1.3283, with `list` itself at 1.3283. **The baseline moved 32.83% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.058, tiers at 1.00x, 1.08x, 21.30x --- and `lib-stage2-lean` leads outside the family at 0.024, priced against `mut-odo-vecdims` at 0.8308 over 9 of 17 shapes at sign p 1, a margin of 16.92% against this class's 3.18% floor (`list-aa-adjacent`). **What this class shows of the pair is the one standing break of the wider claim, and the passes widen it**: `lib-stage1` is slower than `list` on `runs-2` on both halves, 1.1166 on the basis and 1.3363 on the control, and on `runs-3` on the control alone at 1.1314 --- the only three of the run's 1120 timed non-control cells above 1. Its floor, 3.18%, is the widest of the ten besides. Its two columns may NOT be differenced, `list` having moved 32.83 of a point, at a class geomean of 1.0619 over the 16 arms, with 8 of 8 strategies past an A/A bar of 0.64 points. The counted work reads a counts geomean of 1.1614 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 16.14 points in instructions against 6.19 in time.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes: in the order they run, `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which landed 2026-09-09 and is `runs-96`'s construction under a `flip` name --- the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two; `flip-whole-square` (`l` 1798281, `sInner` 1341); `flip-last-c32` (`l` 165888, `sInner` 3); `flip-last-rows` (`l` 1800000, `sInner` 96); and the two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed, `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. The control sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.42* | *66* | *1.05x* |
| liblist-stage1-sum | -- | -- | 0.20 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.11 | 88 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.12 | 92 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.09 | 92 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.11 | 92 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.13 | 82 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.01 | 98 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 98 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 98 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.14* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *93* | *0.00x* |
| lib-stage2-lean | 0.023 | 0.040 | 0.24 | 84 | 1.00x |
| lib-stage2-lean-u1 | 0.023 | 0.047 | 0.26 | 83 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.026* | *0.043* | *0.33* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.042 | 0.09 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.026* | *0.043* | *0.11* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.027 | 0.046 | 0.21 | 80 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.028* | *0.053* | *0.09* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.028** | 0.053 | 0.08 | 77 | 1.00x |
| *mut-odo-vecdims-aa* | *0.028* | *0.053* | *0.09* | *77* | *1.00x* |
| lib-stage1 | 0.034 | 0.048 | 0.36 | 80 | 1.00x |
| *bq-expand-aa-adjacent* | *0.090* | *0.183* | *0.42* | *61* | *1.05x* |
| bq-expand | 0.090 | 0.183 | 0.43 | 61 | 1.05x |
| *bq-expand-aa-distant* | *0.092* | *0.184* | *0.10* | *61* | *1.05x* |
| list (baseline) | 1.000 | 1.000 | 0.63 | 32 | 21.18x |
| *list-aa-distant* | *1.005* | *1.023* | *0.59* | *32* | *21.18x* |
| *list-aa-adjacent* | *1.008* | *1.029* | *0.35* | *32* | *21.18x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0096, worst cell 2.90% on `flip-whole-square`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `flip-whole-square`, its interval covering 1. The in-situ term reads 1.0217, 1.0228 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0093, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h16m42s, peak 209 MiB in use, 76 MiB max residency; the reader reads 32 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 31.2 ms per call raw, 30.1 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.023/0.024/0.053/0.048/0.026/0.026

**Across the halves:** 4 of the 16 arms are faster on this half and 12 slower, at a geomean of 1.0682, from `mut-odo-vecdims-add-in-leaf-u1` at 0.8984 to `list-aa-adjacent` at 1.3153, with `list` itself at 1.3055. **The baseline moved 30.55% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.053, tiers at 1.00x, 1.05x, 21.18x --- and `lib-stage2-lean` leads outside the family at 0.023, priced against `mut-odo-vecdims` at 0.7541 over 5 of 6 shapes at sign p 0.22, a margin of 24.59% against this class's 0.96% floor (`list-aa-adjacent`). **What this class shows of the pair is its known trouble cell, quieter than before and still the class's worst**: `flip-last-rows`, which [the open list](../README.md#what-is-open) records parting from its own A/A copies by 22% in one process, is the control half's worst A/A cell here at 6.19%, where the basis's worst is 2.90% on `flip-whole-square`. Its two columns may NOT be differenced, `list` having moved 30.55 of a point, at a class geomean of 1.0682 over the 16 arms, with 4 of 8 strategies past an A/A bar of 1.28 points. The counted work reads a counts geomean of 1.1513 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 15.13 points in instructions against 6.82 in time.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.12* | *103* | *1.06x* |
| liblist-stage1-sum | -- | -- | 0.11 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.15 | 122 | 0.34x |
| liblist-stage3-sum | -- | -- | 0.02 | 129 | 0.00x |
| liblist-stage4-list-sum | -- | -- | 0.04 | 129 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.08 | 113 | 0.42x |
| libunord-stage12-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.05 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.04 | 129 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.03 | 129 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 129 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *129* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *122* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.021* | *0.024* | *0.13* | *111* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.021* | *0.025* | *0.11* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.021 | 0.025 | 0.10 | 111 | 1.00x |
| lib-stage2-lean | 0.021 | 0.025 | 0.23 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.022 | 0.026 | 0.11 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.022 | 0.026 | 0.13 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.023* | *0.030* | *0.08* | *111* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.030* | *0.10* | *111* | *1.00x* |
| **mut-odo-vecdims** | **0.023** | 0.030 | 0.09 | 111 | 1.00x |
| lib-stage1 | 0.050 | 0.058 | 0.14 | 104 | 1.42x |
| bq-expand | 0.086 | 0.087 | 0.13 | 96 | 1.06x |
| *bq-expand-aa-adjacent* | *0.086* | *0.088* | *0.13* | *96* | *1.06x* |
| *bq-expand-aa-distant* | *0.086* | *0.087* | *0.10* | *96* | *1.06x* |
| *list-aa-adjacent* | *1.000* | *1.003* | *0.16* | *54* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 0.25 | 54 | 21.22x |
| *list-aa-distant* | *1.001* | *1.002* | *0.24* | *54* | *21.22x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0061, worst cell 2.49% on `block-run64-page`, and 8 of 8 intervals cover 1. The `sum-only` halves agree at 0.9994 on a worst cell of 0.52% on `block-run64-gap1`, its interval covering 1. The in-situ term reads 1.0205, 1.0116 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0044, which the correction amplifies by 1.40x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h13m53s, peak 133 MiB in use, 48 MiB max residency; the reader reads 32 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.62 ms per call raw, 4.46 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.019/0.024/0.030/0.024/0.019

**Across the halves:** 1 of the 16 arms are faster on this half and 15 slower, at a geomean of 1.0589, from `mut-odo-vecdims-add-in-leaf-u1` at 0.8766 to `list-aa-adjacent` at 1.3186, with `list` itself at 1.3103. **The baseline moved 31.03% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.030, tiers at 1.00x, 1.06x, 21.22x --- and `lib-stage2-lean` leads outside the family at 0.021, priced against `mut-odo-vecdims` at 0.9251 over 5 of 5 shapes at sign p 0.062, a margin of 7.49% against this class's 0.61% floor (`bq-expand-aa-distant`). **What this class shows of the pair is the narrowest class geomean of the ten**, 1.0589, and the one tie the cross-class summary resolves to the CEILING: `lib-stage2-lean` and the shipped leaf both print 0.021, and at full precision, 0.021366 against 0.021330, the leaf leads by three hundred-thousandths. Its two columns may NOT be differenced, `list` having moved 31.03 of a point, at a class geomean of 1.0589 over the 16 arms, with 7 of 8 strategies past an A/A bar of 0.63 points. The counted work reads a counts geomean of 1.1484 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 14.84 points in instructions against 5.89 in time.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.19* | *222* | *1.44x* |
| liblist-stage1-sum | -- | -- | 0.20 | 227 | 1.65x |
| liblist-stage2-sum | -- | -- | 0.19 | 230 | 1.55x |
| liblist-stage3-sum | -- | -- | 0.24 | 227 | 1.69x |
| liblist-stage4-list-sum | -- | -- | 0.21 | 230 | 1.51x |
| liblist-stage4-sum | -- | -- | 0.20 | 230 | 1.51x |
| libunord-stage1-sum | -- | -- | 0.15 | 222 | 2.08x |
| libunord-stage12-sum | -- | -- | 0.24 | 235 | 0.55x |
| libunord-stage13-sum | -- | -- | 0.25 | 238 | 0.49x |
| libunord-stage6-loop-sum | -- | -- | 0.30 | 235 | 0.81x |
| libunord-stage6-sum | -- | -- | 0.26 | 234 | 0.83x |
| libunord-stage7-sum | -- | -- | 0.26 | 234 | 0.83x |
| libunord-stage9-sum | -- | -- | 0.29 | 233 | 0.55x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *238* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.05* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.05* | *250* | *0.01x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.054* | *0.069* | *0.40* | *229* | *1.28x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.054* | *0.069* | *0.34* | *229* | *1.28x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.055 | 0.069 | 0.42 | 229 | 1.28x |
| lib-stage2-lean | 0.056 | 0.098 | 0.20 | 227 | 1.46x |
| lib-stage2-lean-u1 | 0.057 | 0.100 | 0.26 | 227 | 1.46x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.057 | 0.070 | 0.43 | 227 | 1.28x |
| **mut-odo-vecdims** | **0.062** | 0.089 | 0.33 | 229 | 1.27x |
| *mut-odo-vecdims-aa-distant* | *0.062* | *0.089* | *0.39* | *229* | *1.27x* |
| *mut-odo-vecdims-aa* | *0.063* | *0.089* | *0.26* | *228* | *1.27x* |
| lib-stage1 | 0.092 | 0.109 | 0.26 | 221 | 2.33x |
| bq-expand | 0.137 | 0.199 | 0.15 | 217 | 1.44x |
| *bq-expand-aa-distant* | *0.137* | *0.200* | *0.26* | *217* | *1.44x* |
| *bq-expand-aa-adjacent* | *0.138* | *0.200* | *0.16* | *217* | *1.44x* |
| *list-aa-distant* | *0.999* | *1.001* | *0.16* | *180* | *21.57x* |
| list (baseline) | 1.000 | 1.000 | 0.17 | 180 | 21.57x |
| *list-aa-adjacent* | *1.000* | *1.001* | *0.18* | *180* | *21.57x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 1.0164, worst cell 6.58% on `small-flat64`, and 4 of 8 intervals cover 1. The `sum-only` halves agree at 1.0012 on a worst cell of 0.47% on `small-patch-k5`, its interval missing 1. The in-situ term reads 0.9746, 0.9882 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0105, which the correction amplifies by 1.52x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h14m0s, peak 130 MiB in use, 51 MiB max residency; the reader reads 32 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.71 us per call raw, 6.49 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.042/0.079/0.052/0.059/0.089

**Across the halves:** 0 of the 16 arms are faster on this half and 16 slower, at a geomean of 1.0906, from `lib-stage2-lean-u1` at 1.0033 to `list-aa-adjacent` at 1.2855, with `list` itself at 1.2663. **The baseline moved 26.63% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.089, tiers at 1.27x, 1.44x, 21.57x --- and `lib-stage2-lean` leads outside the family at 0.056, priced against `mut-odo-vecdims` at 0.9039 over 1 of 5 shapes at sign p 0.38, a margin of 9.61% against this class's 1.64% floor (`mut-odo-vecdims-aa`). **What this class shows of the pair is the only population where EVERY arm is slower on the basis**, 0 of 16 faster, and the only one where the mutable fills do not sit at the result vector: `mut-odo-vecdims` allocates 1.27x it on both halves, which is why the ruling of 2026-09-07 puts `small` outside property 3's LEVEL clause. Its counted work is the narrowest of the ten besides, 1.1435. Its two columns may NOT be differenced, `list` having moved 26.63 of a point, at a class geomean of 1.0906 over the 16 arms, with 7 of 8 strategies past an A/A bar of 1.51 points. The counted work reads a counts geomean of 1.1435 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 14.35 points in instructions against 9.06 in time.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.30* | *85* | *1.35x* |
| liblist-stage1-sum | -- | -- | 0.29 | 98 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.31 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.29 | 98 | 1.00x |
| liblist-stage4-list-sum | -- | -- | 0.30 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.29 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.29 | 98 | 1.00x |
| libunord-stage12-sum | -- | -- | 0.01 | 110 | 0.00x |
| libunord-stage13-sum | -- | -- | 0.01 | 110 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.30 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 110 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.26* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *105* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.014* | *0.016* | *0.25* | *98* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.014* | *0.016* | *0.29* | *98* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.014 | 0.016 | 0.28 | 98 | 1.00x |
| lib-stage2-lean | 0.015 | 0.017 | 0.29 | 98 | 1.00x |
| lib-stage1 | 0.015 | 0.019 | 0.29 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.016 | 0.017 | 0.26 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.017 | 0.018 | 0.29 | 96 | 1.00x |
| *mut-odo-vecdims-aa* | *0.023* | *0.029* | *0.24* | *94* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.023* | *0.029* | *0.23* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.023** | 0.029 | 0.20 | 94 | 1.00x |
| *bq-expand-aa-adjacent* | *0.093* | *0.101* | *0.36* | *79* | *1.35x* |
| bq-expand | 0.094 | 0.101 | 0.37 | 79 | 1.35x |
| *bq-expand-aa-distant* | *0.094* | *0.102* | *0.22* | *79* | *1.35x* |
| list (baseline) | 1.000 | 1.000 | 0.68 | 44 | 22.01x |
| *list-aa-distant* | *1.001* | *1.019* | *0.70* | *44* | *22.01x* |
| *list-aa-adjacent* | *1.002* | *1.015* | *0.71* | *44* | *22.01x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9917, worst cell 2.80% on `compose-scalar`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 0.9992 on a worst cell of 0.38% on `compose-slice-bcast`, its interval covering 1. The in-situ term reads 1.0105, 1.0180 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9971, which the correction amplifies by 2.46x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h11m13s, peak 126 MiB in use, 33 MiB max residency; the reader reads 32 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 30.9 ms per call raw, 29.8 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.029/0.029/0.019/0.019

**Across the halves:** 4 of the 16 arms are faster on this half and 12 slower, at a geomean of 1.0605, from `mut-odo-vecdims-add-in-leaf-u1` at 0.7244 to `list-aa-distant` at 1.3557, with `list` itself at 1.3464. **The baseline moved 34.64% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** property 1 HOLDS and property 2 HOLDS --- `worst` 0.029, tiers at 1.00x, 1.35x, 22.01x --- and `lib-stage2-lean` leads outside the family at 0.015, priced against `mut-odo-vecdims` at 0.6252 over 4 of 4 shapes at sign p 0.12, a margin of 37.48% against this class's 0.83% floor (`mut-odo-vecdims-aa`). **What this class shows of the pair is the fastest ceiling in the run**, the shipped leaf at 0.014, and the second of the two places where `mut-odo-vecdims-add-in-leaf-u1` falls furthest across the halves, 0.7244 after `bcast`'s 0.7053 --- both of which step 4a puts on the control half. Its two columns may NOT be differenced, `list` having moved 34.64 of a point, at a class geomean of 1.0605 over the 16 arms, with 4 of 8 strategies past an A/A bar of 0.89 points. The counted work reads a counts geomean of 1.1626 over the same arms, 16 of them counted, and moves further than the clock as it does in all ten: 16.26 points in instructions against 6.05 in time.


## Provenance

**Run 37's halves differ in TWO GHC FLAGS and in nothing else.** One source, `Main.hs` at `05cfe93`; one shim, `align-as.py` at `f31bd1c`, unmoved since Run 33's build; one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1 LOOP_EXITSPAN=1` in front of the assembler shim; ONE compiler, the in-tree stage1 `10.1.20260918` reached through `cabal.project.ghead`, which is Run 36's compiler unmoved; one roster, one shape set, one class list and one bench order; both halves built with `-fobject-determinism`, both launched FROM DISK, `hugebin/` having been suspended on 2026-09-19, and both run under `WILDLOG=1 SATURATE=1`. The two command lines differ in `-fspec-constr -fliberate-case` on one of them, which micro.cabal's own `-O1` makes two of `-O2`'s passes on top of plain -O1 rather than a level. The basis is `run37-gheadnospec` and is what every table here publishes; `run37-gheadtwopass` is the candidate. **What is new against Run 36 is not the pair but its inputs**: the source moved three of the owner's commits, `cabal.project.ghead` was rewritten to the minimum that compiles on a current index, and the box was rebooted twice --- so this is Run 36's pair in its variable, read again under three changed terms.

**The roster is 32 timed arms over 19 main-set shapes and 608 benches, with 61 class views over ten classes for 1952 more, and it is Run 36's LESS THREE ARMS.** `./roster-delta.py run36-gheadnospec run37-gheadnospec`, read off the two binaries, puts NONE in and THREE out --- `libunord-stage10-list-sum`, `libunord-stage10-sum` and `libunord-stage11-sum`, which the owner's commit `2973582` moved to `Only` --- with 32 survivors in the same order, the nineteen main-set shapes unmoved and the sixty-one class views unmoved over the same ten classes. So pre-run step 12's condition --- a membership change --- DOES fire on this run, and its -L1 roster pass was taken before the evening; every cross-run figure in this file is read over the 32 survivors.

**The sequence ran in ONE window and nothing else was on the machine.** The wall-clock log puts the twenty-two processes between 2026-09-20T02:44:57 and 2026-09-20T10:09:25, the largest hole between one process finishing and the next starting 0m, every process reporting rc=0 and the bench count asked of it --- 20 class processes, one per class per half, and two main-set ones. The gate's four processes ran 02:11 to 02:45 before it and the four alone-leg riders 10:09 to 10:22 after it, and the counted work, which wants no quiet machine, from 10:22. **The intrusion verdict is clean over the whole of that**: `--wild` over the 119 logs this run wrote finds no bench at 0.25 foreign in the 114 that carry samples, the other five being the four rider drivers and the wall-clock log, which carry none. **One thing did interrupt this run, and it interrupted nothing this file publishes**: the evening was first launched at 01:59, the box became busy while the gate's second process ran, and the whole evening was stopped and relaunched at 02:11 on a box reading 0.6% busy. Every figure here is the second launch's; the first launch's four gate artifacts are parked under `probe-killed-` names, its truncated JSON among them.

**The gate read SOUND and the machine check did not fire.** The two palindrome passes agree: `mut-odo-vecdims` reads 1.0102 and 1.0052, `bq-expand` 1.3018 and 1.3023, and `list` 1.2957 and 1.3042 --- 0.50, 0.05 and 0.85 points apart --- with the two `sum-only` controls, on raw `slope`, at 0.9995 against 0.9997 and 0.9996 against 0.9999. **What the passes part by is each half's own leg-to-leg movement and not a disagreement about the pair**: the control's `a` leg over its `b` reads `list` at 1.0045 where the basis's two legs read 0.9979, which predicts the second pass at 1.0066 times the first against an observed 1.0066, and the same arithmetic lands two and three thousandths of a point out on `bq-expand` and `mut-odo-vecdims`. The machine check, read against the fingerprint Run 36 installed --- this run's own basis recipe, its compiler, shim, switches, level and launch to the commit --- puts `list`'s net at -2.24%, worst `stretch-coprime-r7` at -28.15%, 1 of 19 shapes past 5% and the geomean inside the 3% bar. **That worst shape is Run 36's wild cell seen from the other side**: the kept fingerprint carries that cell and this run does not repeat it, which is why the one shape past 5% is past it downward and why the geomean sits 2 points below the fingerprint rather than on it.

**Every one of the twenty-two processes gated clean, the plateau refused by declaration, and SIX A/A worst cells sit past 5%.** `read-all.sh` gates each process on its own correction and passes all twenty-two. The plateau band refuses, as the pair note declared before the run that it would: the victim runs 16.7931 to 21.3589 ms/iter across the run, a 27.19% spread against a 5% band --- but it splits exactly by half, the basis's eleven processes flat within 2.24% at 20.8913 to 21.3589 and the control's eleven within 3.51% at 16.7931 to 17.3828, which is the variable working and not the box moving. The six A/A worst cells past 5% are `gheadtwopass-runs` at 6.62%, `gheadnospec-small` at 6.58%, `gheadtwopass-flip` at 6.19%, `gheadtwopass-window` at 6.09%, `gheadnospec-window` at 6.07% and `gheadtwopass-main` at 5.79%; each sits on a population whose floor this file publishes, and none is the 28.36% outlier Run 36 carried on its own main set.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run37-gheadnospec`, md5 `715598f39aeeebbc2b2efcab71fd0241`, and `run37-gheadtwopass`, md5 `dd6d792006a019e55abb7f76a807063d`, built back to back in one call on 2026-09-19 against a clean tree. Their `.text` sections are **20207423** and **20215615** bytes, the first column of `size -A`, and both load at 4214784, which is Run 36's load address unchanged; both are smaller than Run 36's two by 172032 and 167936 bytes, which is where the three retired arms went. **NEITHER md5 reproduces anything, and the compiler is not why this time**: the compiler and the shim are Run 36's to the commit, and the source moved three commits with `cabal.project.ghead` rewritten under it, either of which is enough. What the two md5s do instead is DIFFER, which is the two passes having reached the emission. **And the flagged half is again the LARGER binary carrying the FEWER self-loops**, 312 against 377, by 8192 bytes --- two pages exactly, a fifth exact page multiple in that series and a fifth time the larger half holds the fewer loops, so this pair confirms [the open list's entry on it](../README.md#what-is-open) rather than adding to its puzzle.

**The compiler stood still and the source moved, which is the exact reverse of Run 36's provenance and leaves the pinning claim's strong form untested again.** The compiler is `10.1.20260918` on both builds and on both of Run 36's, so no figure here carries a compiler term; what is new under the recipe is `Main.hs` from `0eda736` to `05cfe93` and a rewritten `cabal.project.ghead`. `./loop-offsets.py --delta run36-gheadnospec run37-gheadnospec` reads EVERY mod-64 offset preserved on both tracked groups, [18, 0, 0, 0, 9, 2] and [0, 0], with no address surviving to the byte, three displacements on the six-copy group and two on the other, and a third group's copy count moving 1 to 2 --- so a moved source displaced every copy and no head's offset, exactly as a moved compiler did on Run 36. Within the pair the two passes do move them: the six-copy group reads [18, 0, 0, 0, 9, 2] on the basis and [18, 0, 7, 7, 9, 26] on the control, and `--library` puts the two halves at 4.3% the same offset in line over 806 common library self-loops.

**The straddling loops stand at TEN on each half, where Runs 32 to 36 read eight, and no exit span sits astride on either.** `loop-offsets.py --survey` reads 377 self-loops of at most 64 B in `_Main_`-compiled code on the basis and 312 on the control, 10 straddling on each and 0 exit spans astride, which is what `LOOP_EXITSPAN=1` owes. The count moved on BOTH halves alike, so it is the moved source and not the regime. **Post-run step 0's naming, taken off the binaries that were timed, puts the SAME ten bodies at the SAME ten mod-64 offsets on each half** --- 42, 50, 19, 42, 19, 49, 45, 42, 19 and 29 in address order --- five named by byte identity against this run's own `-g3` twins, `fillStage2Short` and four leaf bodies, and five refused by both halves alike, of which four carry a `--loose` family of three or four members and one a family of ONE, `fbMutOdoVecdimsAddInLeafU2Ptr`. So the two passes moved no straddling loop across a cache line, and the straddle map is one map for the pair.

**The regime was confirmed in this run's own binaries before the hours were spent, and the two halves read DIFFERENTLY, which is the point of the pair.** `diag` on `vgg-14-c512` puts `baseOffsetsScan` against `baseOffsetsMut` at 24066455 against 2408530 on `run37-gheadnospec`, 9.992 times apart, which is plain -O1 and is `run36-gheadnospec`'s pair of figures to the byte on a moved source; on `run37-gheadtwopass` the same two read 2408978 against 2408530, EQUAL TO THREE FIGURES, which is SpecConstr having fired. So pre-run steps 9 and 9b are one reading on this pair, and the variable is legible in the binary before any bench runs.

**The three main-set anchors** read **6.32 us** on `cnn-slice-c32`, **3.71 ms** on `cnn-L2-24x24-c32` and **39.6 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, with the control half's beside them --- the absolutes every ratio in this file divides away, kept so a later run can tell a moved box from a moved arm. The control column is the flagged half and is 20 to 22 points below the basis on all three, which is the pair's own variable and not the box:
| shape | `l` | `list`, per call | net | `gheadtwopass`, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 6.49 us | 6.32 us | 4.98 us |
| `cnn-L2-24x24-c32` | 165888 | 3.81 ms | 3.71 ms | 2.91 ms |
| `stretch-wide-2xM` | 1800000 | 40.7 ms | 39.6 ms | 31.6 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's own shapes, raw and net, off the basis half.** `rev-primes` 4.6 ms raw and 4.45 ms net; `bcast-inner900` 30.7 ms and 29.6 ms; `bcastmid-b200k` 48.7 ms and 47.6 ms; `window-128x128-k7` 14.4 ms and 13.9 ms; `scaled-rank1-m1` 5.42 ms and 5.24 ms; `runs-2` 40.6 ms and 39.5 ms; `flip-fwd-rows96` 31.2 ms and 30.1 ms; `block-r3-vol64` 4.62 ms and 4.46 ms; `small-row96` 6.71 us and 6.49 us; `compose-zero-mid` 30.9 ms and 29.8 ms. Each is one process's reading of one shape and crosses to no other population.

**The correction sits on the same footing in both halves, and ONE cell of the whole run is one the reader flags.** The two `sum-only` arms agree to within **0.13%** on every population and on both halves of the pair --- 0.9992 on `compose`'s basis to 1.0013 on `bcastmid`'s across the twenty-two --- so the term subtracted from one half is the term subtracted from the other. `CI%` reads a geomean of **1.05** over the 32 arms, 18 wider on the basis and 14 narrower. **ONE cell sits below R2 0.99 in the whole run**, `window-64x64-c16-k3/libunord-stage6-loop-sum` at 0.9876 on the basis half, a reducing consumer that publishes no corrected time; no other cell of any population on either half is under 0.99, and none is under ten samples. Run 36 carried five, its main set's among them.

**The counted work covers every population, no cell was refused anywhere, and the two halves emit very different work.** `run-counts-all.sh` wrote 22 sweep files over eleven populations on each half, 0 cells refused, at a cost of 1352s on the basis and 1106s on the control. The counts geomean over the sixteen timed arms runs **1.1435** on `small` to **1.1765** on `window`, the main set at **1.1664** --- the basis retiring some sixteen percent more instructions than the flagged half across every population. **On the main set `time/counts` separates the families cleanly**: the `bq-expand` trio sits at 0.8697 to 0.8716, retiring 50.6% more instructions on the basis for 31.0% more time; the `list` trio at 1.0023 to 1.0043, cashing almost exactly what it saves; and the ten others between 0.9170 and 0.9706, retiring 3.8 to 5.2 percent more on the basis while their clocks run from 3.70 points BELOW level to 1.60 above --- the 3.70 being the `u1` arm [the open list](../README.md#what-is-open) carries, and the other nine within 1.60. So the two passes buy `bq-expand` far more in instructions than they cash on the clock, and buy `list` rather less.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0002** on the basis and **0.9996** on the control on the main set, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0299** and **1.0831** on the basis and **1.0287** and **1.0792** on the control for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs about three percent over the term it stands for on `mut-odo-vecdims` and about eight on `bq-expand`, by nearly the same on both halves. So the two passes do not move the correction, and no ratio in this file is an artefact of a forcing pass that parted between the halves.

**The decomposition reproduces on both halves and its two columns part by the pair's own variable.** The riders time each shape's `list` alone, one bench to a process, clean and then saturated, and the state the preamble puts on a process comes back at a geomean of **1.1225** on the basis and **1.1743** on the control, **5.2** points apart, where Run 36's two parted by 4.9 and Run 35's by 0.07 --- so the two passes change what the spray costs a process as well as what the roster costs it, and by very nearly what they changed it by a run ago. What the roster adds on top of that state is **1.0196** on the basis, 7 of 19 shapes above 1, and **1.0050** on the control, 8 of 19; the basis's rest runs 0.9731 on `stretch-bigstride` to 1.2138 on `stretch-r5-8x432`, the control's 0.9742 on the same shape to 1.1127 on `stretch-tall-Mx2`. The whole in-process deflation is **1.1445** on the basis and **1.1802** on the control, 18 of 19 shapes above 1 on each.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[ramp]: ../README.md#r2-is-the-ramp-detector-not-the-noise-detector
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

The pair is Run 36's IN ITS VARIABLE, ruled by the owner 2026-09-19 --- not in every input, three of which moved after this entry was written and are amended into it at the foot --- and THIS ENTRY IS ITS ONE DECLARATION SITE by the ruling of the same day recorded in the entry above: both halves GHC HEAD `10.1.20260918` through `cabal.project.ghead` at plain `-O1` in the dead-spot form under the exit span, one source (`Main.hs` at `05cfe93`), one shim (`align-as.py` at `f31bd1c`) under the four switches, one roster, one shape set and one launch FROM DISK, and the control half's command line carrying `-fspec-constr -fliberate-case` besides, nothing else differing, so `--compare` reads the unflagged basis over the flagged control as Run 36's did; [Run 36's *What the next run compares against*](run36.md#what-the-next-run-compares-against) names this entry and restates nothing. The recipes, the half names being the preparation's to hold to the tag grammar: `run37-gheadnospec` is, from this directory, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1 LOOP_EXITSPAN=1 cabal build micro --project-file=cabal.project.ghead --builddir=db-r37a --ghc-options="-fobject-determinism" --ghc-options="-pgma $PWD/align-as.py -fforce-recomp"`, then `cp $(cabal list-bin micro --project-file=cabal.project.ghead --builddir=db-r37a) run37-gheadnospec` and `rm -rf db-r37a`; `run37-gheadtwopass` is the same on the same source with `--builddir=db-r37b` and `--ghc-options="-fspec-constr -fliberate-case -fobject-determinism"`. `LOOP_MAXSKIP` and `LOOP_LOOKTHROUGH` are inert under the dead-spot form and stay on both lines so that the lines differ in the regime flags alone; `LOOP_ENTRIES`, `LOOP_BLOCKRULES`, `LOOP_PIN` and `LOOP_TRACE` are unset on both. Every build wants `-fforce-recomp` and a fresh `--builddir`, the switches being an environment change cabal does not see; every driver still asks `half-bin.sh` for its half, which hands back the on-disk path while the mount is suspended; and under the exit span `loop-offsets.py --survey` reads 0 exit spans astride off a timed binary, so the preparation's legs 10a and 10b stop on anything else. **The PROJECT FILE is new as well**: `cabal.project.ghead` is since 2026-09-19 the minimum that compiles on a current index, where Run 36 built through what is now `cabal.project.ghead.2026-07-25`, and this run's arms against Run 36's are expected inside the drift band. **What is new besides is the MACHINE: Run 37 runs on a rebooted box, ruled 2026-09-19, and the reboot is what two of its readings are about.** [The placement section][floor] records ordinary files coming to be held as 2 MiB folios between 2026-09-16 and 2026-09-18, `pages_collapsed` from 0 to 13174 and `FileHugePages` to a quarter of a gigabyte, and every huge-page reading so far is a hand probe's: the run list's step 16a times each launch instance against a fresh copy and records no frame, and which frame an instance got is `probe-pageflags.py`'s to read, read so far by hand and after the fact, for Run 33's and Run 34's instances, and at no run's gate. So, first, whether that huge-page state persists across the reboot: the kernel's `pages_collapsed`, `FileHugePages` and `ShmemPmdMapped` read before the gate and again after the evening, beside the instance gate's two readings, and the frames of both launch instances read by `probe-pageflags.py`, as root, while the gate's process runs. Second, whether the Run 34 basis that read slower than its own byte-identical copy still does: `hugebin/run34-exit` read a median 1.075 of a fresh copy on `scaled-rank1-m1/mut-odo-vecdims-add-in-leaf-u1` on 2026-09-17 and 1.10 on 2026-09-18, and `hugebin/` is a tmpfs, so the reboot destroys that INSTANCE and keeps the on-disk `run34-exit`; what Run 37 reads is the same bytes re-placed on the remounted `hugebin/` against a second copy, `probe-r34-instance2.sh`'s form, on the quiet box before the gate. A fresh draw slow again says the term is the bytes' layout and not the draw; a level pair says the 17th's instance was one slow draw, which is what the placement section holds. **THREE INPUTS MOVED AFTER THIS ENTRY WAS COMMITTED at `7650b6b` (18:29), and the preparation of that evening amended the declaration above rather than leave them to be discovered.** THE SOURCE: `Main.hs` went `0eda736` to `05cfe93` in three of the owner's own commits, of which `2973582` retires stage ten's two consumers and stage eleven's to Only --- named there and not here, this entry being held to the TIMED roster --- and `05cfe93` adds three Storable-table twins, also Only --- so `roster-delta.py` reads 665 to 608 benches and 35 to 32 timed arms, three out and none in, with the 19 shapes and all 61 class views unmoved, and a cross-run reading against Run 36 is over the 32 survivors. THE LAUNCH: `hugebin/` was suspended the same evening, having failed to come up at the reboot, so both halves launch from disk and the placement term the mount removed is back in both --- a term on a Run 36 comparison and on neither of this run's own two columns. THE PROJECT FILE was already declared new above, and what it RESOLVES is unchanged at every version the dropped pins had named --- criterion 1.6.5.0, vector 0.13.2.0 with `boundschecks` True and `unsafechecks` False, base 4.23.0.0 and hashable 1.5.0.0, with ghc-prim 0.14.0 beside them --- read off the build's own `plan.json` and not off the file. That is the set the old file constrained and so the set a dropped constraint could have moved; it is not a survey of every package the binary links, which nothing here has taken. The one package the new file's header names as moving, aeson 2.3.2.0 against 2.2.5.0, is criterion's JSON writer and reaches no timed code. **And the two reboot readings are no longer symmetrical, which is the owner's to rule before either becomes an item.** The box was rebooted at 23:04 and the counters answered the first question at once: `pages_collapsed` 18951 to 0 and `ShmemHugePages` 290816 kB to 0, while `FileHugePages` read 63488 kB on both sides of the boot and so carries no signal about it. The second has its BEFORE side taken and its after side out of reach: `probe-r34-instance2.sh` on the quiet box put `hugebin/run34-exit` at a median 1.0419 of a fresh copy on `scaled-rank1-m1/mut-odo-vecdims-add-in-leaf-u1`, a third reading after 1.075 and 1.10, with both instances equally PMD-mapped at 16384 kB --- so the gap is which frames each drew and not whether either got folios --- while the after side wants the mount raised, which the suspension makes an emergency rather than this run's default --- **and the owner ruled on 2026-09-20 that this is not emergency enough, so Run 37 raises nothing and the after side is not taken.** What that costs is not recoverable and is worth saying once: `hugebin/` is a tmpfs, so the instance those readings were taken of died with the reboot, and 1.075, 1.10 and 1.0419 are the whole of what will ever be known about it. The [frame-draw entry][open] keeps the answer it has --- that one instance held its sign over two and a half days while its magnitude halved --- and loses the instance that could have extended it. A later run wanting more raises the mount and draws a fresh one, which is a new series and not a continuation of this. **What the run predicts, registered 2026-09-20 before it runs. EVERY FIGURE BELOW COMES FROM RUN 36'S OWN READING OF THIS SAME PAIR**, re-derived from its JSONs with `--predictions` and not quoted from its prose. In items (1), (2) and (4) that figure is the TARGET; in item (3) the target is 1.0, which is a null and nobody's reading, and what Run 36 supplies there is the BAND, off the spread its ten arms actually read. Run 36 drew four of its own bands off Run 31's `-O2` level instead and lost them together, which is what this is written to avoid.

(1) *The two passes are worth on `list` what they were worth on Run 36, across a rebooted box and a moved source.* `predict: cross list 1.3360 within 3% on main basis`, and over the eighteen shapes left when this run's wild cell is set aside, `predict: cross list 1.3129 within 2% on main basis excluding stretch-coprime-r7`. Run 36's basis floor is 1.63%, so 2% is that floor and a third of a point, and 3% adds beside it the 2.31 points one wild cell moved the whole-set figure on Run 36. THE NARROW SPAN IS THE ONE THAT DECIDES: if the wild cell does not recur the whole-set reading lands near 1.3129 and the wide span holds either way, so the wide span alone would say nothing.

**Read by --predictions, item (1):** `cross list 1.3360 within 3% on main basis`: KILLED on main basis, read 1.2960 over 19 shape(s), 4.00 point(s) off, within 3.00% --- `cross list 1.3129 within 2% on main basis excluding stretch-coprime-r7`: HELD on main basis, read 1.2950 over 18 shape(s), 1.79 point(s) off, within 2.00%.

(2) *`bq-expand` repeats as well, on the arm where three accounts already agreed to a quarter of a point.* `predict: cross bq-expand 1.2980 within 1.5% on main basis`. A reading outside is the first sign that something other than the two passes moved between the runs --- the source, the launch from disk, or the box.

**Read by --predictions, item (2):** `cross bq-expand 1.2980 within 1.5% on main basis`: HELD on main basis, read 1.3101 over 19 shape(s), 1.21 point(s) off, within 1.50%.

(3) *No arm outside the two families joins them on a second reading.* `predict: cross mut-odo-vecdims 1.0 within 3% on main basis`, `predict: cross mut-odo-vecdims-aa 1.0 within 3% on main basis`, `predict: cross mut-odo-vecdims-aa-distant 1.0 within 3% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u1 1.0 within 3% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 3% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u2-aa 1.0 within 3% on main basis`, `predict: cross mut-odo-vecdims-add-in-leaf-u2-aa-distant 1.0 within 3% on main basis`, `predict: cross lib-stage1 1.0 within 3% on main basis`, `predict: cross lib-stage2-lean 1.0 within 3% on main basis`, `predict: cross lib-stage2-lean-u1 1.0 within 3% on main basis`. THESE TEN ARE ONE CLAIM READ TEN WAYS and a single miss is one arm joining the families rather than ten results, which is the tally Run 36's own write-up had to disentangle. The band is set on Run 36's measured spread over these same ten, 0.9920 to 1.0119: 3% is its widest (1.19 points) with the 1.63% floor beside it.

**Read by --predictions, item (3):** `cross mut-odo-vecdims 1.0 within 3% on main basis`: HELD on main basis, read 1.0077 over 19 shape(s), 0.77 point(s) off, within 3.00% --- `cross mut-odo-vecdims-aa 1.0 within 3% on main basis`: HELD on main basis, read 1.0075 over 19 shape(s), 0.75 point(s) off, within 3.00% --- `cross mut-odo-vecdims-aa-distant 1.0 within 3% on main basis`: HELD on main basis, read 1.0026 over 19 shape(s), 0.26 point(s) off, within 3.00% --- `cross mut-odo-vecdims-add-in-leaf-u1 1.0 within 3% on main basis`: KILLED on main basis, read 0.9630 over 19 shape(s), 3.70 point(s) off, within 3.00% --- `cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 3% on main basis`: HELD on main basis, read 1.0047 over 19 shape(s), 0.47 point(s) off, within 3.00% --- `cross mut-odo-vecdims-add-in-leaf-u2-aa 1.0 within 3% on main basis`: HELD on main basis, read 1.0042 over 19 shape(s), 0.42 point(s) off, within 3.00% --- `cross mut-odo-vecdims-add-in-leaf-u2-aa-distant 1.0 within 3% on main basis`: HELD on main basis, read 1.0064 over 19 shape(s), 0.64 point(s) off, within 3.00% --- `cross lib-stage1 1.0 within 3% on main basis`: HELD on main basis, read 1.0159 over 19 shape(s), 1.59 point(s) off, within 3.00% --- `cross lib-stage2-lean 1.0 within 3% on main basis`: HELD on main basis, read 1.0160 over 19 shape(s), 1.60 point(s) off, within 3.00% --- `cross lib-stage2-lean-u1 1.0 within 3% on main basis`: HELD on main basis, read 1.0050 over 19 shape(s), 0.50 point(s) off, within 3.00%.

(4) *The counted work parts on the arms the clock parts on, and it repeats more tightly than the clock does.* `predict: counts list 1.2926 within 1.5% on main basis`, `predict: counts bq-expand 1.5063 within 2% on main basis`, `predict: counts mut-odo-vecdims 1.0383 within 1% on main basis`, `predict: counts lib-stage2-lean 1.0522 within 1% on main basis`. THESE ARE NOT DOWNSTREAM OF (1), and Run 36 is the evidence: its two null counts spans, drawn from Run 31 and read across a compiler change, landed 0.03 and 0.06 of a point off, while its two family counts spans, from the same prior, missed by 1.94 and 6.40. So counts carry across runs where times need not, and the bands here are tight where that history says they can be and wide where it says they cannot. **The two reboot readings are NOT registered as items and are the owner's to rule**, for the reason the paragraph above gives: the first is already answered by the counters, before the run rather than by it, and the second wants a mount the same day's ruling suspended.

**Read by --predictions, item (4):** `counts list 1.2926 within 1.5% on main basis`: HELD on main basis, read 1.2930 over 19 shape(s), 0.04 point(s) off, within 1.50% --- `counts bq-expand 1.5063 within 2% on main basis`: HELD on main basis, read 1.5063 over 19 shape(s), 0.00 point(s) off, within 2.00% --- `counts mut-odo-vecdims 1.0383 within 1% on main basis`: HELD on main basis, read 1.0383 over 19 shape(s), 0.00 point(s) off, within 1.00% --- `counts lib-stage2-lean 1.0522 within 1% on main basis`: HELD on main basis, read 1.0522 over 19 shape(s), 0.00 point(s) off, within 1.00%.

**THREE items hold their sentences and ONE is killed, over 17 spans of which 15 held and 2 were killed --- and the two kills point in opposite directions.** Every verdict below is its item's KILL CONDITION applied across the population and half it names, which on this registration is `main basis` for all seventeen spans; every figure is re-derived from this run's own JSONs, by `--predictions` over the main set on the basis half and by `--counts` where a clause names instructions. **One thing governs how the list reads**: this pair is Run 36's read again, so every item asks whether a figure repeats, and what repeats exactly is the counted work, while what does not is the one time figure Run 36's wild cell inflated.

(1) *The two passes are worth on `list` what they were worth on Run 36, across a rebooted box and a moved source.* **The WIDE span is KILLED and the NARROW one HOLDS, which is the outcome the item's own text called decisive.** The wide span asked for 1.3360 within 3 points and the run reads **1.2960**, 4.00 points out; the narrow span asked for 1.3129 within 2 points over the eighteen shapes without `stretch-coprime-r7`, and the run reads **1.2950**, 1.79 points out. The item said that if the wild cell did not recur the whole-set reading would land near 1.3129 and the wide span would hold either way, so that the narrow span was the one that would decide. The cell did not recur --- this run reads it at 1.1174e-03 against Run 36's 1.5371e-03, back among its own A/A copies --- and the whole-set reading landed at 1.2960, a tenth of a point from the narrow one. **So the wide span died because it was centred on a figure the anomaly had inflated, and not because the passes moved**; what the item half got wrong is that the wide span would hold either way, since a band centred 2.31 points above the corrected figure cannot contain it.

(2) *`bq-expand` repeats as well, on the arm where three accounts already agreed to a quarter of a point.* **HELD**, at **1.3101** against 1.2980 within 1.5 points, 1.21 points out. The item registered a reading outside this span as the first sign that something other than the two passes had moved between the runs --- the source, the launch from disk, or the box. It is inside, so none of those three shows on this arm.

(3) *No arm outside the two families joins them on a second reading.* **The SENTENCE HOLDS and ONE of its ten spans is killed.** No arm outside the `list` and `bq-expand` families joins them: nine of the ten come in within 1.60 points of 1, the widest `lib-stage2-lean` at 1.0160 and the narrowest `mut-odo-vecdims-aa-distant` at 1.0026. The tenth, `mut-odo-vecdims-add-in-leaf-u1`, reads **0.9630**, 3.70 points outside a 3% band --- and it moves the BASIS's way, away from the families rather than towards them, so it leaves the sentence the item registered untouched. **What it does instead is open a reading this item cannot hold**: that arm is the low extreme in nine of the ten classes as well, down to **0.7053** on `bcast`, and step 4a's half-local reading puts the whole movement on the CONTROL half against Run 36's control, at 1.0000 counts on both halves in every population. So the span died to this run's control binary rather than to the two passes, and it goes to the open list rather than into this item.

(4) *The counted work parts on the arms the clock parts on, and it repeats more tightly than the clock does.* **HELD on all four spans, three of them at 0.00 points.** `bq-expand` reads **1.5063** against 1.5063, `mut-odo-vecdims` **1.0383** against 1.0383 and `lib-stage2-lean` **1.0522** against 1.0522, each to the fourth decimal, with `list` at **1.2930** against 1.2926, 0.04 of a point out. The item was registered on the argument that counts carry across runs where times need not, drawn from Run 36's two null counts spans landing 0.03 and 0.06 of a point off; on four spans, a changed source and two reboots it is now the best-supported claim this registration makes.
