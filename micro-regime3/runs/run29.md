# Run 29 (SpecConstr against plain -O1)

One run's write-up: its head, its Results, what the next run compares against, the claims that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 29 (SpecConstr against plain -O1), and what the flag is worth is two arms and no more.** The pair is Run 28's basis with and without `-fspec-constr` --- one source, one compiler, one shim, one roster and one bench order, so every figure that moves between the halves moved because that flag was struck off one command line. Over the eighteen main-set arms that carry a corrected time, twelve sit within 1% of 1 and the six outside are two families: `bq-expand` with its two A/A copies at **0.7810**, 0.7779 and 0.7767, and `list` with its two at **0.8788**, 0.8785 and 0.8827. The shipped fill family does not move at all --- `mut-odo-vecdims-add-in-leaf-u2` **1.0001**, `lib-stage1` **0.9945**, `lib-stage2-lean` **1.0044** --- which is registration (1) held and the reason `Data/Array/Internal.hs` may go on not setting the flag. What knowing it costs is registration (2), killed: `list` is the reference every column here divides by, and a reference that moves 12% is one the two halves cannot be differenced against on any population.

**The roster is 36 timed arms over 19 main-set shapes and 684 benches, with 58 class views over ten classes for 2088 more, and it is nearly Run 28's.** `./roster-delta.py run28-g912 run29-spec` reads 39 arms to 36 over 19 shapes to 19: ONE in, `libunord-stage10-sum`, and FOUR out --- stage eight's consumer, stage four's, and the leaf's hoisted-look-ahead and count-bound forms, all parked 2026-09-11 --- with the 35 survivors in the same order and no shape moved. NO CLASS VIEW MOVES: `runs` stays at fourteen, `window` at eight, `bcast` and `flip` at six, `block` and `small` at five, `bcastmid` and `compose` at four, `rev` and `scaled` at three. So the roster term in any distance from Run 28 is three arms net out, and the L1 pass was owed by the arm that LANDED rather than by a population that moved --- an arm lands on every class, which is why all ten were named for it.

**NOT ONE CELL SINKS BELOW THE SHARED FORCING PASS ON EITHER HALF**, as on Run 28 and for the same two reasons: the ruling of 2026-09-10 that a reducing consumer has no corrected time, so the fourteen `-sum` rows read `--` in `time` and `worst` by design, and the retirement of every Fill arm over a list to `check`. `read-all.sh --brief-facts` reports an empty sunk list for both halves, so every row that carries a corrected time at all is a geomean over all nineteen shapes. The L1 roster pass had already said so a day early, on a roster the ruling was not written against: the reader's own warning string is absent from all eleven of its legs.

**The gate read SOUND and the machine check did NOT fire, so absolutes cross between Run 28 and this run with no box term.** The gate's soundness is read the other way round from Run 28's: two of its three arms sit far from 1, which is this pair's variable reaching them, so what says the apparatus is sound is the two palindrome passes AGREEING --- `bq-expand` 0.7710 against 0.7667, `list` 0.8661 against 0.8764, `mut-odo-vecdims` 0.9935 against 1.0063, the widest apart being `mut-odo-vecdims` at 1.29%. Against Run 28's own fingerprint the box reads a geomean of **-0.17%** over all nineteen shapes, inside the **3%** bar the GEOMEAN is held to, with the worst single shape `cnn-L1-6x6-c1` at -1.75%, inside the **5%** bar a SHAPE is held to. The two bars are different quantities and the mode applies both. So this run publishes INSIDE the third machine era rather than opening a fourth, the cross-run column is a subtraction rather than a `--bridge` reading, and no figure below is bridged.

**Every one of the twenty-two processes gated clean --- `--selftest` and both `--aa` gates --- and the PLATEAU GATE FIRED on this pair's own variable rather than on a state difference.** It reports the preamble's victim spanning 19.2701 to 21.5484 ms/iter across the run, an 11.82% spread against a 5% band. Three readings say what that is. WITHIN each half the plateau is flat --- `nospec` 21.1299 to 21.5484, **1.98%**, and `spec` 19.2701 to 19.6332, **1.88%** --- and the split is exactly by half, eleven processes each. The saturation STATE is identical to the byte on all twenty-two, `inuse=95420416` and `keep=8.19844333056e12`, one distinct value apiece. And the victim is `vgg-14-c512-k3/list`, whose own cells read 20.459 ms raw on the control against 18.641 on the basis, 9.75% apart --- against the plateau split's 10.2%, which is the two halves' MEDIAN victim readings, 21.3167 against 19.3415, and not any ratio of the four extremes above. So the gate bands a TIME across two halves whose variable changes that time, which no pair before this one could do to it; the run is sound and the reading is per half. TWO A/A worst cells sit above 5%: `spec-flip` **15.51%** on `flip-last-rows` and `spec-runs` 7.06% on `runs-r3-48x30`, with no other process past 4.63%. That column is `read-all.sh`'s, ONE figure a process over all eight pairs; a class block's Controls line gives a different maximum, the worst cell of that process's LARGEST pair, so `runs`'s 4.64% there is not a fourth reading of this.

**The sequence ran in ONE window and no intrusion was found.** Twenty-two processes from 2026-09-12T01:17:01 to 09:16:42, every one exiting 0 at its roster's count --- 684 twice, 504 twice, 288 twice, and 216, 180, 144 and 108 four times each, 5544 benches in all --- with no hole between one process finishing and the next starting, no complaint, and no `!!` line anywhere in the wall-clock record. The control half ran first throughout. `--wild` over all twenty-two logs puts the highest `fgn/core` reading anywhere at **0.22**, on `nospec-main`'s `cnn-slice-c32/bq-expand`, under the instrument's own 0.25 bar, so post-run step 3's rerun was not owed and every figure in this file is the one window's.

**The manifest's two live claims both held, and claim 7's levels reproduce on the basis to the digit for a seventh run.** Claim 10, `mut-odo-vecdims-add-in-leaf-u2` under `mut-odo-vecdims`, reads **0.6441** paired on the basis over 18 of 19 shapes at sign p 7.6e-05, and **0.6413** over 17 of 19 on the control --- its published-column ratios being 0.5563 and 0.5715, which part from the paired figure widely enough that both are quoted. Claim 7's levels, read over the eighteen main-set shapes it is pinned to, come back on the basis as the `libunord` consumers at **0.00x**, the mutable fills and the `liblist` consumers at **1.00x**, `bq-expand` at **2.06x** and `list` at **22.38x** --- every one of them Runs 23 to 28's figure over the same eighteen. What is new is that the CONTROL half does not share two of them, reading `bq-expand` at 2.76x and `list` at 24.90x, which is registration (11)'s kill and is treated there.

**What the flag is worth is the same shape on every population, and the fill family is inside 1% on seven of the eleven.** `list` moves by about a tenth everywhere, 9.12% at its narrowest --- 12.12 points on the main set and, per class, 9.12% on `rev`, 10.65% on `bcastmid`, 10.83% on `window`, 11.02% on `compose`, 11.23% on `runs`, 11.47% on `scaled`, 11.68% on `small`, 12.08% on `flip`, 12.96% on `block` and 14.67% on `bcast`, its widest. The three shipped-fill arms registration (1) names hold inside 1% on the main set and on `rev`, `window`, `scaled`, `runs`, `flip` and `compose`; **ten of their thirty-three arm-by-population readings sit outside it**, and every one falls on `bcast`, `bcastmid`, `block` or --- for the leaf alone, at 0.9679 --- `small`. So the item's kill, which is main-set only, does not fire, and what the class readings add is that the flag is not quite nothing to the fill on the classes whose calls are shortest.

**The counted work covers every population, no cell was refused anywhere, and its finding is that the flag's instruction saving reaches the clock on two arms and on nothing else.** Twenty-two sweeps in 43 minutes, `runs` longest at 436s and 392s, `rev` and `small` shortest at 26s for the pair apiece, with no `perf could not count` line in any of the eleven pairs of files. Over the eighteen main-set arms that carry a corrected time the counts geomean is **0.9088** --- which is also, by coincidence and not by any relation, what `rev`'s `list` reads across the halves four paragraphs above. Split per arm, the two families the flag speeds up convert their saving almost entirely: `bq-expand` reads counts 0.7687 against time 0.7810, and `list` counts 0.8854 against time 0.8788, so `time/counts` is 1.0161 and 0.9926. **Every other arm reads counts near 0.95 and time near 1.00**, `time/counts` running 1.0340 to 1.0566 --- the flag takes about a twentieth of the instructions off the whole fill family and not one of them reaches the clock. That is the open question about conversion answered in a new regime and at both ends of its range at once.

**This run's two columns may NOT be differenced, and that is true of every one of the eleven populations.** `list` moves between the halves by 9.12% at its narrowest, on `rev`, and 14.67% at its widest, on `bcast`, with the main set at 12.12 points --- so `read-all.sh --brief-facts` marks all eleven PAST the 0.7% bar and each class block says so in its own cross-half line. Every arm-by-arm figure across the halves in this file is therefore an ORDERING and not a subtraction, which is the opposite of what Run 28 could say and is registration (2)'s kill working through the whole document. What survives the bar untouched is everything read WITHIN one half --- every `pair` span, every claim, every class property --- and those carry the run.

**The fastest timed arm on the main set is a three-way tie the column cannot separate, and `--pair` does not separate it either.** `lib-stage2-lean`, `lib-stage2-lean-u1` and `mut-odo-vecdims-add-in-leaf-u2` all read **0.028** in the published `time` column, against `mut-odo-vecdims`'s 0.051 and `bq-expand`'s 0.113. Paired, `lib-stage2-lean` over the shipped leaf is 0.9899 with 9 of 19 wins at sign p 1, which is a tie by every reading the file has; `lib-stage2-lean` over its own unrolling twin is 0.9691 at 13 of 19 and p 0.17. So the lead is the lean fill and the shipped leaf together, and no sentence here puts one of them in front of the other.

**Stage ten composes its two changes where both fire and costs a pass where neither does, and the second half of that is what killed a registration.** `libunord-stage10-sum` is stage seven's tie-break under stage nine's zero-stride move, and over stage seven it reads **0.5850** on `bcast` and **0.6128** on `bcastmid` against 0.55 and 0.6 registered, with `compose` at 0.6938; over stage nine on `window`, where no view carries a zero stride, it reads **0.7433** against 0.72, which is stage seven's tie-break arriving intact. No zero-stride view has it behind stage seven on either half, worst `bcast-inner8` at 0.7106 and 0.7100, and no unstrided `window` view has it behind stage nine PAST THE FLOOR on both halves --- `window-128x128-k7` reads 1.0350 on the basis against 0.9719 on the control, parting in sign. **Where neither change fires it is not free.** On `rev`, whose views carry no zero stride and no tied pair, `zerosOutermost` hands its list back unchanged and `routeUnord10` is `routeUnord7` to the byte, yet the pair reads **1.0229** on the basis, against that process's floor of 0.40%, and **1.0240** on the control, whose own eight pairs span 0.21% --- outside each half's own spread, which is registration (7)'s kill exactly, and the extra pass over the axes is the only thing left to charge it to.

**The fused list survives every consumer this run points at it, and the flag is worth about two percent to it.** Against the fill it replaces with nothing else different, `libunord-stage5-sum` over `libunord-stage3-sum` reads **0.5675** and 0.5795 on `runs` and **0.4453** and 0.4493 on `block`, and ties inside its own floor on all five classes where both routes fill --- widest 1.0019, on `bcastmid`. Against the port's base-offset table, `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.4915** and 0.4664 on `runs` and 0.7826 and 0.7220 on `block`, so the odometer's fold beats master's table on the control half as on the basis. And the flag itself barely reaches it: read raw over every shape, as an arm with no corrected time must be, `libunord-stage7-sum` --- the consumer of `libunord-stage7`, which is checked and not timed --- across the halves is **0.9779** on `runs`, 0.9839 on `block` and 0.9771 on `window`, against the one part in ten registration (3) predicted --- so the list's lead over the fill is the design's and not `-fspec-constr`'s, which is the question that item was written to ask.

**The three class properties hold in all ten classes on both halves.** `mut-odo-vecdims`'s `worst` stays under 1 everywhere; it is ahead of `bq-expand` on every shape of every class, closest at `window-64x64-k1x9`'s 0.8042, then `bcast-tall-Mx2`'s 0.7458 and `runs-2`'s 0.7294; and both allocation clauses hold with the 1% margin, closest at `small-patch-k5`'s 0.06580 against `list` and `small-row96`'s 1.00441 against `bq-expand`. The tier ordering is unbroken in all ten: the fills at the result vector, `bq-expand` between 1.00x and 2.75x it, `list` an order of magnitude above at 18.99x to 24.77x. `small` reads its fills at 1.27x, the per-call constants showing through a call under a microsecond, and its order is unbroken like the rest.

**Across the halves every class votes the same way, and not one of the ten may be read for the pair's variable.** The basis is faster on 18 of 18 arms in `bcast` and `block`, 17 in `small`, 16 in `rev`, `bcastmid`, `scaled` and `compose`, 13 in `runs`, 12 in `window` and 11 in `flip`, at class geomeans from **0.9149** on `window` to **0.9667** on `scaled`. But each class's `list` moved 9.12% to 14.67% between the halves, past the 0.7% that lets two columns be differenced, so every one of those lines is disqualified as a reading of the flag and stands only as an ordering. What the vote does show is WHOSE the movement is: drop the `list` and `bq-expand` families and the same ten geomeans run **0.9754** on `block` to **0.9998** on `window`, with `window`, `runs` and `flip` level to a thousandth and six, five and seven of their twelve remaining arms above 1.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The two `sum-only` halves agree to within a fifth of a percent on every population, from 0.9985 on `rev` to 1.0012 on `scaled`, so the term subtracted from one half is the term subtracted from the other and no ratio here is an artefact of a moved forcing pass. That matters more on this pair than on the last five: the two halves' absolutes part by a tenth, and a correction that had parted with them would have put that tenth into every corrected column.

**The two halves resolve alike.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **0.97** over the 36 arms, 16 of them wider on the basis and 20 narrower, so neither build measures itself better than the other. Read it beside the A/A floor and not off it: this column is sampling error INSIDE one bench where the floor is agreement BETWEEN two placements of one arm, and Run 18 moved the two in opposite directions.

**The straddling loops stand at EIGHT on each half, and the two halves' self-loop counts part by sixty-two.** `--survey` reads 222 self-loops of at most 64 B on the basis, 83 of them at offset 0 and 8 still straddling, against 284 on the control, 118 at offset 0 and 8 straddling --- and it is the FLAGGED half that carries the fewer, where its `.text` is the larger by 16384 bytes exactly. Nothing here explains that pairing and this file does not try: `--survey` counts self-loops of at most 64 B, so both a loop removed and a loop grown past the cutoff leave it. Post-run step 0 names six of each half's eight off the `-g3` twins by byte identity --- `fillStage2Short`, `fbMutOdoVecdimsAddInLeafU2` twice, its `Down`, `Last` and `Ptr` forms --- and refuses the other two, at mod-64 offsets 53 and 42 on both halves, where no twin holds a byte-identical copy. The two parked leaf forms are named though nothing times them: parking takes an arm off the roster and not out of the binary.

**The regime was confirmed in this run's own binaries before the hours were spent, and on BOTH halves for the first time.** `diag` reads `baseOffsetsScan` against `baseOffsetsMut` on `vgg-14-c512` at 2408930 against 2408530 on the basis --- equal to three figures, which is SpecConstr --- and at 24066407 against 2408530 on the control, 9.99 times apart, which is the ten times this README has attributed to plain -O1 since Run 8. On `cnn-L1-24x24` the same two rows read 14202 against 13906 and 138473 against 13906. Every run since Run 8 has had the flag on both halves, so this is the first pair here to read both sides of that sentence, and a control half reading equal would have been the flag still on and the stop the pair note carried.

**Run 29 records every population twice** --- the main set and all ten stride classes from each half, one process apiece, twenty-two in all --- so every class figure below has a control-half twin and every one of the ten was gated on both. That is what lets a class say something the main set cannot, and on this pair it is what shows the flag acting alike on all eleven: the reference moves by about a tenth everywhere, 9.12% at its narrowest, and the fill family moves on four classes and nowhere else.

**The decomposition reproduces on both halves.** The riders time each shape's `list` alone, one bench to a process, clean and then saturated, and the two columns come back at a geomean of **1.1161** on the basis and **1.1177** on the control for the state the preamble puts on a process --- within sixteen ten-thousandths of each other, on halves whose in-process absolutes part by a tenth. What the roster then adds on top of that state is **1.0036** on the basis, 8 of 19 shapes above 1, and **1.0223** on the control, 10 of 19; the control's rest runs 0.9746 on `stretch-bigstride` to 1.2307 on `stretch-r5-8x432`, where the basis's runs 0.9799 to 1.1024, so the wider of the two spreads is the unflagged half's.

**There is NO repetition this run and none was owed.** `Main.hs` moved from Run 28's `bdf06c8` to `7685375` by twelve commits, so neither half can reproduce an earlier binary and an unequal md5 names nothing here; the two are `22f7f57d1728db91ac87b1b0d9308272` and `f507eafbcbbfecf7c2151f390caf9b92`. What stands in its place is `--delta` against `run28-g912`, the previous build of the basis's recipe, which reads every tracked fill's mod-64 offset PRESERVED --- [0, 0, 0, 24, 0, 4] and [0, 0] --- with no address surviving to the byte and 2 and 2 displacements, where Run 28 read 3 and 2 against `run27-g912` and Run 27 read 4 and 2. Within the pair, `--library` reads 914 library self-loops in common, 39.4% at the same offset in line and 61.5% in the same straddle state; there is no band for that on a pair sharing a compiler, README banding only the compiler pairs at a tenth to a quarter, so it is recorded rather than judged.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition. What this run leaves the next one is Run 28's lesson read a second time on a different variable. Eight of its eleven items argue a no-op somewhere --- (1), (2), (3), (5), (7), (8), (9) and (11) --- and all three of its kills fell among them, on (2), (7) and (11); not one item predicting a REAL effect was killed, which is what Run 28 also found and did not claim the converse of. So an argument that a change cannot reach a population remains the cheapest clause a registration can carry and the least reliable, and registrations (2), (7) and (11) are the ones to read before writing a ninth.


## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a `-fspec-constr` table**: it is not the regime `Data/Array/Internal.hs` compiles under, and this run is the first that can say what the difference is worth. **A row's distance from Run 28's column carries a roster term and an evening and NO box change**, `Main.hs` having moved from `bdf06c8` to `7685375` by twelve commits --- one timed arm landed and four left, three net out, the nineteen shapes unmoved and no class view added --- while the gate's machine check read `list`'s net at **-0.17%** against the fingerprint Run 28 installed, inside 3%. **So the raw distance IS readable**: over the 18 arms both rosters time and both give a corrected time, this basis column runs 0.9896 to 1.0213, 16 of the 18 inside 1% of 1, with `list` itself at **0.9999** --- and the two outside 1% are `mut-odo-vecdims-add-in-leaf-u2-ptr` at 0.9896, a hair past it, and `lib-stage1` at **1.0213**, the arm whose route changed, `fillStage2` having taken it on 2026-09-11. `--bridge` against Run 28, each arm as a ratio to `list` in its own run, reads 1.0014 over seventeen of those eighteen --- `list` dropping out as its own denominator --- with none outside the 3.3% drift band. **The `alloc` column is pinned as always**: over the eighteen shapes claim 7 keeps, this basis reads the fills 1.00x, `bq-expand` **2.06x**, `list` **22.38x** and the `libunord` consumers 0.00x, every figure Runs 23 to 28's --- and the control half does NOT share two of them, reading 2.76x and 24.90x, which is the whole of registration (11).

**And it is the basis half's**, `run29-spec`, as every published table here is from Run 11 on: the control half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-six rows. That the published half is the flagged one is this pair's own decision --- `-fspec-constr` is the regime every figure in README was measured through, so it keeps the lineage, and `nospec` is the candidate reading. **One row here is a first reading at full budget**: `libunord-stage10-sum`, stage seven's tie-break under stage nine's zero-stride move, landed 2026-09-11. **Four rows Run 28 published are gone**, parked the same day: `libunord-stage8-sum` and `libunord-stage4-sum`, the longest chain's consumer and stage four's, and `mut-odo-vecdims-add-in-leaf-u2-down` and `-u2-last`, the leaf's hoisted-look-ahead and count-bound forms.

**Comparing runs?** The table below is Run 29's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the claims to test are [the ones after it](#the-claims-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.51%** from 1 on the basis half or **0.82%** on the control, read over the eight pairs this roster carries, and over the four pairs that carry back to Run 10 at **0.26%** and **0.82%**, beside which the worst SINGLE A/A cells of the two MAIN-SET processes, 3.41% on `cnn-L1-24x24-c1` and 3.91% on `alexnet-L1-55-c3-k11`, are not floors at all and are not to be quoted as any --- and are not this run's worst, `spec-flip` carrying **15.51%** on `flip-last-rows` --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **And one thing this run does NOT have**: its two columns may not be differenced on any population, `list` having moved by a tenth or more between the halves everywhere, so every cross-half figure here is an ordering.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What cost a row its population on Run 27 was the correction, not the estimator, and on THIS run nothing does**: that run had nine rows on each half carrying cells the shared forcing pass is not smaller than --- seventy such cells on its basis and forty-three on its HEAD half, all of them `libunord` arms --- and this roster has none, so every row here that carries a corrected time is a geomean over all nineteen shapes. What replaced those rows is the `no_net` ruling and the retirement of the Fill arms over a list, named in the head and in *What the next run compares against*, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what claim 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, which claim 7 now carries on a live basis, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.77* | *60* | *2.11x* | *its base arm, forced with one element* |
| liblist-stage1-sum | -- | -- | 0.60 | 69 | 1.00x | the same, over the ordered list of master's slice recursion |
| liblist-stage2-sum | -- | -- | 0.56 | 70 | 1.00x | the same, over the port's base-offset table |
| liblist-stage3-sum | -- | -- | 0.59 | 70 | 1.00x | the same, over the lazy odometer under the natural-strides dispatch |
| liblist-stage4-sum | -- | -- | 0.58 | 70 | 1.00x | the same, over the lazy odometer under the lean dispatch |
| libunord-stage1-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| libunord-stage10-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage seven's tie-break with stage nine's zero-stride axes moved outermost -- the two reorderings composed |
| libunord-stage2-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over the branch's port |
| libunord-stage3-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over the ceiling's fill -- stage five with its runs turned into fills |
| libunord-stage5-sum | -- | -- | 0.01 | 83 | 0.00x | new mutating `Vector` method -- `sumT` over stage five's list, one slice at a time and no concatenation |
| libunord-stage6-list-sum | -- | -- | 0.02 | 83 | 0.00x | the same, base's `sum` over stage six's list, the fold a library user brings |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 83 | 0.00x | the same, the fold taken into the walk -- a strict loop over the levels and no list |
| libunord-stage6-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage six's list -- stage five with the first canonicalization dropped |
| libunord-stage7-sum | -- | -- | 0.01 | 83 | 0.00x | the same, over stage seven's list -- the tie-break, the longer extent innermost |
| libunord-stage9-sum | -- | -- | 0.02 | 83 | 0.00x | the same, over stage nine's list -- every zero-stride axis moved outermost |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.39* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.02* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.02* | *83* | *0.00x* | *the same, at the other end* |
| lib-stage2-lean | 0.028 | 0.129 | 0.62 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage2-lean-u1 | 0.028 | 0.126 | 0.59 | 69 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| mut-odo-vecdims-add-in-leaf-u2 | 0.028 | 0.130 | 0.49 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.029* | *0.129* | *0.59* | *69* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.029* | *0.129* | *0.58* | *69* | *1.00x* | *A/A control* |
| lib-stage1 | 0.029 | 0.129 | 0.59 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.029 | 0.129 | 0.64 | 70 | 1.00x | the same, unrolled -- the shipped fill's ceiling under an allocator that spills nothing |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.030 | 0.128 | 0.61 | 69 | 1.00x | nothing the library would take -- the merged-bound leaf walking a `Ptr`, a ceiling and not a candidate |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.126 | 0.56 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| **mut-odo-vecdims** | **0.051** | 0.128 | 0.41 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| *mut-odo-vecdims-aa* | *0.051* | *0.128* | *0.38* | *66* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.128* | *0.43* | *66* | *1.00x* | *A/A control* |
| *bq-expand-aa-adjacent* | *0.112* | *0.209* | *0.68* | *53* | *2.11x* | *A/A control* |
| bq-expand | 0.113 | 0.209 | 0.68 | 53 | 2.11x | nothing (pure) -- the last candidate |
| *bq-expand-aa-distant* | *0.113* | *0.210* | *0.37* | *53* | *2.11x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.85 | 22 | 22.64x | -- |
| *list-aa-adjacent* | *1.002* | *1.010* | *0.75* | *22* | *22.64x* | *A/A control* |
| *list-aa-distant* | *1.003* | *1.034* | *0.70* | *22* | *22.64x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and pairs still part in SIGN between the two on this run. `mut-odo-vecdims-add-in-leaf-u2-ptr` against its parent is one: the rows divide to 1.036 where the paired figure is **0.9366**. `mut-odo-vecdims-add-in-leaf-u2-aa` against its base is another, the paired figure **0.9999** and the published column **1.0127**, which `--predictions` names as parting when it adjudicates registration (9). A margin between two arms is `--pair`'s paired figure, which is also the statistic the floor is defined in; `--pair` and `--predictions` print both side by side and say when they part. The column is for reading the table, not for differencing it.

`concat-runs` has no row, and neither do the other 78 arms the roster holds and checks without timing --- **79 of its 115** in all, where Run 28 checked 74 of 113: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). **Nothing was retired to `check` this run**; the thirteen Fill arms over a list, `liblist-stage1` to `-stage4` and `libunord-stage1` to `-stage9`, went that way on 2026-09-09 and stay there, and what moved this run is four timed arms PARKED and one landed. So a movement against Run 28's table is a movement on the **18 arms both runs time and both give a corrected time**, a day apart, with a roster of one in and four out between them and no box change --- which is why the cross-run reading here is a subtraction and not `--bridge`'s.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table is seven real fills**: `mut-odo-vecdims` reads 0.051 with seven timed non-control arms below it --- `lib-stage2-lean` and `lib-stage2-lean-u1` at 0.028, `mut-odo-vecdims-add-in-leaf-u2` at 0.028, `lib-stage1` and `-u2-ptr` at 0.029, `-u1-ptr` at 0.030 and `-u1` at 0.031 --- and every one of them writes the result vector the family's arms write. Run 28's nine became seven by parking `-u2-down` and `-u2-last`, not by any arm slowing. **The lead is a THREE-WAY TIE and the file does not break it**: `lib-stage2-lean`, `lib-stage2-lean-u1` and the shipped leaf all read 0.028 in the column, and no pair among them is separated by a sign test --- lean over the leaf 0.9899 at 9 of 19 with p 1, lean over its twin 0.9691 at 13 of 19 with p 0.17. **The ceiling that ran for eight runs still cannot be read at all**: `bq-scan-rem-gm-mulback`, the denominator, is parked, and what stands where it stood is claim 10 inside the family, **0.6441** on the basis and **0.6413** on the control.

**The pointer fills are not what this run was built to read, and they are here as the ceiling Run 27 made readable.** `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr` are the shipped fill's leaf and its unrolled form rewritten to walk a `Ptr`; they are refused for the library ([dead ideas][dead]) and timed as a ceiling and nothing else. The three spans --- `-u1-ptr` over `-u1`, `-u2-ptr` over `-u2` and `-u2-ptr` over `-u1-ptr` --- read **0.9690**, **0.9366** and **0.9369** on the basis and **0.9709**, **0.9372** and **0.9323** on the control, so the flag does not reach them. **The counted work says the same and says it harder.** In corrected instructions the three read 0.8946, 0.8347 and 0.8605 on the basis and 0.8952, 0.8354 and 0.8606 on the control --- reproducing each other to three figures, and reproducing Run 28's 0.8945, 0.8359 and 0.8605 as well, so the instruction saving is the same number on two compilers' worth of difference and on both regimes. What differs is what reaches the clock: **29.4%, 38.3% and 45.2%** of each saving on the basis and **27.8%, 38.1% and 48.6%** on the control, where Run 28 read 21.9%, 28.5% and 42.8% and Run 27 read 27%, 35% and 46%. So the conversion rate is not the flag's either, and its run-to-run spread is wider than the gap between these two halves.

**The two standing placement controls are still gone with the prune, and the straddlers stand at eight on each half.** `mut-odo-vecdims-add-in` against the arm it varies, and `build` against `mut-odo`, were the two pairs this file read every run for what a slot costs an identical body; both arms of each went to `Only` on 2026-09-04, so nothing on this roster turns an offset into a time. What is left that speaks to placement is the eight A/A pairs and post-run step 0's two twins. **One tracked fill copy of six moved between the halves and no other did**: the basis's six-copy group sits at offsets **0, 0, 0, 24, 0 and 4** and the control's at **0, 24, 8, 0, 4 and 0**, so one copy is at 8 where its counterpart is at 0, and the two-copy group is at 0 and 0 on both. Against the previous build of the basis's own recipe, Run 28's, `--delta` reads every one of those offsets PRESERVED with no address surviving to the byte. **The straddlers are eight on each half**, where Run 28 read eight and seven, and the twins name six of each eight by byte identity --- `fillStage2Short`, `fbMutOdoVecdimsAddInLeafU2` twice, and its `Down`, `Last` and `Ptr` forms --- refusing the same two offsets, 53 and 42, on both. **The 3% [the open list][open] has asked about since Run 10 is not readable on this roster either**, and the entry says so.


## What the next run compares against

**Run 30's pair is NOT settled here, and this run's result is what a request has to decide it against: the flag is worth nothing to the shipped fill family and a fifth to `bq-expand`, so the regime question is answered for the arms the library ships and open for nothing else.** What this run leaves settled is the basis recipe, the dead-spot form decided on 2026-09-02, which has now published six runs, and the regime it publishes in, `-fspec-constr`, which registration (1) shows the tables may go on being read in even though `Data/Array/Internal.hs` does not set it. **The compiler question stays closed**, as Run 27 closed it and Run 28 confirmed it; this run did not ask it, both halves being ghc-9.12.4. **What the roster leaves open is where the next run's work is, and this run names it rather than deciding it**: three of the eleven registrations were killed and every one of them was an argument that a change could not reach a population, so (2), (7) and (11) are the record of which such arguments failed and where --- (7) on `rev` for the second run running. **And two code questions stand named.** Stage ten costs a pass where neither of its changes fires, at 1.0229 and 1.0240 on `rev` and 1.2171 on `small`, so a route that skipped `zerosOutermost` where no stride is 0 would be worth timing. And `lib-stage1` now fills through `fillStage2`, which closed the `bcast` and `bcastmid` gaps registration (8) was written around, leaving `runs` and `block` --- where the two routes genuinely part, at 0.2320 and 0.4225 --- as the only populations that still price the dispatch.

**The REGIME variable was asked for the first time, on a roster no run has read it against, and it answers in two parts.** Five pairs before this one varied the compiler: Run 24 read the two 5.7% and 6.8% apart on the instructions of two pure arms; Run 25 read `list` 1.10% apart in time and 0.33% in counts; Run 26 found twenty-four of twenty-six arms inside a percent in counts and the two `Ptr` arms outside at 0.6219 and 0.9295, which was GHC #27778; Run 27 worked it around and read every arm inside a percent; Run 28 confirmed it at a counted geomean of 1.0027. **This run varies `-fspec-constr` instead, and the counted geomean is 0.9088 over the eighteen arms that carry a corrected time** --- the flag removing about a twentieth of the instructions across the board. **The two parts are which arms that reaches.** In TIME twelve of the eighteen sit within 1% of 1 and the six outside are the `bq-expand` and `list` families alone; in COUNTS every arm moves, and the ones whose time does not move convert none of their saving at all, `time/counts` running 1.0340 to 1.0566 across the fill family against 1.0161 and 0.9926 for `bq-expand` and `list`. What does not close at the class level is worse than the compiler pairs' was: **all eleven populations carry a `list` move past the 0.7% bar**, from `rev`'s 0.9088 to `bcast`'s 0.8533, so every class-level comparison of the halves is an ordering and not a subtraction, where Run 28 had four classes past it and six inside.

**What Run 29 leaves the next run to read against, and the first item is a figure that did NOT move. The box is where Run 28 left it**, its gate machine check reading **-0.17%** on `list`'s net against Run 28's own fingerprint, over all 19 shapes, worst `cnn-L1-6x6-c1` at -1.75% and none past 5% --- inside the 3% that would make it a box move, as Run 28's was against Run 27's. So this run publishes INSIDE the third machine era rather than opening a fourth, an absolute crosses between it and Run 28 unadjusted, and the fingerprint below is the one a Run 30 machine check reads against. **The floor is 0.51% on the basis and 0.82% on the control**, read over the EIGHT A/A pairs this roster carries, and the restricted figure over the four pairs that carry back to Run 10 is **0.26%** and **0.82%**. The basis figure is `mut-odo-vecdims-add-in-leaf-u2-aa-distant`'s for a second run, so the whole-set floor is the fill family's own property rather than one evening's, which is what registration (9) asked by hand; the control's is `bq-expand-aa-distant`'s, and it carries the restricted figure there too, which is why that half's two readings are equal. A margin ON THE MAIN SET is judged against the whole-set pair, 0.51% and 0.82%; a margin on a class is judged against THAT class's own floor, which its block prints and which runs from `window`'s 0.38% to `runs`'s 3.09% on the basis --- so `rev`'s registration (7) verdict clears 0.40% and not 0.51%. Every verdict in this file uses the whole set of its own population's pairs, which is what `whole-set` names; which of the two figures a margin ought to clear at all is [the open list][open]'s question and not settled here. **The two columns below may NOT be differenced**, `list` having moved **12.12 points** on this run's main set against the 0.7% bar, and by 9.12% to 14.67% on every class --- so this section's two columns are an ordering, where Run 28's could be subtracted.

**Registered with the pair.** Run 29's eleven registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. Eight held and three were killed. **What a next registration should take from this one is the SHAPE of the three killings and not their subjects**: not one fell on an item predicting an EFFECT, and all three fell on items predicting a NO-OP --- something argued from the code to be unreachable, tested on the populations the argument said could not move, and moving. That is what Run 28 found of its own four, on a different variable and a different roster, so two runs now say it. On this roster an argument that a change cannot reach a population was the cheapest kind of clause to write and the least reliable, and (2), (7) and (11) are the three instances; five other no-op arguments held, so the converse does not follow. **Nothing here was killed by the box**, which is what Run 27's (3) was and what this run's machine check makes impossible to repeat.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at this run's own area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 29 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 28 to Run 29 --- which, the box not having moved, is a SUBTRACTION and not a bridge. Over the **18 arms both rosters time and both give a corrected time** the basis column runs from **0.9896** on `mut-odo-vecdims-add-in-leaf-u2-ptr` to **1.0213** on `lib-stage1`, sixteen of the eighteen inside 1% of 1 and `list` itself at **0.9999**; the one arm outside by more than a hair is `lib-stage1`, whose route took `fillStage2` on 2026-09-11, so it is a code change and not a drift. The reader names five more as being in one run only --- `libunord-stage10-sum`, this run's, and the four parked with it. **The table below is this run's own two halves and no earlier run's**, nine strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in ONE COMPILE FLAG and in nothing else:

| strategy | Run 29 (`-fspec-constr`, dead-spot, -A32m, 9.12.4) | Run 29 (plain -O1, dead-spot, -A32m, 9.12.4) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.051** | 0.045 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.031** | 0.027 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.028** | 0.026 |
| `mut-odo-vecdims-add-in-leaf-u1-ptr` | **0.030** | 0.025 |
| `mut-odo-vecdims-add-in-leaf-u2-ptr` | **0.029** | 0.024 |
| `lib-stage1` | **0.029** | 0.028 |
| `lib-stage2-lean` | **0.028** | 0.024 |
| `lib-stage2-lean-u1` | **0.028** | 0.026 |
| `bq-expand` | **0.113** | 0.127 |

**READ THE SECOND COLUMN AS A RATIO AND NOT AS A SPEED.** Every entry is that arm's net over `list`'s net in ITS OWN half, and `list` is 13.79% slower without the flag --- so the control column reads lower for every fill because its denominator is slower, not because the fill is faster. **That is the DIRECTION and not an identity**: each entry is winsorized per row within its own half, so dividing an arm's two entries does not reproduce its `--compare` figure and is not meant to --- on `lib-stage1` the two entries divide to about 1.04 where the cross reads 0.9945, which is the capping and not a disagreement. The two columns may not be subtracted, and the arm-by-arm reading of what the flag is worth is in the head, off `--compare`, where the reference is not divided out.

**A published geomean is over the same 19 shapes, and two halves of one run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered, which is what an -O1 reading cannot do at an 8% baseline shift. **THE TABLE ABOVE IS NOT SUCH A PAIR**, `list` having moved **12.12 points** on this run's main set --- which is the same fact as `list` being 13.79% slower without the flag, read the other way round, the two appearing in this file four sections apart: the two columns may NOT be differenced. Eight of the nine rows print lower on the control --- `bq-expand` is the exception, 0.113 against 0.127 --- and the cells are ratios to `list`, so what the eight show is the denominator moving and not eight arms getting faster, which is the whole reason the bar exists. **So the reading of this table is that it cannot be read as a difference at all**, and the arm-by-arm answer is at the head of this file, off `--compare`, where the reference is not divided out: twelve of the eighteen arms within 1% of 1, and the six outside being the `bq-expand` and `list` families alone. `--compare --bridge` is not the way round it either: it cancels a moved reference by taking each arm as a ratio to `list` in its own run, which is the mode for a moved BOX, and these two halves share a box, so what it would cancel is the pair's own variable. **And a bridge figure does not carry across the 2026-09-10 `no_net` ruling**, which is worth knowing before anyone re-derives one: Run 27's write-up published 1.0468 with nine arms outside the band, and today's reader gives that pair 1.0737 over 26 arms with SIX outside, five of them `libunord` rows whose denominator the correction had emptied. Take any size from the counted work instead, which owes criterion nothing and puts `bq-expand` at 0.7687 and `list` at 0.8854 in instructions against 0.7810 and 0.8788 in time, with every other arm near 0.95 in counts and near 1.00 in time. **A pair that varies the allocation area is the other exception, and its two halves may never be subtracted from each other.** `list` moved **9.20%** between Run 14's halves, **5.13%** between Run 15's and **16.51%** between Run 16's --- and this run's 12.12 points sit inside that range, which is the company a regime pair keeps. **Run 16's is the largest of the three and was registered to be the smallest**, on the reasoning that its two halves both sit at enlarged areas where the earlier two each crossed the default --- a prediction refuted by its own run, and the refutation is the finding: what moves the baseline is not the distance from the default but the in-process deflation, which at roster scale is worse at 64 MB than at 32 MB by more than the whole default-to-32 MB step was worth. So the exception widens rather than narrowing, and it covers every pair that varies the area at all --- and, on this run's evidence, every pair that varies the optimiser. Every cell of such a pair's second column is scaled by a denominator the pairing moved: read it for the pairing's direction, take no strategy quality off it, and read the arm-by-arm comparison at the head of this file instead, which divides absolutes rather than ratios.

**The control half's own standings on the arms this run's roster brought, which no table here carries, every published table being the basis half's.** Read off the control half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9658** at 15 of 19 (0.9693 at 10 of 19) and against `mut-odo-vecdims` **0.6413** (0.6441); `lib-stage1` against `-u2` 1.0598 at 7 of 19 (1.0538 at 6 of 19); `lib-stage2-lean` against `-u2` 0.9857 at 12 of 19 (0.9899 at 9 of 19) and against `lib-stage1` **0.9301** at 17 of 19 (0.9393 at 14 of 19). **The three pointer spans hold their direction and their size without the flag**, which is what this pair adds to Run 27's compiler reading: `-u1-ptr` against `-u1` reads **0.9709** at 14 of 19 (0.9690 at 15 of 19), `-u2-ptr` against `-u2` **0.9372** at 14 of 19 (0.9366 at 14 of 19) and `-u2-ptr` against `-u1-ptr` **0.9323** at 16 of 19 (0.9369 at 16 of 19) --- three spans agreeing between the halves to within half a point, on a pair whose reference moves twelve. **Where the two halves differ at all it is in strength and not direction**, and every ordering the basis half reads holds without the flag.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section][pershape]. The two tables below are installed from this run's own JSONs in the per-shape form the README describes, which replaced a fourteen-column one on 2026-09-04.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 5.81 us | 0.085 | `lib-stage2-lean-u1` 0.080 | `mut-odo-vecdims-add-in-leaf-u1` 0.059 |
| `cnn-L1-6x6-c1` | 3 | 324 | 6.97 us | 0.096 | `lib-stage2-lean-u1` 0.073 | `mut-odo-vecdims-add-in-leaf-u2` 0.073 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 110 us | 0.068 | `lib-stage2-lean` 0.035 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.040 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 355 us | 0.048 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `gather48-src-50` | 3 | 22500 | 418 us | 0.052 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `stretch-coprime-r7` | 13 | 60060 | 996 us | 0.033 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.39 ms | 0.057 | `lib-stage2-lean` 0.033 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `stretch-primes` | 89 | 250357 | 3.89 ms | 0.028 | `lib-stage2-lean` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 15.6 ms | 0.044 | `lib-stage2-lean-u1` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `vgg-14-c512-k3` | 3 | 903168 | 18.1 ms | 0.057 | `lib-stage1` 0.034 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 17.8 ms | 0.034 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-inner256` | 256 | 1750784 | 31.6 ms | 0.032 | `lib-stage2-lean-u1` 0.027 | `mut-odo-vecdims-add-in-leaf-u1` 0.027 |
| `stretch-pow2stride` | 64 | 1769472 | 27.3 ms | 0.128 | `lib-stage2-lean-u1` 0.126 | `mut-odo-vecdims-add-in-leaf-u1` 0.126 |
| `stretch-r5-8x432` | 8 | 1769472 | 32.4 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-square-1341` | 1341 | 1798281 | 28.3 ms | 0.095 | `lib-stage1` 0.083 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.074 |
| `stretch-bigstride` | 3 | 1800000 | 47.4 ms | 0.035 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.014 |
| `stretch-tab7MB` | 2 | 1800000 | 36.5 ms | 0.063 | `lib-stage1` 0.024 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 38.2 ms | 0.023 | `lib-stage1` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `stretch-wide-2xM` | 2 | 1800000 | 36.3 ms | 0.062 | `lib-stage2-lean-u1` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 848 us | 0.032 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `bcast-src512` | `bcast` | 3515 | 1799680 | 25.7 ms | 0.021 | `lib-stage1` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 26 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |
| `bcast-src64` | `bcast` | 28125 | 1800000 | 25.6 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |
| `bcast-src8` | `bcast` | 225000 | 1800000 | 25.8 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 36.2 ms | 0.062 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.3 ms | 0.058 | `lib-stage2-lean` 0.012 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 3.76 ms | 0.021 | `lib-stage1` 0.014 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 44.2 ms | 0.037 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.012 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 38.7 ms | 0.024 | `lib-stage1` 0.019 | `mut-odo-vecdims-add-in-leaf-u1` 0.021 |
| `block-run64-gap1` | `block` | 64 | 131072 | 1.96 ms | 0.022 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.018 |
| `block-run64-gap64` | `block` | 64 | 131072 | 1.98 ms | 0.027 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `block-run64-off7` | `block` | 64 | 131072 | 1.99 ms | 0.027 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `block-run64-page` | `block` | 64 | 131072 | 2.07 ms | 0.032 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `block-r3-vol64` | `block` | 64 | 262144 | 3.98 ms | 0.022 | `lib-stage2-lean` 0.021 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.018 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 847 us | 0.032 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 843 us | 0.032 | `lib-stage2-lean` 0.015 | `mut-odo-vecdims-add-in-leaf-u2` 0.015 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 26.3 ms | 0.021 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 26.2 ms | 0.024 | `lib-stage1` 0.019 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.05 ms | 0.029 | `lib-stage1` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.03 ms | 0.029 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.3 ms | 0.058 | `lib-stage2-lean` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 25.8 ms | 0.027 | `lib-stage2-lean-u1` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `flip-fwd-rows96` | `flip` | 96 | 1800000 | 26.4 ms | 0.027 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 28.4 ms | 0.047 | `lib-stage2-lean` 0.049 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.046 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 110 us | 0.067 | `lib-stage2-lean` 0.034 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.040 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 418 us | 0.053 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `rev-primes` | `rev` | 89 | 250357 | 3.92 ms | 0.028 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `runs-65536` | `runs` | 65536 | 1769472 | 25 ms | 0.027 | `lib-stage1` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-16384` | `runs` | 16384 | 1785856 | 25 ms | 0.028 | `lib-stage1` 0.026 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-4096` | `runs` | 4096 | 1798144 | 25.8 ms | 0.027 | `lib-stage1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `runs-1024` | `runs` | 1024 | 1799168 | 25.3 ms | 0.028 | `lib-stage2-lean` 0.029 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-512` | `runs` | 512 | 1799680 | 25.5 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-256` | `runs` | 256 | 1799936 | 25.4 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-7` | `runs` | 7 | 1799994 | 28.8 ms | 0.037 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-2` | `runs` | 2 | 1800000 | 36.6 ms | 0.063 | `lib-stage2-lean` 0.027 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-3` | `runs` | 3 | 1800000 | 32.9 ms | 0.052 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 31.3 ms | 0.044 | `lib-stage2-lean-u1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `runs-5` | `runs` | 5 | 1800000 | 30 ms | 0.042 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-9` | `runs` | 9 | 1800000 | 28.3 ms | 0.034 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-96` | `runs` | 96 | 1800000 | 26 ms | 0.028 | `lib-stage2-lean` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 26 ms | 0.029 | `lib-stage1` 0.028 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `scaled-r5` | `scaled` | 13 | 15015 | 240 us | 0.032 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 919 us | 0.026 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.024 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 4.56 ms | 0.032 | `lib-stage2-lean-u1` 0.034 | `mut-odo-vecdims` 0.032 |
| `small-patch-k5` | `small` | 5 | 150 | 2.65 us | 0.083 | `lib-stage2-lean-u1` 0.102 | `mut-odo-vecdims-add-in-leaf-u2` 0.060 |
| `small-bcast32` | `small` | 32 | 256 | 3.9 us | 0.056 | `lib-stage2-lean-u1` 0.070 | `mut-odo-vecdims-add-in-leaf-u2` 0.048 |
| `small-flat64` | `small` | 64 | 256 | 3.91 us | 0.064 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2` 0.062 |
| `small-patch-r5` | `small` | 4 | 256 | 4.8 us | 0.095 | `lib-stage2-lean-u1` 0.101 | `mut-odo-vecdims-add-in-leaf-u2` 0.071 |
| `small-row96` | `small` | 96 | 384 | 5.78 us | 0.046 | `lib-stage2-lean` 0.061 | `mut-odo-vecdims-add-in-leaf-u2` 0.045 |
| `window-28x28-k5` | `window` | 5 | 14400 | 252 us | 0.045 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 855 us | 0.095 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.19 ms | 0.058 | `lib-stage1` 0.034 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 8.63 ms | 0.057 | `lib-stage1` 0.033 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.15 ms | 0.055 | `lib-stage2-lean` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `window-32x32-c64-k3` | `window` | 3 | 518400 | 10.6 ms | 0.058 | `lib-stage1` 0.034 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `window-64x64-c16-k3` | `window` | 3 | 553536 | 11.2 ms | 0.059 | `lib-stage2-lean` 0.035 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.028 |
| `window-128x128-k7` | `window` | 7 | 729316 | 12.3 ms | 0.036 | `lib-stage1` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 |

**One row to read first, and it is a property of the shape set and not of any arm**: NONE of the thirty-six rows is a geomean over fewer shapes than the rest, where nine of Run 27's thirty-five were. Not one cell on either half sinks below the shared forcing term, against **seventy on Run 27's basis and forty-three on its control** as that run's write-up recorded them -- counts predating the 2026-09-10 `no_net` ruling, which today's reader gives as 25 and 8 -- so every row of both columns that carries a corrected time covers all nineteen shapes and no span in this file is recorded NOT READ for want of a population. Two changes did it, and neither is a measurement: the ruling of 2026-09-10 that a reducing consumer has no corrected time --- it hands back a scalar and never runs the pass being subtracted, so the fourteen `-sum` rows read `--` in `time` and `worst` rather than a ratio of two near-zero numbers --- and the retirement of every Fill arm over a list, which took the rest. **What it costs is one column's comparability**: `best outside family` can no longer name a `-sum` arm, so where Run 27's cross-class summary named a `-sum` consumer on seven of its ten rows --- `libunord-stage5-sum` on four, `-stage2-sum` on two, `-stage4-sum` on one --- this one names `lib-stage2-lean` and `lib-stage1` and nothing else, and the cross-class summary's `best outside family` column --- the one far below, not the fingerprint's just above --- is not to be read across the two runs.


## The claims the next run should test

**Run 29's verdicts first**, since a run reports breaks rather than re-deriving what held: **both live claims held, and neither the roster nor the pair took a level or an ordering off either.** Claim 10's pair is timed on both halves and agrees between them to three thousandths; claim 7's five levels all reproduce on the basis over the shapes it is pinned to. What this run adds is not a break but a qualifier the manifest did not carry: two of claim 7's levels are the flag's, reading 2.06x and 22.38x under it and 2.76x and 24.90x without it, so the preamble's *all of them are `-fspec-constr` claims* is now a measured statement about claim 7 rather than a note on the regime.

**The eight retired claims are not re-read here.** Claims 3, 4, 5 and 9 left the manifest at Run 19's write-up, on a sweep in which all thirteen held on both of that run's halves; claim 8 left on 2026-08-29 on a decision, its subject the pure tier being no longer what ships; claims 2 and 6 left on 2026-08-28 with the parking of the arms their surviving links turned on; and claim 1 left on 2026-09-04 with the prune, which parked every rung below its top. The numbered items below say what each was in a clause. This run does not re-derive any of them and quotes none as its own --- and for claim 1 that is still the only way it could be read at all, three of the four arms its links name being untimed, which is what retiring it gave up and what a run that re-times them would get back. **Eight is the count, and it was written as seven for two runs**, the enumeration above having once skipped claim 8; it names all eight now, claim 8 among them, and the numbered list below has carried it as retired since 2026-08-29.

**Claim 7 held on the basis with every level reproducing to the digit, and the control half is where it has something new to say.** The claim reads its levels over the eighteen main-set shapes it was re-cut to on 2026-09-05; this run times nineteen, `cnn-L1-6x6-c1` being the nineteenth and sitting outside the pin. **Read on the eighteen, the basis gives the `libunord` consumers 0.00x, the mutable fills and the `liblist` consumers 1.00x, `bq-expand` 2.06x and `list` 22.38x** --- every one of them Runs 23 to 28's figure over the same shapes, so this is the seventh run to read them unmoved. What this run's own nineteen show instead, 2.11x and 22.64x, is the median's population and not what any arm allocates. **The control half parts on two of them**, reading `bq-expand` at 2.76x and `list` at 24.90x while every other level is unmoved --- so the two arms `-fspec-constr` speeds up are the two whose allocation it changes, and the claim is a `-fspec-constr` claim in a sharper sense than the manifest's preamble knew: read at plain -O1 two of its five levels are different numbers.

**Claim 10 is the manifest's only live ordering, and this run reads it without the flag for the first time.** `mut-odo-vecdims-add-in-leaf-u2` over `mut-odo-vecdims` reads **0.6441** on the basis, ahead on 18 of 19 shapes at sign p 7.6e-05, from 0.358 on `stretch-wide-2xM` to 1.012 on `stretch-pow2stride`; and **0.6413** on the control, ahead on 17 of 19 at sign p 0.00073. **The two halves agree to three thousandths**, which is what registration (10) was written to find out: what the leaf fusion buys is the design's and not `-fspec-constr`'s, and every earlier reading of this ordering --- taken under the flag, since no run before this one built a half without it --- is not a reading of the regime after all. **Quote both statistics here**: the published-column ratios are 0.5563 and 0.5715, nine and seven points away from the paired figures, because the column winsorizes per row and the pair does not.

**Readings:** `mut-odo-vecdims-add-in-leaf-u2` / `mut-odo-vecdims` 0.6441, 18 of 19, sign p 7.6e-05. 1 of 1 registered ordering held.

Restated as the predicates the next run checks, and carrying no reading of its own: the figures each was last measured at are in the `Readings:` paragraphs above, so an entry here changes when a claim is re-aimed and not when a run moves a margin. **All of them are `-fspec-constr` claims, the regime they are read in** --- the shipped file does not set the flag, measured irrelevant to the shipped family --- so they are the set that decides, and a run at -O1 would test Run 7's instead, the two differing in more than their numbers. **They are read in the caller's allocation regime now**, every figure here being taken at the `-A32m` Run 16 promoted to the basis and every horde-ad test and benchmark bakes since 2026-08-21; the gap Runs 14, 15 and 16 priced against a prevailing `-A1G` is closed, and no claim below needs qualifying by it. **And all of them are read against a measured drift band rather than a layout span**, which is what the last three runs bought. A roster *order* change alone moved arms 0.966 to 1.142 between Run 9 and Run 10, and that is what a margin used to have to clear; with the layout pinned, a repetition moves an arm by at most 3.3% on Run 11's reading and 2.1% on Run 23's, most of them by under a point, so a margin above a few percent is now evidence of a strategy. **Run 13 is the first pair here to hold every tracked loop at one offset in both halves**, which is what lets its arm-by-arm comparison be read as the package costing nothing rather than as two terms cancelling. A claim resting on an arm whose own loop the shim skipped --- `list`'s, which is library code --- is still decidable nowhere until that loop is read. **And the pinning claim is measured only in its weak form**: adding `mut-flat-gm-nosum` left every tracked loop at the same address, but a `Force` arm reuses a rostered function and emits no code for emission order to move. The strong form wants an arm that emits its own, and until one is added the claim covers additions that cost nothing to place.

**The list needed reading against a moved roster again this run, and this time the roster took nothing off a claim.** One arm landed and four left, all five outside both live claims; claim 10's pair is timed on both halves and claim 7 keeps all five of its levels, where Run 28's roster had taken the 2.00x tier's last timed member. **What the roster raises is what Runs 22, 24, 26, 27 and 28 all raised, and this run answers it as Run 28 did**: **seven** timed arms read below `mut-odo-vecdims`, and every one of them is a fill that writes the result --- `lib-stage2-lean` and `lib-stage2-lean-u1` at 0.028, `mut-odo-vecdims-add-in-leaf-u2` at 0.028, `lib-stage1` and `-u2-ptr` at 0.029, `-u1-ptr` at 0.030 and `-u1` at 0.031. Run 28's nine became seven by parking two leaf forms and not by any arm slowing. **What is left for the next run is the decision Run 28 left**, unchanged and now measured on a second regime: `lib-stage2-lean` is the branch's own driver, it ties the shipped leaf at the head of the table, and the flag is worth nothing to either of them.

1. **Retired 2026-09-04** with the prune, which parked every rung below `mut-odo-vecdims`; its last reading is Run 24's, above, where it held on both halves. What it was: `mut-odo-vecdims` < `mut-flat-gm` < `bq-mut-runs-gm-mulback` < each of `bq-scan-rem-gm-mulback` and `bq-odo-gm-mulback`, the whole ordering read on unconditional arms --- **the ladder the `needs` column draws**, each link pricing one thing the implementation is allowed to ask for. **The foot rung retired 2026-08-29**, on a decision rather than on a reading: it registered the two arms needing nothing at all as a tie, so that either was what would ship if the mutating `Vector` method were refused upstream, and it had read as a tie on every run that carried it. Both arms stay rostered and stay in the rung above, `--pair bq-scan-rem-gm-mulback bq-odo-gm-mulback` recovers the reading whenever the upstream answer wants it, and its last is Run 21's, above. The middle link is the one the README has seen a layout term move --- 0.9708 at 15 of 24 on Run 10's unaligned half against 0.9293 at 22 on its aligned one --- and on a placed layout it has now read the aligned figure five runs running. The ordering has survived nine runs, two changes of basis, two repetitions and three compilers.
2. **Retired 2026-08-28** with the parking of `offtab`, the arm its surviving link turned on --- the other, `bq-scan-rem-gm-mulback`, is timed still. What it asked, since the settlement of 2026-08-24 re-aimed it, was where the arms needing something other than the fix sit --- `offtab` behind `bq-scan-rem-gm-mulback`, a mutable `Int` scratch priced against needing nothing --- and its last reading is Run 20's, above. **Its second link, `bq-expand` behind `mut-odo-vecdims`, had gone already, 2026-08-26** on the reading above, its condition having been spent since 2026-08-24 --- what it priced was the branch's own code against its replacement, and the replacement is now the branch's own code. It was the widest ordering the manifest carried, and `--pair` recovers it, both arms staying rostered and timed.
3, 4, 5. **Retired at Run 19's write-up**, on a last reading in which all three held on both compilers, and for the reasons in the settlement paragraph at the foot of this section. Their numbers are left standing here rather than reused: a verdict recorded against *claim 4* in an earlier run's file still means what it said. What they were, in a clause each, so that nothing is recoverable only from a manifest diff --- claim 3, that a mul-back output pays on the `bq-expand` build; claim 4, that the scan build ties its own build control while beating `bq-expand`, a tie Run 17 promoted to an ordering and three runs then read as one; claim 5, that `bq-expand` beats `bq-gen`, whose refutation of the generate-per-element build stands on Runs 7 and 8. **What none of the three could still foreclose is the point**: every one asks where `bq-expand` sits among arms nothing ships, on a branch whose fix is the `mut-odo-vecdims` family. The arms all stay rostered and timed, so any of these orderings is one `--pair` call away.
6. **Retired 2026-08-28** with the parking of `gen-quotrem`, the arm its only link turned on. What it asked: `gen-quotrem` ties `list`, the first attempt's arithmetic ceasing to be dearer than the list's allocation once the flag takes its own allocation to 1.00x against the list's 23.5x --- the mixed picture this suite exists to have refuted, arriving by a route nobody proposed. Its last reading is Run 20's, above; the `cm-gather` < `list` half was untimed throughout and stands as Run 8's. What goes with it is the standing advice to check `list` as an anchor before blaming a strategy, which is now nobody's claim and is why the machine check reads `list` net per shape every run.
7. Allocation, median multiples of the result **over a pinned shape set**: the `libunord` consumers 0.00x, the mutable fills and the `liblist` consumers 1.00x, `bq-expand` 2.06x, `list` 22.38x, every one of them read over **the eighteen main-set shapes claim 7 is pinned to**, of the nineteen this roster times, which are a strict subset of the 24 Runs 15 to 23 shared --- **re-pinned 2026-09-05, six of those 24 having been retired from timing the day before**, so the old population no longer exists and the predicate could not otherwise be evaluated; re-listed 2026-08-28 with the parking, and 2026-09-04 with the prune, which parked the two tiers between the fills and `bq-expand` outright and left the 2.00x one to the two `liblist` arms, named in the paragraph above; **re-aimed 2026-09-11 at the levels the roster times**, the 2.00x level having lost its two arms to the ruling of 2026-09-09, which took the ports to 1.00x and then off the timed roster, and the 0.00x level, read beside the claim since Run 23 on the `libunord` Fill arms, being their consumers now that those arms are checked and not timed, a consumer returning no result at all. Every level has reproduced over the eighteen on Runs 23 to 29, seven runs, which is what makes this the claim to check first when anything else moves. **The pinning is the predicate and not a caveat on it**: Run 24's published column reads 2.40x for `bq-expand` on its own 26, 2.35x on the 24 and 2.06x on these 18 --- so a level compared across two shape sets moves for a reason that is neither a code change nor a slot, and only a level that moves ON ONE POPULATION is a code change. **Read the levels and the cells as two questions**, which Run 19 is the run that separated: its levels all returned while the cross-half cell agreement fell to 1016 of 1080, where the 9.14 pair had 1072 --- so a compiler can reallocate within a tier without moving any tier, and only the cell count sees it.
8. **Retired 2026-08-29**, on a decision and one day after its last re-aiming. What it asked: that every pure arm in the fast tier run its output through the single in-order `vGenerate` over an `m`-length table, so a `bq-*` arm falling behind loses on its table build and not on its output. Its last reading is Run 21's, which found it true and its subject smaller again --- nine arms at or below `mut-odo-vecdims` and not one of them a `bq-*`, so the structure it described governed a tier starting a third of the way down. What retires it is that subject: the pure tier is no longer what ships, and a claim quantified over it was being re-aimed and re-read every run to say so. It was also the one claim with no named invocation, read off the table by eye throughout, so nothing mechanical goes with it.
9. **Retired at Run 19's write-up with 3, 4 and 5**, and for a reason of its own worth keeping: its per-shape half was answered rather than abandoned. `bq-expand-b`'s two best cells were `stretch-inner1` and `stretch-wide-2xM` in every run from Run 8 to Run 16 --- the rank-2 views with one huge outer dimension where seeding from `enumFromStepN` replaces the whole `concatMap` build --- until **Run 17 read `stretch-inner1` and `stretch-square-1341` instead**, which registered the follow-up. Runs 18 and 19 both read the original pair back, on four compilers between them, so the excursion was one run's and the mechanism is cleared of picking the wrong shapes. The geomeans were never the stable part, and the series is why: across Runs 8 to 13 `bq-expand-b` / `bq-expand` read 0.996, 0.9678, 0.9943, 0.9819, 0.9923 and 0.9909, its sign test crossing into significance only on the last of them, while `bq-expand-zf` / `bq-expand` went 3.6% behind, then level at 1.0028, then 1.0325, 1.0197, 1.0256 and 1.0265. Both series were closed at Run 13 and not extended per run, and a closed series in a live manifest is maintenance without a question --- which, with the follow-up spent, is the whole case for retiring it.
10. `mut-odo-vecdims-add-in-leaf-u2` < `mut-odo-vecdims`, the shipped fill against the family root --- **what the leaf fusion buys**, the one thing the roster still prices that the library has taken. Registered 2026-09-04 with the prune, in claim 1's place.

**What the parking of 2026-08-28 did to this set**, recorded in prose because a live item carries a predicate and no reading, and because naming a parked arm inside one is now a `--lint` failure. Claims 2 and 6 retired with `offtab` and `gen-quotrem`, the arms their surviving links turned on. Claim 7 lost three of the arms that carried its levels --- `gen-quotrem` at 1.00x, `bq-mut` at 1.33x and `offtab` at 2.00x, every figure of theirs ending at Run 20 --- but no level with them: `offtab-scan-rem` carries the 2.00x tier, the mutable fills the 1.00x and the scan family the 1.33x, so the list is the same six levels it was. Claim 8 was re-aimed off `bq-expand-zf` and `bq-gen`, its span running from the leading tier to `offtab-scan-rem` instead; the readings above are the last that name the two. Nothing was retired on a reading here, the parking being a decision about what is worth a bench, and the two claims that went had no link left that a run could measure.

**What the prune of 2026-09-04 did to this set** ([what the benchmark does](../README.md#what-the-benchmark-does)), read on the first run after it. Claim 1 retired, every rung below its top parked, and claim 10 is registered in its place, the one live ordering. Claim 7 lost the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x, which have no timed arm left, and keeps `offtab-scan-rem`'s 2.00x through the two `liblist` arms; and it lost its pinned population as well, six of the 24 shapes it named having been retired the same day, which this run re-cut to the eighteen that remain. The ruling of the same day on the short bodies parked `lib-stage2-short` and `lib-stage2-short-lean`, whose last readings stand as Run 24's. The class properties keep their arms, `mut-odo-vecdims` and `bq-expand` both staying timed --- **and property 1's two counter-examples are down to one**: `gen-unsafe`, which carried a `worst` above 1 in seven of ten populations as the baseline's own control, is parked, so the only arms above 1 now are the library-shaped ones and the `list` twins.

**Two homes, and which carries what.** Each live claim has a prose paragraph here and a numbered predicate at the foot, and they divide: the PROSE carries this run's figures and what moved, the ITEM carries the predicate the next run checks and no reading at all. A RETIRED claim keeps its numbered item, which says in a clause what it was, and gets no prose paragraph --- Run 20 wrote two and they restated their own items clause for clause, because stripping the figures had removed the only thing that distinguished them. Each ordering is one line of `--claims`, whose manifest now carries the registered expectation --- the direction of the geomean, a tie by sign test, or claim 9's two best shapes --- and prints HELD or BROKE beside the paired geomean, interval and sign test. `--claims --in-place` then installs that arithmetic as each claim's `Readings:` paragraph above, so a run no longer transcribes it at all; what stays the reading's is whether a HELD margin moved and whether a movement clears the floor, and a BROKE is what obliges the paragraph above its reading to be rewritten rather than requoted. **A claim with no named invocation is a gap in this list, not a claim to be checked by hand**: where a session has to invent the computation it will invent a wrong one, which is how claim 7 came to be read off the raw fitted bytes, explained by a mechanism the previous pair refutes, and then "corrected" onto a rounded print. It has `--compare --alloc` now, and no live claim is without one since claim 8 retired. **The general form, and it is a standing instruction rather than an observation: if a write-up hand-rolls a script to answer something the reader should answer, that is a defect report against the reader** --- fix it there, before the sentence it was written for, or the next run invents its own wrong version. **Two riders, both bought on Run 19.** A new MODE joins the guards its siblings already have, and is checked against them rather than written beside them: `--counts` shipped able to be given without the `--compare` it reads, silently printing the default table and exiting 0 --- the unread-flag family exactly, added next to four sibling readings of `--compare` that were every one of them already guarded, and joining none. **And an instrument may be BACKED OUT, which is not a failure of the write-up but a result of it.** Measure what it flags before shipping it: the obvious mechanical repair for stale paragraphs was built and returned 100 for the four that mattered, so it went, and the refutation with its numbers is under the tasks heading --- worth more than the mode, since it stops the next session building the same thing. A report that never empties is one nobody reads, which this file already knows about hints.

**And for each stride class, the same three properties, now carrying Run 29's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** Both held in every one of the eleven populations, on both halves --- the first in every regime, roster and layout the README has run, so `mut-odo-vecdims` --- and this is a statement about THAT arm and not about the route the library ships, which the paragraph below reads separately --- was never slower than the `list` it replaced, on any shape of any class; the second folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and read that way here for the third time. This is the property the classes exist to test, no geomean can state it, and a break would be the one result here to bear on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims` its `worst` is 0.128 on the main set and between 0.032 and 0.095 in the classes, highest on `window` and `small`, which tie there; the `bq-expand` clause is closest at `stretch-pow2stride` on the main set and at `window-64x64-k1x9`, 0.8042, in a class --- `scaled`'s 0.3351 being the widest of the ten per-class closest readings rather than the narrowest.

Beside property 1, and the case has simplified twice --- the prune of 2026-09-04 parked the arm that used to be half of it, and the retirement of 2026-09-09 took four of the five arms that broke the rest: **exactly ONE arm now breaks the WIDER statement this class set is really read for --- that no arm the library would ship is slower than `list` on any shape --- and it is the route the library ships.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. Of the five library-shaped arms Run 27 found breaking it at `runs-2` --- `libunord-stage1` 1.237, `liblist-stage1` 1.214, `lib-stage1` 1.198, `liblist-stage2` 1.078 and `libunord-stage2` 1.076 --- four are retired to `check` and are not timed here. Those five figures are RECOMPUTED from Run 27's own cells, that run's prose and its own installed table having printed different ones, and the five are the largest of NINE library-shaped arms over `list` there; a tenth row above it is `list-aa-adjacent`, `list`'s own A/A copy, at 1.0111. **What is left is `lib-stage1` at `runs-2`, 1.2107 on the basis and 1.101 on the control, and at `runs-3`, 1.0022 on the basis and under 1 on the control.** A sweep of every cell of all eleven populations on both halves finds no other timed arm above its shape's `list` anywhere, the baseline's own A/A copies aside. So the shipped route is slower than `list` on runs of two and three elements and on nothing else the library can produce, which is a sharper statement of the same defect than any run before this one could make.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves.** The `list` clause is closest at `small-patch-k5`, 0.06580, and under 0.053 everywhere else. The `bq-expand` clause is closest at `small-row96`, 1.00441, then `scaled-rank1-m1` at 1.00003 and `stretch-tall-Mx2` at 1.00000 --- three cells inside the margin the strict form would have failed on, which is why the margin is there and why it is not widened further.

3. **The allocation tiers survive and their ORDER is unbroken in all ten, on the main set and on every class but `small` --- outside the level clause since 2026-09-07, being the class built to break it, and read here for what it shows**: the mutable fills at the result vector, `bq-expand` between 1.00x and 2.75x it, `list` an order of magnitude above. On the main set the fills read 1.00x, `bq-expand` 2.06x and `list` 22.38x over the pinned eighteen, and Runs 23 to 27's own basis halves give every one of those levels over the same shapes. **What this roster removes is the 2.00x tier**, whose members were the list Fill arms now checked and not timed, and what it adds below the fills ON THE MAIN SET is the `libunord` consumers at 0.00x and the `liblist` consumers at 1.00x, the latter falling to a fill there. **Those two added levels are the main set's and no class's**, which the class blocks show and this paragraph used not to say: on `bcast` every `libunord` consumer reads 1.00x and on `small` 1.26x, the per-call constants of a short call swamping a consumer that allocates nothing per element. So `property 3 holds` in a class is the THREE published tiers holding --- the fills, `bq-expand`, `list` --- and never the five. `small` reads its fills at 1.27x and `bq-expand` at 1.34x, the per-call constants showing through a call under a microsecond. It is outside the LEVEL clause and inside the ORDER one --- that is what `every class but `small`` above means --- and its order is unbroken like the rest, which is why the head can say the three properties hold in all ten.

**SETTLED 2026-08-24 and APPLIED 2026-08-25, at Run 19's write-up rather than at the settlement**, so that the retiring orderings got one last cross-compiler reading and the retirement is recorded with it --- which is how Run 17 retired claim 4's tie, in prose at its write-up with the manifest taking it the same day. **They got it**: all thirteen held on both of Run 19's halves, and the eight that remain hold on both too, so each of the four retires on a reading rather than on a decision. `CLAIMS` in `read-run.py` carried claims 1, 2 and 6 alone after that settlement, and **claim 1 alone since 2026-08-28**, `offtab` and `gen-quotrem` having been parked. The test applied: an ordering stays only if it forecloses something anyone would propose again *and* can still break. What fails both is a figure, and figures live in the tables above. **Claims 3, 5 and 9 retire outright.** Claim 3 sets one output form against another on a build nothing ships, where every leading pure arm is a `-gm-mulback` already; claim 5's `bq-expand` / `bq-gen` says of itself that the refutation stands on Runs 7 and 8, and claim 6 keeps that family guarded through `gen-quotrem`; claim 9's two series are closed at Run 13 by this section's own words, and a closed series in a live manifest is maintenance without a question. **Claim 4 retires with them, its tie moving into claim 1** --- and what goes with it is the one place the manifest reads a *builder* apart from its output, which `--pair` recovers whenever it is wanted, both arms staying rostered. **Claim 1 becomes the ladder the `needs` column already draws**, gaining `bq-scan-rem-gm-mulback`, the best arm needing nothing at all, between `bq-mut-runs-gm-mulback` and `bq-odo-gm-mulback`: the first ahead of it at **0.9060** and **0.9171** on Run 18's two halves, 19 and 17 of 24, which is what a mutable `Int` scratch buys; and the second **tied** with it at **0.9902** and **0.9936**, 13 and 12 of 24 at p 0.84 and 1, so the two fastest pure arms were indistinguishable and either was what would ship if the mutating method were refused --- the rung this installed, and retired again on 2026-08-29. Its three existing links stay, the middle one redundant with the two new ones and carrying seven runs of history they do not. **Claim 2 keeps its number and changes its question** to where the arms needing something other than the fix sit: `offtab`, which needs only that `Int` scratch, behind `bq-scan-rem-gm-mulback` at **1.36** and **1.44**; and `bq-expand` behind `mut-odo-vecdims` at **2.09** and **2.13**, kept only while `Data/Array/Internal.hs` carried `bq-expand`, and to retire with the three `TODO: retarget` markers, which were one decision with it. **That condition was spent the same day and the link outlived it by two runs**: the file went to `vFillStrided` on 2026-08-24 and the markers went with the prose they marked, but nothing read the condition back, so Run 20 registered and read the link like any other and it retired only on 2026-08-26. Thirteen registered orderings become eight here, and seven when that second link finally goes; claims 7 and 8 stayed unmanifested prose. **The rewriting the ask paired with this was already done**: the eight *What the class says* paragraphs were written to the re-aimed properties at Run 18's write-up, and only the sentence asking for it survived.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 29 (SpecConstr against plain -O1, dead-spot, -A32m, ghc-9.12.4) records every class twice**, one process per class per half, so each block below has a control-half twin and the cross-half line under it is derived from both. **This run those lines say less than usual, and they say why.** `list` moved between the halves by 9.12% on `rev` at narrowest and 14.67% on `bcast` at widest, past the 0.7% that lets two columns be differenced, so every class's cross-half reading is disqualified as a reading of the pair's variable and stands only as an ordering. What each class still decides, and decides on both halves separately, is the three properties, its own floor, and whichever registrations name it --- which this run is where every one of the ten carries something and the ten differ in what. **Three items read EVERY class** --- (1), (2) and (9) --- so each block below has those three whatever else it has. **What varies is the class-specific items**, and counting only those: `runs` and `block` two apiece, `bcast` and `bcastmid` two, `rev`, `scaled` and `compose` one, `window` one, `small` the one figure item (7) prints there without a verdict, and `flip` NONE --- which is why flip's block below reports a control reading and no registration, the run's widest A/A cell being all it has to say.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- and both are read over the POPULATION, so the arm named here may lead on no single shape and the per-shape fingerprint below may name another, which is [the README's per-shape section][pershape]'s own point and not a disagreement --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the claims](#the-claims-the-next-run-should-test)) --- is bolded. **In practice that marks the FASTER of the two named arms, one cell a row**: the arm outside the family on THREE of the ten --- `rev`, `bcast` and `bcastmid`, on each of which `lib-stage1` leads --- and the family's own ceiling on the other seven, where the ceiling is faster. The class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 3 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. Item 2's own table is NOT among what that call prints, which is why the list above starts where it does: it comes from the separate `--block --in-place` call item 2 itself names. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.046 | 0.067 | **`lib-stage1`** 0.024 | `mut-odo-vecdims-add-in-leaf-u2` 0.025 | 0.40% |
| `bcast` | 6 | 0.022 | 0.062 | **`lib-stage1`** 0.018 | `mut-odo-vecdims-add-in-leaf-u2` 0.018 | 1.00% |
| `bcastmid` | 4 | 0.032 | 0.058 | **`lib-stage1`** 0.014 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 | 0.57% |
| `window` | 8 | 0.056 | 0.095 | `lib-stage2-lean` 0.031 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.026 | 0.38% |
| `scaled` | 3 | 0.031 | 0.032 | `lib-stage1` 0.025 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.025 | 0.56% |
| `runs` | 14 | 0.031 | 0.063 | `lib-stage2-lean` 0.028 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.026 | 3.09% |
| `flip` | 6 | 0.032 | 0.058 | `lib-stage2-lean` 0.026 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.026 | 2.66% |
| `block` | 5 | 0.026 | 0.032 | `lib-stage2-lean` 0.024 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.022 | 0.67% |
| `small` | 5 | 0.067 | 0.095 | `lib-stage2-lean-u1` 0.057 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.057 | 0.63% |
| `compose` | 4 | 0.027 | 0.032 | `lib-stage2-lean` 0.017 | **`mut-odo-vecdims-add-in-leaf-u2`** 0.016 | 0.76% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On SEVEN rows --- `window`, `scaled`, `runs`, `flip`, `block`, `small` and `compose` --- the bold sits in the CEILING column instead**, and it names three arms rather than one: `mut-odo-vecdims-add-in-leaf-u2-ptr` on `window` and `block`, `-u1-ptr` on `scaled`, `runs` and `flip`, and the shipped leaf `-u2` itself on `small` and `compose`. The reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break; this file overrides it for the two pointer fills, which the dead-ideas ruling refuses as a design rather than as a form the family could ship. **FOUR rows tie at three decimals and each is decided by its own block's fastest-timed-arm line**, which is the column read finer than it prints: `bcast`, `lib-stage1` against the ceiling `-u2` at 0.018, to `lib-stage1`; `flip`, `-u1-ptr` against `lib-stage2-lean` at 0.026, to the ceiling; `scaled`, `lib-stage1` against `-u1-ptr` at 0.025, to the ceiling; and `small`, `lib-stage2-lean-u1` against `-u2` at 0.057, to the ceiling. On `bcast` and `flip` on both the paired figure and the column disagree, and the column decides, as it does wherever the two part --- though by very different amounts: `bcast`'s pair parts from the column by one ten-thousandth and `flip`'s by six points, so the second is a three-decimal tie settling a large disagreement and is the weaker of the two calls. So the table's bolding and the class paragraphs' own `leads outside the family` differ by convention and not by measurement, and this sentence is where the two are reconciled.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.13* | *135* | *2.52x* |
| liblist-stage1-sum | -- | -- | 0.11 | 146 | 1.01x |
| liblist-stage2-sum | -- | -- | 0.12 | 147 | 1.01x |
| liblist-stage3-sum | -- | -- | 0.10 | 147 | 1.01x |
| liblist-stage4-sum | -- | -- | 0.10 | 147 | 1.01x |
| libunord-stage1-sum | -- | -- | 0.13 | 146 | 1.03x |
| libunord-stage10-sum | -- | -- | 0.04 | 157 | 0.02x |
| libunord-stage2-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage3-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage5-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage6-list-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage6-loop-sum | -- | -- | 0.06 | 157 | 0.01x |
| libunord-stage6-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage7-sum | -- | -- | 0.05 | 157 | 0.01x |
| libunord-stage9-sum | -- | -- | 0.05 | 157 | 0.02x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.37* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *158* | *0.00x* |
| lib-stage1 | 0.024 | 0.050 | 0.10 | 146 | 1.01x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.045* | *0.12* | *147* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.045* | *0.08* | *147* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.025 | 0.045 | 0.10 | 147 | 1.00x |
| lib-stage2-lean | 0.025 | 0.034 | 0.12 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.040 | 0.55 | 148 | 1.01x |
| lib-stage2-lean-u1 | 0.028 | 0.035 | 0.10 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.029 | 0.042 | 0.14 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.046 | 0.12 | 146 | 1.00x |
| *mut-odo-vecdims-aa* | *0.046* | *0.067* | *0.09* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.046** | 0.067 | 0.13 | 138 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.046* | *0.067* | *0.09* | *138* | *1.00x* |
| *bq-expand-aa-distant* | *0.103* | *0.176* | *0.14* | *129* | *2.52x* |
| *bq-expand-aa-adjacent* | *0.106* | *0.176* | *0.12* | *129* | *2.52x* |
| bq-expand | 0.106 | 0.176 | 0.18 | 129 | 2.52x |
| *list-aa-adjacent* | *1.000* | *1.004* | *0.28* | *87* | *23.43x* |
| list (baseline) | 1.000 | 1.000 | 0.27 | 87 | 23.43x |
| *list-aa-distant* | *1.000* | *1.002* | *0.31* | *87* | *23.43x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9960, worst cell 0.75% on `rev-gather48-src-50`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9985 on a worst cell of 0.46% on `rev-cnn-L1-24x24-c1`, its interval covering 1. The in-situ term reads 0.9971, 1.0130 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9977, which the correction amplifies by 1.72x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m23s, peak 95 MiB in use, 24 MiB max residency; the reader reads 36 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.07 ms per call raw, 3.92 ms net.

**Per shape, in the run's shape order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.067/0.053/0.028

**Across the halves:** 16 of the 18 arms are faster on this half and 2 slower, at a geomean of 0.9395, from `bq-expand-aa-adjacent` at 0.7688 to `mut-odo-vecdims-add-in-leaf-u2-ptr` at 1.0111, with `list` itself at 0.9088. **The baseline moved 9.12% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.067, tiers at 1.00x, 2.52x and 23.43x --- and `lib-stage1` leads outside the family at 0.024, priced against `mut-odo-vecdims` at 0.6501 over 3 of 3 shapes, a margin of 34.99% against this class's floor of 0.40%. **What is this class's own is registration (7)'s kill.** `rev` negates every stride and carries no zero stride and no pair of axes tied on stride, so `zerosOutermost` hands its list back unchanged and `routeUnord10` is `routeUnord7` there --- yet `libunord-stage10-sum` over `libunord-stage7-sum` reads **1.0229** against that 0.40% floor on the basis and **1.0240** against 0.21% on the control, past the floor on both halves. Run 28 killed its own registration (7) on this same class, where stage nine's route was likewise argued identical to stage six's; two runs running, `rev` is where an argument that a reordering cannot reach a population comes apart. Across the halves the class is disqualified as a reading of the flag, `list` having moved 9.12% --- its narrowest anywhere, and still thirteen times the bar.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2), and the repeat ladder that landed 2026-09-09, for Run 28 --- `bcast-src8` (`l` 1800000, `sInner` 225000), `bcast-src64` (`l` 1800000, `sInner` 28125) and `bcast-src512` (`l` 1799680, `sInner` 3515). The ladder is one source length per rung broadcast to the same 1.8 million elements, so what varies is how long a slice stage nine repeats and how many times; the two older views sit ABOVE every rung of it, at 2000 and 900000 source elements against the ladder's 8, 64 and 512, so the ladder extends the sweep downward rather than filling a gap inside it. It was added to find where the repeated slice meets the fill, and Run 28's registration (7) read no crossover on it; this run reads the class for stage ten instead, at item (6).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.61* | *53* | *1.00x* |
| liblist-stage1-sum | -- | -- | 0.50 | 62 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.49 | 62 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.48 | 62 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.01 | 74 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.52 | 62 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.52 | 62 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.51 | 62 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.50 | 62 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.49 | 62 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.01 | 74 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.27* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| lib-stage1 | 0.018 | 0.022 | 0.51 | 62 | 1.00x |
| lib-stage2-lean | 0.018 | 0.022 | 0.52 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.018 | 0.022 | 0.39 | 62 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.018* | *0.022* | *0.45* | *62* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.018* | *0.022* | *0.53* | *62* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.018 | 0.026 | 0.43 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.019 | 0.022 | 0.49 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.019 | 0.026 | 0.49 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.021 | 0.022 | 0.44 | 61 | 1.00x |
| *mut-odo-vecdims-aa* | *0.021* | *0.062* | *0.33* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.022** | 0.062 | 0.08 | 61 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.022* | *0.062* | *0.33* | *61* | *1.00x* |
| bq-expand | 0.097 | 0.099 | 0.68 | 46 | 1.00x |
| *bq-expand-aa-adjacent* | *0.097* | *0.099* | *0.67* | *46* | *1.00x* |
| *bq-expand-aa-distant* | *0.098* | *0.099* | *0.05* | *46* | *1.00x* |
| list (baseline) | 1.000 | 1.000 | 1.18 | 18 | 18.99x |
| *list-aa-distant* | *1.007* | *1.013* | *0.91* | *18* | *18.99x* |
| *list-aa-adjacent* | *1.010* | *1.015* | *0.74* | *18* | *18.99x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0100, worst cell 2.97% on `bcast-tall-Mx2`, and 2 of 8 intervals cover 1. The `sum-only` halves agree at 1.0003 on a worst cell of 0.41% on `bcast-inner8`, its interval covering 1. The in-situ term reads 1.0176, 1.0100 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0072, which the correction amplifies by 1.41x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h18m43s, peak 141 MiB in use, 42 MiB max residency; the reader reads 36 benchmarks over 6 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 27.1 ms per call raw, 26 ms net.

**Per shape, in the run's shape order (bcast-inner8, bcast-inner900, bcast-tall-Mx2, bcast-src8, bcast-src64, bcast-src512):** `mut-odo-vecdims` 0.032/0.021/0.062/0.021/0.021/0.021

**Across the halves:** 18 of the 18 arms are faster on this half and 0 slower, at a geomean of 0.9448, from `list-aa-adjacent` at 0.8532 to `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9964, with `list` itself at 0.8533. **The baseline moved 14.67% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.062, tiers at 1.00x, 1.00x and 18.99x --- and `lib-stage1` leads outside the family at 0.018, priced against `mut-odo-vecdims` at 0.6628 over 6 of 6 shapes, a margin of 33.72% against this class's floor of 1.00%. **This is where stage ten pays off best.** `libunord-stage10-sum` over `libunord-stage7-sum` reads **0.5850**, inside the registered 0.55, and no view has it behind stage seven on either half, the closest being `bcast-inner8` at 0.7106 and 0.7100. **And it is where the two fills meet.** With `lib-stage1` filling through `fillStage2` since 2026-09-11, `lib-stage2-lean` over it reads 0.9975 on the basis and 0.9967 on the control, inside the 3% registration (8) asked for --- so what Run 28 measured as the lean fill's 0.8635 lead was the two bodies the leaf lacked, and giving the leaf them closes it. Across the halves the class is disqualified, `list` having moved 14.67%, the widest of the ten.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.52* | *70* | *1.57x* |
| liblist-stage1-sum | -- | -- | 0.35 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.33 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.32 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.35 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.38 | 82 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 94 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.38 | 82 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.52 | 82 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.37 | 82 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.32 | 82 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.37 | 82 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.33 | 82 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.35 | 82 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 94 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *88* | *0.00x* |
| lib-stage1 | 0.014 | 0.019 | 0.35 | 82 | 1.00x |
| lib-stage2-lean | 0.014 | 0.020 | 0.36 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.014 | 0.020 | 0.38 | 82 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.019 | 0.027 | 0.45 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.022* | *0.034* | *0.28* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.034 | 0.31 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.022* | *0.034* | *0.34* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.024 | 0.031 | 0.48 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.024 | 0.034 | 0.46 | 78 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.058* | *0.30* | *76* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.032* | *0.058* | *0.31* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.032** | 0.058 | 0.24 | 76 | 1.00x |
| bq-expand | 0.087 | 0.132 | 0.45 | 64 | 1.57x |
| *bq-expand-aa-adjacent* | *0.088* | *0.132* | *0.43* | *64* | *1.57x* |
| *bq-expand-aa-distant* | *0.088* | *0.132* | *0.35* | *64* | *1.57x* |
| *list-aa-distant* | *0.998* | *1.000* | *0.99* | *30* | *21.22x* |
| *list-aa-adjacent* | *0.999* | *1.003* | *0.73* | *30* | *21.22x* |
| list (baseline) | 1.000 | 1.000 | 1.00 | 30 | 21.22x |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0057, worst cell 1.83% on `bcastmid-b200k`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 1.0008 on a worst cell of 0.38% on `bcastmid-b200k`, its interval covering 1. The in-situ term reads 1.0161, 1.0328 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0042, which the correction amplifies by 1.35x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m32s, peak 132 MiB in use, 35 MiB max residency; the reader reads 36 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 45.2 ms per call raw, 44.2 ms net.

**Per shape, in the run's shape order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.058/0.021/0.037/0.024

**Across the halves:** 16 of the 18 arms are faster on this half and 2 slower, at a geomean of 0.9384, from `bq-expand-aa-distant` at 0.7851 to `lib-stage2-lean` at 1.0179, with `list` itself at 0.8935. **The baseline moved 10.65% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.058, tiers at 1.00x, 1.57x and 21.22x --- and `lib-stage1` leads outside the family at 0.014, priced against `mut-odo-vecdims` at 0.4179 over 4 of 4 shapes, a margin of 58.21%, the widest of the ten, against this class's floor of 0.57%. **Stage ten pays off here too**, `libunord-stage10-sum` over `libunord-stage7-sum` reading **0.6128** against the registered 0.6, with `bcastmid-primes` the closest to level at 0.7040 and 0.7012 and no view behind on either half. **And the doubling block copy is now the leaf's as well**: `lib-stage2-lean` over `lib-stage1` reads 1.0075 on the basis and 0.9710 on the control, inside registration (8)'s 3%, where Run 28 read 0.6163 for a lean fill the leaf could not match. Across the halves the class is disqualified, `list` having moved 10.65%.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone. Two more landed 2026-09-09, for Run 28, `window-64x64-c16-k3` (`l` 553536, `sInner` 3) and `window-32x32-c64-k3` (`l` 518400, `sInner` 3): patch views with a channel axis, listed as image, channels and kernel rather than as the view shape, at one image size in elements, so the channel stride and the run length vary together while the view's size does not. They are the shape stage seven's tie-break exists for, the channel axis standing untied between the tied pairs.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.29* | *68* | *2.75x* |
| liblist-stage1-sum | -- | -- | 0.18 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.22 | 82 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.18 | 82 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.17 | 82 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.20 | 82 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.06 | 100 | 0.04x |
| libunord-stage2-sum | -- | -- | 0.19 | 82 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.22 | 88 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.07 | 96 | 0.04x |
| libunord-stage6-list-sum | -- | -- | 0.05 | 100 | 0.04x |
| libunord-stage6-loop-sum | -- | -- | 1.86 | 90 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.06 | 96 | 0.04x |
| libunord-stage7-sum | -- | -- | 0.06 | 100 | 0.04x |
| libunord-stage9-sum | -- | -- | 0.08 | 96 | 0.04x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.20* | *86* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *97* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *97* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.028 | 0.45 | 84 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.029 | 0.032 | 0.29 | 84 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.030* | *0.034* | *0.15* | *82* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.030 | 0.034 | 0.15 | 82 | 1.00x |
| lib-stage2-lean | 0.031 | 0.035 | 0.17 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.031* | *0.034* | *0.20* | *82* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.035 | 0.17 | 82 | 1.00x |
| lib-stage2-lean-u1 | 0.031 | 0.036 | 0.19 | 82 | 1.00x |
| lib-stage1 | 0.031 | 0.035 | 0.18 | 82 | 1.00x |
| **mut-odo-vecdims** | **0.056** | 0.095 | 0.15 | 76 | 1.00x |
| *mut-odo-vecdims-aa* | *0.056* | *0.096* | *0.16* | *76* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.056* | *0.095* | *0.18* | *76* | *1.00x* |
| *bq-expand-aa-distant* | *0.122* | *0.131* | *0.28* | *64* | *2.75x* |
| bq-expand | 0.122 | 0.131 | 0.30 | 64 | 2.75x |
| *bq-expand-aa-adjacent* | *0.122* | *0.131* | *0.32* | *64* | *2.75x* |
| list (baseline) | 1.000 | 1.000 | 0.50 | 32 | 24.77x |
| *list-aa-adjacent* | *1.002* | *1.007* | *0.44* | *32* | *24.77x* |
| *list-aa-distant* | *1.004* | *1.008* | *0.51* | *32* | *24.77x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0038, worst cell 0.80% on `window-224x224-k3`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.43% on `window-28x28-k5`, its interval covering 1. The in-situ term reads 1.0061, 1.0815 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0037, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h24m55s, peak 128 MiB in use, 51 MiB max residency; the reader reads 36 benchmarks over 8 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 12.7 ms per call raw, 12.3 ms net.

**Per shape, in the run's shape order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2, window-64x64-c16-k3, window-32x32-c64-k3):** `mut-odo-vecdims` 0.045/0.055/0.095/0.036/0.058/0.057/0.059/0.058

**Across the halves:** 12 of the 18 arms are faster on this half and 6 slower, at a geomean of 0.9149, from `bq-expand-aa-distant` at 0.6559 to `mut-odo-vecdims-add-in-leaf-u2-ptr` at 1.0041, with `list` itself at 0.8917. **The baseline moved 10.83% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.095, the highest of the ten and tied with `small`'s, and tiers at 1.00x, 2.75x and 24.77x --- and `lib-stage2-lean` leads outside the family at 0.031, priced against `mut-odo-vecdims` at 0.4815 over 8 of 8 shapes at sign p 0.0078, a margin of 51.85% against this class's floor of 0.38%, the tightest floor of the ten. **This is where stage ten's tie-break half is read alone**, no window view carrying a zero stride: over `libunord-stage9-sum` it reads **0.7433** against the registered 0.72, which is stage seven's tie-break arriving through stage ten intact. **Two views part in SIGN between the halves on that span** --- `window-128x128-k7` at 1.0350 on the basis against 0.9719 on the control, and `window-64x64-k1x9` at 1.0112 against 0.9624 --- which is what saved registration (6) from its second kill, since that kill wants a view behind on both. Read them as one draw each and not as a mechanism. Across the halves the class is disqualified, `list` having moved 10.83%.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.07* | *119* | *1.14x* |
| liblist-stage1-sum | -- | -- | 0.15 | 127 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.12 | 128 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.11 | 127 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.12 | 127 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.13 | 127 | 1.01x |
| libunord-stage10-sum | -- | -- | 0.18 | 127 | 1.01x |
| libunord-stage2-sum | -- | -- | 0.13 | 127 | 1.01x |
| libunord-stage3-sum | -- | -- | 0.14 | 127 | 1.01x |
| libunord-stage5-sum | -- | -- | 0.12 | 127 | 1.01x |
| libunord-stage6-list-sum | -- | -- | 0.13 | 127 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.14 | 127 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.13 | 127 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.15 | 127 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.13 | 127 | 1.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.13* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *138* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.025 | 0.033 | 0.13 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.025 | 0.035 | 0.12 | 128 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.025* | *0.034* | *0.10* | *128* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.025 | 0.034 | 0.12 | 128 | 1.00x |
| lib-stage1 | 0.025 | 0.034 | 0.27 | 127 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.025* | *0.034* | *0.16* | *128* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.026 | 0.040 | 0.12 | 127 | 1.00x |
| lib-stage2-lean | 0.027 | 0.034 | 0.13 | 127 | 1.00x |
| lib-stage2-lean-u1 | 0.027 | 0.034 | 0.14 | 127 | 1.00x |
| *mut-odo-vecdims-aa* | *0.030* | *0.032* | *0.07* | *127* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.031* | *0.032* | *0.08* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.031** | 0.032 | 0.06 | 127 | 1.00x |
| *bq-expand-aa-distant* | *0.096* | *0.099* | *0.07* | *112* | *1.14x* |
| *bq-expand-aa-adjacent* | *0.096* | *0.099* | *0.07* | *112* | *1.14x* |
| bq-expand | 0.096 | 0.099 | 0.08 | 112 | 1.14x |
| list (baseline) | 1.000 | 1.000 | 0.28 | 71 | 19.43x |
| *list-aa-adjacent* | *1.001* | *1.004* | *0.23* | *71* | *19.43x* |
| *list-aa-distant* | *1.006* | *1.016* | *0.27* | *71* | *19.43x* |

**Controls:** The largest A/A pair is `list-aa-distant` at 1.0056, worst cell 1.61% on `scaled-rank1-m1`, and 3 of 8 intervals cover 1. The `sum-only` halves agree at 1.0012 on a worst cell of 0.42% on `scaled-r5`, its interval covering 1. The in-situ term reads 1.0189, 1.0105 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0054, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m22s, peak 111 MiB in use, 29 MiB max residency; the reader reads 36 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 4.74 ms per call raw, 4.56 ms net.

**Per shape, in the run's shape order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.026/0.032/0.032

**Across the halves:** 16 of the 18 arms are faster on this half and 2 slower, at a geomean of 0.9667, from `list` at 0.8853 to `mut-odo-vecdims-add-in-leaf-u1` at 1.0465, with `list` itself at 0.8853. **The baseline moved 11.47% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.032, the joint lowest of the ten with `compose`'s and `block`'s, and tiers at 1.00x, 1.14x and 19.43x --- and `lib-stage1` leads outside the family at 0.025, priced against `mut-odo-vecdims` at 0.9363 over 2 of 3 shapes at sign p 1, a margin of 6.37% against this class's floor of 0.56%: the narrowest lead of the ten and the one whose sign test does not separate it. **The class has no news of its own this run.** Every span registration (5), (6) and (7) reads here ties inside the floor on both halves --- 1.0005 and 0.9986 for the list against the fill it replaces, 1.0049 and 1.0045 for stage ten against stage seven --- which is what a class of three dense scaled views is for. The A/A slot the open list has watched on `scaled-super-r3` for seven runs is not the widest cell here this run; the class's worst A/A cell is 2.74% on the control and 1.61% on the basis, and neither is that view. Across the halves the class is disqualified, `list` having moved 11.47%.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.47* | *52* | *1.08x* |
| liblist-stage1-sum | -- | -- | 0.08 | 65 | 0.19x |
| liblist-stage2-sum | -- | -- | 0.13 | 69 | 0.14x |
| liblist-stage3-sum | -- | -- | 0.02 | 70 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.11 | 64 | 0.19x |
| libunord-stage10-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.24 | 69 | 0.14x |
| libunord-stage3-sum | -- | -- | 0.15 | 59 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.02 | 71 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.06 | 69 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 70 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 70 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.18* | *78* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *69* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.031 | 0.15 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.029 | 0.13 | 60 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.027* | *0.030* | *0.44* | *59* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.027* | *0.029* | *0.13* | *59* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.029 | 0.14 | 59 | 1.00x |
| lib-stage2-lean | 0.028 | 0.029 | 0.13 | 59 | 1.00x |
| lib-stage2-lean-u1 | 0.028 | 0.030 | 0.13 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.030 | 0.12 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.063* | *0.11* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.031** | 0.063 | 0.10 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.031* | *0.062* | *0.10* | *59* | *1.00x* |
| *bq-expand-aa-adjacent* | *0.097* | *0.103* | *0.48* | *46* | *1.08x* |
| *bq-expand-aa-distant* | *0.097* | *0.102* | *0.04* | *46* | *1.08x* |
| bq-expand | 0.097 | 0.102 | 0.41 | 46 | 1.08x |
| lib-stage1 | 0.106 | 1.211 | 0.20 | 55 | 1.19x |
| list (baseline) | 1.000 | 1.000 | 2.63 | 18 | 19.27x |
| *list-aa-distant* | *1.028* | *1.049* | *0.41* | *18* | *19.27x* |
| *list-aa-adjacent* | *1.031* | *1.046* | *0.38* | *18* | *19.27x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0309, worst cell 4.64% on `runs-16384`, and 6 of 8 intervals cover 1. The `sum-only` halves agree at 0.9998 on a worst cell of 0.65% on `runs-7`, its interval covering 1. The in-situ term reads 1.0279, 1.0341 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0297, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h43m28s, peak 564 MiB in use, 247 MiB max residency; the reader reads 36 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 37.6 ms per call raw, 36.6 ms net.

**Per shape, in the run's shape order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.063/0.052/0.044/0.042/0.037/0.034/0.028/0.028/0.028/0.028/0.027/0.028/0.027/0.029

**Across the halves:** 13 of the 18 arms are faster on this half and 5 slower, at a geomean of 0.9544, from `bq-expand-aa-distant` at 0.8502 to `mut-odo-vecdims-add-in-leaf-u1-ptr` at 1.0089, with `list` itself at 0.8877. **The baseline moved 11.23% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.063, tiers at 1.00x, 1.08x and 19.27x --- and `lib-stage2-lean` leads outside the family at 0.028, priced against `mut-odo-vecdims` at 0.8062 over 7 of 14 shapes at sign p 1, a margin of 19.38% against this class's floor of **3.09%**, the widest floor of the ten. **This class carries three of the run's registrations at once and all three held.** The lazy odometer's consumer over the port's base-offset table, `liblist-stage4-sum` over `liblist-stage2-sum`, reads **0.4915** on the basis and **0.4664** on the control against a registered 0.5; the fused list over the fill it replaces, `libunord-stage5-sum` over `libunord-stage3-sum`, reads **0.5675** and **0.5795** against 0.57; and the lean fill over the shipped leaf reads **0.2320** and **0.2328** against 0.23, where the two routes genuinely part --- stage one concatenating a slice per run where the lean dispatch fills. So the odometer's lead over master's recursion and over the branch's table is the design's on both halves and not the flag's. Across the halves the class is disqualified, `list` having moved 11.23%.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes, in the order they run: `flip-fwd-rows96` (`l` 1800000, `sInner` 96), which landed 2026-09-09 and is `runs-96`'s construction under a `flip` name --- the forward control for `flip-last-rows`, so the class's own reversal finding is read inside ONE process over one baseline where it used to be read across two; `flip-whole-square` (`l` 1798281, `sInner` 1341); `flip-last-c32` (`l` 165888, `sInner` 3); `flip-last-rows` (`l` 1800000, `sInner` 96); and the two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed, `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order. The control sits in this class by its name alone --- `classOf` reads the class off the name --- and not in `flipShapes`, every member of which is asserted to have an innermost stride of -1.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.41* | *70* | *1.03x* |
| liblist-stage1-sum | -- | -- | 0.14 | 82 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.22 | 90 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.09 | 92 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.08 | 92 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.11 | 82 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.05 | 92 | 0.10x |
| libunord-stage3-sum | -- | -- | 0.05 | 90 | 0.50x |
| libunord-stage5-sum | -- | -- | 0.01 | 97 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.01 | 98 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.02 | 96 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 97 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 97 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.14* | *90* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *93* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *93* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.052 | 0.38 | 81 | 1.00x |
| lib-stage2-lean | 0.026 | 0.049 | 0.36 | 83 | 1.00x |
| lib-stage2-lean-u1 | 0.027 | 0.055 | 0.32 | 83 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.027 | 0.046 | 0.31 | 82 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.029* | *0.050* | *0.38* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.030 | 0.050 | 0.11 | 80 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.030* | *0.050* | *0.15* | *80* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.051 | 0.26 | 80 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.058* | *0.12* | *77* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.058* | *0.10* | *77* | *1.00x* |
| **mut-odo-vecdims** | **0.032** | 0.058 | 0.08 | 77 | 1.00x |
| lib-stage1 | 0.039 | 0.054 | 0.32 | 80 | 1.00x |
| bq-expand | 0.103 | 0.131 | 0.45 | 64 | 1.03x |
| *bq-expand-aa-adjacent* | *0.103* | *0.131* | *0.43* | *64* | *1.03x* |
| *bq-expand-aa-distant* | *0.104* | *0.131* | *0.10* | *64* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.80 | 33 | 19.15x |
| *list-aa-distant* | *1.008* | *1.023* | *0.58* | *33* | *19.15x* |
| *list-aa-adjacent* | *1.011* | *1.031* | *0.31* | *32* | *19.15x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 1.0266, worst cell 15.51% on `flip-last-rows`, and 3 of 8 intervals cover 1. The `sum-only` halves agree at 0.9996 on a worst cell of 0.42% on `flip-whole-square`, its interval covering 1. The in-situ term reads 1.0156, 1.0127 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0147, which the correction amplifies by 2.08x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h18m39s, peak 209 MiB in use, 76 MiB max residency; the reader reads 36 benchmarks over 6 shapes of the flip class. Anchor: `flip-fwd-rows96`, `list` at 27.4 ms per call raw, 26.4 ms net.

**Per shape, in the run's shape order (flip-fwd-rows96, flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.027/0.027/0.058/0.047/0.029/0.029

**Across the halves:** 11 of the 18 arms are faster on this half and 7 slower, at a geomean of 0.9653, from `list` at 0.8792 to `lib-stage2-lean-u1` at 1.0433, with `list` itself at 0.8792. **The baseline moved 12.08% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.058, tied with `bcastmid`'s, and tiers at 1.00x, 1.03x and 19.15x --- and `lib-stage2-lean` leads outside the family at 0.026, priced against `mut-odo-vecdims` at 0.7911 over 4 of 6 shapes at sign p 0.69, a margin of 20.89% against this class's floor of **2.66%**, the second widest of the ten. **This class carries the run's widest single A/A cell**, 15.51% on `flip-last-rows` on the basis half --- above the 0.46%-to-11.31% band eighteen legs of 2026-09-09 put that view's A/A spread in, and the reason this class's floor is what it is. It is one draw of a cell the file already knows to be wide, not a finding; what it does mean is that no margin on `flip-last-rows` under three percent is readable this run. Across the halves the class is disqualified, `list` having moved 12.08%.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.12* | *104* | *1.03x* |
| liblist-stage1-sum | -- | -- | 0.13 | 113 | 0.42x |
| liblist-stage2-sum | -- | -- | 0.15 | 124 | 0.31x |
| liblist-stage3-sum | -- | -- | 0.02 | 128 | 0.00x |
| liblist-stage4-sum | -- | -- | 0.02 | 128 | 0.00x |
| libunord-stage1-sum | -- | -- | 0.13 | 113 | 0.42x |
| libunord-stage10-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage2-sum | -- | -- | 0.19 | 123 | 0.31x |
| libunord-stage3-sum | -- | -- | 0.13 | 111 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.02 | 128 | 0.00x |
| libunord-stage6-list-sum | -- | -- | 0.02 | 129 | 0.00x |
| libunord-stage6-loop-sum | -- | -- | 0.03 | 127 | 0.00x |
| libunord-stage6-sum | -- | -- | 0.03 | 128 | 0.00x |
| libunord-stage7-sum | -- | -- | 0.02 | 128 | 0.00x |
| libunord-stage9-sum | -- | -- | 0.02 | 128 | 0.00x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.27* | *130* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *122* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.022 | 0.025 | 0.15 | 112 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.023 | 0.028 | 0.10 | 112 | 1.00x |
| lib-stage2-lean | 0.024 | 0.028 | 0.11 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.024* | *0.028* | *0.12* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.028 | 0.09 | 111 | 1.00x |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.024* | *0.028* | *0.11* | *111* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.029 | 0.15 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.029 | 0.10 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.026* | *0.032* | *0.12* | *111* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.026* | *0.032* | *0.12* | *111* | *1.00x* |
| **mut-odo-vecdims** | **0.026** | 0.032 | 0.12 | 111 | 1.00x |
| lib-stage1 | 0.056 | 0.064 | 0.13 | 103 | 1.42x |
| bq-expand | 0.095 | 0.096 | 0.12 | 97 | 1.03x |
| *bq-expand-aa-distant* | *0.095* | *0.095* | *0.08* | *97* | *1.03x* |
| *bq-expand-aa-adjacent* | *0.095* | *0.095* | *0.13* | *97* | *1.03x* |
| list (baseline) | 1.000 | 1.000 | 0.25 | 57 | 19.19x |
| *list-aa-distant* | *1.002* | *1.006* | *0.26* | *56* | *19.19x* |
| *list-aa-adjacent* | *1.003* | *1.009* | *0.23* | *56* | *19.19x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9933, worst cell 1.95% on `block-r3-vol64`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.50% on `block-run64-off7`, its interval covering 1. The in-situ term reads 1.0264, 1.0131 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9974, which the correction amplifies by 2.51x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m37s, peak 135 MiB in use, 37 MiB max residency; the reader reads 36 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.14 ms per call raw, 3.98 ms net.

**Per shape, in the run's shape order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.022/0.027/0.032/0.027/0.022

**Across the halves:** 18 of the 18 arms are faster on this half and 0 slower, at a geomean of 0.9548, from `list` at 0.8704 to `mut-odo-vecdims-add-in-leaf-u2-aa-distant` at 0.9883, with `list` itself at 0.8704. **The baseline moved 12.96% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.032, joint lowest of the ten with `scaled`'s and `compose`'s, and tiers at 1.00x, 1.03x and 19.19x --- and `lib-stage2-lean` leads outside the family at 0.024, priced against `mut-odo-vecdims` at 0.9272 over 5 of 5 shapes, a margin of 7.28% against this class's floor of 0.67%. **It is `runs`'s second, and it says the same three things at half the size.** The odometer's consumer over the port's table reads **0.7826** on the basis and **0.7220** on the control against a registered 0.78; the fused list over the fill it replaces reads **0.4453** and **0.4493** against 0.45; and the lean fill over the shipped leaf reads **0.4225** and **0.4259** against 0.42 --- three spans registered from Run 28's own readings and three landing inside a point of them, on both halves. Across the halves the class is disqualified, `list` having moved 12.96%.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.19* | *223* | *1.34x* |
| liblist-stage1-sum | -- | -- | 0.23 | 228 | 1.64x |
| liblist-stage2-sum | -- | -- | 0.17 | 231 | 1.49x |
| liblist-stage3-sum | -- | -- | 0.24 | 227 | 1.63x |
| liblist-stage4-sum | -- | -- | 0.27 | 231 | 1.45x |
| libunord-stage1-sum | -- | -- | 0.24 | 223 | 2.08x |
| libunord-stage10-sum | -- | -- | 0.22 | 227 | 1.14x |
| libunord-stage2-sum | -- | -- | 0.32 | 226 | 2.04x |
| libunord-stage3-sum | -- | -- | 0.22 | 222 | 1.84x |
| libunord-stage5-sum | -- | -- | 0.23 | 228 | 1.84x |
| libunord-stage6-list-sum | -- | -- | 0.32 | 231 | 1.24x |
| libunord-stage6-loop-sum | -- | -- | 0.21 | 232 | 1.24x |
| libunord-stage6-sum | -- | -- | 0.21 | 231 | 1.26x |
| libunord-stage7-sum | -- | -- | 0.28 | 231 | 1.26x |
| libunord-stage9-sum | -- | -- | 0.27 | 227 | 1.14x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.30* | *239* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.03* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.03* | *250* | *0.01x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.057* | *0.071* | *0.26* | *230* | *1.27x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.057 | 0.071 | 0.36 | 230 | 1.27x |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.057* | *0.071* | *0.54* | *230* | *1.27x* |
| lib-stage2-lean-u1 | 0.057 | 0.102 | 0.25 | 228 | 1.40x |
| lib-stage2-lean | 0.058 | 0.106 | 0.25 | 228 | 1.40x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.060 | 0.072 | 0.31 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.064 | 0.080 | 0.34 | 228 | 1.44x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.065 | 0.081 | 0.25 | 228 | 1.44x |
| **mut-odo-vecdims** | **0.067** | 0.095 | 0.37 | 229 | 1.27x |
| *mut-odo-vecdims-aa* | *0.067* | *0.095* | *0.31* | *229* | *1.27x* |
| *mut-odo-vecdims-aa-distant* | *0.067* | *0.095* | *0.22* | *229* | *1.27x* |
| lib-stage1 | 0.096 | 0.122 | 0.28 | 221 | 2.32x |
| bq-expand | 0.136 | 0.170 | 0.14 | 218 | 1.34x |
| *bq-expand-aa-distant* | *0.136* | *0.170* | *0.18* | *218* | *1.34x* |
| *bq-expand-aa-adjacent* | *0.136* | *0.170* | *0.22* | *218* | *1.34x* |
| list (baseline) | 1.000 | 1.000 | 0.21 | 182 | 19.50x |
| *list-aa-distant* | *1.000* | *1.003* | *0.19* | *182* | *19.50x* |
| *list-aa-adjacent* | *1.001* | *1.003* | *0.22* | *182* | *19.50x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 1.0063, worst cell 2.09% on `small-flat64`, and 7 of 8 intervals cover 1. The `sum-only` halves agree at 0.9992 on a worst cell of 0.40% on `small-row96`, its interval covering 1. The in-situ term reads 0.9902, 0.9936 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0038, which the correction amplifies by 1.54x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m37s, peak 144 MiB in use, 58 MiB max residency; the reader reads 36 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6 us per call raw, 5.78 us net.

**Per shape, in the run's shape order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.046/0.083/0.056/0.064/0.095

**Across the halves:** 17 of the 18 arms are faster on this half and 1 slower, at a geomean of 0.9493, from `list` at 0.8832 to `lib-stage2-lean` at 1.0090, with `list` itself at 0.8832. **The baseline moved 11.68% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold, and this is the one class where property 3's level clause is read off a `mut-odo-vecdims` above 1.00x --- 1.27x, with `bq-expand` at 1.34x and `list` at 19.50x, the per-call constants showing through a call under a microsecond, and the ORDER unbroken like the rest. `worst` is 0.095, joint highest of the ten with `window`'s. `lib-stage2-lean-u1` leads outside the family at 0.057, priced against `mut-odo-vecdims` at 0.8588 --- **ahead on only 1 of 5 shapes at sign p 0.38**, so the paired figure and the win count disagree here and neither alone is the reading. **Two of the run's readings are this class's own.** Stage ten sits **1.2171** behind stage seven, which is the extra pass over the axes on the shortest calls the set has and is what registration (7) said would print a figure and no verdict; and the shipped leaf is the one arm of registration (1)'s three to fall outside 1% here, at **0.9679** across the halves. Across the halves the class is disqualified, `list` having moved 11.68%.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.34* | *88* | *1.21x* |
| liblist-stage1-sum | -- | -- | 0.45 | 98 | 1.00x |
| liblist-stage2-sum | -- | -- | 0.30 | 98 | 1.00x |
| liblist-stage3-sum | -- | -- | 0.33 | 98 | 1.00x |
| liblist-stage4-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage1-sum | -- | -- | 0.31 | 98 | 1.00x |
| libunord-stage10-sum | -- | -- | 0.03 | 109 | 0.01x |
| libunord-stage2-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage3-sum | -- | -- | 0.31 | 98 | 1.00x |
| libunord-stage5-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage6-list-sum | -- | -- | 0.35 | 98 | 1.00x |
| libunord-stage6-loop-sum | -- | -- | 0.34 | 98 | 1.00x |
| libunord-stage6-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage7-sum | -- | -- | 0.32 | 98 | 1.00x |
| libunord-stage9-sum | -- | -- | 0.03 | 109 | 0.01x |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.23* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *105* | *0.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa-distant* | *0.016* | *0.018* | *0.31* | *98* | *1.00x* |
| *mut-odo-vecdims-add-in-leaf-u2-aa* | *0.016* | *0.018* | *0.32* | *98* | *1.00x* |
| mut-odo-vecdims-add-in-leaf-u2 | 0.016 | 0.018 | 0.29 | 98 | 1.00x |
| lib-stage2-lean | 0.017 | 0.019 | 0.35 | 98 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.017 | 0.018 | 0.31 | 98 | 1.00x |
| lib-stage1 | 0.017 | 0.019 | 0.37 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.018 | 0.018 | 0.28 | 98 | 1.00x |
| lib-stage2-lean-u1 | 0.018 | 0.019 | 0.30 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.019 | 0.021 | 0.29 | 96 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.032* | *0.24* | *94* | *1.00x* |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.032* | *0.24* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.032 | 0.19 | 94 | 1.00x |
| bq-expand | 0.095 | 0.098 | 0.40 | 80 | 1.21x |
| *bq-expand-aa-adjacent* | *0.095* | *0.098* | *0.39* | *80* | *1.21x* |
| *bq-expand-aa-distant* | *0.095* | *0.099* | *0.21* | *80* | *1.21x* |
| *list-aa-distant* | *0.997* | *1.008* | *0.75* | *46* | *19.87x* |
| list (baseline) | 1.000 | 1.000 | 0.94 | 46 | 19.87x |
| *list-aa-adjacent* | *1.003* | *1.021* | *0.73* | *46* | *19.87x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0076, worst cell 1.39% on `compose-scalar`, and 5 of 8 intervals cover 1. The `sum-only` halves agree at 0.9990 on a worst cell of 0.47% on `compose-zero-mid`, its interval covering 1. The in-situ term reads 1.0119, 1.0084 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0054, which the correction amplifies by 1.41x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m33s, peak 126 MiB in use, 33 MiB max residency; the reader reads 36 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 27.3 ms per call raw, 26.2 ms net.

**Per shape, in the run's shape order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.032/0.032/0.024/0.021

**Across the halves:** 16 of the 18 arms are faster on this half and 2 slower, at a geomean of 0.9588, from `list-aa-distant` at 0.8861 to `mut-odo-vecdims-aa-distant` at 1.0074, with `list` itself at 0.8898. **The baseline moved 11.02% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.032, joint lowest of the ten with `scaled`'s and `block`'s, and tiers at 1.00x, 1.21x and 19.87x --- and `lib-stage2-lean` leads outside the family at 0.017, priced against `mut-odo-vecdims` at 0.6116 over 4 of 4 shapes, a margin of 38.84% against this class's floor of 0.76%. **Stage ten's figure here is printed and not adjudicated, as registration (6) asks**: **0.6938** over stage seven, between the `bcast` and `bcastmid` readings and made of two kinds of view at once --- `compose-zero-mid` and the two `-bcast` views, where the zero-stride move fires, and `compose-scalar`, every stride 0 and the route unchanged, which reads 0.9987 on the basis and 0.9997 on the control. The class ties on every other span it carries, the list against the fill it replaces at 0.9997 and 0.9989. Across the halves the class is disqualified, `list` having moved 11.02%.


## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 29's halves differ in ONE COMPILE FLAG and in nothing else.** One source, `Main.hs` at `7685375`, one shim at `b3a1aca` and one shim environment, `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of the assembler, one compiler --- ghc-9.12.4 through `cabal.project` and its freeze, both binaries carrying `ghc-internal-9.1204.0` --- one store, one plan and one set of boot libraries, and both run under `WILDLOG=1 SATURATE=1`. The basis adds `-fspec-constr` to its `--ghc-options` and the control does not; `-O1` is `micro.cabal`'s own and is on both, so `nospec` is a plain -O1 build and SpecConstr is off, GHC enabling that pass at -O2 and not at -O1. No pair on record here has varied this: the tags of every half from Run 8 on name shims, allocation areas and compilers, and the flag was on both halves of all of them.

**The sequence was launched once and ran to the end in ONE window, and no population was rerun**, 2026-09-12T01:17:01 to 2026-09-12T09:16:42, twenty-two processes with no hole between one finishing and the next starting. The gate's four ran before them from 00:43:44 and the four rider stages after them to 09:29:27; the counted work followed on a box already handed back, 09:31:33 to 10:14:10. Every process exited 0 at the bench count its roster holds --- 684 twice, 504 twice, 288 twice, and 216, 180, 144 and 108 four times each --- and the driver's own log recorded no complaint about any of them. That is the main set twice and **20 class processes**, one per class per half, with the control half first throughout.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run29-spec`, md5 `22f7f57d1728db91ac87b1b0d9308272`, and `run29-nospec`, md5 `f507eafbcbbfecf7c2151f390caf9b92`, with `.text` at **20783301** bytes on the basis and **20766917** on the control --- they do not agree, the flagged half being the larger by 16384 bytes, which is 16 KiB exactly and is an observation and not a mechanism. Both bake `-A32m -I0 -T -M8G`, read back by `+RTS --info` and not by `strings`, and each carries one `@@wild` and one `@@saturate` instrument. `check` agreed on every shape on both halves and the two logs are byte-identical, on the first build of this pair --- and one compiler emitted both, so the float-printing difference two codegens could innocently have was not available here at all and any difference would have been a finding. There is no repetition reading and none was owed: `Main.hs` moved from `bdf06c8` to `7685375`.

**The source moved and the roster barely did, and this run's fills say the layout did not move with them.** The tracked 28-byte loops were read at the build against the previous build of the basis's recipe, Run 28's, with `--delta`: **every mod-64 offset preserved**, the two groups at [0, 0, 0, 24, 0, 4] and [0, 0], and **no address surviving to the byte**, two displacements in each group where Run 28 read three and two. So twelve commits to `Main.hs` moved the code without moving where any tracked fill sits in its cache line, and the cross-run column agrees: of the 18 arms both rosters time, 16 read inside 1% of 1 and `list` itself reads 0.9999. **Within the pair the layout DID move, and this is the first pair here that can say what a compile flag does to it.** One tracked fill copy of six sits at offset 8 on the control where its counterpart is at 0 on the basis; `--library` reads 914 library self-loops in common with 39.4% at the same offset in line, against the tenth to a quarter README bands a compiler pair at --- a percentage over 914 and one over 141 not being two readings of one population.

**The three main-set anchors** read **5.81 us** on `cnn-slice-c32`, **3.39 ms** on `cnn-L2-24x24-c32` and **36.3 ms** on `stretch-wide-2xM`, net of the forcing pass on the basis half, against **6.44 us**, **3.7 ms** and **39.6 ms** on the control --- the control slower on all three, by 10.87%, 9.35% and 9.06%, which is `list` being what `-fspec-constr` buys, beside the 13.79% its main-set column reads over all nineteen shapes. They are absolutes and so the first thing a later run checks when a whole column moves; the gate's machine check is the same question asked of nineteen shapes at once, and it did not fire.

| shape | `l` | `list`, per call | net | `nospec`, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 5.98 us | 5.81 us | 6.44 us |
| `cnn-L2-24x24-c32` | 165888 | 3.49 ms | 3.39 ms | 3.7 ms |
| `stretch-wide-2xM` | 1800000 | 37.4 ms | 36.3 ms | 39.6 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at **37.6 ms** raw and **36.6 ms** net on the basis, against Run 28's 37.5 ms and 36.4 ms on a class whose population has not moved for six runs --- half a percent apart, with no box move behind it and no second window --- and its own floor is 3.09% here, the widest of the ten. **No class population moved this run at all**: `roster-delta.py` reads 58 class views to 58 with every one of the ten classes unmoved, so each class anchor is read against its own predecessor over the same shapes.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **0.9999** on the basis and **0.9999** on the control, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0312** and **1.0912** on the basis and **1.0305** and **1.1042** on the control for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent under the term it stands for, by the same few percent on both halves, which is where it has run since Run 17. So the flag reaches neither the forcing pass nor the gap between the two ways of measuring it.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-11, before the run, on the pair the request of the same day decided: this run's basis, ghc-9.12.4 in the dead-spot form, as both halves, `spec` under `-fspec-constr` and `nospec` without it, `spec` the basis, so that `--compare` reads spec over nospec and every `cross` below is what the flag buys one arm; over Run 28's roster less the four arms parked 2026-09-11 --- stage eight's consumer, stage four's, and the leaf's hoisted-look-ahead and count-bound forms --- plus `libunord-stage10-sum`, stage seven's tie-break under stage nine's move. Three code changes since Run 28 move figures below: the shipped leaf carries the broadcast run since 2026-09-11, and `lib-stage1`, `liblist-stage1-sum` and `libunord-stage1-sum` fill through `fillStage2` since the same day, so their Run 28 figures are not this roster's on the views those routes fill; a fourth, the driver's `copies` losing an argument it never read, moves nothing. One probe has timed the flag's effect, the aligned HEAD probe the ceiling records, which read the shipped family level between the two regimes to three decimals and is what item (1) predicts from; no probe has timed the list stages, the ports or the reference under `nospec`, and none has timed stage ten under either flag; every figure carried in from Run 28 was read off that run's JSONs on both halves on 2026-09-11. Each item names its populations and carries a prediction and a kill condition, and where an item predicts a no-op it names the line of `Main.hs` that makes the two routes one.

(1) *The flag on the shipped fill.* On the main set and on every class: `predict: cross mut-odo-vecdims-add-in-leaf-u2 1.0 within 1%`, `predict: cross lib-stage1 1.0 within 1%` and `predict: cross lib-stage2-lean 1.0 within 1%` --- the flag was measured irrelevant to the shipped family on 2026-08-24, when the shipped file was settled not to set it, and nothing since has measured otherwise. Killed by any of the three past 1% on the main set, which would say the regime the shipped file compiles under is not the regime these tables price the fill in.

(2) *The flag on the reference.* On the main set and on every class: `predict: cross list 1.0 within 3%`, `list` being the reference every table is read against and the halves' comparability bar being read on it. Killed by `list` outside 3% on the main set, which would say the two columns cannot be subtracted and every cross below carries the reference's own move.

(3) *The flag on the fused list.* The lazy stages' consumers fold one flat loop, `sumLazyRuns`, whose continuations are known, and nothing has timed it without the flag. On `runs`, `block` and `window`: `predict: cross libunord-stage7-sum 0.9 within 10%`, the list a tenth dearer without the flag; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, where the route fills or is one block: `predict: cross libunord-stage7-sum 1.0 within 1%`. Killed by the consumer inside 1% of level on `runs` and `block`, which would say the flag is irrelevant to the list too and the regime question closes; or by it past 0.8 there, which would say the list's lead over the fill is the flag's.

(4) *The odometer over the table without the flag.* `liblist-stage4-sum` over `liblist-stage2-sum`, the lazy odometer against the port's base-offset table. On `runs`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.5 within 25%`; on `block`, both halves: `predict: pair liblist-stage4-sum liblist-stage2-sum 0.78 within 25%` --- Run 28 reading 0.4951 and 0.5177, 0.7831 and 0.8018 under the flag. Killed by the odometer's consumer behind the table's past the floor on the `nospec` half, which would say the lazy list's lead over the branch's list is the flag's and not the design's.

(5) *The list over the fill without the flag.* `libunord-stage5-sum` over `libunord-stage3-sum`, the fused list against the same route's fill. On `runs`, both halves: `predict: pair libunord-stage5-sum libunord-stage3-sum 0.57 within 15%`; on `block`, both halves: `predict: pair libunord-stage5-sum libunord-stage3-sum 0.45 within 15%` --- Run 28 reading 0.5681 and 0.5810, 0.4457 and 0.4460; on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, both halves: `predict: pair libunord-stage5-sum libunord-stage3-sum 1.0`, both routes filling or returning one block, the consumer summing what `fillRoute` wrote in either. Killed by the pair past the floor on both halves on any of the five fill classes; or by the list not ahead past the floor on `runs` on the `nospec` half.

(6) *Stage ten where both of its changes fire, and where one does.* `libunord-stage10-sum` over `libunord-stage7-sum` on `bcast`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.55 within 20%`; on `bcastmid`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 0.6 within 20%`; on `compose` the same span prints a figure and no verdict, read per view by hand, `compose-zero-mid` and the two `-bcast` views ahead past the floor and `compose-scalar` inside it --- stage nine's move alone read 0.46 and 0.71 on those views on Run 28, and stage ten's route is stage nine's on all three classes: no `bcast` or `bcastmid` view carries two axes tied on stride, and `compose`'s tied axes are its zero strides, which `canonView` merges under either order. Over `libunord-stage9-sum` on `window`, both halves: `predict: pair libunord-stage10-sum libunord-stage9-sum 0.72 within 15%`, stage seven's tie-break alone reading 0.7425 and 0.7237 over stage six there and stage nine's route being stage six's on `window`, no window view carrying a zero stride. Killed by stage ten behind stage seven past the floor on both halves on any zero-stride view, or behind stage nine past the floor on both halves on any unstrided window view, which would say the two changes do not compose.

(7) *Stage ten where neither fires.* On `rev`, `scaled`, `runs` and `block`, both halves: `predict: pair libunord-stage10-sum libunord-stage7-sum 1.0` --- `zerosOutermost` hands its list back unchanged where no stride is 0, both of its branches doing so, the partition being the identity then, so `routeUnord10` is `routeUnord7` there to the byte and only the extra pass over the axes separates the dispatches. Killed by the pair past the floor on both halves on any of the four, which would say the pass is visible at a call of tens of microseconds. On `small` the span prints a figure and no verdict, read per view by hand: behind on the four views other than `small-bcast32` by the pass, as stage nine's read 1.18 to 1.40 over stage six on Run 28 --- `small-flat64`'s zero stride sits on a unit axis `canonView` drops, so its route is stage seven's --- and ahead on `small-bcast32`.

(8) *`lib-stage1` against the lean fill with one fill under both.* Since 2026-09-11 both fill through `fillStage2`, so on `bcast` and `bcastmid`, where Run 28 read the lean fill ahead at 0.8635 and 0.6163 for the two bodies the leaf lacked, the pair prices the dispatch alone --- stage one's raw dims against the lean dispatch's canonical ones, which merge `bcast-inner8`'s two outer levels into one. On `bcast` and on `bcastmid`, both halves: `predict: pair lib-stage2-lean lib-stage1 1.0 within 3%`. Killed by the pair past 3% on both halves on either class, which would say the canonicalization is worth that much to the fill on broadcast views. On `runs` and `block` the routes part, stage one concatenating a slice per run where the lean dispatch fills, and Run 28's reading stands as the prediction: `predict: pair lib-stage2-lean lib-stage1 0.23 within 20%` on `runs` and `predict: pair lib-stage2-lean lib-stage1 0.42 within 20%` on `block`, both halves; killed by the lean fill not ahead past the floor on either, which would say the slice-and-concatenate route no longer loses on short runs.

(9) *The floor with the fill family's pair for a second run.* On the main set and on every class, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa mut-odo-vecdims-add-in-leaf-u2 1.0` and `predict: pair mut-odo-vecdims-add-in-leaf-u2-aa-distant mut-odo-vecdims-add-in-leaf-u2 1.0`, each inside the population's floor as Run 28's forty-four readings were; read beside them by hand whether the whole-set floor is the fill pair's again, 0.50% on Run 28 against the carry-back figure's 0.39%, which is what decides the open question in the floor entry. Killed by the adjacent copy outside the floor on both halves on the main set.

(10) *Claim 10 without the flag.* On the main set, both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2 mut-odo-vecdims 0.64 within 5%`, eight readings across four runs spanning nine thousandths under the flag. Killed by the pair outside 5% on the `nospec` half, which would say what the leaf fusion buys is the regime's.

(11) *Claim 7 without the flag.* Off `--alloc` on both halves, by hand: the re-aimed levels reproduce over the eighteen pinned shapes, the `libunord` consumers 0.00x, the fills and the `liblist` consumers 1.00x, `bq-expand` 2.06x, `list` 22.38x. Killed by any level moving at the column's precision on the `nospec` half, which would say the flag changes what an arm allocates and not only how fast.

**Eight held and three were killed, and all three kills fell on an item predicting a NO-OP --- which is Run 28's largest finding read a second time, on a different variable.** Eight of the eleven items argue somewhere that a change cannot reach a population, and (2), (7) and (11) are the three that failed; not one item predicting a REAL effect was killed, and several landed on their registered figure to a point or two --- (8)'s 0.2320 against 0.23 on `runs` and 0.4225 against 0.42 on `block`, (4)'s 0.4915 against 0.5, (5)'s 0.5675 against 0.57. **The converse is not claimed**: (1), (3), (5), (8) and (9) argue no-ops too and all five held. Every item's verdict below is its KILL CONDITION applied across the populations that item names, never one sweep of the main set; every figure is re-derived from this run's own JSONs; and a span is judged against the floor of the population it is read on. **Two things the registration could not foresee govern how it reads.** The `-sum` consumers have no corrected time, so every paired figure quoted for one is a RAW ratio of two uncorrected slopes --- and the reader's `cross` branch was dividing their nets and dropping whatever shape went non-positive, which read item (3) as KILLED over 3 of 14 shapes on `runs` where the raw ratio over all 14 holds it; that is fixed, with a case, and item (3) below is the corrected reading. And the gate's machine check did NOT fire, so nothing here carries a box term and no reading is bridged.

(1) *The flag on the shipped fill.* **HELD.** On the main set the three arms read **1.0001**, **0.9945** and **1.0044**, none past the registered 1%, so the kill --- any of the three past 1% on the main set --- does not fire, and the regime `Data/Array/Internal.hs` compiles under is the regime these tables price the fill in. **Its class spans are the part that read short**: of the thirty-three arm-by-population readings, ten sit outside 1%, and every one falls on `bcast` (0.9872, 0.9880, 0.9837), `bcastmid` (0.9811, 1.0179, 0.9898), `block` (0.9849, 0.9771, 0.9848) or, for the leaf alone, `small` (0.9679). So the flag is not quite nothing to the fill on the four classes whose calls are shortest, which no earlier reading could have shown.

(2) *The flag on the reference.* **KILLED, and it is the kill the whole file is written around.** `cross list 1.0 within 3%` reads **0.8788** on the main set, 12.12 points off. **The item registered 3% where the rule it invokes is 0.7%**, and that is the registration's slack rather than a second bar: 0.7% is what lets two columns be differenced and is what every other sentence here applies, 3% is what this item chose to be killed at, and 12.12 points is past both by a wide margin, so nothing turns on which. It is past the bar on every population: `rev` 0.9088, `bcastmid` 0.8935, `window` 0.8917, `compose` 0.8898, `runs` 0.8877, `scaled` 0.8853, `small` 0.8832, `flip` 0.8792, `block` 0.8704, `bcast` 0.8533. The item's own clause says what follows and it follows exactly: the two columns cannot be subtracted, and every `cross` in this run is an ordering.

(3) *The flag on the fused list.* **HELD, on the corrected reader.** Read raw over every shape, as an arm with no corrected time must be, `libunord-stage7-sum` across the halves is **0.9779** on `runs`, **0.9839** on `block` and **0.9771** on `window`, each within the registered 10 points of 0.9; and 1.0016, 0.9968, 0.9991, 1.0018 and 0.9969 on `rev`, `bcast`, `bcastmid`, `scaled` and `compose`, each within 1% of level. Neither kill fires --- nothing is inside 1% of level on `runs` and `block`, and nothing is past 0.8. **So the flag is worth about two percent to the fused list and the list's lead over the fill is the design's**, which is the question the item was written to ask.

(4) *The odometer over the table without the flag.* **HELD.** `liblist-stage4-sum` over `liblist-stage2-sum` reads **0.4915** on the basis and **0.4664** on the control on `runs`, against a registered 0.5, and **0.7826** and **0.7220** on `block`, against 0.78. The kill wants the odometer's consumer BEHIND the table's on the control half and it is ahead by half and by a quarter, so the lazy list's lead over the branch's list is the design's and not the flag's.

(5) *The list over the fill without the flag.* **HELD on both of its conditions.** `libunord-stage5-sum` over `libunord-stage3-sum` reads **0.5675** and **0.5795** on `runs` against a registered 0.57 and **0.4453** and **0.4493** on `block` against 0.45. The five fill classes tie inside their own floors on both halves --- `rev` 1.0013 and 0.9999, `bcast` 0.9999 and 0.9996, `bcastmid` 1.0019 and 0.9995, `scaled` 1.0005 and 0.9986, `compose` 0.9997 and 0.9989 --- so neither the fill-class kill nor the `runs` kill fires.

(6) *Stage ten where both of its changes fire, and where one does.* **HELD, on all three spans and both kills.** Over stage seven it reads **0.5850** on `bcast` against a registered 0.55 and **0.6128** on `bcastmid` against 0.6; over stage nine on `window` it reads **0.7433** against 0.72. The first kill wants stage ten behind stage seven past the floor on both halves on some zero-stride view and none is behind at all, the closest being `bcast-inner8` at 0.7106 and 0.7100, `bcastmid-primes` at 0.7040 and 0.7012 and `compose-scalar` at 0.9987 and 0.9997. The second wants it behind stage nine on both halves on some unstrided `window` view: `window-128x128-k7` reads 1.0350 on the basis and 0.9719 on the control and `window-64x64-k1x9` 1.0112 and 0.9624, both parting in sign, so neither fires. `compose` prints **0.6938** and no verdict, as registered.

(7) *Stage ten where neither fires.* **KILLED on `rev`.** The pair reads **1.0229** against that class's 0.40% floor on the basis and **1.0240** against 0.21% on the control --- past the floor on both halves, which is the kill --- on a class where `zerosOutermost` hands its list back unchanged and `routeUnord10` is `routeUnord7` to the byte. The other three named populations tie: `scaled` 1.0049 and 1.0045, `runs` 0.9997 and 1.0004, `block` 1.0033 and 1.0021. So the extra pass over the axes IS visible, at a call of tens of microseconds, and the argument that it could not be is what failed. On `small` the span prints **1.2171** and no verdict, inside the 1.18-to-1.40 band the item carried over from stage nine's own reading.

(8) *`lib-stage1` against the lean fill with one fill under both.* **HELD, and it is the sharpest reproduction in the run.** On `bcast` the pair reads **0.9975** and **0.9967** and on `bcastmid` **1.0075** and **0.9710**, all inside the registered 3%, so the canonicalization is worth nothing to the fill on broadcast views once both routes fill through `fillStage2` --- and Run 28's 0.8635 and 0.6163 were the two bodies the leaf lacked, not the dispatch. Where the routes genuinely part the item registered Run 28's own readings and both come back: **0.2320** and **0.2328** on `runs` against 0.23, **0.4225** and **0.4259** on `block` against 0.42.

(9) *The floor with the fill family's pair for a second run.* **HELD in every one of its forty-four readings**, two spans over eleven populations on two halves, the A/A copy running 0.9943 to 1.0029 against its base and the distant one 0.9832 to 1.0052. The kill --- the adjacent copy outside the floor on both halves on the main set --- does not fire, it reading 0.9999 there. **Read beside it by hand, as the item asks**: the whole-set floor is the fill pair's again, `mut-odo-vecdims-add-in-leaf-u2-aa-distant` carrying **0.51%** on the basis against the carry-back figure's **0.26%**, so the 0.50%-against-0.39% Run 28 read was that pair's property and not one evening's, and the open question about which figure a margin must clear now has two runs' evidence rather than one.

(10) *Claim 10 without the flag.* **HELD.** The pair reads **0.6441** on the basis and **0.6413** on the control, both inside 5% of the registered 0.64, so the kill --- outside 5% on the `nospec` half --- does not fire. The two halves agree to three thousandths, which settles what the item asked: what the leaf fusion buys is not the regime's.

(11) *Claim 7 without the flag.* **KILLED, and it is the run's second-largest finding.** Read off the allocation column over the eighteen pinned shapes, the basis gives every registered level to the digit --- the `libunord` consumers 0.00x, the fills and the `liblist` consumers 1.00x, `bq-expand` **2.06x**, `list` **22.38x** --- and the control does not: `bq-expand` reads **2.76x** and `list` **24.90x**, while every other level is unmoved. Any level moving at the column's precision on the control half was the kill, and two moved. **So the flag changes what those two arms ALLOCATE and not only how fast they run**, and they are exactly the two families it moved in time --- which the counted work then confirms from the third side, `bq-expand` and `list` converting their instruction saving to time almost entirely at 1.0161 and 0.9926 of `time/counts` where every other arm converts none of it.
