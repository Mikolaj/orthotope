# Run 27 (SpecConstr)

One run's write-up: its head, its Results, what the next run compares against, the claims that run should test, the ten class blocks, and its own Provenance. A run replaces this file whole and edits [README.md](../README.md) around it, in the score of places [the replace list under Provenance there][prov] names --- the open list among them, which is where a run's surprises go and where its registrations keep a verdict and a pointer --- the registrations themselves being in this file since 2026-08-29, in the section at its foot. So this file is most of what a run replaces and by no means all of it. What stands between runs is the harness, [the procedure][procedure] that makes a file like this one, and the rulings a measurement does not reach.

**Run 27 (SpecConstr), and what a second compiler does once a workaround has been put under it: the GHC #27778 annotation gave the HEAD half the basis's code on all three pointer arms, and every reading that had parted between the compilers closed at once.** Run 26 read the `Ptr`-walking fill at **2.6731** of its parent on HEAD against 0.9479 on the basis, allocating **2.61x** the result where the basis allocated 1.00x, and its counts parted at 0.6219 and 0.9295. This run reads the same three spans at **0.9713, 0.9357 and 0.9419** on the basis and **0.9751, 0.9239 and 0.9386** on HEAD, both fills at **1.00x** on both halves, and the two counts at **1.0054 and 1.0051** --- inside the 0.9936-to-1.0089 band the untouched roster sits in. That is registrations (1) and (2) held in every clause, and it is the whole of what the pair was built to price. **The roster is where the rest of the run went**: ten timed arms land and five leave, 35 over 19 main-set shapes and 665 benches, and none of the ten had been timed at full budget on either codegen. Of the six registrations written against them --- (6) to (9), (13) and (14) --- **two held and four were killed** --- the lazy ordered candidates on all three of their kill conditions, the lean trick on `small` and `compose`, the lazy unordered port on `runs` and `block`, and the hoisted bound above its own 1.005 --- while the reducing consumers held by margins nothing else here reaches, `libunord-stage1-sum` leading its own fill in all ten classes on both halves and by **0.0928** on `runs`. **And the box moved under the run**: the gate's machine check reads `list`'s net **3.66% below** the fingerprint Run 26 kept, over 19 of 19 shapes and with every shape moving together, so no absolute crosses from Run 26 unadjusted, the cross-run reading is `--bridge`, and the question of what changed on the machine is a person's rather than this file's.

**There is NO repetition this run and none was owed, and what stands in its place is a bridge rather than a column.** `Main.hs` moved from Run 26's 659d898 to bb6d113 by the commits of 2026-09-07, so neither half can reproduce an earlier binary and an unequal md5 would name nothing; the pair note says so at its `repetition` row rather than recording a comparison it cannot make. What a repetition would have given --- a term for everything that moves when nothing changed --- came instead from the gate, and it came back **large**: the box reads 3.66% faster than the kept fingerprint on `list`'s net, worst `stretch-square-1341` at -8.26% and three shapes past 5%, with the worst residual about that geomean **-4.78%**, inside the 7% a single shape wanders on its own. **So one number describes the move and every cross-run ORDERING survives it**, which is what lets this run publish beside Run 26 at all; what does not survive is a cross-run absolute, and the twenty arms both runs time are read here through `--bridge`, each arm as a ratio to `list` in its own run, at a geomean of **0.9940** with 17 of the 20 inside the 3.3% drift band.

**What holds the build to something is the gate and three machine checks, and this run is the first since Run 18 whose machine check FIRED.** The gate ran both halves twice in a palindrome and read SOUND on both passes: all three arms fall the same side of 1 in both and no arm changes sign, `list` reading 0.9786 and 0.9919 at 17 and 18 of 19, `bq-expand` 0.9844 and 0.9936 at 17 and 12, and `mut-odo-vecdims` 0.9937 and 0.9981 at 16 and 9 --- the counts parting widest on the arm nearest 1, which is where a count parts first. Within the basis half the two passes agree to a thousandth, `bq-expand` over `mut-odo-vecdims` reading **2.2162 and 2.2241** at 0 of 19 wins apiece. **The machine check is the separate answer and it is the one that moved**: -3.66% over all nineteen shapes where Run 26 read +0.72% over eighteen, past the 3% that makes it a box move rather than drift. The third check is the plateau, and it is quiet: all twenty-two processes assert the same in-process state, **19.2759 to 19.7876 ms/iter, a 2.65% spread** against the 5% band.

**The manifest's two live claims both held, and claim 7 gained the reading it lost last run.** `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims` --- what the leaf fusion buys over the family root --- reads **0.6380 at 17 wins of 19, sign p 0.00073** on the basis and **0.6392 at 17 of 19** on HEAD, against Run 26's 0.6438 and 0.6467 and Run 24's 0.6464 and 0.6454 pinned to these nineteen: six readings over three runs and two codegens now span nine thousandths. **Claim 7's levels are unmoved for a fifth run and, this time, unmoved on BOTH halves**: over the eighteen main-set shapes the claim is pinned to, the mutable fills read 1.00x, the list consumers 2.00x, `bq-expand` 2.06x, `list` 22.38x and the `libunord` arms 0.00x, and every one of those figures is identical on the HEAD half --- where Run 26's HEAD broke the fill tier at 1.41x and 2.61x on the two pointer arms. What broke that tier was GHC #27778 and the workaround closed it, which the allocation column saw before any time column could.

**What the second compiler is worth on this roster is almost nothing on every arm that carries a corrected time, and the arms whose times part hardest are the ones whose counts part least.** Read as `--compare` prints it, basis over HEAD, **the counted work sits at a geomean of 1.0020 over the 31 arms with a corrected time** and no arm is more than 0.9% off 1 --- the band runs 0.9936 to 1.0089, `bq-expand` at one end and `mut-odo-vecdims-add-in-leaf-u2-down` at the other, and the two pointer arms the workaround touched read 1.0051 and 1.0054. **The times say something else entirely on the same nine rows**: `libunord-stage1` reads 1.6213, `-stage5` 1.4505, `-stage4` 1.4247, `-stage3` 1.3381 and `-stage2` 1.2247, while the four `-sum` consumers go the other way at 0.8185, 0.8481, 0.8804 and 0.9852. Their counts read 0.9998 to 1.0000. **That gap is the correction and not the codegen**: those nine are the rows whose cells the arm empties, so their corrected net is a small difference of two large numbers and the per-shape ratios run 0.328 to 11.373 on one arm and 0.172 to 19.571 on another. The counts column is what says so, owing criterion nothing; without it the run would have had a 62% compiler difference to explain.

**What this pair still cannot separate is placement from codegen, and the four arms that used to show it are parked again as Run 26 said they would be.** `gen-unsafe`, `build`, `mut-odo` and their twins were the arms whose count ratio read 1.0000 while their times moved four to six percent, and none is on this roster; `mut-odo-vecdims-add-in-leaf-down`, `canon-vecdims`, `lib-stage2` and `lib-stage2-short`, lifted for Run 26 alone, went back to `Only` with it. **What this run has in their place is the same shape in a different guise**, and it is the nine `libunord` rows of the paragraph above: counts flat to a ten-thousandth, time apart by tens of percent, and the explanation this time a property of the correction rather than of the machine. The distinction the counts column exists to draw --- time moving with counts is codegen, time moving without them is the runtime or the memory --- has a third answer here, which is that the time figure is not measuring the arm at all.

**The leaf block reads the same ordering on both compilers for a third run, and this run reads it with the two pointer arms restored to the family's own tier.** The block is `-u1`, `-u2`, `-u2-down`, `-u2-last`, `-u1-ptr` and `-u2-ptr`, the counted bound `-down` having gone back to `Only` with Run 26's other three parkings. Unrolling buys **0.9650 of `-u1`'s time** on the basis, walking a `Ptr` instead of the odometer buys 0.9713 and 0.9419 on the two rungs, and the two effects compose at 0.9357 --- every one of them inside a point of its HEAD figure, 0.9599, 0.9751, 0.9386 and 0.9239. **The one rung that moved the wrong way is the one this run added**: `-u2-last`, which holds the bound as the run's last index and compares the cursor against it directly, executes **0.9691** of `-u2`'s corrected instructions and takes **1.0138** of its time on the basis and 1.0157 on HEAD. It is the only arm here whose instruction saving costs time, and registration (13) is killed on it.

**An arm outside the `mut-odo-vecdims` family leads every one of the ten classes for a second run --- and eight of the ten leads are arms this roster invented a day before it ran, which is a caution as much as a result.** The leads are `libunord-stage5-sum` on `rev`, `bcast`, `bcastmid` and `runs`, `libunord-stage2-sum` on `block` and `compose`, `libunord-stage4-sum` on `flip`, `libunord-stage3` on `window`, `lib-stage1` on `scaled` and `lib-stage2-lean-u1` on `small`. **The caution is that a `-sum` arm is a reducing consumer and not a fill**: it folds each slice as it comes and hands back a scalar, so it never writes the result vector the family's arms write, and its lead over `mut-odo-vecdims` --- 0.005 against 0.026 on `block`, 0.000 against 0.047 on `rev` --- prices a copy that the two are not both paying. Read as a like-for-like ordering it says nothing; read as registration (9) states it, it is the measurement the entry-point ruling asked for, and there it is unambiguous: `libunord-stage1-sum` leads its own fill `libunord-stage1` in all ten classes on both halves.

**Across the halves the classes do NOT agree this run, which is new: four put the basis ahead and six the HEAD half, and the vote over every arm is all but even.** Of the 310 arm-comparisons the ten classes carry, **9 are degenerate and not voted** --- seven on `flip` and one each on `runs` and `block`, every one a `libunord` arm whose basis cell the correction did not leave positive --- and of the 301 that vote, **141 put the basis faster and 160 the HEAD half**. The class geomeans run **0.9627 on `rev` to 1.0094 on `block`**, where Run 26 had every one of the ten below 1. **And five of the ten carry a baseline the reader refuses to read the pair through**: `list` moved past the 0.7% bar on `bcast` (0.9823), `window` (0.9768), `scaled` (0.9795), `runs` (0.9887) and `flip` (0.9928), so those five class lines are printed and are not the pair's variable --- which is the opposite of the main set, where `list` moved 0.33% and the two columns may be differenced for the first time since Run 24.

**The fourteen registrations came back seven HELD, six KILLED and one WITHDRAWN with its arm before the run --- and the seven include the two the pair was built for.** (1) the pointer fills on the second codegen: **HELD** in every clause and on both halves, three spans at 0.9713, 0.9357 and 0.9419 on the basis and 0.9751, 0.9239 and 0.9386 on HEAD, both fills at 1.00x on both. (2) the same two arms in counts: **HELD**, 1.0054 and 1.0051. (3) the basis half against Run 26's: **KILLED**, and by the box --- raw, every shared arm outside the `libunord` family reads between 0.9% and 4.6% faster; on `--bridge` nine of twenty are inside the registered 1% and seventeen inside 3.3%. (4) the rate an instruction saving reaches the clock: **HELD**, neither kill firing, 27% to 46% on the three spans. (5) the class-floor asymmetry: **HELD**, the basis the wider half in 6 of the eleven and both side correlations where the entry expects them. (6) the lazy unordered candidates: **KILLED** on `runs` and `block`, though the ceiling crossover landed inside the window predicted for it. (7) the lean trick on the unordered list: **KILLED** on `small` and `compose`. (8) the lazy ordered candidates: **KILLED** on all three of its kill conditions. (9) the reducing consumers: **HELD**, by the widest margins in the run. (10) the baseline between the halves: **KILLED**, `list` moving 0.33% where 1.1% was registered, which reopens the subtraction. (11) the dispatch pair: **WITHDRAWN** with the arm on 2026-09-07. (12) the class-level margin on the four reversed classes: **HELD**, all four inside the probe's band. (13) the hoisted bound: **KILLED**, 1.0138 and 1.0157 against a 1.005 kill. (14) the unroll under the library's dispatch: **HELD**, 1.0516 and 1.0414.

**Every one of the twenty-two processes gated clean**, `--selftest` and both `--aa` gates, so no time column here is uncorrected, and the plateau gate puts all twenty-two inside **19.2759 to 19.8264 ms/iter, a 2.86% spread** against the 5% band, where Run 26 read 2.48% over twenty-two --- the upper end being the reran `runs` process of a second window, which the band absorbs. **This run's floor is 0.83% on the basis half and 0.63% on the control**, read over the same SIX A/A pairs Run 26 read and wider than its 0.31% and 0.46% on both halves: `bq-expand-aa-distant` carries both figures, and 0.83% and 0.63% read on the six pairs and on the four of those six that carry back to Run 10 alike, so one floor is quoted here and not two. **Three of the six intervals cover 1 on each half** where Run 26 had five, and not the same three: the basis misses on `bq-expand-aa-distant`, `bq-expand-aa-adjacent` and `mut-odo-vecdims-aa`, the HEAD half on `bq-expand-aa-distant`, `list-aa-adjacent` and `list-aa-distant`. The A/A worst cells run 0.27% on `ghead-rev` to **9.19% on `ghead-window`**, on `window-224x224-k3-d2`, and that cell was read rather than averaged: the per-sample instrument puts the foreign CPU during its samples at 0.03 of a core with the RTS totals flat across the A/A triple, so it is a wild cell and not an intrusion. **Post-run step 3's rerun was taken on `runs`**, for a reason no A/A cell shows: four benches of the first `run27-ghead-runs` were intrusions by the instrument's own 0.25 bar, and the paragraph on the sequence above says what they moved and what replaced them. Four processes carry a worst cell past 5% --- `ghead-window` at 9.19%, `ghead-flip` at 5.79%, `g912-window` at 5.19% and `ghead-runs` at 5.09% --- each between 1.9 and 5.4 times its own population's floor rather than an order of magnitude outside it, `ghead-runs` narrowest at 5.09% against 2.73% and `g912-window` widest at 5.19% against 0.97%.

**The two halves resolve alike, and the one-sided reading Run 26 corrected stays corrected.** `CI%` --- the median half-width of a cell's own fit --- runs a geomean of **1.05** on the basis against the HEAD half across the roster, **18 arms wider here and 17 narrower**, where Run 26 read 0.98 at 14 and 16. The arm that moves most between the halves is `list`, 0.67 against 0.86, and eight arms do not move at all; `mut-odo-vecdims` reads 0.35 against 0.38 and `libunord-stage4` 0.02 against 0.04, the latter a fit over cells the arm empties rather than a bench that resolved well --- the four `-sum` consumers read 0.01 and are the smallest in the table. A cell resolving worse is not a cell measuring something different, and neither figure is read off the other: this column is sampling error inside one bench, where the A/A floor is agreement between two placements of one strategy.

**The sequence ran in TWO windows, because one intrusion was found and its population rerun.** Twenty-two processes from **02:22:28 to 09:33:51**, every one exiting 0 at its roster's count --- 665 twice, 490 twice, 210 twice, 175 six times, 140 four times and 105 six times --- with the gate's four before them from 01:49:09 and the four alone-leg riders after them to 09:46:23. The per-sample instrument reads the foreign CPU on every bench, as a RATIO of one core rather than a percentage, and calls anything at or above **0.25** an intrusion. **Two processes carried one.** On `run27-g912-main`, one bench of 665: `stretch-bigstride/liblist-stage4` at **0.28**, worst sample 109 ms foreign. It moved nothing past the floor --- that arm reads 0.9865 of `liblist-stage2` there against 0.9932 on the other half, with the two `stretch` shapes either side of it at 0.9991 and 0.9944, all of it inside the main set's 0.83% --- so it is recorded and not acted on, as Run 26's two were. **On `run27-ghead-runs` it was four consecutive benches of `runs-512` and it DID move them**: `-u1`, `-u1-ptr`, `-u2-down` and `-u2-ptr` at **0.96, 0.93, 0.90 and 0.30** of a core, worst samples 233 to 291 ms foreign, datable to about **06:38:19 to 06:38:54** by interpolating the log's own `cpu` counter between the driver's 06:13:06 and 06:55:43. Against `mut-odo-vecdims` those four cells read 1.1762, 1.0949, 1.1352 and 1.0217 where the basis half read 1.0646, 0.9478, 1.0257 and 0.9663 --- five to fifteen points high on one view, `-u1-ptr` the worst of them. **That is three minutes from where Run 26's second episode fell**, `apt-daily-upgrade.service` at 06:41 on one timed cell of its basis half: two runs, two mornings, the same quarter-hour. No journal was read for this run, so the clock is all this file can show; the machine's owner identified the same timer and disabled it before the rerun. **So post-run step 3's rerun was taken**, the timer disabled first, on 2026-09-08 from **11:54:26 to 13:19:29** --- `runs` on BOTH halves, since a pair read across two windows is not a pair --- through `run-major.sh`, whose guards hold each process to its bench count and its instrument lines. Both reran processes exited 0 at 490 benches with one `@@saturate` line each, and **neither carries a bench above 0.17 of a core**. The `runs` figures in this file are the second window's throughout; the first pair is kept beside them as `probe-intruded-*`. Nothing else in the evening is disturbed, the wall-clock log carries no `!!` line, and neither do the twenty-two counts files.

**Seventy cells sink below the shared forcing pass on the basis and forty-three on the control, and the count is a property of the roster rather than of either compiler.** Every one is a `libunord` arm, so nine rows on each half are geomeans over fewer shapes than the rest: on the basis `libunord-stage1` and `-stage4` over 13 of 19, `-stage5` over 14, `-stage2` and `-stage3` over 15, and the four `-sum` consumers over 7 or 8; on HEAD the same nine rows over 17, 18 and 10 or 11. **The eight arms of 2026-09-07 are what tripled it**: Run 26 had three such rows at 15, 16 and 16 of 19, and the L1 roster pass warned of nine before the evening was spent. What it costs is that no `libunord` row's main-set geomean is a like-for-like column entry, which is why registrations (6), (7) and (9) read those arms on the classes and why the reader now records their main-set spans NOT READ instead of adjudicating them.

**The counted work covers every population, no cell was refused anywhere, and its finding this run is that the compilers agree.** Twenty-two sweeps, both halves over all eleven populations, 22 files and not one `!!` line in any of them. Basis over HEAD on the main set the counted geomean is **1.0020** over the 31 arms carrying a corrected time and 1.0015 over all 35 with the controls, and no arm is 1% off 1 --- which, beside a time column that parts by up to 62% on nine rows, is what says those nine are the correction and not the codegen. **Within the basis half the counts price the run's own arms**, differencing each arm's instructions per iteration against the shared forcing term over the nineteen shapes: the two pointer fills read **0.8944 and 0.8358** of their parents and 0.8605 against each other, reproducing to four figures the three ratios the registration was derived from; `-u2-last` reads **0.9691** of `-u2`, reproducing the pre-run probe's 0.9688; and `lib-stage2-lean-u1` reads **1.0878** of `lib-stage2-lean`. Against the times those give a conversion of **27%, 35%, 46% and 59%**, and one arm at which no saving arrives at all.

**The correction sits on the same footing in both halves, as it has on every run since Run 17.** The two `sum-only` halves --- the same term timed twice under two names --- agree at **1.0001** on the basis and **0.9998** on the HEAD half, so the quantity subtracted from every cell is the same quantity in both columns. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0355 and 1.0756** on the basis and **1.0286 and 1.0754** on HEAD for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent under the term it stands for, by the same few percent on both compilers. What the correction cannot do is rescue a cell it is larger than, which is the seventy and forty-three of the paragraph above.

**The ceiling still cannot be read, and what stands in its place is now measured on both compilers rather than one.** `mut-odo-vecdims` against `bq-scan-rem-gm-mulback`, the fastest arm needing nothing at all, was this file's ceiling reading for eight runs back to Run 17; the denominator went to `Only` on 2026-09-04 and no process since has timed it, so that series still ends at Run 24. In its place stand claim 10 inside the family, 0.6380 and 0.6392, and **the two pointer fills, which are a ceiling of a different kind** --- not a candidate the library could take, the `Ptr`-walking form being refused for it ([dead ideas][dead]), but a measurement of what each parent would reach under an allocator that spilled nothing. Both reach it on both halves this run, where Run 26 had them on one: `-u1-ptr` is 2.9% ahead of `-u1` and `-u2-ptr` 5.8% ahead of `-u2` on the basis, 2.5% and 6.1% on HEAD. **What the run adds to the ceiling section is another reading of its rate**, 27% to 46% across the three pointer spans against the nineteenth reading's three quarters, with `lib-stage2-lean-u1` at 59% from outside the family and `-u2-last` below zero --- so the range is fixed at both ends and it does not contain three quarters.

**This run's two columns MAY be differenced, which no run since Run 24 could say.** `list` moves **0.33%** between the halves on the main set --- 0.9967 basis over HEAD, over all 19 shapes, the basis faster on 13 of them --- against the 0.7% bar, where Run 25 read 1.10% and Run 26 1.16% and both were refused. **And the counted work says what the movement is**: `list`'s instructions read 0.9965 across the halves at a time-over-counts of 1.0002, so the baseline moved because the basis executes a third of a percent less work, not because its bytes landed differently. That kills registration (10), which predicted the 1.1% of the last two runs would repeat, and it says the movement those runs saw was their rosters' rather than this pair's. The classes do not follow: five of the ten carry a `list` move past the bar, `window` widest at 2.32%, so a class line there is printed and is not read for the pair's variable.

**The straddling loops stand at SIX on each half, and on the basis the `-g3` twin names all six for the first time --- four of them timed leaf fills, the shipped `-u2` among them.** Each half's self-loops of at most 64 bytes in `Main`-compiled code number **184 on the basis and 181 on HEAD**, 72 and 69 of them at offset 0. Post-run step 0's twin names every one of the basis's six by byte identity: `fillStage2Short` at `0x4256aa` and `fillStage2` at `0x428c2a`, 55-byte bodies at offset 42; `fbMutOdoVecdimsAddInLeafU2` at `0x43abaa`, 55 bytes at offset 42; `fbMutOdoVecdimsAddInLeafU2Down` at `0x437b6f`, 53 bytes at offset 47; `fbMutOdoVecdimsAddInLeafU2Last` at `0x439b6b`, 58 bytes at offset 43; and `fbMutOdoVecdimsAddInLeafU2Ptr` at `0x47a45d`, 48 bytes at offset 29. **Every one of the six straddles in the twin at the same offset**, which is what makes the naming a reading rather than a coincidence, and every one is the outer head of a rotated pair --- the shape the dead-spot form leaves by design, its straddle paid once per exit of the inner rather than per iteration. **The HEAD half's twin names two of its six and refuses four**, holding no byte-identical copy: `-g3` is a different program on that compiler, its twin carrying 184 self-loops where the timed binary carries 181, and the refusal is what keeps the four from being named by proximity. Within the pair, `--library` reads **12.8% of the 141 library self-loops at the same offset in line and 63.8% in the same straddle state**, inside the tenth-to-a-quarter band this README gives a pair that varies a compiler.

**The regime was confirmed in the binary before the hours were spent**, which nothing afterwards can: a `diag` in the run's own regime puts `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512` on the basis, a ratio of **1.000** where plain -O1 is ten times apart. That is `-fspec-constr` in the binary rather than on a command line, which is the only place the question can be settled.

**Run 27 records every population twice** --- the main set and all ten stride classes from each half, one process each --- **in one window**, as Runs 23 to 26 did. Every one of the twenty-two exited 0 at the bench count its roster holds, and the roster is larger than any before it: **665 benches on the main set against Run 26's 570**, from 35 timed arms over the same 19 shapes, and 1820 class cells against 1560 over the same 52 views of the same ten classes. Ten timed arms landed and five left, five net, and no class population moved, which is why only `scaled` was named for the pre-run L1 pass.

**The decomposition reproduces on both halves for a ninth run.** The riders time each shape's `list` by itself, one bench per process on that half's own binary, `SAT=` off and on: the saturated legs split the deflation into the state the preamble puts on a clean process --- **+12.25%** on the basis and **+11.92%** on HEAD --- and everything else the roster does to a cell, which is **+0.47%** and **+0.45%**. The totals are +12.78% and +12.42%, over 19 shapes apiece, least on `cnn-slice-c32` at 0.9946 and 1.0006 and most on `stretch-tall-Mx2` at 1.2508 and 1.2183. So all but half a percent of what a roster cell carries over its own alone leg is the saturated state, and the two compilers agree on that to three hundredths of a point.

**Everything in this file is replaced by the next run, which is what makes it a file.** What a run replaces OUTSIDE it, in README.md and in the sources, is [README's own Provenance](../README.md#provenance). None of it is portable: a run on another machine is a different measurement rather than a repetition, and this run is the first since Run 18 whose own machine check says the box under it moved --- 3.66% on every shape at once, which the next run reads against this file's fingerprint and not Run 26's.

## Results

The shared forcing pass is subtracted here, as every run since Run 6 must ([sum-only](../README.md#sum-only-and-the-correction-now-applied) carries that decision and this run's re-pass of its gates), the scratch vectors are the unboxed ones the shipped code uses, as they have been since Run 7 ([the scratch vector flavour](../README.md#the-scratch-vector-flavour) says what that severed), and **this is a `-fspec-constr` table**: it is not the regime `Data/Array/Internal.hs` compiles under. **A row's distance from Run 26's column carries a roster term, an evening AND a box change**, `Main.hs` having moved from `659d898` to `bb6d113` by the commits of 2026-09-07 --- ten timed arms landed and five left, five net, no shape and no class view moved --- and the gate's machine check having read `list`'s net 3.66% below the fingerprint Run 26 kept. **So the raw distance is not readable and the bridge is**: every shared arm outside the `libunord` family sits between 0.9% and 4.6% below its Run 26 cell before the box is divided out, and after it, as a ratio to `list` in each run, the twenty read a geomean of 0.9940 with seventeen inside the 3.3% drift band. **The `alloc` column is the one thing the box cannot touch**: pinned to the eighteen shapes claim 7 keeps, this run reads the fills 1.00x, the list consumers 2.00x, `bq-expand` 2.06x and `list` 22.38x, the same five figures for a fifth run and, this time, the same on both halves.

**And it is the basis half's**, `run27-g912`, as every published table here is from Run 11 on: the HEAD half's column sits beside the basis one in [What the next run compares against](#what-the-next-run-compares-against) rather than as a second copy of these thirty-five rows. That the published half is the 9.12 one is this pair's own decision --- it keeps the lineage, being the compiler every figure in README was measured through, and the dead-spot form it carries is the basis recipe decided on 2026-09-02 --- and the HEAD half is the reading a form about to be published is owed on a second compiler. **Ten rows here are first readings at full budget**, all of them landing on 2026-09-07: `libunord-stage4` and `libunord-stage5`, the four `-sum` consumers, `liblist-stage3` and `liblist-stage4`, `mut-odo-vecdims-add-in-leaf-u2-last` and `lib-stage2-lean-u1`. **Five rows Run 26 published are gone**: `lib-stage2-disp`, retired that same evening, and the four arms Run 26 lifted out of parking for itself alone, `mut-odo-vecdims-add-in-leaf-down`, `canon-vecdims`, `lib-stage2` and `lib-stage2-short`, all four re-parked with it.

**Comparing runs?** The table below is Run 27's own; what to hold a new run against is [What the next run compares against](#what-the-next-run-compares-against), the claims to test are [the ones after it](#the-claims-the-next-run-should-test), the absolute anchor is under [Provenance](#provenance) below and the population it was measured over in [README's delta chain](../README.md#provenance), and this run's own floor --- no A/A pair further than **0.83%** from 1 on the basis half or **0.63%** on the control, the HEAD half, read over the six pairs the prune leaves and over the four of those six that carry back to Run 10 alike, beside which the worst SINGLE cells of 3.63% and 2.91% are not floors at all and are not to be quoted as any --- is [in the floor section][floor], which is where the figures are DEFINED and which of them answers what: this file quotes them and does not re-derive the rule. **And one thing this run has that its predecessors did not**: the gate's machine check fired, so an absolute here is not comparable with Run 26's without the 3.66% the box moved, while every ratio is.

**It is the main set's table**, and every column below is a statistic of that population: each stride class has a table of its own, on the same rows and in the same columns but its own basis, in [The stride classes, run by run](#the-stride-classes-run-by-run). No figure crosses between them.

How to read the columns:

- **time** is the geomean over **every** shape of the per-shape OLS *slope*, less that shape's forcing term, over `list`'s slope less the same term, with the per-shape log-ratios *winsorized* first --- capped at the row's own median plus or minus three MADs, the MAD scaled by 1.4826 so the cap is in standard deviations. Nothing is dropped by the estimator, so winsorizing costs no row its population and a cell far enough out to distort the mean has its influence bounded instead of its evidence deleted. **What DOES cost a row its population on this run is the correction, not the estimator**: three rows on each half carry cells the shared forcing pass is not smaller than, and such a cell cannot be corrected at all --- so the three `libunord` rows are geomeans on 15, 16 and 16 of 19 shapes on the basis and 16, 15 and 15 on the HEAD half, they are named in the head, and every other row covers all 19. The `CI%`, `smp` and `alloc` columns stay raw: subtracting a shared term moves a point estimate, it does not make a cell better measured. `worst` is a ratio of nets, as `time` is, just per shape and unwinsorized.

  **This replaced a trim** --- drop each strategy's single highest-CI shape --- and the ruling is worth keeping because the trim looks obviously right and is not. It selected on CI, and criterion spends a *time* budget, so a slow cell buys fewer samples and a wider CI: measured on Run 6, the cell it removed was above its own row's geomean in **30 of 41** rows, p about 0.003. It therefore deleted each strategy's worst evidence, differentially, and a catastrophic shape is exactly the shape it would remove: `bq-expand-lemire-out` loses on one shape of 33, and that shape was the one trimmed from its column. Because the cell removed differed by row, two published columns were also geomeans over different shape sets, which is why a published A/A ratio used to disagree with its paired one. Swapping estimators costs a median 2% and moves one row (`mut-offsets`) by 14%, that row having been flattered all along; it buys back exact comparability, and `--selftest` now asserts published == paired for every uncapped pair.

  **Don't reach for inverse-variance weighting**, which is the standard-looking repair and is worse than what it repairs. It assumes every shape estimates one ratio and differs only in precision, where here the between-shape variance runs a median 5,000x the within-shape kind --- the heterogeneity is the README's finding, not its error --- so weighting by precision collapses the effective shape count from 33 to about nine and hands a quarter of the weight to the smallest shape in the set. Worse for the purpose: a catastrophically slow cell buys fewer samples, so it has a wider CI, so IVW discounts precisely the cells the trim used to delete --- the same failure made continuous, not a repair of it.

  **The *slope* rather than criterion's mean, because criterion never times one call**: it times batches --- one call, then four, then twenty --- and every batch also pays for starting the timer and for the first pass through cold code and cold data. A mean divides each batch's time by its calls, so that fixed cost is smeared across them and weighs most in the small batches. The slope is the line through those points: how much more time one *additional* call adds, leaving the fixed part behind as the line's height at zero. On the microsecond shapes, hundreds of samples and no warm-up worth speaking of, the two agree. They part on the slow shapes, where the early batches run cold: there the mean reads high, and by different amounts for different strategies --- which is exactly the part that dividing by `list` cannot cancel. It also keeps `CI%` and R^2 describing the number the table shows, both being properties of that same fitted line.
- **worst** is the row's largest per-shape ratio to `list` --- the shape on which that strategy does least well against the baseline. It is what claim 1 is about, and it is raw rather than winsorized.
- **CI%** is the median across shapes of the slope's confidence half-width as a percentage of the slope --- "how many digits are real". 0.5% is three; 5% is one.
- **smp** is the median sample count. Criterion spends a time budget, so a slow call buys fewer samples; this is where that shows.
- **alloc** is bytes per call as a multiple of the result vector (`8*l`), the median over shapes of the `allocated` fit the harness now runs on every bench of every shape. The multiples were held to be shape-independent --- refitted on a different shape, every one reproduced to within 0.4% --- so that the median was a formality rather than a smoothing and the column did not move with what it was fitted on. **That is wrong**, and Run 6 (-O1) reproduced the refutation at full budget where a rough pass had found it. Re-derived on Run 9's cells and roster it is unanimous: **every one of the 32 benched rows** varies by more than 5% from shape to shape, the median row by 2.00x and the worst by 5.10x (`bq-expand-b`, 1.00x to 5.10x), and the four shapes of identical `l` = 1800000 give `bq-expand` 2.000x, 2.111x, 1.000x and 2.639x. The spread narrowed as the roster was cut --- Run 6's worst was an arm nothing times any more --- and the property it measures did not. Every allocated fit sat at R^2 1.000 on Run 6, so the spread is the quantity and not the measurement, and allocation being deterministic per call the budget does not bear on it either way. What does survive is the column: a median over a *pinned* shape set reproduces, which claim 7 now carries on a live basis, every allocation tier returning on its own level across a roster change. So read `alloc` as a statistic of a strategy **and** a shape set, and pin the shape set before comparing it across runs, exactly as the `time` column already asks. It is the one column the correction does not touch.

| strategy | time | worst | CI% | smp | alloc | needs |
|---|---:|---:|---:|---:|---:|---|
| *bq-expand-nosum* | *--* | *--* | *0.61* | *60* | *2.11x* | *its base arm, forced with one element* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.35* | *78* | *1.00x* | *the same, on the fastest arm* |
| *sum-only-early* | *--* | *--* | *0.02* | *83* | *0.00x* | *the term every row has subtracted* |
| *sum-only-late* | *--* | *--* | *0.04* | *83* | *0.00x* | *the same, at the other end* |
| libunord-stage3 | 0.000 | 0.050 | 0.02 | 83 | 0.00x | new mutating `Vector` method -- the unordered entry point walking the destination in address order |
| libunord-stage2 | 0.000 | 0.055 | 0.02 | 83 | 0.00x | new mutating `Vector` method -- the branch's driver behind the unordered one-block test |
| libunord-stage5 | 0.000 | 0.050 | 0.03 | 83 | 0.00x | new mutating `Vector` method -- the unordered list kept lazy to the exception and read in address order, under the lean rank test |
| libunord-stage4 | 0.000 | 0.054 | 0.02 | 83 | 0.00x | the same, under the natural-strides test |
| libunord-stage1 | 0.000 | 0.051 | 0.04 | 83 | 0.00x | new mutating `Vector` method -- stage one behind the unordered one-block test |
| libunord-stage5-sum | 0.001 | 0.050 | 0.01 | 83 | 0.00x | new mutating `Vector` method -- `sumT` over stage five's list, one slice at a time and no concatenation |
| libunord-stage4-sum | 0.001 | 0.055 | 0.01 | 83 | 0.00x | the same, over stage four's list |
| libunord-stage2-sum | 0.001 | 0.055 | 0.02 | 83 | 0.00x | the same, over the branch's port |
| libunord-stage1-sum | 0.002 | 0.052 | 0.02 | 83 | 0.00x | the same, over stage one's list, which is master's consumer |
| lib-stage2-lean | 0.028 | 0.128 | 0.60 | 69 | 1.00x | new mutating `Vector` method -- the branch's driver, dispatch without the strides comparison |
| lib-stage1 | 0.028 | 0.128 | 0.58 | 69 | 1.00x | new mutating `Vector` method -- stage one as it shipped, dispatch included |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.029 | 0.128 | 0.51 | 69 | 1.00x | new mutating `Vector` method |
| mut-odo-vecdims-add-in-leaf-u2 | 0.029 | 0.129 | 0.49 | 69 | 1.00x | new mutating `Vector` method -- what `genericFillStrided` is a port of |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.029 | 0.128 | 0.49 | 70 | 1.00x | the same, unrolled -- the shipped fill's ceiling under an allocator that spills nothing |
| lib-stage2-lean-u1 | 0.029 | 0.125 | 0.61 | 69 | 1.00x | new mutating `Vector` method -- the lean dispatch with the stepping run not unrolled, the unrolling's control |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.031 | 0.127 | 0.58 | 69 | 1.00x | nothing the library would take -- the merged-bound leaf walking a `Ptr`, a ceiling and not a candidate |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.126 | 0.57 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill's leaf with the bound merged in and the body not unrolled |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.031 | 0.128 | 0.50 | 69 | 1.00x | new mutating `Vector` method -- the shipped fill with its look-ahead hoisted out of the loop's guard |
| liblist-stage4 | 0.048 | 0.161 | 0.93 | 64 | 2.00x | new mutating `Vector` method -- `toVectorListT` kept lazy and canonicalized, under the lean dispatch |
| liblist-stage2 | 0.049 | 0.161 | 0.90 | 64 | 2.00x | new mutating `Vector` method -- the branch at the list entry point |
| liblist-stage3 | 0.050 | 0.161 | 0.91 | 64 | 2.00x | new mutating `Vector` method -- the same, under the natural-strides dispatch |
| *mut-odo-vecdims-aa* | *0.050* | *0.127* | *0.40* | *66* | *1.00x* | *A/A control* |
| *mut-odo-vecdims-aa-distant* | *0.051* | *0.127* | *0.42* | *66* | *1.00x* | *A/A control* |
| **mut-odo-vecdims** | **0.051** | 0.128 | 0.35 | 66 | 1.00x | **new mutating `Vector` method -- THE FIX, decided 2026-08-22** |
| liblist-stage1 | 0.051 | 0.161 | 0.91 | 64 | 2.00x | new mutating `Vector` method -- stage one at the list entry point |
| bq-expand | 0.112 | 0.208 | 0.72 | 53 | 2.11x | nothing (pure) -- the last candidate |
| *bq-expand-aa-adjacent* | *0.112* | *0.207* | *0.64* | *53* | *2.11x* | *A/A control* |
| *bq-expand-aa-distant* | *0.113* | *0.207* | *0.40* | *54* | *2.11x* | *A/A control* |
| list (baseline) | 1.000 | 1.000 | 0.67 | 22 | 22.64x | -- |
| *list-aa-adjacent* | *1.000* | *1.012* | *0.52* | *22* | *22.64x* | *A/A control* |
| *list-aa-distant* | *1.001* | *1.012* | *0.74* | *22* | *22.64x* | *A/A control* |

**DO NOT DIVIDE TWO ROWS OF THIS TABLE FOR A MARGIN.** The `time` column is a geomean over shapes of net over `list`'s net, WINSORIZED per row, so a ratio of two of its entries equals the per-shape paired ratio only where neither row had a cell capped --- and pairs still part in SIGN between the two on this run, `mut-odo-vecdims-add-in-leaf-u2-ptr` against its parent among them, where dividing the rows gives 1.0027 and the paired figure is 0.9419. That is half the parting Run 26 read on the same pair, 1.0688 against 0.9479, the row having narrowed across shapes; the sign still crosses. A margin between two arms is `--pair`'s paired figure, which is also the statistic the floor is defined in; `--pair` and `--predictions` print both side by side and say when they part. The column is for reading the table, not for differencing it.

`concat-runs` has no row, and neither do the other 58 arms the roster holds and checks without timing --- 59 of its 89 in all: the reason is at each entry and the count is [`--lint`'s](../README.md#the-reader-read-runpy). So a movement below is a movement on the 19 arms this run and Run 25 both give a corrected time, on two builds of one recipe a day apart, and it is a roster change and an evening and not a code change.

**Three things in the table are the run's findings rather than its numbers.** **The head of the table gained ten rows and lost five**: `mut-odo-vecdims` reads 0.051 with **twenty-one** timed arms below it and **one** level, `liblist-stage1` --- where Run 26 read seventeen below and none level. The twenty-one are the five `libunord` fills at 0.000 and their four `-sum` consumers at 0.001 and 0.002, `lib-stage2-lean` and `lib-stage1` at 0.028, `-u2-down`, `-u2`, `-u2-ptr` and `lib-stage2-lean-u1` at 0.029, `-u1-ptr`, `-u1` and `-u2-last` at 0.031, and `liblist-stage4`, `-stage2` and `-stage3` at 0.048 to 0.050. **Nine of the twenty-one are `libunord` rows whose cells the arm empties**, so their place at the head of the table is the correction and not a speed: on most main-set shapes those arms return one slice and the cell IS the forcing pass. **The ceiling that ran for eight runs still cannot be read at all**: `bq-scan-rem-gm-mulback`, the denominator, is parked, and what stands where it stood is claim 10 inside the family. **And the `alloc` column is unmoved for a fifth run once the shape set is pinned** --- the fills at 1.00x, the `libunord` arms at 0.00x, the `liblist` arms at 2.00x, `list` at 22.38x and `bq-expand` at 2.06x --- **and this time it is unmoved on the other half too**, where Run 26's HEAD broke the fill tier at 1.41x and 2.61x on the two pointer arms and this run reads both at 1.00x.

**The pointer fills are what this run was built to read, and what they price is a ceiling that now holds on BOTH codegens.** `mut-odo-vecdims-add-in-leaf-u1-ptr` and `-u2-ptr` are the shipped fill's leaf and its unrolled form rewritten to walk a `Ptr`, which takes out the `0x40(%rsp)` reload the twentieth ceiling reading found; they are refused for the library ([dead ideas][dead]) and timed as a ceiling and nothing else. **On the basis half all three spans read as registered**: `-u1-ptr` over `-u1` **0.9713** at 15 of 19, `-u2-ptr` over `-u2` **0.9419** at 14 of 19 and `-u2-ptr` over `-u1-ptr` **0.9357** at 16 of 19, with published columns 0.9919, 1.0027 and 0.9354 --- the middle one parting in sign from its paired figure, as it did on Run 26 and by half as much. **On HEAD every one of the three now agrees with the basis**: 0.9751, 0.9386 and 0.9239, where Run 26 read 1.3084, 2.6731 and 1.9710 and where `-u2-ptr` allocated 2.61x the result. Both fills read 1.00x on both halves here. What changed is not the fills but the compiler they meet: GHC #27778, filed 2026-09-06 with a workaround, and the workaround is the `:: Ptr Double` on every bang-bound `plusPtr` result. **What the pair prices, now that it can be read at all, is the RATE.** The counted work puts the three spans at **0.8944, 0.8358 and 0.8605** of their parents in corrected instructions --- reproducing to four figures the ratios Run 26's registration was derived from --- so against the times, 27%, 35% and 46% of each instruction saving reaches the clock. That is the third to a half [the open list][open] records and not the three quarters the nineteenth ceiling reading measured across two builds, and this run fixes the range at both ends from outside the family: `lib-stage2-lean-u1` converts at 59% and `mut-odo-vecdims-add-in-leaf-u2-last` converts a real 3.1% saving into a 1.4% LOSS.

**The two standing placement controls are still gone with the prune, and this run is the first whose `-g3` twin names every straddler on the basis half.** `mut-odo-vecdims-add-in` against the arm it varies, and `build` against `mut-odo`, were the two pairs this file read every run for what a slot costs an identical body; both arms of each went to `Only` on 2026-09-04, and the four parkings Run 26 lifted went back with it, so nothing on this roster turns an offset into a time. What is left that speaks to placement is the six A/A pairs, which are two placements of one strategy and give the floor, and post-run step 0's two twins. They put the basis half's six tracked fill heads at offsets **0, 0, 0, 24, 0 and 4** and HEAD's at **18, 7, 31, 7, 9 and 26**, under one function name apiece and in the same address order, so the two compilers pin the same group differently rather than carrying different groups; the basis's second group of two sits at 0 and 0 and HEAD carries the same two-copy group at 0 and 0, with a third group of three 7-instruction copies at 0, 0 and 21 beside it. **And the basis half straddles six loops, every one of them named**, where Run 26's twin named five of six and refused a sixth that turned out to be no loop at all: `fillStage2Short`, `fillStage2`, `-u2`, `-u2-down`, `-u2-last` and `-u2-ptr`, each the outer head of a rotated pair and each straddling in the twin at the same offset. **The 3% [the open list][open] has asked about since Run 10 is not readable on this roster either**, and the entry says so.


## What the next run compares against

**Run 28's regime and pair are NOT settled here.** What this run leaves settled is the regime, `-fspec-constr`, as every run since Run 8, and the basis recipe, the dead-spot form decided on 2026-09-02, which has now published four runs. The pair is whoever asks for the run to decide, and this run closes the question that kept the compiler on the variable for four runs running: **GHC #27778 is worked around and the two codegens now agree on every arm the roster carries**, at a counted geomean of 1.0020 with no arm 1% off 1, and the three pointer spans within a point and a half of each other on the two halves. A fifth compiler pair would be asking a question this one answered. **What the roster leaves open is larger**: the four lazy candidates and the four reducing consumers were timed for the first time here, six of the eight registrations written over them were killed, and none of the killings is a measurement anyone would want repeated on a second compiler --- they are decisions about code, and the code is what should move before the next pair is chosen.

**The compiler variable was asked a fourth time on this form, and this is the run that closes it.** Run 24 varied the compiler over 52 timed arms and read the two 5.7% and 6.8% apart on the instructions of two pure arms; Run 25 read `list` 1.10% apart in time and 0.33% in counts; Run 26 found twenty-four of twenty-six arms inside a percent in counts and the two `Ptr` arms outside at 0.6219 and 0.9295, which was GHC #27778. **This run reads every arm inside a percent in counts, the two `Ptr` arms at 1.0054 and 1.0051 among them, and `list` 0.33% apart in time with its counts moving the same 0.35%.** So the compiler difference this form has been chasing since Run 24 was one codegen defect and a baseline that moved with its own instruction count, and neither survives. What does not close is the class level: five of the ten classes carry a `list` move past the 0.7% bar, so a class-level comparison of the halves is still not a controlled one.

**What Run 27 leaves the next run to read against, and the first item is not a figure. The box DID change**, its gate machine check reading **-3.66%** against the fingerprint Run 26 kept, over all 19 shapes, worst `stretch-square-1341` at -8.26% and three shapes past 5%, with the worst residual about that geomean -4.78%. Every shape moved together, so one number describes it and every ordering survives; no absolute in this file may be differenced against Run 26's, and the fingerprint below is the one a Run 28 machine check reads against. **The floor is 0.83% on the basis and 0.63% on the control, and it is ONE figure as it was on Run 26**: the same six A/A pairs, four of them carrying back to Run 10, and the restricted figure over those four is the same 0.83% and 0.63%. A Run 28 margin is judged against those two and against nothing older. **The two columns below MAY be differenced**, `list` having moved 0.33% on this run's main set against the 0.7% bar, which no run since Run 24 could say --- and the counted work says the move is instructions, `list` reading 0.9965 across the halves at a time-over-counts of 1.0002.

**Registered with the pair.** Run 27's fourteen registrations, their kill conditions and their verdicts are [in this file's last section](#what-this-run-was-built-to-answer-and-what-it-answered), and the commands that produced them were the pair note's, which goes with the binaries and is offered for deletion with them. Seven held, six were killed and one was withdrawn with its arm before the run. **What a next registration should take from this one is where the six killings fell**: not on the pointer fills, which held in every clause, but on the eight arms of 2026-09-07 --- the lazy ordered candidates on all three of their kill conditions, the lazy unordered port on `runs` and `block`, the lean trick on `small` and `compose`, and the hoisted bound on both halves. **And one item was killed by the box rather than by anything it registered**, (3), whose whole subject was a cross-run column; it is the one to re-register if the machine question is answered.

**The position term was the candidate Run 15 promoted, and the probes have since spent it.** What Run 14 first saw and Run 15 confirmed is resolved as small-pinned churn --- selector found, ladder re-sized, no poison set --- in [the position-term entry][open] and `small-pinned-churn-investigation/nursery-position-findings2.txt`, so the roster-order pair this paragraph used to ask for is not owed: the corrected scans priced the term per shape in filtered processes, without a pair and without a layout term to argue about.

**The allocation area has now been priced twice and does not want a third pair** --- a ruling superseded in scope, 2026-08-19, and kept because what it refused stays refused. Run 14 took the area at `-A1G` and could not subtract its halves' absolutes; Run 15 took it at `-A32m` and found the cost at about 6% of the roster's time --- so re-PRICING default-against-enlarged is spent, and Run 16's pair does not do that: it changes the published basis to `-A32m` and reads `-A64m` against it, the one comparison neither earlier pair made and the one the churn findings' recommendation turns on, in the saturated in-process state both halves share. On 2026-08-21 the area was fixed at `-A32m` outright, here and in every horde-ad suite, so the one-binary runner this section used to ask for is not owed, and no further `-A` question is the README's.

**Where a run changes basis, the new basis is checked against the half at its OWN allocation area and against no other**, which is the rule the Run 15 to Run 16 change settled and the one place *against the previous run* can still be ambiguous. **The six figures that follow are Run 16's, are no longer checkable, and are stamped so that no later run reads them as its own**, `run15-*` and `run16-*` having been deleted; they are kept as the evidence the ruling was taken on. Against `run15-a32m` Run 16's three anchors read **-0.66%, -1.01% and -0.06%**, every one well inside the 2.32% floor it measured; against `run15-lookrts` the same three would have read **+8.81%, -9.57% and +7.74%**, which is the allocation area and not the shapes, and would have put all three outside that floor for a reason that is not theirs. Distance from a half at another area is that area plus whatever else moved; only distance from the half at this run's own area is drift.

**A pair's two halves are never folded into one.** Merging them puts back, in the record built to outlive every artifact, exactly the term the pairing exists to separate --- and what a given pair's two columns price is that run's own file's to say, not this section's. `--check-doc` catches one half of it: a run named aligned must also be named unaligned. Pruning an aligned column, merging two, and naming a second half accurately are the reading's to catch --- the check cannot demand an unaligned half of every pair without failing the last two runs, which have none, nor an aligned column of every run without failing Runs 6 through 9, which had none either.

**The next run compares against Run 27 and against nothing before it.** Each run's figures and the names of its halves are in its own file, `runs/run<N>.md`, back-filled to Run 7 on 2026-08-29; a comparison reaching further back is a chain of one-step comparisons, each recorded by the run that made it, and walking that chain here is what this section stopped doing. So an older run is read by opening its file, and the one step this run records is Run 26 to Run 27 --- which the box move makes a bridge rather than a subtraction, every absolute here sitting between 0.9% and 4.6% below its Run 26 counterpart for a reason no arm owns. **The table below is this run's own two halves and no earlier run's**, fifteen strategies over the nineteen main-set shapes, the emphasised column being the basis and so this run's published one, and the two differing in the compiler and in nothing else:

| strategy | Run 27 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) | Run 27 (SpecConstr, dead-spot +lookrts, -A32m, GHC HEAD) |
|---|---:|---:|
| `mut-odo-vecdims` | **0.051** | 0.050 |
| `mut-odo-vecdims-add-in-leaf-u1` | **0.031** | 0.031 |
| `mut-odo-vecdims-add-in-leaf-u2` | **0.029** | 0.029 |
| `mut-odo-vecdims-add-in-leaf-u2-down` | **0.029** | 0.029 |
| `mut-odo-vecdims-add-in-leaf-u2-last` | **0.031** | 0.031 |
| `mut-odo-vecdims-add-in-leaf-u1-ptr` | **0.031** | 0.032 |
| `mut-odo-vecdims-add-in-leaf-u2-ptr` | **0.029** | 0.028 |
| `lib-stage1` | **0.028** | 0.028 |
| `lib-stage2-lean` | **0.028** | 0.027 |
| `lib-stage2-lean-u1` | **0.029** | 0.027 |
| `liblist-stage1` | **0.051** | 0.049 |
| `liblist-stage2` | **0.049** | 0.047 |
| `liblist-stage3` | **0.050** | 0.048 |
| `liblist-stage4` | **0.048** | 0.047 |
| `bq-expand` | **0.112** | 0.112 |

**A published geomean is over the same 19 shapes, and two halves of one SpecConstr run usually share a denominator too**, `list` moving under 0.7% between them --- so such a pair may be subtracted and not merely ordered, which is what an -O1 reading cannot do at an 8% baseline shift. **THE TABLE ABOVE IS SUCH A PAIR**, `list` having moved 0.33% on this run's main set, the first since Run 24 to clear the bar: the two columns may be differenced. Eight of the fifteen rows are higher on the basis, six print the same at three decimals, and one --- `mut-odo-vecdims-add-in-leaf-u1-ptr` --- is higher on HEAD; eleven of the fifteen differ by at most a thousandth and four --- `lib-stage2-lean-u1`, `liblist-stage1`, `liblist-stage2` and `liblist-stage3` --- by two. **So the reading of this table is that there is nothing in it**, and that is the run's result rather than a shortfall: the arm this pair was built to separate, `-u2-ptr`, prints 0.029 against 0.028 where Run 26 printed 0.030 against 0.089. Take any size from the counted work, which owes criterion nothing and puts every arm within 0.9% of 1. `--compare --bridge` reads 1.0468 over the thirty arms it covers with nine outside its 3.3% band, and eight of the nine are `libunord` rows whose cells the arm empties --- the bridge divides out a baseline and cannot divide out a denominator that went to zero. The ninth is `lib-stage2-lean-u1` at 1.0331, an ordinary arm on all nineteen shapes, and it is outside the band because the pair really does differ by that much. **A pair that varies the allocation area is the other exception, and its two halves may never be subtracted from each other.** `list` moved **9.20%** between Run 14's halves, **5.13%** between Run 15's and **16.51%** between Run 16's. **Run 16's is the largest of the three and was registered to be the smallest**, on the reasoning that its two halves both sit at enlarged areas where the earlier two each crossed the default --- a prediction refuted by its own run, and the refutation is the finding: what moves the baseline is not the distance from the default but the in-process deflation, which at roster scale is worse at 64 MB than at 32 MB by more than the whole default-to-32 MB step was worth. So the exception widens rather than narrowing, and it covers every pair that varies the area at all. Every cell of such a pair's second column is scaled by a denominator the pairing moved: read it for the pairing's direction, take no strategy quality off it, and read the arm-by-arm comparison at the head of this file instead, which divides absolutes rather than ratios.

**The HEAD half's own standings on the arms this run's roster brought, which no table here carries, every published table being the basis half's.** Read off the HEAD half's main-set process with `--pair`, paired geomeans over all 19 main-set shapes, with the basis half's reading in brackets: `mut-odo-vecdims-add-in-leaf-u2` against `-u1` **0.9599** at 13 of 19 (0.9650 at 15 of 19) and against `mut-odo-vecdims` 0.6392 (0.6380); `-u2` against `-u2-down` 0.9991 (1.0014), a tie on both halves and inside each floor; `lib-stage1` against `-u2` 1.0400 (1.0360); `lib-stage2-lean` against `lib-stage1` **0.9346** at 15 of 19, sign p 0.019 (0.9572 at 10 of 19, p 1) and against `-u2` 0.9720 at 17 of 19, p 0.00073 (0.9917 at 13 of 19, p 0.17); `liblist-stage2` against `liblist-stage1` **0.9486** at 17 of 19, p 0.00073 (0.9743 at 11 of 19, p 0.65); and `liblist-stage4` against `liblist-stage1` 0.9533 at 14 of 19 (0.9673 at 10 of 19). **Every ordering the basis half reads holds in direction on the second compiler, the three pointer spans included --- which is the sentence Run 26 could not write.** Its `-u1-ptr` against `-u1` read 1.3084 where its basis read 0.9693, and this run reads **0.9751 against 0.9713**; `-u2-ptr` against `-u2` read 2.6731 against 0.9479 and now reads **0.9386 against 0.9419**; `-u2-ptr` against `-u1-ptr` read 1.9710 against 0.9431 and now reads **0.9239 against 0.9357**. **Where the two halves still differ is in strength rather than direction**, and on the same two pairs Run 26 named: the lean dispatch against the shipped route, which HEAD reads two points wider and at a sign test that separates where the basis's does not, and `liblist-stage2` against `liblist-stage1`, the same shape. Both sit on a half whose baseline is now only a third of a percent from the basis's, so the two points are the arms and not the denominator.

**Each stride class has its own table below.** Run 8 re-ran every class with the populations pinned, and every run since has again, so each class's paragraph carries what the last change moved and the table above it is what Run 13 reads against. **The two sides of a class comparison across the Run 11/Run 12 boundary are not the same build**, and this is the one place that bites: Run 11's class tables are its *aligned* half's, Run 12's are its *max-skip* basis half's, and the main set prices that difference at nothing below 0.99 and up to 1.06. So read a class figure that moved a point or two across that boundary as the shim rather than as the class, and take the two Run 12 columns above as what a same-build comparison looks like. From Run 13 on, both sides are max-skip again.

**Two tables in this file are NOT installed and are edited by hand: the two-column one above and the cross-class summary below.** Every other table a run publishes comes from `install-tables.sh` and is replaced whole. The one above is replaced whole too, being this run's own halves and no earlier run's; the summary gains a row per run instead. A hand-edited table is edited with the whole line named, never with a prefix anchor. On Run 17 an insertion anchored on ``| `arm` | `` matched an earlier table and put two cells into the element-type probe's header and a loop-offsets row; `--check-doc`'s width pass caught it in the same call, which is the only reason it cost minutes. Name the whole row, assert it occurs exactly once, and read the width check's verdict afterwards.

And because a geomean cannot say *where* it moved, the **fingerprint** below is kept so a future disagreement can be localised rather than only noticed; its membership rule, the column heads and the rulings on dropping a column are [in the README's per-shape section](../README.md#per-shape-where-the-geomean-hides-the-ordering). The two tables below were regenerated on 2026-09-04 from this run's own JSONs in the per-shape form the README describes; the fourteen-column form they carried until then, and the installer's membership note of that day, are in this file's history.

| shape | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---:|---:|---:|---:|---|---|
| `cnn-slice-c32` | 3 | 288 | 5.75 us | 0.086 | `libunord-stage1` 0.033 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.058 |
| `cnn-L1-6x6-c1` | 3 | 324 | 7.02 us | 0.096 | `libunord-stage3` 0.044 | `mut-odo-vecdims-add-in-leaf-u1` 0.072 |
| `cnn-L1-24x24-c1` | 3 | 5184 | 111 us | 0.067 | `libunord-stage3` 0.003 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.039 |
| `lenet-L1-28-c1-k5` | 5 | 19600 | 358 us | 0.048 | `libunord-stage3` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `gather48-src-50` | 3 | 22500 | 422 us | 0.053 | `libunord-stage1` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `stretch-coprime-r7` | 13 | 60060 | 986 us | 0.034 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `cnn-L2-24x24-c32` | 3 | 165888 | 3.4 ms | 0.056 | `libunord-stage5-sum` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `stretch-primes` | 89 | 250357 | 3.87 ms | 0.028 | `libunord-stage5-sum` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `alexnet-L2-27-c48-k5` | 5 | 874800 | 15.6 ms | 0.043 | `lib-stage1` 0.024 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `vgg-14-c512-k3` | 3 | 903168 | 18.1 ms | 0.057 | `lib-stage1` 0.032 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `alexnet-L1-55-c3-k11` | 11 | 1098075 | 17.8 ms | 0.034 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `stretch-inner256` | 256 | 1750784 | 31.8 ms | 0.032 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u1` 0.027 |
| `stretch-pow2stride` | 64 | 1769472 | 27.5 ms | 0.128 | `libunord-stage4` 0.000 | `mut-odo-vecdims-add-in-leaf-u1` 0.126 |
| `stretch-r5-8x432` | 8 | 1769472 | 32.8 ms | 0.032 | `lib-stage2-lean` 0.022 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-square-1341` | 1341 | 1798281 | 28.3 ms | 0.097 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.076 |
| `stretch-bigstride` | 3 | 1800000 | 48.2 ms | 0.035 | `lib-stage1` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.013 |
| `stretch-tab7MB` | 2 | 1800000 | 37 ms | 0.062 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |
| `stretch-tall-Mx2` | 900000 | 1800000 | 38.7 ms | 0.022 | `libunord-stage3` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.021 |
| `stretch-wide-2xM` | 2 | 1800000 | 37.1 ms | 0.061 | `libunord-stage2` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.022 |

| shape | class | `sInner` | `l` | `list`, net | mut-odo-vecdims | best outside family | ceiling |
|---|---|---:|---:|---:|---:|---|---|
| `bcast-inner8` | `bcast` | 8 | 51200 | 835 us | 0.032 | `lib-stage2-lean-u1` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `bcast-inner900` | `bcast` | 900 | 1800000 | 26.1 ms | 0.021 | `libunord-stage4-sum` 0.019 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `bcast-tall-Mx2` | `bcast` | 2 | 1800000 | 36 ms | 0.062 | `libunord-stage1-sum` 0.022 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.022 |
| `bcastmid-c32-cnn` | `bcastmid` | 3 | 165888 | 3.3 ms | 0.060 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `bcastmid-primes` | `bcastmid` | 97 | 250357 | 3.78 ms | 0.021 | `lib-stage2-lean` 0.013 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 |
| `bcastmid-b200k` | `bcastmid` | 3 | 1800000 | 44.2 ms | 0.037 | `libunord-stage5-sum` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.012 |
| `bcastmid-block150k` | `bcastmid` | 300 | 1800000 | 38.1 ms | 0.024 | `libunord-stage3` 0.018 | `mut-odo-vecdims-add-in-leaf-u1` 0.021 |
| `block-run64-gap1` | `block` | 64 | 131072 | 2.05 ms | 0.021 | `libunord-stage5-sum` 0.010 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.018 |
| `block-run64-gap64` | `block` | 64 | 131072 | 2.03 ms | 0.028 | `libunord-stage4-sum` 0.014 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `block-run64-off7` | `block` | 64 | 131072 | 2.03 ms | 0.028 | `libunord-stage5-sum` 0.013 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `block-run64-page` | `block` | 64 | 131072 | 2.13 ms | 0.033 | `libunord-stage2-sum` 0.005 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `block-r3-vol64` | `block` | 64 | 262144 | 3.97 ms | 0.022 | `libunord-stage5-sum` 0.011 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.019 |
| `compose-rev-bcast` | `compose` | 8 | 51200 | 843 us | 0.032 | `libunord-stage4-sum` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `compose-slice-bcast` | `compose` | 8 | 51200 | 848 us | 0.032 | `libunord-stage4-sum` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.016 |
| `compose-scalar` | `compose` | 1500 | 1800000 | 26.4 ms | 0.021 | `libunord-stage2-sum` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `compose-zero-mid` | `compose` | 100 | 1800000 | 26.9 ms | 0.024 | `libunord-stage4-sum` 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `flip-inner-gap64` | `flip` | 64 | 131072 | 2.09 ms | 0.029 | `libunord-stage4-sum` 0.013 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `flip-outer-gap64` | `flip` | 64 | 131072 | 2.04 ms | 0.029 | `libunord-stage4-sum` 0.014 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `flip-last-c32` | `flip` | 3 | 165888 | 3.39 ms | 0.057 | `libunord-stage4` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `flip-whole-square` | `flip` | 1341 | 1798281 | 26.5 ms | 0.027 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.025 |
| `flip-last-rows` | `flip` | 96 | 1800000 | 29.5 ms | 0.053 | `libunord-stage1-sum` 0.048 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.041 |
| `rev-cnn-L1-24x24-c1` | `rev` | 3 | 5184 | 113 us | 0.068 | `libunord-stage5-sum` 0.003 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.039 |
| `rev-gather48-src-50` | `rev` | 3 | 22500 | 417 us | 0.052 | `libunord-stage5-sum` 0.001 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.018 |
| `rev-primes` | `rev` | 89 | 250357 | 3.89 ms | 0.029 | `libunord-stage5-sum` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.023 |
| `runs-65536` | `runs` | 65536 | 1769472 | 24.9 ms | 0.028 | `libunord-stage2-sum` 0.000 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.027 |
| `runs-16384` | `runs` | 16384 | 1785856 | 25.2 ms | 0.027 | `libunord-stage2-sum` 0.000 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-4096` | `runs` | 4096 | 1798144 | 26.3 ms | 0.027 | `libunord-stage2-sum` 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `runs-1024` | `runs` | 1024 | 1799168 | 25.5 ms | 0.028 | `libunord-stage4-sum` 0.001 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-512` | `runs` | 512 | 1799680 | 25.5 ms | 0.028 | `libunord-stage5-sum` 0.002 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-256` | `runs` | 256 | 1799936 | 25.6 ms | 0.028 | `libunord-stage4-sum` 0.003 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-7` | `runs` | 7 | 1799994 | 28.9 ms | 0.037 | `libunord-stage3` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-2` | `runs` | 2 | 1800000 | 36.9 ms | 0.062 | `lib-stage2-lean` 0.026 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-3` | `runs` | 3 | 1800000 | 32.9 ms | 0.052 | `libunord-stage3` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-4` | `runs` | 4 | 1800000 | 31.2 ms | 0.045 | `lib-stage2-lean-u1` 0.026 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 |
| `runs-5` | `runs` | 5 | 1800000 | 30.1 ms | 0.042 | `libunord-stage3` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-9` | `runs` | 9 | 1800000 | 28.7 ms | 0.033 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `runs-96` | `runs` | 96 | 1800000 | 25.7 ms | 0.028 | `libunord-stage4-sum` 0.009 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `runs-r3-48x30` | `runs` | 1440 | 1800000 | 26.1 ms | 0.029 | `libunord-stage5-sum` 0.001 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 |
| `scaled-r5` | `scaled` | 13 | 15015 | 242 us | 0.032 | `lib-stage2-lean` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 |
| `scaled-super-r3` | `scaled` | 30 | 60000 | 915 us | 0.026 | `lib-stage2-lean` 0.025 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.023 |
| `scaled-rank1-m1` | `scaled` | 300000 | 300000 | 4.6 ms | 0.033 | `libunord-stage5-sum` 0.034 | `mut-odo-vecdims` 0.033 |
| `small-patch-k5` | `small` | 5 | 150 | 2.66 us | 0.083 | `libunord-stage1` 0.073 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.059 |
| `small-bcast32` | `small` | 32 | 256 | 3.97 us | 0.055 | `lib-stage2-lean-u1` 0.069 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.053 |
| `small-flat64` | `small` | 64 | 256 | 3.94 us | 0.064 | `lib-stage2-lean-u1` 0.013 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.063 |
| `small-patch-r5` | `small` | 4 | 256 | 4.88 us | 0.094 | `lib-stage2-lean` 0.100 | `mut-odo-vecdims-add-in-leaf-u1` 0.070 |
| `small-row96` | `small` | 96 | 384 | 5.82 us | 0.045 | `libunord-stage2-sum` 0.043 | `mut-odo-vecdims-add-in-leaf-u2-last` 0.045 |
| `window-28x28-k5` | `window` | 5 | 14400 | 255 us | 0.044 | `libunord-stage3` 0.023 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.020 |
| `window-64x64-k1x9` | `window` | 1 | 32256 | 858 us | 0.095 | `lib-stage2-lean` 0.011 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `window-224x224-k3-s2` | `window` | 3 | 110889 | 2.23 ms | 0.056 | `libunord-stage3` 0.020 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.027 |
| `window-224x224-k3-d2` | `window` | 3 | 435600 | 8.65 ms | 0.057 | `libunord-stage4-sum` 0.003 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.026 |
| `window-224x224-k3` | `window` | 3 | 443556 | 9.52 ms | 0.053 | `libunord-stage3` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 |
| `window-128x128-k7` | `window` | 7 | 729316 | 12.3 ms | 0.035 | `libunord-stage3` 0.018 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.017 |

**One row to read first, and it is a property of the shape set and not of any arm**: nine of the thirty-five rows are geomeans over fewer shapes than the rest, and all nine are `libunord` arms. On the basis **seventy cells** are ones the shared forcing term does not leave positive, so `libunord-stage1` and `-stage4` cover 13 of 19 shapes, `-stage5` 14, `-stage2` and `-stage3` 15, and the four `-sum` consumers 7 or 8; on the HEAD half it is **forty-three cells** and the same nine rows over 17, 18 and 10 or 11. Every other row covers all nineteen. **That is the arm removing the work rather than doing it faster**: where the one-block test fires, an unordered arm hands back a slice of the source and its cell IS the forcing pass, so the subtraction lands at or below zero and the reader prints `--`. It is why those nine rows head the table at 0.000 to 0.002, why the five fills among them move 22% to 62% across the halves, and the four `-sum` consumers 1.5% to 22%, while every one of the nine sits within a ten-thousandth of 1 in counts, and why registrations (6), (7) and (9) are read on the classes: the reader records their main-set spans NOT READ rather than adjudicating a ratio of two near-zero numbers.


## The claims the next run should test

**Run 27's verdicts first**, since a run reports breaks rather than re-deriving the table. **Both live claims held.** Claim 10, the manifest's only live ordering, reads **0.6380 on the 9.12 basis and 0.6392 on the HEAD half**, both at 17 wins of 19 with sign p 0.00073, and pinned to these nineteen shapes Run 24's two halves read 0.6464 and 0.6454 and Run 26's 0.6438 and 0.6467 --- six figures across three runs and two codegens spanning nine thousandths. Claim 7 held on every level it has, its pinned eighteen shapes giving back the same five figures for a fifth run --- and this time on BOTH halves, the two pointer fills joining the 1.00x tier on HEAD where Run 26 had them at 1.41x and 2.61x, which is the GHC #27778 workaround showing in the allocation column. Every arm claim 10 names is timed, and `--pair` recovers any retired ordering in one call whenever it is wanted.

**The eight retired claims are not re-read here.** Claims 3, 4, 5 and 9 left the manifest at Run 19's write-up, on a sweep in which all thirteen held on both of that run's halves; claim 8 left on 2026-08-29 on a decision, its subject the pure tier being no longer what ships; claims 2 and 6 left on 2026-08-28 with the parking of the arms their surviving links turned on; and claim 1 left on 2026-09-04 with the prune, which parked every rung below its top. The numbered items below say what each was in a clause. This run does not re-derive any of them and quotes none as its own --- and for claim 1 that is still the only way it could be read at all, three of the four arms its links name being untimed, which is what retiring it gave up and what a run that re-times them would get back. **Eight is the count, and it was written as seven for two runs**: the enumeration above skipped claim 8, which the numbered list below has carried as retired since 2026-08-29.

**Claim 7 held on every level it has, and its pinned shape set did not have to move this run.** The claim reads its levels over the eighteen main-set shapes it was re-cut to on 2026-09-05; this run times nineteen, `cnn-L1-6x6-c1` being the nineteenth and sitting outside the pin. **Read on the eighteen the levels are unmoved and five runs agree to the digit**: the mutable fills at **1.00x**, the list consumers at **2.00x**, `bq-expand` at **2.06x**, `list` at **22.38x** and the `libunord` arms at **0.00x**, every one of them Run 26's, Run 25's, Run 24's and Run 23's figure over the same eighteen. What this run's own nineteen show instead --- 2.11x and 22.64x --- is the median's population and not what any arm allocates, which is the fifth run running that this file has had to say. **The ten new arms land where the claim would put them**: the four `-sum` consumers at 0.00x with the `libunord` fills they fold, `liblist-stage3` and `liblist-stage4` at 2.00x with the list consumers, and `lib-stage2-lean-u1` and `mut-odo-vecdims-add-in-leaf-u2-last` at 1.00x with the family. **And the place the claim broke last run is mended**: on HEAD the two pointer fills read **1.00x**, where Run 26 read 1.41x and 2.61x, so the claim is now identical on both halves at every level and the allocation column is the axis on which GHC #27778's workaround shows most plainly.

**Claim 10 is the manifest's only live ordering and this is the third run to read it as a claim.** What the leaf fusion buys over the family root: `mut-odo-vecdims-add-in-leaf-u2` against `mut-odo-vecdims`, registered 2026-09-04 with the prune in claim 1's place, and read here at geomean **0.6380, 17 of 19, sign p 0.00073** on the basis with the HEAD half at **0.6392, 17 of 19** --- a margin no floor here approaches, so what the registration asserts is the direction and the win count and not the size. Run 26's two halves read 0.6438 and 0.6467 and, pinned to these nineteen shapes, Run 24's read 0.6464 and 0.6454: six readings over three runs, three rosters and two compilers within nine thousandths, and a box change between the last two that moves every absolute 3.66% and this ratio not at all, which is what a within-run ordering is for.

**Readings:** `mut-odo-vecdims-add-in-leaf-u2` / `mut-odo-vecdims` 0.6380, 17 of 19, sign p 0.00073. 1 of 1 registered ordering held.

Restated as the predicates the next run checks, and carrying no reading of its own: the figures each was last measured at are in the `Readings:` paragraphs above, so an entry here changes when a claim is re-aimed and not when a run moves a margin. **All of them are `-fspec-constr` claims, the regime they are read in** --- the shipped file does not set the flag, measured irrelevant to the shipped family --- so they are the set that decides, and a run at -O1 would test Run 7's instead, the two differing in more than their numbers. **They are read in the caller's allocation regime now**, every figure here being taken at the `-A32m` Run 16 promoted to the basis and every horde-ad test and benchmark bakes since 2026-08-21; the gap Runs 14, 15 and 16 priced against a prevailing `-A1G` is closed, and no claim below needs qualifying by it. **And all of them are read against a measured drift band rather than a layout span**, which is what the last three runs bought. A roster *order* change alone moved arms 0.966 to 1.142 between Run 9 and Run 10, and that is what a margin used to have to clear; with the layout pinned, a repetition moves an arm by at most 3.3% on Run 11's reading and 2.1% on Run 23's, most of them by under a point, so a margin above a few percent is now evidence of a strategy. **Run 13 is the first pair here to hold every tracked loop at one offset in both halves**, which is what lets its arm-by-arm comparison be read as the package costing nothing rather than as two terms cancelling. A claim resting on an arm whose own loop the shim skipped --- `list`'s, which is library code --- is still decidable nowhere until that loop is read. **And the pinning claim is measured only in its weak form**: adding `mut-flat-gm-nosum` left every tracked loop at the same address, but a `Force` arm reuses a rostered function and emits no code for emission order to move. The strong form wants an arm that emits its own, and until one is added the claim covers additions that cost nothing to place.

**The list needed reading against a moved roster again this run, and what moved is the list and not a reading.** Ten arms landed and five left, so the two live claims kept every arm they name --- claim 7's levels and claim 10's pair are all timed --- and the retirements of 2026-09-04 stand untouched. **What the roster raises is what Runs 22, 24 and 26 all raised, and this run raises it further**: twenty-one arms read below `mut-odo-vecdims` where Run 26 had seventeen, and the shipped library route is still among them, at 0.028 against 0.051. **But nine of the twenty-one are the `libunord` rows whose cells the arm empties**, so the honest count of arms that are faster at the same work is twelve, and the four `-sum` consumers among the nine do a different job again. That is left to the next run rather than re-aimed here: the ruling on the lazy list ([dead ideas][dead]) names which of those arms are ceilings rather than candidates, and this run's registrations (6) to (9) read the lazy candidates built against them --- three of the four killed.

1. **Retired 2026-09-04** with the prune, which parked every rung below `mut-odo-vecdims`; its last reading is Run 24's, above, where it held on both halves. What it was: `mut-odo-vecdims` < `mut-flat-gm` < `bq-mut-runs-gm-mulback` < each of `bq-scan-rem-gm-mulback` and `bq-odo-gm-mulback`, the whole ordering read on unconditional arms --- **the ladder the `needs` column draws**, each link pricing one thing the implementation is allowed to ask for. **The foot rung retired 2026-08-29**, on a decision rather than on a reading: it registered the two arms needing nothing at all as a tie, so that either was what would ship if the mutating `Vector` method were refused upstream, and it had read as a tie on every run that carried it. Both arms stay rostered and stay in the rung above, `--pair bq-scan-rem-gm-mulback bq-odo-gm-mulback` recovers the reading whenever the upstream answer wants it, and its last is Run 21's, above. The middle link is the one the README has seen a layout term move --- 0.9708 at 15 of 24 on Run 10's unaligned half against 0.9293 at 22 on its aligned one --- and on a placed layout it has now read the aligned figure five runs running. The ordering has survived nine runs, two changes of basis, two repetitions and three compilers.
2. **Retired 2026-08-28** with the parking of `offtab`, the arm its surviving link turned on --- the other, `bq-scan-rem-gm-mulback`, is timed still. What it asked, since the settlement of 2026-08-24 re-aimed it, was where the arms needing something other than the fix sit --- `offtab` behind `bq-scan-rem-gm-mulback`, a mutable `Int` scratch priced against needing nothing --- and its last reading is Run 20's, above. **Its second link, `bq-expand` behind `mut-odo-vecdims`, had gone already, 2026-08-26** on the reading above, its condition having been spent since 2026-08-24 --- what it priced was the branch's own code against its replacement, and the replacement is now the branch's own code. It was the widest ordering the manifest carried, and `--pair` recovers it, both arms staying rostered and timed.
3, 4, 5. **Retired at Run 19's write-up**, on a last reading in which all three held on both compilers, and for the reasons in the settlement paragraph at the foot of this section. Their numbers are left standing here rather than reused: a verdict recorded against *claim 4* in an earlier run's file still means what it said. What they were, in a clause each, so that nothing is recoverable only from a manifest diff --- claim 3, that a mul-back output pays on the `bq-expand` build; claim 4, that the scan build ties its own build control while beating `bq-expand`, a tie Run 17 promoted to an ordering and three runs then read as one; claim 5, that `bq-expand` beats `bq-gen`, whose refutation of the generate-per-element build stands on Runs 7 and 8. **What none of the three could still foreclose is the point**: every one asks where `bq-expand` sits among arms nothing ships, on a branch whose fix is the `mut-odo-vecdims` family. The arms all stay rostered and timed, so any of these orderings is one `--pair` call away.
6. **Retired 2026-08-28** with the parking of `gen-quotrem`, the arm its only link turned on. What it asked: `gen-quotrem` ties `list`, the first attempt's arithmetic ceasing to be dearer than the list's allocation once the flag takes its own allocation to 1.00x against the list's 23.5x --- the mixed picture this suite exists to have refuted, arriving by a route nobody proposed. Its last reading is Run 20's, above; the `cm-gather` < `list` half was untimed throughout and stands as Run 8's. What goes with it is the standing advice to check `list` as an anchor before blaming a strategy, which is now nobody's claim and is why the machine check reads `list` net per shape every run.
7. Allocation, median multiples of the result **over a pinned shape set**: the mutable fills 1.00x, the list consumers 2.00x, `bq-expand` 2.06x, `list` 22.38x, every one of them read over **the eighteen main-set shapes this roster times**, which are a strict subset of the 24 Runs 15 to 23 shared --- **re-pinned 2026-09-05, six of those 24 having been retired from timing the day before**, so the old population no longer exists and the predicate could not otherwise be evaluated; re-listed 2026-08-28 with the parking, and 2026-09-04 with the prune, which parked the two tiers between the fills and `bq-expand` outright and left the 2.00x one to the two `liblist` arms, named in the paragraph above. Every level has reproduced over the eighteen on Runs 23, 24 and 25, which is what makes this the claim to check first when anything else moves. **The pinning is the predicate and not a caveat on it**: Run 24's published column reads 2.40x for `bq-expand` on its own 26, 2.35x on the 24 and 2.06x on these 18 --- so a level compared across two shape sets moves for a reason that is neither a code change nor a slot, and only a level that moves ON ONE POPULATION is a code change. **Read the levels and the cells as two questions**, which Run 19 is the run that separated: its levels all returned while the cross-half cell agreement fell to 1016 of 1080, where the 9.14 pair had 1072 --- so a compiler can reallocate within a tier without moving any tier, and only the cell count sees it.
8. **Retired 2026-08-29**, on a decision and one day after its last re-aiming. What it asked: that every pure arm in the fast tier run its output through the single in-order `vGenerate` over an `m`-length table, so a `bq-*` arm falling behind loses on its table build and not on its output. Its last reading is Run 21's, which found it true and its subject smaller again --- nine arms at or below `mut-odo-vecdims` and not one of them a `bq-*`, so the structure it described governed a tier starting a third of the way down. What retires it is that subject: the pure tier is no longer what ships, and a claim quantified over it was being re-aimed and re-read every run to say so. It was also the one claim with no named invocation, read off the table by eye throughout, so nothing mechanical goes with it.
9. **Retired at Run 19's write-up with 3, 4 and 5**, and for a reason of its own worth keeping: its per-shape half was answered rather than abandoned. `bq-expand-b`'s two best cells were `stretch-inner1` and `stretch-wide-2xM` in every run from Run 8 to Run 16 --- the rank-2 views with one huge outer dimension where seeding from `enumFromStepN` replaces the whole `concatMap` build --- until **Run 17 read `stretch-inner1` and `stretch-square-1341` instead**, which registered the follow-up. Runs 18 and 19 both read the original pair back, on four compilers between them, so the excursion was one run's and the mechanism is cleared of picking the wrong shapes. The geomeans were never the stable part, and the series is why: across Runs 8 to 13 `bq-expand-b` / `bq-expand` read 0.996, 0.9678, 0.9943, 0.9819, 0.9923 and 0.9909, its sign test crossing into significance only on the last of them, while `bq-expand-zf` / `bq-expand` went 3.6% behind, then level at 1.0028, then 1.0325, 1.0197, 1.0256 and 1.0265. Both series were closed at Run 13 and not extended per run, and a closed series in a live manifest is maintenance without a question --- which, with the follow-up spent, is the whole case for retiring it.
10. `mut-odo-vecdims-add-in-leaf-u2` < `mut-odo-vecdims`, the shipped fill against the family root --- **what the leaf fusion buys**, the one thing the roster still prices that the library has taken. Registered 2026-09-04 with the prune, in claim 1's place.

**What the parking of 2026-08-28 did to this set**, recorded in prose because a live item carries a predicate and no reading, and because naming a parked arm inside one is now a `--lint` failure. Claims 2 and 6 retired with `offtab` and `gen-quotrem`, the arms their surviving links turned on. Claim 7 lost three of the arms that carried its levels --- `gen-quotrem` at 1.00x, `bq-mut` at 1.33x and `offtab` at 2.00x, every figure of theirs ending at Run 20 --- but no level with them: `offtab-scan-rem` carries the 2.00x tier, the mutable fills the 1.00x and the scan family the 1.33x, so the list is the same six levels it was. Claim 8 was re-aimed off `bq-expand-zf` and `bq-gen`, its span running from the leading tier to `offtab-scan-rem` instead; the readings above are the last that name the two. Nothing was retired on a reading here, the parking being a decision about what is worth a bench, and the two claims that went had no link left that a run could measure.

**What the prune of 2026-09-04 did to this set** ([what the benchmark does](../README.md#what-the-benchmark-does)), read on the first run after it. Claim 1 retired, every rung below its top parked, and claim 10 is registered in its place, the one live ordering. Claim 7 lost the scan family's 1.33x and `bq-odo-gm-mulback`'s 1.58x, which have no timed arm left, and keeps `offtab-scan-rem`'s 2.00x through the two `liblist` arms; and it lost its pinned population as well, six of the 24 shapes it named having been retired the same day, which this run re-cut to the eighteen that remain. The ruling of the same day on the short bodies parked `lib-stage2-short` and `lib-stage2-short-lean`, whose last readings stand as Run 24's. The class properties keep their arms, `mut-odo-vecdims` and `bq-expand` both staying timed --- **and property 1's two counter-examples are down to one**: `gen-unsafe`, which carried a `worst` above 1 in seven of ten populations as the baseline's own control, is parked, so the only arms above 1 now are the library-shaped ones and the `list` twins.

**Two homes, and which carries what.** Each live claim has a prose paragraph here and a numbered predicate at the foot, and they divide: the PROSE carries this run's figures and what moved, the ITEM carries the predicate the next run checks and no reading at all. A RETIRED claim keeps its numbered item, which says in a clause what it was, and gets no prose paragraph --- Run 20 wrote two and they restated their own items clause for clause, because stripping the figures had removed the only thing that distinguished them. Each ordering is one line of `--claims`, whose manifest now carries the registered expectation --- the direction of the geomean, a tie by sign test, or claim 9's two best shapes --- and prints HELD or BROKE beside the paired geomean, interval and sign test. `--claims --in-place` then installs that arithmetic as each claim's `Readings:` paragraph above, so a run no longer transcribes it at all; what stays the reading's is whether a HELD margin moved and whether a movement clears the floor, and a BROKE is what obliges the paragraph above its reading to be rewritten rather than requoted. **A claim with no named invocation is a gap in this list, not a claim to be checked by hand**: where a session has to invent the computation it will invent a wrong one, which is how claim 7 came to be read off the raw fitted bytes, explained by a mechanism the previous pair refutes, and then "corrected" onto a rounded print. It has `--compare --alloc` now, and no live claim is without one since claim 8 retired. **The general form, and it is a standing instruction rather than an observation: if a write-up hand-rolls a script to answer something the reader should answer, that is a defect report against the reader** --- fix it there, before the sentence it was written for, or the next run invents its own wrong version. **Two riders, both bought on Run 19.** A new MODE joins the guards its siblings already have, and is checked against them rather than written beside them: `--counts` shipped able to be given without the `--compare` it reads, silently printing the default table and exiting 0 --- the unread-flag family exactly, added next to four sibling readings of `--compare` that were every one of them already guarded, and joining none. **And an instrument may be BACKED OUT, which is not a failure of the write-up but a result of it.** Measure what it flags before shipping it: the obvious mechanical repair for stale paragraphs was built and returned 100 for the four that mattered, so it went, and the refutation with its numbers is under the tasks heading --- worth more than the mode, since it stops the next session building the same thing. A report that never empties is one nobody reads, which this file already knows about hints.

**And for each stride class, the same three properties, now carrying Run 27's verdicts** over ten classes, the details beside each class's table:

1. **`mut-odo-vecdims`'s `worst` stays under 1, and `mut-odo-vecdims` is ahead of `bq-expand` on every shape.** Both held in every one of the eleven populations, on both halves --- the first in every regime, roster and layout the README has run, so it was never slower than the `list` it replaced, on any shape of any class the library can produce; the second folded in on 2026-09-06 from the ordering that was property 2 until then, strengthened from a geomean to every shape, and read that way here for the second time. This is the property the classes exist to test, no geomean can state it, and a break would be the one result here to bear on `Data/Array/Internal.hs` directly. Read for `mut-odo-vecdims`: **on Run 27 its worst is 0.128 on the main set and 0.095 in a class, on the basis half, with the HEAD half at 0.125 and 0.096** --- and the class carrying that end is not the same one on the two halves, `window` on the basis and `small` on HEAD, with the other second by a thousandth. So the property holds for the family root on both compilers and neither end is within a tenth of 1: the main-set end is a factor of 7.8 inside it and the class end 10.5. **The second clause's closest cell is `stretch-pow2stride` on the main set, 0.9900 on the basis and 0.9926 on HEAD**, and it is closest on the same shape on both halves --- so the clause has a percent of room where it is tightest, against a main-set floor of 0.83% and 0.63%.

Beside property 1, and the case has simplified since the prune of 2026-09-04 parked the arm that used to be half of it: **only one thing breaks it now, and it is not a baseline control.** `gen-unsafe` and its twins carried a `worst` above 1 in seven of ten populations and were the baseline's own controls rather than the property failing; every one of them is parked. What is left is the other break, and it is a fill the library would ship. **The library-shaped arms break it on `runs` alone and all at `runs-2`**, five of the ten this roster now carries and on both halves: `libunord-stage1` at **1.228**, `liblist-stage1` 1.223, `lib-stage1` --- the shipped route --- at **1.209**, `liblist-stage2` 1.066 and `libunord-stage2` 1.065 on the basis, and 1.201, 1.188, 1.186, 1.050 and 1.047 on HEAD. **`libunord-stage3`, the arm this run added, is not among them**, keeping `worst` at 0.029 on `runs` as the four `lib-stage2*` arms do --- so five of the ten library-shaped arms break the property there and five do not. So on 900000 runs of two elements five of the ten library-shaped arms are slower than the `list` baseline they replace --- every route that takes a slice per run at that length --- while the four `lib-stage2*` arms and `libunord-stage3` fill every run whatever its length and are the five that are not. `runs-3` is the only other cell of any population where a library-shaped arm crosses 1, and ONE does there on each half: `libunord-stage1` at 1.0250 on the basis and 1.0089 on HEAD, with `lib-stage1` at 1.0004 and 0.9774 and `liblist-stage1` at 0.9988 and 0.9942 either side of the line. Run 25 read three crossing on that cell; two of the three have come back under 1. **The property is stated of `mut-odo-vecdims` and holds of it; it does NOT hold of what the library actually calls.** A reader taking property 1 as clearance for the code that ships is reading it wider than it is stated, and the class that catches the difference is `runs`.

2. **`mut-odo-vecdims` allocates at most 1% over `list` and over `bq-expand` on every shape** --- property 1's two inequalities in allocation with a 1% margin, on the `alloc` multiple each cell carries, registered strict on 2026-09-06 and given the margin on 2026-09-07 at its first reading: by `--block` per class and by the default mode on the main set, each clause printed with its closest shape. **Both clauses hold in every one of the eleven populations on both halves.** The `list` clause is closest at `small-patch-k5`, 0.06580, and under 0.053 everywhere else. The `bq-expand` clause is closest at `small-row96`, 1.00441, then `scaled-rank1-m1`, 1.00003, and the main set's `stretch-tall-Mx2`, 1.00000 --- `mut-odo-vecdims` allocating 15, 80 and 45 bytes per call more than `bq-expand` of 3.4 KB, 2.4 MB and 14.4 MB, the same bytes on both halves to within one --- and at or below 0.99998 on every other shape of every population, `runs-65536` being that one. **That is why the margin**: where both arms allocate one result vector and nothing else, the strict inequality was decided by tens of bytes of per-call constant and broke on those three shapes, and only on `small` is that constant a visible share of the call, which is what that class was built to show. **The number's previous holder retired the same day**: the top-of-the-table ordering, `mut-odo-vecdims` fastest in every population with `bq-expand` behind it, broke in nine classes on Run 25 and in all ten on Run 26, each to a library route the class's strides favour or to a ceiling arm the library refuses, the head's paragraph on the leaders and each class block naming which --- a reading the class tables' *best outside family* column carries on its own, so the ordering foreclosed nothing; its `bq-expand` clause is property 1's now. What moved between the two runs is which arms lead: the lean and re-cut dispatches led six classes on Run 25 and two here, and the two arms that took their places, `libunord-stage3` and `-u2-ptr`, are both new to the timed roster this run.

3. **The allocation tiers survive and their ORDER is unbroken in all ten, and every level is Run 23's and Run 24's once the shape set is pinned, on the main set and on every class but `small` --- outside the level clause since 2026-09-07, being the class built to break it, and read here for what it shows**: the mutable fills at the result vector, `bq-expand` between 1.02x and 2.81x it, `list` an order of magnitude above. On the main set the fills read 1.00x, the two `liblist` arms 2.00x, `bq-expand` 2.06x and `list` 22.38x, and Run 23's and Run 24's own basis halves give every one of those five over the same eighteen shapes --- the columns those runs published, 2.35x and 23.5x and 2.40x and 23.50x, being their own populations and not their arms. Where a level moves it is the class's own `m` or its own size showing through, exactly as this property warned --- `bq-expand` at 1.03x on `flip` and `block` and 2.81x on `window`, and on `small` **the five non-pointer fills at 1.27x and the two pointer fills at 1.44x**, the per-call constant that dividing by `8*l` hides at a million elements and cannot at 256 --- with the ordering of tiers unbroken in all ten and `list` running 19.19x to 24.76x across them, on both halves. The three `libunord` arms sit below the result vector at 0.00x on the MAIN SET, and in the classes they part: on six --- `bcast`, `bcastmid`, `window`, `scaled`, `compose` and `small` --- the two older arms read about 2.00x, `libunord-stage1` reaching 3.05x on `small`, **while `libunord-stage3` reads 1.00x on five of the six and 1.74x on `small`** --- it fills where they fall back to a list, which is registration (3)'s whole subject and is what the allocation column is for; on `rev` and `flip`, `libunord-stage1` reads about 2.00x while `libunord-stage2` stays under the vector at 0.01x and 0.00x; and on `runs` and `block` the two older arms read 1.19x and 1.14x, and 1.42x and 1.31x, with `libunord-stage3` at 1.00x on both. So the one-block test's 0.00x tier is the main set's alone, `libunord-stage1` never reaching it in any class, and a class reading of that tier is a different quantity. On a pair whose two halves are two compilers' code this is the property that says the difference is not a change of what gets allocated: every tier but one is identical on both halves, and the 99 main-set cells of 532 that disagree past 1e-4 are one compiler allocating differently INSIDE a tier, which is the distinction Run 19 is the run that separated. **The one tier that is NOT identical is this run's own finding**: the two pointer fills sit at 1.00x with the family on the basis and at 1.41x and 2.61x on HEAD, and the worst disagreeing cell of the 99 is `stretch-wide-2xM`/`-u2-ptr` at 7.50e-01 --- a change of tier and not a change inside one, which is what Run 19's distinction is for, and this file records no earlier occasion needing it that way.

**SETTLED 2026-08-24 and APPLIED 2026-08-25, at Run 19's write-up rather than at the settlement**, so that the retiring orderings got one last cross-compiler reading and the retirement is recorded with it --- which is how Run 17 retired claim 4's tie, in prose at its write-up with the manifest taking it the same day. **They got it**: all thirteen held on both of Run 19's halves, and the eight that remain hold on both too, so each of the four retires on a reading rather than on a decision. `CLAIMS` in `read-run.py` carried claims 1, 2 and 6 alone after that settlement, and **claim 1 alone since 2026-08-28**, `offtab` and `gen-quotrem` having been parked. The test applied: an ordering stays only if it forecloses something anyone would propose again *and* can still break. What fails both is a figure, and figures live in the tables above. **Claims 3, 5 and 9 retire outright.** Claim 3 sets one output form against another on a build nothing ships, where every leading pure arm is a `-gm-mulback` already; claim 5's `bq-expand` / `bq-gen` says of itself that the refutation stands on Runs 7 and 8, and claim 6 keeps that family guarded through `gen-quotrem`; claim 9's two series are closed at Run 13 by this section's own words, and a closed series in a live manifest is maintenance without a question. **Claim 4 retires with them, its tie moving into claim 1** --- and what goes with it is the one place the manifest reads a *builder* apart from its output, which `--pair` recovers whenever it is wanted, both arms staying rostered. **Claim 1 becomes the ladder the `needs` column already draws**, gaining `bq-scan-rem-gm-mulback`, the best arm needing nothing at all, between `bq-mut-runs-gm-mulback` and `bq-odo-gm-mulback`: the first ahead of it at **0.9060** and **0.9171** on Run 18's two halves, 19 and 17 of 24, which is what a mutable `Int` scratch buys; and the second **tied** with it at **0.9902** and **0.9936**, 13 and 12 of 24 at p 0.84 and 1, so the two fastest pure arms were indistinguishable and either was what would ship if the mutating method were refused --- the rung this installed, and retired again on 2026-08-29. Its three existing links stay, the middle one redundant with the two new ones and carrying seven runs of history they do not. **Claim 2 keeps its number and changes its question** to where the arms needing something other than the fix sit: `offtab`, which needs only that `Int` scratch, behind `bq-scan-rem-gm-mulback` at **1.36** and **1.44**; and `bq-expand` behind `mut-odo-vecdims` at **2.09** and **2.13**, kept only while `Data/Array/Internal.hs` carried `bq-expand`, and to retire with the three `TODO: retarget` markers, which were one decision with it. **That condition was spent the same day and the link outlived it by two runs**: the file went to `vFillStrided` on 2026-08-24 and the markers went with the prose they marked, but nothing read the condition back, so Run 20 registered and read the link like any other and it retired only on 2026-08-26. Thirteen registered orderings become eight here, and seven when that second link finally goes; claims 7 and 8 stayed unmanifested prose. **The rewriting the ask paired with this was already done**: the eight *What the class says* paragraphs were written to the re-aimed properties at Run 18's write-up, and only the sentence asking for it survived.

`--pair` works within a class JSON exactly as within the main one, and is still the way to compare two arms; its bootstrap interval, over three shapes, is worth less there than its win count.

Two notes on the columns. The `needs` column splits the class-method tier in two. A **new pure `Vector` method** delegates to a pure function the vector package already ships for every carrier --- `unfoldrExactN`, `backpermute`, the `concatMap`/`enumFromStepN` pipeline --- so it fights only *minimal* in orthotope's pure-and-minimal API rule; the **new mutating `Vector` method** the direct fills need is the [mutable ceiling](../README.md#the-mutable-ceiling-taken)'s ask, which *pure* barred outright until the amendment there turned the bar into a weight, and which the decision of 2026-08-22 takes. `offtab` is the `Vector`-class-expressible shape of these gathers --- output by plain `vGenerate` over a concrete offset table --- so its own cell names only its mutable `Int` scratch. And the geomean weights every benchmarked shape **equally**, so a figure here is a ranking statistic, not a claim about total work saved: the small shapes count as much as the largest.


## The stride classes, run by run

**Run 27 (SpecConstr, dead-spot +lookrts, -A32m, 9.12.4) records every class on BOTH halves**, one process each, in [the sequence](../README.md#making-a-major-benchmark-run), twenty-two processes in one window and `runs` re-measured in a second. Every table below is the **basis half**'s, the 9.12 one, the half that keeps the lineage; what the second half buys is that the pair's variable --- the compiler --- can be read on a class, with the counted work beside it on every population. **Read across the halves the two compilers are close everywhere and the vote is all but even, which is new.** Of the 310 arm-comparisons the ten classes carry, **9 sit out the vote and the geomeans** --- seven on `flip` and one each on `runs` and `block`, every one a `libunord` arm whose basis cell the correction does not leave positive --- and of the 301 that vote, **141 put the basis faster against 160 the HEAD half**, the ten geomeans running 0.9627 on `rev` to 1.0094 on `block`, where Run 26 had every one of the ten below 1. **The low extreme over all ten is `libunord-stage5-sum` at 0.4319 on `rev`** and the high `lib-stage2-lean-u1` at 1.1201 on `flip`; where Run 26's low extreme was the pointer fill in all ten, this run's pointer fills are inside a point of 1 on every class, which is what the workaround did. **Five classes have `list` past the 0.7% bar** --- `bcast` 0.9823, `window` 0.9768, `scaled` 0.9795, `runs` 0.9887 and `flip` 0.9928 --- so those five lines are orderings and say so, and the five read for the pair's variable are `rev`, `bcastmid`, `block`, `small` and `compose`.

First, one table over all of them, so that an inversion is visible without reading every class's table. Every figure in it is transcribed from a class's own table below --- none is computed here, and none is an average across classes, there being no such population to average over. Its header, fixed here so a run fills rows and never reshapes columns:

    | class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |

That header line is written out twice in this file, once here as the spec and once as the table's own, and the two are the same text --- so a session pasting a run's rows must anchor at the line start and check that it landed on the unindented one. Getting that wrong put Run 8's rows under this paragraph and left Run 7's standing in the table, both checks passing, because the check looked the table up the same wrong way the paste did.

`mut-odo-vecdims` and `worst` are that arm's two columns in that class's table; *best outside family* is the leading arm outside the vecdims family, what the dropped stride-conditioned redirect would have taken, and *ceiling* the leading arm OF the family, each with its name --- a column of this table and not the eight-run ratio the head calls the ceiling, which is `mut-odo-vecdims` against the fastest arm needing nothing at all and which this run cannot read --- since which arm leads is half of what the column says --- so where an arm outside the family leads, the two name different arms and the gap between them is what the lead is worth, and Run 21's table, which repeated one arm in both columns on `bcastmid` and `reshape1`, was wrong to; *floor* is the largest deviation from 1 among that process's A/A controls. A cell that breaks property 1, or that leads `mut-odo-vecdims` --- what broke the ordering that was property 2 until 2026-09-06 ([the claims](#the-claims-the-next-run-should-test)) --- is bolded, and the class's own paragraph says what the bold marks; properties 2 and 3 are allocation and have no cell here.

**And the aggregate figures in the paragraph above the blocks are the reader's, emitted rather than assembled.** `./read-run.py --cross-classes --classes BASIS... --others CONTROL...` prints every one of them --- the comparison count, the faster/slower split, the range of the geomeans with the class at each end, the arm holding each extreme and how many populations share it, the degenerate arms it kept out, and the classes whose `list` is past the 0.7% bar --- from the same per-class rows the cross-half lines below print, so the intro and the blocks cannot part. The comparison count, the faster/slower split, the range of the geomeans and the extreme arms are each an aggregate over the `--block --compare` lines below, one per class, so they are read off those lines and never off a population assembled for the purpose: Run 20 assembled its own twice and was wrong both times --- once on the split, once on a low end that excluded a class the sentence said it covered. Where a figure genuinely cannot come off those lines, because a class's own maximum is a degenerate cell, the paragraph says so rather than quoting it as though it could.

Then one block per class, in `classViews`' order --- which `Main.hs` fixes and this file follows, so a class landing or retiring moves the blocks and not a list here --- each carrying the same six things and nothing else:

1. a bolded lead naming the class, the mechanism it models in a clause, and its shapes with their `l` and `sInner`, which is what makes the table under it readable without `Main.hs` open;
2. the table `--block --in-place` installs from `$R-<basis>-$c.json`, whole and never edited --- six columns, with the emphasis carried over from the main table so the `mut-odo-vecdims` row is found at a glance, and `needs` left to that table as a property of a strategy rather than of a population;
3. its own controls, off `--aa`: the A/A deviations with their spans, the two `sum-only` halves, and the in-situ term from the `-nosum` arms --- this process's own floor and its own three gates, neither inherited nor lent --- and where the paragraph quotes the OTHER half's figure, it says so in the form `the other half's own six pairs span N%` and never with the word *floor* beside the number, which `--check-doc` holds to this table's column (Run 23 was refused four times before it learned the shape);
4. its provenance and its anchor: elapsed time and the two heap peaks from that process's stderr line, its population's size from the reader's first line ([why not both from one place](../README.md#making-a-major-benchmark-run)), and `list`'s absolute per-call time on one of its shapes, raw and net. The main set's three anchors guard a baseline that moves for every population at once; this one guards a baseline that could move for this mechanism alone, which is the case a table of ratios hides completely. A three-shape class adds one line here --- the bolded rows' per-shape net ratios, in the lead's shape order --- because its table under-determines its cells, where a two-shape table carried them already, `time` and `worst` jointly fixing both; every class is three shapes or more now, so the line always prints;
5. the cross-half reading, one line, which `--block --compare` against the other half's JSON now emits and `install-tables.sh` writes in with the other three --- how many of the population's arms move, which way, and the spread; a margin on this line is judged against the WIDER of the two halves' floors ([README, the floor section][floor]). Both halves have run every class since 2026-08-14 and this is where that is read: a pair's variable can act on a class and not on the main set, which is how Run 14 answered its `scaled` question. A run whose halves differ in nothing a class can see says so in a clause;
6. one paragraph of what the class says, and none where it says nothing: an ordering that inverted, a `worst` above 1, an allocation tier that moved, a mechanism showing through a single cell. A class that reproduces the main ordering gets one sentence saying so, that being a result and reading as one.

`./read-run.py RUN.json --block --compare OTHER.json` assembles items 2 through 5's mechanical parts, and `install-tables.sh` writes them in in one call --- table, controls, the provenance and anchor skeleton, a three-shape population's per-shape line, and the cross-half line; the lead and the paragraph stay the author's, a skeleton writing no findings. **The cross-half line carries its own disqualification**: where `list` moves more than 0.7% between the halves the line says so and says it is not read for the pair's variable --- a reading Run 18 needed, and which no other output showed.

The blocks carry no headings of their own. One per class would crowd the contents and the replace list alike, where a bolded lead reads the same and lets one link cover the section --- which is what `--check-doc`'s coverage check counts.

| class | shapes | mut-odo-vecdims | worst | best outside family | ceiling | floor |
|---|---:|---:|---:|---|---|---:|
| `rev` | 3 | 0.047 | 0.068 | **`libunord-stage5-sum`** 0.000 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.025 | 0.81% |
| `bcast` | 3 | 0.035 | 0.062 | **`libunord-stage5-sum`** 0.019 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.019 | 0.85% |
| `bcastmid` | 4 | 0.033 | 0.060 | **`libunord-stage5-sum`** 0.015 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.019 | 0.81% |
| `window` | 6 | 0.054 | 0.095 | **`libunord-stage3`** 0.017 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.024 | 0.97% |
| `scaled` | 3 | 0.031 | 0.033 | `lib-stage1` 0.026 | **`mut-odo-vecdims-add-in-leaf-u1-ptr`** 0.024 | 0.29% |
| `runs` | 14 | 0.031 | 0.062 | **`libunord-stage5-sum`** 0.007 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 | 3.15% |
| `flip` | 5 | 0.034 | 0.057 | **`libunord-stage4-sum`** 0.014 | `mut-odo-vecdims-add-in-leaf-u1-ptr` 0.026 | 0.41% |
| `block` | 5 | 0.026 | 0.033 | **`libunord-stage2-sum`** 0.005 | `mut-odo-vecdims-add-in-leaf-u2-ptr` 0.022 | 0.36% |
| `small` | 5 | 0.066 | 0.094 | **`lib-stage2-lean-u1`** 0.057 | `mut-odo-vecdims-add-in-leaf-u2-down` 0.058 | 0.58% |
| `compose` | 4 | 0.027 | 0.032 | `libunord-stage2-sum` 0.018 | **`mut-odo-vecdims-add-in-leaf-u2-ptr`** 0.017 | 0.47% |

The floor-movement paragraph that stood here was cut on 2026-08-22, having read Run 16's column against Run 15's while Run 17 installed this one over it --- the defect `--check-doc` now holds every such movement to. What moves these floors is [an open question](../README.md#what-is-open) and not a sentence under a table.

The pure slot this table carried until 2026-08-22, and the paragraph that read it, retired with the pure/impure distinction when the decision shipped the mutable family's arm; the column now carries the best arm outside the family, which the table above gives per class and which is ahead of `mut-odo-vecdims` in every one of the ten --- the lead that broke the ordering property 2 carried until 2026-09-06. **On three rows --- `runs`, `block` and `compose` --- the bold sits in the CEILING column instead**, the fastest arm there being `mut-odo-vecdims-add-in-leaf-u2-ptr`: the reader's convention counts a `mut-odo-vecdims` sibling as the family's and so as no break, and this file overrides it for that arm alone, the pointer fill being the design the dead-ideas ruling refuses rather than a form the family could ship. So the table's bolding and the head's count of ten differ by convention and not by measurement, and this sentence is where the two are reconciled.

**`rev` --- every stride negated, offset at the top: the view `rev` on every axis builds.** Shapes: `rev-cnn-L1-24x24-c1` (`l` 5184, `sInner` 3), `rev-gather48-src-50` (`l` 22500, `sInner` 3), `rev-primes` (`l` 250357, `sInner` 89).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.12* | *135* | *2.52x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.28* | *147* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.07* | *158* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *158* | *0.00x* |
| libunord-stage5-sum | 0.000 | 0.003 | 0.05 | 157 | 0.01x |
| libunord-stage4-sum | 0.000 | 0.003 | 0.06 | 157 | 0.01x |
| libunord-stage2-sum | 0.000 | 0.003 | 0.08 | 157 | 0.01x |
| libunord-stage3 | 0.001 | 0.003 | 0.11 | 157 | 0.01x |
| libunord-stage5 | 0.001 | 0.003 | 0.06 | 157 | 0.01x |
| libunord-stage4 | 0.001 | 0.003 | 0.07 | 157 | 0.01x |
| libunord-stage2 | 0.001 | 0.003 | 0.06 | 157 | 0.01x |
| libunord-stage1-sum | 0.025 | 0.050 | 0.12 | 147 | 1.03x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.025 | 0.039 | 0.21 | 148 | 1.01x |
| lib-stage1 | 0.026 | 0.047 | 0.10 | 147 | 1.01x |
| lib-stage2-lean | 0.026 | 0.034 | 0.11 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.026 | 0.045 | 0.10 | 147 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.026 | 0.045 | 0.11 | 147 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.045 | 0.11 | 147 | 1.00x |
| lib-stage2-lean-u1 | 0.028 | 0.036 | 0.13 | 146 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.030 | 0.042 | 0.15 | 147 | 1.01x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.030 | 0.045 | 0.13 | 147 | 1.00x |
| liblist-stage4 | 0.039 | 0.046 | 0.21 | 143 | 2.01x |
| liblist-stage2 | 0.039 | 0.046 | 0.23 | 143 | 2.01x |
| liblist-stage3 | 0.040 | 0.047 | 0.24 | 143 | 2.01x |
| liblist-stage1 | 0.044 | 0.058 | 0.31 | 143 | 2.01x |
| libunord-stage1 | 0.045 | 0.061 | 0.28 | 143 | 2.03x |
| *mut-odo-vecdims-aa-distant* | *0.046* | *0.066* | *0.09* | *138* | *1.00x* |
| **mut-odo-vecdims** | **0.047** | 0.068 | 0.09 | 138 | 1.00x |
| *mut-odo-vecdims-aa* | *0.047* | *0.068* | *0.08* | *138* | *1.00x* |
| bq-expand | 0.100 | 0.172 | 0.14 | 129 | 2.52x |
| *bq-expand-aa-adjacent* | *0.101* | *0.172* | *0.11* | *129* | *2.52x* |
| *bq-expand-aa-distant* | *0.101* | *0.174* | *0.17* | *129* | *2.52x* |
| list (baseline) | 1.000 | 1.000 | 0.40 | 87 | 23.43x |
| *list-aa-adjacent* | *1.001* | *1.003* | *0.51* | *87* | *23.43x* |
| *list-aa-distant* | *1.001* | *1.008* | *0.25* | *87* | *23.43x* |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa-distant` at 0.9919, worst cell 2.55% on `rev-cnn-L1-24x24-c1`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 1.0003 on a worst cell of 0.24% on `rev-primes`, its interval covering 1. The in-situ term reads 1.0005, 0.9999 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9941, which the correction amplifies by 1.71x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m7s, peak 96 MiB in use, 24 MiB max residency; the reader reads 35 benchmarks over 3 shapes of the rev class. Anchor: `rev-primes`, `list` at 4.04 ms per call raw, 3.89 ms net.

**Per shape, in the lead's order (rev-cnn-L1-24x24-c1, rev-gather48-src-50, rev-primes):** `mut-odo-vecdims` 0.068/0.052/0.029

**Across the halves:** 11 of the 31 arms are faster on this half and 20 slower, at a geomean of 0.9627, from `libunord-stage5-sum` at 0.4319 to `libunord-stage2` at 1.1199, with `list` itself at 0.9957.

**What the class says:** properties 1, 2 and 3 hold for `mut-odo-vecdims` --- `worst` 0.068 against a `list` it never loses to, and the tiers at 1.00x, 2.52x and 23.43x --- and `libunord-stage5-sum` leads outright at **0.000**, a 99.44% margin against this class's 0.81% floor. That is not a fill beating a fill: master's raw-stride test falls to a copy on a fully reversed view where the lazy unordered list reads it as one slice, and the consumer then folds that slice without concatenating anything, so registration (9)'s clause reads **0.0087 on the basis and 0.0201 on HEAD** --- a hundredfold, and the widest of this run's class-level readings; `libunord-stage5-sum` against `mut-odo-vecdims` on this same class is wider still, at 0.0056. **What this class kills is registration (6)'s tie clause**: `libunord-stage5` against `libunord-stage3` was predicted level here, the sorted view being one block, and reads 1.0549 and 1.0611, past the floor on both halves. **Across the halves this is the class that moves most**, at a geomean of 0.9627 with 11 of 31 arms faster on the basis and 20 slower, `list` itself at 0.9957 and so inside the bar --- the movement is the `libunord` rows and not the baseline.

**`bcast` --- an innermost stride of 0, every run re-reading one element: a broadcast's view.** Shapes: `bcast-inner8` (`l` 51200, `sInner` 8), `bcast-inner900` (`l` 1800000, `sInner` 900), `bcast-tall-Mx2` (`l` 1800000, `sInner` 2).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.61* | *53* | *1.38x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.30* | *83* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *69* | *0.00x* |
| libunord-stage5-sum | 0.019 | 0.026 | 0.48 | 62 | 1.00x |
| libunord-stage4-sum | 0.019 | 0.026 | 0.50 | 62 | 1.00x |
| libunord-stage4 | 0.019 | 0.027 | 0.47 | 62 | 1.00x |
| libunord-stage2-sum | 0.019 | 0.026 | 0.48 | 62 | 1.00x |
| libunord-stage3 | 0.019 | 0.027 | 0.48 | 62 | 1.00x |
| lib-stage2-lean-u1 | 0.019 | 0.026 | 0.48 | 62 | 1.00x |
| libunord-stage5 | 0.019 | 0.027 | 0.55 | 62 | 1.00x |
| lib-stage2-lean | 0.019 | 0.026 | 0.48 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.019 | 0.022 | 0.49 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.020 | 0.026 | 0.46 | 62 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.020 | 0.022 | 0.48 | 61 | 1.00x |
| libunord-stage1-sum | 0.020 | 0.022 | 0.52 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.020 | 0.022 | 0.48 | 61 | 1.00x |
| lib-stage1 | 0.020 | 0.022 | 0.52 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.022 | 0.41 | 61 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.023 | 0.027 | 0.44 | 61 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.035* | *0.062* | *0.39* | *61* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.035* | *0.062* | *0.39* | *61* | *1.00x* |
| **mut-odo-vecdims** | **0.035** | 0.062 | 0.08 | 61 | 1.00x |
| liblist-stage2 | 0.041 | 0.049 | 0.87 | 55 | 2.00x |
| liblist-stage4 | 0.042 | 0.049 | 0.85 | 55 | 2.00x |
| libunord-stage1 | 0.042 | 0.050 | 0.85 | 54 | 2.00x |
| liblist-stage1 | 0.042 | 0.051 | 0.94 | 54 | 2.00x |
| liblist-stage3 | 0.043 | 0.049 | 0.85 | 55 | 2.00x |
| libunord-stage2 | 0.043 | 0.049 | 0.85 | 55 | 2.00x |
| *bq-expand-aa-distant* | *0.092* | *0.098* | *0.39* | *46* | *1.38x* |
| *bq-expand-aa-adjacent* | *0.092* | *0.097* | *0.71* | *46* | *1.38x* |
| bq-expand | 0.092 | 0.097 | 0.72 | 46 | 1.38x |
| list (baseline) | 1.000 | 1.000 | 1.13 | 18 | 20.62x |
| *list-aa-distant* | *1.004* | *1.005* | *0.83* | *18* | *20.62x* |
| *list-aa-adjacent* | *1.007* | *1.013* | *0.86* | *18* | *20.62x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0085, worst cell 1.25% on `bcast-tall-Mx2`, and 1 of 6 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `bcast-inner900`, its interval covering 1. The in-situ term reads 1.0172, 1.0171 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0082, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m10s, peak 141 MiB in use, 42 MiB max residency; the reader reads 35 benchmarks over 3 shapes of the bcast class. Anchor: `bcast-inner900`, `list` at 27.2 ms per call raw, 26.1 ms net.

**Per shape, in the lead's order (bcast-inner8, bcast-inner900, bcast-tall-Mx2):** `mut-odo-vecdims` 0.032/0.021/0.062

**Across the halves:** 25 of the 31 arms are faster on this half and 6 slower, at a geomean of 0.9930, from `libunord-stage1` at 0.9815 to `libunord-stage2` at 1.0082, with `list` itself at 0.9823. **The baseline moved 1.77% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.062, tiers at 1.00x, 1.38x and 20.62x --- and `libunord-stage5-sum` leads at a 40.36% margin against this class's 0.85% floor. An innermost stride of 0 makes every read of the source the same cache line, so the class prices dispatch rather than memory and the reducing consumer's saved concatenation is most of what its lead is. **It is the one class where registrations (6) and (7) both hold**: `libunord-stage5` against `libunord-stage3` reads 1.0014 and 0.9937 and `libunord-stage4` against `libunord-stage5` 0.9974 and 1.0026, every one inside its half's floor, which is the A/A pair both items predicted where the sorted view is one block. **And it is where registration (9)'s tie clause misses**: `libunord-stage5-sum` sits 2.33% and 1.91% behind `libunord-stage1-sum` rather than level with it. **The cross-half line is not read for the pair's variable**, `list` having moved 1.77% here, past the 0.7% bar.

**`bcastmid` --- the stretched axis in the middle instead: stride 0 on an outer dimension.** Shapes: `bcastmid-c32-cnn` (`l` 165888, `sInner` 3), `bcastmid-primes` (`l` 250357, `sInner` 97), `bcastmid-b200k` (`l` 1800000, `sInner` 3), `bcastmid-block150k` (`l` 1800000, `sInner` 300). The fourth landed 2026-08-25 and is the block-copy arm's best case where `bcastmid-b200k` is its worst, its block taken to 150000 elements where the class's others run 3 to 216.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.40* | *70* | *1.57x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.26* | *88* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.02* | *88* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *88* | *0.00x* |
| libunord-stage5-sum | 0.015 | 0.018 | 0.34 | 82 | 1.00x |
| libunord-stage3 | 0.015 | 0.018 | 0.34 | 82 | 1.00x |
| libunord-stage5 | 0.015 | 0.018 | 0.31 | 82 | 1.00x |
| libunord-stage4-sum | 0.016 | 0.018 | 0.33 | 82 | 1.00x |
| libunord-stage4 | 0.016 | 0.018 | 0.33 | 82 | 1.00x |
| lib-stage2-lean | 0.018 | 0.034 | 0.37 | 81 | 1.00x |
| lib-stage2-lean-u1 | 0.018 | 0.033 | 0.36 | 81 | 1.00x |
| libunord-stage2-sum | 0.018 | 0.033 | 0.41 | 81 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.019 | 0.027 | 0.53 | 80 | 1.00x |
| libunord-stage1-sum | 0.022 | 0.033 | 0.37 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.022 | 0.033 | 0.28 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.022 | 0.033 | 0.36 | 80 | 1.00x |
| lib-stage1 | 0.022 | 0.033 | 0.55 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.022 | 0.033 | 0.34 | 80 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.024 | 0.031 | 0.36 | 78 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.024 | 0.034 | 0.44 | 78 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.032* | *0.058* | *0.29* | *76* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.032* | *0.059* | *0.38* | *76* | *1.00x* |
| **mut-odo-vecdims** | **0.033** | 0.060 | 0.31 | 76 | 1.00x |
| liblist-stage4 | 0.033 | 0.047 | 0.55 | 74 | 2.00x |
| libunord-stage2 | 0.034 | 0.048 | 0.59 | 74 | 2.00x |
| liblist-stage3 | 0.034 | 0.050 | 0.68 | 74 | 2.00x |
| liblist-stage2 | 0.034 | 0.050 | 0.62 | 74 | 2.00x |
| libunord-stage1 | 0.039 | 0.046 | 0.63 | 74 | 2.00x |
| liblist-stage1 | 0.039 | 0.045 | 0.56 | 74 | 2.00x |
| bq-expand | 0.087 | 0.132 | 0.45 | 64 | 1.57x |
| *bq-expand-aa-adjacent* | *0.087* | *0.132* | *0.43* | *64* | *1.57x* |
| *bq-expand-aa-distant* | *0.088* | *0.133* | *0.36* | *64* | *1.57x* |
| list (baseline) | 1.000 | 1.000 | 0.93 | 30 | 21.22x |
| *list-aa-adjacent* | *1.001* | *1.016* | *1.21* | *30* | *21.22x* |
| *list-aa-distant* | *1.004* | *1.011* | *0.99* | *30* | *21.22x* |

**Controls:** The largest A/A pair is `bq-expand-aa-distant` at 1.0081, worst cell 2.82% on `bcastmid-b200k`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.02% on `bcastmid-primes`, its interval covering 1. The in-situ term reads 1.0214, 1.0343 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0060, which the correction amplifies by 1.35x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m14s, peak 134 MiB in use, 35 MiB max residency; the reader reads 35 benchmarks over 4 shapes of the bcastmid class. Anchor: `bcastmid-b200k`, `list` at 45.3 ms per call raw, 44.2 ms net.

**Per shape, in the lead's order (bcastmid-c32-cnn, bcastmid-primes, bcastmid-b200k, bcastmid-block150k):** `mut-odo-vecdims` 0.060/0.021/0.037/0.024

**Across the halves:** 8 of the 31 arms are faster on this half and 23 slower, at a geomean of 1.0065, from `list` at 0.9945 to `liblist-stage1` at 1.0205, with `list` itself at 0.9945.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.060, tiers at 1.00x, 1.57x and 21.22x --- and `libunord-stage5-sum` leads at a 54.97% margin against this class's 0.81% floor. The broadcast sits in the middle axis, so the innermost run is real and the class prices a fill that must still walk it. **Registration (6) holds and (7) is killed here**: stage five reads 1.0005 and 1.0012 against stage three, inside both floors, while `libunord-stage4` sits 4.36% and 3.94% behind stage five, past them. **Registration (9)'s tie misses in the arm's favour**, `libunord-stage5-sum` reading 0.6806 and 0.6720 of `libunord-stage1-sum` where a tie was predicted --- master's consumer pays a fill here that the lazy list does not. Across the halves the class reads 1.0065 with 8 of 31 arms faster on the basis, `list` at 0.9945 and inside the bar.

**`window` --- overlapping im2col patches: the workload the README opens by naming, with the overlap the main set's bijective map drops.** Shapes: `window-28x28-k5` (`l` 14400, `sInner` 5), `window-224x224-k3` (`l` 443556, `sInner` 3), `window-64x64-k1x9` (`l` 32256, `sInner` 1), `window-128x128-k7` (`l` 729316, `sInner` 7), `window-224x224-k3-s2` (`l` 110889, `sInner` 3) and `window-224x224-k3-d2` (`l` 435600, `sInner` 3). The last two landed 2026-09-03, a strided and a dilated k3 window, and they are the class's first views whose patches step by more than one; the arm they were registered for was parked the day after, so this run times them for the other arms' sanity alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.21* | *82* | *2.81x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.31* | *100* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *111* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *111* | *0.00x* |
| libunord-stage3 | 0.017 | 0.023 | 0.15 | 102 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.024 | 0.027 | 0.19 | 98 | 1.00x |
| lib-stage2-lean | 0.025 | 0.034 | 0.12 | 96 | 1.00x |
| lib-stage2-lean-u1 | 0.027 | 0.037 | 0.16 | 95 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.027 | 0.032 | 0.16 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.032 | 0.16 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.032 | 0.17 | 96 | 1.00x |
| lib-stage1 | 0.028 | 0.032 | 0.22 | 96 | 1.00x |
| libunord-stage1-sum | 0.028 | 0.031 | 0.17 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.028 | 0.032 | 0.15 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.029 | 0.034 | 0.16 | 96 | 1.00x |
| libunord-stage2-sum | 0.030 | 0.035 | 0.18 | 96 | 1.00x |
| liblist-stage1 | 0.039 | 0.043 | 0.34 | 93 | 2.00x |
| libunord-stage1 | 0.039 | 0.043 | 0.33 | 93 | 2.00x |
| libunord-stage2 | 0.044 | 0.095 | 0.38 | 92 | 2.00x |
| liblist-stage4 | 0.044 | 0.111 | 0.35 | 92 | 2.00x |
| liblist-stage3 | 0.044 | 0.114 | 0.32 | 92 | 2.00x |
| liblist-stage2 | 0.044 | 0.096 | 0.36 | 92 | 2.00x |
| **mut-odo-vecdims** | **0.054** | 0.095 | 0.12 | 90 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.054* | *0.096* | *0.17* | *90* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.054* | *0.096* | *0.16* | *90* | *1.00x* |
| libunord-stage5-sum | 0.106 | 0.292 | 0.95 | 86 | 3.18x |
| libunord-stage4-sum | 0.112 | 0.274 | 1.13 | 86 | 3.18x |
| *bq-expand-aa-distant* | *0.118* | *0.130* | *0.17* | *78* | *2.81x* |
| bq-expand | 0.118 | 0.130 | 0.22 | 78 | 2.81x |
| *bq-expand-aa-adjacent* | *0.118* | *0.130* | *0.22* | *78* | *2.81x* |
| libunord-stage5 | 0.196 | 0.473 | 0.64 | 78 | 4.18x |
| libunord-stage4 | 0.197 | 0.477 | 0.65 | 78 | 4.18x |
| *list-aa-distant* | *0.995* | *1.005* | *0.49* | *44* | *24.76x* |
| *list-aa-adjacent* | *0.996* | *1.004* | *0.41* | *44* | *24.76x* |
| list (baseline) | 1.000 | 1.000 | 0.42 | 43 | 24.76x |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 0.9903, worst cell 5.19% on `window-224x224-k3`, and 6 of 6 intervals cover 1. The `sum-only` halves agree at 0.9991 on a worst cell of 0.33% on `window-224x224-k3-s2`, its interval missing 1. The in-situ term reads 1.0000, 1.0809 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9905, which the correction amplifies by 1.03x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h18m8s, peak 129 MiB in use, 24 MiB max residency; the reader reads 35 benchmarks over 6 shapes of the window class. Anchor: `window-128x128-k7`, `list` at 12.8 ms per call raw, 12.3 ms net.

**Per shape, in the lead's order (window-28x28-k5, window-224x224-k3, window-64x64-k1x9, window-128x128-k7, window-224x224-k3-s2, window-224x224-k3-d2):** `mut-odo-vecdims` 0.044/0.053/0.095/0.035/0.056/0.057

**Across the halves:** 14 of the 31 arms are faster on this half and 17 slower, at a geomean of 1.0083, from `libunord-stage5` at 0.9760 to `lib-stage2-lean-u1` at 1.0557, with `list` itself at 0.9768. **The baseline moved 2.32% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.095, the highest of the ten, and tiers at 1.00x, 2.81x and 24.76x, `bq-expand`'s the highest of the ten where this class's own `m` shows through --- and `libunord-stage3` leads at a 68.32% margin against a 0.97% floor. **This is the class that kills registration (8)'s second condition**: `liblist-stage3` sits **1.2419 and 1.1953** behind `liblist-stage1` here, past floors of 0.97% and 2.67% on both halves, so canonicalization and the odometer cost more on a sliding window than the slice recursion they replace. **And it carries the run's widest reading of an arm BEHIND its own ceiling**, `libunord-stage5` against `libunord-stage3` at 10.5070 and 10.7998, ten times behind: the kernel's width makes the sorted view a list of short runs, which is the shape the slice list is worst at and the fill best at. **The cross-half line is not read for the pair's variable**, `list` having moved 2.32% here, the widest of the ten.

**`scaled` --- superincreasing strides, none of them 1: a hand-built dilated view.** Shapes: `scaled-super-r3` (`l` 60000, `sInner` 30), `scaled-rank1-m1` (`l` 300000, `sInner` 300000 --- rank 1, so `m` is 1 and the whole view is one strided run), `scaled-r5` (`l` 15015, `sInner` 13).

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.07* | *119* | *1.14x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.10* | *146* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.04* | *138* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *138* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.024 | 0.034 | 0.14 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.025 | 0.035 | 0.23 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.026 | 0.035 | 0.13 | 128 | 1.00x |
| lib-stage1 | 0.026 | 0.035 | 0.19 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.026 | 0.034 | 0.16 | 127 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.035 | 0.13 | 128 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.026 | 0.034 | 0.14 | 127 | 1.00x |
| libunord-stage2-sum | 0.026 | 0.034 | 0.23 | 127 | 1.01x |
| libunord-stage3 | 0.027 | 0.035 | 0.19 | 127 | 1.01x |
| lib-stage2-lean | 0.027 | 0.035 | 0.17 | 127 | 1.00x |
| libunord-stage5 | 0.027 | 0.035 | 0.16 | 127 | 1.01x |
| libunord-stage4-sum | 0.027 | 0.034 | 0.19 | 127 | 1.01x |
| libunord-stage5-sum | 0.027 | 0.034 | 0.17 | 127 | 1.01x |
| libunord-stage4 | 0.027 | 0.035 | 0.13 | 127 | 1.01x |
| libunord-stage1-sum | 0.028 | 0.034 | 0.18 | 127 | 1.01x |
| lib-stage2-lean-u1 | 0.028 | 0.040 | 0.17 | 127 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.031* | *0.033* | *0.10* | *127* | *1.00x* |
| **mut-odo-vecdims** | **0.031** | 0.033 | 0.08 | 127 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.033* | *0.08* | *127* | *1.00x* |
| liblist-stage1 | 0.039 | 0.064 | 0.25 | 124 | 2.00x |
| liblist-stage3 | 0.040 | 0.064 | 0.21 | 124 | 2.00x |
| libunord-stage1 | 0.040 | 0.064 | 0.45 | 123 | 2.01x |
| liblist-stage2 | 0.040 | 0.064 | 0.23 | 124 | 2.00x |
| liblist-stage4 | 0.042 | 0.064 | 0.31 | 124 | 2.00x |
| libunord-stage2 | 0.042 | 0.064 | 0.54 | 123 | 2.01x |
| bq-expand | 0.096 | 0.099 | 0.07 | 112 | 1.14x |
| *bq-expand-aa-adjacent* | *0.096* | *0.099* | *0.10* | *112* | *1.14x* |
| *bq-expand-aa-distant* | *0.096* | *0.099* | *0.07* | *112* | *1.14x* |
| list (baseline) | 1.000 | 1.000 | 0.26 | 71 | 19.43x |
| *list-aa-distant* | *1.002* | *1.007* | *0.23* | *71* | *19.43x* |
| *list-aa-adjacent* | *1.003* | *1.008* | *0.21* | *71* | *19.43x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0029, worst cell 0.77% on `scaled-rank1-m1`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 1.0013 on a worst cell of 0.44% on `scaled-rank1-m1`, its interval covering 1. The in-situ term reads 1.0129, 1.0081 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0028, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h9m8s, peak 110 MiB in use, 29 MiB max residency; the reader reads 35 benchmarks over 3 shapes of the scaled class. Anchor: `scaled-rank1-m1`, `list` at 4.78 ms per call raw, 4.6 ms net.

**Per shape, in the lead's order (scaled-super-r3, scaled-rank1-m1, scaled-r5):** `mut-odo-vecdims` 0.026/0.033/0.032

**Across the halves:** 19 of the 31 arms are faster on this half and 12 slower, at a geomean of 0.9977, from `libunord-stage2` at 0.9549 to `lib-stage2-lean-u1` at 1.0634, with `list` itself at 0.9795. **The baseline moved 2.05% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.033, joint second lowest of the ten with `block` and behind `compose`'s 0.032, tiers at 1.00x, 1.14x and 19.43x, and property 2's `bq-expand` clause at 1.00003 on `scaled-rank1-m1`, the closest cell in the run --- and `lib-stage1` leads outside the family at a 7.46% margin against this class's 0.29% floor, the narrowest floor of the ten. **It is the one class the family's own ceiling leads**, `mut-odo-vecdims-add-in-leaf-u1-ptr` at 0.024 against `lib-stage1`'s 0.026: a rank-1 scaling leaves so little work per element that the pointer walk's saved reload is a visible share of it. Across the halves the class reads 0.9977 with 19 of 31 arms faster on the basis, but `list` moved 2.05% here, so the line is printed and is not the pair's variable --- which is the caveat registration (12) did not anticipate, this being one of the four classes that item names.

**`runs` --- run length swept from 2 to 65536 with innermost stride 1 throughout: regime 2, which the library reaches by a route of its own, and the population the rework's question needed --- extended on Run 22 from seven views to eleven and on Run 24 to fourteen.** Shapes: `runs-2` (`l` 1800000, `sInner` 2), `runs-3` (`l` 1800000, `sInner` 3 --- a k3 conv row), `runs-4` (`l` 1800000, `sInner` 4 --- landed on Run 22, and the first view in the suite with a canonical innermost extent of 4, the branch the short-body fills take and which nothing, `check` included, had exercised), `runs-5` (`l` 1800000, `sInner` 5 --- landed on Run 22, beside it), `runs-7` (`l` 1799994, `sInner` 7 --- landed on Run 24, one past the short bodies of `fillStage2Short`, which write runs of 2 to 5: the first length where the stepping loop with its odd tail takes over from them, and a k7 conv row), `runs-9` (`l` 1800000, `sInner` 9 --- the window probe's run), `runs-96` (`l` 1800000, `sInner` 96 --- an image row), `runs-256` (`l` 1799936, `sInner` 256 --- landed on Run 22, and the dispatch threshold's own cell, `>= dispRun` firing exactly here), `runs-512` (`l` 1799680, `sInner` 512 --- landed on Run 22, bracketing `dispRun` within a factor of two), `runs-1024` (`l` 1799168, `sInner` 1024), `runs-4096` (`l` 1798144, `sInner` 4096 --- landed on Run 24), `runs-16384` (`l` 1785856, `sInner` 16384 --- landed on Run 24, the two of them inside the 64x gap the crossover moved into), `runs-65536` (`l` 1769472, `sInner` 65536 --- a few long runs), `runs-r3-48x30` (`l` 1800000, `sInner` 1440 --- rank 3, merging to runs of 1440). Every shape sits at `l` of about 1.8M, so what varies across the class is the run length alone.

| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.40* | *52* | *1.08x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.16* | *78* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *69* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.03* | *69* | *0.00x* |
| libunord-stage5-sum | 0.007 | 0.332 | 0.07 | 66 | 0.13x |
| libunord-stage4-sum | 0.007 | 0.332 | 0.07 | 66 | 0.13x |
| libunord-stage1-sum | 0.011 | 0.508 | 0.11 | 65 | 0.19x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.028 | 0.16 | 60 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.026 | 0.028 | 0.13 | 60 | 1.00x |
| libunord-stage3 | 0.027 | 0.028 | 0.19 | 59 | 1.00x |
| lib-stage2-lean | 0.027 | 0.029 | 0.11 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.027 | 0.029 | 0.12 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.027 | 0.029 | 0.14 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.027 | 0.029 | 0.14 | 59 | 1.00x |
| lib-stage2-lean-u1 | 0.028 | 0.030 | 0.14 | 59 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.028 | 0.032 | 0.15 | 59 | 1.00x |
| *mut-odo-vecdims-aa* | *0.031* | *0.062* | *0.10* | *59* | *1.00x* |
| **mut-odo-vecdims** | **0.031** | 0.062 | 0.13 | 59 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.031* | *0.062* | *0.11* | *59* | *1.00x* |
| libunord-stage2-sum | 0.034 | 0.266 | 0.11 | 69 | 0.14x |
| libunord-stage2 | 0.066 | 1.076 | 0.46 | 57 | 1.14x |
| liblist-stage2 | 0.066 | 1.078 | 0.28 | 57 | 1.14x |
| libunord-stage4 | 0.086 | 1.226 | 0.52 | 56 | 1.13x |
| libunord-stage5 | 0.086 | 1.225 | 0.77 | 56 | 1.13x |
| liblist-stage3 | 0.087 | 1.219 | 0.43 | 56 | 1.13x |
| liblist-stage4 | 0.088 | 1.231 | 0.52 | 56 | 1.13x |
| bq-expand | 0.097 | 0.103 | 0.40 | 46 | 1.08x |
| *bq-expand-aa-adjacent* | *0.097* | *0.101* | *0.39* | *46* | *1.08x* |
| *bq-expand-aa-distant* | *0.097* | *0.102* | *0.05* | *46* | *1.08x* |
| lib-stage1 | 0.106 | 1.198 | 0.22 | 55 | 1.19x |
| libunord-stage1 | 0.106 | 1.237 | 0.40 | 55 | 1.19x |
| liblist-stage1 | 0.107 | 1.214 | 0.28 | 55 | 1.19x |
| list (baseline) | 1.000 | 1.000 | 2.48 | 18 | 19.27x |
| *list-aa-distant* | *1.029* | *1.048* | *0.60* | *18* | *19.27x* |
| *list-aa-adjacent* | *1.032* | *1.047* | *0.22* | *18* | *19.27x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0315, worst cell 4.66% on `runs-65536`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 1.0001 on a worst cell of 0.53% on `runs-512`, its interval covering 1. The in-situ term reads 1.0308, 1.0193 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0303, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h42m28s, peak 564 MiB in use, 247 MiB max residency; the reader reads 35 benchmarks over 14 shapes of the runs class. Anchor: `runs-2`, `list` at 37.9 ms per call raw, 36.9 ms net.

**Per shape, in the lead's order (runs-2, runs-3, runs-4, runs-5, runs-7, runs-9, runs-96, runs-256, runs-512, runs-1024, runs-4096, runs-16384, runs-65536, runs-r3-48x30):** `mut-odo-vecdims` 0.062/0.052/0.045/0.042/0.037/0.033/0.028/0.028/0.028/0.028/0.027/0.027/0.028/0.029

**Across the halves:** 7 of the 30 voting arms are faster on this half and 23 slower, at a geomean of 1.0091, from `list` at 0.9887 to `libunord-stage1-sum` at 1.0853, `libunord-stage2-sum` sitting out as degenerate, a basis cell of theirs not left positive by the correction, with `list` itself at 0.9887. **The baseline moved 1.13% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.062, tiers at 1.00x, 1.08x and 19.27x --- and `libunord-stage5-sum` leads at 0.007 against this class's 3.15% floor, the widest floor of the ten and the class where a margin has to be largest to mean anything. **This class was measured twice.** Its first pair carried the run's one intrusion, four consecutive benches of `runs-512` on the HEAD half at 0.9 to 1.0 of a further core, and post-run step 3's rerun was taken on 2026-09-08 from 11:54:26 to 13:19:29 with the daily timer that caused it disabled --- both halves, as a pair read across two windows is not a pair. Both reran processes are clean, peaking at 0.16 and 0.17 of a core against the instrument's 0.25 bar. **This is the class registration (6) was written around, and it splits.** The ceiling half holds exactly: `libunord-stage5` against `libunord-stage3` runs 45.21 at `runs-2` down through 9.24 at `runs-9`, crosses between **`runs-1024` at 1.0743 and `runs-4096` at 0.9385**, and reaches 0.9017 at `runs-65536` --- inside the `runs-256`-to-`runs-4096` window predicted from where `dispRun` was cut, so one memcpy per run does win once the runs are long enough. The port half is killed: stage five sits **1.0548 and 1.0559** behind `libunord-stage2` on both halves, past both floors, so the on-demand list costs more than the base-offset table it replaces. **Registration (8) is killed here too**, `liblist-stage4` reading 1.0563 and 1.0540 behind `liblist-stage2`. **And registration (9) is at its widest here**: `libunord-stage1-sum` leads its own fill at **0.0928 and 0.0854**, so the concatenation is nine tenths of that call. The cross-half line is printed and not read for the pair, `list` having moved 1.13%.



**`flip` --- a dense array reversed, whole or along its last axis, so the innermost stride is -1: regime 2 mirrored, and one run at stride -1 once canonicalized.** Shapes: `flip-whole-square` (`l` 1798281, `sInner` 1341), `flip-last-c32` (`l` 165888, `sInner` 3), `flip-last-rows` (`l` 1800000, `sInner` 96), and two that landed 2026-09-05 and are the `block` class's gap-64 rows reversed --- `flip-inner-gap64` (`l` 131072, `sInner` 64), each row reversed, and `flip-outer-gap64` (`l` 131072, `sInner` 64), the rows in reverse order.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.16* | *87* | *1.03x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.09* | *103* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.03* | *117* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.02* | *117* | *0.00x* |
| libunord-stage4-sum | 0.014 | 0.014 | 0.02 | 115 | 0.00x |
| libunord-stage5-sum | 0.014 | 0.014 | 0.01 | 115 | 0.00x |
| libunord-stage3 | 0.023 | 0.025 | 0.02 | 111 | 0.00x |
| libunord-stage2-sum | 0.025 | 0.025 | 0.01 | 111 | 0.00x |
| lib-stage2-lean | 0.025 | 0.048 | 0.12 | 107 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.026 | 0.043 | 0.09 | 102 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.027 | 0.044 | 0.41 | 103 | 1.00x |
| libunord-stage1-sum | 0.029 | 0.048 | 0.15 | 102 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.029 | 0.041 | 0.24 | 102 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.030 | 0.041 | 0.11 | 102 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.030 | 0.048 | 0.25 | 102 | 1.00x |
| lib-stage2-lean-u1 | 0.031 | 0.050 | 0.26 | 106 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.031 | 0.046 | 0.08 | 101 | 1.00x |
| **mut-odo-vecdims** | **0.034** | 0.057 | 0.11 | 95 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.034* | *0.057* | *0.10* | *95* | *1.00x* |
| *mut-odo-vecdims-aa* | *0.035* | *0.057* | *0.13* | *95* | *1.00x* |
| lib-stage1 | 0.036 | 0.054 | 0.12 | 102 | 1.00x |
| libunord-stage2 | 0.039 | 0.041 | 0.04 | 107 | 0.00x |
| libunord-stage4 | 0.042 | 0.047 | 0.02 | 106 | 0.00x |
| libunord-stage5 | 0.042 | 0.046 | 0.02 | 106 | 0.00x |
| liblist-stage2 | 0.046 | 0.082 | 0.19 | 102 | 2.00x |
| liblist-stage4 | 0.048 | 0.081 | 0.17 | 102 | 2.00x |
| liblist-stage3 | 0.048 | 0.084 | 0.52 | 102 | 2.00x |
| liblist-stage1 | 0.054 | 0.080 | 0.40 | 98 | 2.00x |
| libunord-stage1 | 0.054 | 0.081 | 0.72 | 98 | 2.00x |
| *bq-expand-aa-adjacent* | *0.102* | *0.130* | *0.26* | *83* | *1.03x* |
| bq-expand | 0.103 | 0.129 | 0.18 | 83 | 1.03x |
| *bq-expand-aa-distant* | *0.103* | *0.129* | *0.17* | *83* | *1.03x* |
| *list-aa-distant* | *0.995* | *1.006* | *0.28* | *47* | *19.19x* |
| *list-aa-adjacent* | *0.999* | *1.011* | *0.24* | *47* | *19.19x* |
| list (baseline) | 1.000 | 1.000 | 0.30 | 47 | 19.19x |

**Controls:** The largest A/A pair is `list-aa-distant` at 0.9959, worst cell 1.21% on `flip-inner-gap64`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 0.9980 on a worst cell of 0.52% on `flip-inner-gap64`, its interval missing 1. The in-situ term reads 1.0119, 1.0231 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9960, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m12s, peak 187 MiB in use, 62 MiB max residency; the reader reads 35 benchmarks over 5 shapes of the flip class. Anchor: `flip-last-rows`, `list` at 30.6 ms per call raw, 29.5 ms net.

**Per shape, in the lead's order (flip-whole-square, flip-last-c32, flip-last-rows, flip-inner-gap64, flip-outer-gap64):** `mut-odo-vecdims` 0.027/0.057/0.053/0.029/0.029

**Across the halves:** 16 of the 24 voting arms are faster on this half and 8 slower, at a geomean of 1.0007, from `mut-odo-vecdims-add-in-leaf-u2-down` at 0.9627 to `lib-stage2-lean-u1` at 1.1201, `libunord-stage2`, `libunord-stage2-sum`, `libunord-stage3`, `libunord-stage4`, `libunord-stage4-sum`, `libunord-stage5`, `libunord-stage5-sum` sitting out as degenerate, a basis cell of theirs not left positive by the correction, with `list` itself at 0.9928. **The baseline moved 0.72% between the halves, past the 0.7% that lets two columns be differenced, so this line is NOT read for the pair's variable.** The table above is one process's and stands; what goes is the comparison.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.057, tiers at 1.00x, 1.03x and 19.19x --- and `libunord-stage4-sum` leads at 0.014, **a lead the reader refuses to price**: its cells are their own forcing term on three of the five views, so the margin against `mut-odo-vecdims` is not computed at all and the number is a floor rather than a figure. **This class carries the most degenerate arms in the run**, seven of the thirty-one --- `libunord-stage2`, `-stage3`, `-stage4`, `-stage5` and three of the four `-sum` consumers --- kept out of the vote, the extremes and the geomeans alike, so registrations (6), (7) and (9) are all NOT READ here on the basis. What the class does read is the gap views: `flip-inner-gap64` and `flip-outer-gap64` are where a reversed axis leaves runs for a list to find, and they are the two views the unordered family was invented for. Across the halves the class reads 1.0007 over the 24 arms that vote, `list` at 0.9928 and past the 0.7% bar, so the line is printed and not read for the pair.

**`block` --- regime 2 as a sub-block of a wider array, the gap between one run and the next being the variable.** Shapes: `block-run64-gap1` (`l` 131072, `sInner` 64), `block-run64-gap64` (`l` 131072, `sInner` 64), `block-run64-page` (`l` 131072, `sInner` 64), `block-run64-off7` (`l` 131072, `sInner` 64), `block-r3-vol64` (`l` 262144, `sInner` 64). The first three sweep the gap from one element to a page at one run length, the fourth is `block-run64-gap64` moved off an eight-element boundary, and the fifth is a rank-3 block whose two outer dimensions do not merge.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.10* | *104* | *1.03x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.19* | *129* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *122* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.04* | *122* | *0.00x* |
| libunord-stage2-sum | 0.005 | 0.005 | 0.23 | 123 | 0.31x |
| libunord-stage5-sum | 0.014 | 0.024 | 0.13 | 115 | 0.28x |
| libunord-stage4-sum | 0.014 | 0.024 | 0.12 | 115 | 0.28x |
| libunord-stage1-sum | 0.021 | 0.032 | 0.19 | 113 | 0.42x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.022 | 0.025 | 0.11 | 112 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.023 | 0.028 | 0.13 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.024 | 0.028 | 0.12 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.024 | 0.028 | 0.10 | 111 | 1.00x |
| lib-stage2-lean | 0.024 | 0.028 | 0.13 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.024 | 0.028 | 0.13 | 111 | 1.00x |
| libunord-stage3 | 0.024 | 0.028 | 0.12 | 111 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.025 | 0.030 | 0.13 | 111 | 1.00x |
| lib-stage2-lean-u1 | 0.025 | 0.030 | 0.12 | 111 | 1.00x |
| *mut-odo-vecdims-aa* | *0.026* | *0.033* | *0.12* | *110* | *1.00x* |
| **mut-odo-vecdims** | **0.026** | 0.033 | 0.09 | 110 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.026* | *0.034* | *0.13* | *110* | *1.00x* |
| libunord-stage2 | 0.040 | 0.041 | 0.20 | 107 | 1.31x |
| liblist-stage2 | 0.040 | 0.041 | 0.18 | 107 | 1.31x |
| liblist-stage3 | 0.045 | 0.046 | 0.26 | 106 | 1.28x |
| libunord-stage5 | 0.046 | 0.046 | 0.36 | 106 | 1.28x |
| liblist-stage4 | 0.046 | 0.046 | 0.23 | 106 | 1.28x |
| libunord-stage4 | 0.046 | 0.047 | 0.25 | 105 | 1.28x |
| lib-stage1 | 0.056 | 0.065 | 0.10 | 103 | 1.42x |
| libunord-stage1 | 0.056 | 0.065 | 0.12 | 103 | 1.42x |
| liblist-stage1 | 0.058 | 0.065 | 0.11 | 103 | 1.42x |
| *bq-expand-aa-adjacent* | *0.094* | *0.095* | *0.10* | *97* | *1.03x* |
| bq-expand | 0.094 | 0.095 | 0.11 | 97 | 1.03x |
| *bq-expand-aa-distant* | *0.094* | *0.095* | *0.09* | *96* | *1.03x* |
| *list-aa-distant* | *0.996* | *0.999* | *0.20* | *56* | *19.19x* |
| *list-aa-adjacent* | *0.997* | *1.000* | *0.30* | *56* | *19.19x* |
| list (baseline) | 1.000 | 1.000 | 0.27 | 56 | 19.19x |

**Controls:** The largest A/A pair is `list-aa-distant` at 0.9964, worst cell 0.57% on `block-run64-gap64`, and 4 of 6 intervals cover 1. The `sum-only` halves agree at 0.9992 on a worst cell of 0.48% on `block-run64-off7`, its interval covering 1. The in-situ term reads 1.0215, 1.0068 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9966, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m8s, peak 133 MiB in use, 36 MiB max residency; the reader reads 35 benchmarks over 5 shapes of the block class. Anchor: `block-r3-vol64`, `list` at 4.13 ms per call raw, 3.97 ms net.

**Per shape, in the lead's order (block-run64-gap1, block-run64-gap64, block-run64-page, block-run64-off7, block-r3-vol64):** `mut-odo-vecdims` 0.021/0.028/0.033/0.028/0.022

**Across the halves:** 3 of the 30 voting arms are faster on this half and 27 slower, at a geomean of 1.0094, from `libunord-stage2` at 0.9820 to `libunord-stage1-sum` at 1.0405, `libunord-stage2-sum` sitting out as degenerate, a basis cell of theirs not left positive by the correction, with `list` itself at 1.0062.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.033, tiers at 1.00x, 1.03x and 19.19x --- and `libunord-stage2-sum` leads at 0.005, again a lead the reader refuses to price, its cell being its own forcing term on four of the five views. **This class kills three registrations between them.** `libunord-stage5` sits **1.1290 and 1.1174** behind `libunord-stage2`, the widest reading of registration (6)'s port condition and past floors of 0.36% and 0.68%. `liblist-stage4` sits **1.1327 and 1.1020** behind `liblist-stage2`, the widest reading of registration (8)'s first. And registration (9) reads the other way, `libunord-stage5-sum` **0.6164 and 0.6396** ahead of `libunord-stage1-sum`, which is where the odometer earns its keep: long runs at a known gap are what a slice list is for once something folds it rather than concatenating it. Across the halves the class reads 1.0094, the highest of the ten, with only 3 of 30 voting arms faster on the basis and `list` at 1.0062, inside the bar.

**`small` --- one view per canonical regime at a few hundred elements, where a per-call cost is a share of the call: the one class defined by a size and not by an operation.** Shapes: `small-row96` (`l` 384, `sInner` 96), `small-patch-k5` (`l` 150, `sInner` 5), `small-bcast32` (`l` 256, `sInner` 32), `small-flat64` (`l` 256, `sInner` 64), and `small-patch-r5` (`l` 256, `sInner` 4), a rank-5 im2col patch canonicalizing to rank 4, which landed 2026-09-05.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.15* | *223* | *1.34x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.33* | *238* | *1.27x* |
| *sum-only-early* | *--* | *--* | *0.03* | *250* | *0.01x* |
| *sum-only-late* | *--* | *--* | *0.03* | *250* | *0.01x* |
| lib-stage2-lean-u1 | 0.057 | 0.105 | 0.23 | 228 | 1.40x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.058 | 0.072 | 0.29 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.058 | 0.072 | 0.32 | 229 | 1.27x |
| lib-stage2-lean | 0.058 | 0.103 | 0.25 | 228 | 1.40x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.058 | 0.072 | 0.34 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.059 | 0.070 | 0.36 | 229 | 1.27x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.064 | 0.079 | 0.29 | 228 | 1.44x |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.065 | 0.082 | 0.28 | 228 | 1.44x |
| *mut-odo-vecdims-aa* | *0.065* | *0.094* | *0.22* | *229* | *1.27x* |
| *mut-odo-vecdims-aa-distant* | *0.066* | *0.094* | *0.21* | *229* | *1.27x* |
| **mut-odo-vecdims** | **0.066** | 0.094 | 0.23 | 229 | 1.27x |
| libunord-stage2-sum | 0.081 | 0.190 | 0.21 | 226 | 2.04x |
| liblist-stage4 | 0.082 | 0.131 | 0.26 | 224 | 2.52x |
| liblist-stage2 | 0.082 | 0.134 | 0.37 | 223 | 2.49x |
| libunord-stage4-sum | 0.084 | 0.246 | 0.40 | 225 | 1.96x |
| libunord-stage5-sum | 0.087 | 0.252 | 0.34 | 224 | 1.88x |
| libunord-stage3 | 0.092 | 0.164 | 0.27 | 222 | 1.74x |
| lib-stage1 | 0.092 | 0.120 | 0.22 | 221 | 2.30x |
| libunord-stage1-sum | 0.093 | 0.175 | 0.23 | 223 | 2.06x |
| liblist-stage3 | 0.093 | 0.160 | 0.20 | 222 | 2.67x |
| libunord-stage2 | 0.105 | 0.216 | 0.21 | 217 | 1.94x |
| libunord-stage5 | 0.105 | 0.326 | 0.24 | 221 | 1.81x |
| libunord-stage4 | 0.105 | 0.323 | 0.31 | 222 | 1.92x |
| liblist-stage1 | 0.107 | 0.135 | 0.31 | 221 | 2.61x |
| libunord-stage1 | 0.119 | 0.201 | 0.24 | 218 | 3.05x |
| *bq-expand-aa-adjacent* | *0.134* | *0.168* | *0.16* | *218* | *1.34x* |
| bq-expand | 0.134 | 0.167 | 0.19 | 218 | 1.34x |
| *bq-expand-aa-distant* | *0.134* | *0.168* | *0.18* | *218* | *1.34x* |
| *list-aa-adjacent* | *0.998* | *1.002* | *0.24* | *182* | *19.50x* |
| *list-aa-distant* | *0.998* | *1.002* | *0.16* | *182* | *19.50x* |
| list (baseline) | 1.000 | 1.000 | 0.20 | 182 | 19.50x |

**Controls:** The largest A/A pair is `mut-odo-vecdims-aa` at 0.9942, worst cell 1.12% on `small-patch-k5`, and 5 of 6 intervals cover 1. The `sum-only` halves agree at 1.0000 on a worst cell of 0.05% on `small-patch-r5`, its interval covering 1. The in-situ term reads 0.9663, 0.9914 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 0.9961, which the correction amplifies by 1.55x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h15m10s, peak 134 MiB in use, 54 MiB max residency; the reader reads 35 benchmarks over 5 shapes of the small class. Anchor: `small-row96`, `list` at 6.05 us per call raw, 5.82 us net.

**Per shape, in the lead's order (small-row96, small-patch-k5, small-bcast32, small-flat64, small-patch-r5):** `mut-odo-vecdims` 0.045/0.083/0.055/0.064/0.094

**Across the halves:** 28 of the 31 arms are faster on this half and 3 slower, at a geomean of 0.9828, from `libunord-stage5-sum` at 0.9576 to `liblist-stage2` at 1.0329, with `list` itself at 0.9988.

**What the class says:** properties 1 and 2 hold, property 2 at its closest reading anywhere, `small-row96` at 1.00441 on both halves, and **property 3's level clause does not apply here**, this being the class built to break it: the five non-pointer `mut-odo-vecdims` fills read **1.27x** the result rather than 1.00x, and `bq-expand` 1.34x. `worst` is 0.094 and `lib-stage2-lean-u1` leads outside the family at a 13.73% margin against a 0.58% floor --- the arm this run added as the unrolling's control, leading the class its parent used to. **This class kills two registrations.** `libunord-stage4` is **0.9816 and 0.9644** ahead of `libunord-stage5` where stage five was predicted ahead, which is registration (7)'s kill in its own words: at a few hundred elements the second canonicalization costs more than the stride list it replaces. And `liblist-stage3` and `liblist-stage4` part by **1.1377 and 1.1388**, registration (8)'s third kill, so the natural-strides comparison is priced here and nowhere else. Across the halves the class reads 0.9828 with 28 of 31 arms faster on the basis, `list` at 0.9988 and inside the bar --- the one class where the compilers part and the baseline does not.

**`compose` --- a zero stride combined with a second mechanism, as the library composes its operations and no one operation's class builds.** Shapes: `compose-rev-bcast` (`l` 51200, `sInner` 8), `compose-slice-bcast` (`l` 51200, `sInner` 8), `compose-zero-mid` (`l` 1800000, `sInner` 100), `compose-scalar` (`l` 1800000, `sInner` 1500). The first is a broadcast reversed, the second the same broadcast at an offset, the third a second zero stride the first cannot merge with, and the fourth every stride zero.
| strategy | time | worst | CI% | smp | alloc |
|---|---:|---:|---:|---:|---:|
| *bq-expand-nosum* | *--* | *--* | *0.33* | *88* | *1.21x* |
| *mut-odo-vecdims-nosum* | *--* | *--* | *0.20* | *113* | *1.00x* |
| *sum-only-early* | *--* | *--* | *0.01* | *105* | *0.00x* |
| *sum-only-late* | *--* | *--* | *0.01* | *105* | *0.00x* |
| mut-odo-vecdims-add-in-leaf-u2-ptr | 0.017 | 0.018 | 0.33 | 98 | 1.00x |
| libunord-stage2-sum | 0.018 | 0.019 | 0.29 | 97 | 1.00x |
| libunord-stage4-sum | 0.018 | 0.018 | 0.27 | 98 | 1.00x |
| lib-stage2-lean | 0.018 | 0.019 | 0.30 | 97 | 1.00x |
| libunord-stage4 | 0.018 | 0.019 | 0.29 | 98 | 1.00x |
| libunord-stage5-sum | 0.018 | 0.019 | 0.28 | 97 | 1.00x |
| lib-stage2-lean-u1 | 0.018 | 0.018 | 0.30 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1-ptr | 0.018 | 0.019 | 0.32 | 97 | 1.00x |
| libunord-stage5 | 0.018 | 0.019 | 0.29 | 97 | 1.00x |
| libunord-stage3 | 0.018 | 0.019 | 0.30 | 97 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u1 | 0.019 | 0.021 | 0.32 | 96 | 1.00x |
| libunord-stage1-sum | 0.019 | 0.021 | 0.28 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2 | 0.020 | 0.021 | 0.26 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-down | 0.020 | 0.021 | 0.27 | 96 | 1.00x |
| lib-stage1 | 0.020 | 0.021 | 0.31 | 96 | 1.00x |
| mut-odo-vecdims-add-in-leaf-u2-last | 0.021 | 0.022 | 0.25 | 96 | 1.00x |
| *mut-odo-vecdims-aa-distant* | *0.027* | *0.032* | *0.26* | *94* | *1.00x* |
| **mut-odo-vecdims** | **0.027** | 0.032 | 0.17 | 94 | 1.00x |
| *mut-odo-vecdims-aa* | *0.027* | *0.033* | *0.24* | *94* | *1.00x* |
| liblist-stage2 | 0.038 | 0.046 | 0.46 | 92 | 2.00x |
| liblist-stage4 | 0.038 | 0.046 | 0.51 | 92 | 2.00x |
| liblist-stage3 | 0.038 | 0.046 | 0.52 | 92 | 2.00x |
| libunord-stage2 | 0.039 | 0.046 | 0.53 | 91 | 2.00x |
| libunord-stage1 | 0.039 | 0.047 | 0.57 | 91 | 2.00x |
| liblist-stage1 | 0.039 | 0.048 | 0.55 | 91 | 2.00x |
| *bq-expand-aa-adjacent* | *0.094* | *0.096* | *0.39* | *80* | *1.21x* |
| bq-expand | 0.095 | 0.097 | 0.36 | 80 | 1.21x |
| *bq-expand-aa-distant* | *0.095* | *0.097* | *0.22* | *80* | *1.21x* |
| *list-aa-distant* | *0.998* | *1.008* | *0.74* | *46* | *19.87x* |
| list (baseline) | 1.000 | 1.000 | 0.79 | 46 | 19.87x |
| *list-aa-adjacent* | *1.005* | *1.014* | *0.73* | *46* | *19.87x* |

**Controls:** The largest A/A pair is `list-aa-adjacent` at 1.0047, worst cell 1.45% on `compose-scalar`, and 6 of 6 intervals cover 1. The `sum-only` halves agree at 1.0012 on a worst cell of 0.50% on `compose-slice-bcast`, its interval covering 1. The in-situ term reads 1.0106, 1.0135 of `sum-only` as medians, on `mut-odo-vecdims`, `bq-expand`. Raw, that pair reads 1.0046, which the correction amplifies by 1.04x --- quote both wherever that is past 1.5.

**Provenance:** elapsed 0h12m11s, peak 131 MiB in use, 33 MiB max residency; the reader reads 35 benchmarks over 4 shapes of the compose class. Anchor: `compose-zero-mid`, `list` at 27.9 ms per call raw, 26.9 ms net.

**Per shape, in the lead's order (compose-rev-bcast, compose-slice-bcast, compose-zero-mid, compose-scalar):** `mut-odo-vecdims` 0.032/0.032/0.024/0.021

**Across the halves:** 10 of the 31 arms are faster on this half and 21 slower, at a geomean of 1.0059, from `libunord-stage1` at 0.9832 to `mut-odo-vecdims-aa` at 1.0358, with `list` itself at 1.0040.

**What the class says:** properties 1, 2 and 3 hold --- `worst` 0.032, the lowest of the ten, with `scaled`'s 0.033 and `block`'s 0.033 behind it, and tiers at 1.00x, 1.21x and 19.87x --- and `mut-odo-vecdims-add-in-leaf-u2-ptr` leads everything at 0.017, the family's own ceiling ahead of `libunord-stage2-sum`'s 0.018 by a thousandth. **Registration (6)'s tie holds here**, `libunord-stage5` against `libunord-stage3` at 1.0024 and 1.0014, inside both floors. **Registration (7) is killed**, `libunord-stage4` sitting **0.9474 and 0.9588** ahead of `libunord-stage5` past floors of 0.47% and 0.44% --- the second of the two classes where the lean trick costs rather than saves. And registration (9)'s tie misses in the arm's favour, `libunord-stage5-sum` reading 0.9342 and 0.9251 of `libunord-stage1-sum`. Across the halves the class reads 1.0059 with 10 of 31 arms faster on the basis, `list` at 1.0040 and inside the bar.

## Provenance

What this run's figures have to be read against, and it is a section of this file because a run replaces every word of it. What does NOT move with a run --- the delta chain that says which shape set and roster each run measured, and the list of what a run replaces outside this file --- is [README's own Provenance][prov].

**Run 27's halves differ in the compiler and in the boot libraries that come with it, and in nothing else.** One source, `Main.hs` at `bb6d113`, one shim, `align-as.py` at `b3a1aca`, set the same way on both halves, and one shim environment --- `LOOP_MAXSKIP=1 LOOP_LOOKTHROUGH=1 LOOP_DEADSPOT=1` in front of each build, under which `rewrite()` returns through `plan_dead()` before either of the first two is read, so they are on both command lines to keep the lines identical and reach the emission on neither. Both halves were built `-fspec-constr -fobject-determinism` with `-pgma` on that shim and `-fforce-recomp` into a fresh builddir, the basis through `cabal.project` and 9.12.4 on PATH and the other through `cabal.project.ghead`, which selects the in-tree stage1 of the GHC checkout and pins its plan. The compilers reported 9.12.4 and 10.1.20260803 at build time and the binaries carry `ghc-internal-9.1204.0` and `ghc-internal-10.100.0`, which is the reading that settles it, there being no such compiler on PATH to ask. **The stage1 has not moved since Run 24**, so this run's HEAD half and Runs 24, 25 and 26's are the same compiler. Both binaries bake `-A32m -I0 -T -M8G`, read back by `+RTS --info`, and carry one `@@wild` and one `@@saturate` instrument each; every process was launched under `WILDLOG=1 SATURATE=1` and recorded that line in its own log. **The shim did NOT move from Run 26's**, so a distance from Run 26 carries no shim term and the source is the only input under either recipe that changed.

**The sequence was launched once and ran to the end in one window, and one population was then rerun in a second**, 2026-09-08T02:22:28 to 2026-09-08T09:33:51 and `runs` again from 11:54:26 to 13:19:29, twenty-two processes --- two main-set ones and 20 class processes, one per class per half --- every one exiting 0 at the count its roster holds, 665 twice, 490 twice, 210 twice, 175 six times, 140 four times and 105 six times, 4970 benches in all. The control half ran first in every pair and the basis second, as on every run since Run 20. Before them the gate's four processes ran from 01:49:09 to 02:22:26 at 95 benches each, and after them the four alone-leg riders to 09:46:23; the counted work followed on a box that no longer had to be quiet, forty-two minutes over all eleven populations of both halves. **The wall-clock log carries no complaint** --- no `!!` line in it and none in any of the twenty-two counts files --- but the per-sample instrument carried two, which the head records with what each moved: one bench of `run27-g912-main` at 0.28 of a core, left as it stands, and four of `run27-ghead-runs` at 0.96, 0.93, 0.90 and 0.30, which post-run step 3's rerun replaced. Every other bench of both windows is under 0.25, and the two reran processes peak at 0.16 and 0.17. The machine is the desktop named at the head of this file, its BIOS idle settings as they have been since the reboot of 2026-08-22.

**The pair's own identity, transcribed before its note goes with it.** The two binaries are `run27-g912`, md5 `b7910a22ddbd611d8aac91c973c70053`, and `run27-ghead`, md5 `60830315410284daf30a74d0bfc2f209`, with `.text` of **20644037** and **20784959** bytes --- read from the first column of `size -A`, the second being a load address that is equal on every build here. The two do not agree and are not meant to, two compilers being this pair's variable. **No repetition reading is available and none is owed**: `Main.hs` moved from Run 26's `659d898` to `bb6d113`, so neither md5 can reproduce an earlier binary and an unequal one would name nothing. The regime was read out of the binary rather than off a command line, `diag` putting `baseOffsetsScan` at 2408930 bytes against `baseOffsetsMut`'s 2408530 on `vgg-14-c512`, a ratio of 1.000 where plain -O1 is ten times apart. Both halves' `check` output is byte-identical, which has held on every pair since Run 19; that it holds here took a second build, the laziness gate added the day before printing an allocation figure per row that four of its twelve rows parted the compilers on, fixed at `bb6d113` so the count prints only where the gate fails.

**The roster and the source moved, which is what would put a layout term in the cross-run column, and this run's fills say the layout did not.** The tracked 28-byte loops were read at the build against `run26-g912`, the previous build of this recipe, with `--delta`: **every mod-64 offset preserved**, the two groups at [0, 0, 0, 24, 0, 4] and [0, 0], and **no address surviving to the byte**, four displacements in the first group and two in the second. So ten timed arms landing and five leaving moved the code without moving where any tracked fill sits in its cache line, which is the pinning claim's only reading and the thing registration (3) was to be read beside. What registration (3) then found is that the cross-run column moved anyway, by 3 to 5% on every arm --- and the fills are why that is read as the box and not as layout, together with the gate's own machine check, which says the same thing about `list` alone. **Within the pair, `--library` reads 12.8% of the 141 library self-loops at the same offset in line and 63.8% in the same straddle state**, inside the tenth-to-a-quarter band this README gives a pair varying a compiler.

**The three main-set anchors** read **5.75 us** on `cnn-slice-c32`, **3.40 ms** on `cnn-L2-24x24-c32` and **37.1 ms** on `stretch-wide-2xM`, net of the forcing pass, on the basis half, against **5.81 us**, **3.41 ms** and **37.6 ms** on the other --- the HEAD half slower on all three, by 0.97%, 0.54% and 1.29%, which is the same direction `list`'s 0.33% geomean carries and a size the per-shape spread covers. They guard a baseline that moves for every population at once, so a run whose anchors move and whose ratios do not has moved its box: **Run 26's three read 5.96 us, 3.47 ms and 37.6 ms on its basis half**, so all three are lower here, by 3.41%, 2.18% and 1.29%, against the 3.66% the gate's machine check reads on `list`'s net over all nineteen shapes --- the same direction on each and the same order of size, which is what a box move looks like from three anchors.

| shape | `l` | `list`, per call | net | HEAD, net |
|---|---:|---:|---:|---:|
| `cnn-slice-c32` | 288 | 5.92 us | 5.75 us | 5.81 us |
| `cnn-L2-24x24-c32` | 165888 | 3.50 ms | 3.40 ms | 3.41 ms |
| `stretch-wide-2xM` | 1800000 | 38.2 ms | 37.1 ms | 37.6 ms |

**Each stride class carries an anchor of its own, beside its table, and all ten are `list` on one of that class's shapes, raw and net.** The main set's three guard a baseline that moves for every population at once; a class anchor guards one that could move for that mechanism alone, which is the case a table of ratios hides completely. The `runs` anchor is `runs-2` at 37.9 ms raw and 36.9 ms net, against Run 26's 38.7 ms and 37.6 ms on a class whose population has not moved for four runs --- 1.8% lower, which is the box moving under the run and not the class, and a second window besides --- and its own floor is 3.15% here, the widest of the ten. No class population moved this run, so every class anchor is on the shape it was on. Read a class anchor against the same class's anchor in an earlier run and against nothing else, and against that class's own floor rather than the main set's.

**The correction is invertible, so pre-correction figures stay comparable.** The `sum-only` term subtracted from every cell is published per shape, and the two `sum-only` halves agree at **1.0001** on the basis and **0.9998** on the HEAD half, so the quantity taken out of the two columns is the same quantity. The in-situ term, an arm minus its `-nosum` twin against the `sum-only` the correction actually subtracts, reads **1.0355** and **1.0756** on the basis and **1.0286** and **1.0754** on HEAD for the `mut-odo-vecdims` and `bq-expand` pairs: the proxy runs a few percent under the term it stands for, by the same few percent on both compilers, which is where it has run since Run 17. What the correction cannot do is rescue a cell it is larger than, and this run has seventy such cells on the basis and forty-three on HEAD, every one a `libunord` arm.

[dead]: ../README.md#dead-ideas
[floor]: ../README.md#what-moves-a-figure-when-no-strategy-changed
[open]: ../README.md#what-is-open
[pershape]: ../README.md#per-shape-where-the-geomean-hides-the-ordering
[procedure]: ../README.md#making-a-major-benchmark-run
[prov]: ../README.md#provenance


## What this run was built to answer, and what it answered

Registered in README's open list on the date the entry carries, before the run, and moved here whole at post-run step 5; the verdicts are the write-up's to add beside each prediction, and the summary sentence its to write.

Registered 2026-09-06, before the run, on Run 26's pair --- ghc-9.12.4 as the basis against the same in-tree HEAD stage1, `10.1.20260803`, both under `LOOP_DEADSPOT=1`, the same recipe and the same shim --- over Run 26's roster less the four arms it lifted out of parking and plus the ten arms of 2026-09-07 --- the lazy candidates and the reducing consumers, items (6) to (9), the hoisted-bound fill, item (13), and the fill not unrolled under the lean dispatch, item (14) --- less lib-stage2-disp, retired that evening with item (11) --- 35 timed arms over 19 main-set shapes, 665 benches, and the 52 class views of ten classes unmoved. Two changes of code: the GHC #27778 workaround, `:: Ptr Double` on every bang-bound `plusPtr` result in the three pointer arms (the answered entry above), which leaves the three arms' STG on 9.12.4 byte-identical and takes every `Ptr` allocation out of the HEAD build's, both read off STG dumps of the two builds before this run; and the ten arms, each landing beside its control and touching no arm timed before it, with one arm retired beside them. Each item names its populations and carries a prediction and a kill condition. (1) *The pointer fills on the second codegen.* With the workaround, HEAD reads the two pointer fills as the basis does. On the main set, on both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u1-ptr mut-odo-vecdims-add-in-leaf-u1 0.97 within 2%`, where Run 26's basis read 0.9693 and its HEAD half 1.3084; `predict: pair mut-odo-vecdims-add-in-leaf-u2-ptr mut-odo-vecdims-add-in-leaf-u1-ptr 0.94 within 2%`, where Run 26 read 0.9431 and 1.9710; and `predict: pair mut-odo-vecdims-add-in-leaf-u2-ptr mut-odo-vecdims-add-in-leaf-u2 0.95 within 3%`, where Run 26 read 0.9479 paired at 13 of 19 and 2.6731, the wider tolerance being that pair's own, its win count not having separated on the basis. And both fills allocate 1.00x the result on both halves, read off the allocation column, where Run 26's HEAD half read 1.41x and 2.61x. Killed by either pointer fill above 1.00x on either half, which would say the annotation did not reach the build timed, or by any of the three pairs reversing direction on HEAD past the main set's floor, which would say the box was not all HEAD lost on them. (2) *The compiler on the pointer fills, in counts.* The two arms whose counts parted the compilers on Run 26, 0.6219 and 0.9295 basis over HEAD, read as the rest of the roster does. On the main set: `predict: counts mut-odo-vecdims-add-in-leaf-u2-ptr 1.0 within 3%` and `predict: counts mut-odo-vecdims-add-in-leaf-u1-ptr 1.0 within 3%`, the tolerance being the kill's so that the reader's verdict is the item's; the size predicted is the band twenty-four of Run 26's twenty-six arms sat in, 0.9936 to 1.0089. Killed by either outside 3%, which would say HEAD still emits a different loop for the `Ptr` form once the box is gone. (3) *The basis half against Run 26's.* The annotation changing no 9.12 code, and five arms leaving and ten landing moving slots alone --- an assumption, the ten bringing new code where a `Force` twin brings none, so the loop-offsets read of pre-run step 2, `./loop-offsets.py --delta` against Run 26's basis binary, is owed before the run and this item is read beside what it prints --- every one of the 25 timed arms Run 26 also timed (`./roster-delta.py run26-g912 run27-g912` counts them) reads within 1% of its Run 26 basis cell at the geomean over the nineteen shared shapes, the pointer fills included, `--pin` to Run 26's column, the arms of 2026-09-07 having no cell there and being outside this item; a per-shape excursion past 1% on a slot that moved is what Run 26's item (2) found on every untouched arm and is not this item's kill. Killed by any arm's geomean past 1%, which would be a layout term the STG comparison cannot see, or by either pointer fill's basis cell past the floor on any shape, which would say the annotation is not a no-op on 9.12 after all. This item is adjudicated by hand, on the main set. (4) *The rate at which an instruction saving reaches the clock, read per run length and on the second codegen.* Read per shape from Run 26's own artifacts, the rate is a profile of run length and not one number, and the profile is the pair's and the shape's rather than the compiler's or the run's ([the rate entry][open] carries the reading). Prediction, over `runs` on both halves, the workaround having given HEAD the basis's code: `-u1-ptr` over `-u1` at or below 0.91 in time on every view from `runs-256` up and at or above 0.96 on `runs-2` to `runs-7`; `-u2-ptr` over `-u1-ptr` within 3% of 1.00 on every view from `runs-256` up; and each of the three pointer spans on the basis within 2 points of its Run 26 cell on every view from `runs-256` up. Killed by HEAD's `-u1-ptr` over `-u1` above 0.94 on any view from `runs-256` up, which would say the reload's cost was the codegen's and not the loop's, or by `-u2-ptr` ahead of `-u1-ptr` past the class's floor on both halves on any view from `runs-256` up, which would say the long-run fill is not at a bound instructions cannot move. `runs-2`, where `-u2-ptr` executes exactly `-u1-ptr`'s instructions and takes 1.087 of its time on the basis, is read beside these for whether that term survives the roster change, the counts being unable to carry it. The quantity is a ratio of two ratios the reader does not compute, so this item carries no span and is read by hand from `--pair --per-shape` on the `runs` JSONs and the corrected counts of the `runs` sweeps. (5) *The class-floor asymmetry, and whether it is back.* Registered 2026-09-07 and costing this run nothing to answer: the asymmetry [the class-floor entry][open] is named for held on Runs 15 to 18 and has not reproduced on Runs 24 to 26 or on the probes since. This run's eleven populations are eleven more of the same comparison, the basis half's class floor against the other half's, each a `--aa` floor line on a process this run writes anyway. Prediction, adjudicated by hand: the basis is the wider half in **3 to 8 of the eleven**, continuing the three-run absence. Killed by 9 or more, or by 2 or fewer --- the band and the kill are complementary on purpose, so that no count falls outside both, and the two tails together are the two-sided 7% sign test on eleven, this file's own convention. Either tail says the asymmetry is back and its three-run absence the anomaly. Two readings go beside it on the same 22 processes: a process's floor against the median A/A half-width beneath it, which read +0.26, +0.49 and +0.52 by run and which [the floor section][floor]'s order-statistic ruling expects to stay positive, killed by a negative correlation on this run; and its position in the evening against its floor, which read -0.06, -0.04 and +0.16 and is a null this run is not expected to disturb. **What this item cannot settle is the within-population decay**, since a major run's two processes of a population ARE its two halves, so position and compiler move together there --- four processes of one population with the binaries alternating is what saw one, and that is [the class-floor entry][open]'s probe rather than this run's. (6) *The lazy unordered candidates against their ceiling and their port.* Registered 2026-09-07 with the arms. `libunord-stage4` and `libunord-stage5` are the unordered list kept lazy up to the exception and read in address order, a single slice or fill handed back as the ports hand theirs and the runs under the ports' own `VS.concat` ([the stride classes](../README.md#the-stride-classes-and-what-they-cover)), so a pair with a port prices the list alone where both list. Every main-set view is a dense array with its innermost two axes transposed, one block to the unordered test, so there every unordered arm returns one slice and its cells are the forcing pass; the readings are the classes', on both halves, by hand. Against the ceiling, `libunord-stage5` is the same code as `libunord-stage3` wherever the sorted view is one block or has no run --- `rev`, the dense `flip` views, `bcast`, `bcastmid`, `scaled`, `compose` and the one-block `small` views --- and a tie there, `predict: pair libunord-stage5 libunord-stage3 1.0` on those populations, read raw where both slice and the cells are the forcing pass, as Run 26 read its item (3); the same span read on a population with run views, `runs`, `block`, `window`, `flip` and `small`, the last two holding their gap and run views in one JSON with their one-block ones, prints a figure and no verdict there, the reading being per view; where the sorted view has runs --- `runs`, `block`, `window`, the two gap views of `flip`, `small-row96` and `small-patch-r5` --- it is the slice list against the fill, behind past the floor at short runs and ahead at long, the crossover on `runs` read per view and expected between `runs-256` and `runs-4096`, where `dispRun` was cut. Against the port, `libunord-stage5` ahead of `libunord-stage2` past the floor wherever stage two lists --- `runs`, `block`, `flip-outer-gap64`, `window-64x64-k1x9` and `small-row96`, where its list is a base-offset table built whole and this one is produced on demand --- and on `flip-inner-gap64`, whose reversed rows stage two fills backwards and this one slices forward, allocating less than it on all of them, read off the allocation column; the tie wherever both slice; ahead by the copy stage two's port pays for concatenating its one fill wherever both fill, `bcast`, `bcastmid`, `scaled`, `compose` and `small-bcast32`, where stage two allocates 2.00x the result and this arm 1.00x; and read per view with no prediction on the other `window` views and `small-patch-r5`, where stage two fills and this one lists runs of the kernel's width. Killed by `libunord-stage5` ahead of `libunord-stage3` past the floor on both halves on `runs-2` to `runs-9`, which would say the fill is not the ceiling of a slice list at short runs; by `libunord-stage5` behind it past the floor on both halves at `runs-65536`, which would say one memcpy per run never wins; or by `libunord-stage5` behind `libunord-stage2` past the floor on both halves on `runs` or `block`, which would say the on-demand list costs more than the table. (7) *The lean trick on the unordered list.* `libunord-stage4` against `libunord-stage5`, on every population and both halves: the same list wherever the sorted pairs merge nothing, an A/A pair inside the floor there, `predict: pair libunord-stage4 libunord-stage5 1.0` on `runs` and `block`, where both list the same runs --- not on the main set, where both return one slice and the cells are the forcing pass; stage five ahead past the floor on `small`, where the `getStridesT` it does not build is a visible share of the call. No timed view has sorted pairs that merge into a longer run, the two dispatches reading the same route and the same run length on every one of the sixty-three checked views, so the pair is the dispatch alone. Killed by `libunord-stage4` ahead of `libunord-stage5` past the floor on both halves on any population, which would say the second canonicalization costs more than the stride list it replaces. (8) *The lazy ordered candidates.* `liblist-stage3` and `liblist-stage4` are `toVectorListT` kept lazy up to the exception, canonicalized, one slice per run from the odometer list and no table, then the one concatenation the two ports carry, under the natural-strides dispatch and the lean one. On every population, both halves. Against the port of the branch: `liblist-stage4` against `liblist-stage2`, the same lean dispatch, is the same fill wherever no canonical run exists, `predict: pair liblist-stage4 liblist-stage2 1.0` on the main set and inside the floor on `bcast`, `bcastmid`, `scaled`, `rev`, `compose` and every `window` view but `window-64x64-k1x9`; where runs exist it is the odometer list against the strict base-offset table under the same concatenation, at or ahead on `runs`, `block`, `flip-outer-gap64`, `small-row96` and `window-64x64-k1x9`, read per view by hand. Against master's port: `liblist-stage3` at or ahead of `liblist-stage1` everywhere --- `predict: pair liblist-stage3 liblist-stage1 0.96 within 3%` on the main set, where both fill and the dispatch is the difference, as `liblist-stage2` read 0.9656 and 0.9560 against it on Run 26 under the lean dispatch, stage three paying a strides comparison over that --- and ahead past the floor on the two views canonicalization moves, the exception's case: `runs-r3-48x30`, whose three canonical levels merge into runs of 1440 where the shipped route reads runs of 30, and `small-flat64`, one slice where it reads 64 runs. And `liblist-stage3` against `liblist-stage4`, the natural-strides comparison alone, `predict: pair liblist-stage3 liblist-stage4 1.0` on the main set, inside the floor everywhere but `small`. The three spans are read on every population the loop hands them; a class's verdict counts only where this item names that class, and elsewhere the figure printed is read against the per-view predictions above. Killed by `liblist-stage4` behind `liblist-stage2` past the floor on both halves on any `runs` view, which would say the odometer costs more than the table it replaces; by `liblist-stage3` behind `liblist-stage1` past the floor on both halves on any population, which would say canonicalization or the odometer costs more than the slice recursion; or by `liblist-stage4` off `liblist-stage3` past the floor on both halves on `small`, either way, which would say the strides comparison is priced there. (9) *The reducing consumers, the ruling's own measurement.* `libunord-stage1-sum`, `libunord-stage2-sum`, `libunord-stage4-sum` and `libunord-stage5-sum` are `sumT` over each stage's list, one slice at a time and no concatenation, the first reading of the entry point as it is used. On every population, both halves, the loop's span figures first and a per-view reading where a figure falls outside the floor and on the views named with no prediction; on the main set all four sum one slice and their cells are the forcing pass, so the readings are the classes'. `libunord-stage5-sum` against `libunord-stage1-sum`, master's consumer: the tie inside the floor wherever both sum one slice or both sum a fill, `predict: pair libunord-stage5-sum libunord-stage1-sum 1.0` on `bcast`, `bcastmid`, `scaled` and `compose`, and per view on `small-bcast32` and `small-patch-k5`; the tie or a small lead on `runs`, `block`, `flip-outer-gap64` and `small-row96`, where both fold the same slices, master's by its recursion and stage five's by the odometer; ahead past the floor wherever master's raw-stride test falls to a fill that stage five reads as one slice or as long runs --- `rev`, the dense `flip` views and `small-flat64` for the slice, `flip-inner-gap64` and `runs-r3-48x30` for the runs; and read per view with no prediction where stage five lists runs of the kernel's width against master's fill, `window` and `small-patch-r5`. `libunord-stage1-sum` ahead of `libunord-stage1` past the floor on every view that is not one block to stage one's raw-stride test, the copy saved being what every Fill arm over a list carries. And `libunord-stage5-sum` ahead of `libunord-stage2-sum` past the floor wherever stage two lists, `runs`, `block`, `flip-outer-gap64`, `window-64x64-k1x9` and `small-row96`, and on `flip-inner-gap64`, the table and the backward fill against the on-demand forward list with no concatenation to hide it. Killed by `libunord-stage5-sum` behind `libunord-stage1-sum` past the floor on both halves on `runs` or `block`, which would say the odometer list costs a fold more than the slice recursion; or by `libunord-stage1-sum` at or behind `libunord-stage1` past the floor on both halves on any view that is not one block, which would say the concatenation costs the consumer nothing. The laziness gate in `check` is this run's fourth instrument on the same question and needs no run: it is read at pre-run steps 4 and 5, which are what run `check`, once per build. (10) *The baseline between the halves.* Registered 2026-09-07, with (11) and (12). `list` moved 1.10% between Run 25's halves and 1.16% between Run 26's, past the 0.7% bar that lets two columns be subtracted, in time and not in instructions, and no code change of this pair touches it, the roster change moving the layout Run 26's head blamed. On the main set: `predict: cross list 1.011 within 0.5%`, the basis over HEAD as Run 26 read it. Killed by the figure inside the 0.7% bar, which would say the movement was those two rosters' and not the pair's, and which reopens the subtraction the last two runs refused. (11) *The dispatch pair after its answer --- WITHDRAWN 2026-09-07 with the arm.* lib-stage2-disp was ruled out for the library and parked that evening, code complexity at the threshold and a hard-coded L1-sized constant tipping it ([dead ideas][dead]), so no process times it and the span below is unreadable; the item stays for what it registered. The counts pair of 2026-09-07 put lib-stage2-disp at 1.0188 and 1.0180 of `lib-stage2-lean`'s corrected instructions on `small` and 1.0142 and 1.0148 in time, on this run's recipe as it stood before the eight arms ([the disp/lean entry][open]). The pair differs in the `dispRun` comparison and has no twin among the eight arms, item (8)'s `liblist-stage3` against `liblist-stage4` being the natural-strides comparison under the list, which `lib-stage2-lean`'s parked control prices under the fill. On `small`, both halves, the span printing a figure and no verdict on any other population, `runs` most of all, where the two are different code by design: predict pair lib-stage2-disp against lib-stage2-lean 1.015 within 1%, the span unbackticked since the withdrawal. Killed by the pair inside the class's floor on both halves, which would say the counts pair's 1.9% does not reach the clock on a major run. `cnn-L1-6x6-c1`, where Run 26's halves parted on this pair, 1.013 against 1.139, is read beside it on the main set by hand, for whether the HEAD half's figure survives the roster change. (12) *The class-level margin on the four reversed classes.* The order-reversal probe of 2026-09-07 read the basis faster than HEAD in 15 of 16 class-by-order readings and most of the margin as the compiler, the four classes' basis-over-control geomeans moving from 0.8976 to 0.9687 on Run 26's pair to 0.9538 to 1.0103 on this run's recipe as it stood before the eight arms ([the class-floor entry][open]). On `rev`, `bcast`, `scaled` and `bcastmid`, the two halves being the comparison, read off `--cross-classes` by hand: each class's basis-over-HEAD geomean inside the probe's band, 0.95 to 1.01. Killed by any of the four below 0.95, which would say the workaround did not take the compiler's share out of the class level, or by all four above 1 past their floors, which would put the faster half on the other side. It overlaps item (1), the pointer fills being what moved the class geomeans, and reads on the classes what (1) reads on the main set. (13) *The look-ahead hoisted out of the shipped fill's guard.* `mut-odo-vecdims-add-in-leaf-u2-last`, added 2026-09-07, is `-u2` with the bound held as the run's last index, so the loop compares the cursor against it directly where `-u2` computes `o + 1` per pair; the live set is unchanged. Counted the same day on a 9.12.4 build, N=50 and not a column, it executes 0.9688 of `-u2`'s instructions on `runs-65536` and `stretch-tall-Mx2`, half an instruction an element, and is level with it on the three-wide cnn runs, where the odd tail gives the pair's saving back; `-u2-down` is level with `-u2` on those two and under it by about a percent on the short runs. At the leaf family's rate of a third to a half, that is a percent or less of time on the main set and up to a point and a half on the long runs. On the main set, on both halves: `predict: pair mut-odo-vecdims-add-in-leaf-u2-last mut-odo-vecdims-add-in-leaf-u2 0.99 within 1.5%`. Killed above 1.005 on either half, which would say the hoist cost the run level a spill the counts did not show, or below 0.975, which would be more time than the instructions can buy. Read beside it, by hand and not as a span: on `runs` from 256 up, expected about 0.985, and on the three-wide cnn shapes a tie. (14) *The unroll under the library's dispatch.* `lib-stage2-lean-u1` is `lib-stage2-lean` with the stepping run not unrolled, `-u1`'s loop for `-u2`'s in the driver and nothing else changed. The leaf family reads the unrolling at 0.9644 in time and 0.9208 in counts, `-u2` over `-u1` on Run 26's main set, and under the dispatch that shipped it should read the same. On the main set, both halves: `predict: pair lib-stage2-lean-u1 lib-stage2-lean 1.037 within 2%`. Killed by the pair inside the floor on both halves, which would say the unrolling buys nothing once the driver's fused level sits around the run, or above 1.06, which would say that level costs the u1 loop more than the arm's odometer does.

**Seven held, six were killed and one was withdrawn with its arm before the run --- and the headline is the first of the seven: the GHC #27778 workaround did everything it was registered to do, in time, in counts and in allocation at once.** Every figure below is re-derived from this run's own JSONs and counts files rather than carried from the registration; a margin is judged against the population's own floor on the half it is read on, 0.83% and 0.63% on the main set and each class's own figure in the summary table above. **One thing the registration could not foresee governs three items**: the gate's machine check fired, the box reading 3.66% faster on `list`'s net than the fingerprint Run 26 kept, so no absolute crosses from Run 26 unadjusted and item (3), whose whole subject is a cross-run column, is read through `--bridge` and killed either way.

(1) *The pointer fills on the second codegen.* **HELD, in every clause and on both halves, and it is the run's result.** On the main set the three spans read **0.9713, 0.9357 and 0.9419** on the basis and **0.9751, 0.9239 and 0.9386** on HEAD, each inside its registered tolerance, where Run 26's HEAD half read 1.3084, 1.9710 and 2.6731. Both pointer fills allocate **1.00x** the result on both halves, against Run 26's 1.41x and 2.61x on HEAD, so the first kill --- either fill above 1.00x on either half --- does not fire; and no span reverses direction on HEAD, so the second does not either. The annotation reached the build that was timed.

(2) *The compiler on the pointer fills, in counts.* **HELD.** `-u2-ptr` reads **1.0054** and `-u1-ptr` **1.0051** basis over HEAD on the main set, and 0.9946 and 0.9949 read the other way round, all inside the registered 3% and inside the 0.9936-to-1.0089 band the rest of the roster sits in. Run 26 read these two arms at 0.6219 and 0.9295. HEAD emits the same loop for the `Ptr` form now.

(3) *The basis half against Run 26's.* **KILLED, and the box is what killed it.** Raw, the eighteen shared arms outside the `libunord` family --- `list` among them --- read between 0.9% and 4.6% faster than their Run 26 basis cells: `mut-odo-vecdims` 0.9692, `list` itself 0.9699, `liblist-stage2` the narrowest at 0.9912 and `-u2-ptr` the widest at 0.9544 --- which is the gate's -3.66% and not a layout term, so the item cannot be adjudicated on its own words. The three `libunord` arms sit apart at 0.7810, 0.9084 and 1.1645, one of them the wrong way round, their cells being their own forcing term on most shapes. On `--bridge`, each arm as a ratio to `list` in its own run, which a box change cannot move, the twenty read a geomean of **0.9940** with **17 inside the 3.3% drift band and 9 inside the registered 1%**. So the item fails even with the box divided out: the ten arms landing did not move slots alone. The three outside the band are `libunord-stage1`, `-stage2` and `-stage3` at 1.2001, 0.9389 and 0.8073, whose cells on most main-set shapes are their own forcing term; every arm outside the family is inside 2.2%.

(4) *The rate at which an instruction saving reaches the clock.* **HELD: neither kill fires, and on the reran `runs` its first clause now holds in full.** On `runs` from 256 up, HEAD's `-u1-ptr` over `-u1` reads 0.8904, 0.8887, 0.8885, 0.8788, 0.8880 and 0.8786, every one below the 0.94 that would have killed it; and `-u2-ptr` is nowhere ahead of `-u1-ptr` past the class's floor on either half, so the second kill cannot fire either. **The basis reads `-u1-ptr` over `-u1` at or below 0.91 on all six views** --- 0.8694, 0.8816, 0.8801, 0.8832, 0.7991 and 0.8805 --- where the intruded first pair missed at `runs-65536`; the 0.7991 at `runs-16384` sits eight points below its neighbours and is the one cell of the six worth distrusting. **Where the rerun moved the reading the other way is the second clause**: `-u2-ptr` over `-u1-ptr` within 3% of 1.00 from 256 up now misses at `runs-256` (1.0497) and `runs-65536` (1.0390) on the basis and at `runs-65536` (1.0418) on HEAD, where the first pair held on all six of the basis's. Both readings are of clean processes; what separates them is an evening, and the moves are of the size this class's own 3.15% floor allows, which is the honest reading of a class whose floor is the widest of the ten. The cross-run clause, within 2 points of the Run 26 cell, misses at `runs-256` (86.94 against 89.77) and `runs-16384` (79.91 against 89.87) for the first span and holds elsewhere. **`runs-2` survives everything**: `-u2-ptr` executes exactly `-u1-ptr`'s instructions there and takes **1.0834** of its time on the basis and 1.0868 on HEAD, against Run 26's 1.0872 and the first pair's 1.0900. Within the basis the rate reads **27%, 35% and 46%** on the three spans on the main set --- 0.8944, 0.8358 and 0.8605 in corrected instructions against 0.9713, 0.9419 and 0.9357 in time --- where Run 26 read 29%, 32% and 41%.

(5) *The class-floor asymmetry, and whether it is back.* **HELD in all three of its readings, and the three-run absence is now four.** The basis is the wider half in **6 of the eleven** populations, inside the registered 3 to 8 and clear of both tails: wider on the main set, `rev`, `bcastmid`, `runs`, `small` and `compose`, narrower on `bcast`, `window`, `scaled`, `flip` and `block`. The floor against the median A/A half-width beneath it stays positive at **r = +0.01** over the twenty-two processes, so the kill --- a negative correlation --- does not fire, but at that size the order-statistic expectation is no longer supported either, against +0.26, +0.49 and +0.52 on the three runs before. Position in the evening against the floor reads **r = -0.08**, against -0.06, -0.04 and +0.16 --- still a null, but a contaminated one: the `runs` pair was re-measured in a second window, so its position is not this evening's, and dropping those two processes takes the coefficient to **-0.25** over the remaining twenty. The second window flattened it toward zero rather than inflating it, so the null is the weaker claim here and not the safer one.

(6) *The lazy unordered candidates against their ceiling and their port.* **KILLED by its third condition, on two populations and both halves --- and the ceiling half of it held exactly.** `libunord-stage5` sits behind `libunord-stage2` at **1.0548 and 1.0559** on `runs` and **1.1290 and 1.1174** on `block`, every one past the floor on its own half, which is the item's own words for the on-demand list costing more than the table it replaces. Against the ceiling it does what it was registered to do: on `runs` it is 9 to 45 times behind `libunord-stage3` at `runs-2` to `runs-9` and **0.9017** ahead at `runs-65536`, so neither of the other two kills fires, and **the crossover lands between `runs-1024` at 1.0743 and `runs-4096` at 0.9385** --- inside the `runs-256`-to-`runs-4096` window the item predicted from where `dispRun` was cut. The tie clause holds on `bcast`, `bcastmid` and `compose` and fails on `rev`, 1.0549 and 1.0611 against floors of 0.81% and 0.14%.

(7) *The lean trick on the unordered list.* **KILLED, on `small` and on `compose`.** `libunord-stage4` is ahead of `libunord-stage5` past the floor on both halves on both: **0.9816 and 0.9644** on `small`, which is the population where stage five was predicted ahead, and **0.9474 and 0.9588** on `compose`. So the second canonicalization costs more than the stride list it replaces, which is exactly what the kill was written to catch. The A/A clause holds on `runs`, 1.0000 and 0.9976 inside floors of 3.15% and 2.73%, and fails on `block`, where the pair is outside the floor on both halves and parts in sign between them, 1.0105 against 0.9927.

(8) *The lazy ordered candidates.* **KILLED on all three of its kill conditions, which no earlier item has managed.** `liblist-stage4` sits behind `liblist-stage2` on `runs` at **1.0563 and 1.0540** against floors of 3.15% and 2.73%, so the odometer costs more than the table. `liblist-stage3` sits behind `liblist-stage1` on `window` at **1.2419 and 1.1953** against 0.97% and 2.67%, so canonicalization and the odometer cost more there than the slice recursion. And on `small` the two candidates part by **1.1377 and 1.1388** against 0.58% and 0.44%, so the strides comparison is priced. The one span that held on both halves is `liblist-stage4` against `liblist-stage2` on the main set, 0.9928 and 1.0050, where no canonical run exists and the item predicted the same fill.

(9) *The reducing consumers, the ruling's own measurement.* **HELD: neither kill fires, and the consumer's own clause holds by margins nothing else here reaches.** `libunord-stage1-sum` is ahead of `libunord-stage1` past the floor in **all ten classes on both halves**, from **0.0854** on HEAD's `runs` to 0.7871 on HEAD's `small`, so the concatenation the consumer skips is between a fifth and nine tenths of the call --- the copy every Fill arm over a list carries. `libunord-stage5-sum` is far ahead of `libunord-stage1-sum` on `runs` and `block`, 0.6051 and 0.6137 on the reran `runs` --- readable on both halves now, where the intruded pair left the basis's degenerate --- and 0.6164 and 0.6396 on `block`, the opposite of the kill, and reads **0.0087 and 0.0201** on `rev`, where master's raw-stride test falls to a fill and stage five reads one slice. What the item got wrong is the ties: stage five's consumer is ahead rather than level on `bcastmid` (0.6806), `compose` (0.9342) and `scaled` (0.9631), and behind by about 2% on `bcast`.

(10) *The baseline between the halves.* **KILLED, and the subtraction is reopened.** `list` reads **0.9967** basis over HEAD on the main set, a 0.33% move, inside the 0.7% bar where 1.011 within 0.5% was registered --- so what Runs 25 and 26 read at 1.10% and 1.16% was those two rosters' and not this pair's. The counted work moves with it, **0.9965**, at a time-over-counts of 1.0002, so the movement this run is instructions and nothing else. This run's two columns may be differenced, which no run since Run 24 could say.

(11) *The dispatch pair after its answer.* **WITHDRAWN 2026-09-07 with the arm**, before the run: `lib-stage2-disp` was parked at 2080298 and no process times it, so the span is unreadable and nothing here adjudicates it. What the item registered stands as the record of what the counts pair of 2026-09-07 had put at 1.0188 and 1.0180 in corrected instructions on `small`.

(12) *The class-level margin on the four reversed classes.* **HELD, with a caveat the item did not anticipate.** The four basis-over-HEAD class geomeans read **`rev` 0.9627, `bcast` 0.9930, `bcastmid` 1.0065 and `scaled` 0.9977**, every one inside the probe's 0.95-to-1.01 band; none is below 0.95 and only one is above 1, so neither kill fires. The caveat is that `list` moved past the 0.7% bar on `bcast` (0.9823) and on `scaled` (0.9795), so the reader refuses those two class lines for the pair's variable --- two of the item's four populations are read on a line the reader says is not the pair's, and only `rev` and `bcastmid` carry the verdict unqualified.

(13) *The look-ahead hoisted out of the shipped fill's guard.* **KILLED on both halves, and the counts say why it is worth recording rather than repairing.** `-u2-last` over `-u2` reads **1.0138 and 1.0157**, both past the 1.005 that kills it, where 0.99 within 1.5% was registered. The instruction saving is real and reproduces the pre-run probe: **0.9691** in corrected instructions on the basis against the 0.9688 that probe read at N=50. So the hoist removes 3.1% of the instructions and costs 1.4% of the time, which is the only one of the five spans this run prices whose rate is negative --- on `runs` from 256 up, which is the population the item registers, it reads 1.0015 and 0.9937 against the 0.985 expected --- the HEAD figure inside the item's own 1.5% and the basis one just outside it.

(14) *The unroll under the library's dispatch.* **HELD on both halves.** `lib-stage2-lean-u1` over `lib-stage2-lean` reads **1.0516 and 1.0414**, inside 2% of the registered 1.037 and below the 1.06 that would have killed it, and far outside both floors, so the other kill --- the pair inside the floor --- does not fire either. The counted ratio is **1.0878**, so not unrolling the stepping run costs 8.8% of the instructions and 5.2% of the time: a rate of **59%**, the highest of the five spans this run prices and half again the leaf family's.
