
2026-09-16, micro-regime3, the day after Run 33. The review of Run 33's
straddlers and of LOOP_EXITSPAN found the switch doing what it claims --
exit spans astride 65 and 72 on Run 32's halves, 0 and 0 on Run 33's, read
off the binaries by a reader mirroring the shim's definition -- and found
the open list's 29.5% "compiler" figure on lib-stage2-lean-u1 over runs to
be the basis FILE's: each half against Run 32's same half, HEAD's u1 unmoved
on all fourteen shapes and the basis's 12 to 19 percent slow from runs-7 up,
same bytes, same counts, then a byte-identical copy of run33-exit running the
cell at Run 32's speed while the original stayed slow, and posix_fadvise
DONTNEED bringing the original level. A probe build of today's source without
the exit span read fast too and was, for an hour, taken as proof that the
switch's placement was the cause, until the copy test said the probe was
fast for being a fresh file. The khugepaged story that followed died to the
sysfs readings (enabled madvise, READ_ONLY_THP_FOR_FS unset, pages_collapsed
0) and the large-folio story to probe-pageflags.py reading single frames.
The slow frame's address was never read: the first probe launch had no
delay and the fill page was not yet present. What went in: --half-movers in
read-run.py with its mutant, post-run step 4a, a placement paragraph, the
open entry answered, Run 33's file corrected by clauses, probe-pageflags.py
with --heap, probe-r33-instance.sh and probe-hugebin.sh.
Later the same day: HEAD's half read for the same term, --half-movers
flagging mut-odo-vecdims-add-in-leaf-u1 on compose and scaled, the copy
test confirming 11 percent an iteration, and the frame read while it
lived, 0x401194040. The heap's frames share its low bits at the random
rate, the front end reads equal, resyncs are hundreds on both, and only
the store-to-load interlocks move; the structure stays open. The tmpfs
copy came in 128 KiB compound pages with physical L2 sets equal to the
virtual ones, which is item 34's determinism from a mount that exists.
Evening: IBS over the compose cell on HEAD's slow instance and its copy,
probe-ibs.sh after three false starts (per-thread refused, a user-only
modifier refused, root refused a user's stale file in /tmp): every
instruction of the leaf's loop samples alike on both, all L1 hits, and
the miss-buffer counts for loads, stores and prefetches agree, so the
mechanism stays open with the front end, the caches, the TLB, resyncs
and prefetch all ruled out by counters.
Then tmpfs, adopted the same evening at the owner's word: hugebin/
mounted huge=always under the checkout, half-bin.sh born as the one
place a driver asks where a half launches from, five drivers and
preflight routed through it, its stub branch found by the corpus (a
stub copied to the mount, and the runner stopping for a tree that
moved) and fixed by an ELF test. A session started before the mount saw
it unsandboxed and not sandboxed; the next saw it both ways, and the
README sentence that said never was corrected. The mount's copy read
both hot lines in one 2 MiB THP, physical equal to virtual, still
PTE-mapped. The leaf-u1 copy test per population: frame on compose,
bcast and scaled, none on flip and window.

2026-09-17, run-chapter checklists, history stripped

Pre-run list:
- PREV went unset in the list until 2026-09-04, when the commands that use it moved in from the framing.
- Step 0: Run 24's preparation arrived to find 12a and 12c taken, the registration having landed with the roster commits days before the pair.
- Step 1 was an `ls $R-*` read by eye until run-status.sh's pre-run block took it over.
- Step 2: the halves launch from hugebin/ since 2026-09-16. Before `--draft`, hand-copying a previous note's `[SAME]` blocks carried a previous run's gate arms and --library figures forward twice. A blind walk arrived at step 2 with neither the compares-against section nor the previous note read. Run 27's preparation recorded `size -A`'s second field, the load address, as .text, and reasoned about why it had not moved across a roster change.
- Step 2b: until 2026-09-02 the halves were set in five scripts by hand, the shape of edit that gets four fifths done, which is what an older note's `scripts set` row records. Until 2026-09-15 pair-halves.sh cut a tag at the first character outside `[A-Za-z0-9_]`, so a declared `ghead-exit` would have reached every driver as `ghead`; `--draft` refused it first, at Run 33's step 2, after the recipes and the registration were committed, and Run 33 renamed both in a commit of their own.
- Step 2d: preflight's fill-in block reads the previous basis off the note's HALVES line since 907c218. Before `loop-offsets.py --delta` (2026-09-04) two preparations did the fills subtraction by hand and recorded it as an improvised computation.
- Step 3: the steps were renumbered; an older note's 0 is 2a, its 3b is 2, its 3c is 2b, and its 2, 3 and 3a are step 3.
- Steps 4,5: Run 25's preparation re-ran `check` by hand mid-roster-pass to confirm it was spent, and killed it at a timeout.
- Step 6: no pair varies the roster, ruled 2026-08-26 after a walk of the list found all four steps such a pair breaks.
- Step 6b: before 2026-09-04 the note's `gate arms` line was read out of run-gate.sh by eye.
- Step 6c: until 2026-09-04 the note's roster block and Provenance's delta bullet were written from a `diff` of two `--list` outputs nothing performed; Run 25's preparation got a clause of its note wrong that way and improvised the per-class view count.
- Step 8b: Run 33's preparation launched `check-all` in the background, kept editing, and had to run the whole suite again.
- Step 8c: `check-all`'s properties step was narrowed to CORPUS_RUN=newest on 2026-09-09, from a quarter of an hour to under a minute.
- Steps 8c and 8d: Run 24's preparation spent a round trip on a leg caught half written. Their line said `before 12b` until 2026-09-04, which cannot be done, 12a and 12b being taken under the sweeps.
- Step 11: Run 30's preparation found two --draft defects at step 2 and waited out the L1 pass; Run 33's made its document changes while the pass ran, and wrote and tested four reader changes on a copy inside that hour. The sweep and the pass wait for preflight's 4,5, ruled 2026-09-10: minutes against an hour, on a step that had failed once in the four runs before. A bare `&` registering nothing with the harness was measured 2026-09-04.
- Step 12: Run 29 named all ten classes and its eleven legs ran an hour. Run 23's preparation hung a call on a waiter that grepped a process list and matched its own command line.
- Step 12a: `--lint` holds a deferral target's arms since 2026-09-05.
- Step 12b: Run 33's preparation rewrote a statement in every carried block of its note. Until `--compare` read a `cross` prior on a `-sum` arm (2026-09-15), such a prior was re-derivable only by a hand-written geomean, which Run 32's preparation wrote and which left Run 33's unable to quote two of the three figures its note wanted. On 2026-09-11 a preparation ran every re-derivation call in its own context. Run 30's registration was Run 29's carried over and quoted Run 28 throughout; its `--carried` read five items against run29 and two against run28, and the two were the finding. Run 27's preparation checked the note's figures a call apiece and proved only presence, and Run 28's wrote `--figures` by hand and threw it away.
- Step 12c: Run 27's preparation committed at 12c, then rewrote its chain four times and ran `check-all` three times, for corrections one commit would have carried.

Run list:
- Step 13: its `or the whole note` branch was retired 2026-09-16, when `--note-check` began refusing a note without an [EXEC] block at pre-run 12c; both runs that met the branch took it, Run 32 reading all eight hundred lines of its note and Run 33 all 745, each preparation having left the entry point untagged.
- Step 17: a killed sequence process left a truncated JSON, 3.3 MB of an expected 24, 2026-08-30.
- Step 19a: $R-evening-out.txt held all 146 lines of Run 25's wallclock log. Chained to the evening, the counts held Run 24's box an hour past the last stage that needed it, 13:15 to 14:18, with the session asleep.
- After step 20: Run 28 armed both monitors correctly, ran to EVENING COMPLETE and left both running through the whole write-up until the person watching said `I see 2 monitors`, the heartbeat ticking against a finished run every 45 minutes.

Post-run list, steps 0 to 5c:
- Step 0: Run 25 re-ran the `--match` its preparation had spent early and reproduced all four owners and both refusals.
- Step 1: `--brief-facts` was named only at step 6b until 2026-09-11, where it serves the checker's brief, and a run wrote its whole head before meeting it.
- Step 2: Run 27 read 0.96 of a core in `--wild`'s `fgn/core` column as 0.96%, cleared it, wrote the whole run up against the intruded window, and derived every figure of one class again when 6d's checker found it (2026-09-08).
- Step 3: Run 27's `runs` rerun took 42 minutes a process at 490 benches, where the step's older figure had a session promise the box back in a third of that. Both rerun rules, the quiet window for the driver too and driving through `run-major.sh`, were bought on Run 18. A driver's own stdout redirected to $R-install.log failed the plateau gate on 2026-08-23. A rerun of 141 benches at rc=0 with no stamps in its log looked perfect.
- Step 4: `--compare` prints the reducing consumers under its table since 2026-09-15. Run 29 hand-rolled a cross-half geomean with an arm dropped over `--cells`, for want of knowing `--exclude`. `--exclude`'s line first said it drops a `family`, and was measured and corrected. Run 23 read the persisted output of the one-call reading three times over. floor-pairs.txt is a mode since 2026-09-16; Run 32 read it as sixteen spans and Run 33 by a script it threw away. The deflation decomposition stood at 10a until 2026-09-15. The cross-half A/A bar is printed by `--compare` since 2026-09-15; Run 32's head was written without it and called the pair's variable worth nothing the roster can measure, where three of eight strategies cleared it.
- Step 5: Run 32 repointed 25 link occurrences and two were wrong, found by reading the diff; Run 18 lost three to a literal rename over the wrapped document.
- Step 5a was a clause inside 5b until 2026-09-01, and `--movement` a mode only since 2026-09-16, two runs having taken it by hand and one having published `sixteen points` for a row that moved fourteen, off the table's three decimals.
- Step 5c was step 5's third act until 2026-09-05.

Post-run list, steps 6 to 11:
- Step 6: the bullet on the write-up's own commits rewrapping the working copy did not stop it biting twice more on Run 32, the hook's `restored ... (wrapped)` line going unread. Run 26 cleared the turn-end hold at the deletion offer, kept editing, and had every turn end rewrap README under it, so one edit replaced a single line of a wrapped paragraph and the wrap gate caught it; Run 26's script edits also replaced one line of a wrapped paragraph and left the rest. Run 21 put a head paragraph into the wrong section when a first `--replace` removed a second anchor's other occurrence, and recovered it only because step 5 had committed the copy.
- Step 6a: Run 20's write-up forgot to add its own columns to the two-column table. `--inherited` was named at 6d until 2026-09-13; Run 30, meeting it there, skipped it and had its checker return twelve stale carried paragraphs, and Run 27 shipped nine past both passes and two past its probe. Run 32 wrote its head first and every large fix of its cycle landed there, the largest a claim about the pair's variable made before the bar that tests it was read. Run 33 wrote three head paragraphs with HEAD as the slower half, past its own end-to-end read, caught by `--cells` on one arm on one shape, 0.000614 s against 0.000574 s. Run 25 shipped `nine of them below 1` into a sentence where six were, past three of its gates, found by its checker sorting the population. Run 23 forgot a deferred paragraph with no marker, the comparison section's first, until an end-to-end read.
- Step 6b: the step once asked for a subject naming only the step, which is what Run 27 wrote at 6d and at 7, so a done 6d reads NOT DONE in run-status.sh for good; Run 25 named its commit `step 6a`, caught at 10b. Run 33's first pass returned after its 6c and its fixes landed in 7's cycle. Run 32's hand copy of the brief's THIS RUN ONLY items carried a count the reader contradicts, which the write-up copied into the run file and the checker found. A rerun stranded four of Run 27's readings mid-block and both passes read them. The brief is one line per paragraph since 2026-09-06; its hand fill left a ragged 53-column line, and the same evening's hand fill of the step's own block a 96-column one.
- Step 6d: steps 9 and 10 moved before this commit on 2026-09-15, after Run 32 took them there by accident and had both read by the pass. Run 21's second pass returned nine findings of which seven were made by the first round of fixes. `--inherited` was named at 6d until 2026-09-13, and Run 30 missed it entirely at that position.
- Step 6e: Task 6's ratio was copied into Run 23's draft registration and does not reproduce from the JSONs the same sentence names.
- Step 9: Run 15's durable output was four checker gaps, two corrected rules and the finding that a probe needs no pair, none of it in a table.
- Step 10: registrations and their verdicts live in the run's own file since 2026-08-29.
- Step 10a: the deflation figure was hand-rolled before its mode existed.
- Step 10c was numbered 7b until 2026-09-11 and ran before 8, 9 and 10 while its text covered their output, so a session either read a tail that did not exist yet or reordered in silence. Run 27's tail carried the twelve references its retirement broke, a disclosure that reached four of the six places it had to, and three wrong claims in the evidence that retirement moved, all found after both passes, and its brief, which no pass reads, carried six more.
- After step 10c: Run 27 read `check-all | tail`'s exit 0 and ran the suite again; Run 33 lost check-all's status to a trailing echo and ran it again too. Run 28 took `--counts --pair`'s RAW column for its CORRECTED one and `--pair`'s arm name for its ratio by counting fields, and the first survived into a published rate. Run 31 built its compares-against composition arithmetic by inverting Run 29's 0.8788 and 0.7810, then re-derived them and found the reader agreeing to four places, which it need not have. `rate` and the summary's `bold` were hand arithmetic over two other modes' output until 2026-09-11, each got wrong the run it was last hand-rolled, and `--floor-pairs` and `--movement` became modes on 2026-09-16 for the same reason.


2026-09-17, micro-regime3, the evening of Run 34's day. A review of Run 34's straddlers and of LOOP_EXITSPAN read the switch doing its work on the timing side too -- HEAD's two- and four-element runs level with 9.12.4 where Run 32 had them 8 to 17 percent behind, runs-3 unmoved as the sweep said -- and then ranked every cell of the run by its time ratio over its count ratio, which no reading in the chapter had done: the arm geomeans had hidden the run's largest cells, and the largest count differences were GHC #27799's latch on lib-stage2-lean-u1, named in the open list and met on no results path. The probes that followed: compose-zero-mid's mut-odo-vecdims family is one mispredict a run on the basis, 18000 an iteration against none on HEAD; lib-stage1 on the 24x24 views is three instructions a run; the u1 cell on cnn-L1-24x24-c1 belongs to the Run 34 binaries on both halves and not to an instance, with every fillStage2U1 loop at Run 33's residue, instructions, branches, mispredicts, BTB corrects, decoder redirects, resyncs, op-cache hits and misses, icache and TLB misses all level against Runs 32 and 33, and the op-cache event 0x28F level too, asked for as the chapter's probes ask it, `r20000078f`; the one counter that moved, a quarter, was `r28F`, which perf's raw encoding reads as event 0x8F with umask 2, an event nothing here names -- a probe of this session's and not the chapter's, misattributed to it for an hour; the mechanism stays unread. The runs-16384 family's 25 percent on the basis is the evening's process: all four instances read level in fresh processes. What went in: --cell-movers with its mutant, post-run step 4b, the latch entry's pointer to it, the run file's counts paragraph read per cell, and every `sudo` out of the README at the owner's word.